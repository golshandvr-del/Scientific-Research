# پیش‌ثبتِ داوری‌های معوقهٔ بلوک S790–S799 (S791, S793, S794, S795)

**دانشمند**: ابوریحان بیرونی · **تاریخ**: 2026-08-26
**قانون کاربر**: هر شمارهٔ بلوک باید حکم رسمی موتور (`engine/rqs2.compute_rqs2` v2.6) داشته باشد.
**مسیر چندگانگی**: C — جست‌وجوی همهٔ این لایه‌ها **قبلاً و فقط روی نیمهٔ اول** انجام و در گیت ثبت شده
(کامیت‌های recon/scan هر لایه). نیمهٔ دوم برای هر چهار لایه **بکر** است. این سند پیش از هر لمسِ
نیمهٔ دوم کامیت می‌شود. برای هر لایه **دقیقاً یک** فراخوان compute_rqs2 با `split_bar=n//2` اجرا
می‌شود؛ حکم موتور هرچه بود، ثبت نهایی همان است. هیچ تعمیری پس از دیدن هولد‌اوت مجاز نیست.

**داور مشترک**: `strategies/s79x_final_adjudicator.py` — null متعارف = ۴۰×بی‌قید + K=500 جایگشت
(حفظ nL/nS، فقط کندل‌های معتبر)، seed = شمارهٔ لایه. داده: `data/mt5_full/` کامل ۱۵.۶ سال، src چاپ می‌شود.
هزینه: اسپرد 3.3pip، pip=0.10. ورود همیشه openِ کندلِ بعد از سیگنال. overlap ممنوع.

---

## S791 — Regime-Aligned ORB — XAUUSD-M15 (کارت منجمد از s791_scan_b/c، کامیت‌های 583bc1f6/460459f8)
- ORB = ۲۰ کندلِ نخستِ روز (۵ ساعت)؛ شکستِ close از باند + thrust ≥ 0.236·ATR89؛
  فقط هم‌جهت با درفتِ L=144 روزه؛ اولین شکست هر روز.
- SL = TP = 2.618·ATR89 (متقارن) · mh=192 · **n_trials=64** (شمارش صادقانهٔ scanهای a/b/c)
- پیش‌بینی صادقانه: فرسایش سال‌به‌سال در IS دیده شد؛ احتمال REJECT بالا — حکم موتور ملاک است.

## S793 — Drift-Aligned Streak Pullback — XAUUSD-H3 (از s793_recon_streak، کامیت a38a6c71)
- K=2 کندل متوالی خلاف درفتِ ۸۹کندله (رگه == دقیقاً ۲) ⇒ ورود هم‌جهت درفت.
- SL = TP = 2.618·ATR89 · mh=21 · **n_trials=55**
- پیش‌بینی صادقانه: PF~1.04 در IS؛ احتمال REJECT در H1 بالا.

## S794 — First-Bar Fade (Anti-Intraday-Momentum) — XAUUSD-H1 (از s794_recon_intraday، کامیت f4e4dd6c)
- تفکیک روز با گپ زمانی > max(1800s, 1.5·TF)؛ |بازده کندل اول| ≥ 0.09·ADR21 (علّی، شیفت‌خورده)
  ⇒ محو: کندل اول مثبت → SHORT، منفی → LONG؛ سیگنال روی خودِ کندل اول (بسته‌شده).
- SL = TP = 1.618·ATR89 · mh=13 · **n_trials=60**
- پیش‌بینی صادقانه: فرسایش پس از ۲۰۱۵ در IS؛ کم‌توان.

## S795 — Pre-EOM Short — XAUUSD-H3 (از s795_recon_tom، کامیت 3945e65a)
- آخرین روزِ معاملاتیِ ماه (offset=−1)؛ SHORT در open آن روز (سیگنال = کندل قبل).
- SL = TP = 4.236·ATR89 · mh=24 (۳ روز) · **n_trials=45**
- پیش‌بینی صادقانه: امضای بتای رژیم خرسی ۲۰۱۱–۲۰۱۵ + سند S145 (شکست WF در ۲۰۲۰–۲۰۲۶)؛
  انتظار REJECT — اما حکم را موتور صادر می‌کند، نه من.

## بند مشترک ابطال
حکم موتور (هرچه باشد) در `results/S79x_..._rqs2_<score>_<VERDICT>.md` ثبت و آن شماره بسته می‌شود.
هیچ pooling/rescue برای این چهار لایه پیش‌ثبت نمی‌شود.
