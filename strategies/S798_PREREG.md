# S798 — PREREG — London-Session Shock Continuation (LSSC) — XAUUSD H8 (کندل 08–16 UTC)

**پژوهشگر:** ابوریحان بیرونی (بلوک S790–S799) · **تاریخ:** 2026-09-06
**وضعیت:** پیش‌ثبت **قبل از** هر محاسبه روی نیمهٔ دوم. پس از کامیت تغییر نمی‌کند.

## ۱) فرضیه
ارزش اطلاعاتی کندل شوک به نشست بستگی دارد. کندل H8 ساعت 08 UTC (08–16: لندن + هم‌پوشانی نیویورک،
شامل هر دو فیکس LBMA) جای کشف قیمت طلاست؛ شوک این کندل «خبر» است و ادامه می‌یابد. کندل 00 (آسیا)
و 16 (اواخر نیویورک) نقدینگی کمتر دارند و شوکشان نویز است.
تمایز از S965/S602/S950 (شوک بی‌قید نشست)، S583/S832 (رویداد ساعت ثابت بدون شوک، H1 — مرده)، S797 (هضم).
در recon نیمهٔ اول: H8@08 lift +36pp (n=50) در برابر H8@16 +12pp (n=64) و H8@00 (n<25) ⇒ تمایز نشست واقعی است.

## ۲) قاعدهٔ منجمد (صفر پارامتر آزاد)
- کارت: XAUUSD **H8** فقط. رویداد در i: hour(open_i)=**08 UTC** و (high_i−low_i) ≥ **2.618**×ATR21[i−1] و close_i≠open_i
- ATR21: EMA(TR, α=2/22) شیفت ۱ (علّی). بدون شرط ρ.
- جهت = sign(close_i−open_i)؛ ورود open(i+1) (کندل 16 UTC همان روز)
- SL = TP = **1.618**×ATR21[i−1] (RR=1) · max_hold **12** کندل H8 (۴ روز؛ ثابت recon)
- allow_overlap=False. تک‌کارت؛ **بدون استخر**.

## ۳) نیمهٔ اول (2011-01 → 2018-11-12)
n=50 · WR 82.0٪ · null(۱۲ قرعه) 46.1٪ · lift +35.9pp · PF 3.93 · exp +84.7 pip · long 18 (89٪) / short 32 (78٪) ·
سال‌ها: ۷/۸ مثبت (فقط 2011 −80 pip).

## ۴) چندگانگی — صادقانه
شبکهٔ recon 144 (3TF×3θ×2ρ×2هندسه×ساعات) + ۴ بررسی پایداری + ۱ نهایی ⇒ **n_trials = 149**.
مسیر C: `compute_rqs2(..., split_bar=n//2)` روی کل ۱۵.۶ سال؛ نیمهٔ دوم تا این لحظه لمس نشده.

## ۵) پیش‌بینی و ابطال‌گرها
- n کل ≈ 100. اگر lift نیمهٔ دوم ≥ نصف نیمهٔ اول (≈+18pp): z ≈ 3.6 ⇒ **ACCEPT ممکن (≈۴۰٪)**؛
  اگر lift به ~+12pp بریزد: POWER-LIMITED؛ اگر <+8pp: REJECT.
- F1: WR holdout < wr_req موتور. F2: counter_drift سود فقط هم‌راستا ⇒ regime-beta.
- F3: PF کل < 1.3. F4: تمایز نشست در نیمهٔ دوم ناپدید شود (این را فقط *پس از* حکم، توصیفی گزارش می‌کنم؛ آزمون نیست).

## ۶) تعهدها
یک `compute_rqs2` · حکم عیناً · بدون تعمیر/تیون/تغییر ساعت یا θ پس از holdout · REJECT ⇒ گزارش کوتاه و S799 ·
ACCEPT ⇒ توقف و گزارش به کاربر · هیچ تغییری در سایت/استقرار (کار من فقط اکتشاف است) · داده mt5_full (sha256 ۱۹/۱۹).
