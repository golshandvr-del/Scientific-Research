# گزارشِ نهاییِ مأموریتِ S540 — استخرِ لنگردارِ مولدِ S382 روی تایم‌فریم‌های درشت

> **بازه:** 2026-08-13 · **پرسونا:** داوید هیلبرت · **داده:** کاملِ ۱۵.۵۹ سالهٔ mt5_full
> **حکمِ نهایی:** REJECT در سدِ مشروعیتِ C5 + **قضیهٔ مرزِ بسته** (یافتهٔ ساختاریِ ماندگار)
> **لایه‌های زندهٔ سایت:** دست‌نخورده — S382-H4 همچنان ACCEPT با n=869, z=4.75

---

## ۱. خلاصهٔ اجرایی

مأموریت از مسیرِ ۱ (بهبودِ لایهٔ زنده) پیش رفت: آیا می‌توان توانِ آماریِ
S382-H4 را با ادغامِ تایم‌فریم‌های درشتِ هم‌خانواده بالا برد؟ پاسخِ اندازه‌گیری‌شده:
**خیر — و برای همیشه.** زنجیرهٔ اثبات:

1. **غربالِ توان** (۵ کارتِ بکر، K=12): گرادیانِ لبه تأیید شد — H2 مرده (+1.01)،
   H8 قوی‌ترین (+6.28) ولی هیچ‌کدام تنها به z≥3.09 نمی‌رسد.
2. **کشفِ هم‌زمانی با rebase**: مأموریتِ موازیِ S520 دقیقاً استخرِ بی‌لنگرِ
   {H6,H8,H12} را آزموده و کشته بود (REJECT 21.8). پیش‌ثبتِ v1 من **پیش از
   اجرای هر آزمونی** رسماً باطل شد — قانونِ مرگِ ابدی، صفر نگاهِ حکمی خرج شد.
3. **پیش‌ثبتِ v2 (لنگردار)**: استخرِ {H4,H6,H8,H12} — متمایز از هر دو جسد؛
   خانوادهٔ ساختاری بدونِ درجهٔ آزادی؛ معیارِ شکست از پیش پذیرفته (کامیت 11812eca).
4. **اجرای قطعیِ واحد**: FIFO استخرِ ۲۲۰۳تایی را به ۸۲۲ فروکاست — کمتر از
   n خودِ لنگر (۸۶۹)! سهمِ H4 = 51.6% > 50% ⇒ نقضِ C5 ⇒ توقف طبقِ پیش‌ثبت،
   بدونِ دستکاریِ آستانه، بدونِ دیدنِ هیچ دروازه‌ای.
5. **دفعِ آخرین نامزد با حساب**: H8-nonOV (lift +11.84, n=194) از نظرِ ریاضی
   نمی‌تواند H5 را پاس کند (n_needed=286>194) — آزموده نشد، تریاژِ توان.

### قضیهٔ مرزِ بسته (سهمِ علمیِ ماندگارِ این مأموریت)

**برای مولدِ S382، هیچ ترکیبی از تایم‌فریم‌های درون‌روزیِ XAUUSD نمونهٔ مستقلِ
افزوده تولید نمی‌کند.** عبورِ WillR(14) از −13 روی کندل‌های ≥4h یک رویدادِ
تقویمیِ واحد است که در چند آینهٔ هم‌راستا منعکس می‌شود. اثباتِ سه‌قسمتی:
S520 (استخرِ بی‌لنگر z=2.24) + ممیزیِ هم‌پوشانیِ S520 (۴۶–۵۷٪ درونِ پوزیشنِ H4)
+ S540 (حتی با لنگر، ادغام n را **می‌کاهد**). راهِ رشدِ n فقط عمود بر محورِ
زمان است: دارایی/مولدِ غیرهم‌زمان — نه کارتِ زمانیِ جدید.

---

## ۲. شواهدِ عدمِ ارتکابِ ۸ خطای رایج

| # | خطا | شاهدِ عدمِ ارتکاب |
|---|---|---|
| ۱ | **قضاوت با معیارِ غیررسمی** | تنها داور `engine/rqs2.compute_rqs2`؛ غربالِ توان صراحتاً «غربال، نه داور» و هیچ حکمی از آن صادر نشد |
| ۲ | **بهینه‌سازی/انتخابِ پسینی** | صفر پارامترِ جدید (import از S382)؛ عضویتِ خانواده با تعریفِ ساختاریِ از-پیش-موجود؛ انتخابِ زیرمجموعه با الگوریتمِ موجودِ `choose_homogeneous_subset` |
| ۳ | **نادیده‌گرفتنِ چندگانگی** | n_trials=23755 (ارثِ کامل)؛ دفترِ کاملِ نگاه‌ها در §۶ MD رسمی؛ H8-nonOV با همین بودجه دفع شد |
| ۴ | **HARKing** | هر دو پیش‌ثبت در کامیتِ جداگانه پیش از اجرا؛ معیارِ شکست از پیش پذیرفته و عیناً اجرا شد |
| ۵ | **تکرارِ آزمونِ مرده** | v1 به محضِ کشفِ S520 باطل شد بدونِ اجرا (f6a54951)؛ تمایزِ v2 از هر دو جسد جدول شد |
| ۶ | **نشتِ زمانی/دادهٔ اشتباه** | گاردِ BUG-DATASETDRIFT سرِ هر خروجی؛ محورِ ۱ساعتهٔ ضدِ BUG-AXIS/QUANT/SPAN؛ searchsorted(right)−1؛ assertِ بازتولیدِ n هر کارت |
| ۷ | **گزارشِ گزینشی** | REJECT با دقتِ یک ACCEPT مستند شد؛ ابطالِ v1 علنی در همان فایل |
| ۸ | **تورمِ n با هم‌پوشانی** | FIFO تقویمی concurrency=1؛ گزارشِ per-card حذف (51–78٪) — همین صداقت استخر را کشت و درست همین است |

## ۳. پایبندی به قوانین

- **هم‌پوشانی (بدونِ تعویق):** ممیزی درون‌ساختِ ابزار (fifo_by_card)؛ دِینِ S520 ادا شد.
- **MTF:** هر کارت جداگانه غربال/گزارشِ افزایشی؛ **EURUSD مطلقاً لمس نشد** (استثنای کاربر).
- **ذخیرهٔ افزایشی:** ۹+ کامیت/پوش؛ اجرای پس‌زمینه با چک‌پوینتِ JSON هر کارت.
- **مرگِ ابدی:** ابطالِ v1؛ بسته‌شدنِ ابدیِ ادغامِ MTF این مولد.
- **حفظِ بودجه:** RR=1.5 (TP>SL) منجمد.
- **همه‌چیز شناور است:** فرضیهٔ محبوبم با داده ابطال و بی‌درنگ رها شد.
- **تریاژِ توان پیش از قضاوت:** غربال اول؛ H8-nonOV بدونِ خرجِ نگاه دفع شد.

## ۴. پرسونای هیلبرت

- صورت‌بندیِ مسئله به‌مثابهٔ پرسشِ دقیق: «تابعِ لبه بر حسبِ مقیاسِ زمانی چه شکلی دارد؟»
- برنامهٔ اصلِ موضوعی: پیش‌ثبت = اصولِ موضوعه؛ حکم = استنتاجِ ماشینی (C5 ⇒ توقف، بی‌چانه).
- احترام به سلف: زیرساختِ S520 صفر-بازنویسی بازاستفاده شد؛ تقدمِ S520 با ابطالِ کارِ خودم پاسخ گرفت.
- قدرِ حکمِ سلبی: «ما خواهیم دانست» شاملِ دانستنِ قطعیِ «نه» است.

## ۵. مصنوعات

| فایل | نقش |
|---|---|
| `tools/s540_power_screen.py` + `results/_s540_power/` | غربالِ توان |
| `results/S540_PREREG_S382_COARSE_TF_POOL.md` | پیش‌ثبتِ v1 + ابطال |
| `results/S540_PREREG_S382_ANCHORED_POOL_H4H6H8H12.md` | پیش‌ثبتِ v2 |
| `tools/s540_pool_adjudicate.py` + `results/_s540_run.log` | اجرای قطعی |
| `results/_s540/pool_c5_violation.json` | مدرکِ توقف در C5 |
| `results/S540_S382AnchoredCoarsePool_Xauusd_H4H6H8H12_rqs2_0_REJECT.md` | MD رسمی |

## ۶. توصیه‌ها

1. پیش از هر پیش‌ثبتِ ادغام، نسبتِ حذفِ FIFO را برآورد کنید؛ >۵۰٪ ⇒ استخر پیشاپیش مرده.
2. نامزدهای POWER-LIMITED (S323/S334/S354) پیش‌ثبتِ فعالِ موازی دارند (S570/S600/S610) — ورود = تکرار.
3. مسیرِ عمود بر زمان: مولدهای غیرهم‌زمان روی همان H4 — ولی فقط خانوادهٔ پیش‌ثبت‌شدهٔ مسیرِ B (تک‌مولد به z_luck=4.07 نمی‌رسد).
4. هر rebase را **پیش از** پیش‌ثبت انجام دهید (درسِ ابطالِ v1).

---
*«Wir müssen wissen — wir werden wissen.» و اکنون می‌دانیم: این در، بسته است. — S540*
