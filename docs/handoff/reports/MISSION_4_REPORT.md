# MISSION 4 — دفترچهٔ گزارشِ علمی
### احیای لایه‌های سوخته با فیلترهای رژیم · بازهٔ استراتژی: `S430–S449`

> **ماهیتِ این سند:** این یک *دفترچهٔ آزمایشگاه* است، نه یک گزارشِ پایانی.
> هر کشف به‌صورتِ یک **ورودیِ شماره‌دار** ثبت می‌شود و سند در طولِ مأموریت
> **رشد** می‌کند. قاعدهٔ حاکم: *هیچ عددی از حافظه نقل نمی‌شود* — هر عدد به یک
> فایلِ مدرکِ خام در `results/` ارجاع دارد.
>
> **چرا جداست:** به درخواستِ سرپرستِ پروژه، این دفترچه در
> `docs/handoff/reports/` نگه داشته می‌شود تا با احکامِ رسمیِ لایه‌ها در
> `results/` قاطی نشود. احکامِ رسمی سرِ جای خود می‌مانند؛ اینجا **روایتِ
> علمی، مکانیزم‌ها و درس‌ها** ثبت می‌شود.

---

## فهرست
| ورودی | موضوع | نتیجه |
|---|---|---|
| [E-00](#e-00) | کشفِ حاکمیتی: سندِ معیار عقب‌تر از موتور است (`v2.4` vs `v2.6`) | اصلاحِ مسیر |
| [E-01](#e-01) | سرشماریِ بایگانیِ احیا (`۵۹۲` جفت در ۴ طبقه) | ابزارِ نو |
| [E-02](#e-02) | `S430` — احیای `S73` با هندسهٔ هزینه | **REJECT** (پرثمر) |
| [E-03](#e-03) | **قانونِ قفلِ افق**: چرا TP/SL می‌تواند «تزئینی» باشد | قانونِ نو |
| [E-04](#e-04) | معیارِ نامزدیابیِ درست: **حاشیهٔ H9** به‌جای `z` | ابزارِ نو |
| [E-05](#e-05) | `S431` — تجمیعِ چند-کارتی | **ACCEPT · RQS2=93.9** |
| [E-06](#e-06) | پنج باگ و یک نقصِ صداقت که خودم گرفتم | درسِ روش‌شناختی |
| [E-07](#e-07) | همپوشانیِ اندازه‌گیری‌شدهٔ `S431` (افشا به ضررِ خودم) | مکان‌یابیِ ارزشِ افزوده |
| [E-15](#e-15) | `S434` — احیای `S139` «رانشِ شبانه» + هشت باگ + سیاستِ دادهٔ کاربر | **REJECT · RQS2=18.7** (پرثمر) |
| [E-17](#e-17) | `S436` — احیای `S214` Brooks·فصل ۱۱ + **دیوارِ توان** + قانونِ رقیق‌شدنِ رشته | **REJECT · RQS2=14.3** (پرثمر) |

---

<a name="e-00"></a>
## E-00 · کشفِ حاکمیتی — معیارِ واقعی `v2.6` است، نه `v2.4`

**مشاهده:** `docs/RQS2_SPEC.md` نسخهٔ `v2.4` را توصیف می‌کند، ولی موتورِ
اجراییِ `engine/rqs2.py` نسخهٔ **`v2.6`** است. یعنی اگر یک محققِ تازه فقط سند
را بخواند، با معیاری کار می‌کند که **دیگر حاکم نیست**.

**تفاوتِ حیاتی که کلِ مسیرِ من را شکل داد:** در `v2.6` دروازهٔ **`H9`
(استحکامِ هزینه)** «نابخشودنی» است. یعنی حکمِ تخفیفیِ `UNPROVEN` — که برای
لایه‌های کم‌نمونه راهِ فرار باز می‌کند — **هیچ راهی برای شکستِ `H9` باز
نمی‌کند**.

**نتیجهٔ عملی:** دو نوعِ کاملاً متفاوتِ «شکست» وجود دارد و اشتباه گرفتنشان
مأموریتِ احیا را نابود می‌کند:

| نوعِ شکست | دروازه‌ها | معنی | قابلِ احیا؟ |
|---|---|---|---|
| **توانی** (بخشودنی) | `H3`, `H5`, `H7`, `H10` | لبه هست، نمونه کم است | ✅ بله — با توان |
| **ساختاری** (نابخشودنی) | `H9` (و هندسه) | لبه **کوچک‌تر از هزینه** است | ❌ معمولاً نه |

این تفکیک، **کلِ استراتژیِ نامزدیابیِ من** شد و در `E-04` به یک ابزار تبدیل شد.

---

<a name="e-01"></a>
## E-01 · سرشماریِ بایگانیِ احیا

**ابزار:** `tools/s430_resurrection_census.py`

۱۴۷ فایلِ حکمِ رسمی خوانده شد و **۵۹۲ جفتِ (لایه × کارت)** در چهار طبقه ریخته شد:

| طبقه | تعداد | معنی |
|---|---|---|
| `TIER-A` | ۷ | هر ۱۱ دروازه پاس (احیای رایگان) — ۵ تا از قبل روی سایت بودند |
| `TIER-B` | ۴۲ | فقط دروازه‌های **آماری** افتاده (`H3/H5/H7/H10`) ⇒ **شکارگاهِ اصلی** |
| `TIER-C` | ۱۴ | نقصِ اقتصادی/هندسی |
| `TIER-D` | ۵۲۹ | چندنقص |

**یک اصلاحِ کوچک ولی مهم:** باگِ فرمت را اصلاح کردم تا دادهٔ **غایب** با `?`
نشان داده شود، نه `0` — چون **«نبودِ داده ≠ صفر»**. گزارشِ صفر برای یک کمیتِ
اندازه‌گیری‌نشده، یک تحریفِ آماری است.

---

<a name="e-02"></a>
## E-02 · `S430` — احیای `S73` با هندسهٔ هزینه · **REJECT**

**مدرک:** `results/S430_SessionDriftCostGeometry_Eurusd_M15_rqs2_0_REJECT.md`

### نامزد
```
S73 / EURUSD-M15 «رانشِ بازگشاییِ سشنِ اروپا»
n = ۱۰۰۷ · WR = ۶۰.۴۸٪ · PF = ۱.۷۴۴ · maxDD = ۲.۹۴٪
z مهارت = ۱۱.۳۲σ  ← رکوردِ کلِ بایگانی (سدِ لازم ۳.۰۹)
خارج‌نمونه (n=۳۳۳): WR=۶۳.۹۶٪ PF=۲.۰۸۸ ← بهتر از درون‌نمونه
تنها دروازهٔ افتاده: H9
```

### تشخیص و فرضیه
لایه با `SL=12 / TP=12` (`RR=1`) آزموده شده بود و بارِ هزینه‌اش `c/SL = 0.271`.
طبقِ **قانونِ پنجمِ بهبود (حفظِ بودجه)**: به‌جای بالا بردنِ `WR` با فیلتر (که
معامله را می‌کشد)، **نقطهٔ سربه‌سر را با `TP>SL` پایین بیاور**.

### نتیجه: نیمهٔ اول درست، نیمهٔ دوم شکست
| TP | سربه‌سرِ مقاوم | نتیجه |
|---|---|---|
| ۱۲ | ۶۳.۳۳٪ | `WR=۵۸.۷٪` ❌ |
| ۱۸ | ۵۰.۶۷٪ | تخفیفِ **۱۲.۶۶ واحدی** ✅ |

سربه‌سر دقیقاً همان‌طور که پیش‌بینی کردم فروریخت — ولی **امیدِ ریاضی تکان
نخورد**. علت در `E-03` است.

### اثباتِ مرگِ ابدی (حسابی، نه از خستگی)
```
بهترین سودِ خامِ کلِ ۲۰ ترکیب = ۱.۲۰۹ pip
سدی که H9 می‌خواهد            = ۱.۶۰۰ pip
کمبودِ ساختاری                = ۳۲٪
```
طبقِ **قانونِ سومِ بهبود (مرگِ ابدی)**، حکمِ `REJECT` تنها وقتی مجاز است که
*هیچ* ترکیبی نجات ندهد. اینجا شکافِ ۳۲٪ روی **بهترین** ترکیبِ شبکه است.

### یک نکتهٔ صداقت
راهِ تقلب در دسترسم بود: کافی بود اسلیپیجِ یورو را صفر بگیرم تا هزینه از ۱.۶
به ۱.۰ برسد و `H9` راحت پاس شود. **انجام ندادم** — این دقیقاً اشتباهِ رایجِ ۸
(دور زدنِ معیار) است.

---

<a name="e-03"></a>
## E-03 · قانونِ نو — **قفلِ افق**: کِی TP/SL «تزئینی» است؟

این مهم‌ترین کشفِ *مفهومیِ* من تا اینجاست و از دلِ شکستِ `S430` بیرون آمد.

**مشاهدهٔ تناقض‌آمیز:** `WR` در سه `TP`ِ کاملاً مختلف **دقیقاً یکسان** بود.
این ریاضیاً محال است — مگر اینکه سطوح هرگز **لمس** نشوند.

**اندازه‌گیری:**
```
میانهٔ حرکتِ مطلوب در پنجرهٔ ۱.۵ ساعته = فقط ۴.۷۵ pip
سهمِ معاملاتی که به ۱۲ pip می‌رسند   = ۸.۱٪
سهمِ معاملاتی که به ۲۴ pip می‌رسند   = ۱.۴٪
⇒ ۹۶٪ معاملات با انقضای زمانی بسته می‌شوند
⇒ سطوحِ TP/SL در این کارت «تزئینی» بودند
```

### قانون (برای استفادهٔ AIهای بعدی)
> **هندسهٔ `TP/SL` تنها وقتی معنا دارد که افقِ نگه‌داری آن را در دسترس کند.**
> اگر با تغییرِ `TP` نرخِ برد **تغییر نکند**، سطح لمس نمی‌شود و هر بهینه‌سازیِ
> `TP` روی آن کارت **بی‌اثر** است. این یک آزمونِ **ارزانِ تشخیصی** است که باید
> **قبل از** جاروی شبکهٔ `TP` اجرا شود.

این کشفِ **مستقلِ** «قانونِ قفلِ سه‌گانه»ِ خودِ پروژه است (`hold ≥ (k_sl·rr)²`) —
من از راهِ تجربی به همان نتیجه رسیدم.

**تلاشِ رفع قفل و شکستش:** افق را به ۵ ساعت بردم تا سد در دسترس شود. سد لمس
شد و سودِ خام به بیشینه رسید (`+۱.۲۰۹`) ولی **`WR` از ۵۸.۷٪ به ۵۰.۰٪ سقوط
کرد** — چون رانشِ سشن یک پدیدهٔ **زمان‌محدود** است. دو خواسته **ضدِ یکدیگرند**؛
همین خودِ قفل است.

---

<a name="e-04"></a>
## E-04 · معیارِ نامزدیابیِ درست — **حاشیهٔ H9** به‌جای `z`

**درسِ `S430`:** معیارِ انتخابِ نامزدِ من **غلط** بود. من صدرنشینِ `z` را
برداشتم، ولی:

> `z` می‌پرسد **«آیا لبه واقعی است؟»**
> `H9` می‌پرسد **«آیا لبه بزرگ است؟»**
> و این دو **مستقل‌اند**.

`S73` بالاترین `z`ِ کلِ بایگانی را داشت (`۱۱.۳۲σ`) و با این حال ساختاراً مرده
بود، چون لبه‌اش با آنکه **قطعی** بود، **کوچک‌تر از هزینه** بود.

**ابزار:** `tools/s430_h9_headroom_rank.py` — کلِ بایگانی را بر اساس
**حاشیهٔ `H9`** (سودِ خامِ pip منهای `۲×` هزینهٔ کامل) رتبه‌بندی می‌کند.

**نتیجه:** ۳۲۶ از ۵۹۰ جفت حاشیهٔ مثبت دارند. و یک **الگویِ تکرارشونده** در صدر:

| نشانهٔ «طبقهٔ طلایی» | مقدار |
|---|---|
| حاشیهٔ `H9` | `+۳۸` تا `+۱۷۳` pip (فراوان) |
| `n` | ۴۲ تا ۷۷ (کم) |
| دروازهٔ افتاده | فقط `H3`, `H5` (**توانی** ⇒ بخشودنی) |

یعنی **دقیقاً برعکسِ `S73`**: لبهٔ عظیم با نمونهٔ کم.

با تابعِ خودِ موتور (`n_required_for_h3`) اثبات کردم هر نامزدِ صدر **زیرِ سقفِ
شیشه‌ای** است — یعنی شکستِ `H3`ِ آن‌ها **ریاضیاً از کمبودِ نمونه** است، نه از
نبودِ لبه. این تشخیص، مسیرِ `E-05` را ساخت.

---

<a name="e-05"></a>
## E-05 · `S431` — تجمیعِ چند-کارتی · **ACCEPT · RQS2 = 93.9**

**مدارک:** `results/S431_LpsbMulticardPool_Xauusd_M5M15M30H1_rqs2_93_ACCEPT.md`
· دادهٔ خام: `results/_scan_S431/pool_verdict.json`

### ایدهٔ مرکزی
اگر شکست **توانی** است (نمونهٔ کم، لبهٔ بزرگ)، راهِ حل **افزودنِ فیلتر نیست** —
فیلتر نمونه را *کم‌تر* می‌کند و مسئله را بدتر. راهِ حل **افزودنِ نمونه** است:
همان سازوکار روی چند کارت، تجمیع در **یک جمعیتِ تقویمیِ واحد**.

### اعضا (هر چهار تا به‌تنهایی `POWER-LIMITED` بودند)
| کارت | n | lift | WR | امید (pip) |
|---|---|---|---|---|
| `XAUUSD_M5` | ۴۷ | +۲۵.۹۱ | ۷۲.۳۴٪ | ۵۶.۸۱ |
| `XAUUSD_M15` | ۳۸ | +۱۷.۴۱ | ۶۵.۷۹٪ | ۶۴.۲۹ |
| `XAUUSD_M30` | ۲۸ | +۱۹.۵۵ | ۶۷.۸۶٪ | ۸۰.۱۸ |
| `XAUUSD_H1` | ۶۶ | +۱۶.۱۷ | ۶۵.۱۵٪ | ۶۷.۳۰ |

`n_before = 179` → `n_after = 168` (۱۱ معامله به‌خاطرِ **حذفِ همپوشانیِ زمانی**
کنار رفتند — تجمیع نباید همزمانی بسازد).

### حکمِ نهایی
| معیار | مقدار | سدِ لازم | ✓ |
|---|---|---|---|
| حکم | **ACCEPT** | — | ✅ |
| `RQS2` | **۹۳.۹** | — | رتبهٔ A |
| `n` | ۱۶۸ | ۳۰ | ✅ |
| `WR` | ۶۶.۶۷٪ | ۴۷.۳۲٪ (سربه‌سر) | ✅ `+۱۹.۳۵` |
| `PF` | ۲.۸۵۸ | — | ✅ |
| `z` مهارت | **۴.۷۰۶** | ۳.۰۹ | ✅ |
| `p_perm` | `1e-06` (K=۲۰۰۰) | — | ✅ |
| سدِ بخت (`H5`) | `z_obs=۴.۷۱` vs `z_luck=۱.۸۰` | — | ✅ حاشیه `+۲.۹۱` |
| امید | `+۶۴.۲۹` pip | — | ✅ |
| **امید @ ۲× هزینه (`H9`)** | **`+۶۰.۹۹`** pip | `> 0` | ✅ |
| `maxDD` | ۱.۶۲٪ | — | ✅ |
| بیشترین باختِ پیاپی | ۳ | ≤ ۸ | ✅ |
| همزمانی | ۱ | ≤ ۱ | ✅ |
| بازه‌های تقویمی | ۴/۴ مثبت | — | ✅ |
| هر دو نیمه | مثبت | — | ✅ |
| **خارج‌نمونه** | `n=۶۷` `WR=۷۴.۶۳٪` `PF=۴.۶۰۶` | ۴۷.۳۲٪ | ✅ **بهتر از درون‌نمونه** |

### چرا این تجمیع **مشروع** است (سه شرطِ اندازه‌گیری‌شده، نه ادعا)
1. **صفر پارامترِ نو:** فیلتر یک قانونِ *صفر-پارامتری* است (`state == -1` با
   `L=8, f=0.33`) و روی هر چهار کارت **یکسان**. هیچ متغیری جست‌وجو نشد.
2. **هندسهٔ ارثی و قفل‌شده:** همهٔ `TP/SL` از `S333.BEST_CFG` ارث می‌رسد
   ⇒ هیچ چندگانگیِ نو تحمیل نشد (`n_trials = 16` صادقانه اعلام شد).
3. **همگنی:** `lift`ها هم‌علامت و هم‌مقیاس‌اند (`+۱۶.۲` تا `+۲۵.۹`) و **هیچ
   عضوی از ۳۶.۹٪ نمونه بیشتر نیست** ⇒ قیدِ `C5` که خودم گذاشتم.

### قویترین شاهدِ ضدِ بیش‌برازش
`WR` خارج‌نمونه (**۷۴.۶۳٪**) از درون‌نمونه (۶۶.۶۷٪) **بالاتر** است و
`PF` خارج‌نمونه (**۴.۶۰۶**) از درون‌نمونه (۲.۸۵۸) **بالاتر**. بیش‌برازش
الگویِ **معکوس** تولید می‌کند.

### دو محدودیتِ صادقانه که باید ثبت شود
1. **فقط لانگ:** `side_n = {long: 168, short: 0}`. لایه هیچ سیگنالِ شورتی
   ندارد ⇒ در بازارِ نزولیِ بلندمدت **بی‌دفاع** است. این یک ریسکِ ساختاریِ
   افشا‌شده است.
2. **توزیعِ نامتوازنِ تقویمی:** `cal_nets = [۶۵.۵, ۱۵۳.۰, ۱۹۶.۴, ۴۹۷۰.۲]`.
   هر ۴ بازه مثبت‌اند (`H8` پاس) **ولی** بازهٔ چهارم ~۲۵× بقیه است. علتِ
   احتمالی: قیمتِ اسمیِ طلا در سال‌های اخیر ۲–۳ برابر شده، پس همان حرکتِ
   درصدی سودِ دلاریِ بزرگ‌تری می‌دهد. `WR` هم در آن بازه پایدار است، پس این
   **تورمِ مقیاس** است نه شکستِ لبه — ولی باید در تفسیرِ «سودِ خالص» لحاظ شود.

---

<a name="e-06"></a>
## E-06 · پنج باگ و یک نقصِ صداقت که **خودم** گرفتم

این بخش عمداً مفصل است، چون در یک پروژهٔ علمی **گزارشِ خطاهای خود** بخشی از
داده است، نه چیزی برای پنهان کردن.

| # | نشانه | ریشه | اصلاح |
|---|---|---|---|
| `BUG-REPRO` | `n=122` در برابرِ `n=1007`ِ بایگانی؛ و `WR` در هر سه `TP` **دقیقاً یکسان** | فیلترِ `dip` را «۴ کندلِ نزولیِ پیاپی» فهمیدم، ولی قانونِ واقعیِ `S73` **جابه‌جاییِ خالص** است (`close[t]-close[t-4]<0`) ⇒ نسخهٔ من ۱۰× سخت‌گیرتر | بازتولیدِ وفادار (`n=1194, WR=58.71%`) |
| `BUG-AXIS` | `cal_occupied=1` با ۶ سال داده (**محال**) | `bar_time` را زمانِ معاملات دادم ولی موتور با `exit_bar` (~۱۲۰٬۰۰۰) ایندکس می‌کند ⇒ همه به ۱۰۸ کلیپ شدند | محورِ تقویمیِ مشترک + بازنویسیِ ایندکس‌ها |
| `BUG-QUANT` | `concurrency=2` | همپوشانیِ واقعی **صفر** بود (اثباتِ عددی)، ولی ۴ معامله فاصلهٔ `<۶۰` دقیقه داشتند ⇒ در سطلِ ساعتی برخورد کردند | محورِ ریزتر |
| `BUG-SPAN` | `concurrency=34` (**بدتر**) | فایلِ `M5` از ۲۰۲۳ شروع می‌شود ولی `M30/H1` از ۲۰۱۱ ⇒ همه به ایندکسِ صفر کلیپ | شبکهٔ مصنوعیِ ۵‌دقیقه‌ای روی کلِ ۱۵ سال |
| `BUG-OOS` + `BUG-SPLITDIR` | `H3/H7` نامعلوم؛ سپس تقسیمِ **معکوس** (۱۸ اکتشاف / ۹۱ خارج‌نمونه) | `holdout` نداده بودم؛ سپس ۶۰٪ **زمان** ≠ ۶۰٪ **نمونه** | صدکِ ۶۰٪ِ زمانِ ورودِ معاملات |
| `BUG-DEFAULTARG` | استخرِ «کامل» هم فقط ۳ کارت داشت | **دامِ کلاسیکِ پایتون:** `def f(x, add_margin=POOL_ADD_MARGIN)` مقدارِ پیش‌فرض را **در لحظهٔ تعریف** قفل می‌کند (`0.15`)؛ تغییرِ بعدیِ متغیرِ ماژول **بی‌اثر** است | جایگزینیِ خودِ تابع با wrapper (`add_margin=-1.0` صریح) |

### `ISSUE-C2` — نقصِ صداقتِ علمی (خودگزارش‌شده)
`pool_cards` درونِ خود `choose_homogeneous_subset` را صدا می‌زند که کارتی را که
`z_proxy` را کمتر از ۱۵٪ بالا نمی‌برد **حذف** می‌کند. در اجرای من این کارتِ
`XAUUSD_H1` بود.

آن حذف **پس از دیدنِ نتیجه** رخ می‌دهد ⇒ یک درجهٔ آزادیِ پس‌ازدیدن-داده ⇒
نقضِ قیدِ `C2`ِ **خودم** («فهرستِ اعضا پیش از دیدنِ عدد قفل است»). با ۴ کارت،
**۱۵ زیرمجموعه** در اختیار دارم؛ برداشتنِ «بهترین» دقیقاً همان چندگانگیِ
پنهانی است که `H5` برای مجازاتش ساخته شده.

**راه‌حل:** حکمِ **اصلی** روی استخرِ **کاملِ ۴ کارتی** صادر شد (بدونِ هیچ
گزینش) و نسخهٔ گزینش‌شده فقط تحلیلِ ثانویه ماند.

### خطرناک‌ترین لحظهٔ مأموریت
`BUG-DEFAULTARG` **در سکوتِ کامل** شکست خورد: اجرا یک `ACCEPT`ِ تمیز با
`RQS2=93.3` داد و من می‌توانستم آن را به‌عنوانِ «استخرِ ۴ کارتی، وفادار به قیدِ
`C2`» منتشر کنم — در حالی که در واقع زیرمجموعهٔ **پس‌ازدیدن-دادهٔ ۳ کارتی** بود؛
یعنی دقیقاً همان تخلفی که اصلاح برای جلوگیری‌اش نوشته شده بود.

**تنها به این دلیل گرفته شد که خطِ تشخیصیِ خودم را خواندم، به‌جای پریدن به
حکم.** درسِ روش‌شناختی برای AIهای بعدی:

> **هر اصلاح باید یک خطِ تشخیصیِ *قابلِ مشاهده* چاپ کند که ثابت کند اصلاح
> واقعاً اثر کرده.** یک اصلاحِ بی‌اثر که نتیجهٔ «خوب» می‌دهد، از یک شکستِ
> صریح **بسیار خطرناک‌تر** است.

نکتهٔ جالب: پس از اصلاح، نتیجه **بهتر** شد (`n` از ۱۰۹ به ۱۶۸، `z` از ۴.۴۲ به
۴.۷۱، `RQS2` از ۹۳.۳ به ۹۳.۹). یعنی صداقت **هزینه‌ای نداشت** — کارتی که
انتخابگر «رقیق‌کننده» می‌دانست، در واقع بزرگ‌ترین عضو (۳۶.۹٪) و مفید بود.

---

<a name="e-07"></a>
## E-07 · همپوشانیِ اندازه‌گیری‌شدهٔ `S431` (افشا به ضررِ خودم)

**ابزار:** `tools/s431_overlap_audit.py` · **مدرک:** `results/_scan_S431/overlap_audit.json`

| کارت | ورودهای من | لایهٔ فعلیِ سایت | همپوشانی |
|---|---|---|---|
| `M5` | ۴۷ | `S355` = `S333 + withLpsbGate` (`registry.ts:510`) | **۱۰۰.۰٪** (۴۷ از ۴۷، Jaccard ۱۰۰٪) |
| `M15` | ۳۸ | `S344` (سازوکارِ متفاوت) | `N/A` (مقایسه‌ناپذیر) |
| `M30` | ۲۸ | `S312` (سازوکارِ متفاوت) | `N/A` |
| `H1` | ۶۶ | `S356` (سازوکارِ متفاوت) | `N/A` |

**افشای صریح:** روی `XAUUSD-M5` لایهٔ من **هیچ لبهٔ نویی اضافه نمی‌کند** —
همپوشانی **دقیقاً ۱۰۰٪** است. این را به ضررِ خودم گزارش می‌کنم، چون همان کارتی
است که سازوکار در آن کشف شده بود و آسان بود که `ACCEPT` را «چهار کارتِ نو»
جلوه دهم.

**یک تصمیمِ آگاهانه در طراحیِ ابزار:** برای سه کارتی که لایهٔ سایت سازوکارِ
**دیگری** است، همپوشانی در این گام قابلِ محاسبه نیست، پس ابزار صریحاً `N/A`
گزارش می‌کند و **نه `0`**. گزارشِ یک کمیتِ مقایسه‌ناپذیر به‌عنوانِ «صفر
همپوشانی» تحریفی است که به نفعِ لایهٔ خودم تمام می‌شد.

**مکانِ دقیقِ ارزشِ افزوده:** سازوکارِ `S333+LPSB` هرگز به `M15`/`M30`/`H1`
وصل نشده بود. طبقِ **قانونِ سومِ همپوشانیِ پروژه** (حتی ۹۹٪ همپوشانی، آن ۱٪
متفاوت را ارزشمند می‌کند)، این سه کارت قلمروِ نو هستند.

**و طبقِ قانونِ استفاده از بخشِ همپوشان به‌عنوان فیلتر:** آن ۱۰۰٪ همپوشانیِ
`M5` دقیقاً همان چیزی است که تجمیع را **مشروع** می‌کند نه زائد — لنگرِ
از-پیش-معتبرشده‌ای که **توانِ آماریِ** لازم را به سه کارتِ دیگر می‌دهد.

---

## E-08 · پورتِ `S431` به سایت و `local-mobile` — و سه باگ در **آزمونِ خودم**

**تاریخ:** ۲۰۲۶-۰۸-۰۹ · **مدرک:** `results/_scan_S431/parity_wiring_run.txt`

### چه چیزی وصل شد
سازوکارِ `S333 + دروازهٔ LPSB` که پیش‌تر **فقط** روی `XAUUSD-M5` وصل بود، به
سه کارتِ `M15`/`M30`/`H1` گسترش یافت. هر سه اتصال **افزون بر** لایهٔ موجود
است، نه جایگزینِ آن — تا ماژولار بودنِ `ROS2`-مانند حفظ شود. و در هر سه کارت،
لایهٔ قدیم و نو **مکمل** یکدیگرند نه رقیب:

| کارت | لایهٔ موجود | لایهٔ من | چرا مکمل‌اند |
|---|---|---|---|
| `M15` | `S344` (**SHORT**) | `S431` (**LONG**) | پوششِ دو سوی بازار |
| `M30` | `S312` (زمان-محورِ خالص) | `S431` (ساختار-محور) | دو منبعِ **مستقل** اطلاعات |
| `H1` | `S356` (WR ۵۱.۲۸٪، لبه از هندسه `RR=2`) | `S431` (WR ۶۵.۱۵٪، لبه از دقتِ ورود) | دو **مکانیزمِ سودآوریِ** متفاوت |

**تأییدِ وفاداریِ پورت:** پیش از اتصال، `S333_CFG`ِ تایپ‌اسکریپت را با
`BEST_CFG`ِ پایتون **پارامتر-به-پارامتر** مقایسه کردم؛ هر چهار کارت مطابق بودند
(`M5` 120/120/96 · `M15` 200/240/96 · `M30` 380/420/80 · `H1` 450/520/64). پس
آنچه سایت اجرا می‌کند همان چیزی است که `RQS2` نمره داد. **هیچ پارامترِ نویی
جست‌وجو نشد** ⇒ هیچ چندگانگیِ نو ⇒ حکم آلوده نمی‌شود.

### درسِ اصلیِ این مرحله: «اتصالِ ساختاری ≠ اتصالِ رفتاری»
آزمونِ دودِ موجودِ پروژه (`_smoke_card_inventory.mjs`) فقط **ساختار** را
می‌سنجد: «آیا کارتِ M15 دو لایه دارد؟». ولی یک لایه می‌تواند وصل باشد و در عمل
هرگز سیگنال ندهد — مثلاً اگر `undefined` پاس شود، یا مقیاسِ pip نخواند، یا
استثنایی درونِ `runCard` **بی‌صدا بلعیده** شود (که رفتارِ درستی است تا کارت
نشکند، ولی خطا را هم پنهان می‌کند). در هر سه حالت آزمونِ ساختاری **سبز** می‌ماند
و کاربر هیچ سیگنالی نمی‌بیند. پس یک آزمونِ **رفتاری** نوشتم که کارت‌ها را روی
دادهٔ واقعیِ تاریخی با پنجرهٔ **رو-به-جلو** می‌راند.

### سه باگ — همه در **آزمونِ من**، هیچ‌کدام در لایه
| باگ | نشانه | ریشه | چرا خطرناک بود |
|---|---|---|---|
| `BUG-ESBUILD-RESOLVE` | `ERR_MODULE_NOT_FOUND` | `local-mobile` **عمداً** `node_modules` ندارد (فلسفه‌اش: اجرا روی گوشی بدونِ `npm install`) | بی‌خطر؛ صدای بلند داشت |
| `BUG-FIELDNAME` | «۰ ورود و **۰ خطا**» در هر سه کارت | کدِ لایه را از `out.layerCode \|\| out.code` می‌خواندم که **هیچ‌کدام در `RouterDecision` وجود ندارند**؛ مسیرِ درست `out.sourceLayer.code` است (`router.ts:174`) | **بسیار خطرناک** — شرط همیشه false بود و هر `ENTRY`ِ واقعی با `continue` دور ریخته می‌شد. آن صفر «صفرِ لایه» نبود، **«صفرِ آزمون»** بود |
| `BUG-PROBEWINDOW` | `M15` صفر داد ولی `M30`/`H1` ورود دادند | پنجرهٔ یکسانِ ۶٬۰۰۰ کندل برای همه. ولی نرخِ ورودِ `M15` یک در هر ~۳٬۹۴۷ کندل است ⇒ امیدِ ریاضی فقط **۱.۵** ⇒ احتمالِ صفرِ کاذب (پواسون، λ=۱.۵) ≈ **۲۲٪** | صفر **هیچ چیزی را اثبات نمی‌کرد**. این دقیقاً **اشتباهِ رایجِ ۵** (نتیجه‌گیریِ سریع از یک پنجرهٔ کوچک) بود که این بار درونِ **پوششِ آزمونِ خودم** ظاهر شد — جایی که کمتر از همه دنبالش می‌گشتم |

**اصلاحِ `BUG-PROBEWINDOW`:** پنجره را per-card از **نرخِ ورودِ خودِ حکم**
می‌بندم تا امیدِ تعداد ≥۱۰ باشد (احتمالِ صفرِ کاذب < ۰.۰۰۵٪)، و امیدِ ریاضی را
**کنارِ** عددِ مشاهده‌شده چاپ می‌کنم تا هیچ صفری دیگر بی‌مرجع تفسیر نشود.

### نتیجهٔ نهایی — `PASS` روی هر سه کارت
| کارت | پنجرهٔ کاوش | امیدِ ریاضی | `ENTRY`ِ مشاهده‌شده | خطا |
|---|---|---|---|---|
| `XAUUSD-M15` | ۶۰٬۰۰۰ | ۱۵.۲ | **۳۲** ✅ | **۰** |
| `XAUUSD-M30` | ۶۰٬۰۰۰ | ۹.۳ | **۱۷** ✅ | **۰** |
| `XAUUSD-H1` | ۶۰٬۰۰۰ | ۴۳.۵ | **۷۰** ✅ | **۰** |

**صفر خطا در ۱۸۰٬۰۰۰ پنجره** ⇒ مسیرِ اجرا پاک است و هیچ استثنایی بی‌صدا
بلعیده نمی‌شود.

**یک ناسازگاریِ صادقانه که ثبتش می‌کنم نه توجیهش:** تعدادِ مشاهده‌شده در هر سه
کارت **۱.۶ تا ۲.۱ برابرِ** امید است — و چون این انحراف **سیستماتیک** است نه
تصادفی، علتِ معین دارد: این کاوشگر هر کندل را مستقل می‌سنجد و هر کندلِ
`ENTRY` را می‌شمارد، در حالی که بک‌تستِ `RQS2` معامله را باز می‌کند و تا بسته
شدنش ورودِ نو را **قفل** می‌کند. پس یک معاملهٔ اقتصادی که چند کندل طول می‌کشد،
آنجا **یک‌بار** و اینجا **چندبار** شمرده می‌شود.
⇒ این اعداد فقط **نشانگرِ زنده بودن**‌اند و **هرگز برآوردِ فراوانیِ معامله
نیستند**. کسی نباید «۷۰ سیگنالِ H1» را «۷۰ فرصتِ معاملاتی» بخواند.

### ابزارِ نگهبان که به‌روز شد (نه تضعیف)
`_smoke_card_inventory.mjs` پس از اتصالِ من **درست FAIL داد** (۲ لایه در برابرِ
قرارِ ۱ لایه) — و همین FAIL مدرکِ خوبی بود که ویرایش‌هایم واقعاً اثر کرده‌اند.
دستورِ صریحِ خودِ آن فایل: «اگر احیای موجه است، جدول را به‌روز کن و **هرگز آزمون
را برای سبز شدن تضعیف نکن**». پس `ACCEPT_CONTRACT` را با ضمیمه‌کردنِ سندِ مجوز
به‌روز کردم و **هیچ‌یک از پنج بررسیِ ساختاری** را دست نزدم. تیترِ آن آزمون هم که
می‌گفت «هر کارت ۱ لایه · v2.4» اصلاح شد، چون سندی که خلافِ واقعیت بگوید همان
پوسیدگی‌ای است که در `E-00` کشفش کردم.

---

## E-09 · اولویت‌بندیِ ۵۸۷ نامزد + `S432` — **ACCEPT · RQS2 = 84.7**

### ابزارِ اولویت‌بندی و دو باگ در **معیارِ خودم**
پیش از انتخابِ نامزدِ بعدی، ۵۸۷ جفتِ (لایه × کارت) را در شش طبقه ریختم:

| طبقه | تعداد | معنی |
|---|---|---|
| `POOL-READY` | ۳۸ | الگویِ `S431` ⇒ بالاترین شانس |
| `POOL-SOLO` | ۳ | لبهٔ واقعی، خواهرِ کافی ندارد |
| `FILTER-CAND` | ۱ | فیلترِ کیفیت لازم |
| `STRUCT-BROKEN` | ۲۷۹ | بازطراحی لازم |
| `DEAD-COST` | ۲۶۴ | حاشیهٔ `H9` ≤ ۰ ⇒ غیرقابلِ احیا |
| `UNKNOWN-COST` | ۲ | دادهٔ غایب ⇒ **ادعا نمی‌کنم** |

**`BUG-SCALEBIAS`** — صدرنشینِ اولِ رتبه‌بندی `S350/W1` شد با حاشیهٔ `+۱۷۳.۲`
ولی `z = ۱.۰۴` (یعنی **نویزِ محض**). اندازه‌اش گرفتم: میانهٔ حاشیه از `M5 = ۳۸.۵`
به `W1 = ۱۷۳.۲` می‌رود در حالی که `lift` برعکس حرکت می‌کند ⇒ حاشیهٔ `H9`
عمدتاً **مقیاسِ تایم‌فریم** را می‌سنجد نه کیفیتِ لبه. این **اشتباهِ رایجِ ۷ در
لباسِ نو** بود: از تلهٔ `z` گریختم و در تلهٔ `H9` افتادم.

**`BUG-SCALEBIAS-2`** — اصلاحِ اولم **کار نکرد** و فقط چون خروجی را دوباره
خواندم فهمیدم: ستونِ `cost` برای هر ردیف دقیقاً `۳.۳۰` است، پس تقسیم بر یک
عددِ ثابت **ترتیب را عوض نمی‌کند**. اصلاحِ درست: نرمال‌سازی بر امیدِ خودِ کارت
+ وزنِ بیشتر به `z`.

### نتیجهٔ `S432` — «رانشِ میانِ ماه» (زمان-محورِ خالص)
| معیار | مقدار | سد |
|---|---|---|
| حکم | **ACCEPT** | — |
| `RQS2` | **۸۴.۷** | — |
| `n` | ۲۹۸ | ۳۰ |
| `WR` | ۵۸.۳۹٪ | سربه‌سرِ هزینه‌دار ۵۰.۴۷٪ |
| `PF` | ۱.۷۴۱ | — |
| `z` مهارت | ۳.۱۸ | ۳.۰۹ |
| خارج‌نمونه | `WR ۵۹.۶۶٪` · `PF ۲.۱۳۲` | **بهتر** از درون‌نمونه |

**پیش‌بینیِ پیش‌ثبت‌شده‌ام `z ≈ ۳.۳۳` بود و واقعیت `۳.۱۸` شد** — و علتش را
اندازه گرفتم نه توجیه: `FIFO` حدودِ ۲۶.۲٪ معاملات را حذف کرد و آن‌ها **بهترِ**
از میانگین بودند (وقتی دو تایم‌فریم همزمان شلیک می‌کنند، خودش تأییدِ `MTF`
است). از این یک **قاعدهٔ تصحیح‌شدهٔ پیش‌بینی** ساختم: تخفیفِ ۱۰–۱۵٪ برای `FIFO`.

**دو محدودیتِ صادقانه:** هر دو دروازهٔ توانی **باریک** پاس شدند
(`H3`: +۵.۱٪ · `H5`: +۱۰.۰٪) ⇒ این `ACCEPT` واقعی ولی **باریک** است. و حکم
رویِ **جمعیتِ تجمیعیِ `H1`+`M15`** است؛ کارتِ `M15` به‌تنهایی `REJECT`
(`RQS2 = ۲۷.۳` · `z = ۱.۷۱`) داشت.

---

## E-10 · پورتِ `S432` — یک باگِ **در لایهٔ زندهٔ سایت** و دو باگ در آزمونِ خودم

### 🔴 `BUG-S312-FILTGATE` — عدم‌تطابقِ پورت در کدی که **از قبل زنده بود**
این مهم‌ترین یافتهٔ این گام است و **ربطی به لایهٔ جدیدِ من نداشت**:
`computeMidMonth(times, utcHour, filt?)` پارامترِ سومی دارد که فیلترِ کیفیتِ
`close > EMA200` را اعمال می‌کند، ولی **تنها فراخوانیِ کلِ پروژه** آن را پاس
نمی‌کرد و `midFiltersPass(undefined)` صریحاً `return true` می‌دهد ⇒ دروازه
**همیشه باز** بود.

سه دلیل که این «ریزه‌کاری» نیست:
1. حکمِ `ACCEPT`ِ کارتِ `XAUUSD-M30` (`RQS2 = ۸۷.۷` · `n = ۲۸۹` · `z = ۳.۶۶`)
   **با** این فیلتر گرفته شده (`s312_oos_check.py`: `quality_filter=True`)
   ⇒ سایت لایه‌ای را می‌راند که با نسخهٔ نمره‌گرفته **یکی نبود**.
2. آرایهٔ `filters` صریحاً به کاربر می‌گفت «فیلترِ کیفیت (روندِ کلان)» ⇒ سایت
   چیزی را **اعلام** می‌کرد که اجرا نمی‌کرد. این از خودِ باگ بدتر است.
3. جهتِ خطا **سخاوتمندانه** بود نه محافظه‌کارانه: بدونِ فیلتر، لایه در روندِ
   نزولیِ کلان هم `LONG` می‌داد — دقیقاً موقعیتی که فیلتر برای حذفش بود.

اصلاح: `close > EMA200` از `ctx.candles` محاسبه و پاس می‌شود. وفاداریِ عددی
**بررسی شد نه فرض**: `indicators.ema` با `alpha = 2/(period+1)` و بدونِ
bias-correction ⇒ معادلِ `pandas.ewm(span=200, adjust=False)`. گرم‌نشدنِ
`EMA200` عمداً **fail-CLOSED** است: «نبودِ داده» هرگز نباید به «تأییدِ سیگنال»
ترجمه شود.

### 🐞 `BUG-EPOCHPARSE` — چرا اجرای اولِ آزمونم «روزِ-میان = ۰» داد
اجرای اول `FAIL` داد با شمارشِ **صفر** برای روزهای ۱۰/۱۳/۲۰ در ۳۰٬۰۰۰ کندلِ
`H1` (≈۵ سال). این **ریاضیاً محال** است ⇒ پس باگ در **آزمون** بود نه لایه.
اگر فقط سطرِ آخرِ حکم را می‌خواندم، «صفر ورود» را «لایهٔ مرده» ترجمه می‌کردم.

دو خطای مستقل که روی هم افتاده بودند:
1. ستونِ `time` یک **epochِ ثانیه‌ای** است (`1294012800` ⇒ 2011-01-03)، پس
   `Date.parse('1294012800' + 'Z')` مقدارِ **`NaN`** می‌دهد و
   `new Date(NaN).getUTCDate()` هم `NaN` ⇒ هرگز با ۱۰/۱۳/۲۰ برابر نمی‌شود.
2. حتی با پارسِ موفق، آن مسیر **میلی‌ثانیه** می‌داد ولی مرجعِ سایت
   `isMidMonthWindow` صریحاً `new Date(times[last] * 1000)` است ⇒ واحد
   **ثانیه** است. دادنِ میلی‌ثانیه در سالِ ۵۶۰۰۰ می‌افتاد.

واحدِ درست از **سه مرجعِ مستقل** تأیید شد نه از حدس: `engine/backtest.py`
(`unit='s'`) · `mid_month_drift.ts` (`* 1000`) · `_parity_s356_causal.mjs`
(`parseInt`). گاردِ `assertSecondsEpoch` افزوده شد تا اگر فرمتِ داده عوض شد،
آزمون **بلند فریاد بزند** نه اینکه صفرِ کاذبِ آرام بسازد.

**یافتهٔ جانبیِ مهم (افشا به ضررِ کارِ قبلیِ خودم):**
`_parity_s431_wiring.mjs` **همین** لودرِ خراب را دارد و با این حال `PASS` شد.
تصادفی نبود و بررسی‌اش کردم: مسیرِ `S431` (`withLpsbGate` + `s333Layer`)
هیچ‌گاه `ctx.times` یا `utcHour` را نمی‌خواند ⇒ `NaN` **بی‌اثر** بود و آن
`PASS` معتبر می‌ماند. ولی `S432` **زمان-محورِ خالص** است و همان باگِ نهفته
دقیقاً جایی کُشنده شد که اهمیت داشت.

### نتیجهٔ آزمونِ رفتاری — `PASS` روی **هر سه** ادعا
| کارت | `ENTRY` | خطا | واجدِ شرطِ زمانی | فیلتر رد کرد |
|---|---|---|---|---|
| `XAUUSD-H1` | ۹۹۱ | ۰ | ۱۵۹۶ | **۳۷.۹٪** |
| `XAUUSD-M15` | ۷۳۶ | ۰ | ۱۴۸۸ | **۵۰.۵٪** |

سطرِ «فیلتر رد کرد» مهم‌ترین ستون است: اگر اصلاحِ `BUG-S312-FILTGATE` در سکوت
کار نمی‌کرد (الگویِ `BUG-DEFAULTARG`)، `ENTRY` **برابرِ** ۱۵۹۶ و ۱۴۸۸ می‌شد.
نشد ⇒ دروازه واقعاً می‌بندد، و روی `M15` **نیمی** از فرصت‌های زمانی را که
سایت پیش‌تر کورکورانه معامله می‌کرد حذف می‌کند.

### 🧮 آشتیِ واحدها — تا `PASS`ِ خودم بدخوانده نشود
خواندنِ سطحی می‌گوید «`ENTRY = ۹۹۱` در برابرِ امیدِ `۸۷.۷` ⇒ ۱۱ برابر ⇒ لبه
متورم شده». این **غلط** است و به‌جای نوشتنِ یک بند توضیحی، آزمون را واداشتم
هر بار **عددی اثباتش کند**: `pool_verdict.json` واحدش **معامله**ی FIFOِ
ناهم‌پوشان است، ولی این کاوشگر **کندلِ سیگنال‌دِه** می‌شمارد — چون حالتِ
`ENTRY` سایت در هر ساعتِ واجدِ شرط بازنشر می‌شود و این رفتارِ **درستِ** رابطِ
کاربری است (کاربری که ساعتِ ۵ اپ را باز می‌کند هم باید سیگنال را ببیند).

اتحادی که باید برقرار باشد و برقرار است:
```
ساعت‌های واجدِ شرط/معامله × نرخِ عبورِ فیلتر = ENTRY/معامله
H1 : 18.2 × 0.621 = 11.3   و   991/87.7 = 11.3  ✅
M15: 53.9 × 0.495 = 26.7   و   736/27.6 = 26.7  ✅
```
دو کارتِ مستقل تا یک رقمِ اعشار می‌خوانند ⇒ **صفر** سیگنالِ اضافی. این حالا
یک **گزاره** است که اگر بیش از `۰.۰۵` بشکند حکم را `FAIL` می‌کند.

### نکتهٔ بازتولیدپذیری که آموختم
`build.mjs` **باید** از داخلِ `local-mobile/` اجرا شود. یک‌بار از ریشهٔ مخزن
اجرایش کردم و باندل ۸۳ خط `diff` داد — همه‌اش پیشوندِ مسیرِ کامنت
(`../web_tool/...` در برابرِ `web_tool/...`) و **صفر** تغییرِ منطقی. پس از
اجرای درست، باندلِ ثبت‌شده **بایت‌به‌بایت** یکسان شد ⇒ آرتیفکتی که کاربر
`git pull` می‌کند دقیقاً همان است که آزمون‌هایم اعتبارسنجی کردند.

---

## E-11 · بازنشستگیِ رابطِ کاربریِ متروکِ «موتورِ پایتون» (Pyodide)

### چگونه کشف شد — یک تصحیحِ کاربر که یک نقصِ **معماری** را لو داد

در پایانِ `E-10` من لینکِ `/app` را به‌عنوان «سایت» تحویل دادم. کاربر تصحیح کرد
که آن **سایتِ پروژه نیست**، بلکه بقایای نسخهٔ کنارگذاشته‌شدهٔ `local-mobile`
است که «مفسرِ پایتونی» داشت. بررسی، حرفِ او را کلمه‌به‌کلمه تأیید کرد:

| مدرک | یافته |
|---|---|
| `web_tool/public/static/app/index.html` | عنوان: «اپلیکیشن (**موتورِ پایتون**)» |
| همان فایل | `<script src=".../pyodide/v0.26.2/full/pyodide.js">` |
| `static/app/app.js` (سربرگِ خودش) | «منطقِ اپلیکیشنِ **APK** (WebView + **Pyodide**)» |
| `static/app/pyengine/` | ۹ فایلِ `.py` شاملِ `live_engine.py`, `backtest.py`, `indicators.py`, `scalp_engine.py`, `capital_engine.py` + دو استراتژی |

**درسِ حاکمیتیِ این ورودی — «رابطِ کاربریِ دوم» یک بدهیِ خطرناک است:**
پروژه **دو** رابطِ کاربری با **دو** موتور داشت. آزمون‌های نگهبانِ من
(`_smoke_card_inventory`, `_parity_*`) فقط موتورِ `TypeScript` را می‌سنجند، پس
نسخهٔ پایتونی **صفر** پوششِ آزمون داشت و آزادانه واگرا می‌شد. بدتر: در
`pyengine/strategies/` فایلِ `s73_eurusd_session_drift.py` نشسته بود — یعنی
کپیِ همان `S73`ای که من در `S430` اثباتِ `REJECT` گرفتم. خوانندهٔ بعدی
می‌توانست آن را «مرجع» بپندارد. **کپیِ کهنه‌ای که هیچ آزمونی آن را نمی‌سنجد،
بدتر از نبودِ کپی است.**

### سایتِ رسمی کدام است (تثبیتِ سندی، تا این ابهام تکرار نشود)

| مورد | مقدار |
|---|---|
| مسیر | `GET /` (**نه** `/app`) |
| صفحه | `<title>دستیارِ تصمیمِ معاملات — چند دارایی</title>` · `dir="rtl"` · `bg-slate-950` |
| منطقِ رابط | `web_tool/public/static/app.js` (۹۲KB · «ماشینِ حالتِ ۴-وضعیتی») |
| موتور | `TypeScript` سمتِ سرور، از `local-mobile/app.bundle.mjs` |
| راه‌اندازیِ گوشی | `bash update.sh` سپس `bash start.sh` ⇒ پورتِ **۸۰۸۰** |

⚠️ تلهٔ نام‌گذاری که نزدیک بود گران تمام شود: `static/app.js` (سایتِ **واقعی**،
۹۲KB) و `static/app/app.js` (بقایا، ۲۸KB) فقط به اندازهٔ **یک اسلش** تفاوت
دارند. به‌جای اعتماد به مسیر، ۲۵۰ بایتِ نخستِ هر دو را خواندم و از سربرگِ
خودشان تفکیک کردم.

### چه چیزی حذف شد و چه چیزی **عمداً** نگه داشته شد

بررسیِ ارجاع در کلِ مخزن (`ts/tsx/mjs/js/sh/cjs`) **تنها یک** ارجاعِ زنده به
درختِ متروک یافت (همان ری‌دایرکت) ⇒ حذف بی‌خطر بود.

| مورد | تصمیم | دلیل |
|---|---|---|
| `static/app/**` (۱۱ فایل · ۱۶۰KB) | ❌ **حذف** | رابطِ دومِ متروک + کپیِ فورک‌شدهٔ موتور |
| مسیرِ `/app` | ♻️ `301` به `/` | نشانی ممکن است در بوکمارکِ گوشی مانده باشد؛ حذفِ کامل ⇒ `404`. مسیرِ کهنه باید به **حقیقت** هدایت شود نه به خطا |
| `static/models/*.onnx` (۹ فایل) | ✅ **نگه داشته شد** | متروک **نیست**: `src/browser/signal_client.ts` صریحاً بارگذاری‌شان می‌کند |
| `static/browser-signal.js` | ✅ **نگه داشته شد** | آرتیفکتِ ساختِ زندهٔ `build:client` در `package.json` و مستند در `README` |

دو موردِ آخر مهم‌اند: در نگاهِ اول «یتیم» به‌نظر می‌رسیدند (هیچ `<script>`ی در
`PAGE` صدایشان نمی‌زند)، ولی پیش از حذف **جست‌وجو کردم** و زنده بودند. حذفشان
یک پاکسازیِ ظاهراً بی‌ضرر بود که مدلِ `ONNX` را می‌کشت.

### تأییدِ رفتاریِ پس از پاکسازی

| بررسی | نتیجه |
|---|---|
| `GET /` | `HTTP 200` — سایتِ اصلی |
| `GET /app` | `HTTP 301` · `location: /` ✅ |
| `/static/app/index.html` · `app/app.js` · `pyengine/live_engine.py` | هر سه `404` ✅ (واقعاً رفته‌اند) |
| `app.js` · `style.css` · `signal_latch.js` · `ui/badges.js` · `browser-signal.js` | هر پنج `200` ✅ (صفر آسیبِ جانبی) |
| `/api/decision/*` روی ۵ کارت | همه `ok=True` |
| حضورِ `S432` | فقط روی `M15`/`M30`/`H1` ✅ — مطابقِ دقیقِ رجیستری |
| `S432` و `aboveEma` در باندلِ نو | ۸ و ۳ ارجاع ⇒ کارِ `E-09`/`E-10` سالم ماند |

### نکتهٔ مستندات

`local-mobile/README.md` بازبینی شد و **هیچ** اشاره‌ای به نسخهٔ پایتونی نداشت
⇒ مستنداتِ لوکال‌موبایل از ابتدا درست بودند و خطا **فقط** در انتخابِ مسیرِ من
بود. سه اشارهٔ «پایتون» در `web_tool/README.md` هم به موتورِ **مرجعِ**
`engine/*.py` و آزمونِ `parity` است (کاملاً زنده) ⇒ عمداً دست‌نخورده ماند.
هیچ سندی نیاز به اصلاح نداشت.

---

## E-12 · `BUG-LASTLAYER` — وقتی آزمونِ **خودم** خود را «رگرسیونِ محصول» جا زد

### نشانه و استدلالی که مرا نجات داد

پس از پاک‌سازیِ `S433`، اجرای همهٔ نگهبان‌ها این را داد:

```
_smoke_card_inventory  → PASS
_parity_s432_wiring    → PASS
_parity_s431_wiring    → ❌ FAIL   (۲ کارت صفر سیگنال)
```

راهِ آسان این بود که بگویم «پاک‌سازی چیزی را شکست» و تغییرات را برگردانم.
ولی **الگویِ** شکست با آن روایت نمی‌خواند:

| کارت | نتیجه |
|---|---|
| `M15` | ۰ ❌ |
| `M30` | **۱۷ ✅** |
| `H1` | ۰ ❌ |

اگر رگرسیونِ واقعیِ سایت بود، باید **هر سه** صفر می‌شدند. و آن دو کارتی که صفر
شدند **دقیقاً** همان دو کارتی بودند که در `S432` دست‌کاری کردم. این تقارنِ
مشکوک، فرضیه را ساخت.

### علتِ ریشه‌ای — یک وابستگیِ **اعلام‌نشده** به موقعیت

ترتیبِ واقعیِ رجیستری را **استخراج** کردم، نه حدس:

```
M15 → [ s344 ,  withLpsbGate(S431) ,  s312 ]   ⇒ آخرین = s312  ❌
M30 → [ s312 ,  withLpsbGate(S431) ]           ⇒ آخرین = S431  ✅
H1  → [ s354 ,  withLpsbGate(S431) ,  s312 ]   ⇒ آخرین = s312  ❌
```

آزمون هدفش را با `layers[layers.length - 1]` می‌شناخت، با این فرضِ **نانوشته**
که «آخرین لایهٔ هر کارت = `S431`». من در `S432` لایهٔ زمان-محورِ `s312Layer` را
به **انتهای** `M15` و `H1` افزودم ⇒ آزمون **لایهٔ اشتباه** را می‌آزمود.
`M30` فقط به‌خاطرِ **تصادفِ ترتیب** درست کار می‌کرد.

⇒ **هیچ رگرسیونی در سایت وجود نداشت.**

### درسِ ساختاری (ارزشش از خودِ اصلاح بیشتر است)

> آزمونی که هدفش را با **موقعیت** می‌شناسد، در نخستین افزودنِ لایه می‌شکند —
> و بدترین شکلِ ممکن را به‌خود می‌گیرد: ظاهرِ **رگرسیونِ محصول**. یعنی خواننده
> را می‌فرستد در کدِ *منتشرشده* بگردد، در حالی که عیب در *خودِ آزمون* است.

این سومین باگ از این خانواده در همین مأموریت است: `BUG-FIELDNAME` (مسیرِ غلطِ
میدان)، `BUG-PROBEWINDOW` (پنجرهٔ کوچک)، و اکنون `BUG-LASTLAYER` (هدف‌یابیِ
موقعیتی). هر سه یک ریشه دارند: **آزمون دربارهٔ خودش فرضی داشت که نسنجیده بود.**

### اصلاح و دو تصمیمِ روش‌شناختی

۱. هدف با **کد** شناخته می‌شود: همهٔ لایه‌های کارت اجرا می‌شوند و فقط تصمیمی
   شمرده می‌شود که `sourceLayer.code` آن شاملِ `S431` است ⇒ نسبت به ترتیبِ
   اتصال، تعدادِ لایه و افزودن‌های آینده **مقاوم**.

۲. **عمداً** هر لایه جدا اجرا می‌شود، نه از طریقِ روترِ کارت. چون روتر ممکن
   است یک لایه را بر دیگری اولویت دهد و آن‌وقت «صفرِ `S431`» می‌توانست یعنی
   «`S432` در داوری برنده شد»، نه «`S431` مرده است». این آزمون سؤالِ
   **زنده‌بودنِ** لایه را می‌پرسد نه **برنده‌شدنِ** آن؛ قاطی‌کردنِ این دو،
   آزمون را در سکوت ضعیف می‌کرد.

۳. `BUG-EPOCHPARSE` در همین فایل هم اصلاح شد — و این **پیش‌شرط** بود نه
   تمیزکاریِ اختیاری: در `E-10` افشا کردم که این فایل لودرِ خراب را دارد و
   فقط چون مسیرِ `S431` زمان نمی‌خواند `PASS` می‌شد. آن تحملِ تصادفی به‌محضِ
   اجرای **همهٔ** لایه‌ها از بین می‌رفت، چون `s312Layer` زمان-محورِ خالص است و
   با `NaN` در سکوت هرگز شلیک نمی‌کرد.

### نتیجه — و قوی‌ترین شکلِ اعتبارسنجی

| کارت | پیش از اصلاح | پس از اصلاح | امید | خطا | عددِ اصلیِ `S431` |
|---|---|---|---|---|---|
| `XAUUSD-M15` | ۰ ❌ | **۳۲ ✅** | ۱۵.۲ | ۰ | ۳۲ ✅ **یکسان** |
| `XAUUSD-M30` | ۱۷ ✅ | **۱۷ ✅** | ۹.۳ | ۰ | ۱۷ ✅ **یکسان** |
| `XAUUSD-H1` | ۰ ❌ | **۷۰ ✅** | ۴۳.۵ | ۰ | ۷۰ ✅ **یکسان** |

دو شاهدِ مستقل که اصلاح **هدفمند** بوده و اعداد را «درست نکرده»:

* `M30` **دقیقاً** همان ۱۷ را داد ⇒ کارتی که از قبل سالم بود، دست‌نخورده ماند.
* هر سه عدد **بایت‌به‌بایت** با گزارشِ اصلیِ `S431` (ورودیِ `E-08`) می‌خوانند
  ⇒ اصلاح، آزمون را به حالتِ درستِ خودش **برگرداند**، نه به حالتِ دلخواه.

صفر خطا در ۱۸۰٬۰۰۰ پنجره.

### نکتهٔ اجرایی

آزمونِ اصلاح‌شده ~۳ دقیقه می‌گیرد (پیش‌تر ~۱ دقیقه)، چون حالا به‌جای ۱ لایه،
میانگین ۲.۶۷ لایه در هر پنجره اجرا می‌شود. طبقِ **قانونِ سومِ پروژه (اندک
اندک)** در پس‌زمینه اجرا و لاگ مرحله‌به‌مرحله چک شد، نه با انتظارِ مسدودکننده.

---

## E-13 · `MODULE_NOT_FOUND` — یک **تشخیصِ اشتباهِ خودم** که صادقانه ثبت می‌کنم

### ادعای اولِ من (و اینکه چرا نادرست بود)

هنگامِ اجرای همزمانِ نگهبان‌ها، آزمونِ دود با `MODULE_NOT_FOUND` افتاد و
تک‌به‌تک بی‌نقص پاس شد. من نتیجه گرفتم علت **تعارضِ فایلِ موقت** است، چون آن
آزمون نامِ ثابتِ `/tmp/_registry_smoke.mjs` داشت. اصلاحش کردم (نامِ یکتا +
پاکسازی) و با سه اجرای همزمان `PASS` گرفتم ⇒ به‌نظر تأیید شد.

**ولی تأیید نشده بود.** بارِ بعد که همان الگو را اجرا کردم، دوباره افتاد. این
بار متنِ **کاملِ** خطا را خواندم نه فقط کدِ خطا:

```
Error: Cannot find module '/home/user/_parity_s432_wiring.mjs'
```

مسیر `/home/user/` است، **نه** `/home/user/webapp/local-mobile/`.

### علتِ واقعی — باگِ **الگویِ فرمانِ من**، نه باگِ کد

الگویی که نوشته بودم این بود:

```bash
cd /home/user/webapp/local-mobile && nohup node A.mjs > /tmp/a.log 2>&1 &
sleep 2; nohup node B.mjs > /tmp/b.log 2>&1 &
```

عملگرِ `&` بندِ **اولِ** `cd X && nohup node A` را کامل می‌بُرد؛ دستورِ **دوم**
دیگر پشتِ آن `cd` نبود و در پوشهٔ پیش‌فرضِ ابزار (`/home/user`) اجرا می‌شد.

اثباتِ سنجیده (نه استدلالی): در **زیرشل** مسیر حفظ می‌شود
(`( cd X && nohup ... &)` ⇒ `pwd = /home/user/webapp/local-mobile` ✅) و با
**مسیرِ مطلق** هم بی‌عیب اجرا می‌شود ✅. پس محیط سالم بود و الگوی من نبود.

### درسِ روش‌شناختی — سه خطای پشتِ سرِ هم

1. **کدِ خطا را خواندم، متنِ خطا را نه.** `MODULE_NOT_FOUND` مبهم است؛ *نامِ
   ماژولِ گم‌شده* تشخیص را در یک نگاه می‌داد. من از سطرِ `code:` به فرضیه
   پریدم و از سطرِ `Error:` که پاسخ را داشت رد شدم.
2. **یک باگِ محیطی را به کد نسبت دادم.** فرضیه‌ام «باورپذیر» بود (نامِ ثابت
   *واقعاً* بد است) و همین باورپذیری خطرناکش کرد.
3. **آزمونِ تأییدیِ من ناتوان بود.** سه اجرای همزمانِ من همه با **مسیرِ درست**
   شروع شده بودند، پس هرگز شرطی را که خطا را می‌سازد بازتولید نمی‌کردند.
   *آزمونی که نمی‌تواند فرضیه را رد کند، آن را تأیید هم نمی‌کند.*

⚠️ **آیا اصلاحاتِ گام‌های ۹–۱۱ بی‌ارزش شدند؟ نه — ولی برچسبشان عوض می‌شود.**
نامِ ثابتِ فایلِ موقت **حقیقتاً** یک نقصِ همزمانی است و پاکسازی **حقیقتاً**
نشتیِ ~۴۰۰KB در هر اجرا را می‌بندد (۷ فایل شمرده شد). آن‌ها اصلاحاتِ درستی
هستند که به **دلیلِ نادرست** انگیزه گرفتند. اینجا ثبتشان می‌کنم تا خوانندهٔ
بعدی نپندارد `BUG-TMPCOLLISION` علتِ آن `MODULE_NOT_FOUND` بوده.

### تأییدِ نهایی — هر سه نگهبان با فراخوانیِ درست

| نگهبان | حکم | اعداد | مطابقِ گزارشِ اصلی؟ |
|---|---|---|---|
| `_smoke_card_inventory` | ✅ PASS | ۵ کارت · ۱۰ اتصال · ۶ بررسی | ✅ |
| `_parity_s431_wiring` | ✅ PASS | M15=۳۲ · M30=۱۷ · H1=۷۰ · صفر خطا | ✅ `E-12` |
| `_parity_s432_wiring` | ✅ PASS | H1=۹۹۱ · M15=۷۳۶ · فیلتر ۳۷.۹٪/۵۰.۵٪ · آشتی ۱۱.۳=۱۱.۳ و ۲۶.۷=۲۶.۷ | ✅ `E-10` |

دو شاهدِ مستقل که اصلاحاتِ بهداشتِ فایلِ موقت **هیچ رفتاری را عوض نکردند**:
همهٔ اعداد بایت‌به‌بایت با گزارش‌های پیشین می‌خوانند، و پس از همهٔ اجراها
`ls /tmp/*.mjs` صفر می‌دهد ⇒ پاکسازی کار می‌کند و نشتی بسته شد.

### قاعدهٔ عملیاتی که از این ورودی بیرون می‌آید

> برای اجرای پس‌زمینه **همیشه** مسیرِ مطلقِ اسکریپت داده شود
> (`node /home/user/webapp/local-mobile/X.mjs`)، و هرگز به بقای `cd` پشتِ
> عملگرِ `&` تکیه نشود. این با **قانونِ سومِ پروژه (اندک اندک)** هم‌راستاست:
> اجرای پس‌زمینه + چکِ مرحله‌به‌مرحلهٔ لاگ.

---

## E-14 · تأییدِ نهاییِ `S433` — سه نگهبان، اجرای **همزمان**، صفر نشتی

پس از دوازده چک‌پوینتِ `S433`، تأییدِ یکجا انجام شد. مهم‌ترین نکته این است که
هر سه نگهبان **همزمان** اجرا شدند — چون همین همزمانی بود که `BUG-TMPCOLLISION`
و سپس تشخیصِ اشتباهِ `E-13` را بیرون کشید. اگر تک‌به‌تک می‌آزمودم، شرطی را که
خطا را می‌سازد هرگز بازتولید نمی‌کردم (همان نقدی که در `E-13` به آزمونِ
تأییدیِ خودم وارد کردم).

| نگهبان | حکم | اعداد | مطابقِ گزارشِ اصلی؟ |
|---|---|---|---|
| `_smoke_card_inventory` | ✅ PASS | ۵ کارت · ۱۰ اتصال · **۶** بررسی · ۳۹۰.۳ KB | ✅ |
| `_parity_s431_wiring` | ✅ PASS | M15=۳۲ · M30=۱۷ · H1=۷۰ · صفر خطا | ✅ `E-08`/`E-12` |
| `_parity_s432_wiring` | ✅ PASS | H1=۹۹۱ · M15=۷۳۶ · فیلتر ۳۷.۹٪/۵۰.۵٪ · آشتی ۱۱.۳ و ۲۶.۷ | ✅ `E-10` |

سه شاهدِ مستقل که اصلاحاتِ `S433` **هیچ رفتاری را عوض نکردند**:
* همهٔ اعداد **بایت‌به‌بایت** با گزارش‌های پیشین می‌خوانند.
* `ls /tmp/*.mjs` پس از همهٔ اجراها **صفر** می‌دهد ⇒ نشتی بسته شد.
* `node build.mjs` باندلی **بایت‌به‌بایت یکسان** با نسخهٔ کامیت‌شده ساخت
  ⇒ آرتیفکتی که کاربر `git pull` می‌کند دقیقاً همان است که آزمون‌ها سنجیدند.

### تأییدِ رفتاریِ سایتِ زنده (پورتِ ۸۰۸۰، همان پورتِ `start.sh`)

| بررسی | نتیجه |
|---|---|
| `GET /` | `HTTP 200` — سایتِ اصلی (فارسی/RTL · `bg-slate-950`) |
| `GET /app` | `HTTP 301` · `location: /` ✅ |
| `/static/app/index.html` · `app/app.js` · `pyengine/live_engine.py` | هر سه **۴۰۴** ✅ |
| `app.js` · `style.css` · `signal_latch.js` · `ui/badges.js` · `browser-signal.js` | هر پنج **۲۰۰** ✅ صفر آسیبِ جانبی |
| `/api/decision/*` روی ۵ کارت | همه `ok=True` |
| حضورِ `S432` | فقط `M15`/`M30`/`H1` ✅ — و **نه** `M5`/`H4` ⇒ مطابقِ دقیقِ رجیستری |
| باندل: `S432`=۸ · `aboveEma`=۳ · `pyodide`=**۰** | ✅ |

نگهبانِ **یگانگیِ رابطِ کاربری** (بررسیِ ششم، افزودهٔ `S433`) هم سبز است:
تنها یک رابط (`GET /`) وجود دارد و رابطِ متروکِ `Pyodide` بازنگشته.

---

<a name="e-15"></a>
## E-15 · `S434` — احیای `S139` «رانشِ شبانه» · **REJECT · RQS2 = 18.7**

**مدرکِ اصلی:** `results/S434_NightlyDriftDualLever_Xauusd_M30_rqs2_19_REJECT.md`
· پیش‌ثبت‌ها: `S434_PREREG_S139_DUAL_LEVER_SELECTION.md` ·
`S434_PREREG_CANDIDATE_ATTRIBUTION_AUDIT.md` · `S434_PREREG_LOWVOL_ARM_ADJUDICATION.md`
· یافتهٔ جانبی: `S434_FINDING_volatility_at_entry_negative_predictor.md`

### ۱. نامزد و تعمیرِ دو-اهرمی

`S139` زمان‌محورِ خالص است (LONG در `open` کندلِ بعد از بستنِ ساعتِ ۲۲/۲۳ UTC،
`SL=۱۵۰` `TP=۵۰۰`)، پشتوانه‌اش Lou, Polk & Skouras (2019, JFE)، و `S307` قبلاً
ردش کرده بود. دو اهرم طراحی کردم: **مقیاسِ ۱.۳۷× + تریلینگ ۱۳۰** برای افت، و
**فیلترِ رژیمِ `mom13`** برای PF.

| دروازه | پایه | نامزد | سد | نتیجه |
|---|---|---|---|---|
| `H8` افت | ۱۸.۸۳٪ | **۵.۷۸٪** | ۸.۰٪ | ✅ |
| `H1` PF | ۱.۱۶۱ | **۱.۳۸۳** | ۱.۳ | ✅ |
| بازیابی | ۳.۷۱ | **۱۱.۱۴** | ۳.۰ | ✅ |

**و بودجهٔ آماری خرج نشد:** افت ۳.۳× بهتر شد و WR فقط ۰.۸pp افت کرد — برعکسِ
`S307` که WR را ۱۶pp بالا برد ولی لبهٔ واقعی را سه‌برابر ضعیف کرد.

### ۲. 🔴 نزدیک بود یک لایهٔ **سالم** را به‌غلط اعدام کنم (`BUG-NULLUNCOND`)

مدلِ صفر گفت مهارتِ لایه **منفی** است (−۲.۶۰pp) و داشتم REJECT می‌زدم. ولی
پرسیدم «آیا مقایسهٔ خودم عادلانه است؟» و باگ را یافتم: **خطِ مبنا بدونِ تریلینگ
اجرا می‌شد در حالی که لایه با تریلینگ.**

| | آلوده | پاک |
|---|---|---|
| خطِ مبنا | ۴۸.۳۷٪ | **۴۳.۷۳٪** |
| مهارتِ لایه | −۲.۶۰pp ❌ | **+۲.۰۴pp** ✅ |

دو خطِ مبنای **مستقل** به ۴۳.۷۲۹ و ۴۳.۶۹۰ رسیدند (اختلاف ۰.۰۴٪) ⇒ تأییدِ متقاطع.
**درس: جهتِ آسیبِ باگ معکوسِ فرضِ من بود** — تریلینگ WR را *پایین* می‌آورد.

### ۳. حکم — و چرا **صداقت** باعثِ افتادنش شد

**۸ دروازه پاس · ۳ افتاده (`H3`,`H5`,`H6`) · صفر نامعلوم · `RQS2 = ۱۸.۷`**

| دروازه | لازم | دارم |
|---|---|---|
| `H3` لیفت | ۴.۰pp | ۲.۰۴pp (۵۱٪) |
| `H3` z | ۳.۰۹ | ۱.۷۵ |
| `H5` (`n_trials=۱۲۹۶`) | z ≥ ۳.۳۳ | ۱.۷۵ |

`n_trials = ۱۲۹۶` را **صادقانه شمردم** و همان صداقت `H5` را انداخت. اگر
کم‌گزارش می‌کردم پاس می‌شد — و همین است که این REJECT را قابلِ اعتماد می‌کند.

### ۴. اثباتِ مرگِ ابدی — پنج مسیر، همه **سنجیده** نه فرض‌شده

| مسیر | چگونه بسته شد |
|---|---|
| فیلترِ عمیق | اثباتِ ریاضیِ خودشکن‌بودن (`n`↓ ⇒ `perm_sd`↑ ⇒ سدِ z↑، ولی سدِ لیفت مستقل از `n`) |
| فیلترِ کم‌عمق | اسکنِ **۳۹۸ از ۴۰۱** اندیکاتور ⇒ **صفر** عبور از `AUC ۰.۵۴۷۲`؛ سپس **داوریِ مستقیمِ** ۴ بازو با موتورِ کامل |
| تایم‌فریمِ دیگر | **۱۹ از ۱۹** کارتِ طلا؛ بهترین (`M5`) فقط ۵۷٪ سد |
| تعمیرِ `H6` | شیبِ تقویمی در **پایهٔ دست‌نخورده** هم هست (−$۲,۰۷۰) ⇒ ساختاری |
| جفت‌ارزِ دوم | لیفتِ یورو روی هر سه کارت **منفی** (−۷ تا −۱۰pp) |

**کنترلِ مثبت** ساختم تا مطمئن شوم ابزارِ اسکن شکسته نیست — هر پنج حالت درست،
از جمله تشخیصِ اثرِ ضعیفِ `d=۰.۱۵` ⇒ نتیجهٔ صفر **دادهٔ** است، نه نقصِ ابزار.

### ۵. 🎁 سه یافتهٔ علمیِ مثبت (ارزشِ ماندگارِ یک REJECT)

1. **قانونِ MTF کامل — ۱۹/۱۹:** جهشِ **سه‌برابری** بینِ کارت‌های ساعتی
   (`H1`:+۰.۶۹) و باندِ زیر-۳۰-دقیقه (`M5`:+۲.۲۷) ⇒ تأییدِ مکانیزمِ «بازشدنِ
   نشستِ آسیا». اشباع در `M1`–`M30` ⇒ **مقیاسِ زمانیِ مشخصهٔ ۵–۳۰ دقیقه**.
   و مرزِ **ساختاری**: ۷ کارت (`H3`,`H6`,`H8`,`H12`,`D1`,`W1`,`MN1`) صفر سیگنال،
   چون کندلشان روی ساعتِ ۲۲/۲۳ نمی‌افتد.
2. **یورو = یافتهٔ *مثبت*، نه شکست:** تلهٔ بزرگ را گرفتم — `SL=۲۰۵pip` برای طلا
   ۰.۱۰۳ دامنهٔ روزانه است ولی برای یورو **۲.۵۸** (استاپی که هرگز نمی‌خورد). با
   ترجمهٔ نوسان‌محور (`SL=۸.۲۱`) لیفت **منفی** شد. **اگر یورو هم مثبت بود،
   محتمل‌ترین توضیح یک آرتیفکتِ ساعتِ سرورِ بروکر بود که کلِ نتیجهٔ طلا را باطل
   می‌کرد.**
3. **رتبه‌بندی با WR کارتِ *غلط* را انتخاب می‌کرد:** `H2` بالاترین WR را دارد ولی
   تقریباً کمترین مهارت — چون خطِ مبنای خودش هم بالاترین است.

### ۶. 🔬 پنجرهٔ فرارِ باریک — کمّی‌سازیِ دینامیکِ خودشکن

من ابتدا استدلال کرده بودم «فیلتر همیشه خودشکن است». **سنجیدم و اشتباه بود** —
ولی نه به آن اندازه که مسیر را باز کند. با آستانهٔ **بدونِ نشتی**:

| نگه‌داشت | لیفت | z | PF | RQS2 |
|---|---|---|---|---|
| ۱۰۰٪ | +۲.۰۴ | ۱.۷۵ | ۱.۳۸۳ ✅ | ۱۸.۷ |
| ۷۵٪ | +۳.۳۷ | **۲.۴۸** | ۱.۳۱۴ ✅ | **۱۹.۴** |
| ۵۰٪ | **+۳.۶۰** | ۲.۱۹ | ۱.۱۹۷ ❌ | ۱۹.۷ |
| ۲۵٪ | +۲.۰۴ | ۱.۲۰ | — | — |

**لیفت ۸۶٪ بهبود می‌یابد ولی z تقریباً صاف می‌ماند**، چون `perm_sd` هم‌زمان
متورم می‌شود. ⇒ **قانونِ قابلِ‌استفاده برای مأموریت‌های آینده: پنجرهٔ کاربردیِ
فیلتر تقریباً ۱۰–۳۰٪ حذف است، و باید *مسیرِ z* را ردیابی کرد نه مسیرِ لیفت را.**

### ۷. 🔴 دو تصحیح **به زیانِ رَدِ خودم** (مهم‌ترین بخشِ صداقتیِ این ورودی)

1. ادعا کرده بودم «فیلتر لایه را بدتر کرد». **غلط بود.** با آستانهٔ پاک، بازوی
   ۷۵٪ نمرهٔ **۱۹.۴ > ۱۸.۷** می‌گیرد، `H1` را **پاس** می‌کند، افت را به
   **۵.۶۹٪** و بازیابی را به **۸.۴۵** می‌برد.
2. ممیزیِ نشتیِ نگاه‌به‌آینده را **ساخته بودم ولی اجرا نکرده بودم** و به
   استدلال بسنده کرده بودم. اجرایش کردم: **جهتِ پیش‌بینی‌ام غلط بود** — حذفِ
   نشتی نتیجه را **بهتر** کرد (که خودش تأییدی است بر اینکه یافته **آلوده نبود**).

**و با این‌همه حکم عوض نمی‌شود:** بهترین `z` کلِ مأموریت **۲.۴۸** است در برابرِ
سدِ ۳.۰۹ و سدِ چندگانگیِ ۳.۳۲۹ ⇒ **کسریِ ۲۵٪ در z**، و `H6` در **همهٔ** بازوها
منفی. شکست **جفت‌شده** است: بستنِ کسریِ لیفت فیلترِ عمیق‌تر می‌خواهد که z را
**پایین‌تر** می‌برد.

### ۸. تشخیص: لایه **`POWER-LIMITED` است، نه کاذب**

مانع هرگز افت یا سودآوری نبود؛ **اندازهٔ خودِ لبه** بود. تعمیرِ دو-اهرمی دقیقاً
کارِ خودش را کرد، ولی هیچ مدیریتِ معامله‌ای نمی‌تواند **توانِ آماری‌ای** بسازد
که فرضیهٔ ورودی در خود ندارد. **حفظِ این تمایز ارزشِ علمی دارد: اثرِ توان-محدود
را می‌توان با فرضیهٔ ورودِ قوی‌تر بازبینی کرد؛ اثرِ کاذب را نه.**

### ۹. 💾 سیاستِ دادهٔ کاربر — و ششمین نجات

کاربر تاریخِ کاملِ MT5 را داد، ولی `XAUUSD_M1.csv` خام **۲۳۲.۶ MB** است و از سدِ
سختِ **۱۰۰ MB** گیت‌هاب رد نمی‌شود. **سنجیدم، نه فرض کردم:** `gzip` نسبتِ
**۳.۷×** داد (۵۷۳→۱۵۵ MB؛ `M1` به **۵۷.۴ MB** ⇒ عبور)، و بازگشتِ **بیت‌به‌بیت**
با `SHA256` تأیید شد. ابزارِ `--unpack` ساخته شد.

**در عمل شش بار آزموده شد:** پس از هر ریستِ سندباکس، بازیابی در **~۶ ثانیه**
(در برابرِ ۲.۵ دقیقه دانلود+استخراج)، هر بار **سالم=۱۹ · ناهمسان=۰**.

> 🎁 **و بدونِ این داده، یک لایهٔ شکننده را ACCEPT گزارش می‌کردم:** فایلِ `M5`
> قدیمی فقط **۲.۸ سالِ اخیر** (پرسودترین ربع) را می‌پوشاند ⇒ روی آن `H6` **پاس**
> می‌شد.

### ۱۰. هشت باگ در ابزارِ **خودم** — با یک الگوی مشترکِ خطرناک

`BUG-NULLUNCOND` · `BUG-TABLEATTRIB` و شش موردِ دیگر. **الگوی مشترکِ هر هشت:**
سنجهٔ درستی سرِ جایش بود، کد **بی‌خطا** اجرا شد، و سنجه **هیچ اثری نداشت**.

> ⚠️ **درسِ ماندگار: در این پروژه «موفقیتِ خاموش» بسیار خطرناک‌تر از کرش است.**
> کرش فقط وقت می‌گیرد؛ موفقیتِ خاموش **حکمِ غلط** تولید می‌کند.

---

## E-16 · `S435` — احیای `SoS-H1` (Brooks فصل ۱۹) · **REJECT · RQS2 = 23.9**

### ۱. چرا این نامزد: مانعی که کشتش، **دیگر وجود نداشت**
`S205` این لایه را رد کرد **نه به دلیل ضعف آماری** (net ‎+$۵٬۹۰۸‎ · PF ۱.۸۴ ·
walk-forward ۴/۴ مثبت) بلکه **فقط** به دلیل ۹۸٪ همپوشانی با اجتماعِ آن زمان.
هر چهار عضوِ آن اجتماع امروز مرده‌اند — و یکی‌شان (`Overnight h22-23`) **همان
`S139` است که من خودم در `E-15` اعدامش کردم**.

### ۲. اندازه‌گیریِ همپوشانی — و یک پیش‌بینیِ غلط **به نفعِ خودم**
| | `S205` | **اندازه‌گیری‌شده** |
|---|---|---|
| اجتماع | ۱٬۵۳۴ روز | **۴۲۷ روز** |
| پوشش | ۹۸٪ | **۱۶.۸٪** |
| روزهای نو | ~۵ معامله | **۲۷۳ روز** |

پیش‌بینی ثبت‌شده‌ام ۴۰–۷۰٪ بود ⇒ **غلط، و در جهتِ تقویتِ فرضیهٔ خودم**.
**آزمونِ مقاومت:** افزودنِ `S431`ِ جاافتاده اجتماع را ۱۷٪ بزرگ کرد ولی پوشش
فقط **۰.۶ واحد** بالا رفت.

### ۳. بندِ سومِ قانونِ همپوشانی — **بسته شد، نه موکول**
بازوی **همپوشان** (n=۵۷) لیفتِ **منفیِ** ‎−۰.۸۳‎ دارد ⇒ **نمی‌تواند فیلترِ
تأییدِ لایه‌های موجود باشد**. آزموده شد و رد شد.

### ۴. تشخیص **قبل از** بهبود — سه یافتهٔ سنجیده
| یافته | اندازه‌گیری |
|---|---|
| **فیلترِ ارثیِ ATR مضر است** | بدون: n=۵۴۸ WR ۵۰.۰٪ انتظار ۴۴.۰pip · با: n=۳۳۳ WR ۴۸.۱٪ انتظار ۳۲.۱pip |
| **هندسه با افق نمی‌خواند** | TP=750 فقط **۸.۴٪** لمس · ۵۶.۲٪ تایم‌اوت با میانگین **+۱۰۵pip** |
| **لبهٔ جهتی واقعی است** | LONG ‎+۳۲.۱‎ vs SHORT ‎−۲۰.۸‎ ⇒ نامتقارنیِ **۵۲.۹pip** |

> 🔴 **خطای خودم:** فیلترِ ATR را `S204` روی **۳۱ معامله** تأیید کرده بود. من
> اصلِ «شرایط عوض شده» را روی **رَدِ** `S205` اعمال کردم ولی روی **تأییدِ**
> `S204` نه. یک فیلترِ «اعتبارسنجی‌شده» **۲۷٪ از انتظار** را می‌سوزاند.

### ۵. سه بازوی بهبود — همه از تشخیص، هیچ‌کدام از جست‌وجو
| بازو | n | WR | لیفت | z | PF | net | maxDD | RQS2 |
|---|---|---|---|---|---|---|---|---|
| `I1` حذف ATR | ۵۴۸ | ۵۰.۰۰ | **+۳.۵۵** | ۱.۶۷ | **۱.۴۸۲** | **$۱۵٬۱۵۱** | ۹.۰٪ | **۲۳.۹** |
| `I2` +TP=۳۹۴.۹ | ۵۹۷ | ۵۲.۴۳ | **+۳.۹۹** | **۱.۹۵** | ۱.۳۴۸ | $۱۰٬۶۲۵ | ۸.۴٪ | ۲۲.۴ |
| `I3` +سربه‌سر ۱۱۰.۵ | ۶۴۹ | ۳۰.۶۶ | **−۱.۰۲** | −۰.۵۶ | ۱.۳۲۶ | $۶٬۷۰۶ | **۷.۸۶٪** | ۱۷.۳ |

حذفِ فیلترِ ارثی لیفت را از **۰.۰۵ به ۳.۵۵** برد — **۷۰ برابر**. صفر دروازهٔ
نامعلوم در هر سه بازو.

### ۶. 🎓 کشفِ اصلی — **«مسیر» در برابر «مقصد»**
`I3` **کارِ خودش را انجام داد** (`H8` پاس شد، دراوداون ۸.۴ ← ۷.۸۶) ولی **پنج
دروازهٔ دیگر را شکست**. علت:

> آستانهٔ سربه‌سر را روی چندکِ MFEِ بازنده‌ها گذاشتم چون **۹۷.۱٪ برنده‌ها** از
> آن عبور می‌کنند در برابر **۵۰.۰٪ بازنده‌ها**. آن اندازه‌گیری **از نظر حسابی
> درست** و **از نظر پرسش غلط** بود: **MFE می‌گوید قیمت کجا رفت، نه به چه
> ترتیبی.** برنده‌ای که به ۴۰۰ می‌رسد ممکن است اول به ۱۱۰ برود، **زیر ورودی
> برگردد**، بعد راه بیفتد.
>
> **سربه‌سر مداخله‌ای وابسته به مسیر است که با آمارِ مستقل از مسیر اندازه شد.**

**دو اثرِ انگشتِ مستقل:** معاملات ۵۹۷←۶۴۹ (خروجِ زودهنگام جا باز می‌کند) ·
باختِ پیاپی ۷←**۱۳** در حالی که دراوداون **پایین** آمد.

### ۷. 🛡️ `BUG-NULLUNCOND` در **سومین لباس** — و ۱۷ برابر خطایی که نشد
WRِ `I3` از ۵۲.۴ به ۳۰.۷ سقوط کرد. ولی چون مدلِ صفر **هم با سربه‌سر** ساخته
شد، WRِ **تصادف** هم از ۴۸.۴ به ۳۱.۷ افتاد ⇒ سقوط **مکانیکی** است.

> **بدون آن اصلاح، لیفت را ‎−۱۷.۸‎ می‌خواندم به‌جای ‎−۱.۰۲‎ — ۱۷ برابر خطا،
> با اطمینانِ کامل.** قوی‌ترین توجیهِ عملیِ این وسواس در کلِ مأموریت.

### ۸. 🔴 ناخوشایندترین حقیقت — صراحتاً در حکم، نه در پیوست
z=۱.۹۵ عملاً از آستانهٔ متعارفِ ۱.۹۶ رد می‌شود. **اگر `n_trials` را ۵ اعلام
می‌کردم (فقط بازوهای خودم)، سد ۱.۷۹ می‌شد و `I2` پاس می‌کرد.**

من **۲۹۲** اعلام کردم (۲۶۷ باتریِ `S202` + ۲۰ جاروبِ `S204` + ۳ + ۲).
**۲۸۷ از ۲۹۲ را کارِ قبلی خرج کرده.** کنارگذاشتنشان یعنی قضاوتِ بازماندهٔ یک
جست‌وجوی ۲۶۷-راهه انگار اولین چیزِ آزموده باشد — **همان مکانیزمی که بیش‌برازش
را به «معناداری» تبدیل می‌کند**.

### ۹. ⚖️ تعهدی که **شکستم** — ثبتِ صریح
در گامِ ۹۰ نوشتم «بازوی سوم اضافه نمی‌شود». در گامِ ۹۵ اضافه کردم. **پنهانش
نکردم:** قانونِ **مرگِ ابدیِ پروژه** بر تعهدِ من مقدم است، چون تعهدِ من
**ابزارِ خدمت** به همان قانون بود. مرزِ درست: **بازوی برخاسته از جست‌وجو
ممنوع، بازوی برخاسته از تشخیصِ سنجیده مجاز.** مرزِ سخت‌ترِ جایگزین: `I3`
آخرین بازو — **و همین شد**.

### ۱۰. شش باگ در ابزارِ **خودم** — دو الگوی مشترک
| نام | چرا خطرناک |
|---|---|
| `BUG-EPOCH` | لودر تاریخِ بی‌معنا می‌ساخت و **سکوت** می‌کرد ⇒ پوشش ~صفر |
| `BUG-S312RULE` | قانونِ `S312` را **حدس زدم** ⇒ پنجرهٔ ۱۰ روزه به‌جای ۳ |
| `BUG-UNIONMISS` | `S431` در اجتماع نبود |
| `BUG-DAYSET` | فقط **شمارشِ** روزها ذخیره می‌شد نه خودِ روزها ⇒ اجتماعِ تهی، **بدون خطا** |
| `BUG-PERMK` | `perm_k` = اندازهٔ نمونه به‌جای تعدادِ جای‌گشت ⇒ در یک بازو تصادفاً ۶۳۲>۵۰۰ و `H3` «معلوم» با فقط **۴۰** جای‌گشت |
| `BUG-PIPGUESS` | pipِ طلا ۰.۰۱ حدس زده شد (درست ۰.۱۰) — فقط چون نتیجه **محال** بود گیر افتاد |

**الگوی ۱ (سه مورد):** بازسازیِ مقدار از حافظه به‌جای خواندن از منبع ⇒
**قاعده: هر ثابتی که در موتور زندگی می‌کند باید از موتور خوانده شود.**
**الگوی ۲ (سه مورد، خطرناک‌تر):** باگ **بی‌صدا** بود و نتیجه‌اش **دقیقاً به
نفعِ فرضیهٔ من**.

### ۱۱. حکم و آنچه باقی می‌ماند
> 🔴 **`REJECT` · RQS2 = ۲۳.۹** · مرگِ ابدی (سه بازو، همه از تشخیص)

**این REJECT روی «سیگنال ندارد» نیست** — نامتقارنیِ جهتیِ ۵۲.۹pip ثابت می‌کند
`SoS` چیزی واقعی روی طلا-H1 می‌بیند. **روی «سیگنالش به اندازهٔ کافی قوی نیست
که از تاریخچهٔ جست‌وجوی خودش جان به در ببرد» است.** دو ادعای علمیِ کاملاً
متفاوت.

📄 `results/S435_SoSBrooksCh19Revival_Xauusd_H1_rqs2_23_REJECT.md`

---

<a name="e-17"></a>
## E-17 · `S436` — احیای `S214` (Brooks فصل ۱۱): کشفِ **دیوارِ توان**

> **حکم:** 🔴 `REJECT` · **RQS2 = ۱۴.۳** · مرگِ ابدی
> **مدرکِ رسمی:** `results/S436_BrooksTrendBarRunCalendar_Xauusd_M5M15M30H1_rqs2_14_REJECT.md`
> **گام‌ها:** ۱۰۴ → ۱۲۴ · هر چهار کارتِ طلا · `n_trials=1930` ⇒ سدِ z ≈ ۳.۸۹

### ۱. چرا این نامزد و نه آن دو

سه لایه در ردهٔ نامزدی بودند. دو تا **قبل از** آزمون رد شدند، با دلیلِ ساختاری:

| نامزد | هندسه | چرا کنار گذاشته شد |
|---|---|---|
| `S225` | `SL=300 / TP=60` | نسبتِ ۵:۱ **علیهِ** معامله؛ PF واقعی ۱.۰۸ **پس از** ۸۰.۸٪ برد |
| `S221` | `SL=200 / TP=40` | نسبتِ ۷.۵:۱ علیه معامله |
| **`S214`** ✅ | `SL=150 / TP=300` | `RR=2.0` ⇒ سربه‌سرِ هزینه‌دار فقط **۳۴.۰۷٪** |

هر دو ردشده محصولِ قانونِ منسوخِ «WR بالا به هر قیمتی» بودند = **اشتباهِ
رایجِ ۸**. انتخابِ `S214` مصداقِ **قانونِ حفظِ بودجه** است: به‌جای بالا بردنِ
WR با فیلتر، نقطهٔ سربه‌سر پایین آورده شده.

> 🎓 **درسِ ردهٔ نامزدی:** ردهٔ `POOL-READY` «۳۸ فرصت» تبلیغ می‌کرد. پس از
> حذفِ تکراری‌ها (۳۱٪)، لایه‌های همین‌الان‌زنده، و خانوادهٔ `SoS` (که در
> `S435` مرد) ⇒ فقط **۵** نامزدِ واقعی. عددِ فهرست، عددِ فرصت نیست.

### ۲. تصحیحِ پیش‌ثبت — قابِ تقویمی دکور نبود

در گام ۱۰۸ پیش‌ثبتِ خودم را **قبل از اجرا** تصحیح کردم. نوشته بودم نسخهٔ
«بدونِ قابِ تقویمی» آزموده نشده؛ کدِ ماژولِ خواهر را خواندم و دیدم آن نسخه
قبلاً آزموده و رد شده بود — به‌دلیلِ **همپوشانیِ ۶۳.۸٪**، نه کمبودِ نمونه.
قابِ تقویمی **مکانیزمِ استقلال** بود، نه تزئین.

### ۳. قانونِ MTF — اجرا شد، و سه کارت «اندازه‌گیری‌ناپذیر» ماند نه «مردود»

| کارت | n | وضعیت |
|---|---|---|
| M5 | **۵۳** | داوری‌شده ⇒ REJECT · RQS2 ۱۴.۳ |
| M15 | ۲۵ | ⛔ قیدِ ۲ ⇒ MEASUREMENT-LIMITED |
| M30 | ۲۵ | ⛔ قیدِ ۲ ⇒ MEASUREMENT-LIMITED |
| H1 | ۱۸ | ⛔ قیدِ ۲ ⇒ MEASUREMENT-LIMITED |

قیدِ ۲ عمداً زیرِ ۳۰ معامله **حکم نمی‌دهد**، تا کارتِ اندازه‌گیری‌ناپذیر
بی‌سروصدا به نتیجهٔ منفی تبدیل نشود. «نمونهٔ ناکافی روی سه کارت» و «لایه روی
سه کارت شکست خورد» دو یافتهٔ متفاوت‌اند.

### ۴. 🎓 قانونِ نو: **رقیق‌شدنِ رشته با تجمیعِ میله**

تشخیصِ اولم غلط بود و خودم گرفتمش (گام ۱۱۶). گفتم «`max_hold=96` روی M30 یعنی
۴۸ ساعت ⇒ مسدودسازی». سنجیدم:

| | M5 | M30 |
|---|---|---|
| سیگنال/سال | ۱۸۹ | ۱۴ |
| نرخِ تبدیلِ سیگنال→معامله | ۱۰.۰٪ | **۱۱.۳٪** |

نرخِ تبدیل **یکسان** ⇒ مسدودسازی علت نیست. علتِ واقعی: فیلتر به **رشتهٔ
چهار میلهٔ روندیِ پیاپی** نیاز دارد. روی M5 این یک ساختارِ ۲۰ دقیقه‌ای است؛
روی H1 یک ساختارِ **۴ ساعته**. هر میلهٔ بزرگ‌تر بازگشت‌به‌میانگینِ بیشتری در
خود جمع می‌کند ⇒ رشته نادرتر. H1: ۷۲۹ میله در ۱۵.۵ سال = ۴۷/سال، در برابرِ
M5 با ۱٬۴۱۱/سال ⇒ **ضریبِ ۳۰**.

> **قابلِ انتقال به مأموریت‌های بعدی:** هر فیلترِ «رشتهٔ میلهٔ روندیِ Brooks»
> با همین نرخ رقیق می‌شود ⇒ احیای سیگنال‌های **رشته‌محور** را به `M1`/`M5`
> محدود کنید، به‌جای جاروی روتین روی همهٔ کارت‌ها.

### ۵. 🎓 سندِ اصلیِ `S214` نتیجه‌ای گرفته بود که داده‌اش اجازه نمی‌داد

سند نوشته بود «منبعِ سود قطعاً فیلترِ مومنتوم است». ولی هر سه سلولِ
`ablation` آن **داخلِ** پنجرهٔ تقویمی بود. سلولِ چهارم — بیرونِ پنجره —
هرگز اجرا نشد. آن سلول **بازوی `A1` من** است، و علامت را برمی‌گرداند:

| بازو | قید | n | لیفت | z | RQS2 |
|---|---|---|---|---|---|
| **B0** | تقویم + ساعت + مومنتوم | ۵۳ | **+۳.۱۷** | ۰.۴۶ | ۱۴.۳ |
| A1 | ساعت + مومنتوم (بدونِ تقویم) | ۳۱۲ | **−۲.۲۶** | −۰.۸۰ | ۶.۹ |
| A2 | فقط مومنتوم | ۳۶۶ | −۱.۰۵ | −۰.۴۰ | ۹.۸ |
| A3 | A1 + TP=۲۳۳ (از MFE) | ۳۲۳ | −۲.۲۵ | −۰.۸۱ | ۳.۴ |

⇒ منبعِ لبه **قابِ تقویمی** است، نه مومنتوم. انتسابِ سندِ اصلی نادرست بود.

### ۶. بندِ سومِ قانونِ همپوشانی — و یافته‌ای با علامتِ برعکس

پوششِ `B0` روی لایهٔ زندهٔ `S355`: **۵ روز از ۴۵** (۱۱.۱٪).

| نسخه | n | WR | لیفت | z | PF | خالص | RQS2 |
|---|---|---|---|---|---|---|---|
| `BASE` | ۴۷ | ۷۲.۳۴ | +۲۰.۲۵ | ۲.۷۸ | ۳.۹۵۱ | \$۲۴۶۹.۰ | ۲۶.۰ |
| `CONFIRM` | ۵ | — | — | — | — | — | ⛔ n<30 |
| `VETO` | ۴۲ | ۷۱.۴۳ | +۱۹.۶۱ | ۲.۵۴ | ۴.۱۳۸ | \$۲۱۳۳.۲ | ۲۵.۳ |

معیارِ **سه‌شرطیِ پیش‌ثبت‌شده** (RQS2↑ **و** n≥۳۰ **و** لیفت↑) هر دو جهت را رد
می‌کند. ولی ترکیبِ آنچه `VETO` حذف کرد تشخیصی است:

```
حذف‌شده: ۵ معامله = ۴ برنده + ۱ بازنده
WR بخشِ حذف‌شده   = ۸۰.۰٪      (کلِ لایه: ۷۲.۳۴٪)
انتظارِ بخشِ حذف‌شده = $۶۷.۲     (کلِ لایه: $۵۲.۵)
```

فرضیه‌ام این بود که روزهای نامزد رژیمِ **بد** برای `S355` است. عکسش درست
بود: آن روزها **بهترین** روزهای `S355` بودند. ⇒ جهتِ `CONFIRM` علامتِ درست
دارد ولی با n=۵ توانِ آماری‌اش صفر است.

> ⚠️ **دامِ PF — اشتباهِ رایجِ ۸ با لباسِ تازه.** `VETO` نسبتِ سود را از
> ۳.۹۵ به **۴.۱۴** بالا برد و دراوداون را از ۱.۹۸٪ به ۱.۸۳٪ پایین آورد. اگر
> فقط این دو گزارش می‌شد، «بهبود» بود. ولی سودِ خالص **\$۳۳۵.۸ کم شد** و لیفت
> و z هر دو افت کردند — چون چهار برندهٔ کوچک و یک بازندهٔ بزرگ حذف شد. یعنی
> بهبودِ یک **نسبت** با کوچک‌کردنِ **مخرج**، نه با افزودنِ لبه. اشتباهِ رایجِ ۸
> فقط دربارهٔ WR نیست؛ **هر نسبتی** می‌تواند همین ترفند را بپوشد. شرطِ سومِ
> معیار (لیفت↑) دقیقاً همین را گرفت — توجیهِ اینکه چرا سه شرط لازم بود نه یکی.

### ۷. 🎓 کشفِ اصلی: **دیوارِ توان** — وقتی لبه واقعی است ولی اثبات‌ناپذیر

دروازه‌های شکست‌خوردهٔ `B0`/M5: `H3` · `H5` · `H7` · `H8` — **صفر دروازهٔ
نامعلوم** ⇒ داوری همگراست و شکست، اندازه‌گیری است نه کمبودِ جای‌گشت.

`H3` دقیقاً **دو** راهِ فرار دارد. هر دو حسابی بسته شدند:

**راهِ الف — نمونهٔ بیشتر.** چون `z ∝ √n`:
```
n لازم = ۵۳ × (۳.۰۹ / ۰.۴۶)² = ۲٬۳۹۲ معامله
کارتِ M5: ۱۸.۹ معامله/سال ⇒ نیاز به ۱۲۶ سالِ داده   (موجود: ۲.۸ سال)
```

**راهِ ب — لبهٔ قوی‌تر.** چون `z ∝ lift`:
```
lift لازم = ۳.۱۷ × ۳.۰۹ / ۰.۴۶ = ۲۱.۳pp  ⇒  WR باید ۶۹.۱٪ شود
یعنی ۶.۷ برابرِ لبهٔ فعلی
```

و **سه بازوی بهبود آزموده شد و هر سه لبه را بدتر کرد**: حذفِ هر قید علامتِ
لیفت را منفی می‌کند، افزودنِ هر قید نمونه را زیرِ ۳۰ می‌برد. `H8` هم
مستقلاً شکسته است (بازیافت ۱.۳۲ در برابرِ کفِ ۳.۰) که با دراوداونِ
از-پیش-پایینِ ۴.۸۹٪ یعنی سود باید ۲.۳ برابر شود — همان مسئلهٔ لبه در لباسِ دیگر.

> **این REJECT روی «لبه ندارد» نیست.** `B0` تنها بازو با لیفتِ مثبت است و
> هندسه‌اش سالم. روی این است که **خانوادهٔ سیگنال ذاتاً کم‌فرکانس است**، پس
> لبه‌اش — حتی اگر کاملاً واقعی باشد — در افقِ دادهٔ موجود اثبات‌ناپذیر است.
> این را **دیوارِ توان** می‌نامم و عمداً از «نبودِ لبه» جدا نگه می‌دارم؛
> یکی‌گرفتنشان درسِ غلطی به خوانندهٔ بعدی می‌دهد.
>
> **نسبتِ خانوادگی با `E-16`:** در `S435` مسئله «سیگنال هست ولی از تاریخچهٔ
> جست‌وجوی خودش جان به در نمی‌برد» بود. اینجا مسئله «سیگنال هست ولی به
> اندازهٔ کافی **تکرار نمی‌شود** که سنجیده شود». دو شکستِ متفاوت با ظاهرِ یکسان.

### ۸. پنج نقصِ بی‌صدا در ابزارِ خودم

| کد | نقص | چرا خطرناک بود |
|---|---|---|
| `BUG-LMPATH` | `local mobile/` با فاصله؛ واقعی `local-mobile/` | فهرستِ لایه‌های زنده **خالی** بازگشت بی‌خطا ⇒ صدرِ نامزدها `S355` می‌شد که همین‌الان زنده است |
| `BUG-LPSBIMPORT` | `CENTRAL` از ماژولِ غلط | اگر ماژولِ غلط تصادفاً همان نام را داشت، دو ابزار **دو لایهٔ متفاوت** می‌ساختند بی‌خطا |
| `BUG-SCOREKEY` | `res.get('score')` به‌جای `rqs2_score` | `None > None` همیشه `False` ⇒ **هر** فیلتری «بی‌فایده» می‌شد |
| `BUG-DAYDTYPE` | `np.isin(datetime64, list[Timestamp])` | تطبیقِ صفرِ بی‌صدا ⇒ `CONFIRM` تهی و `VETO` **کلِ** لایه (n=۴۷ دقیقاً برابرِ BASE) ⇒ هر دو جهتِ فیلتر جعلی |
| `BUG-POOLCOUNT` | «۳۸ فرصت» در ردهٔ POOL-READY | در واقع **۵** نامزدِ واقعی |

**گاردِ نو (گام ۱۲۱):** `CONFIRM` و `VETO` ذاتاً یک **افراز** از `BASE`اند، پس
جمعِ میله‌هایشان **باید** برابرِ `BASE` باشد. ابزار حالا این اتحاد را می‌سنجد
و در صورتِ شکست با کدِ ۳ **متوقف** می‌شود. اجرای موفق: `۶۰ = ۹ + ۵۱` ✅.

> 🎓 **اصلاحِ فهمِ خودم دربارهٔ الگوی باگ‌ها.** هشت نقصِ قبلیِ این مأموریت
> نتیجه‌ای **به نفعِ** فرضیه‌ام می‌دادند و من آن را نشانهٔ سوگیری خواندم.
> `BUG-SCOREKEY` جهتش **علیهِ** من بود و می‌توانست پرونده را زودتر ببندد.
> ⇒ توضیحِ قبلی‌ام ناقص بود: این نقص‌ها اساساً محصولِ **بازسازیِ مقدار از
> حافظه به‌جای خواندن از نمونهٔ کارآمد**اند. جهتِ مطلوبِ قبلی‌ها تا حدی
> **انتخابی** بود — نقصی که نتیجهٔ نامعقول بدهد بررسی می‌شود، نقصی که نتیجهٔ
> موردِ انتظار بدهد نه. قاعدهٔ عملیاتی باریک‌تر و مکانیکی‌تر است: *وقتی
> ابزارِ کارآمدی در همین مخزن خروجیِ موتور را به رکورد نگاشت می‌کند، آن نگاشت
> را **کپی** کن، بازنویسی نکن.*
>
> **و نکتهٔ روش‌شناختیِ مشترک:** هر دو باگِ کشنده (`DAYDTYPE`, `SCOREKEY`) با
> **مقایسهٔ دو کمیتی که باید توافق کنند** پیدا شدند — حسابِ روز در برابرِ
> حسابِ معامله، و نه با نگاه‌کردن به نتیجه و پرسیدنِ «معقول است؟».

### ۹. چهار دارایی که این شکست تولید کرد

۱. **قانونِ رقیق‌شدنِ رشته** (ضریبِ ~۳۰ از M5 به H1) ⇒ محدودسازیِ دامنهٔ
   سیگنال‌های رشته‌محور به `M1`/`M5`.
۲. **تصحیحِ انتسابِ سندِ `S214`** ⇒ لبه در قابِ تقویمی است نه مومنتوم.
۳. **`S355` روزهای مرغوب دارد** (+۲۸٪ انتظار روی روزهای مشترک) — با n=۵
   اثبات‌ناپذیر، ولی جهتی ثبت‌شده برای پژوهشِ آینده.
۴. **گاردِ افراز** ⇒ خانوادهٔ کاملِ نقص‌های ماسک از «بی‌صدا» به «کشنده».

📄 `results/S436_BrooksTrendBarRunCalendar_Xauusd_M5M15M30H1_rqs2_14_REJECT.md`
📄 `results/_s436_filter/CLAUSE3_ANALYSIS.md`

---

## وضعیتِ جاری و گامِ بعد
- ✅ `S430` — `REJECT` (مرگِ ابدی، حسابی اثبات شد)
- ✅ `S431` — `ACCEPT` `RQS2=93.9` (نخستین احیای موفقِ مأموریت) · پورت کامل
- ✅ `S432` — `ACCEPT` `RQS2=84.7` (دومین احیا) · پورت کامل
- ✅ **اصلاحِ باگ در لایهٔ زندهٔ `M30`** (`BUG-S312-FILTGATE`) — فیلترِ اعلام‌شده
  حالا واقعاً اعمال می‌شود
- ✅ `S433` — **بهداشتِ زیرساخت** (نه استراتژی): بازنشستگیِ رابطِ متروکِ
  `Pyodide` + شش باگ در **ابزارِ خودم** (`BUG-LASTLAYER` · `BUG-EPOCHPARSE` ×۲ ·
  `BUG-KBCHARS` · `BUG-TMPCOLLISION` · `BUG-TMPLEAK`) + یک تشخیصِ اشتباهِ
  صادقانه‌ثبت‌شده (`E-13`)
- ✅ آزمون‌های نگهبان: `_smoke_card_inventory` (۶ بررسی · ۵ کارت · ۱۰ اتصال) +
  `_parity_s431_wiring` + `_parity_s432_wiring` — همه `PASS`، **در اجرای همزمان**
- ✅ `S434` — **`REJECT` · `RQS2 = 18.7`** (مرگِ ابدی، **پنج** مسیر سنجیده شد) ·
  `E-15`. سه یافتهٔ علمیِ مثبت (قانونِ MTF ۱۹/۱۹ · شاهدِ منفیِ یورو · پنجرهٔ
  فرارِ فیلتر) + هشت باگ در ابزارِ خودم + **سیاستِ ذخیره‌سازیِ دادهٔ کاربر**
  (`gzip` ۳.۷× ⇒ عبور از سدِ ۱۰۰MB گیت‌هاب؛ **شش بار** در ریستِ واقعی آزموده شد)
- ✅ `S435` — **`REJECT` · `RQS2 = 23.9`** (مرگِ ابدی، **سه** بازوی بهبود، همه
  از تشخیصِ سنجیده نه جست‌وجو) · `E-16`. کشفِ روش‌شناختیِ «**مسیر در برابر
  مقصد**» (مداخلهٔ وابسته به مسیر نباید با آمارِ مستقل از مسیر اندازه شود) +
  `BUG-NULLUNCOND` در سومین لباس که **۱۷ برابر** خطا را جلوگیری کرد + شش باگ
  در ابزارِ خودم + ثبتِ صریحِ **شکستنِ تعهدِ خودم** با دلیل
- 📊 **بازهٔ من `S430–S439` تا اینجا:** ۲ ACCEPT (`S431` ۹۳.۹ · `S432` ۸۴.۷) ·
  ۳ REJECT با مرگِ ابدیِ اثبات‌شده (`S430` · `S434` · `S435`) · ۱ بهداشتِ
  زیرساخت (`S433`)
- ⏳ **گامِ بعد:** `S436` — نامزدِ چهارمِ احیا. **دو توصیهٔ مشخص از `S435`:**
  (الف) `SoS` روی کارتی که باتریِ `S202` نگشته (`M30`/`H4`/`EURUSD`) — آنجا
  `n_trials` از صفر شروع می‌شود، **ولی حتی z=۱.۹۵ هم پاس نمی‌شود** پس آزمونِ
  واقعی است نه دور زدنِ سد؛ (ب) هر مداخلهٔ **وابسته به مسیر** (سربه‌سر/تریلینگ)
  باید با آمارِ **وابسته به مسیر** اندازه شود.

> 🎁 **دو دارایی که `S434` برای مأموریت‌های بعدی باقی گذاشت:**
> ۱) **دادهٔ کاملِ ۱۵.۶ سالهٔ طلا در ۱۹ تایم‌فریم**، فشرده و `SHA256`-تأییدشده در
> خودِ مخزن — بدون نیاز به دانلودِ مجدد (`python3 tools/s434_fetch_mt5_full.py --unpack`).
> ۲) **بانکِ ۴۰۱ اندیکاتور با نمرهٔ AUC سنجیده‌شده** برای LONGهای شبانهٔ طلا —
> هیچ AI بعدی لازم نیست آن محاسبهٔ ۲۰۵ ثانیه‌ای را تکرار کند.

---
_`MISSION_4` · معیار: `engine/rqs2.py` **v2.6** · بازهٔ من: `S430–S449`_
