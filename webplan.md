# 🏗️ webplan.md — معماریِ ماژولارِ ROS2-گونه + موتورِ کشف/احیای استراتژی

> **وضعیت:** طرحِ تصویب‌شده (نشستِ معماری، نسخهٔ ۲ — با گره‌های هوشمند). ساخته‌شده **پیش از پیاده‌سازی**.
> **هدف دوگانه:**
> 1. **معماری:** تبدیلِ ساختارِ «نیمه‌ماژولارِ به‌هم‌ریخته» به معماریِ **کاملاً ماژولار** با **قراردادهای پیامِ ثابت** — تا هیچ AI بعدی نتواند ساختار را به‌هم بریزد.
> 2. **تحقیق (مهم‌تر!):** معماری صرفاً «تمیزکاری» نیست؛ باید خودش به یک **موتورِ کشف و احیای لایه** تبدیل شود.
>    درسِ واقعیِ ROS2 «pub/sub و گره‌های مستقلی که رفتارِ جمعیِ نوظهور می‌سازند» است، نه فقط پوشه‌بندی.
> **اولویت:** طبقِ User Note، `local-mobile` (اجرا روی Termux) مهم‌تر از سایت است و باید همیشه هم‌گام بماند.

---

## ۰. 💡 بارشِ فکری — ۹ ایده (و اینکه کدام‌ها به معماری تزریق شدند)

طرحِ نسخهٔ ۱ فقط «تمیزکاری» بود. نسخهٔ ۲ این ۹ ایده را افزود؛ **دو تای آن‌ها ماهیتِ طرح را از
«بازآرایی» به «موتورِ احیا» تغییر می‌دهند** (شورای لایه‌ها + رادارِ رژیم):

| # | ایده | چه می‌کند | ارزشِ تحقیقاتی | حکم |
|---|---|---|---|---|
| ۱ | **EventBus (pub/sub)** | گره‌ها به‌جای فراخوانیِ خطی، رویداد منتشر/مصرف می‌کنند؛ `bar.closed@XAU.M5` → مشترکان واکنش می‌دهند. پلِ بین **شبیه‌سازِ رویداد-محور** و **سایتِ زنده**: bar-replay = live-tick. | ⭐⭐⭐⭐⭐ | **هستهٔ معماری (P1)** |
| ۲ | **🏛️ شورای لایه‌ها (Layer Council)** | همهٔ لایه‌های یک کارت رأی می‌دهند؛ **اجماع/اکثریت/تضاد** ⇒ سیگنال با اطمینان و لاتِ متغیر. **«فیلترِ اجماع» یک راهِ کاملاً نوِ بهبودِ RQS+/احیاست** (اگر لایهٔ سوختهٔ A فقط وقتی B تأیید می‌کند WR بالا می‌رود). | ⭐⭐⭐⭐⭐ | **برجستهٔ طرح (P4.5)** |
| ۳ | **دفترِ RQS زنده (Live RQS Ledger)** | نتیجهٔ واقعیِ معاملاتِ کاربر ذخیره ⇒ RQS+ زندهٔ هر لایه محاسبه؛ اگر لایه‌ای افت کرد، سایت خودش موقتاً بایگانی‌اش می‌کند. سایتِ **یادگیرنده** نه عکسِ ثابت. | ⭐⭐⭐⭐ | فاز میانی (P7) |
| ۴ | **🛰️ رادارِ رژیم (Regime Radar)** | گرهِ مستقلِ تشخیصِ رژیم (روند/رنج/نوسان) که به هر کارت می‌گوید **همین‌الان کدام لایه‌ها را روشن/خاموش کن**. پیاده‌سازیِ «قانونِ شاید هیچ‌چیز ثابت نیست» در سطحِ معماری. لایهٔ سوخته شاید فقط «در رژیمِ درستش» زنده باشد. | ⭐⭐⭐⭐⭐ | **زودهنگام (P3.5)** |
| ۵ | **ماشینِ زمان (Replay slider)** | چون تاریخچه ذخیره می‌شود، کاربر عقب می‌رود و سیگنالِ گذشته را می‌بیند. ابزارِ اعتمادسازی + دیباگِ چشمیِ تحقیق. | ⭐⭐⭐ | پس از ذخیره‌سازی (P6+) |
| ۶ | **کاوشگرِ اندیکاتور (Indicator Scanner)** | حالتِ پژوهشیِ پس‌زمینه: همبستگیِ زندهٔ ده‌ها اندیکاتور با حرکتِ بعدیِ قیمت روی دادهٔ ذخیره‌شده ⇒ کاندیدِ فیلترِ احیا به AI گزارش می‌دهد. مستقیماً علیهِ اشتباهِ رایجِ #۳. | ⭐⭐⭐⭐⭐ | ابزارِ پژوهش (P8) |
| ۷ | **Heartbeat گره‌ها** | اگر قیمتِ زنده >۹۰ ثانیه کهنه شد، نوارِ قرمز «دادهٔ زنده قطع — سیگنال منجمد». جلوگیری از معامله روی قیمتِ کهنه. | ⭐⭐⭐ (ایمنی) | ارزان و حیاتی (P2) |
| ۸ | **قراردادِ پیامِ نسخه‌دار (v1)** | هر قرارداد نسخه دارد؛ تغییر ⇒ ساختِ `v2`+adapter. قفلِ فنیِ ضدِ به‌هم‌ریختگی. | ⭐⭐⭐⭐ | در P0/P1 |
| ۹ | **کدِ Isomorphic (Backtest≡Live)** | منطقِ لایه یک‌بار نوشته شود و هم شبیه‌ساز هم سایت همان را بخوانند ⇒ پایانِ باگِ «دو پیاده‌سازیِ ناسازگار». | ⭐⭐⭐⭐⭐ | هدفِ بلندمدت (پیامدِ #۱ و #۲) |

> **جانِ کلام:** ایده‌های #۲ و #۴، معماری را به ابزارِ مستقیمِ **هدفِ اصلیِ پروژه (احیای لایه‌های سوخته)**
> گره می‌زنند — یعنی این بازآرایی، سرمایه‌گذاری روی خودِ تحقیق است، نه انحراف از آن.

---

## ۱. چرا این بازآرایی؟ (تشخیصِ درد)

معماریِ فعلی **نیمه‌ماژولار** است:

- ✅ **لایهٔ استراتژی خوب ماژولار شده:** `web_tool/src/strategy_registry.ts` با `CARD_LAYERS` + `runCard`.
- ❌ **ولی سه بخشِ دیگر در `index.tsx` (۷۶۸ خط) در هم تنیده‌اند:**
  - **قیمت:** `fetchGold`, `rebaseFuturesToSpot`, `mergeLiveQuote`, `closedBars`, `aggregateCandles`, `/api/proxy`, `/api/candles`, `/api/spot(s)`.
  - **اندیکاتور:** پراکنده در `signal.ts`, `indicators.ts`, `features.ts` و داخلِ خودِ فایل‌های لایه.
  - **ظاهر:** HTML به‌صورتِ inline string + `public/static/app.js` (+ منطقِ تصمیمِ نشتی‌کرده به فرانت‌اند).

**نتیجه:** هر نشست یک تکه به `index.tsx`/`app.js` افزوده؛ مرزها محو شده‌اند («big ball of mud»).

**راه‌حلِ ریشه‌ای (درسِ ROS2):** مرزهای بین گره‌ها را با **نوع دادهٔ ثابت و امضاشده** قفل می‌کنیم.
وقتی هر مرز یک «قرارداد پیام» تغییرناپذیر داشته باشد، AI بعدی **مجبور** است داخلِ گرهٔ خودش بماند.

---

## ۲. نگاشتِ ROS2 → پروژه

| مفهومِ ROS2 | معادل در پروژهٔ ما |
|---|---|
| Node (گرهٔ مستقل) | ۴ ماژول: قیمت · اندیکاتور · بستر · ظاهر |
| Topic / Message | جریانِ دادهٔ استاندارد + نوعِ ثابت بینِ گره‌ها |
| Message contract | `Candle`, `PriceFeed`, `IndicatorSnapshot`, `LayerSignal`, `CardDecision` |
| Composability | افزودن/حذفِ یک گره یا لایه بدونِ شکستنِ بقیه |

---

## ۳. چهار گره و قراردادهای پیامِ ثابت

### 🟦 گره ۱ — Price Module (`price/`)
**مسئولیت:** تنها منبعِ حقیقتِ قیمت. دریافتِ زنده (failover چند-API) + **ذخیره‌سازیِ تاریخچه با سقفِ حجمِ کاربر**.

- **failover:** ترتیبِ منابع (Yahoo GC=F → پشتیبان‌ها)؛ اگر یکی قطع شد، بعدی.
- **ذخیره‌سازیِ محلی (ویژگیِ نو):** ring-buffer روی دیسکِ Termux، partition بر اساسِ `asset-TF`.
  - سقفِ حجم: پیش‌فرض ۲۰۰MB، قابلِ تنظیم توسطِ کاربر، با **کفِ حداقل** (مثلاً ۲۰MB).
  - وقتی به سقف رسید ⇒ حذفِ قدیمی‌ترین کندل‌ها (FIFO).
  - فرمتِ ذخیره: باینریِ فشرده (Float32/Int) برای بیشینه‌سازیِ کندل در حجمِ داده‌شده.
  - **gap-fill:** موقعِ روشن‌شدنِ سرور، حفرهٔ زمانیِ خاموشی از API پر می‌شود ⇒ تاریخچهٔ پیوسته.
- **دو backendِ ذخیره‌سازی (مهم):** رابطِ `HistoryStore` با دو پیاده‌سازی:
  - `DiskHistoryStore` (Termux/Node — دسترسی به فایل‌سیستم).
  - `MemoryHistoryStore` (Cloudflare — بدونِ فایل‌سیستم؛ سازگاریِ رو به آینده).
- **خروجی (قراردادِ ثابت):**
  ```ts
  interface Candle { time:number; open:number; high:number; low:number; close:number; volume:number }
  interface PriceFeed {
    asset:string; tf:string;
    livePrice:number; liveAgeSec:number; source:string;
    candles:Candle[];        // تاریخچهٔ بسته‌شده (تا سقفِ حجم)
    lastClosedTime:number;
  }
  ```

### 🟩 گره ۲ — Indicator Module (`indicators/`)
**مسئولیت:** محاسبهٔ هر اندیکاتوری که لایه‌ها می‌خواهند — با **رجیستریِ اندیکاتور** (مشابهِ رجیستریِ استراتژی).

- **رجیستریِ اندیکاتور:** هر اندیکاتور یک `IndicatorDef` با نام + پارامتر + تابعِ compute.
- **تنبل + کش‌شده (lazy + memoized):** فقط اندیکاتورهای موردنیازِ لایه‌های فعالِ همان کارت محاسبه می‌شوند؛
  اگر دو لایه EMA200 بخواهند، **یک‌بار** حساب می‌شود (کلیدِ کش = نام+پارامتر+آخرین‌زمانِ کندل).
  → مبارزهٔ مستقیم با کندیِ گوشیِ ضعیف.
- **ارزشِ تحقیقاتی:** خانهٔ رسمیِ اندیکاتورهای کمیاب (Alligator, Vortex, Kaufman-ER, Ichimoku, …) —
  یک‌بار نوشته، همه‌جا استفاده. مستقیماً علیهِ «اشتباهِ رایجِ ۳» (غفلت از اندیکاتورهای پیچیده).
- **ورودی:** `PriceFeed`. **خروجی (قراردادِ ثابت):**
  ```ts
  interface IndicatorSnapshot {
    get(name:string, params?:Record<string,number>): number | number[] | null
    // مقادیرِ آمادهٔ پرکاربرد برای دسترسیِ سریع:
    price:number; atr:number; ema50:number; ema200:number; rsi14:number; adx:number; /* … */
  }
  ```

### 🟨 گره ۳ — Strategy Runtime / بستر (`runtime/`)
**مسئولیت:** بسترِ اجرای فایل‌های لایه (همان `strategy_registry` فعلی، تمیز و رسمی‌شده) + **تثبیتِ سیگنال (latch)**.

- هر لایه یک فایلِ مستقل با امضای ثابت: ورودی `PriceFeed`+`IndicatorSnapshot`+context، خروجی `LayerSignal`.
- **تثبیتِ سیگنال داخلِ این گره رسمی می‌شود** (نه در فرانت‌اند) — چون تثبیت یک تصمیمِ **منطقی** است نه ظاهری.
- `runCard`: اجرای همهٔ لایه‌های یک کارت، انتخابِ تصمیمِ اصلی (ENTRY>APPROACHING>NEUTRAL)، جمعِ لایه‌های هم‌جهت.
- **افزودن/حذفِ لایه = فقط یک ردیف در رجیستری** (اصلِ ماژولار حفظ می‌شود).
- **خروجی (قراردادِ ثابت):**
  ```ts
  type SignalState = 'NEUTRAL' | 'APPROACHING' | 'ENTRY'
  interface LayerSignal {
    code:string; name:string; kind:string;
    state:SignalState; direction?:'LONG'|'SHORT';
    entry?:number; tp?:number; sl?:number; rr?:number; probability?:number;
    reason:string; confirmations?:string[]; indicators?:{name:string;value:string;status:string}[];
    manage?: { style:string; note:string };      // پلنِ مدیریت (TP/SL متحرک و…)
    timeGate?: { layerCode:string; label:string; endHourUtc?:number; windowOpen?:boolean; /*…*/ }
  }
  interface CardDecision {                        // خروجیِ runCard برای یک کارت
    primary: LayerSignal
    otherLayers: LayerSignal[]                    // لایه‌های هم‌جهتِ تأییدکننده
    cardTimeGates?: NonNullable<LayerSignal['timeGate']>[]
  }
  ```

### 🛰️ گره ۵ — Regime Radar (`regime/`) — [ایدهٔ #۴]
**مسئولیت:** گرهِ مستقلِ تشخیصِ رژیمِ بازار که همهٔ کارت‌ها مشترکِ آن‌اند و **مجموعهٔ لایه‌های فعال را شناور می‌کند.**

- ورودی: `IndicatorSnapshot` (ADX, ATR%, شیبِ EMA, پهنای بولینگر، …).
- خروجی (قراردادِ ثابت):
  ```ts
  type Regime = 'TREND_UP' | 'TREND_DOWN' | 'RANGE' | 'VOLATILE' | 'QUIET'
  interface RegimeInfo { regime:Regime; strength:number /*0..1*/; note:string;
    enabledKinds:string[] /* کدام نوعِ لایه اجازهٔ روشن‌شدن دارد */ }
  ```
- **کاربردِ نبوغ‌آمیز:** در رژیمِ رونددار، لایه‌های trend-follow (S321) روشن و لایه‌های fade (S324/S328/S330) خاموش؛
  در رنج برعکس. این «قانونِ شاید هیچ‌چیز ثابت نیست» را در سطحِ معماری پیاده می‌کند: **لایهٔ سوخته شاید فقط
  «در رژیمِ درستش» زنده باشد** ⇒ یک مسیرِ احیای تازه که در فازِ بعدیِ تحقیق آزموده می‌شود.

### 🏛️ گره ۶ — Layer Council (`council/`) — [ایدهٔ #۲، برجستهٔ طرح]
**مسئولیت:** جمعِ رأیِ همهٔ لایه‌های یک کارت و تولیدِ **تصمیمِ اجماعی** (به‌جای انتخابِ ساده‌ٔ اولین سیگنال).

- ورودی: `LayerSignal[]` همان کارت + `RegimeInfo`.
- منطقِ رأی‌گیری:
  - **اجماعِ کامل** (همه هم‌جهت) ⇒ اطمینانِ بالا، پیشنهادِ لاتِ ×۱.۵.
  - **اکثریت** (مثلاً ۲ از ۳) ⇒ سیگنالِ عادی + نمایشِ «لایهٔ دوم هم تأیید کرد».
  - **تضاد** (یکی LONG یکی SHORT) ⇒ **خنثی + هشدارِ «بازارِ دوقطبی»** (خودْ یک فیلترِ محافظ).
- **چرا برجسته؟** این گره یک **مکانیزمِ رسمیِ احیا** است: «فیلترِ اجماعِ دو لایهٔ ناهمبسته» می‌تواند WR/RQS+ یک
  لایهٔ سوخته را بالا ببرد **بدونِ تغییرِ خودِ لایه** — دقیقاً مطابقِ «قانونِ همپوشانی/بهبود» پروژه.
- خروجی: همان `CardDecision` (میدانِ `primary` از رأیِ شورا، `otherLayers` = تأییدکننده‌ها/مخالف‌ها).

### 🟧 گره ۴ — Frontend / ظاهر (`ui/`)
**مسئولیت:** فقط **مصرفِ** `CardDecision` و نمایش. **هیچ منطقِ تصمیمی اینجا نیست.**

- کارت‌ها + چهار حالت (خنثی / نزدیک‌شدن / ورود / مدیریت).
- کارهای ریزِ ظاهری: محاسبهٔ لات (ریسک×موجودی)، درصدِ سود/زیانِ احتمالی، **ساعتِ ایران**،
  **شمارشِ معکوس**، نمایشِ «لایهٔ دوم هم همین سیگنال را تأیید کرد» (از خروجیِ شورا).
- **Heartbeat [ایدهٔ #۷]:** اگر `liveAgeSec > 90` ⇒ نوارِ قرمزِ «دادهٔ زنده قطع — سیگنال منجمد».
- **ماشینِ زمان [ایدهٔ #۵]:** اسلایدرِ replay روی تاریخچهٔ ذخیره‌شده (فازِ بعدی).
- همهٔ ویژگی‌های مثبتِ ظاهریِ فعلی حفظ می‌شوند.
- **قانونِ طراحی (User Note):** هیچ اطلاعاتِ اضافه (سودِ خالص، نتایجِ تحقیق، RQS) به کاربر نشان داده نمی‌شود.

---

## ۴. جریانِ داده (خطِ لولهٔ گره‌ها روی EventBus — ایدهٔ #۱)

```
                                   ┌───────────────► [🛰️ Regime] ──RegimeInfo──┐
                                   │                                            ▼
[Price] ──PriceFeed──► [Indicators] ──IndicatorSnapshot──► [Runtime/بستر] ──LayerSignal[]──► [🏛️ Council] ──CardDecision──► [UI]
   │ (ذخیره/بازیابی)        (کشِ تنبل)                        (تثبیتِ سیگنال)                    (رأی‌گیریِ اجماعی)      (لات/تایمر/heartbeat)
   │
   └──publish── "bar.closed@ASSET.TF" ──►  EventBus  ◄── همهٔ گره‌ها subscribe/publish
```

- هر فلش یک **قراردادِ ثابتِ نسخه‌دار (v1)** است [ایدهٔ #۸]. تغییرِ داخلِ هر گره تا قرارداد را نشکند، بقیه را دست نمی‌زند.
- **EventBus** ستونِ فقراتِ pub/sub است: همان رویدادها که سایتِ زنده منتشر می‌کند، شبیه‌سازِ backtest هم با
  bar-replay منتشر می‌کند ⇒ زمینه‌سازِ کدِ **Isomorphic** [ایدهٔ #۹] و **پلِ شبیه‌ساز↔سایت**.

---

## ۵. سازگاریِ دوگانه: Termux (Node) و Cloudflare

- تفاوتِ کلیدی: **فایل‌سیستم** فقط روی Termux/Node هست، Cloudflare ندارد.
- گرهٔ قیمت با رابطِ `HistoryStore` این تفاوت را پنهان می‌کند (Disk vs Memory).
- بقیهٔ گره‌ها محیط‌آگنوستیک‌اند (فقط Web-Fetch/محاسبه).
- باندلِ `local-mobile/app.bundle.mjs` مثلِ حالا با `build.mjs` (esbuild) ساخته می‌شود.

---

## ۶. راهبردِ اجرا: بازآراییِ تدریجیِ ایمن (Strangler Fig) — نه بازنویسی از صفر

⚠️ سایتِ فعلی **کار می‌کند** (۷ کارت با لایه‌های ACCEPTED). بازنویسیِ یک‌جا = ریسکِ رگرسیون.
پس گره‌به‌گره و با تستِ برابری (parity) پیش می‌رویم؛ بعدِ هر گام، خروجیِ `/api/decision` باید **بیت‌به‌بیت** مثلِ قبل بماند.

| فاز | کار | معیارِ پذیرش | چرخهٔ چک‌پوینت |
|---|---|---|---|
| **P0** | این `webplan.md` (نسخهٔ ۲ با گره‌های هوشمند) | تصویب | ✅ commit+push (انجام‌شده) |
| **P1** | استخراجِ `price/` از `index.tsx` + قراردادِ **نسخه‌دارِ** `PriceFeed@v1` + **EventBus** پایه | `/api/decision` خروجیِ **بیت‌به‌بیت** یکسان | ✅ **انجام‌شده** (parity تأییدشده) |
| **P2** | `HistoryStore` (Disk+Memory) + ذخیره/بازیابی + gap-fill + سقفِ حجمِ کاربر + **Heartbeat** [#۷] | تاریخچهٔ M5 روی Termux > ۵ روز؛ نوارِ قطعیِ داده کار کند | ✅ **انجام‌شده** (زیرساخت + تست ۲۴/۲۴ + endpointها زنده) |
| **P3** | استخراجِ `indicators/` + رجیستریِ اندیکاتور + کشِ تنبل + `IndicatorSnapshot@v1` | خروجیِ یکسان + سرعتِ گوشی بهتر | هر فایل: commit+push |
| **P3.5** | 🛰️ گرهِ **Regime Radar** [#۴] + قراردادِ `RegimeInfo@v1` (فعلاً فقط گزارش‌گر، بدونِ خاموش‌کردنِ لایه) | رژیم درست تشخیص داده شود؛ رفتارِ فعلی دست‌نخورده | هر فایل: commit+push |
| **P4** | رسمی‌سازیِ `runtime/` (تثبیتِ سیگنال به این گره) + `LayerSignal@v1`/`CardDecision@v1` | خروجیِ یکسان | هر فایل: commit+push |
| **P4.5** | 🏛️ گرهِ **Layer Council** [#۲] — رأی‌گیریِ اجماعی، ابتدا «سایه‌ای» (لاگ، بدونِ تغییرِ تصمیم)، سپس فعال | تضاد/اکثریت درست؛ «تأییدِ لایهٔ دوم» از شورا بیاید | هر فایل: commit+push |
| **P5** | جداسازیِ `ui/` (کشیدنِ منطقِ باقی‌مانده به گره‌ها) + اتصالِ heartbeat/شورا به ظاهر | همهٔ ویژگی‌های ظاهری سالم | هر فایل: commit+push |
| **P6** | بازساختِ باندل + تستِ Termux/سرور + هم‌گام‌سازیِ `local-mobile` + READMEها | سرورِ محلی سالم، لینکِ لوکال | هر فایل: commit+push |
| **P7** | دفترِ **RQS زنده** [#۳] (ثبتِ نتیجهٔ واقعیِ کاربر + بایگانیِ خودکارِ لایهٔ ضعیف) | RQS زنده محاسبه و نمایش داده شود | هر فایل: commit+push |
| **P8** | **کاوشگرِ اندیکاتور** [#۶] (حالتِ پژوهشیِ همبستگی روی تاریخچهٔ ذخیره‌شده — خروجی فقط برای AI/تحقیق) | گزارشِ کاندیدِ فیلترِ احیا تولید شود | هر فایل: commit+push |

**هر فاز به‌تنهایی قابلِ‌عرضه است؛ اگر نشست تمام شد، پروژه در حالتِ سالم می‌ماند.**
**فازهای P1–P6 هستهٔ ضروری‌اند؛ P3.5/P4.5/P7/P8 لایه‌های هوشمندِ ارزش‌افزوده که به تحقیق وصل‌اند.**

> **درسِ ایمنی از ایده‌ها:** گره‌های هوشمند (رژیم/شورا) ابتدا در **حالتِ سایه‌ای (shadow)** اضافه می‌شوند —
> یعنی محاسبه و لاگ می‌کنند اما تصمیمِ نهایی را عوض نمی‌کنند — تا وقتی برابری (parity) اثبات شود. سپس فعال می‌شوند.
> این «Strangler Fig» را با «feature-flag» ترکیب می‌کند و ریسکِ رگرسیون را صفر نگه می‌دارد.

---

## ۷. اصولِ تغییرناپذیر (که هر نشستِ بعدی باید رعایت کند)

1. **مرزها فقط از راهِ قراردادهای پیام** — هیچ گره‌ای داخلِ گرهِ دیگر دست نمی‌زند.
2. **فرانت‌اند بدونِ منطقِ تصمیم** — فقط مصرفِ `CardDecision`.
3. **افزودنِ لایه = یک ردیف در رجیستری** — نه دستکاریِ بستر.
4. **افزودنِ اندیکاتور = یک `IndicatorDef`** — نه کدِ پراکنده.
5. **`local-mobile` هم‌گام با هر تغییرِ منطق** (بازساختِ باندل).
6. **چرخهٔ چک‌پوینت (commit+push پس از هر فایل) بدونِ استثنا.**

---

## ۸. جمع‌بندیِ داوریِ صادقانه

**ارزشِ اجرا: ✅ بله.** درد واقعی است و در هر نشست بدتر می‌شود؛ ماژولِ ذخیره‌سازیِ قیمت یک قابلیتِ نوِ
واقعی است (رفعِ باگِ تاریخچهٔ کوتاه)؛ قراردادهای ثابت آیندهٔ پروژه را بیمه می‌کنند.
**ریسک (رگرسیون/زمان/CPU گوشی)** با راهبردِ تدریجیِ Strangler + تستِ برابری + کشِ تنبل مهار می‌شود.

> نشستِ بعدی از **P1** آغاز می‌کند: استخراجِ گرهِ قیمت با حفظِ دقیقِ رفتار.
