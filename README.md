# پروژه تحقیقاتی: الگویابی ریاضی در بازار XAUUSD

> # 🎯 قانونِ شمارهٔ ۱ پروژه (بالاترین اولویت — در همهٔ اسناد تکرار می‌شود)
> **هدفِ پروژه فقط و فقط «سودِ خالصِ بیشتر» است — نه Win-Rate.**
> از این پس Win-Rate صرفاً یک عددِ گزارشی است، نه هدف و نه قید. تعدادِ معامله در
> روز و Profit Factor هم هدف نیستند. **ما دنبالِ پول هستیم، نه آمارِ زیبا.** یک
> سیستمِ WR=۴۵٪ با R:R بالا می‌تواند سودِ خالصِ بیشتری از یک سیستمِ WR=۶۵٪ بسازد —
> و ما همان اولی را انتخاب می‌کنیم. تنها تابعِ هدفِ کلِ پروژه: **سودِ خالصِ تجمعیِ
> پس از اسپرد/کارمزد.** محصولِ نهایی یک **سایتِ دستیارِ تصمیمِ ۴-حالته** (خنثی /
> نزدیک‌شدن به سیگنال / ورود / مدیریتِ معامله) برای هر دارایی است.

> ## ⚠️ تغییرِ پارادایم (User Note 2) — بخوانید: [`PARADIGM.md`](./PARADIGM.md)
> از این پس معیارِ موفقیت **فقط «سودِ خالصِ بیشتر»** است — نه WR، نه Profit Factor،
> نه تعدادِ معامله در روز. دیگر دنبالِ نجاتِ دورهٔ رنجِ ۲۰۲۱–۲۰۲۲ نیستیم.
> محصولِ نهایی یک **سایتِ دستیارِ تصمیمِ ۴-حالته** (خنثی / نزدیک‌شدن به سیگنال /
> ورود / مدیریتِ معامله) برای هر دارایی است، با یک **Routerِ رژیم-محور** که تصمیم
> می‌گیرد «هر استراتژی در کدام محل/رژیم فعال شود». مشکل، خودِ استراتژی‌ها نیست؛
> **محلِ درستِ استفاده از آن‌هاست.** بخش‌های قدیمیِ زیر (هدفِ WR>65٪ و مثلثِ
> ناممکن) صرفاً به‌عنوانِ تاریخچهٔ تحقیق نگه داشته می‌شوند.

## هدف (نسخهٔ فعلی)
**بیشینه‌سازیِ سودِ خالصِ تجمعی** با تشخیصِ رژیم و فعال‌سازیِ استراتژیِ درست در محلِ درست.

## 🏆 برندهٔ فعلیِ پروژه (معیار: سودِ خالصِ کل = XAUUSD + EURUSD) — پرتفویِ چند-لایه: S67 (طلا M15 long) + S91 (طلا M5 اسکالپ) + S81 (طلا M30 swing) + **SHORT-MA-Confluence (طلا M15 short)** + S73 (یورو M15)

> از آنجا که معیار **فقط سودِ خالص** است (نه WR)، «برندهٔ فعلی» بر اساسِ **بیشترین
> سودِ خالصِ رو-به-جلوِ اثبات‌شده** انتخاب می‌شود — نه WR.
>
> **تعریفِ رسمیِ سودِ خالص در این پروژه = جمعِ سودِ دو ارز: XAUUSD + EURUSD.**

> ⚠️ **بازآزماییِ هزینهٔ واقعیِ حسابِ کاربر (User Note 2):** همهٔ اعداد اکنون با
> مشخصاتِ واقعیِ حسابِ کاربر محاسبه شده‌اند — **طلا اسپرد ۰.۴۰$ (۴ pip)، کمیسیون صفر؛
> EURUSD اسپرد ۱.۵ pip، کمیسیون صفر.** این جایگزینِ فرضِ خوش‌بینانهٔ قدیمی شد؛ در نتیجه
> رکوردِ رسمی از عددِ قدیمیِ +۴۴٬۴۵۸$ به عددِ واقع‌بینانهٔ **+۴۳٬۹۶۸$** به‌روز شد
> (رقمِ قدیمی با هزینهٔ غیرواقعی محاسبه شده بود).

**🥇 رکوردِ فعلی: سودِ خالصِ کل +۹۵٬۶۴۵$ = XAUUSD (S67+S91+S81+SHORT: +۸۶٬۴۲۲$) + EURUSD (S73: +۹٬۲۲۳$).**
(چرخهٔ اخیر — s116→s118، پاسخِ User Note «روندها را شماره‌گذاری کن، دقتِ شروع/پایان بده،
بفهم چطور مغزها را بهتر کنیم؛ راستی نشستِ قبل اشتباهاً با موتورِ long تست کردیم»:
ابتدا **باگِ «موتورِ long اشتباه» رفع شد** و با کنترلِ منفی اثبات شد (اختلافِ $19,831).
سپس ممیزیِ **MFE/MAE** (s117) نشان داد خروجِ قبلیِ SHORT فقط +۴.۸pip می‌گرفت درحالی‌که
MFE=۶۹.۳pip در دسترس بود (۶۴.۶pip روی میز جامانده). با بازطراحیِ خروجِ SHORT به
`SL70/TP800/mh48/be6/trail6` («بگذار بردها بدوند») سهمِ SHORT از +۲۷٬۸۵۲$ به **+۳۴٬۵۴۲$**
رسید (Δ +۶٬۶۹۰$)، **هر دو نیمهٔ داده مثبت** (h1 +۵٬۱۶۱$، h2 +۲۲٬۶۴۸$)، هر ۴ پنجرهٔ
walk-forward مثبت، corr با long = +۰.۱۶. سودِ کل از +۸۸٬۹۵۵$ به **+۹۵٬۶۴۵$** رسید
(**+۷.۵٪**). جزئیات: `results/ShortExitLetWinnersRun_NetProfit_95645.md`.)

> پیش از آن — چرخهٔ s113→s115 (رکوردِ +۸۸٬۹۵۵$): بازبهینه‌سازیِ اولِ خروجِ SHORT روی ۳ سالِ
> اخیر ⇒ `SL60/BE6/trail6/mh8`، سودِ SHORT از +۱۴٬۹۷۹$ به +۲۷٬۸۵۲$، DD نصف شد.
> جزئیات: `results/ShortExitReopt_Recent3y_NetProfit_88955.md`.

**پاسخِ پارادوکسِ فلسفیِ User note2** (چرا coverage بالا ولی WR پایین؟): مشخص شد
پیش‌فرضِ «همه روندها کشف می‌شوند» غلط است — coverage واقعی فقط ۱۶–۲۸٪ است؛ و WR پایین
یک نقص نیست بلکه **امضای ساختاریِ سیستمِ R:R>1** («چند بردِ بزرگ، بسیار باختِ کوچک»)
است. برای سودِ بیشتر نباید WR را هدف گرفت؛ باید گذاشت بردها بدوند و باخت را کوچک نگه
داشت. (s111)

### ✅ تحققِ هدفِ ۸۰٪ِ کشفِ روندِ اسکالپ (User Note فلسفی — چرخهٔ s127)
User Note خواست «انقدر تکرار کن تا حداقل ۸۰٪ روندها را درست تشخیص بدهیم» (تمرکز:
موتورِ اسکالپ روی M5). چرخه‌های قبل اثبات کردند موتورِ *تک‌آشکارسازِ* فعلی
(`EMA20>EMA100 ∧ RSI<35`) به‌دلیلِ تناقضِ دو شرط فقط ۰.۱٪ کندل‌ها را فعال می‌کند ⇒
پوشش ۱–۱۶٪ (هدفِ ۸۰٪ غیرممکن). **راه‌حل (s127):** به‌جای یک موتور، یک **مجموعهٔ ۵
آشکارسازِ مکمل** ساخته شد (MOMO / PULL / MACD / EXPAND / MSWING) — دقیقاً مطابقِ
استعارهٔ s1/s2/s3ِ User Note که هرکدام DNAِ خاصی از روند را می‌بیند. نتیجه روی
ماتریسِ ۵۷+۵۷ روند: **پوششِ UP=۹۸.۲٪ و DOWN=۹۸.۲٪** (هدفِ ۸۰٪ محقق شد ✅). برای
صداقتِ علمی، هر آشکارساز با یک **null-modelِ تصادفیِ هم‌نرخ** مقایسه و z-score گرفت:
**D3_MACD (z≈+۵–۶) و D5_MSWING (z≈+۴–۵)** آشکارسازهای *واقعی* هستند، اما **D2 (موتورِ
فعلی) با z≈−۶ عملاً کور است** (تأییدِ کمّیِ s124). حتی با حذفِ کورها پوشش ≥۹۴.۷٪
می‌ماند ⇒ پوشش «تقلبِ چگالی» نیست. تنها روندهای کشف‌نشده تک‌کندلی‌اند (حدِ ریاضیِ ذاتی).
جزئیات: `results/ScalpMultiDetector_Coverage98_NetProfit_95645.md`. این یک ابزارِ
**تشخیصی** است ⇒ رکوردِ سودِ خالص دست‌نخورده = **+۹۵٬۶۴۵$**.

پیش از آن: بازنگریِ لایهٔ اسکالپِ M5 (S79→S91) سودِ کل را از +۵۸٬۲۹۵$ به +۶۱٬۱۰۲$ رساند.

- **XAUUSD — S67** (لایهٔ نوسانی M15، کاملاً دست‌نخورده طبقِ User Note): +۳۰٬۴۹۰$
  با هزینهٔ واقعی، ریسکِ ثابت ۱٪ روی ۱۰٬۰۰۰$.
- **★ XAUUSD — S91** (لایهٔ اسکالپ M5 — **بازنگری‌شده طبقِ User Note**: Signal-Exit با
  «هدفِ پنهان»، فقط BUY، EMA20>EMA100 + RSI(21)<35 برای ورود؛ خروجِ داخلی TP۱۲۰/SL۸۰pip
  که **هرگز به کاربر نمایش داده نمی‌شود**): **+۱۰٬۰۴۴$** سرمایه‌محور (در برابرِ +۷٬۲۳۷$
  بازتولیدِ S79 با همان موتور ⇒ **+۲٬۸۰۷$**)، WR ۵۳.۵٪ (فقط گزارشی)، PF ۱.۳۲،
  MaxDD −۱۳٪، Sharpe ۲.۶۲، هر دو نیمهٔ داده مثبت.
  **در بخشِ اسکالپِ سایت هیچ TP/SL/حجمی نمایش داده نمی‌شود؛ فقط BUY/SELL و سپس پیامِ
  لحظه‌ای «ما سودمونو گرفتیم، ببند» / «تشخیصم اشتباه بود، ببند» + دکمهٔ بستن.** با
  تستِ paper broker تأیید شد (`strategies/s96_...`: هر دو نیمه مثبت، PF ۱.۳۸).
- **★ XAUUSD — S81** (لایهٔ نوسانی M30، کشفِ نو در پاسخِ User Note: Swing Trend-Pullback،
  فقط Long، EMA20>EMA100 + RSI(14)<35، SL=۱۲۰pip/TP=۱۲۰۰pip، R:R≈۱:۱۰، نگهداریِ تا ۳ روز):
  **+۱۴٬۳۲۷$**، WR ۳۵.۳٪ (فقط گزارشی)، PF ۱.۳۱، MaxDD −۹.۴٪، Sharpe ۲.۴۱، هر دو نیمه مثبت.
  **جایگزینِ لایهٔ H1/S80 شد** چون همان لبهٔ trend-pullback را قوی‌تر می‌گیرد و با H1
  همبستهٔ ۰.۷۵ بود (نمی‌توان هر دو را جمع کرد؛ جزئیات در فایلِ نتیجه).
- **★ XAUUSD — SHORT-MA-Confluence** (لایهٔ SHORTِ نو در پاسخِ User Note «چرا SHORTِ سودده
  نداریم؟»، M15، **تنها لایهٔ SHORTِ پروژه**): وقتی قیمت خطِ میانهٔ سه میانگین
  [EMA50,EMA100,SMA200] را از بالا رو به پایین قطع می‌کند ⇒ SHORT. **خروجِ بازطراحی‌شدهٔ
  s118 «بگذار بردها بدوند»: SL=۷۰pip، TP سقفِ ۸۰۰pip، بریک‌ایون در ۶pip، trailing ۶pip،
  حداکثر ۴۸ کندل.** سهمِ **+۳۴٬۵۴۲$**، هر دو نیمهٔ داده مثبت، هر ۴ پنجرهٔ walk-forward مثبت.
  **هم‌بستگیِ روزانه با جریانِ long = +۰.۱۶ ⇒ غیرِهم‌بسته و افزایشی** (جریانِ غیرِهم‌بستهٔ
  موردِنیازِ گلوگاه). جزئیات: `results/ShortExitLetWinnersRun_NetProfit_95645.md`.
  (تاریخچه: پارامترِ قدیمِ SL۴۰/BE۸/trail۸/mh۱۲ ⇒ +۱۴٬۹۷۹$؛ سپس SL۶۰/BE۶/trail۶/mh۸ ⇒ +۲۷٬۸۵۲$.)
- **EURUSD — S73** (لایهٔ نوسانی M15، کشفِ نو: Session-Open Drift، ساعتِ ۰ UTC، buy-the-dip):
  +۹٬۲۲۳$ با هزینهٔ واقعی، **هر دو نیمهٔ داده مثبت**.

> **★ آخرین چرخه (L61–L63 / s116→s118 — پاسخِ User Note «روندها را شماره‌گذاری کن،
> دقتِ شروع و پایان بده، بفهم چرا کشف/کشف‌نشده، بعد فکر کن چطور مغزها را بهتر کنیم؛
> راستی نشستِ قبل اشتباهاً با موتورِ long تست کردیم!»):**
> - **s116 — کارتِ امتیازِ زمان‌بندیِ روند:** هر روندِ بازار شماره‌گذاری شد و دقتِ
>   *شروع* و *پایان* سنجیده شد. کشفِ کلیدی: **«کشف ≠ گرفتن»** — سیگنال روند را می‌بیند
>   ولی exit زودهنگام سود را پس می‌دهد؛ پس ضعفِ اصلی خودِ **زمان‌بندیِ خروج** است.
> - **s117 — رفعِ باگِ «موتورِ long اشتباه» + ممیزیِ MFE/MAE:** همان سیگنالِ SHORT دوبار
>   اجرا شد؛ با موتورِ درست (short) +$27,852 ولی با موتورِ long فقط +$8,021 ⇒ اختلافِ
>   **$19,831** به‌طورِ کمّی ثابت کرد جهتِ موتور تعیین‌کننده است و باگِ نشستِ قبل رفع شد.
>   ممیزیِ مسیرِ قیمت نشان داد میانگینِ realized فقط **+۴.۸pip** بود ولی MFE=**۶۹.۳pip**
>   در دسترس بود ⇒ **۶۴.۶pip روی میز جامانده**؛ ۴۳٪ باخت‌ها زمانی MFE≥۲۰pip داشتند.
>   ریشه: trail=۶pip و max_hold=۸ **بیش‌ازحد تنگ** بودند و بردهای بزرگ را زودهنگام می‌بریدند.
> - **s118 — بازطراحیِ exit «بگذار بردها بدوند» (رکوردِ جدید):** فقط پارامترهای *خروج*
>   جارو شدند (ماشهٔ ورود ثابت). برنده: **`SL70/TP800/mh48/be6/trail6`**. سهمِ SHORT از
>   +$27,852 به **+$34,542** رسید (Δ +$6,690). گیت‌های ضدِ overfit همه سبز: هر دو نیمهٔ
>   داده مثبت (h1 +$5,161، h2 +$22,648)، هر ۴ پنجرهٔ walk-forward مثبت، corr با long +۰.۱۶.
> **🥇 رکوردِ جدید: سودِ خالصِ کل +۹۵٬۶۴۵$ (قبلی +۸۸٬۹۵۵$، +۷.۵٪).** سایت و APK با این
> پارامتر همگام شدند. جزئیات: `results/ShortExitLetWinnersRun_NetProfit_95645.md`.
> **قانونِ شمارهٔ ۱ بازتأکید می‌شود: معیار فقط سودِ خالص (XAUUSD+EURUSD) است، نه WR.**

> **آخرین چرخه (L51–L53 — پاسخِ User Note «فهمِ رفتارِ چارت؛ چرا سایت در روندِ صعودی
> SHORT می‌داد و در روندِ نزولی ساکت بود»):**
> کاربر گزارش داد سایت در روندِ صعودیِ قوی سیگنالِ SHORT می‌داد و در نزولیِ قوی هیچ
> پیشنهادی نمی‌داد. یک بازهٔ یک‌ساله (۲۳٬۶۲۲ کندلِ M15، حرکتِ قیمت +۲۶.۱٪) استخراج و
> «پیش‌بینیِ رفتارِ چارت» را علمی آزمودیم (S85–S90):
> - **L51 — پیش‌بینیِ تک‌کندل شکست می‌خورد:** baselineِ «همیشه UP» با ۵۱.۳٪ از همهٔ
>   اندیکاتورها/ML بالاتر بود؛ autocorrelation≈۰. بازار نیمه‌کارا است. مدلِ ML به‌جای
>   یادگیری، **mean-reversionِ متقارن** یاد می‌گرفت و خلافِ روندِ صعودی SHORT می‌زد —
>   دقیقاً بیماریِ سایت.
> - **L52 — معاملهٔ متقارن فاجعه است:** روی همان بازه، حالتِ SYMMETRIC = **−۶٬۹۹۴$**،
>   SHORT-only = **−۵٬۰۷۳$**. فیلترِ رژیمِ جهت ضرر را به ≈۰ رساند. راهِ درست =
>   long-biased هم‌سو با drift، **نه** پیش‌بینیِ متقارن.
> - **L53 — طلا داراییِ با بایاسِ صعودیِ ساختاری است؛ SHORT ذاتاً بازنده:** حتی SHORT با
>   تشخیصِ درستِ رژیمِ نزولی روی ۱۵۰k کندل = **−۲٬۰۸۳$ (PF ۰.۹۳)**. پس مغزِ طلا فقط Long.
> - **لبهٔ اثبات‌شده و اعمال‌شده در سایت:** ماشهٔ «عبورِ MACD-hist از منفی به مثبت در
>   رژیمِ صعودی» (S88) = **+۱۳٬۰۵۸$، PF ۱.۲۴، Sharpe ۲.۵۸، هر دو نیمهٔ داده مثبت**.
>   اما در آزمونِ dedup با برندهٔ فعلی (S67) **افزایشی نبود** (هم‌بستگی ۰.۳۷؛ زیرمجموعهٔ
>   S67 که PF ۱.۳۷ دارد). پس رکوردِ کل تغییر نکرد، ولی **بیماریِ رفتارِ سایت درمان شد**.
> - **اصلاحِ سایت (فعال و تست‌شده):** تستِ رفتاری روی ۱۵۰k کندل تأیید کرد
>   **shortInUp=۰ و shortInDown=۰** (هرگز SHORT صادر نمی‌شود)، LONG در ۲۵۱ از ۲۵۷ موردِ
>   رژیمِ صعودی درست صادر شد، و در رژیمِ نزولی صادقانه NONE با توضیحِ L53 می‌دهد.
> **رکورد وقت: +۵۸٬۲۹۵$.** جزئیات: `results/MACDRegimeTrigger_ChartBehavior_NetProfit_13058.md`.
> **قانونِ شمارهٔ ۱ بازتأکید می‌شود: معیار فقط سودِ خالص (XAUUSD+EURUSD) است، نه WR.**

> **آخرین چرخه (L54 — پاسخِ User Note «بازنگریِ بخشِ اسکالپ»):**
> کاربر خواست بخشِ اسکالپِ سایت **بدونِ TP/SL/حجم** باشد: فقط BUY/SELL طبقِ تشخیص،
> یک دکمهٔ تأییدِ بازکردنِ معامله، سپس حالتِ مدیریتِ **مینیمال** که فقط لحظه‌ای می‌گوید
> «ما سودمونو گرفتیم، سریع معامله رو ببند» یا «متاسفم تشخیصم اشتباه بود، سریع معامله رو
> ببند» و یک دکمهٔ «معامله را بستم». هیچ عدد/TP/SL/حجمی نمایش داده نمی‌شود.
> - **راهِ‌حلِ علمی — «هدفِ پنهان» (Hidden-Target):** بک‌تست نشان داد از نظرِ سودِ خالص،
>   خروجِ TP/SL ثابت بهتر از خروجِ سیگنال-محورِ زودهنگام است. پس منطقِ برنده را **پنهان**
>   کردیم: سایت داخلی همان آستانهٔ TP۱۲۰/SL۸۰pip را دارد، ولی به‌جای نمایشِ عدد فقط پیامِ
>   لحظه‌ایِ خروج می‌دهد. این هم User Note را برآورده می‌کند و هم بیشترین سود را می‌دهد.
> - **نتیجه:** لایهٔ اسکالپ از +۷٬۲۳۷$ (بازتولیدِ S79) به **+۱۰٬۰۴۴$** (S91) رسید ⇒
>   **+۲٬۸۰۷$ بهبودِ خالص**، PF ۱.۳۲، Sharpe ۲.۶۲، هر دو نیمه مثبت.
> - **تستِ paper broker (الزامِ User Note):** منطقِ *دقیقِ* endpointِ سایت
>   (`/api/scalp/manage`) در `strategies/s96_scalp_endpoint_paperbroker_test.py` روی
>   ۲۰۰k کندلِ واقعی اجرا شد؛ همهٔ گیت‌ها سبز (net کل و هر دو نیمه مثبت، PF ۱.۳۸).
> **🥇 رکوردِ جدید: +۶۱٬۱۰۲$.** جزئیات: `results/Scalp_SignalExit_HiddenTarget_NetProfit_10044.md`.
> **قانونِ شمارهٔ ۱ بازتأکید می‌شود: معیار فقط سودِ خالص (XAUUSD+EURUSD) است، نه WR.**
  اولین لبهٔ سوددهِ اثبات‌شده روی فارکس (پس از رد شدنِ S69–S72).

> **آخرین چرخه (L54–L55 — پاسخِ User Note «چرا SHORTِ سودده نداریم؟ بازار امروز ۳۰pip
> نزولی بود و ربات هیچ سیگنالی نداد و کلِ روز علافم کرد»):**
> کاربر پیشنهاد داد با ترکیبِ چند MA (۲۰ema/۵۰sma/۲۰۰sma/۵۰ema) و رفتارِ خطِ چارت نسبت به
> آن‌ها در تایم‌فریمِ پایین، روندِ نزولی را کشف کنیم — حتی سودهای کوچکِ ۳-۴ دلاری کافی است.
> (S97–S102، بازهٔ ۲ سالِ اخیر + کلِ ۱۵۰k):
> - **L54 — L53 «مطلق» نبود؛ به مکانیکِ خروج بستگی دارد:** رویدادِ «قیمت خطِ میانهٔ سه
>   میانگین را از بالا رو به پایین قطع می‌کند» یک driftِ نزولیِ کوتاه‌مدت دارد که در افقِ
>   بلند به V-recovery برمی‌گردد. پس SHORT روی طلا **فقط با خروجِ سریعِ trailing** سودده
>   است (SL۴۰pip/BE۸pip/trail۸pip/max۱۲کندل). این پالایشِ L53 است، نه نقضِ آن.
> - **L55 — دو باگِ look-ahead در موتورِ trailing کشف و رفع شد:** پیش از اصلاح +۳۰٬۸۵۳$
>   جعلی گزارش شد؛ پس از رفعِ باگ (چکِ exit پیش از به‌روزرسانیِ trailing + عدمِ فعال‌سازیِ
>   trailing در کندلِ ورود) عددِ واقع‌بینانه **+۱۴٬۹۷۹$** شد. درس: هر سودِ نجومی/نامتقارن
>   را با استرس‌تستِ look-ahead بسنج.
> - **افزوده به سایت (فعال و تست‌شده):** لایهٔ سیگنالِ SHORT (`src/short_ma_confluence.ts`)
>   به هر ۴ حالتِ router اضافه شد + مدیریتِ سریعِ SHORT در `trade_manager.ts`. تستِ
>   یکپارچه: ۴۰/۴۰ ماشهٔ اخیر ⇒ ENTRY SHORTِ معتبر؛ حالتِ MANAGE (BE/trail/close) هر سه
>   شلیک شد. **سایت دیگر در روندِ نزولی «علاف» نمی‌کند.**
> - **آزمونِ افزایشی‌بودن:** هم‌بستگیِ روزانه با جریانِ long = ‑۰.۱۱ ⇒ کاملاً غیرِهم‌بسته.
> **🥇 رکوردِ وقت: +۷۶٬۰۸۲$ (+۲۴.۵٪).** جزئیات: `results/ShortMAConfluence_Trailing_NetProfit_76082.md`.
>
> #### چرخهٔ ۹ (User note2): پارادوکسِ WR + بازبهینه‌سازیِ خروجِ SHORT روی ۳ سالِ اخیر
> - **L56 — تلهٔ compounding:** عددِ اولیهٔ +۱۲۷٬۴۲۴$ به‌خاطرِ صدا زدنِ `run_capital` با
>   `compounding=True` (پیش‌فرض) نجومی شده بود؛ رکورد با `compounding=False` ساخته شده.
>   مقایسهٔ درستِ «سیب با سیب» (هر دو `compounding=False`): baseline **دقیقاً +۱۴٬۹۷۹$**
>   بازتولید شد. **درس: همیشه با همان تنظیماتِ حسابداریِ رکورد مقایسه کن.**
> - **پاسخِ پارادوکسِ WR (s111):** coverage واقعی فقط ۱۶–۲۸٪ است (نه «همه روندها»)؛ WR
>   پایین یک نقص نیست، امضای ساختاریِ سیستمِ R:R>1 است. سودِ خالص = (نرخِ برد×میانگینِ
>   برد) − (نرخِ باخت×میانگینِ باخت)؛ WR بی‌ربط است.
> - **کشفِ رکورد (s113–s115):** بازبهینه‌سازیِ خروجِ SHORT فقط روی ۳ سالِ اخیر ⇒
>   `SL60/BE6/trail6/mh8`: سودِ SHORT از +۱۴٬۹۷۹$ به **+۲۷٬۸۵۲$**، PF ۱.۱۲→۱.۳۵،
>   DD −۱۸.۳٪→**−۷.۱٪**، هر ۴ پنجرهٔ WF مثبت (W2ِ رنج از −۲٬۸۶۸$ به +۴۷۳$)، corr=+۰.۲۰.
>   استرس‌تستِ look-ahead سالم (۷۴.۷٪ سود از bars_held=2).
> **🥇 رکوردِ جدید: +۸۸٬۹۵۵$ (+۱۶.۹٪).** جزئیات: `results/ShortExitReopt_Recent3y_NetProfit_88955.md`.
> **قانونِ شمارهٔ ۱ بازتأکید می‌شود: معیار فقط سودِ خالص (XAUUSD+EURUSD) است، نه WR.**

> #### چرخهٔ ۱۰ (User Note: سؤالِ فلسفیِ «چرا این روند کشف شد و آن یکی نه؟» + معیارِ نوینِ دقتِ شروع/پایان)
> ابزار: `strategies/s116_trend_timing_scorecard.py` — روی پنجرهٔ واقعیِ [145000,150000]
> (2026-04-21→2026-07-07) که دقیقاً ~۵۰ روندِ صعودی و ~۵۰ نزولیِ ≥$۸ (ZigZag close-based) دارد.
> برای اولین بار علاوه بر باینریِ «کشف/عدم‌کشف»، سه امتیازِ زمان‌بندی معرفی شد:
> **start** (نزدیکیِ ورود به کفِ روند)، **end** (نزدیکیِ خروج به قله)، **captured** (٪ دامنهٔ گرفته‌شده؛ منفی=استاپ در جهتِ درست).
> - **L61 — «کشف ≠ گرفتن»:** پوششِ UP=۱۶٪ و DOWN=۳۴٪ بود، ولی captured اغلب **منفی** است
>   (UP: ۷ از ۸ کشف ضررده؛ DOWN: ۶ از ۱۷). دقتِ شروع قابل‌قبول (UP≈۴۳، DOWN≈۵۲) اما
>   دقتِ پایان ضعیف (UP≈۳۰، DOWN≈۴۵). نقطه‌ضعفِ اصلیِ سیستم **مدیریتِ خروج** است، نه کشف.
> - **چرا؟ (پاسخِ فلسفی):** مغزِ LONG ذاتاً فقط روندهای بلند/بزرگ (میانگین ۱۶ کندل/$۵۵)
>   را می‌بیند چون عبور از میانهٔ سه MA فقط با حرکتِ بزرگ رخ می‌دهد؛ روندهای کوتاه (۵ کندل/$۲۰)
>   ساختاراً نامرئی‌اند. مغزِ SCALP در روندِ صعودی تقریباً غیرفعال است (RSI(21)<۳۵ در آپ‌ترند
>   نادر) ⇒ حفرهٔ پوشش. مغزِ SHORT روندهای برگشت از RSI بالا (۶۲.۸) را می‌گیرد (mean-reversion).
> - **بینشِ عملی برای چرخهٔ بعد:** بهبودِ exit-timing و کاهشِ captureهای منفی (trailing/BE هوشمندتر)
>   پتانسیلِ افزایشِ سودِ خالص دارد. **این تحلیل تشخیصی است ⇒ سودِ خالص بدون تغییر: +۸۸٬۹۵۵$.**
> جزئیات: `results/TrendTimingScorecard_StartEndAccuracy_NetProfit_88955.md`.
> **قانونِ شمارهٔ ۱ بازتأکید می‌شود: معیار فقط سودِ خالص (XAUUSD+EURUSD) است، نه WR.**

> **پاسخِ User Note دربارهٔ سرمایهٔ کم (۵۰$):** لایه‌های نوسانی/بلندمدت (M30, H1)
> ذاتاً برای سرمایهٔ خیلی کم **نامناسب**‌اند. SL این لایه ۱۲۰pip=۱۲$ است؛ روی ۵۰$
> حتی حداقلِ‌حجمِ بروکر (۰.۰۱ لات) یعنی ریسکِ ~۲۴٪ حساب در یک معامله ⇒ چند ضررِ
> پیاپی = نابودیِ حساب (شبیه‌سازی: DD=−۱۰۱.۶٪). **حداقلِ سرمایهٔ امن ≈ ۱٬۲۰۰$.**
> این هشدار به‌صورتِ زنده روی کارتِ M30 در سایت به کاربر نمایش داده می‌شود.
>
> **پاسخِ «آیا کارت جدید دارند و با M5/M15 تداخل می‌کنند؟»:** بله کارتِ مستقلِ
> «طلا/دلار — نوسانی (M30)» اضافه شد؛ داده (۳۰m)، منطق (`decideGoldM30`) و
> localStorageِ آن کاملاً جدا از کارت‌های M5/M15 است ⇒ **هیچ تداخلی ندارند**.

| مرحله | استراتژی | سودِ خالص | WR (فقط گزارشی) | توضیح |
|-------|----------|-----------------|------------------|-------|
| L36 | S63 — Rolling Regime-Router | ۲۹۴۹$ (اونس-محور) | ۶۱.۷٪ | پایه |
| L38 | S64 — + حجمِ Kelly | ۵۴۱۲$ (اونس-محور) | ۶۱.۷٪ | +۸۳٪ |
| L39 | S65 — + TP تطبیقی | ۶۰۸۲$ (اونس-محور) | ۵۱.۲٪ | اهرمِ سوم |
| L40 | S66 — + SL/ER تطبیقی | ۶۸۲۳–۷۳۵۰$ (اونس-محور) | ۵۰.۷٪ | اهرمِ چهارم |
| **L41** | **★ S67 — + مدلِ سرمایهٔ واقعی + TP/SL-Plan مستقل (XAUUSD)** | **+۳۷٬۱۵۶$ (سرمایه‌محور)** | ۵۳.۸٪ | **موتورِ طلا — دست‌نخورده** |
| L42 | S68 — آزمونِ «معاملهٔ بیشتر» (رد شد) | +۳۶٬۲۵۶$ | ۵۴.۱٪ | ❌ کمتر از S67 |
| L43 | S69/S70 — منطقِ طلا روی فارکس (رد شد) | +۱۳٬۴۳۶$ | — | ❌ لبه ندارد |
| L44 | S71 — Mean-Reversion فارکس (رد شد) | −۲۰٬۱۴۷$ | ۲۰٪ | ❌ ruin |
| L45 | S72 — Breakout فارکس (رد شد) | −۲۰٬۱۵۳$ | ۱٪ | ❌ ruin |
| **L46** | **★ S73 — EURUSD Session-Open Drift (کشفِ نو)** | **+۹٬۲۲۳$ (EURUSD، هزینهٔ واقعی)** | ۶۷.۵٪ | **موتورِ یورو** |
| **L47+** | **★ S79 — XAUUSD M5 Trend-Pullback (موتورِ نو)** | **+۴٬۲۵۶$ (طلا M5، هزینهٔ واقعی)** | ۳۸.۹٪ | **لایهٔ اسکالپِ طلا — رکوردِ وقت +۴۳٬۹۶۸$** |
| L48 | S80 — XAUUSD H1 Swing Trend-Pullback | +۱۰٬۰۶۴$ (طلا H1) | ۳۸.۵٪ | لایهٔ swing روی H1؛ بعداً با M30/S81 جایگزین شد (همبستهٔ ۰.۷۵) |
| **L49** | **★ S81 — XAUUSD M30 Swing Trend-Pullback (پاسخِ User Note)** | **+۱۴٬۳۲۷$ (طلا M30، هزینهٔ واقعی)** | ۳۵.۳٪ | **جایگزینِ H1؛ رکوردِ کل +۵۸٬۲۹۵$ (PF ۱.۳۱، DD −۹.۴٪)** |
| **L50** | **S82 — Visual-Shape Regime (خوشه‌بندیِ شکلِ بصری، پاسخِ User Note)** | **+۱۹٬۱۴۹$ مستقل (both-halves ✅)** | ۳۴.۸٪ | **الگوی بصری کار می‌کند اما ۹۸٪ long-only ⇒ هم‌بسته با S67؛ سود اضافه نشد. گلوگاه = نبودِ جریانِ غیرهم‌بسته. `VisualShapeRegime_TwoWindow_S82_19149.md`** |
| **L51** | **S85 — پیش‌بینیِ تک‌کندل (رد شد)** | baseline«همیشه UP» ۵۱.۳٪ > همهٔ اندیکاتورها/ML | — | ❌ بازار نیمه‌کارا؛ ML خلافِ روند SHORT می‌زد (ریشهٔ بیماریِ سایت). `MACDRegimeTrigger_ChartBehavior_NetProfit_13058.md` |
| **L52** | **S86 — متقارن vs فیلترِ رژیم (تشخیصی)** | SYMMETRIC −۶٬۹۹۴$ / SHORT-only −۵٬۰۷۳$ | ۳۳–۳۵٪ | ❌ معاملهٔ متقارن فاجعه؛ فیلترِ رژیم ضرر را به ≈۰ رساند. راهِ درست = long-biased هم‌سو با drift |
| **L53** | **★ S88 — MACD-Trigger-in-Regime (اعمال‌شده در سایت)** | **+۱۳٬۰۵۸$ (طلا M15، PF ۱.۲۴، Sharpe ۲.۵۸، both-halves ✅)** | ۴۷.۵٪ | **لبهٔ واقعیِ long؛ اما در dedup با S67 افزایشی نبود (هم‌بستگی ۰.۳۷). SHORT در نزول = −۲٬۰۸۳$ ⇒ طلا فقط Long. بیماریِ رفتارِ سایت درمان شد (shortInUp=۰). رکورد دست‌نخورده +۵۸٬۲۹۵$** |
| **L54** | **★ S91 — بازنگریِ اسکالپ: Signal-Exit با «هدفِ پنهان» (پاسخِ User Note)** | **+۱۰٬۰۴۴$ (طلا M5، PF ۱.۳۲، Sharpe ۲.۶۲، both-halves ✅) — جایگزینِ S79** | ۵۳.۵٪ | **بخشِ اسکالپِ سایت بدونِ TP/SL/حجم: فقط BUY/SELL + پیامِ لحظه‌ای «سودمونو گرفتیم/اشتباه بود، ببند» + دکمهٔ بستن. TP۱۲۰/SL۸۰ فقط منطقِ داخلی (پنهان). با paper broker تأیید شد (s96). بهبودِ +۲٬۸۰۷$ ⇒ رکوردِ کل +۶۱٬۱۰۲$. `Scalp_SignalExit_HiddenTarget_NetProfit_10044.md`** |

**«محلِ درستِ استفاده» پنج بُعد دارد و هر پنج اهرمِ مستقلِ سودِ خالص‌اند:**
1. **کجا وارد شویم** (S63، رژیمِ رو-به-جلو) → پایه.
2. **با چه حجمی** (S64، Kelly رژیم-آگاه) → +۸۳٪.
3. **تا کجا سود بگیریم** (S65، TP-Plan رژیم-آگاه) → اهرمِ سوم.
4. **کجا ضرر را ببندیم** (S66، SL-Plan رژیم-آگاه) → اهرمِ چهارم.
5. **★ با چه سرمایه و ریسکی** (S67، مدلِ سرمایه‌محور + لاتِ واقعیِ XAUUSD) → **عینکِ
   درستِ سنجش: سودِ خالص را به یک سرمایهٔ اولیهٔ واقعی گره می‌زند** (L41).

> **کشفِ L41 (پاسخِ User Note):** «سودِ خالص» بدونِ مدلِ سرمایه یک عددِ **بی‌مقیاس**
> است. اعدادِ قدیمی (۶۸۰۰/۷۳۵۰$) روی **۰.۰۱ لاتِ ثابت** بودند و به هیچ سرمایه‌ای گره
> نمی‌خوردند. موتورِ جدید `capital_engine.py` (ریسکِ درصدی + لاتِ واقعی + کمیسیون)
> نشان می‌دهد همان استراتژی روی **۱۰٬۰۰۰$ سرمایه با ریسکِ ۱٪** ⇒ **+۳۷٬۱۵۶$ (+۳۷۱.۶٪)**.
> ماژولِ مستقلِ `tpsl_plan.py` همان منطقِ TP/SL رژیم-آگاهِ برندهٔ ۷۳۵۰$ را احیا کرد.

> **کشفِ L42 (اهرمِ منفیِ کمیتِ معامله):** آزمونِ فرضیهٔ User Note 2 («معاملهٔ بیشتر ⇒
> سودِ بیشتر») رد شد. افزودنِ ۱۷۵ سیگنالِ حد وسط (proba ۰.۵۲–۰.۵۸) سودِ خالص را
> **۸۹۹$ کم** کرد. **مرزِ کیفیت: proba ≥ ۰.۵۸.** این سومین تأییدِ عددیِ قانونِ L3
> (WinRate-Trap) است: WR بالاتر (۵۴.۱٪ در برابر ۵۳.۸٪)، سودِ کمتر.

- جزئیات: [`results/TPSL_Plan_CapitalEngine_NetProfit_37156.md`](./results/TPSL_Plan_CapitalEngine_NetProfit_37156.md)
  · [`results/DualThreshold_Combo_NetProfit_36256.md`](./results/DualThreshold_Combo_NetProfit_36256.md)
  · [`results/Adaptive_SL_Router_NetProfit_54.md`](./results/Adaptive_SL_Router_NetProfit_54.md)
- کد: `strategies/s67_tpslplan_capital.py` · `engine/capital_engine.py` · `engine/tpsl_plan.py`
  · `strategies/s68_dual_threshold_combo.py`

## هدف (تاریخچه — منسوخ)
~~یافتن یک استراتژی معاملاتی روی داده‌های XAUUSD با **Win Rate بالای 65٪**~~ (طبق PARADIGM v2 دیگر معیار نیست).

## داده‌ها
فایل M15:
- **فایل:** `data/XAUUSD_M15.csv`
- **تعداد:** ۱۵۰,۰۰۰ کندل
- **تایم‌فریم:** M15 (۱۵ دقیقه‌ای)
- **بازه زمانی:** ۲۰۲۰-۰۲-۲۰ تا ۲۰۲۶-۰۷-۰۷
- **بازه قیمت:** ۱۴۵۳ تا ۵۵۸۵ دلار
- **ستون‌ها:** `time` (unix timestamp), `open`, `high`, `low`, `close`, `volume`

### مشخصات آماری داده
- میانگین رنج هر کندل: ~۳.۸ دلار
- میانگین بدنه (body): ~۱.۹ دلار
- انحراف معیار بازده هر کندل: ~۰.۱۲٪
- گپ غالب: ۹۰۰ ثانیه (۱۵ دقیقه) — داده‌ی پیوسته و تمیز

  به زودی فایل های داده بیشتری آپلود می‌شوند. 

## متدولوژی
- تمام استراتژی‌ها با یک **موتور بک‌تست استاندارد و مشترک** (`engine/backtest.py`) ارزیابی می‌شوند.
- هر معامله دارای **Stop Loss** و **Take Profit** مشخص است؛ Win Rate = درصد معاملاتی که به TP رسیده‌اند قبل از SL.
- برای واقع‌گرایی: اسپرد و کمیسیون در نظر گرفته می‌شود، و از **look-ahead bias** (نگاه به آینده) پرهیز می‌شود.
- هر استراتژی روی کل ۱۵۰ هزار کندل و همچنین در بازه‌های زمانی مختلف (تقسیم train/test) بررسی می‌شود تا از **overfitting** جلوگیری شود.

## قوانین ثبت نتایج
- هر استراتژی در **یک فایل MD مجزا** با نام `نام‌استراتژی_وین‌ریت.md` ذخیره می‌شود.
- هرگز بیش از یک استراتژی در یک فایل قرار نمی‌گیرد.
- نتایج موفق و ناموفق هر دو ثبت می‌شوند تا کارها تکرار نشوند.

## فهرست استراتژی‌های تست‌شده
| # | استراتژی | فایل | Win Rate | تعداد معاملات | وضعیت |
|---|----------|------|----------|---------------|--------|
| ۱ | BB+RSI Mean Reversion | `BB_RSI_MeanReversion_47.md` | ۴۷٪ | ۳۹۷۸ | ❌ بدون edge |
| ۲ | تله Win Rate (عدم‌تقارن TP/SL) | `TPSL_Asymmetry_WinRateTrap_84.md` | تا ۸۴٪ | زیاد | ❌ exp منفی |
| ۴ | LightGBM Long Walk-Forward | `ML_LightGBM_Long_WalkForward_71.md` | ۷۱.۵٪ | ۷۰۴۰ | ⚠️ exp مرزی-منفی |
| ۶ | Session ML Long (RR متعادل) | `Session_ML_Long_Balanced_56.md` | تا ۵۶٪ | ~۵۵۰ | ❌ WR کم، exp مثبت |
| ۷ | Session Asymmetric ML | `Session_Asymmetric_ML_Long_70.md` | ۷۰٪ | ~۱۴۰ | ⚠️ نامعنادار |
| ۸ | Structural Golden Pullback ML | `Structural_Golden_Pullback_ML_68.md` | ۶۸٪ | ۲۱۶ | ✅ سودآور، WR<70 |
| ۹ | Wide Candidate ML Golden | `Wide_Candidate_ML_Golden_67.md` | ۶۷٪ | ۵۰۰–۸۶۰ | ✅ سودآور، WR<70 |
| ۱۰ | **S/R Breakout-Retest (Price Action)** | `SR_Breakout_Retest_PriceAction_47.md` | ۴۷٪ | ~۱۲۰۰۰ | ❌ بدون edge |
| ۱۱ | **S/R Pullback + Trend + Golden (PA)** | `SR_Pullback_Trend_Golden_PriceAction_71.md` | ۷۱–۷۳٪ | ۷۸–۱۵۱ | ⚠️ نامعنادار + کم‌فرکانس |
| ۱۲ | **Adaptive S/R Target (Price Action)** | `Adaptive_SR_Target_PriceAction_36.md` | ۲۵–۳۶٪ | ~۷۰۰ | ❌ trade-off |
| ۱۳ | Range BB Mean-Reversion Scalp | `Range_BB_MeanReversion_Scalp_58.md` | ۵۸٪ | ~۵۰۰۰ | ❌ exp منفی (درس متد) |
| ۱۴ | **⭐ VWAP-Regime Selective ML (BE60)** | `VWAP_Regime_Selective_ML_BE60_62.md` | **۶۱.۶٪** | ۱۹۴۷ | ✅ **هدف کاربر: WR>60 + سودآور + ۵.۳ معامله/روز** |
| ۲۰ | **Squeeze-Breakout + Chandelier Trailing** | `Squeeze_Chandelier_TrailingExit_30.md` | ۳۰٪ (سودآور) / ۶۰٪ (exp≈۰) | ۲۳۰۲ | ❌ WR↔exp↔فرکانس هم‌زمان برآورده نشد؛ سود وابسته به رژیم |
| ۲۱ | **Session Opening-Range Breakout + Coiled-Spring** | `Session_ORB_CoiledSpring_47.md` | ۴۷٪ (معنادار) / تا ۷۴٪ (تله‌ی RR) | ۱۴۵–۱۶۴۵ | ❌ WR بالا فقط با RR نامتقارن؛ exp مثبت فقط با فرکانس ۰.۰۹/روز |
| ۲۲ | **Streak-Reversal (serial dependence)** | `Streak_Reversal_MeanRev_58.md` | تا ۵۹.۵٪ | ۴۱۰–۵۵۰۲ | ❌ edge خام ~۵۴٪ واقعی اما بسیار ضعیف؛ WR<60 و exp/فرکانس در تضاد |
| ۲۳ | **Weekly Mean-Reversion (Thursday)** | `Weekly_MeanReversion_Thursday_57.md` | ۵۷.۴٪ | ۱۶۸۳ | ❌ اثر هفتگی معنادار (corr=-0.217,p<0.001) اما خام WR<60 + exp منفی |
| ۲۴ | **Hourly-Drift Pockets** | `Hourly_Drift_Pockets_OOS_Fail_43.md` | TRAIN ~۶۰ / OOS ۴۳–۵۴ | ~۵۰۰ | ❌ overfitting کلاسیک؛ روی OOS مستقل فروپاشید |
| ۲۵ | **⭐⭐ ML + Weekly-Reversion Context** | `ML_WeeklyReversion_Context_62.md` | **۶۲.۰۴٪** | ۲۱۸۴ | ✅✅ **برندهٔ جدید: S14 را بهبود داد — exp+0.53$, PnL+73%, p=0.027 (معنادار!), ≥۳ معامله/روز** |
| ۲۶ | **RR-Sweep با هدف PF (طرح P01)** | `RR_Sweep_PF_Objective_P01_60.md` | **۶۰.۴٪** | ۱۵۸۳ | ⚠️ **PF حل شد (PF=1.353, exp+1.08$, p<0.001) اما فرکانس ~۰.۹۵/روز — تأیید تضاد WR↔فرکانس درون-استراتژی** |
| ۳۴ | **MTF کم‌شمار + گیت هم‌راستایی** | `MTF_Parsimony_AlignmentGate_59.md` | ۵۹٪ | زیاد | ❌ MTF واقعی سقف را نشکست (بستن گروه MTF) |
| ۳۵ | **پرتفوی جریان‌های چند-تایم‌فریمی** | `MTF_Portfolio_Streams_56.md` | ۵۶٪ | ~۲۰۰۰ | ❌ جریان‌های هم‌مکانیزم در زمان همبسته‌اند (dedup) |
| ۳۶ | **پرتفوی دو-مکانیزمی Bull+Bear** | `BullBear_DualMechanism_Portfolio_58.md` | ۵۸.۹٪ | تا ۹۷۰۰ | ⚠️ **tpd رکورد ۴.۲۴ (فرکانس حل شد!) اما WR<60** |
| ۳۷ | **اسکن RR + خروج دومرحله‌ای** | `Portfolio_RR_ScaleOut_63.md` | تا ۶۴٪ | زیاد | ❌ **کشف L15: دیوار هم‌ارزی WR↔PF (WR بالا ⇒ PF زیر ۱.۳)** |
| ۳۸ | **⭐ گیت متا رژیم-نوسان** | `Meta_VolatilityRegime_Gate_58.md` | ۵۸.۶٪ | ~۳۲۰۰ | ⚠️ **کشف L17: edge در High-vol ~۷× — بهترین PF پروژه (۱.۲۴, exp+۰.۸۸$)** |
| ۳۹ | **متخصص رژیم پرنوسان** | `HighVol_Specialist_58.md` | ۵۸.۳٪ | ~۲۶۰۰ | ❌ **L18: تخصص‌گرایی WR را بالا نبرد؛ بستن مسیر OHLCV** |
| ۴۰ | **بستهٔ feature اطلاعات/آشوب** | `Info_Chaos_FeaturePack_NoLift_62.md` | ۶۲٪ | ~۲۲۰۰ | ❌ **L19: اهمیت feature ≠ اطلاعات جدید (Hurst/PermEnt/VR بدون lift)** |
| ۴۱ | **پرتفوی فرکانسیِ جریان‌های مستقل** | `Frequency_Portfolio_IndependentStreams_60.md` | ۵۹.۶٪ | زیاد | ⚠️ **PF=۱.۳۲ حاصل شد + کشف edge نزولی؛ L20 (مرز پارتوی WR↔PF↔freq)** |
| ۴۲ | **پرتفوی سه-رژیمی با قید سخت WR≥۶۰** | `TriRegime_WR60_Constraint_MutualExclusion_60.md` | ۶۰.۸٪ | ۴۰۷۹ | ❌ **L21: انحصار متقابل — فقط جریان UP قید WR≥۶۰ را با فرکانس معنادار داد** |
| ۶۱ | **Regime-Router (نسبت‌دهیِ سودِ خالص)** | `RegimeRouter_NetProfitAttribution_Phase1.md` | — | — | 🔬 **L34: Bear در رژیم روندی ستاره است (exp ۱.۴۵$)؛ WR≠سود** |
| ۶۳ | **★ Rolling Regime-Router** | `RollingRegimeRouter_NetProfit_BeatsBaseline_L36.md` | ۶۱.۷٪ | ۲۵۴۳ | ✅ **L36: سودِ خالص +۳۵.۵٪ (۲۹۴۹$) با فعال‌سازیِ رژیمِ رو-به-جلو** |
| ۶۴ | **Kelly-Sized Rolling Router** | `Kelly_Sized_Router_NetProfit_62.md` | ۶۱.۷٪ | ۲۵۴۳ | ✅✅ **L38: سودِ خالص +۸۳.۵٪ (۵۴۱۲$)؛ حجمِ رژیم-آگاه** |
| ۶۵ | **Adaptive-TP + Kelly Router** | `Adaptive_TP_Router_NetProfit_51.md` | ۵۱.۲٪ | ۱۸۵۳ | ✅✅✅ **L39: سودِ خالص +۱۰۶٪ (۶۰۸۲$)؛ TP رژیم-آگاه؛ WR↓ ولی سود↑** |
| ۶۶ | **ER=۰.۱۵ Threshold + Adaptive-SL** | `ER_Threshold_Sweep_NetProfit_UserNoteA_L40_50.md` · `Adaptive_SL_Router_NetProfit_54.md` | ۵۰.۷٪ | ۱۶۱۹ | ✅✅✅ **L40: سودِ خالص ۶۸۲۳–۷۳۵۰$ (اونس-محور)؛ آستانهٔ روند + SL رژیم-آگاه، اهرمِ چهارم؛ پایدار دو-نیمه** |
| ۶۷ | **🏆 TP/SL-Plan روی موتورِ سرمایه‌محور** | `TPSL_Plan_CapitalEngine_NetProfit_37156.md` | ۵۳.۸٪ | ۱۶۵۱ | ✅✅✅ **L41: سودِ خالصِ واقعی +۳۷٬۱۵۶$ روی ۱۰k$/۱٪ (MaxDD −۷.۸٪)؛ مدلِ سرمایه = عینکِ درستِ سنجش؛ TP/SL-Plan مستقل — برندهٔ فعلی** |
| ۶۸ | **ترکیبِ دو-آستانه‌ای (User Note 2)** | `DualThreshold_Combo_NetProfit_36256.md` | ۵۴.۱٪ | ۱۸۲۶ | ❌ **L42: «معاملهٔ بیشتر» رد شد (−۸۹۹$)؛ مرزِ کیفیت proba≥۰.۵۸؛ سومین تأییدِ WinRate-Trap** |
| ۷۳ | **★ EURUSD Session-Open Drift** | `S73_EURUSD_SessionDrift_NetProfit_44458.md` | ۶۷.۵٪ | ۱۰۰۷ | ✅✅ **L43: EURUSD +۷٬۳۰۲$؛ رکوردِ کل +۴۴٬۴۵۸$؛ لبهٔ یورو ساختارِ سشنی است** |
| ۷۴/۷۵ | **الگوهای بصری/هندسی (User Note: چشمِ تریدر)** | `VisualPattern_Confluence_NetProfit_44458.md` | — | — | ⚠️ **L44: الگوها لبهٔ آماریِ جهت‌دار دارند (t تا −۴.۱) اما سود اضافه نکردند — S74 ruin، S75 کاهشِ سود؛ تأییدِ چهارمِ «لبهٔ خام ≠ سود»** |

## 🏆 برندهٔ عصرِ WR (تاریخچه — منسوخ طبقِ PARADIGM v2): استراتژی ۲۵
> ⚠️ این بخش مربوط به دورانی است که معیار WR بود. **برندهٔ فعلیِ پروژه بر اساسِ
> سودِ خالص، استراتژی ۶۴ (Kelly-Sized Router) است** (بخشِ بالای همین README).
> S25 صرفاً به‌عنوانِ «مغزِ صعودی» در معماریِ Router باقی مانده است.

پس از پاسخ به User Note دوم (الگوهای زمانی روز-هفته)، یک **بهبود واقعی** بر برندهٔ
قبلی (S14) به‌دست آمد:

| معیار | S14 (برندهٔ قبلی) | **S25 (برندهٔ جدید)** |
|-------|-------------------|----------------------|
| Win Rate | ۶۱.۶٪ | **۶۲.۰٪** |
| Expectancy | +۰.۳۵$ | **+۰.۵۳$** (+۵۴٪) |
| Total PnL (OOS) | +۶۷۵$ | **+۱۱۶۶$** (+۷۳٪) |
| p-value (WR>60) | ۰.۰۸ ⚠️ مرزی | **۰.۰۲۷ ✅ معنادار** |
| معامله/روز | ۵.۳ | ~۴.۴ (median ۳) ✅ |

**راز بهبود:** دو feature زمانی جدید — `early_atr` (حرکت اوایل هفته نرمال‌شده) و
`weekly_rev` (فشار برگشت هفتگی) — که مستقیماً از الگوی زمانی مطرح‌شده در User Note
الهام گرفتند. `early_atr` به **قوی‌ترین feature کل مدل (رتبه #۱ از ۵۹)** تبدیل شد.
این اثبات کمی است که حس تجربی تریدرها دربارهٔ اهمیت زمان، یک edge واقعی است — به‌شرط
کمی‌سازی درست و ترکیب با ML (نه معاملهٔ خام؛ رجوع به شکست S23/S24).

## کشف بنیادی پروژه ⭐
پس از ۱۳+ استراتژی از پنج مسیر مستقل (آماری، یادگیری ماشین، اکشن قیمت،
نوسان/trailing-exit، و breakout زمان‌محورِ سشن):

> **سقف Win Rate آماری-معنادار (p<0.05، n بزرگ) با داده‌ی صرف OHLCV روی XAUUSD M15
> حدود ۶۶–۶۸٪ است.** این بازتاب مستقیم کارایی نیمه‌قوی این بازار است.
> - WR>70% یا با n کوچک (نامعنادار/cherry-pick) یا با RR نامتقارن (exp منفی) ممکن می‌شود.
> - جابه‌جایی TP/SL فقط WR و RR را با هم **معاوضه** می‌کند؛ expectancy را مثبت نمی‌کند.

### تضاد «WR بالا ↔ فرکانس بالا» (مانع ساخت ربات)
استراتژی‌های با WR~۷۰٪ ذاتاً کم‌فرکانس‌اند (~۰.۰۶–۰.۴ معامله در روز)، در حالی‌که ربات
به **≥۳ معامله در روز** نیاز دارد. باز کردن فیلترها برای فرکانس، WR را به سقف ~۶۸٪ برمی‌گرداند.

### آزمون خروج پویا (Chandelier Trailing) — استراتژی ۲۰، پاسخ به User Note
استراتژی ۲۰ برای اولین‌بار **خروج trailing پویا** (Chandelier ATR-stop) را روی
تریگر **TTM-Squeeze** (فشردگی نوسان BB داخل Keltner) آزمود. نتیجه: یک سیستم
trend-following سودآور اما با **WR پایین (~۳۰–۴۳٪)**؛ بالا بردن WR با TP سقف،
expectancy را منفی کرد (WinRate-Trap) و سودآوریِ حالت خالص هم به رژیمِ روندِ اخیر
طلا وابسته بود. اسکن کامل ۴۵۰ ترکیب هیچ نقطه‌ای با WR≥۶۰ + exp>0 + ≥۳ معامله/روز
نیافت. → **تأیید یازدهم سقف اطلاعاتی**؛ trailing-exit این دیوار را نمی‌شکند.

### 🔬 جلسهٔ Multi-Timeframe (S34–S39) — پاسخ به User Note سوم (ترید چند-تایم‌فریمی)
کاربر پرسید: «با تایم‌فریم بالاتر خطوط حمایت/مقاومت و روند کشیده می‌شوند و در
تایم‌فریم پایین‌تر معامله می‌شود.» شش استراتژی متوالی این ایده و پیامدهایش را
به‌صورت علمی آزمودند. سه **قانون بنیادی جدید** کشف شد:

- **L15 — قانون هم‌ارزی WR↔PF:** روی OHLCV صرف، `WR` و `PF` دو نمای یک edge خام‌اند.
  با کوچک‌کردن TP، WR تا ۶۴٪ بالا می‌رود اما PF دقیقاً به همان نسبت زیر ۱ سقوط
  می‌کند. هدفِ «WR>60 **و** PF>1.3 هم‌زمان» نیازمند **بالا بردن خودِ edge** است،
  نه بازآرایی خروج/آستانه/ساختار. (S37)
- **L17 — وابستگی edge به رژیم نوسان:** edgeِ trend-pullback طلا در رژیم پرنوسان
  **~۷ برابر** قوی‌تر است (expectancy +۱.۰۳$ در High-vol در برابر +۰.۱۴$ در Low-vol).
  فیلتر رژیم High-vol بهترین اهرمِ **PF/exp** پروژه است (PF=۱.۲۴, exp=+۰.۸۸$). (S38)
- **L18 — سقف تخصص‌گرایی:** آموزش مدل *فقط* روی زیرمجموعهٔ پُر-edge، WR را بالا
  نمی‌برد (افتِ حجم دادهٔ آموزش، مزیت تخصص را خنثی می‌کند). رژیم باید فیلترِ خروجی
  باشد نه تفکیکِ آموزش. (S39)

**دستاورد مثبت مهم:** پرتفوی دو-مکانیزمی Bull+Bear (S36) — که long در uptrend و
short در downtrend را ترکیب می‌کند و چون رژیم‌ها در زمان **ناهمبسته**‌اند فرکانس‌ها
بدون dedup جمع می‌شوند — به **tpd=۴.۲۴ (رکورد پروژه)** رسید و قید فرکانس را عملاً
حل کرد. اما WR روی ۵۸.۹٪ ماند.

> **جمع‌بندی MTF:** MTF *واقعی* (S33/S34) سقف WR را نشکست چون از همان دارایی
> اطلاعاتِ جهت‌دارِ مستقل تولید نمی‌کند. تنوعِ **مکانیزمی** (Bull+Bear) فرکانس را
> حل کرد اما WR نه. **سقف WR ~۵۸–۶۰٪ روی OHLCV صرف یک واقعیت ساختاری تأییدشده است**
> (پنج اهرم مستقل شکست خورد). تنها مسیر باقی‌مانده برای عبور WR: **دادهٔ برون‌زای
> جهت‌دار** (گروه G — DXY، همبستگی منفیِ قوی طلا با دلار).

### الگوهای زمانی روز-هفته (Day-of-Week / Weekly Mean-Reversion) — پاسخ به User Note دوم
کاربر پرسید آیا الگوهای زمانی (رفتار مشابه هر روز هفته، و برگشت پنجشنبه/جمعه خلاف
حرکت اوایل هفته) در داده وجود دارند. تحلیل آماری روی ۳۳۳ هفته نشان داد:
- **فرضیهٔ mean-reversion هفتگی واقعی و معنادار است:** `corr(early_move[Mon-Wed],
  thu_fri_chg) = -0.217, p<0.001`. یعنی هرچه اوایل هفته صعودی‌تر، پنجشنبه/جمعه
  تمایل نزولی بیشتر (و بالعکس) — دقیقاً حسی که کاربر از تریدرها نقل کرد.
- اما این اثر **خام** (استراتژی ۲۳) به WR>60 نرسید (بهترین ۵۷.۴٪، exp منفی)، چون
  drift ضعیف‌تر از نویز درون‌روزی است.
- اثر ساعتی خام (استراتژی ۲۴) با اعتبارسنجی OOS، **overfitting** از آب درآمد.
- **راه‌حل موفق (استراتژی ۲۵):** تبدیل اثر زمانی به دو feature و تزریق به مدل ML
  → WR=۶۲٪، exp+۰.۵۳$، و مهم‌تر p=0.027 (معناداری قطعی). این feature ها
  **قوی‌ترین سیگنال‌های مدل** شدند. → **بهبود واقعی بر برندهٔ قبلی.**

### 👁️ الگوهای بصری/شکلی (Candlestick + Geometric) — پاسخ به User Note («چشمِ تریدر»)
تریدر روی چارت یک **double-top** کشید و گفت «من با چشمم شکلِ بازار را می‌فهمم و
ادامهٔ روند را حدس می‌زنم» و به shooting-star/hammer و الگوهای شکلیِ تکرارشونده
اشاره کرد. برای اولین‌بار در پروژه (هیچ‌کدام از S1..S73 این را نسنجیده بودند)
الگوهای کندلی (doji, hammer, shooting_star, engulfing, marubozu, pin bar,
inside/outside bar) و هندسی (double-top/bottom, higher-high/lower-low) به‌صورت
آماری روی M15 و H1 سنجیده شدند. جزئیات:
[`results/VisualPattern_Confluence_NetProfit_44458.md`](./results/VisualPattern_Confluence_NetProfit_44458.md).

- **آیا وجود دارند؟ بله** — لبهٔ آماریِ معنادار اثبات شد: `bear_engulf` (t=−۳.۶)،
  `pin_bear` در H1 (t=−۴.۱)، `Double-Top` (t=−۲.۸، **دقیقاً همان که تریدر کشید**)،
  `Higher-High seq` صعودی (t=+۳.۷)، `inside_bar` مومنتوم (t تا +۴.۸).
- **کجا؟** رژیم و تایم‌فریم حیاتی‌اند: سمتِ **نزولی قوی‌تر** از صعودی است (طلا
  نامتقارن)؛ `shooting_star` فقط در روند/پرنوسان؛ `pin_bear`/`double-top` در **H1
  قوی‌تر**؛ `bull_marubozu` فقط در **رنج** صعودی است (در روند معکوس می‌شود).
- **اما سود اضافه نکردند (L44):** به‌عنوانِ ماشهٔ مستقل (S74) ⇒ **ruin**؛ به‌عنوانِ
  فیلترِ confluence روی موتورِ برندهٔ S67 (S75) ⇒ **کاهشِ سود ۲.۵–۵.۳ هزار دلار**.
  لبهٔ خام فقط چند bps است و با اسپرد + روندِ صعودیِ طلا از بین می‌رود.
- **کشفِ L44:** «چشمِ تریدر» جهت را درست حس می‌کند اما **معناداریِ آماری ≠
  سودآوریِ معاملاتی** — چهارمین تأییدِ این دیوارِ ساختاری (پس از L15 و شکستِ PA).
  رکوردِ سودِ خالصِ کل **+۴۴٬۴۵۸$ دست‌نخورده ماند**؛ موتورهای S67 و S73 تغییر نکردند.

### پوشش روش‌های تریدرها (Price Action) — پاسخ به User Note
استراتژی‌های ۱۰، ۱۱، ۱۲ دقیقاً روش‌های کلاسیک تریدرها را کمی‌سازی و بک‌تست کردند
(حمایت/مقاومت، breakout-retest، pullback، هدف‌گذاری ساختاری). نتیجه‌ی علمی: این
روش‌ها به‌تنهایی **edge آماری معناداری ندارند** و به همان سقف ساختاری ~۶۸٪ می‌رسند.

## ساختار پروژه
```
Scientific-Research/
├── data/
│   └── XAUUSD_M15.csv         # داده خام
├── engine/
│   ├── backtest.py            # موتور بک‌تست استاندارد
│   ├── features.py            # ساخت ۵۷ feature برای مدل‌های ML
│   ├── structure.py           # ابزار ساختار بازار (S/R)
│   └── indicators.py          # کتابخانه اندیکاتورها
├── strategies/                # کد پایتون هر استراتژی
├── results/                   # فایل‌های MD نتایج (نام‌استراتژی_وین‌ریت.md)
├── mt5_robot/                 # 🤖 ربات کامل MetaTrader 5
│   ├── XAUUSD_S14_Robot.mq5   # Expert Advisor (کد MQL5)
│   ├── xauusd_s14_model*.onnx # مدل‌های ONNX (ensemble ۳-seed)
│   ├── train_export_final.py  # آموزش/صادرات مدل
│   ├── validate_parity.py     # اعتبارسنجی feature-parity
│   └── README.md              # راهنمای نصب کامل ربات
├── web_tool/                  # 🌐 ابزار آنلاین تحلیل زنده (Hono + Cloudflare)
│   ├── src/index.tsx          # backend + دریافت داده زنده Yahoo
│   ├── src/indicators.ts      # بازتولید اندیکاتورها در TS
│   ├── src/structure.ts       # بازتولید S/R در TS
│   ├── src/signal.ts          # موتور سیگنال شفاف S14
│   ├── public/static/app.js   # داشبورد فرانت‌اند
│   └── README.md              # راهنمای اجرا و دیپلوی
└── README.md
```

## 📱 اپلیکیشنِ اندروید (APK) ساخته شد ✅ — پاسخِ User Note

مطابقِ User Note، یک **پروژهٔ کاملِ Capacitor** برای تولیدِ APK اندروید افزوده شد که
دقیقاً مطابقِ سایت کار می‌کند اما موتورِ تصمیم **روی خودِ دستگاه** (Pyodide + numpy/pandas)
اجرا می‌شود — همان موتورِ واقعیِ پروژه (`pyengine/`) که رکوردِ +۹۵٬۶۴۵$ را ساخت.

- **کارت‌ها مطابقِ سایت — فقط دو ارز (طبقِ User Note):** `XAUUSD` (`GC=F`) و
  `EURUSD` (`EURUSD=X`). **DXY و AUDUSD حذف شدند** چون لبهٔ سوددهی نداشتند و تعریفِ
  رسمیِ سودِ خالص = **XAUUSD + EURUSD** است.
- **دادهٔ لحظه‌ای بدونِ کمترین تأخیر (طبقِ User Note):** داخلِ APK افزونهٔ
  **CapacitorHttp** فعال است ⇒ `fetch` از native عبور می‌کند، CORS دور زده می‌شود و اپ
  **مستقیم** از Yahoo (query1/query2) داده می‌گیرد (بدونِ پروکسی). دریافتِ **موازیِ**
  هر دو دارایی، به‌روزرسانیِ خودکار هر **۱۵ ثانیه** + دکمهٔ فوری، و نمایشِ زمانِ آخرین
  به‌روزرسانی. (بازارِ بسته = دادهٔ جدید ندارد؛ به‌محضِ بازگشایی، لحظه‌ای از سر می‌گیرد.)
- **ماشینِ حالتِ ۴-وضعیتی** (خنثی / نزدیک‌شدن / ورود / مدیریت) دقیقاً مثلِ سایت.
- **ساخت:** `cd apk && bash build_apk.sh` (لینوکس/مک) یا `build_apk.bat` (ویندوز).
  خروجی: `apk/android/app/build/outputs/apk/debug/app-debug.apk`.
  در سندباکس تأیید شد که `cap add android` ساختارِ نیتیو را کامل می‌سازد (assets شاملِ
  کاملِ `pyengine/` + مجوزِ INTERNET). *ساختِ نهاییِ فایلِ APK به Android SDK/JDK نیاز*
  *دارد و باید روی سیستمِ کاربر انجام شود (سندباکسِ ابری این ابزارها را ندارد).*
- **راهنمای کامل:** [`docs/APK_Android_Build_Guide.md`](./docs/APK_Android_Build_Guide.md).

## 🤖 ربات MT5 ساخته شد ✅
پس از رسیدن به استراتژی برنده (WR=۶۱.۶٪)، ربات کامل MetaTrader 5 ساخته شد:
- **`mt5_robot/XAUUSD_S14_Robot.mq5`** — Expert Advisor کامل که ۵۷ feature را
  دقیقاً مطابق `engine/features.py` در MQL5 بازسازی می‌کند.
- **مدل ONNX** (ensemble ۳-seed) صادر و در برابر LightGBM **اعتبارسنجی شد**
  (اختلاف <۱e-۴ ✅).
- WR=۶۱.۵۸٪ روی Walk-Forward بازتولید شد (n=۱۹۴۷, exp=+۰.۳۵$, ۵.۳ معامله/روز).
- **راهنمای نصب گام‌به‌گام** در `mt5_robot/README.md`.
- ربات ≥۳ معامله در روز انجام می‌دهد (شرط الزامی کاربر برآورده شد ✅).

> **درس مهم حین ساخت ربات:** آزمایش نشان داد زیرمجموعهٔ کوچک feature (۲۴ یا ۳۰ تا)
> WR را به ۵۸–۵۹٪ افت می‌دهد؛ قدرت استراتژی به **مجموعهٔ کامل ۵۷ feature** (به‌ویژه
> VWAP لنگرشده + MTF) وابسته است. پس ربات با feature کامل ساخته شد.

## 🌐 سایتِ دستیارِ تصمیمِ چند-دارایی — نسخهٔ بازطراحی‌شده (PARADIGM v2 / User Note 2) ✅

> **بازطراحیِ کامل (User Note):** سایت از یک داشبوردِ پرباگ و شلوغ (با نمودار، بخشِ
> «وضعیتِ تحقیقِ علمی»، اجرای ONNX در مرورگر و انبوهِ اطلاعاتِ غیرمفید) به یک ابزارِ
> **تمیز و متمرکز** تبدیل شد که کاربر فقط **تصمیمِ نهایی** را می‌بیند. نمودار، بخشِ
> تحقیق و همهٔ اطلاعاتِ اضافه **حذف شدند**.

ساختارِ سایت اکنون دقیقاً طبقِ PARADIGM v2 است: برای **هر دارایی** یک کارت با یک
**ماشینِ حالتِ ۴-وضعیتی**. پشتِ‌صحنه یک **Routerِ رژیم-محور** (`web_tool/src/router.ts`،
منطقِ برندهٔ S63/L36) تصمیم می‌گیرد؛ کاربر فقط خروجی را می‌بیند.

- **دارایی‌ها (کاهش به دو داراییِ دارای لبهٔ اثبات‌شده — طبقِ User Note):**
  `XAUUSD` (GC=F + rebase به spot، موتورِ S67 — دست‌نخورده) و `EURUSD` (EURUSD=X،
  موتورِ نوِ S73 — Session-Open Drift). منبع: Yahoo Finance، کندل M15، بدون کلید.
  **`DXY` و `AUDUSD` حذف شدند** چون هیچ لبهٔ سوددهی روی آن‌ها یافت نشد (S69–S72 زیان‌ده).
  هر دارایی موتورِ تصمیمِ مستقلِ خود را دارد: طلا `router.ts` (رژیم-محور)، یورو
  `eurusd_router.ts` (سشن-محور) — استراتژیِ طلا به یورو تحمیل نشده است.
- **چهار حالتِ هر کارت:**
  1. **خنثی** — صراحتاً می‌گوید وارد نمی‌شود + دلیل (رژیمِ رنج/ER پایین/proba کم) با مقادیرِ دقیق.
  2. **نزدیک‌شدن به سیگنال** — ستاپ در حالِ شکل‌گیری + فهرستِ **تأییدهایِ موردِ انتظار**.
  3. **ورود** — جهت (Long/Short) + **TP** + **SL** + R:R + احتمال. دکمهٔ «معامله را ثبت کردم».
  4. **مدیریتِ معامله** — **فقط پس از ثبتِ معاملهٔ کاربر** فعال می‌شود؛ سود/زیان (R)،
     پیشرفت به TP، جمع‌بندیِ اقدام و توصیه‌های زندهٔ SL/TP/بستن (با اعمالِ تک‌کلیکی).
- **گذارِ حالت‌ها** ترتیبی و وابسته به اقدامِ کاربر است (ورود → مدیریت فقط با ثبت).
- **معیارِ تصمیم:** سودِ خالص (طبقِ L36) — در رنج/بی‌روند سایت خنثی می‌ماند تا سودِ خالص حفظ شود.
- **API:** `GET /api/decision` (همهٔ دارایی‌ها یک‌جا) · `GET /api/decision/:asset` ·
  `POST /api/trade/advice` (با فیلدِ `asset`) · `GET /api/health`.
- **ذخیره:** معاملهٔ ثبت‌شدهٔ هر دارایی در **localStorage** (مقاوم به رفرش)، حذف فقط با
  دکمهٔ «بستنِ معامله». به‌روزرسانیِ خودکار هر ۳۰ ثانیه.
- راهنمای کامل نصب/دیپلوی در `web_tool/README.md`.

### پاسخ به سوالِ MA_Ribbon (WR=74٪) و RangeMR (WR=70٪)
مستندِ مستقل: [`results/StrategyTiming_Audit_WhenToUseEach_L37.md`](./results/StrategyTiming_Audit_WhenToUseEach_L37.md).
خلاصه: طبقِ معیارِ سودِ خالص، **RangeMR اکسپکتنسیِ منفی** دارد (WR بالا فریب بود) و
**MA_Ribbon** فقط در رژیمِ صعودی سودده است؛ پس هیچ‌کدام مبنای مستقل نیستند. «زمانِ
مناسبِ استفاده» برای همه به‌صورتِ **خودکار توسطِ Router (L36)** تشخیص داده می‌شود، نه دستی.

## وضعیت فعلی (به‌روزرسانی — تا استراتژی ۴۲)

پروژه اکنون **۴۲ استراتژی** از ۸ گروه مستقل را آزموده و **۲۱ قانون بنیادی (L1–L21)**
تقطیر کرده است. تصویر صادقانهٔ فعلی نسبت به **هدف سه‌گانهٔ کاربر**
(Exp مثبت پایدار + PF>1.3 + WR>60٪ + ≥۵ معامله/روز):

| قید کاربر | بهترین نتیجهٔ ثبت‌شده | وضعیت |
|-----------|----------------------|--------|
| Win Rate > 60% | S25: ۶۲.۰٪ (p=0.027 معنادار) | ✅ حل شد |
| Profit Factor > 1.3 | S26: ۱.۳۵ • S36: ۱.۳۳ | ✅ حل شد |
| Expectancy مثبت پایدار | S26: +۱.۰۸$ • S36: +۱.۱۴$ | ✅ حل شد |
| ≥ ۵ معامله/روز | S36: **۴.۲۴** (رکورد پروژه) | ⚠️ نزدیک ولی نه کامل |
| **هر ۴ قید هم‌زمان** | روی OHLCV صرف | ❌ **برآورده نشد (L21)** |

### 🔑 کشف نهایی — قانون L21 (انحصار متقابل WR↔PF↔فرکانس)
هر یک از سه قید به‌تنهایی حل‌شدنی است، اما **هر چهار قید هم‌زمان** روی دادهٔ صرف
OHLCV در M15 برآورده نشد. این یک **مرز پارتوی ساختاری** است (نه شکست مهندسی):
- در نقطهٔ PF-محور (TP1.4/SL1.7): PF>1.3 و WR>60 حاصل می‌شود اما فرکانس ~۱/روز (S26).
- در نقطهٔ فرکانس‌محور (شل‌کردن thr): tpd به ۴.۲۴ می‌رسد اما WR به زیر ۶۰ سقوط می‌کند (S36).
- افزودن رژیم پرنوسان (L17) بهترین اهرم PF بود اما فرکانس را پایین نگه داشت (S38).

> **تنها مسیر باقی‌مانده برای عبور از این مرز: دادهٔ برون‌زای جهت‌دار (گروه G — DXY،
> US10Y، تقویم اخبار).** ابزار وب پروژه هم‌اکنون این لایهٔ بنیادی را به‌صورت زنده
> دریافت و نمایش می‌دهد.

### برندگان تاریخی پروژه (برای ساخت ربات/ابزار)
- **استراتژی ۱۴ (VWAP-Regime ML):** WR=۶۱.۶٪، ۵.۳ معامله/روز — منتخب اولیهٔ ربات MT5.
- **استراتژی ۲۵ (ML + Weekly-Reversion):** WR=۶۲.۰٪، p=0.027 معنادار — برندهٔ ML، مبنای «مغز صعودی» سایت.
- **استراتژی ۳۱ (Bear Specialist):** PF=۱.۴۹ در رژیم نزولی — «مغز نزولی» سایت.

### همگام‌سازی با ابزار وب ✅
ابزار وب زنده اکنون یک پنل **«وضعیت تحقیق علمی»** دارد که همین جدول پیشرفت،
قوانین L15–L21 و مسیر باقی‌مانده را به‌صورت زنده به کاربر نشان می‌دهد (نه فقط آمار
یک استراتژی منفرد). معماری تصمیم سایت: **روتر سه‌مغزی** (S25 صعودی / S31 نزولی / رنجِ بدون‌معامله).
