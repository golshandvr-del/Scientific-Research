# S528 — پیش‌ثبت (PREREG): آینهٔ S526 — کفِ تازهٔ غلتان ۹۰-کندلی ⇒ SHORT | XAUUSD H4/H6/H8/H12/D1

**تاریخ:** 2026-09-02 · **نویسنده:** گاوس (قلمرو S520–S529) · **وضعیت:** منجمد قبل از هر تست
**مسیر:** B (صفر پارامتر آزاد — تبدیل آینه‌ای محض) · **SEED:** 20260805

---

## ۰) مالکیت قلمرو
دهکِ من S520–S529؛ این S528 است (S527+1). فایل‌های دیگر دهک‌ها فقط خوانده می‌شوند.

## ۱) انگیزه — ablation اجباری سمت short (قانون §۵ سند MTF)

S526 (سقف تازهٔ ۹۰ ⇒ LONG) روی H8 با ACCEPT 88.2 پذیرفته شد. قانون خانوادگی
(اجراشده برای WPR توسط S522) می‌گوید هر لبهٔ جدید باید آینهٔ short خود را رسماً
داوری کند تا مشخص شود لبه «تقارنی» است یا «جهت‌مند» (سواری بر روند سکولار).
این هم سند علمی است هم ممیزی ریسک برای استقرار احتمالی آیندهٔ S526.

## ۲) قانون منجمد (تبدیل آینه‌ای محض — صفر انتخاب)

- **رویداد:** `nl = close < min(close[i−90..i−1])`؛ سیگنال = لبهٔ تازه `nl & ~nl[i−1]` ⇒ **SHORT** در open کندل بعد.
- **براکت:** SL=1.5×median(ATR100)، TP=1.5×SL — عین S526، سمت short.
- **شبیه‌ساز:** `simulate_trades(..., is_long=False)` به‌طور یکنواخت برای سیگنال
  و **هر دو نول** (دستورکار S522 — نول short صادقانه).
- **نول شرطی‌شده:** رویداد آینه‌ای بنا به ساخت در فضای درفت-منفی است ⇒ هر دو نول
  فقط در کندل‌های `close[i−1]−close[i−90] < 0` نمونه می‌گیرند (uncond استرایدهای
  1/3/7 سخت‌ترین + perm K=2000 seed=20260805، حاشیه 200/n−2).
- **کارت‌ها:** H4/H6/H8/H12/D1 (عین S526؛ data/full، fallback data/ برای H4).

## ۳) بودجهٔ چندگانگی
ادامهٔ خانوادهٔ S526: n_trials = 5 (S526) + 5 (این آینه) = **10**.

## ۴) فرضیه و ابطال‌گرها — منجمد

- **H_dir (پیش‌بینی رسمی گاوس، هم‌راستا با S522/S524):** لبهٔ خانواده جهت‌مند
  است؛ آینهٔ short روی همهٔ کارت‌ها lift ضعیف/منفی خواهد داشت (< +4pp).
  تحقق H_dir = تثبیت قانون جهت‌مندی برای دومین خانوادهٔ مستقل + سند ریسک S526.
- **F1 (شگفتی):** اگر کارتی lift > +4pp و z بالای سد H5 (n_trials=10) داشت ⇒
  لبهٔ تقارنی است؛ حکم موتور هرچه گفت همان — بدون جراحی.
- **هم‌پوشانی:** برای هر کارت غیرمنفی، ممیزی فوری با لایو S382-H4 (قانون).

## ۵) گردش‌کار
runner (هارنس S526 + wrapper سراسری is_long=False از S522) → اجرا → چک‌پوینت
per-card فوری → MD حکم → گزارش خلاصه (توقف فقط اگر ACCEPT).

*کامیت این فایل = مهر زمانی پیش‌ثبت. هیچ عددی قبل از آن محاسبه نشده.*
