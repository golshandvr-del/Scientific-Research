# S870 — الحاقیهٔ ۱: **ابطالِ پیش‌ثبتِ نخست** (شکافِ آخرِ هفته) پیش از هر آزمونِ جهت‌دار

> **نوعِ سند:** ابطالِ رسمی (retraction). صفر آزمونِ جهت‌داری روی مفهومِ
> شکافِ آخرِ هفته توسطِ این نشست اجرا شد.

## ماجرا — شفاف و کامل

1. این نشست `results/S870_PREREG_WEEKEND_GAP_FADE.md` را نوشت و commit کرد
   (commit `32a11489`).
2. هنگامِ push و `git pull --rebase`، تغییراتِ یک **نشستِ موازی** وارد شد و
   معلوم شد همان مفهوم پیش‌تر با شمارهٔ **S810** پیش‌ثبت، اجرا و داوری شده:
   - `results/S810_PREREG_WEEKEND_GAP_HOLDOUT.md` (مسیرِ C، برش 2019-01-01)
   - `results/_s810/judgment_m1.json` → **حکم: REJECT · score=0.0**
     (برندهٔ نیمهٔ اول: cont/thr=0.5/SL80/RR1 با z=1.97 و PnL=−508 pip؛
     در هولد‌اوت تقریباً همهٔ دروازه‌ها قرمز)
   - `results/_s810/HOLDOUT_SPENT.lock` → هولد‌اوتِ این مفهوم **سوخته** است.
3. طبق قانونِ «تکرارِ مفهومِ سوخته ممنوع»، پیش‌ثبتِ نخستِ S870 **باطل** اعلام
   می‌شود. فایلِ آن برای شفافیتِ تاریخی حفظ می‌شود ولی هیچ آزمونی بر مبنای آن
   اجرا نخواهد شد.

## چرا این ابطال هزینهٔ چندگانگی ایجاد نمی‌کند

هیچ عددِ جهت‌داری توسطِ این نشست دیده یا تولید نشد؛ تنها تحلیلِ توانِ کورِ
اندازهٔ شکاف (اعلام‌شده در §۳ همان پیش‌ثبت) انجام شد. ابطال، پیش از نخستین
آزمون رخ داد ⇒ `n_trials` این نشست از این مفهوم صفر است.

## قلمروهای رزروشدهٔ نشست‌های موازی (برای پرهیز از برخورد)

- `S800` فشردگی→گشایشِ نوسان (پیش‌ثبت‌شده، در انتظار اجرا)
- `S890` شکستِ بازتابیِ کانالِ close (پیش‌ثبت‌شده، در انتظار اجرا)
- سوخته‌ها: `S810` شکافِ هفتگی، `S830` بازگشتِ وحشت، `S850` تشدیدِ چندمقیاسی؛
  `S840` شوکِ انگل = POWER-LIMITED.

## گامِ بعد

پیش‌ثبتِ جایگزینِ S870 (نسخهٔ ۲) با مفهومِ بکرِ «سطوحِ رُندِ روانیِ طلا»
در سندِ جداگانه نوشته و **پیش از هر آزمونِ جهت‌دار** commit می‌شود.
