# S894 — پیش‌ثبت: «برداشتِ لبهٔ تقویمیِ ساعتِ پایانی» (Calendar-Hour Harvest) · XAUUSD

> ⏱ **commit و push پیش از هر اجرا.** موتور: RQS2 v2.6 · نویسنده: جورج سوروس (S890–S899)
> منشأ علمی: یافتهٔ S893 — لبهٔ ساعت-محورِ خوشهٔ H1/H2/H3 (لیفتِ کانونی +4.9 تا +6.3pp،
> z≈8σ روی لنگرِ H2) **واقعی اما با رانشِ خام برداشت‌ناپذیر** (PF=1.07، exp@2×=−2.0).

---

## ۰. 🔴 بدترین خبر، اول

۱. **این ادامهٔ یک رگه است، نه ایدهٔ بکر** ⇒ کلِ تاریخچهٔ S892/S893 در `n_trials`
   شمرده می‌شود: **1488 = 912 (میراث) + 576 (جست‌وجوی جدید)**. سدِ H5 بالا می‌رود
   (`expected_max_z(1488)≈3.8σ`) — این را آگاهانه می‌پذیرم.
۲. **خطرِ اصلی: لبه از جنسِ «همه‌جا کمی» باشد نه «جاهایی زیاد».** اگر اهرمِ انتخاب
   lift را متمرکز نکند، PF از 1.07 به 1.3 نمی‌رسد و REJECT اقتصادی تکرار می‌شود.
۳. **درسِ S603 (همین امروز):** فیلتری که فقط حذف می‌کند و lift را بالا نمی‌برد، توانِ
   آماری می‌سوزاند. پادزهر: قاعدهٔ قفل، فیلترِ بی‌اثر را خودکار کنار می‌گذارد (زیر).
۴. **درسِ میکرو-TF (۳ بارِ من + S800/S981):** زیر M30 هزینه لبه را می‌خورد. قاعدهٔ
   توقفِ از-پیش-اعلام: اگر بهترین `exp@2×cost` اکتشافی کارتی ≤0 باشد، شلیکِ نهایی
   انجام نمی‌شود ⇒ همان‌جا UNPROVEN.

## ۱. خانوادهٔ جست‌وجو (قفل‌شده — فقط روی ۷۰٪ اول)

**رویداد (میراثِ S892، تغییرناپذیر):** اولین کندلِ ساعتِ H سرور؛ **فقط لانگ**
(خوشهٔ کشف‌شده). `H ∈ {21, 22, 23}`.

**اهرمِ ۱ — هندسهٔ خروج (۳ بازو):**
- **A (مرجع/S892):** SL=1.5×ATRᵐ · TP=1.5×SL · hold=4h
- **B (خروجِ زمانی، روحِ S560):** SL=TP=3.0×ATRᵐ (براکتِ نادر-فعال) · hold=4h
- **C (برداشتِ WR-بالا):** TP=1.0×ATRᵐ · SL=2.058×ATRᵐ · hold=4h

(ATRᵐ = میانهٔ ATR(100) روی ۷۰٪ اول، همان رویهٔ S892 — هیچ عددِ گِرد.)

**اهرمِ ۲ — فیلترِ انتخابِ علّی (۴ بازو):**
- **none** · **drift**: close[t−1] > close[t−bars(24h)] (هم‌راستاییِ رانشِ روزانه، روحِ S950)
- **calm**: ATR100[t−1] < چندکِ q70 رولینگِ علّیِ ATR100 روی 5000 کندلِ قبل (min 500؛ روحِ S404)
- **drift+calm**

⇒ ۳×۳×۴ = **۳۶ پیکربندی/کارت**. کارت‌ها: هر ۱۶ TF ساعت-دار (M1…H12)؛ D1/W1/MN1 INCOMPLETE.

## ۲. قاعدهٔ قفل و مسیرِ چندگانگی (C — hold-out)

- کشف فقط روی ۷۰٪ اول. شرطِ اهلیت: `n_IS ≥ 100` **و** `exp@2×cost_IS > 0`.
- در میانِ اهل‌ها، قفل = بیشینهٔ `t_pnl = exp_pip/σ_pnl×√n` (پایداری، نه قله‌گزینی).
- اگر هیچ پیکربندی اهل نبود ⇒ کارت **UNPROVEN بدونِ لمسِ هولد-اوت** (انضباطِ S982).
- شلیکِ نهایی: **یک** `compute_rqs2` روی کلِ داده با `split_bar=70٪` · `n_trials=1488`.
- **نالِ کانونیِ هندسه-همتا** (قانونِ دائمیِ دهه از S893): ۳ قرعهٔ یکنواختِ غیرشرطیِ
  بذردار {894,1894,2894} + جای‌گشتِ زمانی K=500 seed=894 برای هر کارتِ شلیک‌شده.

## ۳. تعهدها

- صفر تغییر پس از دیدنِ هولد-اوت؛ حکمِ موتور عیناً در نامِ فایل.
- commit تدریجی هر کارت. اگر ACCEPT شد: توقف + حسابرسیِ هم‌پوشانی (S312 تقویمی-M30
  نزدیک‌ترین خویشاوند است — event-driven overlap الزامی) + گزارش به کاربر.
- شکست هم درس است: «آیا لبهٔ 8σ تقویمی با اهرمِ انتخاب برداشت‌پذیر می‌شود؟» پاسخِ
  منفی = بستنِ صادقانهٔ کلِ رگهٔ ساعت-محور در دههٔ S890 (قانونِ مرگِ ابدی).
