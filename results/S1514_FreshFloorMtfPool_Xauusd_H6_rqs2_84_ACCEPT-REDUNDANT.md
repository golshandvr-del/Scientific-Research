# S1514 — استخرِ چند-TF رکوردِ تازهٔ کف — XAUUSD — موتور: ACCEPT 84.4 — حکمِ علمی: **ACCEPT-REDUNDANT** (استخر به S1511-H6 تباهیده شد؛ لایهٔ جدیدی نیست)

**داور:** RQS2 v2.6 — verbatim · **نویسنده:** Isaac Newton — بلوک S1510–S1519، شمارهٔ ۵ از ۱۰ · 2026-09-05
**پیش‌ثبت:** `results/S1514_PREREG_FRESH_FLOOR_MTF_POOL.md` (کامیت `7937e2df` — قبل از هر عدد)
**پیاده‌سازی:** `strategies/s1514_floor_pool.py` · **رکوردها:** `results/_scan_S1514/{H6,H8,H12}_member.json`، `*_trades.csv`، `pool_verdict.json`، `pool_trades.csv`، `pool.log`
**داده:** `data/mt5_full/` ۱۵.۵۹ سال (گارد E-16) · اسپرد 3.3 · n_trials=30 · SEED=20260908 · نولِ اعضا مشروط drift>0 K=500

## ۱) حکم موتور — عیناً
```
اعضا (نول مشروط K=500):
  H6 : n=279 wr=50.90 lift=+9.27 z=4.17
  H8 : n=219 wr=48.86 lift=+5.56 z=2.38
  H12: n=158 wr=48.73 lift=+3.71 z=1.23
pool_cards (شرط ۴ همگنی): chosen=['XAUUSD-H6'] · dropped: H8 'dilutes pool', H12 'dilutes pool'
n_before=279 → n_after=279 · سهم: H6=100%
S1514-POOL OFFICIAL  | ACCEPT RQS2=84.4 | n=279 WR=50.90% PF=1.62 lift=+9.27pp z=3.1 | 11/11 ✓
S1514-POOL STRESS(100)| ACCEPT RQS2=83.7 | همان
```

## ۲) چرا این ACCEPT «موفقیتِ جدید» نیست — صداقتِ علمی
موتورِ همگنیِ `rqs2_pool` (که پیش‌ثبت کردم «موتور تصمیم می‌گیرد، من نه») **هر دو عضوِ جدید را حذف کرد** چون z_proxy استخر را پایین می‌آوردند. استخرِ نهایی = **دقیقاً همان ۲۷۹ معاملهٔ S1511-H6** (سند `S1511_..._85_ACCEPT.md`)، با نولِ مستقلِ K=500 (به‌جای 2000) که همان حکم را بازتولید کرد (84.4 vs 84.8). هیچ معاملهٔ جدید، هیچ اطلاعِ جدید. طبق سابقهٔ S1580 (`ACCEPT-REDUNDANT`)، این شماره **در شمارشِ لایه‌های موفقِ بلوک به‌عنوان ACCEPT جدید ثبت نمی‌شود**؛ حکمِ موتور صادقانه گزارش می‌شود، ولی ادعای لایهٔ نو نمی‌کنم و طبق قانونِ کاربر برای «لایهٔ موفق» توقف نمی‌کنم — این لایه *ناموفق در هدفش* بود.

## ۳) ابطال‌گرها
| ابطال‌گر | پیش‌بینی | نتیجه |
|---|---|---|
| **P1** z_pool > 3.10 | باید بزرگ‌تر | **✗** z=3.1 (= S1511؛ هیچ افزایشی) |
| **P2** n_pool 380–450 | — | **✗** n=279 (<330) ⇒ همان‌طور که پیش‌ثبت کرده بودم: **«pooling چند-TF روی رویدادِ ساختاریِ یکسان بی‌فایده است»** — H8/H12 نه‌تنها هم‌پوشانِ تقویمی‌اند، بلکه کیفیتِ پایین‌ترشان استخر را رقیق می‌کند |
| **P3** H7 سبز، سال‌های ضعیف باقی | — | H7 ✓؛ استخر همان S1511 است پس سال‌های ضعیف عیناً باقی ✓ (پیش‌بینی درست ولی بی‌مزه) |
| پیش‌بینی شخصی n≈410, z 3.3–3.9 | — | ✗ هر دو |

## ۴) قانونِ S1514 (برای دیتابیس)
**قانونِ تباهیدگیِ استخر:** استخرِ چند-TF فقط وقتی توان می‌افزاید که اعضا «رویدادهای *متفاوت*» باشند (S604: D1-raw + H6-drift؛ S770: ADR روی D1 و H8 با دو منطق) یا کیفیتِ هم‌ترازی داشته باشند (S517: M15≈M30). وقتی یک رویدادِ ساختاری روی TFهای مجاور تکرار می‌شود، عضوِ TF بزرگ‌تر زیرمجموعهٔ ضعیف‌ترِ TF کوچک‌تر است و `choose_homogeneous_subset` — درست — آن را دور می‌اندازد. پیش‌سنجیِ لازم که نکردم: **z اعضا باید در یک ردهٔ هم‌تراز باشند** (4.17 vs 2.38 vs 1.23 = تباهیدگیِ پیش‌بینی‌پذیر).
**نتیجهٔ بلوک:** خانوادهٔ کفِ تازه در ۴ شماره (S1511–S1514) کامل کاویده شد: پایه ACCEPT-نازک، فیلترِ هم‌خط ✗، فیلترِ متعامد ✗ (توان)، pooling ✗ (تباهیدگی). **خانواده بسته.** S1515 باید پارادایمِ ورود را عوض کند.

## ۵) انطباق
③ ✓ · یک شماره = یک لایه (یک حکم رسمی + تنش) ✓ · mt5_full ✓ · TP≥SL ✓ · بدون تیون پس از عدد ✓ · rqs2_pool دست‌نخورده ✓ · سایت دست‌نخورده ✓ · گام بعد (خودکار): fetch ⇒ S1515 با پارادایمِ نو.

*نیوتن: جرمِ بیشتر آوردم، اما موتور گفت جنس‌شان یکی نیست — و حق داشت. همان سیب، همان شاخه.* 🍎
