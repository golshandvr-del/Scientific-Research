# S502 — پیش‌ثبت (PREREG): احیای بیمار S202 «SoS» با دروازهٔ درفت علّی | XAUUSD-H1

> **این سند قبل از هر آزمون قضاوت‌شده commit می‌شود.** (قانون چندگانگی، مسیر C)
> دهه: S500–S509 (قلمرو ارشمیدس). شمارهٔ S502 آزاد است (بررسی ۲۰۲۶-۰۸-۲۶).

## ۰) پیش‌نیازهای قانونی انجام‌شده
- fetch گیتهاب + بررسی همهٔ لایه‌های جدید موازی ✅ (تا head `745e50a5`؛ ۲۰ سند ACCEPT مرور شد؛
  یافتهٔ کلیدی: S523 با «null هم‌شرط برای فیلتر رژیم» اولین عبور 3.09 را ثبت کرد اما H5 خورد به
  n_trials=23773 ارثی؛ S516 دارد drift-align را روی پایهٔ **خودش** (S515 V-TIME) تست می‌کند —
  پایهٔ من S202/SoS است؛ تداخل قلمرو نیست، **همکاری** است و هر دو منبع ذکر می‌شوند)
- قلمرو SoS/S202 توسط هیچ دانشمند موازی ادعا نشده (grep در همهٔ نتایج S51x..S9xx: خالی) ✅

## ۱) بیمار و شواهد
کارت لنگر از دفتر رسمی `results/_audit_rename/verdicts/S202_H1_PriceAction_SoS_Inertia_NetProfit_252471.json`:

| مورد | مقدار |
|---|---|
| قانون | SoS rising-edge (thr=2, ema=20, win=32) → LONG، ورود کندل بعد |
| داده | XAUUSD_H1 کامل ۲۰۱۱-۰۱→۲۰۲۶-۰۷ (**از ابتدا full-span — بیماری پنجرهٔ S225/S501 ندارد**) |
| هندسه | SL=250 / TP=375 (rr=1.5 ≥ 1 ✅ قانون حفظ بودجه) / mh=96 |
| کارنامه | n=603، WR=52.57، lift=+4.55pp، z=2.24، PF=1.32، H9=+25.3 — فقط H3،H5 قرمز |
| بازتولید | این جلسه، بیت‌سازگار: n=603 wr=52.57 net=9619 pf=1.32 ✅ |

سقف شیشه‌ای: z=2.24 < 3.09. برای عبور در n≈400 (پس از فیلتر) نیاز به lift ≈ +7.9pp.

## ۲) فرضیهٔ مکانیزم (H_drift²)
«لبهٔ SoS طلا — مثل لبهٔ WPR (زنجیرهٔ S520→S523) و پرش-پس‌لرزه (S950) — **جهت‌دار و متمرکز در رژیم
صعودی** است. حذف سیگنال‌های SoS که با درفت میان‌مدت ناهم‌راستایند باید lift را ≥ +3pp بالا ببرد.»
- منابع مستقل و منجمد: تعریف درفت عین S950-ACCEPT/S523: `close[i−1] − close[i−1−90] > 0` (علّی؛ L=90 **بدون جاروب** — همان عدد ACCEPT شده).
- پایه (SoS) از S202 منجمد؛ فیلتر (درفت) از S950 منجمد؛ هندسه از کارت لنگر منجمد ⇒ **صفر پارامتر آزاد**.

## ۳) قانون منجمد S502 (قبل از هر اجرا)
```
سیگنال = sos_rising_edge(df, thr=2, ema=20, win=32)   # عین s202 (خودش shift(1) دارد)
گیت    = close[i−1] > close[i−91]                      # درفت L=90، فقط کندل‌های بسته
ماسک   = سیگنال ∧ گیت → LONG-only
خروج   = SL=250 / TP=375 pip، max_hold=96، allow_overlap=False، spread=3.3pip
داده   = data/XAUUSD_H1.csv کامل (۲۰۱۱→۲۰۲۶)
```

## ۴) مدل صفر هم‌شرط (درس روش‌شناختی S523 — الزامی)
هر دو مرجع در **فضای drift>0** نمونه‌گیری می‌شوند، نه هرجا:
- `uncond_wr` = ورود بی‌قید LONG در همهٔ بارهای معتبرِ drift-مثبت (همان براکت)
- جایگشت K=2000 (بذر 23): k ورودی تصادفی از بارهای drift-مثبت
بدون این، فیلترِ رژیم خودش را می‌ستود و lift = بتای رژیم می‌شد (درس مشترک S501/S523).

## ۵) معیارهای ابطال پیش‌ثبت‌شده (self-REJECT، قبل از داور)
- **F1 (مکانیزم مرده)**: WR معاملات فیلترشده ≤ WR پایهٔ بی‌فیلتر (52.57%) ⇒ self-REJECT.
- **F2 (کنترل علمی ضد-درفت، سبک S950)**: WR زیرگروه drift≤0 از معاملات پایه باید < WR فیلترشده
  باشد؛ اگر ≥ بود، روایت مکانیزم دروغ است ⇒ self-REJECT.
- **F3 (کفایت نمونه)**: n فیلترشده < 120 ⇒ self-REJECT (توان بی‌معنا).
- دروازهٔ بازتولید لنگر: n=603±5٪ و WR=52.57±2pp؛ شکست ⇒ STOP بدون قضاوت.

## ۶) داوری
یک فراخوان `engine.rqs2.compute_rqs2` v2.6 روی کل بازه:
`sl_pip=250, tp_pip=375, bar_time, close, null=هم‌شرط K=2000, holdout=۴۰٪ آخر (SPLIT_FRAC=0.60), allow_overlap=False`
`np.random.seed(20260813)` قبل از فراخوان.

## ۷) حسابداری n_trials (صادقانه)
- ارث S202 (کل باتری جستجوی ثبت‌شده در دفتر): **267**
- S502: **۱** پیکربندی (صفر پارامتر آزاد — همهٔ اجزا از منابع منجمد مستقل)
- **n_trials = 268** ⇒ کران H5 ≈ 2.86σ < 3.09σ ⇒ **H3 گیت مقید است، نه H5** — نقطهٔ قوت این
  خانوادهٔ کم‌آزمون در برابر سرنوشت S523 (که ۲۳۷۷۳ آزمون ارثی داشت).

## ۸) ممیزی هم‌پوشانی (الزامی، پس از داوری، قبل از حکم نهایی)
- vs **S356** (ACCEPT لایو، H1 LONG — همان TF و جهت! بحرانی‌ترین): بازتولید سیگنال لایو از
  `s450_paired_s356.FROZEN` و شمارش برخورد ±2 بار.
- گزارش درصد هم‌پوشانی + lift زیرگروه مستقل (nonOV) مثل الگوی S523.

## ۹) ثابت‌های منجمد
SEED=20260813 · K_PERM=2000 · SPLIT_FRAC=0.60 · pip=0.10 · spread=3.3 · comm=0 · N_TRIALS=268

## ۱۰) تعهد
اگر REJECT شد: گزارش کوتاه، ثبت درس، و حرکت خودمختار به S503. اگر ACCEPT شد: توقف و گزارش به فرمانده.

— ارشمیدس، ۲۰۲۶-۰۸-۲۶
