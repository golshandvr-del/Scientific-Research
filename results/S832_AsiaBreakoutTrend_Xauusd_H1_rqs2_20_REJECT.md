# S832 — شکستِ متقارنِ رنجِ آسیا + همراستاییِ EMA300 — XAUUSD-H1 — حکم نهایی

**حکم: REJECT (امتیاز 20.9) — درسِ بزرگ: لبه‌ی «مدیریتی» لبه‌ی «اطلاعاتی» نیست**

- استراتژی: `strategies/s832_asia_breakout_trend.py`
- پیش‌ثبت: `results/S832_PREREG_ASIA_BREAKOUT_TREND_HOLDOUT.md` (کامیت `0d4af129`، قبل از آزمون)
- نال: `results/_scan_S832/s832_null_holdout.json` (K=500, seed=832832)
- حکم: `results/_scan_S832/s832_judgment_h1.json` · قفل: `S832_HOLDOUT_SPENT.lock`
- محدوده‌ی مجاز: S830–S839 ✅ (بدون تماس با کار دانشمندان موازی)

## ۱) اعداد هولد‌اوت (2020-05-27 تا 2026، ۴۰٪ محافظت‌شده)

| متریک | اکتشاف (مرجع پیش‌ثبت) | هولد‌اوت | نال جای‌گشتی |
|---|---|---|---|
| n | 1,224 | 881 (L=525 S=356) | K=500 |
| WR | 48.94% | **61.86%** | **60.07%** (sd=1.39) |
| PF | 1.412 | **2.008** | — |
| exp | +6.07pip | +22.71pip | — |
| lift vs null | +33.9pp (vs سربه‌سرِ هزینه) | **+1.79pp** (vs نالِ هم‌هندسه) | — |

گیت‌ها: H0✓ H1✓ H2✓ **H3✗ (lift=1.79pp < 4pp، z=1.29 < 3.09)** H4✗ H6✗ H7✓ H8✓ H9✓ H10✓

## ۲) کالبدشکافی — چرا PF=2.0 ولی REJECT؟ (مهم‌ترین درسِ سه لایه‌ی اخیر)

1. **نالِ درست، توهم را کشت.** نال جای‌گشتیِ جهت با *همان هندسه‌ی کامل* (SL=2.1×ATR،
   TP=6×SL، trail=0.7×ATR، hold=55) اجرا شد. نتیجه: جهتِ **تصادفی** روی همین
   کندل‌های ورود WR=60.07% می‌گیرد! trailing تنگ + TP دور در بازارِ پرنوسانِ
   ۲۰۲۰+ برای هر ورودی سود قفل می‌کند. **اقتصادِ ظاهری از مدیریت بود، نه از سیگنال.**
2. **سهم اطلاعاتیِ واقعیِ «شکست رنج آسیا + EMA300» فقط +1.79pp است** (z=1.29).
   یعنی جهت‌گیریِ سیگنالِ ما تقریباً به‌اندازه‌ی سکه‌انداختن روی همان کندل‌هاست.
3. **مقایسه با اکتشاف گمراه‌کننده بود**: در اکتشاف lift را نسبت به «سربه‌سرِ
   هزینه‌دار» می‌سنجیدیم (سنجه‌ی اقتصادی)، نه نسبت به نالِ هم‌هندسه (سنجه‌ی
   اطلاعاتی). PF=1.41 اکتشاف هم به احتمال زیاد عمدتاً مدیریتی بود.
4. **تقارن کار کرد ولی کافی نبود**: برخلاف S830/S831، این‌بار لبه در هولد‌اوت
   «نمرد» (PF حتی بالاتر رفت) — طراحیِ متقارن + جهتِ درون‌زاد در برابر تغییرِ رژیم
   مقاوم بود. اما RQS2 درست می‌پرسد: «آیا *سیگنالِ تو* چیزی می‌داند؟» پاسخ: نه.

## ۳) میراث برای لایه‌های بعدی (بانک درس‌ها)

- **درس S832**: هرگاه مدیریتِ pathدار (trail/BE) در هندسه هست، سنجه‌ی اکتشاف باید
  **lift نسبت به نالِ هم‌هندسه** باشد، نه سربه‌سرِ هزینه. وگرنه فلاتِ زیبا فقط
  فلاتِ مدیریت است. (این دقیقاً مکملِ درسِ S830 «قله‌ی تیز» است: فلاتِ پهن هم
  می‌تواند فریب باشد اگر محورش سیگنال نباشد.)
- WR نالِ ۶۰٪ با جهتِ تصادفی یعنی خودِ هندسه‌ی «trail تنگ + TP دور» در طلای ۲۰۲۰+
  ماشینِ WRسازی است — هر لایه‌ی آینده با trail باید این پایه را صفرِ مرجع بگیرد.
- زنجیره‌ی سه‌گانه: S830 (رژیم‌وابستگی) → S831 (تک‌جهتی‌بودن) → S832 (مدیریت≠اطلاعات).
  لایه‌ی بعد باید هر سه پادزهر را هم‌زمان داشته باشد **و** lift اطلاعاتی‌اش از
  ابتدا در اکتشاف با نالِ هم‌هندسه سنجیده شود.

## ۴) انضباط رعایت‌شده

- جست‌وجو فقط روی ۶۰٪ اکتشاف (۶ کاوش، همه ثبت و push شده).
- پیش‌ثبت قبل از آزمون کامیت شد (`0d4af129`)، `n_trials=1`، آزمونِ واحد، قفل نوشته شد.
- داده‌ی کامل ۱۵.۶ سال `data/mt5_full` (تله‌ی E-16 رعایت). EURUSD: هرگز.
- trail مشکوکِ 0.2×ATR (PF=2.11 اکتشافی) تعقیب نشد — مستند در کاوش ۶.
- MTF/همپوشانی: طبق قانون فقط برای بازمانده‌ها — اجرا نشد.

— جیم سایمونز · S832 · 2026-08-20
