# S911 — پیش‌ثبتِ رسمی · «اشتعال روحیات حیوانی» (Wide-Range Bull Ignition) · XAUUSD

> **تاریخ ثبت:** 2026-08-14 · **نویسنده:** Builder-AI (نقش: جان مینارد کینز)
> **وضعیت:** کامیت **پیش از اجرای هر تستِ نهایی** — مطابق §H5 در `docs/RQS2_SPEC.md`.
> **پیشینه:** S910 (اجماع چندافقی) REJECT شد؛ درس‌هایش در طراحی حاضر لحاظ شده است.

## ۱) مفهوم

«روحیات حیوانی: انگیزهٔ خودجوش به عمل». کندلِ گاویِ با دامنهٔ استثنایی که نزدیکِ
سقف می‌بندد = اشتعال جمعیِ انتظارات. فرضیه: پس از اشتعال، ادامهٔ صعود در طلا
(دارایی رونددوست) محتمل‌تر از برگشت است. **فقط Long** — درس S910: اجماع نزولی
در طلا سم است (z=−۱.۰۴).

## ۲) تعریف سیگنال (منجمد — صفر جاروب)

روی کندل `i`:
- `range_i = high−low` · `body_pos = (close−low)/range` · گاوی: `close>open`
- **اشتعال:** `range_i >` صدکِ **۸۹** از ۲۳۳ کندلِ قبلی (پنجرهٔ غلتان، بدون کندل جاری)
  **و** `body_pos ≥ 0.786` **و** گاوی.
- لبه: کندل قبل اشتعال نبوده. ورود در **کندل بعد**.
- اعداد ۸۹، ۲۳۳ (فیبوناچی) و 0.786 (نسبت طلایی) — ضد-اشتباهِ ۷؛ همین‌جا منجمد شدند.

## ۳) مسیر چندگانگی: **مسیر C — تأیید هولد-اوت**

- تقسیم ۶۰/۴۰ (train/holdout) — همان قرارداد S910.
- **کارت اصلی: XAUUSD-M30** (هزینه/SL≈۶٪، نمونهٔ مورد انتظار در train ≥۱۰۰۰).
- جست‌وجوی مجاز فقط در train: هندسه با قاعدهٔ ثابت `TP=q60(MFE)` · `SL=q30(MAE)` ·
  قید `TP≥SL` · `max_hold=55` (فیبوناچی، منجمد).
- **تست نهایی روی هولد-اوتِ M30: دقیقاً ۱ اجرا** ⇒ `n_trials=1` برای کارت اصلی.
- خانواده: پیکربندی منجمد روی هر ۱۹ TF طلا، فقط ناحیهٔ train (توصیفی)؛ هر کارتِ
  غیر-اصلی که به داوری برود `n_trials=19`. EURUSD مطلقاً خارج از دامنه.

## ۴) قیود از پیش

- `TP≥SL` همیشه؛ اگر لبهٔ train زیر هزینه (۳.۳ پیپ) بود، همان‌جا می‌سوزد.
- null اختصاصی بازو (الگوی s437)، `K_PERM=500`، گارد BUG-PERMK.
- حسابرسی هم‌پوشانی با لایه‌های سایت (به‌ویژه S312-M30 و S356-H1) با
  `engine/trade_simulator.py` **فقط اگر** حکمی غیر-REJECT بگیرد؛ بخش هم‌پوشان
  به‌عنوان فیلتر هم آزموده می‌شود.
- حکم موتور عیناً گزارش می‌شود؛ نتیجه در
  `results/S911_AnimalSpiritsIgnition_Xauusd_<TFs>_rqs2_<score>_<verdict>.md`.
