# S501 — پیش‌ثبت (PREREG): احیای بیمار S225 «فنر فشردهٔ بولینگر» با گسترش n به تاریخچهٔ کامل
**تاریخ:** 2026-08-18 · **پژوهشگر:** ارشمیدس (بلوک شماره‌های S500–S509) · **مسیر چندگانگی:** C
**وضعیت:** این سند **قبل از هر آزمون نهایی** commit می‌شود. هیچ عدد قضاوتی هنوز تولید نشده.

---

## ۰) شجره و انگیزه
- بیمار: کارتِ حسابرسی‌شدهٔ S225 (احیای S91/S132 — Squeeze-Breakout طلا)،
  `results/_audit_rename/verdicts/S225_..._INCOMPLETE.json` کارت idx=11:
  **XAUUSD-M30، n=287، WR=59.23، lift=+6.80pp، z=2.31، p_perm=0.0106، PF=1.44،
  maxDD=4.34٪، H9=+25.1pip، SL=200/TP=200 (متقارن!) — فقط H3/H5 قرمز.**
- تشخیص: S225 فقط روی **۴ سال آخر** (last_n_years=4، ~۲۰۲۲/۰۷ → ۲۰۲۶/۰۷) اجرا شده،
  اما `data/XAUUSD_M30.csv` از **۲۰۱۱/۰۱/۰۳** موجود است. یعنی **۱۱.۵ سال پنجرهٔ بکر (W0)**
  که هرگز توسط جستجوی S225 لمس نشده.
- فرضیهٔ H-S501: «لبهٔ انفجار-از-فشردگی روی XAUUSD-M30 در کل تاریخچه (۲۰۱۱→۲۰۲۶)
  پابرجاست؛ با گسترش n از ~۲۹۰ به ~۹۵۰+، دروازهٔ H3 (z≥3.09) بدون تغییر هیچ
  پارامتری قابل عبور می‌شود.» — همان مکانیزم احیای موفق S356 (گسترش n، صفر پارامتر جدید).

## ۱) اِعمال درس‌های ثبت‌شده
- **درس S430:** انتخاب با عصارهٔ H9 (این کارت +25.1pip در ۲×هزینه — بالاترین میان بیماران درمان‌پذیر).
- **درس S500 (رقیق‌سازی تجمیع):** اینجا تجمیع چندکارتی **نداریم** — یک کارت، یک قانون؛ درس رعایت شد.
  به‌علاوه در این خانواده σ_perm ≈ σ_binomial (z ممیزی 2.306 = z دوجمله‌ای دقیق) — تورمی در کار نیست.
- **اکتشاف بیمودال:** هندسهٔ متقارن SL=TP=200 (ژن برنده‌ها)، صفر پارامتر آزاد جدید.

## ۲) قانون منجمد (هیچ درجهٔ آزادی باقی نمانده)
سیگنال LONG روی close کندل i (ورود کندل بعد، مطابق بازتولید امروز):
1. `BandWidth(BB20,k=2) = (upper−mid)/mid` در کندل i−1 در پایین‌ترین **۱۵٪** صدکِ ۱۰۰ کندل اخیر؛
2. `close[i] > max(high[i−10..i−1])` (شکست صعودی)؛
3. `EMA50[i] > EMA200[i]` (گیت روند).
پیاده‌سازی مرجع: `strategies.s225_s91_squeeze_wr60.base_s91_squeeze` (import مستقیم — صفر واگرایی).
- خروج: **SL=200pip / TP=200pip (ثابت، متقارن) · max_hold=48 · LONG-only · allow_overlap=False**
- دارایی: XAUUSD (pip=0.10، spread=3.3pip، comm=0) — `engine.scalp_engine.ASSETS` استاندارد.
- **هیچ فیلتری از استخر ۱۳تاییِ S225 استفاده نمی‌شود** — قانونِ پایهٔ عریان.

## ۳) پروتکل قضاوت
- داور: `engine/rqs2.py` v2.6 — یک فراخوان `compute_rqs2` روی کل ۲۰۱۱→۲۰۲۶.
- null: `build_null_canonical` (جایگشت زمانی هم‌تعداد، K=2000، seed=23) — استاندارد خانواده.
- holdout: ماسک استاندارد موتور (۴۰٪ آخر بر حسب زمان ورود).
- **n_trials=97** (چتر صادقانه: ~۳۶ سلول هندسهٔ جستجوی تاریخی S225 روی W1 + انتخاب از
  دفتر ~۳۴ بیمار + حاشیه؛ همان سطح S500/S356). توجه: z_luck(97)<3.09 ⇒ H5 زیرمجموعهٔ H3.
- SEED=20260813.

## ۴) افشای صادقانهٔ آلودگی و پادزهر آن
- ۴ سال آخر (W1) پنجرهٔ جستجوی S225 بوده؛ holdout داخلی موتور (۴۰٪ آخر ≈ ۲۰۲۰→۲۰۲۶)
  بخشی از W1 را در بر می‌گیرد. **پادزهر پیش‌ثبت‌شده (شرط هم‌ارزِ اولیه):**
  **اگر lift زیرگروه W0 (فقط ۲۰۱۱→۲۰۲۲/۰۷/۱۶، پنجرهٔ کاملاً بکر) ≤ 0 باشد،
  S501 خودرد (self-REJECT) است — فارغ از حکم موتور روی کل بازه.**
  (W0 امروز فقط «شمارش سیگنال» شد — ۱٬۱۰۲ سیگنال؛ هیچ نتیجه/WR روی W0 دیده نشده.)

## ۵) دروازهٔ بازتولید لنگر (قبل از قضاوت)
اجرای قانون منجمد روی پنجرهٔ ۴سالهٔ S225 باید بدهد: **n در ±۱۰٪ از ۲۹۴ و WR در ±۲pp از ۵۸.۱۶**
(اندازه‌گیری امروزِ من با دادهٔ فعلی؛ کارت ممیزی 287/59.23 با دادهٔ قدیمی‌تر). شکست ⇒ توقف.

## ۶) قانون مولتی‌تایم‌فریم
کارت اولیهٔ قضاوت: **فقط XAUUSD-M30** (تنها بیمار حسابرسی‌شدهٔ درمان‌پذیر).
M15 و H1 با همان قانون منجمد **فقط اندازه‌گیری و گزارش** می‌شوند (بدون ادعای قبولی،
بدون ورود به n_trials — افشای کامل در سند نتیجه).

## ۷) شرایط شکست (پیش‌ثبت)
- بازتولید لنگر ناموفق ⇒ STOP بدون قضاوت.
- lift زیرگروه W0 ≤ 0 ⇒ self-REJECT.
- حکم موتور ≠ ACCEPT ⇒ همان حکم، بدون هیچ چرخش/تیونِ پس‌ازمشاهده (قانون بیمودال: ۲۵ را ۸۰ نمی‌کنیم).
- ممیزی همپوشانی با ۵ لایهٔ زندهٔ طلا (به‌ویژه S312-M30) الزامی است و در سند نتیجه افشا می‌شود.

## ۸) هم‌زیستی با پژوهشگران موازی
بلوک من S500–S509 است. S501 آزاد است (بررسی شد: صفر رخداد در git log/results/strategies).
هیچ فایلی خارج از فضای نام S50x ایجاد/ویرایش نمی‌شود.
