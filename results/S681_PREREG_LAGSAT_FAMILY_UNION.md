# S681 — پیش‌ثبت: LAGSAT-UNION — اجتماعِ خانوادهٔ مدت-اشباع (وارثِ S680)

**نوشته‌شده پیش از هر اجرا** · موتور `RQS2 v2.6` · دادهٔ کامل ۱۵.۶ ساله
📌 **بدترین خبر در صدر**

---

## ۰. 🔴 بدترین خبر، اول

**این لایه پس از دیدنِ نتایجِ S680 طراحی شده ⇒ خطرِ برازشِ نسل دوم
(garden of forking paths). سه راهِ مشخصِ شکست:**

۱. **رقیق‌شدن:** اجتماعِ ۹ سلول یعنی سلول‌های ضعیف‌تر هم شلیک می‌کنند؛
   میانگینِ لیفتِ اجتماع می‌تواند بسیار زیر +۱۶.۲ppِ سلولِ صدر بیفتد و
   حتی زیرِ +۴pp (آستانهٔ H3) برود. ⇒ نتیجه: REJECT با n بزرگ‌تر.
۲. **همبستگیِ درونی:** ۹ سلولِ همسایه سیگنال‌های به‌شدت هم‌پوشان دارند؛
   شاید اجتماع n را فقط ۱.۵ برابر کند نه ۳ برابر — توان همچنان ناکافی.
۳. **شکستِ hold-out نسل دوم:** H7ِ D1 در S680 پاس شد ولی با نمونهٔ کوچک؛
   با نمونهٔ بزرگ‌ترِ اجتماع، H7 می‌تواند شکست بخورد و کلِ ایده بمیرد.
   اگر چنین شد، خانوادهٔ LAGSAT در همین‌جا **برای همیشه** بسته می‌شود
   (قانونِ مرگِ ابدی برای این خانواده در بلوکِ S68x — بدونِ استیناف).

---

## ۱. فرضیه — دقیق و ابطال‌پذیر

> روی XAUUSD-D1 (و به‌تبعِ قانونِ MTF روی همهٔ کارت‌ها)، رخدادِ
> «**هر یک** از ۹ عضوِ خانوادهٔ زیر شلیک کند» یک سیگنالِ واحدِ
> مدت-اشباع است که لبهٔ جهتیِ هم‌جهت با اشباع با `lift≥+4pp` و
> `z≥3.09` تولید می‌کند.

**خانوادهٔ قفل‌شده (پیش از اجرا — هیچ عضوی حذف/اضافه نمی‌شود):**
- `per ∈ {7, 11, 18}` (همسایه‌های لوکاسِ برندهٔ نیمهٔ اولِ S680-D1)
- `D ∈ {3, 5, 8}` (همسایه‌های فیبوناچیِ همان)
- `hi = 76` (lo=24) — ثابت
- سیگنال: `OR` روی ۹ سلول از تریگرِ لبه‌ای `runlen==D` (همان S680)
- **این اجتماع یک فرضیهٔ واحد است، نه گزینش** — هیچ «بهترین عضوی»
  انتخاب و گزارش نمی‌شود (پروتکلِ «گزینش نکن»ِ H5، سیاقِ S346).

## ۲. هندسه و اجرا (قفل)

- هندسه عیناً ارثِ S680: SL = 1.618×میانهٔ ATR34ِ **نیمهٔ اول** هر کارت
  (از JSONِ اکتشافِ S680 خوانده می‌شود — BUG-GEOMDRIFT)، TP = 2.0×SL
  (هرگز TP<SL)، max_hold همان جدولِ S680.
- ضدنشتی: ورود در openِ کندلِ بعد (رفتارِ موتور).
- allow_overlap=False؛ اگر چند عضو هم‌زمان شلیک کنند، موتور طبعاً
  یکی را اجرا می‌کند (قفلِ busy) — بخشی از تعریفِ فرضیه است.

## ۳. شمارشِ صادقانهٔ بودجهٔ چندگانگی

| قلم | تعداد |
|---|---|
| گریدِ اکتشافِ S680 (پیش‌ثبتِ قبلی) | ۴۰۵ |
| داوری‌های نهاییِ S680 | ۵ |
| این آزمون (اجتماعِ واحد، per کارت ولی فرضیهٔ واحد) | ۱ per کارت × ۱۹ ≤ ۱۹ |
| **جمعِ تجمعیِ خانوادهٔ LAGSAT** | **≤ ۴۲۹ < ۵۶۶** |

⇒ طبقِ حل‌شدهٔ `expected_max_z(566)=3.09`، سدِ H5 هنوز زیرِ H3 است.
**اعلامِ رسمی: `n_trials = 429`، `z_eff = 3.090`.**

## ۴. معیارِ موفقیت/شکست (پیش از دیدنِ هر عدد)

- ACCEPT فقط اگر **همهٔ ۱۱ دروازه** پاس شوند (تعریفِ رسمی).
- اگر D1 دوباره فقط-توان کم آورد (H3/H5 تنها ردها): مسیرِ رسمیِ بعدی
  تجمیعِ چند-کارتی است و پیش‌ثبتِ خودش را می‌خواهد (S682).
- اگر H7 یا H4 رد شود: خانوادهٔ LAGSAT در بلوکِ S68x **بسته** می‌شود.
- هیچ سلولِ منفرد گزارش/انتخاب نمی‌شود؛ فقط اجتماع.

## ۵. پروتکل

۱. اجرا روی هر ۱۹ کارتِ دادهٔ کامل (شروع M1 — قانون؛ یورو مستثنای صریح).
۲. داوری یگانه per کارت: کل داده، `split_bar=n//2`، نالِ کانونی K=500
   (دو خطِ مبنا، سه تلهٔ کنترل بسته)، تمامِ ورودی‌های compute_rqs2.
۳. حکمِ موتور عیناً؛ ذخیرهٔ افزایشی per کارت؛ کامیت+پوش فوری.
۴. در صورتِ ACCEPT: اطلاع به کاربر و توقف تا تأیید (قانونِ استقرار).

*فرما: اگر برهانِ نخست ده معامله کم داشت، برهانِ دوم را از همان مسیر ولی
با پنجرهٔ فراخ‌تر می‌نویسم — بی‌آنکه قلم را به دستِ وسوسهٔ گزینش بدهم.*
