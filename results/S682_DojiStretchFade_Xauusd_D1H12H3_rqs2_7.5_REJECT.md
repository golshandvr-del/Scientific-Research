# S682 — DojiStretchFade · XAUUSD · همهٔ ۱۹ کارت · RQS2 v2.6

**حکمِ سرخطِ لایه (عیناً از موتور، بهترین کارتِ داوری‌شده): `REJECT` · score = 7.5 (H12)**

- پیش‌ثبت: `results/S682_PREREG_DOJI_STRETCH_FADE.md` (کامیت `a0d674b3` — پیش از هر اجرا)
- داده: `data/mt5_full/XAUUSD_<TF>.csv` — ۱۵.۶ سالِ کامل (src در JSONها)
- موتور: `engine/rqs2.py` v2.6 + `engine/scalp_engine.py` · seed=20260826
- بودجهٔ چندگانگی: `n_trials = 247` (۱۲ سلول × ۱۹ کارت + ≤۱۹ داوری) < 566 ⇒ z_eff=3.090

---

## ۰. 🔴 بدترین خبر، اول

**هر ۳ کارتِ واجدِ داوری REJECT شدند و لبهٔ نیمهٔ اولِ D1 روی hold-out
کاملاً فروپاشید:** نیمهٔ اول exp=+30.9pip/z=3.57 → کلِ داده exp=**−20.3pip**،
lift=−2.8pp، z=−0.85. راهِ شکستِ ۱ پیش‌ثبت (گورستانِ mean-reversion) محقق شد.

## ۱. فرضیه (از پیش‌ثبت)

دوجی (body ≤ b×range) در کششِ افراطی (|close−EMA34|/ATR34 ≥ s) ⇒ خستگیِ
طرفِ غالب ⇒ فیدِ بازگشتی به میانگین. گریدِ قفل: b∈{0.1,0.25} × s∈{1.5,2.5} ×
rr∈{1,1.5,2}. هندسه: SL=1.618×medATR34 (غیرگرد per کارت)، TP=rr×SL≥SL،
mh جدولِ قفل، ورود openِ کندلِ بعد، allow_overlap=False.

## ۲. اکتشاف — فقط نیمهٔ اول (۱۹/۱۹ کارت، JSONها در `results/_s682_explore/`)

- M1…H8: دیوارِ اسپرد (بهترین‌ها exp<0) — ششمین تأییدِ قانونِ لبهٔ pip.
- کارت‌های واجد (n≥30، exp>0، فلات): **D1** (b=.25,s=1.5,rr=2: n=101,
  lift=+16.8pp, exp=+30.9, z=3.57)، **H12** (n=187, +6.8pp, +4.1)،
  **H3** (b=.1,s=2.5,rr=2: n=258, +9.6pp, +2.3, z=3.25).
- MN1: منحط (نیمهٔ اول ۹۴ کندل < گرم‌سازی). W1: n=3.

## ۳. داوریِ یگانه — کلِ داده، split_bar=n//2، نالِ کانونی K=500

| TF | نامزدِ قفل | n | WR% | exp_pip | z | p_perm | lift_pp | حکمِ موتور | score |
|---|---|---:|---:|---:|---:|---:|---:|---|---:|
| D1 | b=.25 s=1.5 rr=2 | 235 | 39.57 | −20.26 | −0.85 | 0.802 | −2.80 | **REJECT** | 2.5 |
| H12 | b=.25 s=1.5 rr=2 | 432 | 38.19 | +0.93 | −0.22 | 0.589 | −0.53 | **REJECT** | 7.5 |
| H3 | b=.1 s=2.5 rr=2 | 541 | 39.74 | −2.00 | +0.59 | 0.277 | +1.45 | **REJECT** | 4.6 |

گیت‌های ردشدهٔ مشترک: H1 (PF<1.3)، H3 (نال)، H4، H5، H6، H7 (hold-out)، H8.
حکم‌ها عیناً از `compute_rqs2` (کلیدهای `verdict`/`rqs2_score`).

## ۴. کالبدشکافی

۱. **فروپاشیِ hold-out در D1:** لبهٔ +16.8pp نیمهٔ اول در نیمهٔ دوم معکوس
   شد. دوجی-در-کشش روی D1 برازشِ نمونه بود، نه ساختار. مسیرِ C دقیقاً
   برای گرفتنِ همین ساخته شده و گرفت.
۲. **پیش‌بینیِ ابطال‌گرِ پیش‌ثبت شکست خورد:** لبه نه دوسویه بود نه با s
   بزرگ‌تر قوی‌تر — سازگار با «برچسبِ تازه روی جنازهٔ MR».
۳. گورستانِ mean-reversionِ طلا (S167/S320/S341...) یک قبرِ جدید گرفت:
   حتی رخدادِ آناتومیکِ خودنرمال‌شده هم MR را نجات نمی‌دهد. رانشِ سکولار
   طلا فیدِ کشش را می‌خورد.

## ۵. اثباتِ نبودِ ۸ خطای رایج

| خطا | شاهد |
|---|---|
| گزینشِ پسینی | گرید و قاعدهٔ نامزد در پیش‌ثبت `a0d674b3` پیش از اجرا؛ گزینش مکانیکی (فلات) |
| نشتِ آینده | EMA/ATR بازگشتیِ علّی؛ ورود openِ بعد؛ اکتشاف فقط `iloc[:n//2]`؛ split_bar رسمی |
| چندگانگیِ پنهان | n_trials=247 اعلام‌شده در پیش‌ثبت §۴ |
| نالِ نادرست | دو خطِ مبنا per سمت، K=500، سه تلهٔ کنترل بسته (build_null مشترکِ ممیزی‌شده) |
| هندسهٔ شناور | SL از JSONِ اکتشاف (GEOMDRIFT)؛ TP=rr×SL≥SL؛ SLهای غیرگرد (4.6…292.8) |
| حکمِ دست‌ساز | verdict/score عیناً از موتور؛ یادداشت‌های موتور نقل شد |
| pip/spread حدسی | از `se.ASSETS` (PIPGUESS) |
| بازتنظیم پس از رد | هیچ؛ رد=مرگ؛ این سند بلافاصله بسته شد |

## ۶. درس‌ها

- MR روی طلا با هر لباسی (نوسانگر، آناتومی، کشش) در برابرِ رانشِ سکولار
  می‌میرد؛ در بلوکِ S68x دیگر هیچ فیدِ ضدروند آزموده نمی‌شود.
- z_screen>3 در نیمهٔ اول (D1: 3.57، H3: 3.25) هیچ ضمانتی نیست — فقط
  hold-out حقیقت را می‌گوید. (دومین نمونهٔ متوالی پس از S680.)

## ۷. ردِ صداقت

`a0d674b3` پیش‌ثبت → `f02dfe14`* اکتشاف (فقط نیمهٔ اول) → داوری ×۳ →
این سند. خام‌ها: `results/_s682_explore/` (×۱۹)،
`results/_s682_adjudicate/adj_{D1,H12,H3}.json`.
ممیزیِ هم‌پوشانی: موضوعیت ندارد (ACCEPT نشد).

*فرما: دو قرن پس از من مِرِه گفت «طبیعت از تقارن گریزان نیست، از حافظهٔ
ما گریزان است.» دوجیِ نیمهٔ اول حافظه بود، نه طبیعت. S683 را می‌سازم.*
