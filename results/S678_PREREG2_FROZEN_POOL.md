# S678 — PREREG-2: فریزِ استخر قبل از لمسِ holdout

> پیش‌ثبتِ اصلی: `results/S678_PREREG_NBAR_OVERREACTION_FADE_PATHB.md` · اسکن: `results/_scan_S678/` (۱۹ TF، ۶۰٪ اول، همه کامیت‌شده)
> **holdout [0.6n, n) تا این لحظه هیچ‌بار لمس نشده.**

## ۱. نتیجهٔ جستجو (۶۰٪ اول)
- M1–M30: PF 0.35–1.10 → P1 تأیید (fade روی TF ریز = اسپرد).
- **هیچ تک‌کارتی** ۷ معیار را پاس نکرد. نزدیک‌ترین: H2 `N3 z2.5 counter long 1×1` (n=115, lift +12.2, PF 1.57, lsn 131) — رد به‌دلیلِ (f): سالِ ۲۰۱۱ = ۶۳٪ از netِ long.
- قاعدهٔ استخر (S675 §۴.۲؛ عضو = lift>+4 ∧ PF≥1.2 ∧ n≥30 در H1–D1؛ ≥۲ عضو): **۲ سلول واجد**؛ برنده با بیشترین Σlift·√n:

| سلول | اعضا (n, lift, PF) | Σlift·√n | (f) per-side | (g′) per-side lift>0 |
|---|---|---|---|---|
| **N=5, z=2.5, counter, k_sl=1.0, RR=1.0, both** | H2 (133, +8.93, 1.30) · H3 (104, +8.43, 1.27) · H8 (30, +16.27, 1.46) | **278** | L: ۷/۱۰ سال ≥0، بیشینه‌سهم 0.44 ✓ · S: ۷/۱۰ ≥0، بیشینه‌سهم 0.44 ✓ | H2 L +13.9/S ≈+3.3 ✓ · H3 L +9.7/S +6.9 ✓ · H8 L +13.1/S +21.8 ✓ |
| N=5, z=2.5, counter, 1.0×1.0, long | H2 (71), H3 (55) | 189 | ✓ | ✓ |

## ۲. استخرِ منجمد (verbatim به adjudicator)
- سیگنال: `overreact_signals(c, N=5, z=2.5)` از `strategies/s678_overreact_signals.py`؛ گیت counter (LONG فقط اگر `c[t−1]>c[t−181]`؛ SHORT آینه)؛ side=both.
- هندسه: SL=1.0×ATR21[t−1]، TP=1.0×SL؛ max_hold: H2=64, H3=64, H8=32.
- اعضای نامزد: **H2, H3, H8** (هر سه روی دادهٔ کامل `data/mt5_full/` اجرا؛ lift هر عضو از دادهٔ کامل به `pool_cards` داده می‌شود — pool_cards خود عضوِ lift≤0 یا رقیق‌کننده را حذف می‌کند؛ نتیجهٔ حذف هرچه بود، همان).
- مدلِ صفرِ هر عضو: **درفت-شرطی** per side (ورودهای تصادفی از بارهای valid ∧ drift هم‌جهت)، K_PERM=1000، SEED=20260908؛ ترکیب با `blend_pool_null` (وزن = سهمِ کارت در استخرِ FIFO).
- **یک** `compute_rqs2` روی استخر؛ `bar_time` = زمانِ ورودِ معاملات؛ `split_bar` = تعدادِ معاملات با t_entry < t_split، که t_split = زمانِ بارِ `int(0.6·n_full)` در H2 (مرزِ ۶۰٪، همان مرزِ جستجو؛ ثابت برای همهٔ اعضا).
- **n_trials = 12 + 1 + 3 = 16**.
- خروجی: `results/_final_S678/POOL.json` + `POOL_trades.csv` + `{TF}_member.json`؛ سپس MD حکم. بدونِ v2، بدونِ لمسِ دوم.
- **فقط اکتشاف — هیچ استقراری حتی با ACCEPT.**
