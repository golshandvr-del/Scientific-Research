# S1511 — پیش‌ثبت: رکوردِ تازهٔ کف (Fresh Floor Record — «هوای پاک زیرِ کندل») ⇒ LONG — XAUUSD H6/H8/H12/D1

> **این سند قبل از تولید هر عدد PnL کامیت می‌شود** (قانون ③). داور: `engine/rqs2.py` v2.6 — حکم verbatim، دست‌نخورده.
> نویسنده: Isaac Newton — بلوک S1510–S1519، شمارهٔ ۲ از ۱۰. تاریخ: 2026-09-05.

## ۱) درس S1510 و چرخشِ پارادایم
S1510 (رکوردِ سرعت) ۱۹/۱۹ REJECT: «لبهٔ تازه» دربارهٔ **سطح** صادق است، نه مشتق. پس به سطح برمی‌گردم، اما به **نیمهٔ دیگرِ کندل**:
- S526/S1520 (ACCEPT 88/91): `close` از همهٔ ۹۰ closeِ قبلی بالاتر می‌رود = **حضورِ تقاضای مهاجم** (سقف شکسته می‌شود).
- **S1511 (بکر):** `low[t] > max(low[t−90..t−1])` = حتی *بدترین* قیمتِ کندل بالاتر از همهٔ کف‌های ۹۰ کندلِ قبلی است = **غیابِ عرضه** (Wyckoff «no supply»؛ نیوتن: جسمی که هیچ نیرویِ مخالفی به آن نمی‌رسد در حرکت می‌ماند). کندل در «هوای پاک» بالای کلِ نوارِ کف‌ها معلق است.
- ممیزی بکر بودن: grep `low > max(low` / `whole bar above` / `clear air` / «کف کندل بالاتر از» ⇒ **صفر سند**. S628 (RollingSupportHold) پول‌بک به کف است — متفاوت. S850 رزونانس چندمقیاسی — متفاوت.
- شمارشِ خام پیش از PnL (فقط فرکانس، بدون هیچ خروجی): H6 480 · H8 358 · H12 258 · D1 128 رویدادِ تازه؛ ۹۷٪ در فضای drift>0؛ هم‌پوشانیِ **همان-بار** با رویداد S526: H8 96/358=27%، D1 45/128=35% ⇒ رویدادِ مستقل، نه بازنویسیِ S526.

## ۲) تعریف منجمد (صفر پارامتر آزاد پس از این کامیت)
| جزء | مقدار | منبع |
|---|---|---|
| رویداد | `ff = low[t] > max(low[t−90..t−1])`؛ سیگنال = `ff & ~ff.shift(1)` (**فقط لبهٔ تازه**) | S963/S526 (90 منجمد از S950/S523) |
| گیت دریفت | `close[t−1] − close[t−90] > 0` (علّی) — برای هم‌ارزیِ فضای نول با S526 | S523/S526 |
| جهت | **LONG-only** (آینه‌های SHORT: S522/S528 مرده) | — |
| ورود | open کندلِ بعد، FIFO، `allow_overlap=False` | استاندارد |
| براکت | SL = 1.5×median(ATR100) · TP = 1.5×SL (TP≥SL ✓) | S382/S526 عیناً |
| هزینه | اسپرد 3.3 پیپ، بدون کمیسیون | کالیبراسیون حساب |
| کارت‌ها | **H6, H8, H12, D1** (خانوادهٔ زندهٔ لبهٔ تازه؛ H4 در mt5_full وجود ندارد — E-16) | S526 |
| داده | `data/mt5_full/` با گارد `assert 'mt5_full'` | E-16 |
| نول | **مشروط** در فضای drift>0: uncond stride 1/3/7 (سخت‌ترین) + جایگشت K=2000، SEED=**20260905**؛ مرجع=MAX | S523/S526 |
| n_trials | **12** = ۴ کارت + ۴ پارامتر وام‌گرفته (90، 1.5، 1.5، ATR100) + ۴ بازوی تشخیصی | صادقانه |
| مسیر | B (یک حکم به‌ازای هر کارت؛ بدون جست‌وجو) | ③ |

## ۳) ابطال‌گرهای پیش‌ثبت (پیش از عدد)
- **P1 (لبه در برابر حالت — قانون S963):** بازوی تشخیصی «حالت» = همهٔ کندل‌های `ff` غیرِتازه (کف هنوز بالای نوار است اما تازه نیست). پیش‌بینی: lift(تازه) > lift(حالت). اگر معکوس شد، قانون S963 برای «کف» نقض شده — ثبت می‌شود.
- **P2 (استقلال از S526 — ضدِ «تأخیرِ S526»):** بازوی تشخیصی «S1511 بدونِ رویداد S526 در ۳ کندلِ اخیر». قانون S964 (تأخیر توان می‌کُشد) می‌گوید اگر S1511 فقط سایهٔ دیرهنگام S526 باشد، این بازو باید فرو بریزد. پیش‌بینی نیوتن: **می‌ماند** (غیابِ عرضه اطلاعی مستقل از حضورِ تقاضاست). این بازو *داوری* نمی‌شود — فقط اندازه‌گیریِ تشخیصی برای تفسیر؛ در n_trials شمرده شد.
- **F1:** حکم موتور ۱۱/۱۱ روی حداقل یک کارت ⇒ ACCEPT ⇒ **توقف و گزارش به کاربر** + ممیزی هم‌پوشانی فوری با S526/S1520/S965/S966/S950/S919/S770/S382 + overlap-as-filter.
- **F2:** هیچ کارتی ⇒ REJECT ⇒ گزارش کوتاه ⇒ fetch ⇒ S1512 خودکار. **هیچ تغییرِ پارامتری پس از دیدنِ عدد.**
- پیش‌بینی شخصی (برای ارزیابیِ کالیبراسیون): H8 n≈150–200 (FIFO)، WR 52–58، lift +8..+16، z 2.0–3.5. اگر lift مثبت اما z<3.09 ⇒ REJECT ثبت می‌شود بدون نجات.

## ۴) انطباق
قانون ③ ✓ · یک لایه به‌ازای یک شماره ✓ · TP≥SL ✓ · بدون EURUSD ✓ · قلمروی دیگران دست‌نخورده ✓ · دیتابیس fetch و ACCEPTهای جدید (S966=86، S919=88.9، S1520=91) مطالعه شد ✓ · هزینهٔ حساب ✓.

*نیوتن: قانون سوم — هر کنشی واکنشی دارد. اگر واکنش (عرضه) غایب است، کنش (تقاضا) بی‌مانع ادامه می‌یابد. این را می‌آزمایم.* 🍎
