# S450 — قاعدهٔ مدیریتیِ «خروجِ LONG در شکستِ کفِ ساعتِ اول» (M1) · **REJECT**

| فیلد | مقدار |
|---|---|
| شمارهٔ رکورد | `S450` (مأموریت ۵ — مدیریت معامله) |
| نوع | قاعدهٔ مدیریتِ معامله (حالت ۴) — **نه** لایهٔ ورود ⇒ RQS2 مستقل ندارد |
| موتورِ حاکمِ پروژه | rqs2 **v2.6** (لایه‌های پایه همه ACCEPT رسمی v2.6-سازگارند) |
| معیارِ داوری | بک‌تستِ مقایسه‌ایِ جفتی با/بدونِ قاعده، معیارِ پیش‌ثبت‌شده |
| پیش‌ثبت | `results/S450_PREREG_MGMT_RULES_PROTOCOL.md` (commit `65a01cd7`، **قبل** از هر اجرا) |
| مادهٔ خام | M1 — فاز 15-C2/16-D (Rejewski Procedural): sd −51٪، بدترین روز −$505→−$87 |
| بیماران | S312 (M30/M15/H1) · S356 (H1) · S382 (H4، نسخهٔ ویژه) — همگی LONG |
| حکم | **REJECT روی هر ۵ کارت** — قاعده برای لایه‌های سوئینگِ فعلی مناسب نیست |
| داده‌های خام | `research/mgmt/S450_paired_*.json` |
| اسکریپت‌ها | `strategies/s450_mgmt_first_hour_low.py`، `s450_paired_replay.py`، `s450_paired_s356.py`، `s450_paired_s382.py` |

## ۱. قاعدهٔ آزموده‌شده (عینِ پیش‌ثبت، بدونِ جست‌وجوی پارامتر)

اگر در پوزیشنِ LONG هستیم و `close` کندلِ بسته‌شده زیرِ **کفِ ساعتِ اولِ
روزِ معاملاتیِ جاری** برود ⇒ خروج در `open` کندلِ بعد (سببی، بدونِ look-ahead).
قاعده فقط پس از ورود فعال است و SL/TP اعلامیِ حالتِ ۳ را در لحظهٔ ورود
تغییر نمی‌دهد (مرزِ مسئولیت).

- مرزِ روزِ معاملاتی = گپِ زمانی > (طولِ TF + ۳۰ دقیقه) — لنگرِ ضدDST.
- ساعتِ اول: M15→۴ کندل، M30→۲، H1→۱. تا کامل‌شدنش قاعده خاموش است.
- H4: «ساعتِ اول» از TF قابلِ استخراج نیست ⇒ نسخهٔ «کفِ کندلِ اول» جداگانه.

## ۲. روش — و باگی که وسطِ راه پیدا و اصلاح شد (گزارشِ صادقانه)

**(الف) چرا بازپخشِ جفتی؟** Wrapper سرراست (خروج و آزادسازیِ استراتژی)
جمعیتِ معاملات را عوض می‌کرد: در S312-M30 تعدادِ معاملات ۲۹۷→۹۰۰ شد چون
پس از خروجِ زودهنگام، شرطِ ورودِ همان روز هنوز برقرار بود و استراتژی دوباره
وارد می‌شد ⇒ اثرِ قاعده با اثرِ ورودهای جدید مخلوط می‌شد. متنِ مأموریت
صریح است که قاعده باید روی «معاملاتِ لایه‌های زنده» سنجیده شود ⇒ روشِ
نهایی: همان معاملاتِ پایه کندل‌به‌کندل بازپخش شدند (جمعیت ثابت، جفت‌به‌جفت)
با فرمولِ هزینه و تقدمِ SL/TP عینِ موتورِ رسمی.

**(ب) باگِ لنگرِ روز (کشفِ اجرای اول).** قانونِ خامِ «گپ>۳۰ دقیقه» برای H1
هر کندل را روزِ جدید می‌کرد (فاصلهٔ عادی H1 = ۶۰ دقیقه!) ⇒ قاعده هرگز فعال
نمی‌شد (changed=0/266)، و در M30 گپِ تک‌کندلِ جاافتاده (۶۰min، ۳۹۸ مورد)
مرزِ کاذبِ روز می‌ساخت. توزیعِ گپِ واقعیِ داده استخراج شد (M15:75، M30:90،
H1:120، آخرهفته ~۳۰۰۰ دقیقه) و لنگر به `گپ > TF+30min` تعمیم یافت؛ سپس
**همهٔ** TFها بازاجرا شدند. sanity: روز/سال M15=260، M30/H1=235 ✓.

## ۳. نتایج (pnl بر ۱ لات، USD)

| بیمار | n | avg پایه | avg با قاعده | Δسود | sd | worst | maxDD | تعدادِ تغییر | حکم |
|---|---|---|---|---|---|---|---|---|---|
| S312 M30 | 297 | 407.0 | 262.2 | **−35.6٪** | −20٪ ✓ | ✓ | ✗(+5٪) | 181 | **FAIL** |
| S312 M15 | 138 | 533.4 | 421.0 | **−21.1٪** | −9٪ ✓ | = | ✗ | 68 | **FAIL** |
| S312 H1 | 266 | 498.4 | 300.0 | **−39.8٪** | −21٪ ✓ | ✓(−58٪) | ✗(+16٪) | 191 | **FAIL** |
| S356 H1 | 117 | 164.9 | 127.3 | **−22.8٪** | −7٪ ✓ | = | ✓(−3٪) | 34 | **FAIL** |
| S382 H4* | 869 | 273.6 | 251.1 | **−8.2٪** | −9٪ ✓ | **✗(−50٪ بدتر)** | ✓(−14٪) | 185 | **FAIL** |

(*نسخهٔ H4: به‌دلیلِ جذبِ وقفهٔ روزانه در گپِ عادیِ ۲۴۰min، لنگر عملاً به
«کفِ کندلِ اولِ **هفته**» تنزل یافت — طبق پیش‌ثبت جداگانه گزارش شد و
تعمیمی از آن ادعا نمی‌شود. بدترشدنِ worst به‌خاطرِ خروجِ گپی زیرِ لنگر است.)

اعتبارسنجیِ بازتولید: S356 → WR بازتولید 51.3٪ = عددِ سندِ ACCEPT (51.28٪)؛
S382 → n=869 = عددِ سندِ ACCEPT. نیمه‌های زمانی در هر ۵ کارت هم‌جهت بودند
(شکستِ سود در هر دو نیمه) ⇒ نتیجه ساختاری است، نه نویزِ یک دوره.

## ۴. چرا قاعده شکست خورد — عددِ کلیدی (دلیلِ داده‌محور، نه سلیقه)

معاملاتی که قاعده تغییرشان داد (S312-H1): در پایه به‌طور میانگین
**+$14.9** درمی‌آوردند، با قاعده **−$261.4**. در S356: +$53.4 → −$76.0.
یعنی در این لایه‌ها افتِ زیرِ کفِ ساعتِ اول غالباً **خریده می‌شود** و
معامله برمی‌گردد؛ قاعده دقیقاً در نقطهٔ بازگشت خارج می‌کند.

ریشهٔ علمی: M1 در فازِ اکتشاف روی الگوی «ورودِ صبح، خروجِ همان روز»
(بازهٔ نگه‌داریِ چند-ساعته) اثبات شده بود. بیمارانِ فعلیِ سایت همه
**سوئینگِ چند-روزه**اند (S312: max_hold تا ۴۸ کندلِ M15؛ S356: mh=20×H1؛
S382: براکتِ بدونِ سقفِ زمانی، میانگینِ نگه‌داری >۱۰ کندلِ H4). ساختارِ
درون-روزیِ M1 به افقِ چند-روزه منتقل نمی‌شود — درست همان «تقارن/تعمیمِ
مفت»ی که مأموریت ممنوع کرده بود، این‌بار در محورِ افقِ زمانی.

## ۵. پیامد برای سایت (فقط توصیه — بدونِ اعمال)

- قاعدهٔ M1 به‌عنوانِ خروجِ مدیریتی برای **هیچ‌یک** از لایه‌های زندهٔ فعلی
  توصیه نمی‌شود و نباید واردِ `trade_manager.ts` شود.
- کاربردِ مشروعِ M1 همچنان همان یافتهٔ اکتشافی است: خروجِ روز-معاملهٔ
  همان‌روزه — اگر روزی لایهٔ ورودِ intraday با خروجِ همان‌روز ACCEPT شود،
  M1 دوباره نامزدِ آزمون است (با همین پروتکل).
- طبق قیدِ کارفرما هیچ تغییری در سایت اعمال نشده است.

## ۶. ردِ اشتباهاتِ رایج در همین آزمون

| # | اشتباه | چگونه رد شد |
|---|---|---|
| 1 | look-ahead | خروج همیشه در open کندلِ i+1؛ fhl فقط از کندل‌های ≤i؛ تقدمِ SL/TP عینِ موتور |
| 2 | تغییرِ جمعیت | بازپخشِ جفتی — n پایه = n قاعده در هر ۵ کارت |
| 5/6 | تعمیمِ بینِ TFها | هر TF جدا داوری شد؛ H4 نسخهٔ جدا با هشدارِ صریح |
| 7 | جست‌وجوی پارامتر | صفر پارامترِ آزاد — فرمِ قاعده عیناً از M1، پیش‌ثبتِ قبل از اجرا |
| 8 | گزینشِ نتیجه | نتیجهٔ منفی کامل گزارش شد؛ هر دو نیمه، هر ۵ کارت |

— AI مأموریت ۵ · 2026-08-12 · وضعیت: **REJECT** (قاعده واردِ سایت نمی‌شود)
