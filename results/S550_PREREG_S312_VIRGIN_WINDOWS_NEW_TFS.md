# S550 — پیش‌ثبت (PREREG): قاعدهٔ منجمد S312 روی پنجره‌های بکر و نردبان تایم‌فریم‌های نو

**تاریخ پیش‌ثبت:** 2026-08-15 — ⏱ این سند **پیش از اجرای هر بک‌تستی** commit و push می‌شود. مُهرِ زمانیِ git = سندِ پیش‌ثبت.
**نویسنده:** Riemann (AI strategy builder)
**نیا (lineage):** S312 (ACCEPT, XAUUSD-M30) → S432 (ACCEPT, استخر H1+M15، rqs2=84.7)

---

## ۱. تز علمی

مولّد سیگنالِ «رانش میان‌ماه طلا» (S312) روی H1 با دادهٔ تمام‌طول (۱۵٫۵ سال) قضاوت و پذیرفته شده است.
اما ممیزی پنجره‌های داده (این جلسه، پیش از این سند) نشان داد:

- **M15 قدیمی** فقط از 2020-02-20 شروع می‌شد ⇒ قطعهٔ **[2011-01-03, 2020-02-20) روی M15 بکر است** (۹٫۱ سال).
- **M5 قدیمی** فقط از 2023-09-18 شروع می‌شد ⇒ قطعهٔ **[2011-01-03, 2023-09-18) روی M5 بکر است** (۱۲٫۷ سال).
- ۱۲ تایم‌فریم (M1,M3,M4,M6,M10,M12,M20,H2,H3,H6,H8,H12) **هرگز در هیچ آزمونی لمس نشده‌اند** (فقط در `data/mt5_full/` موجودند).

پرسش S550: آیا قاعدهٔ منجمدِ S312 — بدون هیچ جست‌وجوی پارامتر — روی این قلمروهای بکر نیز برتری دارد؟

## ۲. قاعدهٔ منجمد (هیچ پارامتری جست‌وجو نمی‌شود)

عیناً از S312/S432 (کد مرجع: `strategies/sim_strategies.py::S312_MidMonth_Long`،
`strategies/s432_midmonth_h1m15_pool.py::build_s312_layer`):

- جهت: فقط **LONG**
- روزِ ماه: `dom ∈ {10, 13, 20}`
- ساعت UTC: `hour ∈ {1..12}` با معنای **SESSION_OPEN** (فقط نخستین بارِ هر ساعتِ هدف — دکترین `tools/s434_fast_data.py`؛ روی H1 هر دو تعریف یکی است)
- فیلتر کیفیت: `close > EMA200` **بومیِ همان تایم‌فریم** (این «بومی‌بودن» جزو تعریفِ منجمدِ نیاست — S432 نیز EMA200 بومی M15 و H1 داشت؛ پس یکنواختیِ عددِ 200 اشتباه رایج #6 نیست بلکه بازتولید وفادار است و در گزارش نهایی با همین سند اثبات می‌شود)
- `WARMUP = 300` بارِ نخستِ هر پنجره سیگنال ندارد
- خروج: SL/TP متقارن + بستن پس از max_hold بار؛ `allow_overlap=False`

## ۳. هندسهٔ هر تایم‌فریم — نگاشتِ از‌پیش‌اعلام‌شده، بدون جست‌وجو

هندسه‌های منجمدِ موروثی: M15→(sl=295, tp=295, mh=48) ، M30→(295,295,36) ، H1→(395,395,24).
برای تایم‌فریم‌های نو، **قاعدهٔ نگاشت** (اعلام‌شده همین‌جا، پیش از هر عدد):

- **زیرساعتی** (M1..M20): هندسهٔ M15 را به ارث می‌برد: `sl=tp=295`؛ `mh = بارهای معادل ۱۲ ساعت` از `hold_bars_for(tf, 12.0)`
- **فراساعتی** (H2..H12): هندسهٔ H1 را به ارث می‌برد: `sl=tp=395`؛ `mh = بارهای معادل ۲۴ ساعت` از `hold_bars_for(tf, 24.0)`

مقادیر قطعی (محاسبه‌شده پیش از این سند، قطعیت‌پذیر از فرمول):

| TF | sl=tp | mh (بار) | | TF | sl=tp | mh (بار) |
|---|---|---|---|---|---|---|
| M1 | 295 | 720 | | M20 | 295 | 36 |
| M3 | 295 | 240 | | H2 | 395 | 12 |
| M4 | 295 | 180 | | H3 | 395 | 8 |
| M5 | 295 | 144 | | H6 | 395 | 4 |
| M6 | 295 | 120 | | H8 | 395 | 3 |
| M10 | 295 | 72 | | H12 | 395 | 2 |
| M12 | 295 | 60 | | M15 | 295 | 48 |

**مستثنی‌ها:** D1/W1 (قاعدهٔ ساعت روی آن‌ها منحط می‌شود و دادهٔ قدیمی‌شان از قبل تمام‌طول بود)، MN1 (بار ماهانه اصلاً نمی‌تواند dom×hour را بیان کند).

## ۴. ساختار آزمون و مسیر تعدد (multiplicity)

### بازوی A — مسیر C (تأیید hold-out؛ هر پنجرهٔ بکر دقیقاً یک‌بار لمس می‌شود)

دو آزمونِ اولیهٔ تعیین‌کننده:

1. **A1:** XAUUSD **M15**، پنجرهٔ بکر `[2011-01-03, 2020-02-20)`، هندسهٔ 295/295/48.
   نقش: hold-out تأییدیِ عضو M15 استخر S432.
2. **A2:** XAUUSD **M5**، پنجرهٔ بکر `[2011-01-03, 2023-09-18)`، هندسهٔ 295/295/144.
   نقش: کارت نو؛ ادعای پذیرش مستقل مستلزم عبور کامل ۱۱ دروازهٔ RQS2 روی همین پنجره است.

- `n_trials = 299` برای هر آزمون (۲۹۸ موروثی از نیای S312/S432 + ۱ برای خودِ این انتقال — محافظه‌کارانه؛ گرد نشده، اشتباه رایج #7 رعایت شد).
- هر پنجرهٔ بکر **فقط یک‌بار** ارزیابی می‌شود. هیچ آزمون دوباره‌ای روی آن مجاز نیست (در صورت شکست: مرگ ابدی برای این نیا روی آن پنجره).

### بازوی B — پیمایش اطلاع‌رسان نردبان تایم‌فریم‌های نو (بدون ادعای پذیرش)

SESSION_OPEN روی تمام‌طولِ M1,M3,M4,M6,M10,M12,M20,H2,H3,H6,H8,H12 با هندسهٔ بخش ۳.
نتایج **per-TF** و **افزایشی** (قانون MTF کاربر: شروع از M1، گزارش تدریجی، commit تدریجی) گزارش می‌شوند اما
**هیچ ادعای پذیرشی از این بازو برنمی‌خیزد** — چون ورودی‌های SESSION_OPEN این‌ها زیرمجموعه/هم‌زمانِ ورودی‌های H1 تمام‌طولِ از‌قبل‌قضاوت‌شده‌اند (همپوشانی زمان-ورود ≈۱۰۰٪؛ افشای کامل در بخش ۵). ارزش این بازو: نقشهٔ برتری در دانه‌بندی/افق‌های نگه‌داشتِ متفاوت، و خوراک طراحی‌های آینده. هر ادعای پذیرش آتی روی این کارت‌ها باید پیش‌ثبتِ جداگانه با پرداخت تعدد بدهد.

### پارامترهای ثابت آماری

- `SEED = 20260815` ، `K_PERM = 2000` (≥500 لازمهٔ H3)
- null: نول اندازه‌گیری‌شدهٔ هم‌سو (الگوی دقیق S432: `build_null_side` با همان sl/tp/mh روی زمان‌های تصادفی معتبرِ همان پنجره)
- موتور: `s333.evaluate` → `SE.simulate_trades` با `allow_overlap=False`؛ داده از `tools/s434_fast_data.load_fast` (مسیر `data/mt5_full/`، طول کامل ۱۵٫۶ سال — سپس برش پنجرهٔ بکر برای بازوی A)
- داور: `engine/rqs2.compute_rqs2` نسخهٔ v2.6 — پذیرش فقط با ۱۱ دروازهٔ H0–H10

## ۵. افشای همپوشانی (پیشاپیش)

- ورودهای SESSION_OPEN در همهٔ تایم‌فریم‌ها روی مهرزمان‌های یکسان می‌افتند (نخستین بار ساعتِ h در روز dom) ⇒ همپوشانی زمان-ورود S550 با S312/S432 روی پنجره‌های مشترک **≈۱۰۰٪ طبقِ ساخت**. تازگیِ ادعاشده فقط: (الف) پنجره‌های تاریخیِ بکر، (ب) دانه‌بندی/هندسه/افقِ نو. درصد دقیق همپوشانی پس از اجرا اندازه‌گیری و گزارش می‌شود.
- طبق قانون همپوشانی، «بخش همپوشان به‌عنوان فیلتر» اینجا بی‌معناست (این خودِ همان لایه است، نه لایه‌ای نو) — به‌جای آن، تمایز hold-out/کارت-نو صریحاً اعلام شد.

## ۶. معیارهای حکم (از پیش)

- **A1 پاس ⇒** استحکام عضو M15 استخر S432 تأیید؛ سند S432 به‌روزرسانی می‌شود (ACCEPT ابقا/تقویت).
- **A1 شکست ⇒** هشدار جدی برای عضو M15 لایو؛ گزارش UNPROVEN/REJECT طبق دروازه‌ها + اطلاع به کاربر.
- **A2 پاس همهٔ ۱۱ دروازه ⇒** پیشنهاد لایهٔ نو XAUUSD-M5 به کاربر (S550) و انتظار برای تأیید پیش از استقرار.
- **بازوی B:** فقط ثبت علمی per-TF با STATUSهای UNPROVEN/POWER-LIMITED؛ بدون ادعای ACCEPT.

## ۷. نظم اجرا

اجرای پس‌زمینه؛ ترتیب نردبانی از M1 به بالا (قانون MTF)؛ پس از هر تایم‌فریم: ذخیرهٔ JSON در `results/_scan_S550/` + commit + push (قانون افزایشی — sandbox ممکن است ریست شود).
