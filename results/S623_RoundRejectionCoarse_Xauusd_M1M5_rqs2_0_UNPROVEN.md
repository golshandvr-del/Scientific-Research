# S623 — رویداد رند منجمد × هندسهٔ درشت | XAUUSD | M1/M5 | UNPROVEN (سوخته در جست‌وجو)

**پیش‌ثبت:** `results/S623_PREREG_XAUUSD_round_rejection_coarse_geometry_pathC.md` — commit `7fa24b13`
**داده:** `data/mt5_full/` (src در لاگ‌ها) · **مسیر:** C · **نگه‌داشت:** بکر ماند
**seed:** 20260815 · **کوشش‌ها:** 72 (بدهی تجمیعی نیمهٔ جست‌وجو: 3672+192+384+72=4320)

## حکم: UNPROVEN — هیچ نامزدی معیار چهارشرطی را نگذراند

| TF | بهترین (به L·√n) | n | lift | exp_pip |
|---|---|---|---|---|
| M1 | mh=960, LONG, k=16, RR=1.5 | 2329 | +2.92 | **−2.56** ❌ |
| M5 | mh=480, BOTH, k=16, RR=1.0 | 1439 | +2.15 | +0.89 (اما lift<4) ❌ |

## قضیهٔ فروپاشی افق (یافتهٔ نظری S622+S623)

اطلاعات رویداد رند با بزرگ‌شدن هندسه **فرو می‌پاشد**:

```
هندسهٔ ریز  (k=1.5–2, hold≤240):  lift=+7.1pp، exp=−3.3pip  ← اطلاعات هست، هزینه می‌بلعد
هندسهٔ درشت (k=4–16, hold≤960):   lift≤+2.9pp، exp<0 یا lift ناکافی ← هزینه کم می‌شود، اطلاعات محو
```

پیش‌بینی‌پذیریِ سطح رند **افق کوتاه** دارد (چند ده کندل M1)؛ در SLهای بزرگ‌تر از
~4×ATR نویز مسیر، سیگنال را می‌شوید. حاصل: در هیچ نقطه‌ای از صفحهٔ (افق، هندسه)
همزمان «lift≥4pp ∧ exp>0 ∧ n کافی» برقرار نمی‌شود. خانوادهٔ سطح رند (S622+S623)
**بسته** اعلام می‌شود — ادامهٔ تیون = انکار یافتهٔ دوقله‌ای.

## ترازنامهٔ بلوک S620–S623 (چهار لایه، چهار مرگ پاک، سه قضیه)

1. **قضیهٔ سقف خانواده** (S620/S621): lift·√n خانوادهٔ LagRSI-خروج ناوردا ≈2.2–2.8σ < 3.09σ.
2. **قضیهٔ مرز معامله‌پذیری** (S622): اطلاعات z≈10σ می‌تواند به‌کلی زیر اسپرد مدفون باشد؛ H2 مرز predictability/tradability است.
3. **قضیهٔ فروپاشی افق** (S623): انتقال رویداد ریزساختاری به هندسهٔ درشت اطلاعات را حفظ نمی‌کند.

نگه‌داشت ۱۵.۶ سال پس از ۴۳۲۰ کوشش هنوز **بکر** است — بزرگ‌ترین دارایی بلوک.

## ردپا
`strategies/s623_round_coarse_search.py` · `results/_scan_S623/*.json` · EURUSD: هرگز.
