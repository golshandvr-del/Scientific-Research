# S1623 — PREREG — حاشیهٔ رکوردِ کف (Floor-Margin Gate) روی رکوردِ تازهٔ کفِ S1511 — XAUUSD H6/H8/H12 — LONG

**دانشمند:** اقلیدس · بلوک S1620–S1629 · شمارهٔ ۴/۱۰ · 2026-09-09 · **commit قبل از کد و هر عدد.** داور `compute_rqs2` v2.6 دست‌نخورده. **فقط اکتشاف — سایت هرگز لمس نمی‌شود.**

## ۱) منطق: آزمونِ انتقال‌پذیریِ قانون S1621
S1621 (ACCEPT 88.3) قانون داد: «روی لبهٔ تازه، اطلاعات در **قاطعیت شکست** (حاشیه بر ATR) است». S1622 نشان داد سخت‌تر کردن حاشیه رویداد را می‌کشد. آزمون علمی درست بعدی: **آیا این قانون به رویداد سطحی دیگر خانواده منتقل می‌شود؟** رویداد آینه: رکورد تازهٔ کف S1511 (نیوتن، ACCEPT 84.8؛ S1581 با گیت حجم ACCEPT 88.0). پیش‌بینی بیرونی (falsifiable): حاشیهٔ `(low − max(low[t−90..t−1]))/ATR100[t−1] ≥ 0.25` باید lift را از +10.03 بالا ببرد و مکمل پایین‌تر باشد. اگر نشد ⇒ قانون S1621 محدود به رکورد close است (تعمیم‌ناپذیر) — یافتهٔ منفی معتبر.

بداعت: `grep -riE "floor.*margin|margin.*floor|fresh.floor.*ATR.*0\.25"` ⇒ فقط اسناد خودم. S1581 روی همان رویداد **حجم** گذاشت (بُعد متعامد)؛ من **هندسه** می‌گذارم. هم‌پوشانی با S1581 گزارش می‌شود.

## ۲) شمارش خام پیش از قفل (blind — بی‌PnL، درس S1622/S610)
| کارت | رویداد S1511 (ff ∧ drift>0) | گیت margin≥0.25 | نرخ عبور | چارک‌های margin |
|---|---|---|---|---|
| H6 | 477 | 264 | 55.3% | 0.13 / 0.31 / 0.65 |
| H8 | 354 | 202 | 57.1% | 0.11 / 0.31 / 0.68 |
| H12 | 257 | 147 | 57.2% | 0.12 / 0.32 / 0.64 |
نرخ عبور در بازهٔ سالم [15%, 75%] ✅؛ n مورد انتظار پس از FIFO روی H6 ≈ 150–170 (S1511: 477→279).
sd_coin(H6, n≈160) ≈ 50/√160 ≈ 4pp ⇒ lift لازم برای z≈3.1 حدود +12..+13pp — دست‌یافتنی (S1581 با +12.10 گذشت).

## ۳) قانون منجمد (صفر پارامتر آزاد)
- **رویداد (عیناً S1511):** `ff = low[t] > max(low[t−90..t−1])`، سیگنال `ff ∧ ¬ff[t−1]`، گیت درفت `close[t−1] − close[t−90] > 0`.
- **گیت حاشیه (S1623):** `(low[t] − max(low[t−90..t−1])) / ATR100[t−1] ≥ 0.25` (ATR علّی RMA(TR,100) شیفت ۱؛ آستانه همان S1621، بی‌جست‌وجو). counter: `< 0.25`.
- LONG؛ ورود open بعدی؛ FIFO؛ اسپرد 3.3؛ SL=1.5×median(ATR100)، TP=1.5×SL. هارنس S382/S1511 دست‌نخورده (رانر s162x؛ رویداد پایه برای این لایه = fresh-floor به‌جای fresh-high).
- کارت‌ها: **H6 (اصلی — کارت ACCEPT S1511)**، H8، H12. داده mt5_full ۱۵.۶ سال.
- Null شرطی فضای درفت>0، stride {1,3,7} + perm K=2000، **SEED=20260905** (همان S1511 ⇒ null هم‌مقیاس با پایه).
- مسیر B؛ split_bar 0.70·n موتور.

## ۴) بودجه
۳ کارت × ۲ حالت + ۱ اجرای مرجع (ungated = بازتولید S1511-H6، گارد G0) = **n_trials = 7**. تنش 50 تشخیصی.

## ۵) گاردها/ابطال‌گرها
- **G0:** ungated H6 باید S1511 را بازتولید کند (n=279, lift +10.03, z=3.10). شکست ⇒ رانر نامعتبر.
- **P1a** lift_gated(H6) > +10.03 · **P1b** lift_gated > lift_counter · **P1c** z_gated > 3.10. شکست P1a یا P1b ⇒ REJECT ولو ۱۱ گیت سبز.
- **P2 هم‌پوشانی:** با S1581 (حجم) روی H6: اگر Jaccard>0.6 ⇒ ACCEPT-REDUNDANT (توقف اعلام نمی‌شود). با S1511: به‌ناچار زیرمجموعه (~۵۵٪) — «جانشین خالص‌تر» فقط اگر P1 گذشت.
- **F1** هیچ کارتی lift>+4 ⇒ محور مرده.
- حکم رسمی = موتور روی **H6 gated**.
- **پیش‌بینی شخصی:** P1 می‌گذرد (Δ +2..+3pp، مانند S1621)؛ z ~3.3؛ ACCEPT ۴۵٪، POWER-LIMITED ۳۰٪، REJECT ۲۵٪. اگر REJECT شد، قانون «قاطعیت» فقط برای رکورد *تقاضا* (close) معتبر است، نه *غیاب عرضه* (low).

## ۶) قلمرو
فقط S1623؛ هیچ فایل نیوتن/دیگران و هیچ فایل سایت لمس نمی‌شود.
