# S531 — احیای S355 با دروازهٔ رژیم · XAUUSD-M5 · RQS2 = **19.5 / REJECT**

> | | |
> |---|---|
> | **مسیر** | احیای لایهٔ مشروط‌شده (S530 ⇒ REGIME-ONLY) — راهِ دوم پروژه |
> | **روش** | مسیر C (hold-out): جست‌وجو فقط نیمهٔ اول، **یک** آزمون روی کل با split |
> | **حکم** | `REJECT` (19.5) — H3 ✗ · H5 ✗ · H6 ✗ · H8 ✗ |
> | **پیش‌ثبت** | `results/S531_PREREG_S355_RegimeGate_PathC.md` · commit `2c10ca31` (قبل از اجرا) |
> | **قفل جست‌وجو** | `results/_s531/LOCKED.json` · commit `e86dc53b` (قبل از باز شدن holdout) |
> | **مصنوع حکم** | `results/_s531/verdict.json` · commit `1147fd7c` |
> | **داده** | `data/mt5_full/XAUUSD_M5.csv` · ۱٬۰۸۹٬۵۷۴ ردیف · 2011→2026 (۱۵.۵۹ سال) |

## ۱. فرضیه (پیش‌ثبت‌شده) و نتیجه در یک خط

**فرضیه**: اگر لبهٔ S355 مختصِ «رژیم روندی» است، دروازهٔ رژیمِ لحظهٔ سیگنال
(خانوادهٔ منجمد hurst/r2/er/chop) باید دورهٔ ضرردهِ پیش-۲۰۲۳ را حذف کند.
**نتیجه**: نکرد ⇒ **فرضیهٔ رژیم ابطال شد**. مرگ صادقانه، بدون چرخهٔ دوم.

## ۲. فاز جست‌وجو (فقط bar < 544787؛ اندیکاتورها هم فقط روی نیمهٔ اول محاسبه شدند)

پایهٔ S355 در نیمهٔ اول: `n=56 · WR=53.57% · net=+571pip` (خودش زیر لبه).

از ۲۰ ترکیب فقط **یکی** هر دو قید پیش‌ثبت (n≥30 و حذف≥20%) را پاس کرد:

| رتبه | ترکیب | n | حذف | WR | t | واجد شرایط |
|---|---|---:|---:|---:|---:|---|
| ۱ | `r2_34 ≥ 0.618` | 28 | 50% | 60.7% | 1.194 | ✗ (n<30) |
| ۲ | `chop_21 ≤ 45` | 55 | **1.8%** | 54.5% | 1.161 | ✗ (عمل همانی) |
| ۳ | **`r2_34 ≥ 0.5`** | **40** | **28.6%** | **57.5%** | **1.135** | **✓ ⇒ قفل** |

⚠️ **شرط چهارم (گارد عمل همانی) کار کرد**: ۱۳ ترکیب از ۲۰ تقریباً هیچ معامله‌ای حذف
نمی‌کردند — بدون این قید، «فیلتری» قفل می‌شد که همان S355 خالی بود.

**ثبت صداقت پیش از آزمون** (در پیام commit `e86dc53b`): «t=1.135 ضعیف است؛
برآورد پاس H3 به <15% سقوط کرد.» — برآورد پیش‌ثبت اولیه ۳۵٪ بود.

## ۳. حکم holdout (یک اجرا، seed=20260814، n_trials=20، K=500)

قاعدهٔ آزموده: `S355 & (r2_34 ≥ 0.5)` · هندسهٔ منجمد `sl=120/tp=120/mh=96`

| سنجه | مقدار |
|---|---|
| n / WR | 97 / 57.73% |
| lift / z / p_perm | +8.66pp / **1.71** / 0.044 |
| PF / net | 1.425 / +977.5$ |
| نال | uncond=49.07% · perm_sd=4.34 · perm_max=59.5 |
| دروازه‌های مردود | **H3** (z<3.09) · **H5** · **H6** · **H8** |
| H7 (نیمهٔ دوم دست‌نخورده) | ✓ — ولی کافی نیست |

## ۴. تفسیر علمی — چرا این مرگ ارزشمند است

1. **ابطال تمیز**: چهار سنجهٔ استاندارد رژیم (Hurst، R²، Kaufman-ER، Choppiness)
   نمی‌توانند دورهٔ سوددهِ S355 را از دورهٔ ضرردهش جدا کنند. تفاوت ۲۰۲۳-۲۰۲۶ با
   ۱۲.۵ سال قبل در لحظهٔ سیگنال با «روندی بودن» قابل مشاهده نیست — عاملی
   ساختاری‌تر است (نامزدها: رفتار طلا در ابررالی، تغییر microstructure/اسپرد واقعی).
2. **تأیید الگوی شکاف دوقطبی** (گزارش مأموریت ۴): لایهٔ ۱۸ نمره‌ای با فیلتر به ۱۹.۵
   رسید، نه ۸۰. تلاش برای «هل دادن» لایه‌های میان‌مایه با فیلتر، بی‌ثمر است.
3. **پیامد عملی برای S355 روی سایت**: گزینهٔ ۲ (افزودن دروازهٔ رژیم) از میز حذف شد.
   باقی می‌ماند: ابقا با آگاهی از ریسک رژیمی، یا حذف. تصمیم با کاربر (S530 §۶).

## ۵. ردپای نظم مسیر C

- جست‌وجو هرگز نیمهٔ دوم را ندید (برش دیتافریم **قبل از** محاسبهٔ اندیکاتورها).
- قفل قبل از باز شدن holdout در commit جدا (`e86dc53b`).
- اسکریپت اجرای دوم را فیزیکی رد می‌کند (وجود `verdict.json` ⇒ توقف).
- حکم پس از دیدن نتیجه هیچ دستکاری نشد؛ همین سند تنها خروجی است.

— اویلر · 2026-08-17
