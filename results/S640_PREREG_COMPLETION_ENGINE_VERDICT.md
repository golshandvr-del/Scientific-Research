# S640 — پیش‌ثبتِ تکمیلی: حکمِ رسمیِ موتور (اصلاحِ روش‌شناختی به‌دستورِ کاربر)

**نویسنده:** امی نوتر · **دکاد:** S640–S649 · **تاریخ:** 2026-08-26
📌 قاعدهٔ جدیدِ کاربر: «هر یک از ده شماره باید به یک لایه اختصاص یابد و **حکم رسمی موتور**
برایش به دست آید.» S640 در اکتشاف متوقف شده بود (lift·√n=77.3<78) و حکمِ MD قبلی
دست‌نویس بود. این سند قاعدهٔ منجمد را **پیش از هر اجرا روی نیمهٔ دوم** قفل می‌کند —
همان الگوی اصلاحیِ لاگرانژ (`research/S630_S639_OFFICIAL_PREREG.md`).

## قاعدهٔ منجمد (بهترین سلولِ درون‌نمونه‌ایِ ثبت‌شده در MD قبلی — §۳.۲)

| قلم | مقدارِ قفل‌شده |
|---|---|
| سیگنالِ شورت | close کراسِ رو به پایینِ KAMA(10,2,30) **و** شیبِ KAMA منفی (slope_len=1) |
| لانگ | ندارد — فقط شورت (الگوی یکنوای §۳.۲) |
| کارت‌ها | {H3, H4, H6, H8, H12} |
| SL | 1.5×median(ATR100) روی نیمهٔ دومِ همان کارت؛ TP=SL؛ max_hold=64 |
| دورهٔ آزمون | فقط `df.iloc[len//2:]` |
| نول | per-side هم‌هندسه/هم‌تعداد K=500، uncond N=20000 (الگوی S670/S641) |
| استخر | `rqs2_pool.pool_cards` + محورِ ۵دقیقه‌ای + holdout_mask میانه (الگوی S641) |
| n_trials | 1 (مسیرِ C — جست‌وجو فقط نیمهٔ اول بود) |
| seed | 640640 |
| داور | `compute_rqs2` — حکم هرچه باشد بدون تغییر ثبت می‌شود |

**پیش‌بینیِ صادقانه:** در-نمونه lift·√n=77.3 زیرِ کفِ ۷۸ بود ⇒ محتمل‌ترین حکم
REJECT/UNPROVEN با z<2. هیچ بهبودِ پس-از-مشاهده انجام نمی‌شود؛ این تنها و آخرین
آزمونِ این خانواده است.

کد: `strategies/s640_final_holdout.py` — پس از این commit، **یک بار**.
