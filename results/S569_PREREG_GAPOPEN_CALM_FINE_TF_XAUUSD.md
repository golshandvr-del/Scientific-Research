# S569 — پیش‌ثبت (PREREG) — گپِ منفیِ بازگشایی × گیتِ رژیمِ آرام روی TF ریز (M5/M1) — XAUUSD

**دانشمند:** لایب‌نیتس — بلوک S560–S569 (شمارهٔ ۱۰ از ۱۰)
**تاریخ قفل:** 2026-09-05 — این سند **قبل از هر محاسبه‌ای** کامیت می‌شود؛ مُهر git = پیش‌ثبت.
**دارایی:** فقط XAUUSD. **داده:** `data/mt5_full/XAUUSD_{M5,M1}.csv` (gunzip از `.csv.gz`، ۱۵.۶ سال) — در کد `assert 'mt5_full' in src`.
**داور:** فقط `engine/rqs2.compute_rqs2` v2.6 — حکم فقط از موتور (یا مرگ صادقانه در دروازهٔ پیش‌ثبت = REJECT 0).
**SEED = 20260905**

---

## ۰. 🔴 بدترین خبر، اول

1. **این لایه آلفای مستقل نیست و ادعای استقلال نمی‌کند.** S560-M5 (ACCEPT 96) و S560-M1 (ACCEPT 95.6) *بدون گیت رژیم* پذیرفته شده‌اند. S569 زیرمجموعهٔ همان روزها را می‌گیرد. اگر ACCEPT شد، مثل S1521↔S1520 و S606↔S602، **پالایشِ** والد است، نه لایهٔ رقیب؛ در گزارش نهایی با این برچسب ثبت می‌شود و ممیزی هم‌پوشانی با S560-M5/M1 و S562 الزامی است.
2. **خطر قطع توان (قانون S525 گاوس):** هر گیت n را می‌کاهد (M5: 407 → ~۲۵۰–۳۰۰). ابطال‌گر P2 دقیقاً همین را می‌سنجد: گیتی که lift را بالا نبرد، فقط توان سوزانده.
3. **خطر گیتِ مرده (قانون S603):** اگر نرخ عبورِ گیت > ۹۰٪ باشد، گیت معنایی ندارد؛ چگالی قبل از PnL شمرده و گزارش می‌شود.
4. **خطر «آرامشِ» بی‌ربط در TF ریز:** σ RiskMetrics روی بازده M5 نیم‌عمر ~۱۱ کندل (~۱ ساعت) دارد؛ «آرام» یعنی *ساعتِ آخرِ روزِ قبل* آرام بوده — چیزی متفاوت از V (۱۴ روزِ دامنهٔ روزانه). این تفاوت خودِ پرسش است، اما ممکن است در M5/M1 فقط نویز باشد.

## ۱. فرضیهٔ علّی و پرسش

**قانون DD-انتقال (خانوادهٔ گپ، ۷ نقطهٔ شاهد: S404, S405, S407, S408, S562×2, S403/S406 منفی):** لبهٔ گپ در رژیم آرام زنده است و در رژیم پرنوسان DD منفجر می‌شود؛ گیت V معاملاتِ بد را *حذف* می‌کند (اهرم گزینش، نه بازشکل‌دهی pnl). این قانون فقط روی M15/M30/H1 اثبات شده. **پرسش ۱:** آیا روی ریزترین TFها (M5/M1) هم صادق است؟

**قانون آرامشِ σ (چهار ACCEPT موازی: S606, S607, S1521, S1911):** رویداد ساختاری × `σ_t ≤ median(σ_{t−233..t−1})` روی TF درشت. **پرسش ۲:** آیا این تعریف از آرامش روی TF ریز، برای رویداد گپ، همان اطلاعاتِ V را حمل می‌کند (هم‌خط) یا متعامد است؟ (سنجهٔ هم‌خطی: نرخ عبور مشترک ÷ حاصل‌ضرب نرخ‌ها؛ الگوی S607 P3.)

**پیش‌بینی ابطال‌پذیر اصلی:** بازوی V78 در هر دو TF lift بالاتری از پایهٔ S560 (M5: +43.98، M1: +44.98) و DD کمتری می‌گیرد؛ بازوی σ-CALM روی TF ریز یا هم‌خط با V است یا بی‌اثر.

## ۲. تعریف منجمد (بدون نگاه به جلو)

- **رویداد پایه:** عیناً `tools/s560_adjudicate.build(tf)` (ماسک S560 منجمد: گپ منفی، |gap| > چندک علّی q80 روزانه با تفکیک آخرهفته، سیگنال روی آخرین کندل روز قبل، ورود open کندل اول روز). **assert ماسک بازسازی‌شده == n_signals قفل S560** (BUG-DATASETDRIFT).
- **هندسه و hold:** عیناً از `results/_s560_arms/locked_config.json` واریانت V-TIME (M5: SL=TP=48.1، mh=1؛ M1: SL=TP=48.9، mh=4) — BUG-GEOMDRIFT؛ **هیچ بازتنظیمی**.
- **گیت V78:** `tools.s562_volfilter.vol_filter_mask(d, tf, mask, qv=78)` — qv منجمدِ پروژه (S404/S408/S562-H1)؛ جست‌وجو نمی‌شود.
- **گیت σ-CALM:** σ از `strategies.s840_*::ewma_z` منجمد (λ=0.94) روی close همان TF؛ `calm[i] = σ_i ≤ median(σ_{i−233..i−1})` روی کندلِ سیگنال i (آخرین کندل روز قبل؛ کاملاً علّی — همان تعریف S606/S1911، W=233 منجمد).
- **گیت DUAL:** V78 ∧ σ-CALM.
- **فضای جست‌وجو = ۶ بازو:** TF ∈ {M5, M1} × گیت ∈ {V78, SIGMA, DUAL}. **n_trials تجمعیِ خانوادهٔ گپ: 433 → 439.**

## ۳. پروتکل Path C (عین S562/S564–S568)

1. **LOCK** (فقط نیمهٔ اول، split 2018-10-20): برای هر TF، بازوی برنده = بیشینهٔ `t_fh` بین بازوهایی با n_fh ≥ 30. قبل از PnL: چگالی هر گیت (نرخ عبور روی سیگنال‌های پایهٔ نیمهٔ اول) و نسبت هم‌خطی V×σ ثبت می‌شود. بازویی با نرخ عبور > ۹۰٪ «گیت مرده» اعلام و از انتخاب حذف می‌شود.
2. **STOP-GATE پیش‌ثبت‌شده:** n_fh<30 ⇒ NO-VERDICT؛ lift_fh (vs نول LONG غیرشرطی، df نیمهٔ اول، n_perm=200) < +4pp ⇒ STOPPED_DEAD (نیمهٔ دوم بکر، REJECT 0).
3. **JUDGE:** یک تماس با کل داده؛ نول `null_for` (uncond 50000 + K=500 جایگشت هم‌هندسه)؛ n_trials=439؛ حکم عیناً از موتور.
4. **ممیزی هم‌پوشانی (فقط اگر ACCEPT):** سهم معاملات S569 در S560 همان TF (به‌تعریف ۱۰۰٪ زیرمجموعه) + lift و DD بخشِ حذف‌شده (complement) — تا نشان دهد گیت *چه چیزی* را حذف کرده.

## ۴. پیش‌بینی‌های شخصی (قبل از هر عدد)

- **P1 (DD-انتقال، نقطهٔ ۸ و ۹):** DD کلِ بازوی V78 < DD کلِ S560 همان TF (M5: 2.43٪، M1: 3.10٪) **و** lift ≥ پایه. اگر lift کمتر شد ⇒ گیت روی TF ریز توان‌سوز است (قانون DD-انتقال به TF ریز منتقل نمی‌شود).
- **P2 (هم‌خطی):** نسبت هم‌خطی V×σ در بازهٔ 0.95–1.10 (تقریباً مستقل) — چون یکی ۱۴ روز و دیگری ~۱ ساعت را می‌سنجد. پیش‌بینی: σ-CALM تنها **بی‌اثر یا ضعیف** است (lift ± 3pp نسبت به پایه)، DUAL ≈ V78.
- **P3 (چگالی):** نرخ عبور V78 ≈ ۷۵–۸۰٪، σ-CALM ≈ ۵۰–۶۵٪ (بازار در ساعت آخر روز معمولاً آرام است، اما نه همیشه).
- **حکم:** prior شخصی ACCEPT برای بازوی برنده ~۶۵٪ (چون پایه ACCEPT است)؛ ارزش علمی لایه در *پاسخ P1/P2* است نه در حکم.

## ۵. ابطال‌گرها (منجمد)

- **F1:** lift(V78) < lift(S560 پایه) در هر دو TF ⇒ قانون DD-انتقال روی TF ریز **ابطال** (گیت فقط توان می‌سوزد).
- **F2:** نرخ عبور گیت > ۹۰٪ ⇒ گیت مرده، بازو حذف.
- **F3:** دروازهٔ توقف بدون استثنا.
- **F4:** اگر σ-CALM بهتر از V78 شد، پیش‌بینی P2 غلط و «آرامشِ کوتاه‌مدتِ ساعت آخر» اطلاعات مستقل دارد — ثبت به‌عنوان کشف برای بلوک بعدی.

## ۶. تمایز از همسایه‌ها (ضد تجاوز)

- ≠ S560 (پایهٔ بی‌گیت؛ اینجا گیت) · ≠ S562 (همان گیت V روی M15/H1؛ اینجا M5/M1 + گیت σ + مقایسهٔ هم‌خطی).
- ≠ S404/S407/S408 (بلوک S400: گپ‌فیل با TP=close قبلی و SL=2×gap روی M30/M15 — خروج متفاوت، TF متفاوت؛ فقط read-only خوانده شد).
- ≠ S606/S607/S1521/S1911 (گیت σ روی شوک انگل/کایل/سقف تازه در H6–D1، بلوک‌های دیگر؛ کد σ فقط import می‌شود، بازنویسی نمی‌شود).
- هیچ فایلی خارج از `results/S569_*`, `results/_s569_arms/`, `tools/s569_*` لمس نمی‌شود.

**این سند پیش از هر محاسبه commit می‌شود. پس از commit، هیچ بندی تغییر نمی‌کند.**
