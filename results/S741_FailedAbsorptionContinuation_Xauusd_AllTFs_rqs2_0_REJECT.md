# S741 — «جذبِ ناکام ⇒ ادامهٔ حرکت» — XAUUSD (هر ۱۹ TF) — حکم: REJECT

> **راهبرد:** S741_FailedAbsorptionContinuation (جهتِ وارونهٔ S740)
> **حکم:** `REJECT` · **RQS2 = 0** (هیچ نامزدی به آزمونِ نهایی نرسید — شکست در دروازهٔ اکتشاف)
> **skill_p_perm:** — (آزمونِ H3 هرگز اجرا نشد؛ هیچ پیکربندی‌ای n≥30 با PF≥1.3 در اکتشاف نداشت)
> **پیش‌ثبت:** `results/S741_PREREG_FAILED_ABSORPTION_CONTINUATION_PATH_C.md` (commit `8ed3e2f6`، پیش از کد) · n_trials موروثی=128
> **کد:** `strategies/s741_failed_absorption.py` · چک‌پوینت‌ها: `results/_scan_S741/*.json`
> **داده (src):** `data/mt5_full/XAUUSD_<TF>.csv` — ۱۵٫۵۹ سال، بارگذاری با `tools/s434_fast_data.py`

## ۱) نتیجهٔ MTF (هر ۱۹ TF جداگانه؛ EURUSD به دستور کاربر حذف)

هر ۶۴ پیکربندیِ منجمدِ شبکه، در **هر ۱۸ TF قابلِ‌آزمون** (MN1=TOO_SHORT)، در پنجرهٔ اکتشاف یا n<30 داشت یا PF<1.3 ⇒ `NO_CANDIDATE` سراسری. بهترینِ M1 (که انگیزهٔ فرضیه بود): z=−2.29، همچنان زیانده.

## ۲) یافتهٔ علمیِ بسته‌شدنِ خانواده — «هر دو جهت می‌بازند»

| جهتِ رویدادِ جذب در کفِ پنجره (M1، بهترین‌ها) | WR | be_cost | lift |
|---|---|---|---|
| S740: بازگشت (LONG در کف) | 27–40٪ | 61–66٪ | −22..−36pp |
| S741: ادامه (SHORT در کف) | 41–43٪ | 61–66٪ | −18..−35pp |

جمعِ WRِ دو جهتِ متضادِ همان رویداد ≈ ۷۰–۸۳٪ < ۲×be (≈۱۲۳–۱۳۲٪): یعنی مشکل **جهت نیست، بارِ هزینه است** — رویدادِ جذب پس از هزینهٔ ۳٫۳pip روی SLِ کوچکِ ATRمحورِ TFهای ریز، از هر دو سو زیر سربه‌سر می‌ماند؛ و در TFهای درشت (که SL بزرگ و be کوچک می‌شود) رویداد **قابلِ‌نمونه‌گیری نیست** (کمیابیِ ساختاریِ تقاطعِ حجمِ بالا × بازهٔ تنگ — همبستگیِ ذاتیِ حجم-دامنه). این دو فک، خانوادهٔ جذبِ حجمی-تک‌کندلی را روی طلا کامل می‌بندند.

## ۳) پرهیز از ۸ خطای رایج

همان سپرهای S740 (سند `S740_..._REJECT.md` §۵) + دو مورد ویژهٔ این راهبرد:
- **چندگانگیِ موروثیِ صادقانه:** فرضیه از دیدنِ اکتشافِ S740 زاده شد ⇒ n_trials=64+64=128 پیش‌ثبت شد.
- **آلودگیِ holdout:** سقفِ حکمِ M5 به UNPROVEN پیش‌ثبت شده بود (به کار نیامد — M5 هم NO_CANDIDATE شد).

## ۴) قانونِ مرگِ ابدی

خانوادهٔ «جذبِ حجمی تک‌کندلی» (S740+S741؛ هر دو جهت؛ شبکهٔ پیش‌ثبت‌شده؛ XAUUSD همهٔ TFها) **برای همیشه مُرد**. هر ایدهٔ حجمیِ آینده باید سازوکاری غیر از تقاطعِ اسپایک×تنگی باشد (مثلاً انباشتِ چندکندلیِ OBV/AD یا واگرایی) و پیش‌ثبتِ مستقل بگیرد.

---
*«از آمدن و رفتنِ ما سودی کو؟» — سود همین دانش است: دو در بسته شد تا درِ درست پیدا شود.*
