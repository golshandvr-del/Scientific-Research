# S375 — «احیای S371 با قاعدهٔ شکستِ Kennedy روی ابزارِ تک» · `XAUUSD` · `H1` · **rqs2-07 · ❌ DEAD**

> پیش‌ثبت: [`S375_PREREG_kennedy_single_instrument_h1.md`](./S375_PREREG_kennedy_single_instrument_h1.md)
> کد: `strategies/s375_kennedy_single_instrument.py` · ابزارها: `tools/s375_overlap_audit.py`, `tools/s375_causality_check.py`, `tools/s375_family_integrity.py`
> دادهٔ خام: `results/_scan_S375/` (۱۴ کارت + ۲ داوری + ۳ ممیزی)
> نسب‌نامه: قاعدهٔ Kennedy (کتاب *Trading the Line*، نشستِ S374) روی لایهٔ سوختهٔ S371 (شکستِ هم‌جهتِ کانال)

---

## ۰. حکم در یک بند

لایه به‌عنوانِ لایهٔ معاملاتی **مُرد** — داوری کاملِ ۱۱ دروازهٔ RQS2 روی لنگرِ متنی
`REJECT` با امتیازِ `6.8` داد (`z=0.271` در برابر سدِ `2.5854`؛ ۱۰ از ۱۱ دروازه افتاد)
و بندِ قفلِ §۵ پیش‌ثبت («ردِ لنگر = ردِ لایه») اجازهٔ آزمودنِ عضوِ دیگر را نمی‌دهد.
**اما** نشست سه یافتهٔ ساختاریِ اندازه‌گیری‌شده تولید کرد که هرکدام مستقلاً برای
نشست‌های بعد ارزش پیش‌ثبت دارند (§۵).

---

## ۱. چرا این کاندیدا؟ (استدلالِ پیش از محاسبه — طبق درخواستِ کاربر)

از میانِ ۸ لایهٔ کانالیِ سوخته، `S371/XAUUSD-H1-ABL` انتخاب شد چون:

1. **تک‌بندی مرد**: `z=+2.855` پاس، `n=1637` فراوان، ولی `e_pip=3.38` در برابر هزینهٔ
   `3.3` ⇒ مازادِ `+0.08` عملاً صفر. گزارشِ خودش: «تمامِ مهارتش را اسپرد می‌بلعد.»
2. **جنسِ درمان با جنسِ بیماری یکی است**: یافتهٔ ساختاریِ S374 اندازه گرفت که قاعدهٔ
   Kennedy الگو غربال نمی‌کند (همپوشانیِ کانال ~کامل) بلکه **بارِ ورود** را عوض می‌کند
   (تأخیر ۱–۷ بار) ⇒ مستقیماً به صورتِ کسرِ `e_pip` حمله می‌کند.
3. **عدد از قبل در بایگانی بود ولی داوری نشده بود**: `results/_scan_S374/H1.json` بازوی
   طلای gate=False را با مازاد `+7.206` در برابرِ `+0.539` نگه داشته بود — و چون آمارهٔ
   پذیرشِ S374 «جفتِ ادغام‌شده» بود و یورو زیرِ هزینه بود، کارتِ H1 هرگز تک‌ابزاری
   پرسیده نشد.

بندِ ابطال‌پذیری (پیش‌ثبت §۶): «اگر e_pip طلای Kennedy زیرِ 6.0 درآمد، استدلالِ انتخاب
باطل است.» → **فعال نشد** (10.506 بازتولید شد).

---

## ۲. نتایجِ MTF — هر ۱۴ کارت (قانونِ اولِ پروژه)

### XAUUSD (Δe_pip = اثرِ خالصِ قاعدهٔ Kennedy)

| TF | close: مازاد | kennedy: مازاد | Δe_pip | حکمِ پیش‌غربال |
|----|----|----|----|----|
| M5 | −3.233 | −4.111 | **−0.878** | ❌ DEAD_BELOW_COST |
| M15 | −4.492 | −5.604 | **−1.112** | ❌ DEAD_BELOW_COST |
| M30 | −2.823 | −1.929 | **+0.894** | ❌ DEAD_BELOW_COST (بهبود ولی ناکافی) |
| H1 | +0.539 | **+7.206** | **+6.667** | ✅ PRESCREEN_PASS (z: 3.02→4.33) |
| H4 | +8.374 | **+29.674** | **+21.300** | ✅ PRESCREEN_PASS |
| D1/W1 | — | — | — | NO_SAMPLE |

### EURUSD

| TF | close: مازاد | kennedy: مازاد | Δe_pip | حکم |
|----|----|----|----|----|
| M5 | −2.008 | −2.320 | −0.312 | ❌ |
| M15 | −2.344 | −2.683 | −0.339 | ❌ |
| M30 | −3.322 | −2.854 | +0.468 | ❌ |
| H1 | −2.165* | −1.358 | +0.929 | ❌ |
| H4 | −8.5* | −7.214 | +1.317 | ❌ |
| D1/W1 | — | — | — | NO_SAMPLE |

**یافتهٔ بین‌ابزاری**: ترتیبِ Δe_pip روی هر دو ابزار **اکیداً صعودی با طولِ کندل** است و
تغییرِ علامت در هر دو بینِ M15 و M30 رخ می‌دهد. یکنواختیِ تایم‌فریمیِ S374 (ρ=1.0)
حالا روی ابزارِ دوم با pip-value و اسپرد و هندسهٔ متفاوت **تکرار** شد ⇒ خاصیتِ خودِ
تعریفِ شکست است، نه تصادفِ طلا. خوانشِ فیزیکی: شرطِ «کلِ دامنه آن‌سوی خط» روی
کندلِ ریز تقریباً هم‌زمان با close برآورده می‌شود (اطلاعِ صفر، حذفِ ~۶۰٪ نمونه) و روی
کندلِ درشت تأخیرِ واقعیِ چندباری می‌سازد که قیمتِ ورود را بهتر می‌کند.

**حدِ تواناییِ قاعده**: یورو حتی در بازوی close مهارتِ **منفی** دارد (z از −0.56 تا −5.75).
قاعدهٔ Kennedy درمان‌گرِ قیمتِ ورود است؛ نمی‌تواند علامتِ لایه‌ای را که جهتش غلط است
برگرداند. ⇒ فقط لایه‌هایی که «با مهارتِ مثبت روی هزینه مردند» نامزدِ این قاعده‌اند.

---

## ۳. داوریِ ۱۱ دروازه روی لنگرِ متنی — REJECT و تشخیصِ علت

لنگر (k=3, m=1.0, s=0.786؛ برگزیده از میانهٔ محورِ متنی، صفر درجهٔ آزادی):

| سنجه | close | kennedy |
|----|----|----|
| n | 129 | 64 |
| z (perm=500) | 1.685 | **0.271** |
| PF | 1.268 | 1.053 |
| net (pip) | +1,774.7 | +180.4 |
| expectancy | +25.41 | **−1.96** |
| دروازه‌های پاس | 6/11 | 1/11 |
| امتیاز RQS2 | 19.7 | **6.8** |

- ردِ لنگر **پیش‌بینی‌شده** بود (پیش‌ثبت §۴.۱: ~۷۰٪ احتمالِ شکست، مکانیزم = فروپاشیِ نمونه).
- تصحیحِ perm از ۴۰۰ به ۵۰۰ (به تذکرِ خودِ موتور) z را **کوچک‌تر** کرد (0.327→0.271) —
  یعنی اطاعت از موتور سخت‌گیرانه‌تر بود، نه مساعدتر.
- `share(TP<SL)=0.000`, `RR=1.272`, `WR=46.88٪` ⇒ اشتباهِ رایج #۸ مطلقاً رخ نداده.

**علتِ دقیق**: جدولِ اعضا نشان داد قاعدهٔ Kennedy **محلِ سود را در محورِ lookback جابه‌جا
می‌کند**: در دنیای close سود در شاخهٔ k=3 بود (لنگرِ من دومین عضوِ برتر)، در دنیای
kennedy سود به k=2 و k=5 منتقل شد و k=3 فروریخت. لنگرِ متنیِ من در تنها شاخه‌ای
نشسته بود که این قاعده جریمه می‌کند. طبق بندِ قفل، عوض‌کردنِ لنگر پس از دیدنِ نتیجه
ممنوع است — لایه با لنگرش رد می‌شود.

---

## ۴. ممیزیِ همپوشانی + آزمونِ علیّت (قانونِ سوم — به بعد موکول نشد)

- **فضای کانال**: 96.9٪ همپوشان (62/64) ⇒ قاعده لبهٔ الگوییِ نو نمی‌دهد.
- **فضای بارِ ورود**: 4.7٪ (3/64) ⇒ قاعده جای ورود را عوض می‌کند.
- **پرسشِ اجباریِ فیلتر**: نسخهٔ «هرزمانی» تفکیکِ فریبندهٔ `+0.4408` در برابرِ `−0.1464` داد
  (اثرِ غربال `+0.3050`) — اما آزمونِ علیّت آن را **نابود کرد**: از ۶۳ معاملهٔ فیلترشده، ۶۰
  مورد تأییدِ Kennedy را **پس از** ورود گرفتند (میانهٔ تأخیر ۱ بار، هیچ موردی قبل از ورود
  نبود). نسخهٔ علّیِ قابلِ‌معامله: n=3, meanR=−0.286. مکانیزمِ توهم: **علیّتِ معکوس** —
  معاملهٔ موفق قیمت را آن‌قدر می‌بَرد که کلِ دامنه از خط رد شود؛ تأیید معلولِ موفقیت است
  نه علتش.
- ⛔ **نتیجهٔ الزام‌آور**: این شرط به‌عنوانِ «فیلترِ صحتِ سیگنال» روی هیچ لایهٔ دیگری
  قابلِ استفاده نیست. مسیرِ بازمصرفِ قانونِ سوم برای این شرطِ خاص **بسته** است.

---

## ۵. آزمونِ سلامتِ خانواده — قاعده اطلاعِ واقعی دارد

بازوی کورِ «فقط ۱ بار صبر کن، بدونِ هیچ شرطی» (میانهٔ تأخیرِ Kennedy):

| بازو | trade_pooled | Δ نسبت به close |
|----|----|----|
| close | +0.0092 | — |
| **kennedy** | **+0.1170** | **+0.1078** |
| blind (+1 bar) | −0.0021 | −0.0113 |

**سهمِ اطلاعیِ قاعده = +0.1191** (آستانهٔ «تأخیرِ محض» = 0.02؛ ~۶ برابر رد شد).
صبرِ کور حتی زیان‌ده است ⇒ کلِ اثر از **محتوای اطلاعاتیِ** شرطِ دامنه می‌آید.
حذفِ صف (queue reordering) هم منتفی: نگه‌داشت 98.1/98.8/98.4٪.

**ساختارِ شاخه‌ای (یافتهٔ اندازه‌گیری‌شده برای آینده):**
- هر ۴ عضوِ k=5: منفی→مثبت (بیشینه +0.3450)
- هر ۴ عضوِ k=2: بهبود +0.1665 تا +0.2847
- هر ۴ عضوِ k=3: بدتر (−0.0478 تا −0.1107)

فرضیهٔ پیش‌ثبت‌پذیر برای نشستِ آینده: «زیرِ تعریفِ شکستِ دامنه‌ای، lookbackِ سودده
به‌طورِ نظام‌مند از lookbackِ سوددهِ تعریفِ close **متفاوت** است (کوتاه‌تر/بلندتر، نه میانی)» —
فرضیه‌ای ساختاری دربارهٔ قاعده، نه جست‌وجوی عضوِ پاس‌شونده.

---

## ۶. دفترِ چندگانگی

`۱۱۷` آزمونِ تثبیت‌شده در پیش‌ثبت (۱۱۲ قبلی + ۵ جریمهٔ محافظه‌کارانه برای درجهٔ آزادیِ
«آمارهٔ تک‌ابزاری به‌جای ادغامی»). سدِ `2.5854` **پیش از** وجودِ هر نتیجه منجمد شد.
آزمون‌های تشخیصیِ پس از رد (overlap/causality/integrity) عضوِ جدیدی نیازمودند و
درجهٔ آزادیِ انتخابی خرج نکردند ⇒ شمارش از ۱۱۷ فراتر نرفت.

---

## ۷. درس‌های نشست (آنچه پروژه نگه می‌دارد)

1. **قاعدهٔ Kennedy اطلاعِ واقعی دارد** ولی فقط به‌صورتِ «ماشهٔ ورود» — نه فیلترِ پسینی
   (علیّتِ معکوس) و نه روی ابزار/لایهٔ بی‌مهارت (یورو).
2. **دامنهٔ کاربرد**: TFهای درشت (H1+)؛ روی M15− فعالانه مضر است. بین‌ابزاری تکرار شد.
3. **تلهٔ متدولوژیک برای همیشه**: هر فیلتری که شرطش «پس از ورود» فعال می‌شود، باید
   پیش از هر ادعایی آزمونِ توزیعِ تأخیر بدهد. تفکیکِ تمیزِ سود/زیان می‌تواند صرفاً
   بازتابِ خودِ نتیجه باشد.
4. **جابه‌جاییِ شاخه**: تغییرِ تعریفِ ماشه می‌تواند عضوِ بهینهٔ خانواده را جابه‌جا کند ⇒
   لنگرِ متنیِ ثابت بینِ دو تعریف قابلِ انتقال نیست.

## ۸. تصمیم

- **لایهٔ S375: ❌ DEAD** — به سایت وصل **نمی‌شود**؛ `local-mobile` تغییری نمی‌کند.
- کارتِ H4 پیش‌غربال پاس کرد ولی همان قلمروِ **S374ِ پذیرفته‌شده** است (همپوشانیِ
  ~کامل با لایهٔ فعالِ سایت) ⇒ ارزشِ افزودهٔ مستقل ندارد؛ ادعای جدیدی ثبت نمی‌شود.
- سه یافتهٔ §۵ و §۷ سرمایهٔ نشست‌اند.

---

_S375 — داوری با RQS2 v2.4 · perm=500 · سد 2.5854 · دفترِ آزمون ۱۱۷ · کدها و JSONهای خام در مخزن_
