# S526 — سقفِ تازهٔ غلتانِ ۹۰-کندلی → ادامه — XAUUSD-H8 — RQS2=88.2 — ✅ ACCEPT

**داور:** RQS2 v2.6 (موتور رسمی، دست‌نخورده) · **نویسنده:** گاوس (قلمرو S520–S529) · **تاریخ:** 2026-08-26
**پیش‌ثبت:** `results/S526_PREREG_FreshRollingHighContinuation_Xauusd_H4H6H8H12D1.md` (کامیت 47edc86c — **قبل** از هر عدد)
**runner:** `tools/s526_fresh_high_runner.py` · **رکوردها:** `results/_s526/` (۵ کارت + stress + overlap_audit)
**داده:** `data/full/` ۱۵.۵۹y (H4 از `data/XAUUSD_H4.csv` ۱۵.۵۳y) · SEED=20260805 · K=2000
**وضعیت:** ⏸ **در انتظار تأیید کاربر — هیچ استقراری انجام نشده** (قانون گردش‌کار)

---

## ۱) حکم موتور — عیناً

```
S526-H8 رسمی (n_trials=5):   ACCEPT  RQS2=88.2 | n=200 WR=55.50 BE=40.73 lift=+14.77 z=3.23 PF=1.939
                              گیت‌ها: H0✓ H1✓ H2✓ H3✓ H4✓ H5✓ H6✓ H7✓ H8✓ H9✓ H10✓ — هر ۱۱ سبز
S526-H8 تنش (n_trials=50):    ACCEPT  RQS2=86.6 — زیر بودجهٔ تنشِ ۱۰×، حکم برنمی‌گردد
نول: شرطی‌شده در فضای درفت-مثبت (سخت‌ترین ممکن) — uncond=44.17، perm_max=52.92، beats_perm_max=True
net=+$11,354 · per_year=12.8 معامله · نسبت trades/signals=200/361 (allow_overlap=False)
```

## ۲) قانون لایه (صفر پارامتر آزاد — همه از پیشینه‌های منجمد)

- **رویداد:** `nh = close > max(close[i−90..i−1])`؛ سیگنال = فقط **لبهٔ تازه** `nh & ~nh[i−1]` (قانون S963: اطلاعات در لبهٔ تغییر است، نه حالت انباشته)
- **جهت:** LONG (قانون جهت‌مندی S522) · **ورود:** open کندل بعد، شبیه‌ساز محافظه‌کار S382
- **براکت:** SL=1.5×median(ATR100)=179.7pip، TP=1.5×SL=269.5pip — **RR=1.5≥1 ✓ قانون بودجه**
- **90:** منجمد از S950/S523 — جست‌وجو نشده
- **هزینه:** اسپرد 3.3pip لحاظ (BE=40.73%)

## ۳) چرا این ACCEPT صادقانه است

1. **پیش‌ثبت کامل قبل از هر عدد** (47edc86c): رویداد، کارت‌ها، هندسه، نول، بودجه، ابطال‌گرها.
2. **صفر جست‌وجو:** 90 و هندسه از خانوادهٔ منجمد؛ تنها تصمیمِ این نشست خودِ رویداد بود که در پیش‌ثبت قفل شد. n_trials=5 (۵ کارت). حتی زیر تنش ۱۰× (n_trials=50) حکم ACCEPT می‌ماند — حاشیهٔ H5 فراخ است.
3. **سخت‌ترین نول ممکن:** نه نمونهٔ تصادفی کل تاریخ، بلکه نمونهٔ تصادفی **فقط در فضای درفت-مثبت** (قانون S523/S502). یعنی lift=+14.77 مهارتِ خالص **ورای بتای رژیم صعودی** است. با perm_max=52.92 هم WR=55.50 بالاتر است (beats_perm_max).
4. **MTF کامل گزارش شد** (قانون MTF): H4=POWER-LIMITED 27.7 (z=3.09 دقیقاً روی سد، n=414) · H6=REJECT 26.6 (z=2.65) · H12=REJECT 22.8 · D1=REJECT 8.2. قلهٔ لبه در H8 — همان جایی که S520/S523 قله را نشان داده بودند. منحنی لیفت یکنوا-صعودی تا H8 و سپس نزولی: ساختار، نه نویز.
5. **پیشینهٔ نامساعد خانوادهٔ breakout لحاظ شد:** S346/S371/S373/S543 همه REJECT بودند — اما همه یا کانال‌محور/state-machine بودند یا TF ریز. رویداد «سقف تازهٔ close غلتان + لبهٔ تازه + TF درشت + نول شرطی» بکر بود (grep در پیش‌ثبت).

## ۴) ممیزی هم‌پوشانی با لایهٔ زندهٔ S382-H4 (فوری، طبق قانون)

```
H8: overlap=53.0% | OV n=106 lift=+18.70 | nonOV n=94 lift=+10.33
H4(PL): overlap=62.8% | nonOV n=154 lift=+7.63
```

- ۴۷٪ معاملات H8 **کاملاً خارج از پنجره‌های لایو** است و همان‌جا هم lift=+10.33 دارد ⇒ لایه ارزش افزودهٔ مستقل واقعی دارد، نه سایهٔ S382.
- هم‌پوشانی 53% طبیعی است: هر دو ادامه‌دهندهٔ روند بر TF درشت‌اند؛ رویدادها متفاوت‌اند (سقف تازهٔ ۹۰-کندلی ≠ گذر WPR از −13).

## ۵) داوری ابطال‌گرهای پیش‌ثبت‌شده

- **F1 (هیچ کارتی lift>+4pp؟):** رد — ۴ کارت از ۵ بالای +4.8pp.
- **F2 (بهترین z زیر سد H5؟):** رد — H8 از سد گذشت، هر ۱۱ گیت سبز.
- **پیش‌بینی شخصی:** n در بازهٔ پیش‌بینی (100–300: شد 200) ✓، lift بالاتر از بازهٔ پیش‌بینی (+14.77 > +9) — رویداد از انتظار قوی‌تر بود.

## ۶) جایگاه در دانش خانواده

- چهارمین لبهٔ مستقل H8/D1 درشت: S602 (شوک)، S770 (انبساط ADR)، S950 (پرش)، **S526 (سقف تازه)** — همه ادامه‌دهنده، همه هم‌راستا با رژیم. الگوی «TF درشت + رویدادِ لبهٔ تغییر + رژیم» بار دیگر تأیید شد.
- برای دهکِ گاوس: نخستین ACCEPT پس از ۶ لایهٔ REJECT — و دقیقاً از سنتز قوانین همان ۶ لایه (S522 جهت + S523 رژیم/نول شرطی + S524 ادامه-نه-بازگشت + S963 لبهٔ تغییر + قلهٔ H8 از S520).

## ۷) گام بعد (فقط پس از تأیید کاربر)

استقرار طبق قانون: **اول local-mobile، سپس سایت** (Edit→Commit→Push per file). تا آن زمان هیچ فایل مستقری لمس نمی‌شود.
