# S894 — برداشتِ لبهٔ تقویمیِ ساعتِ پایانی — XAUUSD × ۱۹ TF — **UNPROVEN (همهٔ ۱۶ کارت)**

> **حکم:** UNPROVEN در هر ۱۶ کارتِ ساعت-دار — **قاعدهٔ توقفِ پیش‌ثبتی روی همه فعال شد؛
> هولد-اوت در هیچ کارتی لمس نشد** (ششمین حفظِ متوالیِ هولد-اوت در پروژه، اولی در دههٔ من).
> D1/W1/MN1: INCOMPLETE (مفهومِ ساعت تعریف‌نشده).
> پیش‌ثبت: `S894_PREREG_CalendarHourHarvest_Xauusd_MTF.md` (کامیت `e083e7ec`، پیش از هر اجرا)
> کد: `strategies/s894_hour_harvest.py` · JSONها: `results/_s894/` · n_trials صادقانه: 1488
> نویسنده: جورج سوروس (دههٔ S890–S899)

## ۱) چه آزمایش شد

پرسش: آیا لبهٔ تقویمیِ **واقعیِ** 8σ (یافتهٔ S893: خوشهٔ H21–23 لانگ، لیفتِ کانونی
+4.9…+6.3pp) با اهرمِ انتخاب/هندسه **اقتصادی** می‌شود؟

- رویداد: اولین کندلِ ساعتِ H∈{21,22,23}، فقط لانگ (میراثِ قفل‌شدهٔ S892).
- هندسه: A (1.5ATR/RR1.5) · B (SL=TP=3ATR، روحِ V-TIME S560) · C (TP=1ATR/SL=2.058ATR).
- فیلترِ علّی: none · drift(24h، روحِ S950) · calm(q70 رولینگ، روحِ S404) · drift+calm.
- اهلیتِ شلیک: `n_IS ≥ 100` **و** `exp@2×cost_IS > 0` روی ۷۰٪ اول.

## ۲) نتیجه — صفر پیکربندیِ اهل در ۵۷۶ سلولِ جست‌وجو

در **هیچ‌یک** از ۱۶ کارت × ۳۶ پیکربندی، انتظارِ IS پس از تنشِ ۲×هزینه مثبت نشد.
حتی در کارت‌های خوشهٔ طلایی (H1/H2/H3) که لیفتِ آماریِ +6pp دارند، بهترین سلول‌ها
زیرِ سدِ `exp@2×cost>0` ماندند ⇒ طبق پیش‌ثبت، **شلیکِ نهایی در هیچ کارتی اجرا نشد**.

## ۳) درسِ ثبت‌شده — بستنِ رگه با قانونِ مرگِ ابدی

**«رانشِ تقویمیِ ساعت-محور روی طلا: آماری واقعی، اقتصادی مرده — در هر سه هندسه و
هر چهار فیلترِ انتخاب.»** لبهٔ +6pp روی مرجعِ ~45٪ یعنی WR≈51٪ با RRِ متقارن‌نما؛
حاشیهٔ سود آن‌قدر نازک است که حتی یک لایهٔ هزینهٔ اضافی آن را می‌بلعد. فیلترهای
drift/calm لیفت را متمرکز نکردند (همگرا با درسِ S603 امروز: فیلتری که فقط حذف
می‌کند، توان می‌سوزاند) و هندسهٔ V-TIME هم نجاتش نداد (برخلافِ گپِ روزانهٔ S560 که
رویدادش «اطلاعاتِ شبانهٔ متراکم» دارد، نه صرفِ عبورِ عقربه).

⚰️ **مرگِ ابدی:** خانوادهٔ «ساعتِ روز به‌مثابهِ سیگنالِ مستقل» در دههٔ S890 برای
همیشه بسته شد (S892→S893→S894، سه آزمایشِ پیش‌ثبت‌شده). بازگشتِ مجاز فقط به‌صورتِ
*فیلترِ زمینه* روی رویدادِ آناتومیکِ دیگر (الگوی پنجرهٔ طلاییِ S571).

## ۴) اثباتِ پرهیز از ۸ اشتباه
۱. نالِ کانونیِ هندسه-همتا آماده بود (قانونِ دهه از S893) — به شلیک نرسید.
۲. هولد-اوت: **صفر لمس** در هر ۱۶ کارت (قاعدهٔ توقفِ از-پیش-اعلام).
۳. شمارشِ صادقانه: n_trials=1488 شاملِ کلِ میراثِ رگه (912+576).
۴. هزینه: اهلیت خودِ سدِ `exp@2×cost>0` بود — سخت‌گیرترین داورِ ممکن.
۵. بدونِ عددِ گرد: همهٔ هندسه‌ها مضاربِ ATRِ اندازه‌گیری‌شده (47.34pip در H2 و…).
۶. بدونِ نشتِ آینده: فیلترها همه با شیفتِ t−1؛ ورود openِ کندلِ بعد.
۷. یک شبیه‌ساز: همان `se.simulate_trades` برای لایه و نال.
۸. حکم/توقف عیناً طبقِ پیش‌ثبت؛ هیچ «فقط یکی دیگر» رخ نداد.

## ۵) وضعیتِ دهه
S890 REJECT(21.8) · S891 REJECT(13.2) · S892 REJECT(10.4) · S893 REJECT(19.5،مترولوژیک)
· **S894 UNPROVEN (توقفِ پیش از شلیک)** → بعدی: **S895** — ایده‌ای از خانوادهٔ برندگان
(رویداد-آناتومیک + اهرمِ انتخاب)، دور از هر رگهٔ مرده.
