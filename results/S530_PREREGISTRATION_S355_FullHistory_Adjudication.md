# S530 — پیش‌ثبتِ داوریِ تمام‌تاریخیِ لایهٔ زندهٔ S355 · XAUUSD-M5

**تاریخ**: 2026-08-13 · **نویسنده**: AI محقق (نشستِ S530+) · **وضعیت**: پیش‌ثبت، **قبل از اجرای آزمون**

---

## ۱. پرسش علمی (از گزارش رسمی مأموریت ۴، بخش ۶.۱ و توصیهٔ اولویت ۱)

لایهٔ `S355` هم‌اکنون روی کارتِ `XAUUSD-M5` سایت **زنده** است. حکم ACCEPT آن (83.9)
روی پنجرهٔ ~۲.۸ سالهٔ `data/XAUUSD_M5.csv` صادر شد. اندازه‌گیریِ ثانویهٔ مأموریت ۴
روی کلِ ۱۵.۶ سال نشان داد:

| پنجره | n | WR | z |
|---|---:|---:|---:|
| ۲.۸ سال اخیر | 47 | 72.34% | 3.47 |
| کل ۱۵.۶ سال | 160 | 56.88% | 2.01 |

دو تفسیر رقیب و هیچ‌کدام اثبات‌نشده: (۱) لبهٔ واقعیِ مختصِ رژیم اخیر؛ (۲) بیش‌برازش
به پنجرهٔ تنظیم. **این آزمون برای فیصلهٔ همین دوگانگی است.**

## ۲. قاعدهٔ منجمد (صفر جست‌وجو در این نشست)

هیچ پارامتری در این نشست تنظیم یا انتخاب نمی‌شود. قاعده عیناً از منابع commit شده:

- **مولد**: `strategies/s333_s79_pullback_revival.py :: BEST_CFG['XAUUSD_M5']`
  (`ef=20, es=100, rp=21, rth=35, confirm='rsi_turn', hurst=0.57, er=0.25`)
- **فیلتر**: `strategies/s351_lpsb.py :: lpsb_signals(df, L=8, f=0.33, warmup=200)`
  با `CENTRAL` از `strategies/s351_verdict.py` — شرط: `state == -1`
- **هندسه**: `sl=120pip, tp=120pip, max_hold=96` (از خود BEST_CFG خوانده می‌شود، رونویسی نمی‌شود — درس BUG-CFGKEYS)
- **سیمولاتور**: `se.simulate_trades(..., asset='XAUUSD', allow_overlap=False)` — اسپرد 3.3pip، کمیسیون ۰ (مشخصات حساب کاربر)

## ۳. داده (الزام صریح کاربر: دادهٔ کامل)

- **فایل**: `data/mt5_full/XAUUSD_M5.csv` (استخراج‌شده از gz مخزن؛ ~۱٬۰۸۹٬۵۷۴ میله، ۲۰۱۱→۲۰۲۶، ۱۵.۶ سال)
- گارد `BUG-DATASETDRIFT`: اسکریپت **مسیر فایل، تعداد ردیف و بازهٔ تاریخ** را در سر خروجی چاپ می‌کند. اگر بازه < ۱۴ سال بود ⇒ شکست اندازه‌گیری، اجرا متوقف.

## ۴. مسیر چندگانگی و آستانه‌ها (قفل‌شده پیش از اجرا)

- **ماهیت آزمون**: تأییدی (confirmatory) — یک قاعدهٔ ازپیش‌منجمد، یک دیتاست، **یک اجرا**. هیچ جاروبی انجام نمی‌شود ⇒ `n_trials = 1` برای این آزمون.
- **اعلام صادقانهٔ سوگیری انتخاب**: S355 به این دلیل آزموده می‌شود که زنده است و مأموریت ۴ نگاهی بدون‌جریمه به عدد کامل آن انداخته (z=2.01 دیده شده). بنابراین این آزمون **نمی‌تواند ACCEPT جدیدی تولید کند** — فقط می‌تواند وضعیت لایهٔ زنده را **تأیید یا ابطال** کند. جهتِ محافظه‌کاری: هر خطای تصمیم به زیانِ لایه تفسیر می‌شود.
- **معیار حکم** (RQS2 v2.6، `engine/rqs2.py::compute_rqs2` با نالِ اندازه‌گیری‌شده، `n_perm=500, seed=20260813`):
  - **CONFIRMED-FULL**: هر ۱۱ دروازه روی کل ۱۵.۶ سال پاس ⇒ لایه زنده می‌ماند، گزارش به‌روزرسانی.
  - **REGIME-ONLY**: H3 روی کل داده رد (z<3.09) ولی معاملات دورهٔ اخیر (از 2023-09، شروع دادهٔ قدیم) سودده و z دورهٔ اخیر ≥3.09 ⇒ لایه «مشروط به رژیم» است؛ به کاربر گزارش می‌شود که کارت M5 ریسکِ وابستگی به رژیم دارد؛ تصمیم حذف/ابقا با کاربر.
  - **BURNED**: H3 رد و دورهٔ اخیر هم z<3.09 بازتولید نشود ⇒ حکم ACCEPT قبلی متزلزل؛ گزارش هشدار برای کاربر.
- **پیش‌بینی من (ثبت پیش از اجرا)**: بر پایهٔ z=2.01 گزارش ۴، محتمل‌ترین خروجی REGIME-ONLY است. اگر CONFIRMED-FULL درآمد، یعنی نالِ اندازه‌گیری‌شده روی ۱۵.۶ سال ضعیف‌تر از برآورد ثانویهٔ مأموریت ۴ است — این را باید با شک بررسی کنم نه جشن.

## ۵. گام بعدی از پیش اعلام‌شده (برای جلوگیری از garden of forking paths)

فقط اگر حکم REGIME-ONLY یا BURNED شد: کوشش احیا به‌عنوان **S530** با فیلتر رژیمی از
خانوادهٔ ازپیش‌موجود پروژه (`hurst55 / r2_34 / er_21 / chop_21` — همان خانوادهٔ
پیش‌ثبت‌شدهٔ S351 با همان آستانه‌های فیبوناچی)، با **مسیر C** (hold-out): جست‌وجو فقط
روی نیمهٔ اول ۱۵.۶ سال، یک آزمون روی نیمهٔ دوم. آن مرحله پیش‌ثبتِ جداگانهٔ خودش را
خواهد داشت.

— اویلر (persona)، پیش از اجرای هر آزمون. مُهر زمانی این commit = پیش‌ثبت.
