# S1524 — پیش‌ثبت (قبل از هر آزمون): کفِ تازهٔ تقویم‌همتا (Calendar-Matched Fresh Floor) — آزمون مستقل قانون S1523 روی رویداد نیوتن

**نویسنده**: Carl Friedrich Gauss — بلوک دوم S1520–S1529، شمارهٔ ۵ از ۱۰
**داور**: `engine/rqs2.py::compute_rqs2` v2.6 (دست‌نخورده) · **مسیر B** (یک تعریف منجمد، صفر پارامتر آزاد)
**داده**: `data/full/*.csv` = gunzip از **`data/mt5_full/`** (۱۵.۶ سال؛ H4 از mt5_full-H1 بازنمونه — S759 تأیید کرده OHLC ۱۰۰٪ با mt5_full/XAUUSD_H4 یکسان است) · **فقط اکتشاف؛ سایت هرگز**

## ۰) درس‌های اعمال‌شده (صریح)
1. **کارت اصلی = پرشمارترین کارت** (خطای S1523/D1 تکرار نمی‌شود): **H4** (511 لبه، ۳۲.۸/سال).
2. **پنجره به زمان تقویمی** منجمد می‌شود (قانون S1523).
3. **هیچ گیتی روی رویداد** — رویداد خام؛ گیت رژیمی روی کارت با z<3.5 محکوم است (S1513/S1515).
4. **رویداد S526 بازیافت نمی‌شود** — رویداد این آزمون «کفِ تازه» نیوتن (S1511) است؛ هم‌بار با سقفِ تازهٔ ماهانه فقط ۲۳–۳۴٪ (سنجش توصیفی، بدون PnL) ⇒ مستقل.

## ۱) افشا و پرسش علمی
- fetch: ACCEPTهای جدید بررسی شد — **S1581** (کفِ تازهٔ ۹۰ × حجم، H6 88.0؛ H8/H4 با ۹۰ کندل PL)، S759 (شکست ساختاری داو × ρ، H4)، S798 (شوک نشست لندن H8)، S1742، S1400/S1560 (گپ)، S803 (REDUNDANT). REJECT مهم: **S1515 نیوتن** — قانون وارونگی رژیم فقط H6 است؛ «هر گیت رژیمی روی کارت z<3.5 محکوم».
- **S1511 نیوتن** (کفِ تازه، `low > max(low[t−90..t−1])`): با ۹۰ کندل ثابت، فقط **H6 ACCEPT** (+10.03، z=3.10)؛ H8 (+8.12, z 1.4)، H12 (+8.16, z 0.21)، D1 (+10.90, z 0.56) REJECT. دقیقاً همان نقش S526 پیش از S1523 — یک برندهٔ تک‌TF با پنجرهٔ کندلی ثابت.
- **پرسش**: آیا قانون S1523 («افقِ تقویمی، نه شمار کندل») روی رویدادی که **من نساخته‌ام** هم برقرار است؟ افق برندهٔ S1511 = ۹۰ کندل H6 = **۵۴۰ ساعت = ۲۲.۵ روز معاملاتی**. اگر H_cal عام باشد، همهٔ کارت‌ها با LB همتا باید به lift ≈ +10 همگرا شوند و کارت پرشمار (H4) با z بالاتر برنده شود.
- **سنجش توصیفی بدون PnL** (با افق ۳۰ روز، پیش از تصمیم افق): لبه‌های کف تازه D1 193 · H12 301 · H8 358 · H6 415 · H4 511؛ ۹۸–۱۰۰٪ در drift>0 ⇒ نول شرطی درست. افق نهایی (۲۲.۵ روز) بر اساس ACCEPT منتشرشدهٔ S1511 انتخاب شد، **نه** بر اساس این شمارش. هیچ WR/سود تولید نشد.
- تازگی: هیچ سندی کفِ تازه را با پنجرهٔ تقویم‌همتا نسنجیده (S1511: ۹۰ ثابت؛ S1581: گیت حجم؛ S1623: حاشیهٔ کف). **بکر.**

## ۲) تعریف منجمد
| جزء | مقدار |
|---|---|
| افق | **۵۴۰ ساعت** (= ۹۰ کندل H6، افق ACCEPT S1511) · LB = round(540 / ساعتِ کندل): **H4→135، H8→68 (67.5 گرد بالا)، H12→45، D1→23 (22.5 گرد بالا)** |
| رویداد | `ff = low[t] > max(low[t−LB..t−1])`؛ سیگنال = `ff & ~ff[t−1]` (لبهٔ تازه) ⇒ **LONG** |
| گیت | هیچ |
| براکت/ورود/هزینه | SL=1.5×median(ATR100)، TP=1.5×SL · open کندل بعد · FIFO · 3.3 پیپ (هارنس S382/S1511 عیناً) |
| نول | شرطی در فضای `close[t−1]−close[t−LB] > 0`؛ uncond strides 1/3/7 + جایگشت K=2000 seed=20260805؛ MAX |
| کارت‌ها | **H4** (اصلی، پرشمارترین)، H8، H12، D1 (H6 = S1511 عیناً، دوباره داوری نمی‌شود) |

## ۳) بودجهٔ چندگانگی
n_trials = **34** = خانوادهٔ سقف/کف تازهٔ گاوس (30 تا S1523) + ۴ کارت. تنش 50 در MD.

## ۴) ابطال‌گرها و پیش‌بینی‌ها (منجمد)
- **F1**: حکم رسمی = موتور روی **H4(135)**؛ ACCEPT فقط اگر ۱۱/۱۱.
- **P1 (H_cal عام)**: میانگین lift چهار کارت تقویم‌همتا باید ≥ +9pp و **پراکندگی** (max−min) آن‌ها کمتر از پراکندگی نسخه‌های ۹۰-کندلی S1511 (+8.12..+10.90، دامنه 2.8 — این‌جا از قبل کم است؛ پس معیار اصلی: **همگرایی به سطح ≥ +9** و بهبود D1 و H12 نسبت به z قبلی). اگر کارت‌های همتا هم‌چنان z<2 و lift پراکنده ⇒ H_cal برای کف برقرار نیست ⇒ قانون S1523 «مخصوص سقف تازه» بازنویسی می‌شود.
- **P2 (توان از رزولوشن)**: H4(135) باید z > z(S1511-H6)=3.10 بدهد (n بزرگ‌تر با همان افق). اگر lift ≈ +10 ولی z<3 ⇒ FIFO/اسپرد روی H4 توان را می‌خورد (S525).
- پیش‌بینی شخصی: H4(135) n≈300–330، WR 51–54، lift +9..+12، z 3.0–3.9 ⇒ **ACCEPT مرزی احتمالی (۵۵٪)**؛ H8(68) lift +9..+11 z 2–2.8 PL؛ H12/D1 PL/REJECT از کم‌شماری.
- اگر ACCEPT: توقف و گزارش؛ overlap فوری با S1511-H6، S1523-H4(180)، S382-H4. اگر REJECT: گزارش کوتاه ⇒ S1525.
