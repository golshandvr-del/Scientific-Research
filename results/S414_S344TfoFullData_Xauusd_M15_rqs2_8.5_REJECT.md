# S414 — بازداوریِ تأییدیِ S344 (Brooks Trend-from-Open، XAUUSD-M15 SHORT) روی دادهٔ کاملِ ۱۵.۶ ساله — ❌ REJECT (RQS2 = 8.5)

**دانشمند:** MISSION_2 (بلوک S410–S419) · **تاریخ:** 2026-09-10 · **داور:** `engine/rqs2.py::compute_rqs2` v2.6 — حکم عیناً
**پیش‌ثبت:** `results/S414_PREREG_S344_TFO_FULLDATA_CONFIRMATORY.md` (کامیت `82602c91` — **قبل** از هر عدد)
**اسکریپت:** `strategies/s414_s344_fulldata.py` (کامیت قبل از اجرا) · **رکوردها:** `results/_scan_S414/{arm_A,arm_B,summary}.json`
**داده:** `data/mt5_full/XAUUSD_M15.csv` — ۳۶۳٬۷۷۸ کندل، 2011-01-03 → 2026-08-07 (src در JSON ثبت) · تلهٔ E-16 خنثی

---

## ۱) حکم رسمی موتور — عیناً (بازوی حاکم B، هندسهٔ ATR-نرمال)

```
S414-B (k_sl=8.282×ATR100, RR=1.545, mh=32, SHORT, r2h) | n_trials=79
REJECT  RQS2=8.5 | n=267 WR=47.57% PF=0.835 lift=+1.60pp z=0.51 p_perm=0.305
exp=−0.07pip  exp@2c=−3.37pip  oos(پنجرهٔ بکر)={n:173, WR:38.73%, PF:0.625, net:−$1,453; نیاز WR≥40.19}
گیت‌ها: H0✓ H1✗ H2✗ H3✗ H4✗ H5✗ H6✗ H7✗ H8✗ H9✗ H10✓   (۹ گیت قرمز)
نول: قوی‌ترِ {uncond 45.96, gated_r2h 45.97} → gated_r2h · K=500 · SEED=414
```

بازوی اطلاع‌رسان A (SL/TP لفظی 220/340): **REJECT 10.7** — n=267، WR=47.57، lift=+1.83، z=0.60، exp2c=−0.07، همان oos مرده. هر دو بازو هم‌حکم.

## ۲) نتیجهٔ محوری — تلهٔ E-16 با دقتِ جراحی تأیید شد

| پنجره | n | WR | PnL (pip، بازوی B) |
|---|---|---|---|
| **دیده‌شده** 2020-02-20 → 2026-08 (همان دادهٔ S344) | 94 | **63.83%** | +1,995 |
| **بکر** 2011-01-03 → 2020-02-20 (۹.۱ سال، هیچ‌کس ندیده) | 173 | **38.73%** | **−2,013** |

- بازتولید کامل: روی پنجرهٔ دیده‌شده، کارتِ منجمد دقیقاً همان چیزی را می‌دهد که S344 گزارش کرده بود (n=92→94، WR=64.1→63.8). یعنی کد، داده و feed سازگارند — تفاوت فقط از **افزودنِ ۹.۱ سالِ بکر** است.
- روی پنجرهٔ بکر، WR زیر نقطهٔ سربه‌سرِ هزینه (40.2%) است و lift تقریباً صفر ⇒ «اولین پولبکِ روزِ trend-from-open» در دههٔ 2011–2019 **هیچ اطلاعی** نداشت.
- **P3 پیش‌ثبت (پیش‌بینیِ متمایزکننده) رد شد:** اگر لبهٔ SHORT واقعی بود باید در بازارِ نزولیِ 2011–2015 حتی بهتر می‌بود؛ برعکس، همان‌جا بدترین بود. پس لبهٔ S344 نه «ساختارِ پرایس‌اکشن» بلکه **آرتیفکتِ نوسانِ درون‌روزیِ رژیمِ 2020+** بود (پنجرهٔ کوچکِ opening-range + پولبکِ سریع در بازاری با ATR ۱.۸× بزرگ‌تر).

## ۳) سه یافتهٔ علمی

1. **تنها ACCEPTِ «لایهٔ نو» در تحلیلِ MISSION_4 حالا سقوط کرد.** با این حکم، **هر** ACCEPTِ پایدارِ تمام‌بازهٔ پروژه از خانوادهٔ «احیای مولدِ اثبات‌شده» یا «شوک/تداومِ درشت‌مقیاس» است؛ هیچ لایهٔ نوی ریزمقیاس (≤M15) روی ۱۵.۶ سال زنده نمانده (هم‌راستا با S550-M15V، S570-M30، S610).
2. **الگوی تشخیصیِ E-16:** بازتولیدِ دقیقِ WR روی پنجرهٔ دیده‌شده + سقوط به زیرِ BE روی پنجرهٔ بکر = امضای آرتیفکتِ رژیم. این آزمونِ دو-پنجره‌ای ارزان است (یک اجرا) و باید پیش از هر «احیا» انجام شود — اگر برای S412 (S353-D1) انجام می‌دادم، آن شماره را نمی‌سوزاندم.
3. **⚠️ توصیهٔ رسمی (بدون اقدام — سایت دست‌نخورده طبق دستور):** کارتِ لایوِ S344-M15 SHORT فقط بر ۶.۵ سالِ دورهٔ گاوی استوار است و روی ۹.۱ سالِ مستقل **زیان‌ده** بود. تصمیم دربارهٔ وضعیتِ لایوِ آن با کاربر است.

## ۴) ممیزی همپوشانی
- S567 (ORB-Quiet، قلمرو S560): breakout خالص LONG بدون پولبک/فیلتر رژیم — مکانیزم متفاوت، هر دو REJECT، هم‌راستا: «ساختارِ صبحگاهیِ ریزمقیاس روی طلا تمام‌بازه زنده نیست».
- S426 (OpeningDrift H1، EARLY DEATH): همان پیام در مقیاسِ ساعتی.
- هیچ تداخلی با S500–S980 و S1xxx: فقط کارتِ خودِ S344 (قلمرو آزاد، هیچ پیش‌ثبتِ دیگری آن را نگرفته بود) داوری شد.

## ۵) پرهیز از ۸ اشتباه — شواهد
1. داده دیده‌شده: holdout_mask دقیقاً پنجرهٔ بکر (BUG-SPLITDIR آگاهانه معکوس شد و در پیش‌ثبت اعلام). 2. جست‌وجوی پنهان: صفر؛ k_sl فرمولی (220/26.56). 3. تعدد: n_trials=79 صادقانه. 4. بقای گزینشی: هر دو بازو گزارش شد؛ بازوی حاکم از پیش تعیین‌شده بود. 5. تک‌TF: آزمونِ تأییدیِ یک کارتِ لایو (هدف همین بود). 6. پارامترِ یکسانِ کور: بازوی A همین را سنجید و مرد؛ B هم مرد ⇒ مشکل هندسه نیست، سیگنال است. 7. اعداد رند: ارثی. 8. TP<SL: RR=1.545 ✓.

## ۶) وضعیت بلوک
S410 REJECT · S411 REJECT · S412 REJECT · S413 REJECT · **S414 REJECT (8.5)** · بعدی: S415.
*هیچ استقراری انجام نشده. سایت و local-mobile دست‌نخورده.*
