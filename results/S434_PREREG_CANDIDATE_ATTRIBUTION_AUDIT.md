# S434 — پیش‌ثبت: ممیزیِ انتسابِ نامزد (BUG-TABLEATTRIB)

| | |
|---|---|
| **شمارهٔ استراتژی** | S434 (بازهٔ من: S430–S449) |
| **مأموریت** | `MISSION_4_RESURRECTION_FILTERS.md` — احیای لایه‌های سوخته |
| **معیار** | RQS2 **v2.6** (موتور: `engine/rqs2.py`) |
| **نوع این گام** | ممیزیِ ابزارِ انتخابِ نامزد — **پیش‌نیازِ اجباریِ** احیای سوم |
| **وضعیت** | PRE-REGISTERED (قبل از هر بک‌تستی نوشته شد) |

---

## ۰. چرا این گام قبل از احیای سوم لازم شد

در S432 ابزاری ساختم (`tools/s432_resurrection_priority.py`) که ۵۸۷ جفتِ
(لایه × کارت) را در شش طبقه ریخت تا نامزدهای احیا را رتبه‌بندی کند. آن ابزار
دو احیای موفق (S431 و S432) را هدایت کرد، پس اعتمادِ من به آن بالا بود.

هنگام انتخابِ **نامزدِ سوم**، صدرنشینِ فهرست این ردیف بود:

```
S187_S73_M5_TimeframeUpgrade_NetProfit_250918_ACCEPTED.md | XAUUSD-M15
n=683 · z=3.60 · lift=+12.6pp · WR=41.29% · حاشیهٔ H9=+29.6 · failed=['H8'] · rank_score=2.197
```

این ردیف از هر جهت جذاب بود — بالاترین `rank_score` کلِ فهرست (بالاتر از
S312 که به S432 تبدیل شد و ۱.۸۴۹ داشت)، `n` بزرگ، `z` **بالای سدِ ۳.۰۹**،
و تنها **یک** دروازهٔ افتاده.

سپس سندِ منبع را باز کردم و ناسازگاری دیدم: سندِ S187 دربارهٔ
**«ارتقای تایم‌فریمِ S73 روی EURUSD»** است، اما ردیف روی **XAUUSD** بود.
یک لایهٔ EURUSD نمی‌تواند ۶۸۳ معاملهٔ XAUUSD داشته باشد.

---

## ۱. تشخیص: ردیف صدرنشین، لایهٔ S187 نبود

فایلِ مصنوعِ `results/_s187_m5_audit.json` را باز کردم. آن سند یک
**جدولِ ممیزی از نُه لایهٔ مختلف** است، نه یک لایهٔ واحد:

| لایهٔ واقعی | جهت | SL | TP | n روی M15 | WR | PF |
|---|---|---|---|---|---|---|
| **S139 Overnight** | Long | ۱۵۰ | ۵۰۰ | **۶۸۳** | **۴۱.۲۹٪** | ۱.۳۴۴ |
| S73 Session-Open | Long | ۱۲ | ۱۲ | ۵۹۹ | ۵۵.۲۶٪ | ۱.۳۷۴ |
| … (۷ لایهٔ دیگر) | | | | | | |

اثرِ انگشتِ `n=683` + `WR=41.29%` یگانه است و مطابقتِ آن قطعی است:

> **ردیفِ صدرنشینِ من در واقع «S139 Overnight» است، نه S187.**

ابزارِ من نامِ **فایلِ سند** را به‌عنوان هویتِ لایه برداشته بود.

## ۲. ریشهٔ فنی

منبعِ دادهٔ ابزار `results/_audit_rename/verdicts/*.json` است و هویت لایه از
اینجا می‌آید:

```python
layer = doc.get('layer') or os.path.basename(fp)   # ← نامِ فایل، نه نامِ لایه
cards = doc.get('cards') or []
for card in cards: ...                              # ← هر کارت، لایهٔ مستقلی است
```

و در آن json، فیلدِ `layer` **نامِ فایلِ md** است:

```
"layer": "S187_S73_M5_TimeframeUpgrade_NetProfit_250918_ACCEPTED.md"
```

هیچ فیلدی در هیچ کارتی هویتِ لایهٔ واقعی را نگه نمی‌دارد (نه `rule`، نه
`name`، نه `label`؛ فیلدِ `notes` فقط علتِ افتادنِ دروازه‌ها را دارد).
پس ابزارِ من **نمی‌توانست** درست نسبت دهد — این نقصِ منبعِ داده است که در
ابزارِ من به‌صورت ادعای نادرست ظاهر شد.

## ۳. دامنهٔ خطا — اندازه‌گیری، نه تخمین

معیارِ تشخیص: اگر یک فایلِ حکم، **دو یا چند کارت با نامِ کارتِ یکسان** دارد،
آن ردیف‌ها لزوماً لایه‌های *مختلف*‌اند که زیر یک نام جمع شده‌اند.

| سنجه | مقدار |
|---|---|
| کلِ ردیف‌های فهرستِ اولویت | ۵۸۷ |
| فایل‌های حکمِ چند-لایه‌ای | **۶۱** |
| ردیف‌های بدنامِ (mis-attributed) | **۴۱۹** |
| سهمِ آلوده | **۷۱.۴٪** |

بدترین موارد: `S374` (۱۹ کارت، ۵ نامِ کارت تکراری)،
`S221`/`S222a…e` (هر یک ۱۹ کارت زیر نامِ `XAUUSD-M5`)،
`S202` (۱۷ کارت زیر `XAUUSD-H1`)، `S208` (۱۵ کارت زیر `XAUUSD-H4`).

## ۴. چرا این «ریزه‌کاری» نیست — سه پیامدِ عملی

1. **انتخابِ نامزدِ اشتباه:** اگر ردیف صدرنشین را «S187» می‌گرفتم، برای
   احیا سراغِ سندِ S187 می‌رفتم و منطقِ **S73/EURUSD** را بازسازی می‌کردم،
   در حالی که آمارِ جذاب از **S139/XAUUSD** آمده بود. نتیجه: احیای لایه‌ای
   با آماری که هرگز به آن لایه تعلق نداشت.
2. **شمارشِ خواهرها (`n_siblings`) نادرست بود:** معیارِ POOL-READY من بر
   «چند کارتِ خواهر از همین لایه» تکیه دارد. وقتی ۱۹ لایهٔ مختلف یک نام
   می‌گیرند، شمارشِ خواهر متورم می‌شود — و این دقیقاً همان سنجه‌ای است که
   الگویِ موفقِ S431 (تجمیعِ چند-کارتی) را انتخاب می‌کند.
3. **آلودگیِ گزارش‌های قبلی من:** جدولِ شش‌طبقهٔ S432 که به کاربر گزارش دادم
   (۳۸ POOL-READY / ۲۶۴ DEAD-COST / …) با این برچسب‌های آلوده ساخته شده بود.
   **این را صادقانه ثبت می‌کنم**؛ در بخش ۶ می‌گویم چه چیزی از آن گزارش
   دست‌نخورده می‌مانَد و چه چیزی نمی‌مانَد.

## ۵. آیا احکامِ S431 و S432 آسیب دیده‌اند؟ — نه، و دلیلش

این تفکیک مهم است و آن را با دقت ثبت می‌کنم:

- ابزارِ اولویت‌بندی فقط **نامزد پیشنهاد می‌دهد**؛ هیچ نمرهٔ RQS2ای صادر
  نمی‌کند.
- برای S431 و S432 من پس از انتخابِ نامزد، **سندِ منبع را خواندم**، منطق را
  از خودِ سند بازسازی کردم، و روی دادهٔ خام با موتورِ v2.6 داوریِ تازه گرفتم.
- در هر دو مورد، لایهٔ انتخابی **تک-لایه‌ای** بود: `S312` (سندِ mid-month) و
  `S351/S355` (سندِ LPSB) — و انتسابشان با خواندنِ سند تأیید شد.

بنابراین: **احکامِ ACCEPT برای S431 (۹۳.۹) و S432 (۸۴.۷) پابرجاست.** آنچه
آلوده بود، *فهرستِ پیشنهادها* بود، نه *داوری‌ها*.

## ۶. تعهدِ پیش‌ثبت‌شدهٔ این گام (قبل از دیدنِ هر نتیجه‌ای)

1. ابزار را اصلاح می‌کنم تا هویتِ لایه را از **جدول‌های داخلِ سند** استخراج
   کند، نه از نامِ فایل؛ و هر ردیفی که هویتش قابلِ اثبات نیست را با برچسبِ
   `UNATTRIBUTED` جدا می‌کنم — **حذف نمی‌کنم و حدس هم نمی‌زنم**.
2. فهرست را بازتولید می‌کنم و **تفاوتِ آن با فهرستِ S432 را گزارش می‌دهم**،
   حتی اگر آن تفاوت به زیانِ روایتِ قبلیِ من باشد.
3. نامزدِ سومِ احیا را **فقط** از ردیف‌هایی انتخاب می‌کنم که هویتشان با
   خواندنِ سندِ منبع تأیید شده باشد.
4. اگر نامزدِ اصلاح‌شده همان `S139 Overnight` بود، آن را می‌پذیرم؛ و اگر
   ردیفِ دیگری بالا آمد، هم آن را می‌پذیرم. **ترتیبِ خروجی را برای رسیدن به
   نامزدِ دلخواه دستکاری نمی‌کنم** (این همان خطایی است که در S432 با
   BUG-SCALEBIAS تجربه کردم).

## ۷. نسبتِ این گام با اشتباهات رایج

- **اشتباه ۷ (شبکهٔ محدودِ اعداد):** این باگ نمونهٔ دیگری از همان خانواده است
  که در S432 با BUG-SCALEBIAS دیدم — سنجه‌ای که به‌نظر معنادار بود، در واقع
  چیزِ دیگری را می‌سنجید (آنجا مقیاسِ تایم‌فریم، اینجا نامِ فایل).
- **اشتباه ۳ (اعلامِ سریعِ سوخته‌بودن):** طبقهٔ `DEAD-COST` با ۲۶۴ عضو،
  حکمِ «غیرقابلِ احیا» می‌داد. با برچسب‌های آلوده، ممکن است لایه‌ای زنده در
  آن دفن شده باشد. بازتولیدِ فهرست این خطر را رفع می‌کند.

---

# بخشِ دوم — نامزدِ سوم شناسایی شد: **S139 «رانشِ شبانه»**

## ۸. هویتِ لایهٔ واقعی

پس از اصلاحِ انتساب، ردیفِ صدرنشین به لایهٔ زیر تعلق دارد:

| | |
|---|---|
| **لایه** | **S139 — Gold Overnight Drift** (زمان‌محورِ خالص، LONG) |
| منبعِ اصلی | `results/GoldOvernightDrift_NetProfit_171738.md` |
| پشتوانهٔ آکادمیک | Lou, Polk & Skouras (2019) *J. Financial Economics*؛ Cooper-Cliff-Gulen (2008) |
| قانون | ورودِ LONG در open کندلِ بعد از بسته‌شدنِ کندلی با ساعتِ **۲۲ یا ۲۳ UTC** |
| هندسه | SL=۱۵۰ pip · TP=۵۰۰ pip · max_hold=۹۶ کندل (۲۴h روی M15) |
| اندیکاتور | **هیچ** — صرفاً زمان |
| آمارِ M15 (از ممیزیِ S187) | n=۶۸۳ · WR=۴۱.۲۹٪ · PF=۱.۳۴۴ · z=۳.۶۰ · حاشیهٔ H9=+۲۹.۶ |
| دروازهٔ افتاده | **تنها `H8`** (ریسکِ دنباله: maxDD / MCL / recovery) |

تلاشِ احیای قبلی: `S307_S139OvernightDriftRevival…INCOMPLETE.md` — **حکم: REJECTED**.

## ۹. 🔬 کشفِ E-15 — تلاشِ احیای قبلی، لایه را در **جهتِ اشتباه** هدایت کرد

سندِ S307 چهار خانوادهٔ بهبود را جارو کرد و «بهترین حالتِ ممکن» را این خواند:

> `h23 + dow{0,2,4} + SL200/TP200 ⇒ WR=۵۶.۷٪، PF=۱.۴۵`

و دلیلِ ردِ نهایی را چنین نوشت:

> «رساندنِ WR به ۶۰٪ فقط با نمونهٔ کوچک ممکن شد … با قیدِ **WR≥۶۰٪** لبهٔ
> قابلِ‌اتکایی باقی نمی‌ماند.»

**دو ایرادِ بنیادی در این استدلال هست:**

### ایرادِ اول — قیدِ WR≥۶۰٪ در v2.6 به این لایه اعمال نمی‌شود

در `engine/rqs2.py` خطِ ۷۷:

```python
WR_FLOOR_NO_RR   = 60.0     # فقط وقتی `tp_pip` نامعلوم است ⇒ H2 نمی‌تواند بسنجد
```

و خطِ ۹۷۸ آن را تنها در شاخهٔ `tp_pip is None` صدا می‌زند. وقتی TP معلوم است
— که برای S139 هست (TP=۵۰۰) — معیارِ حاکم `breakeven_wr_cost` است:

```
WR_سربه‌سر = (SL + c) / (SL + TP) × 100
```

پس سدِ ۶۰٪ یک **فالبکِ ارثی** است، نه قانونِ این لایه. S307 لایه را با
معیاری کُشت که موتور برای آن لایه اصلاً به کار نمی‌بندد.

### ایرادِ دوم — «بهبودِ» S307 لبه را سه‌برابر **ضعیف‌تر** کرد

| هندسه | WR واقعی | WR سربه‌سر (c=۳.۳) | **حاشیهٔ لبه** |
|---|---|---|---|
| **S139 اصلی** SL۱۵۰/TP۵۰۰ | ۴۱.۲۹٪ | **۲۳.۵۸٪** | **+۱۷.۷۱ pp** 🟢 |
| «بهترین» S307 SL۲۰۰/TP۲۰۰ | ۵۶.۷۰٪ | ۵۰.۸۲٪ | **+۵.۸۸ pp** 🔴 |
| SL۱۲۰/TP۱۸۰ (جاروی S307) | — | ۴۱.۱۰٪ | — |

برای بالا بردنِ WR، S307 هندسه را از `TP = 3.33×SL` به `TP = SL` برد. WR
ظاهراً از ۴۱٪ به ۵۷٪ **بهتر** شد، اما نقطهٔ سربه‌سر از ۲۳.۶٪ به ۵۰.۸٪ **بالا
رفت** و حاشیهٔ واقعیِ لبه از +۱۷.۷ pp به +۵.۹ pp **سقوط کرد**.

این مصداقِ نقضِ **«قانونِ حفظِ بودجه»** است:

> «گاهی باید به‌جای اینکه با فیلتر WR را بالا ببرید (که معامله را می‌کشد)،
> نقطهٔ سربه‌سر را پایین بیاورید (که معامله را نمی‌کشد). یعنی TP بزرگ‌تر از SL.»

S307 دقیقاً برعکس عمل کرد: **سربه‌سر را بالا برد تا WR زیبا شود.** و همین
توضیح می‌دهد چرا هر چهار خانوادهٔ بهبودش به دیوار خورد — او در فضایی
جستجو می‌کرد که بودجهٔ معامله را از قبل خرج کرده بود.

## ۱۰. فرضیهٔ من برای S434 (پیش‌ثبت‌شده، قبل از هر اجرایی)

**تشخیص:** قاتلِ این لایه `H8` است، نه لبه. یعنی مسئله **ریسکِ دنباله** است:
`maxdd_pct ≤ 8.0` و `mcl ≤ mcl_allowed` و `recovery ≥ 3.0`. سندِ اصلیِ S139
خودش `MaxDD = −۱۵.۹٪` را گزارش می‌کند — تقریباً **دو برابرِ سدِ ۸٪**.

**فرضیه:** لبهٔ زمان‌محورِ شبانه واقعی است (z=۳.۶۰ بالای سدِ ۳.۰۹، و
حاشیهٔ سربه‌سرِ +۱۷.۷ pp)، اما در شکلِ فعلی **بی‌انتخاب** است: هر شب سیگنال
می‌دهد (n>۱۳۰۰ خام) و شب‌های زیان‌ده را هم می‌گیرد، پس افت انباشته می‌شود.
بنابراین بهبودِ درست، **فیلترِ کیفیت** است که هندسهٔ `TP > SL` را **دست‌نخورده
نگه دارد** — نه تغییرِ هندسه.

**تعهدِ صریح:** هندسهٔ `TP > SL` را برای بالا بردنِ WR **تغییر نمی‌دهم**.
اگر ناچار به تغییرِ هندسه شدم، در همان گام گزارش می‌کنم که حاشیهٔ سربه‌سر
چه شد، تا خطای S307 تکرارِ خاموش نشود.

**قانونِ MTF:** طبق قانونِ اول، از `XAUUSD-M1` شروع می‌کنم و هر تایم‌فریم را
مستقل می‌سنجم و مرحله‌به‌مرحله چک‌پوینت می‌کنم.

---

# بخشِ سوم — تشخیصِ اندازه‌گیری‌شده (گام‌های ۵ تا ۷)

## ۱۱. کالبدشکافیِ H8 — قاتل شناسایی شد

`H8` عطفِ سه‌جزئی است، پس «H8 افتاد» بی‌جهت است. تفکیکِ اجزا روی پایهٔ
دست‌نخوردهٔ S139 (SL=۱۵۰ · TP=۵۰۰ · هولد=۲۴h):

| TF | n | WR | **حاشیهٔ لبه** | PF | **maxDD** (سد ۸٪) | MCL | recovery (سد ۳) |
|---|---|---|---|---|---|---|---|
| M5 | ۷۱۳ | ۳۸.۹۹٪ | **+۱۵.۴۱ pp** | ۱.۲۳۱ | **۲۱.۶۹٪** ❌ | ۱۴/۲۰ ✅ | ۲.۵۶ ❌ |
| M15 | ۱۳۴۰ | ۴۵.۰۰٪ | **+۲۱.۴۲ pp** | ۱.۲۹۸ | **۱۵.۵۸٪** ❌ | ۱۴/۱۸ ✅ | ۴.۸۷ ✅ |
| M30 | ۲۹۱۱ | ۴۶.۳۸٪ | **+۲۲.۸۰ pp** | ۱.۱۶۱ | **۱۴.۷۲٪** ❌ | ۱۷/۱۹ ✅ | ۴.۳۰ ✅ |
| H1 | ۲۹۲۶ | ۴۶.۹۹٪ | **+۲۳.۴۱ pp** | ۱.۱۵۴ | **۲۳.۹۵٪** ❌ | ۱۲/۱۸ ✅ | ۲.۳۲ ❌ |

**سه نتیجه:**
1. **لبه واقعی و بزرگ است** — حاشیهٔ سربه‌سر در هر چهار کارت **+۱۵ تا +۲۳ pp**.
   این تأییدِ مستقلِ E-15 است: هندسهٔ `TP=3.33×SL` دقیقاً همان کاری را می‌کند
   که «قانونِ حفظِ بودجه» پیش‌بینی می‌کند.
2. **تنها جزءِ شکسته `maxDD` است** — MCL در هر چهار کارت با فاصله پاس است.
   `recovery` نیز جزءِ *مشتق* است (سودِ خالص ÷ افتِ دلاری)، پس با کوچک شدنِ
   افت خودبه‌خود درست می‌شود؛ مسئلهٔ مستقلی نیست.
3. **دروازهٔ دومی هم هست که فهرستِ آلوده پنهانش کرده بود: `H1` روی PF.**
   و حاشیهٔ M15 حیرت‌آور نازک است: **۱.۲۹۸ در برابرِ سدِ ۱.۳ ⇒ کمبودِ ۰.۰۰۲**.

### مرزِ ساختاریِ کشف‌شده (نه فرض‌شده)
H4/D1/W1 **صفر سیگنال** می‌دهند، چون کندل‌های H4 روی ساعاتِ ۰/۴/۸/۱۲/۱۶/۲۰
باز می‌شوند و D1/W1 روی نیمه‌شب؛ پس هیچ کندلی ساعتِ ۲۲ یا ۲۳ ندارد. این لایه
**ذاتاً** روی M5…H1 زندگی می‌کند و قانونِ MTF با آزمودنِ همین چهار کارت ادا
می‌شود، نه با اجبارِ سه کارتِ بی‌معنا.

## ۱۲. ساختارِ افت — کدام فرضیه برنده شد

| فرضیه | سنجهٔ تفکیک | نتیجه |
|---|---|---|
| **C** — نویزِ پراکنده | سهمِ بدترین ۵٪ از کلِ زیان | **رد**: ۸.۸٪ / ۱۰.۷٪ / ۱۳.۴٪ / ۱۳.۷٪ ⇒ زیان پخش است |
| **B** — رویدادِ تقویمیِ منفرد | طولِ بازهٔ بدترین افت | **رد**: ۶۸d / ۲۲۹d / ۲۵۳۴d / ۱۱۵۵d ⇒ شوکِ کوتاه نیست |
| **A** — رژیمِ نزولیِ کلان | Δانتظار بین دو رژیم | ✅ **تأیید** |

بازهٔ بدترین افتِ M15 (`۲۰۲۲-۰۳-۰۳ → ۲۰۲۲-۱۰-۱۹`) منطبق است بر **چرخهٔ
افزایشِ نرخِ بهرهٔ فدرال‌رزرو ۲۰۲۲** که طلا را از ~۲۰۷۰ به ~۱۶۲۰ برد. و بازهٔ
M30/H1 هر دو از `۲۰۱۱-۰۹-۰۷` شروع می‌شوند — **قلهٔ تاریخیِ طلا** و آغازِ بازارِ
خرسیِ چندساله. افتِ طولانی که بر یک رژیمِ کلانِ قابلِ‌شناسایی منطبق است،
مصداقِ فرضیهٔ A است نه B.

### اندازهٔ اثر (تفکیک بر حسبِ رژیم در لحظهٔ ورود)

| TF | انتظار در رژیمِ سالم | انتظار در رژیمِ نامساعد | **افتِ بدترین: سالم → نامساعد** |
|---|---|---|---|
| M5 | +۱۴۴.۳۳ pip (n=۴۰) | +۲۱.۱۷ (n=۶۷۳) | **−۳۶۱.۹ ← −۳۵۸۴.۵** (۱۰×) |
| M15 | +۶۱.۵۰ (n=۱۰۰) | +۱۸.۶۱ (n=۱۲۴۰) | −۱۰۵۲.۹ ← −۳۹۶۹.۷ |
| M30 | +۴۴.۹۶ (n=۲۷۵) | +۳.۶۸ (n=۲۶۳۶) | −۱۸۲۰.۳ ← −۷۰۹۷.۶ |
| H1 | +۴۰.۷۴ (n=۳۵۲) | +۲.۹۵ (n=۲۵۷۴) | −۲۲۱۰.۵ ← −۷۰۳۴.۵ |

**اثر در هر سه تعریفِ مستقلِ رژیم (MA / مومنتومِ بی‌اندیکاتور / عمقِ ATR-نرمال)
و در هر پنج پنجرهٔ غیرِرند (۷/۱۳/۲۳/۴۱/۶۷ روز) ظاهر می‌شود، و ترتیبِ اندازهٔ
اثر در همهٔ کارت‌ها یکنوا در پنجره است.** پس این آرتیفکتِ یک فرمول‌بندیِ خاص
یا یک عددِ جادویی نیست — آزمونِ ضدِ اشتباهِ ۷ پاس شد.

**هشدارِ صادقانه:** قوی‌ترین تعریفِ منفرد (`peak_67d`) روی M15 فقط ۱۰۰ از
۱۳۴۰ معامله و روی M5 فقط ۳۷ از ۷۱۳ را نگه می‌دارد — یعنی >۹۰٪ نمونه را
می‌کُشد و خطرِ **معاوضهٔ شکستِ H8 با شکستِ H3 (توان)** را می‌سازد. `ma_13d`
با نگه‌داشتنِ ۷۲۹ از ۱۳۴۰ و شکافِ همچنان بزرگِ ۲۶.۳۲ pip، نقطهٔ شروعِ
متعادل‌تری است. **هدفِ گامِ بعد بیشینه‌کردنِ Δ نیست، بهینه‌کردنِ همین معاوضه است.**

## ۱۳. 🔬 کشفِ E-16 — بایاسِ دورهٔ تاریخی: چرا Δها بین تایم‌فریم‌ها قابلِ مقایسه نیستند

بازهٔ زمانیِ فایل‌های داده **یکسان نیست**:

| TF | بازه | طول | رژیمِ غالبِ پوشش‌داده‌شده |
|---|---|---|---|
| M5 | ۲۰۲۳-۰۹-۱۸ → ۲۰۲۶-۰۷-۱۶ | **۲.۸ سال** | تقریباً تماماً **گاوی** (طلا ~۱۹۰۰ → ۳۰۰۰+) |
| M15 | ۲۰۲۰-۰۲-۲۰ → ۲۰۲۶-۰۷-۰۷ | ۶.۴ سال | گاوی + خرسیِ ۲۰۲۲ |
| M30 | ۲۰۱۱-۰۱-۰۳ → ۲۰۲۶-۰۷-۱۶ | **۱۵.۵ سال** | خرسیِ ۲۰۱۱–۱۸ + گاوی |
| H1 | ۲۰۱۱-۰۱-۰۳ → ۲۰۲۶-۰۷-۱۶ | **۱۵.۵ سال** | خرسیِ ۲۰۱۱–۱۸ + گاوی |

**پس مقایسهٔ «Δ=۱۱۵.۹ روی M5» با «Δ=۳۷.۸ روی H1» نامعتبر است**: کارتِ M5
اصلاً یک بازارِ خرسیِ ممتد را **ندیده** است، پس رژیمِ «نامساعد»ش تنها
اصلاح‌های کوتاه است، نه یک روندِ نزولیِ چندساله.

این هم‌خانوادهٔ **BUG-SCALEBIAS** (کشفِ S432) است، این بار در لباسِ **بایاسِ
دورهٔ تاریخی** به‌جای بایاسِ مقیاسِ تایم‌فریم. درسِ مشترک: هر سنجه‌ای که
بین کارت‌ها مقایسه می‌کنم، باید نسبت به آن چیزی که کارت‌ها در آن **تفاوتِ
ساختاری** دارند نرمال شود.

**پیامدِ مثبت و مهم:** M30 و H1 با ۱۵.۵ سال داده و پوشش کاملِ یک بازارِ خرسیِ
هفت‌ساله، **سخت‌ترین آزمونِ** این فیلترند. اگر فیلترِ رژیم آنجا H8 را پاس کند،
ادعای «این فیلتر رژیم را واقعاً می‌فهمد» پشتوانه دارد. بنابراین **M30/H1 را
کارتِ داورِ اصلی می‌گیرم، نه M5** — برخلافِ چیزی که Δهای بزرگ وسوسه می‌کنند.
