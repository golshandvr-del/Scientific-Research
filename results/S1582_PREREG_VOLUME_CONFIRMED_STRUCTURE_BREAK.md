# S1582 — پیش‌ثبت: شکستِ ساختاریِ داو (S758) × تأییدِ حجمِ هم‌اسلات (S589) — آزمون سوم قانون «سطح»

**تاریخ:** 2026-09-10 · **دانشمند:** بلوک S1580–S1589، شمارهٔ ۳ از ۱۰ · **مسیر:** ۱ (بهبود لایهٔ سالم با گیت متعامد)
**دارایی:** XAUUSD · **داده:** `data/mt5_full` (لودر S758: H4 از H1 رسمی resample، assert `mt5_full`) · **فقط اکتشاف — هیچ تغییری در سایت.**
**این سند قبل از هر عدد کامیت می‌شود.**

## ۱) فرضیه و جایگاه در نقشه

قانون S1580 (تأییدشده در S1581): *گیت حجم هم‌اسلات روی رویدادهای «سطح» متعامد است.* دو شاهد تاکنون: fresh-high 90 (S589 ✅) و fresh-floor 90 (S1581 ✅) — هر دو **رکوردهای rolling ثابت**. S758/S759 رویداد سطحیِ **نوع سوم** است: شکستِ `close` از سقفِ میانیِ ساختارِ داو (HL → break of hh) — سطحی که از پیوت‌های فراکتال ساخته می‌شود، نه از پنجرهٔ ثابت. اگر قانون واقعاً به «سطح» تعلق دارد و نه به «rolling-90»، حجم باید اینجا هم توان بیفزاید.

S759 (ACCEPT 85.2، H4) همان رویداد را با گیت **ρ≥0.618** (شکل کندل) خالص کرد. S589 نشان داد حجم و ρ متعامدند (61٪ اشتراک). پس آزمون این است: **S758-خام × RVOL** (نه روی S759، تا با ρ مخدوش نشود) — بازوی ρ فقط برای مقایسهٔ گزارشی.

## ۲) بکارت قلمرو (پس از fetch `21cc5900`)
`grep -iE 'volume|حجم|rvol'` روی S758/S759: **صفر**. S803 (لیو هویی) RVOL_slot را روی squeeze (رویداد دامنه) گذاشت → ACCEPT-REDUNDANT — **پیش‌بینی قانون S1580 توسط دانشمند مستقل تأیید شد** (شاهد سوم برای «زائد روی دامنه»). لایه‌های S758/S759/S803 دست‌نخورده می‌مانند.

## ۳) قاعدهٔ فریز (صفر پارامتر آزاد — همه ارثی)
| جزء | مقدار | منبع |
|---|---|---|
| ساختار | پیوت فراکتال نیم‌پهنای **L=5** (برندهٔ کشفِ S759 در `results/s759/H4.json`؛ جست‌وجو نمی‌شود)؛ دو پیوت‌کف آخر `l₂>l₁`؛ `hh` = بالاترین پیوت‌سقف تأییدشده بین آن‌ها؛ کیفیت پا ≥1×ATR89 | S758/S759 |
| رویداد | `close[t] > hh` و `close[t−1] ≤ hh` (لبهٔ اول) و `close[t] > l₂` | S758 |
| **گیت حجم** | `RVOL_slot[t] = volume[t]/median(volume of prev 30 same-hour bars, shift(1), min_periods 20) ≥ 1.0` | S589 |
| جهت | LONG-only | S759/S522 |
| براکت | SL=1.45×ATR89 (شناور)، TP=1.618×SL، max_hold=55، allow_overlap=False، 3.3 pip | S758 |
| نول | بی‌قیدِ همان‌براکت لانگ روی استخر کل داده + جایگشت K=500 (S8.build_null) | S758/S759-A1 |
| داوری | معاملات کل داده + `split_bar = 0.50·len` (رویهٔ S759-Addendum-1 = استاندارد S965/S770) | S759-A1 |

هارنس = کپیِ عینیِ `strategies/s759_addendum1_full.py`؛ تنها تغییر: `informed_long` → سیگنال S758 خام × RVOL. بازوها: `gated` (داوری)، `ungated` (S758 خام، P1)، `counter` (RVOL<1، P2)، `rho` (S759 = S758×ρ، مقایسهٔ گزارشی).

## ۴) کارت‌ها و شمارش
**اصلی:** H4 (خانهٔ S759). **ثانوی:** H6، H8. **n_trials صادقانه = 3** (۳ کارت؛ فقط gated داوری). تنش 50 روی هر ACCEPT. SEED=1582.

## ۵) پیش‌بینی‌ها و ابطال‌گرها (قبل از عدد)
- **P1:** H4 gated lift و z > ungated (S758 خام روی همین داده). ✗ → گیت فقط n برید.
- **P2:** counter < gated. ✗ → **قانون S1580 محدود می‌شود** به «رکوردهای rolling»، نه هر سطح.
- **P3:** نرخ عبور < 75٪. پیش‌بینی 62–72٪ (شکست ساختاری با کندلِ معمولی رخ می‌دهد).
- **P4 تعامد با ρ:** اشتراک ورودیِ gated با بازوی ρ (S759) باید < 80٪ (S589: 61٪).
- پیش‌بینی شخصی H4: n≈200–240 (S758 خام n بیشتر از S759)، WR 50–54، lift +10..+14، z 2.9–3.6. prior ACCEPT ≈ ۳۵٪.
- ACCEPT با P3 پاس → توقف و گزارش. غیر از آن → گزارش خلاصه، S1583.

## ۶) پرهیز از ۸ خطای رایج (تعهد پیشینی)
1. Look-ahead: پیوت‌ها با تأیید علّی در i+5 (S758)، RVOL shift(1) درون گروه ساعت، ورود open بعدی. 2. چندگانگی: L=5 و همهٔ هندسه ارثی از آرتیفکت `results/s759/H4.json` (BUG-GEOMDRIFT)؛ n_trials=3. 3. نال: S8.build_null کل داده. 4. Holdout: split_bar=0.50 (موتور H7). 5. هزینه: 3.3 pip، TP>SL. 6. داده: assert `mt5_full`. 7. حکم: فقط `compute_rqs2`. 8. گزارش هر کارت/بازو.

*— بلوک دوم، موج سوم: اگر «سطح» یعنی سطح — و نه فقط پنجرهٔ ۹۰ — این تپه باید بالای آب بماند.*
