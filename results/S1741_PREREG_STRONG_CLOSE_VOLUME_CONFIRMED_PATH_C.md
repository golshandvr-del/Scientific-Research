# S1741 — پیش‌ثبت: بستهٔ قوی × رانش دوگانه × تأیید حجم هم‌اسلات (Strong-Close Dual-Drift × RVOL_slot) — Path C

**نویسنده**: عمر خیام نیشابوری (بلوک دوم S1740–S1749 — شمارهٔ ۲ از ۱۰)
**تاریخ پیش‌ثبت**: 2026-09-04 — **قبل از هر خط کد S1741 کامیت می‌شود.**
**دارایی**: XAUUSD · **داده**: فقط `data/mt5_full` (کد src را enforce می‌کند) · **فقط اکتشاف — هیچ تغییری در سایت.**

## ۰) مالکیت
S1741 آزاد است (`ls results/ | grep S1741` خالی). هیچ فایل دیگران لمس نمی‌شود؛ `rvol_slot` از `tools/s589_volume_fresh_high_runner.py` عیناً import می‌شود.

## ۱) درس‌های بازبینی گیت‌هاب (همین ساعت — ۱۰ ACCEPT جدید مطالعه شد)
- **S589 (88)، S1581 (88)، S1580 (81-REDUNDANT)**: گیت `RVOL_slot ≥ 1.0` (حجم / میانهٔ ۳۰ کندل قبلیِ همان ساعتِ روز) روی سه خانوادهٔ متفاوت lift را بالا برد و بازوی counter (RVOL<1) در هر سه **مرد** — کندل بی‌حجم = بی‌خبر.
- **S709 (کوشی) REJECT 14**: «رکورد رنج» به‌جای ضریب ATR شکست خورد (OOS WR 40٪) ⇒ ایدهٔ اولیهٔ من برای S1741 (رکورد رنج) را **رها می‌کنم** — دیتابیس گفت مرده است.
- **S1912 «ACCEPT-REDUNDANT»** (کینز): وقتی گیت دوم روی گیت اول lift اضافه نکند، ACCEPT «زائد» است. این معیار را وارد S1741 می‌کنم.
- **مشکل S1740**: گیت آرامش lift را +3.8pp بالا برد ولی n را نصف کرد ⇒ margin فقط +0.28σ. حجم گیتی **مستقل از σ** است (حجم بالا ≠ نوسان بالا؛ همبستگی متوسط) پس شاید lift را بی‌قتل n بالا ببرد.

## ۲) فرضیه
کندل انبساطی با بستهٔ قوی و **حجم بالاتر از میانهٔ همان ساعت** = جریان مطلع واقعی (کایل: حجم غیرمنتظره + اثر قیمتی دائمی)؛ همان کندل با حجم کم = حرکت نازک/نویز که برمی‌گردد. پیش‌بینی: گیت RVOL روی S749-H2 lift را بالا می‌برد؛ بازوی counter (RVOL<1) ضعیف‌تر است.

## ۳) قاعدهٔ منجمد — همه به ارث، صفر پارامتر آزاد

| جزء | مقدار | منبع |
|---|---|---|
| رخداد + رانش دوگانه + براکت + long_only | **عیناً S749/S748-H2** (`_scan_S748/H2.json`, assert) | S749 |
| **گیت حجم (جدید)** | `RVOL_slot[t] = volume[t] / median(volume[t−k], k∈۳۰ کندل قبلی همان hour-of-day, min_periods=20)`؛ ورود فقط اگر `≥ 1.0` | S589 `rvol_slot` import |
| گیت آرامش S1740 | **استفاده نمی‌شود** (این لایه یک گیت را جدا می‌سنجد؛ ترکیب = لایهٔ بعدی اگر هر دو مستقل مفید بودند) | |

## ۴) کارت‌ها و n_trials
- **کارت داوری: H2** (TF اثبات‌شده). **n_trials_official = 646** (640 خانواده + 3 کارت S1740 + 3 کارت این لایه). تنش 2000 و 5176.
- **تأییدی**: H4, H6 (همان قاعده، n_trials=646) — فقط P1/P3.
- هیچ TF/آستانهٔ دیگری. RVOL_THR=1.0 قفل (متعارف S589).

## ۵) ابطال‌گرها (پیش از هر عدد)
- **P1 (اطلاعات‌افزا)**: lift_vs_BE بازوی RVOL≥1 **باید > بی‌گیت S749** (+17.68 روی H2) باشد. شکست ⇒ حتی با ACCEPT، حکم «ACCEPT-REDUNDANT» و لایه *ناموفق* شمرده می‌شود (الگوی S1912).
- **P3 (counter مرده‌تر)**: بازوی RVOL<1 باید WR/lift پایین‌تر داشته باشد.
- **P5 (pass-rate)**: ۳۰–۸۵٪.
- **P6 (استقلال از σ — جدید)**: هم‌پوشانی رخدادهای RVOL≥1 با رخدادهای calm (S1740) گزارش می‌شود؛ اگر >۸۵٪ هم‌پوشان بودند، گیت‌ها یکی‌اند و ترکیب در لایهٔ بعد بی‌معناست.

## ۶) قاعدهٔ تصمیم پیش از اجرا
- H2 ACCEPT با 646 **و** P1 گذشت ⇒ ✅ موفق؛ توقف و گزارش.
- H2 ACCEPT ولی P1 شکست ⇒ ACCEPT-REDUNDANT ⇒ ناموفق؛ گزارش کوتاه و S1742.
- H2 غیر ACCEPT ⇒ ناموفق؛ S1742. هیچ آستانه/پنجرهٔ دیگری آزموده نمی‌شود.

## ۷) تعهدات
کامیت سند قبل از کد · کد قبل از اجرا · src enforce · جدول ۸ خطا · هیچ استقراری · هیچ تغییری در سایت.
