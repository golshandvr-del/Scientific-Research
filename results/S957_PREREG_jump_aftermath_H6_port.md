# S957 — پیش‌ثبت: «انتقالِ قاعدهٔ منجمدِ S950 به کارتِ XAUUSD-H6» (Jump-Aftermath Drift-Aligned — H6 Port)

**دانشمند:** Robert Merton (باند S950–S959 — شمارهٔ ۸) · **تاریخ:** 2026-09-10 · **فقط اکتشاف — سایت دست‌نخورده.**

## ۰. صداقتِ کامل دربارهٔ پیش‌آگاهی
این لایه «پیش‌بینیِ کور» نیست. در S955 و S956 بازوی کنترلِ `base` (قاعدهٔ S950 بی‌گیت) روی H6 دو بار عدد ~90 نشان داد (n_trials 34/35). پس من **عدد را دیده‌ام**. چرا با این حال شماره‌ای از باند خرج می‌شود؟
1. طبق قانونِ پروژه، حکمِ رسمی فقط از داوریِ **پیش‌ثبت‌شدهٔ** یک لایه با شمارهٔ خودش معتبر است؛ «مشاهدهٔ کنترل» حکم نیست.
2. در اسکنِ اصلیِ S950، H6 با عضوِ **دیگری** داوری شده بود (k=2.6, SL 1.272×ATR, RR 1.618 ⇒ REJECT 18.5، lift −2.6). قاعدهٔ نهاییِ S950 (SL=TP=2.058، drift-aligned) هرگز رسماً روی H6 داوری نشد.
3. شمارشِ چندگانگی این پیش‌آگاهی را جبران می‌کند: n_trials **= 36** (33 تجمعیِ S950 + S955 + S956 + این) — و **تنش تا n_trials=2000** گزارش می‌شود؛ اگر با 2000 هم ACCEPT بماند، پیش‌آگاهی نمی‌تواند حکم را توضیح دهد.
4. **آزمونِ ابطالِ تازه** که هنوز ندیده‌ام: تقسیمِ زمانی مستقل از موتور (نیمهٔ اول / دوم، هر ۸ سال) و بازوهای long/short جدا — پیش‌بینی: هر دو نیمه WR>BE و هر دو سمت lift>0. اگر یکی شکست ⇒ گزارش می‌شود.

## ۱. قاعدهٔ منجمد (صفر پارامتر — عینِ S950 روی H8، فقط TF عوض شده)
`|r_t| > 2.6·σ_BV(89)` علّی · long فقط اگر `close[t−1] − close[t−90] > 0` (short آینه) · SL=TP=2.058×ATR(89) (SMA-TR شیفت ۱) · max_hold=34 · بدونِ همپوشانی · spread 3.3pip.

## ۲. آزمون
کارتِ اصلی **H6** (یک آزمونِ منجمد). H12 و D1 گزارشی (همان قاعده). null کانونیِ جای‌گشتِ علامت K=2000، ۴ بذر. تنش n_trials ∈ {36, 200, 1000, 2000}.
ابطال‌گرها: **P1** هر دو نیمهٔ زمانی WR > BE_cost؛ **P2** هر دو سمت lift>0؛ **P3** همپوشانی با S919-H6 (ACCEPT 88.9، هم‌کارت) با ماژولِ منتشرشدهٔ S919 — اگر same-bar ≥۷۰٪ ⇒ REDUNDANT-of-S919.

## ۳. پیش‌بینیِ مکتوب
H6: ACCEPT ≈ 88–91، n≈300، WR≈62–65، lift +12…+13، z≈4.3–4.6. H12: REJECT ~21 (پایه در S955 دیده شد). D1: REJECT ~9. اگر تنش 2000 ACCEPT ماند و P1–P3 گذشتند ⇒ حکمِ رسمی ACCEPT با برچسبِ «پیش‌آگاه، تأییدی».
