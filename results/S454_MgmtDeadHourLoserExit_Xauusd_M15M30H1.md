# S454 — قاعدهٔ مدیریتی: خروج بازنده در ساعات مرده / پایان پنجرهٔ U (مواد خام M7+M6)

**وضعیت نهایی: REJECT (هر ۵ بیمار). دو بیمار به holdout رسیدند و آنجا شکست خوردند — شکست تعمیم صادقانه.**

- نوع: قاعدهٔ مدیریت معامله (state 4) — RQS2 مستقل ندارد؛ داوری با پروتکل مادر `S450_PREREG_MGMT_RULES_PROTOCOL.md` §4 و ضمیمهٔ پیش‌ثبت `results/S454_PREREG_ADDENDUM_DEADHOUR_EXIT.md` (قبل از هر اجرا).
- هارنس: `strategies/s454_deadhour_exit.py` · خروجی خام: `research/mgmt/S454_<patient>.json` (۵ فایل).
- توازی: هر ۵ بیمار صفر عدم‌تطابق (max|Δ|=0.000000000) — نتایج واقعی‌اند، نه باگ.

## ۱) قاعده (قفل‌شده در پیش‌ثبت)

در close هر کندل با `hour ∈ H`، اگر pnl شناور ≤ 0 → خروج در open کندل بعد؛ معاملهٔ در سود هرگز لمس نمی‌شود. سه واریانت: V23={23}، V567={5,6,7} (ساعات مردهٔ M7)، V17={17} (پایان پنجرهٔ U-شکل M6). SL/TP/mh حالت ۳ دست‌نخورده و مقدم.

## ۲) نتایج

| بیمار | V23 (H1) | V567 (H1) | V17 (H1) | holdout | حکم نهایی |
|---|---|---|---|---|---|
| S312_M30 | FAIL (سود ✓، ریسک ✗) | FAIL (ریسک ✓✓✓، سود ✗) | **PASS** | **FAIL** — سود 91.9%، maxDD بدتر | REJECT |
| S312_M15 | FAIL | FAIL | FAIL | باز نشد | REJECT |
| S312_H1 | FAIL | FAIL (ریسک ✓✓✓، سود ✗) | **PASS** (هر ۳ ریسک ✓) | **FAIL** — سود 88.2%، maxDD **+43% بدتر** | REJECT |
| S356_H1 | FAIL | FAIL | FAIL | باز نشد | REJECT |
| S344_M15 (SHORT) | FAIL | FAIL (ریسک ✓✓✓، سود ✗) | FAIL | باز نشد | REJECT |

اعداد کلیدی holdout (از JSON خام):
- S312_M30: avg پایه 668.07 → 614.22 (91.9%)؛ maxDD 8,956 → 9,106.
- S312_H1: avg پایه 820.16 → 723.00 (88.2%)؛ maxDD 9,536 → **13,695** (+43.6% بدتر!).

## ۳) تحلیل ریشه‌ای

1. **V17 در نیمهٔ اول قشنگ بود و در نیمهٔ دوم فروپاشید** — الگوی کلاسیک شکست تعمیم. در نیمهٔ اول بازندهٔ ساعت 17 واقعاً مرده بود؛ در نیمهٔ دوم (روند صعودی قوی‌تر طلا) بازندهٔ ساعت 17 فردا برمی‌گشت و TP می‌خورد — قاعده دقیقاً همان برگشت‌ها را برید و در S312_H1 حتی DD را عمیق‌تر کرد.
2. **V567 در همهٔ بیماران ریسک را عالی بهتر کرد ولی سود را همیشه بیش از بودجه خورد** — ساعات 5–7 برای لایه‌های سوئینگ «وسط راه» است، نه پایان راه؛ همان درس S450/S452.
3. **V23 تقریباً هیچ‌وقت فعال نشد** (1–13 معامله) — پوزیشن‌ها به‌ندرت در 23:00 منفی‌اند؛ قاعدهٔ بی‌اثر.
4. تفاوت با PASS S453: آنجا معیارِ «راکد پس از k کندل از ورود» بود (نسبی به عمر معامله)؛ اینجا «ساعت مطلق روز». یافته: **برای لایه‌های سوئینگ، سن معامله سیگنال مدیریتی بهتری از ساعت دیواری است.**

## ۴) ارزش holdout در معماری مسیر C

اولین بار در مأموریت، قاعده‌ای از کالیبراسیون گذشت و در holdout مرد. بدون مسیر C، V17 با «سود ✓ و هر ۳ ریسک ✓ روی نیمهٔ اول» به‌غلط ACCEPT می‌شد. **این دقیقاً کاری است که پیش‌ثبت باید بکند.**

## ۵) قضاوت per-TF و توصیه

- هیچ واریانتی برای هیچ TF/لایه‌ای تأیید نشد؛ **وارد trade_manager.ts نشود**.
- سایت/local-mobile دست‌نخورده تا اجازهٔ صریح کاربر.

## ۶) انطباق

پیش‌ثبت قبل از اجرا ✅ · توازی بیت‌به‌بیت ۵/۵ ✅ · holdout فقط برای برندهٔ کالیبراسیون ✅ · آستانه‌ها ثابت ✅ · per-TF ✅ · مرز مسئولیت state 3 ✅ · دلیل از دادهٔ زنده ✅ · عدم دخالت در فایل‌های دانشمندان موازی (S500+) ✅
