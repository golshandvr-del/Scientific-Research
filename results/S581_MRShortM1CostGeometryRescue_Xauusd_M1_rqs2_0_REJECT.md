# S581 — MRShort-M1 Cost-Geometry Rescue — XAUUSD-M1 — RQS2: 0 — REJECT (dead in exploration)

> **دههٔ من: S580–S589.** این مأموریت S581 است. هیچ شماره یا آرتیفکتِ دانشمندانِ موازی (S500–S579, S590–S980) لمس نشده.

## 0) شناسنامه
| فیلد | مقدار |
|---|---|
| Mission | S581 |
| Strategy | فروش fade فریز‌شدهٔ S334 (سیگنال+رژیم دست‌نخورده) + جست‌وجوی هندسهٔ خروجِ cost-aware |
| Pair / TF | XAUUSD-M1 (full official card) |
| Prereg | `results/S581_PREREG_M1_ANTIPERSISTENCE_COST_GEOMETRY.md` — کامیت 90f06ee1 **قبل از هر تست** |
| Path | C (multiplicity: search نیمهٔ اول ⇒ ONE confirmatory test نیمهٔ دوم) |
| Verdict | **REJECT — مرگِ صادقانه در مرحلهٔ اکتشاف؛ آزمونِ تأییدی طبق قانونِ پیش‌ثبت هرگز اجرا نشد** |
| RQS2 score | 0 (تعریف‌نشده — موتور فراخوانی نشد چون هیچ کاندیدی از فیلتر اکتشاف عبور نکرد؛ صفر = «مرده پیش از داوری») |
| Data | `data/mt5_full/XAUUSD_M1.csv` — 5,000,000 bars, span > 12y ✅ (گاردِ BUG-DATASETDRIFT) |
| Artifacts | `results/_s581_explore.json` (grid کامل ۳۶ نقطه)، `tools/s581_explore.py` (227ab0ed) |

## 1) انگیزهٔ مأموریت (ارث S580)
S580 در آرم C:M1 یک **سایهٔ آماری** پیدا کرد: z=3.01، n=1010، اما PF=0.607 — لبه از نظر آماری واقعی بود ولی اقتصادش زیر هزینهٔ 3.3-pip اسپرد له می‌شد. فرضیهٔ S581: شاید لبه زنده باشد و فقط **هندسهٔ خروج** (SL=110/TP=125 که برای M5 تنظیم شده بود) در مقیاس M1 غلط باشد. راه‌حل پیش‌ثبت‌شده: هندسهٔ ATR-محور مقیاس‌شده با نوسان بومی M1.

## 2) طرح پیش‌ثبت‌شده (قفل‌شده در 90f06ee1، قبل از دیدن حتی یک عدد)
- سیگنال و رژیم S334 **فریز**: z_win=34, z_thr=2.4, RSI(14)>70, bear candle, Hurst(64)<0.50, Kurt(20)<1.8 — از آرتیفکت `results/_s334_XAUUSD_M5.json` خوانده شد، هرگز بازنویسی نشد (گارد BUG-GEOMDRIFT).
- split = n_bars//2 = bar 2,500,000؛ نیمهٔ دوم **در لود دور ریخته شد** — نه فقط ماسک، بلکه فیزیکاً حذف.
- Grid دقیقاً ۳۶ نقطه: sl_mult ∈ {8, 11.17, 16} × medianATR14(نیمهٔ اول) ، rr ∈ {1.5, 2.0, 2.5, 3.0} ، mh ∈ {30, 60, 120}.
- قانونِ برنده (قفل‌شده): max expectancy_pip×√n بین نقاط n≥150 و net>0.
- قانونِ مرگ (قفل‌شده): «اگر هیچ نقطه‌ای net>0 نداشت ⇒ مأموریت **در مرحلهٔ اکتشاف صادقانه می‌میرد** و آزمونِ تأییدی هرگز اجرا نمی‌شود.»

## 3) نتیجهٔ اکتشاف — هر ۳۶ نقطه منفی
- medianATR14(نیمهٔ اول M1) = **2.802 pip** — اسپرد 3.3 pip یعنی هزینهٔ ورود > یک ATR کامل!
- n_signals نیمهٔ اول = 594؛ بهترین‌ها:

| sl_mult | rr | mh | SL | TP | n | WR% | exp_pip | net_pip |
|---|---|---|---|---|---|---|---|---|
| 16.0 | 3.0 | 120 | 44.8 | 134.4 | 556 | 47.84 | −1.566 | −870.6 |
| 16.0 | 2.5 | 120 | 44.8 | 112.0 | 556 | 47.84 | −1.688 | −938.7 |
| 16.0 | 2.0 | 120 | 44.8 | 89.6 | 556 | 47.84 | −1.786 | −992.9 |
| 16.0 | 1.5 | 120 | 44.8 | 67.2 | 556 | 47.84 | −2.143 | −1191.6 |
| 8.0 | 3.0 | 120 | 22.4 | 67.2 | 562 | 42.53 | −2.151 | −1208.6 |

- **هیچ** نقطه‌ای با n≥150 و net>0 وجود نداشت ⇒ برنده = null ⇒ آزمون تأییدی روی نیمهٔ دوم **اجرا نشد** (نیمهٔ دوم بکر ماند).

## 4) یافتهٔ علمی (ارزش مأموریت)
1. **لبهٔ fade در M1 ذاتاً کوتاه‌بُرد است.** شاهد قاطع: در sl_mult=16 / mh=120، نرخ برد در هر ۴ مقدار RR (TP از 67 تا 134 pip) دقیقاً **47.84٪ ثابت** است — یعنی TP تقریباً هیچ‌وقت لمس نمی‌شود و معاملات با timeout می‌میرند. قیمت پس از سیگنال fade در M1 به‌سادگی *سفر نمی‌کند*؛ بزرگ‌کردن RR فقط توهم است.
2. **مانع هزینه ساختاری است، نه هندسی:** اسپرد 3.3 pip > medianATR(M1)=2.8 pip. هر معاملهٔ M1 قبل از شروع، بیش از یک واحد نوسانِ طبیعی بدهکار است. لبه‌ای با میانگین حرکت ~چند pip هرگز از این چاه بیرون نمی‌آید.
3. **مرگ ابدی خانوادهٔ S334/fade روی دادهٔ کامل، در همهٔ هندسه‌ها:** S580 مسیر «دادهٔ بیشتر» را سوزاند (REGIME-ONLY اثبات شد)؛ S581 مسیر «هندسهٔ خروج» را سوزاند. طبق قانونِ مرگ ابدی، این خانواده دیگر با این دو مسیر احیا نمی‌شود. تنها مسیر فرضیِ باقی‌مانده: بازار/بروکری با هزینهٔ زیر ~1 pip — خارج از مشخصات حساب ما.

## 5) اثبات پرهیز از ۸ اشتباه رایج
1. **Look-ahead**: ورود در open کندل بعد (موتور)؛ رژیم causal از کش npz علّی. ✅
2. **Multiplicity ignored**: Path C پیش‌ثبت‌شده در کامیتِ جدا قبل از تست؛ grid دقیقاً ۳۶ نقطهٔ اعلام‌شده؛ قانون برنده قفل‌شده. ✅
3. **Survivorship / cherry-pick**: هر ۳۶ نقطه در JSON ذخیره و کامیت شد، نه فقط بهترین‌ها. ✅
4. **Cost blindness**: کل مأموریت *دربارهٔ* هزینه بود؛ اسپرد 3.3 pip از `engine/scalp_engine.py::ASSETS` خوانده شد (گارد BUG-PIPGUESS). ✅
5. **Regime overfit**: رژیم فریز از آرتیفکت S334؛ هیچ پارامتر رژیمی تیون نشد. ✅
6. **Small-n worship**: آستانهٔ n≥150 در قانون برنده؛ n واقعی 556–562. ✅
7. **Verdict by hand**: هیچ حکم دستی صادر نشد — چون طبق پیش‌ثبت، موتور اصلاً نباید فراخوانی می‌شد؛ score=0 به‌معنای «داوری‌نشده/مرده»، نه امتیاز جعلی. ✅
8. **Data drift**: provenance چاپ و ذخیره شد (`src` در JSON)؛ span>12y تأیید شد. ✅

## 6) وضعیت شکارِ بعدی
S582 (شمارهٔ بعدی دههٔ من) با کاندیدی تازه آغاز می‌شود — پیش‌ثبت اول، تست بعد.

*— نوشته در روحیهٔ Grothendieck: دریا بالا آمد، و این صخره حل نشد بلکه معلوم شد صخره نیست — تهِ اقیانوسِ هزینه است. ما نقشهٔ آن کف را برداشتیم و رفتیم.*
