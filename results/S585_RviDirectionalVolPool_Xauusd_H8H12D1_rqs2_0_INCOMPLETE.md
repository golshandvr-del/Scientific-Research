# S585 — RVI(14) Cross 70/30 Pool {H8, H12, D1} — حکم رسمی: INCOMPLETE (rqs2 = 0)

**دارایی:** XAUUSD · **کارت‌ها:** H8, H12, D1 · **دانشمند:** دههٔ S580–S589
**پیش‌ثبت:** `results/S585_PREREG_RVI_POOL_H8H12D1.md` (کامیت 39e29a3a — قبل از هر آزمون)
**ابزار:** `tools/s585_pool_adjudicate.py` · **آرشیو:** `results/_s585/POOL_stopped.json`
**داده:** mt5_full، هر سه کارت 2011-01-03 → 2026-08-07 (۱۵.۶ سال، assert>12y پاس)

---

## ۱. چه شد (خلاصهٔ صادقانه)

اجرای کامل‌دادهٔ سیگنالِ فریزِ S584 (عبور RVI(14) از 70/30، دوسویه) با
هندسهٔ مطلقِ فریز از آرتیفکتِ S584، به‌اضافهٔ نالِ اندازه‌گیری‌شدهٔ
دوسویه (K=500/سمت/کارت، seed قطعی):

| کارت | n | L/S | WR٪ | perm_ref٪ | lift (pp) |
|------|-----|--------|-------|-----------|-----------|
| H8   | 572 | 340/232 | 40.73 | 38.04 | **+2.70** |
| H12  | 386 | 217/169 | 45.85 | 37.91 | **+7.95** |
| D1   | 181 | 123/58  | 39.78 | 38.29 | **+1.49** |

شرط هم‌جهتی (هر سه lift>0) برقرار بود — پیش‌بینی P1 پیش‌ثبت تا نیمه درست
درآمد. اما `choose_homogeneous_subset` رسمی (حاشیهٔ +۱۵٪ z_proxy، درسِ
S351: بهبودِ ناچیزِ proxy ارزشِ ریسکِ فروپاشیِ ساختار نمونه را ندارد)
افزودن H8 و D1 به H12 را «رقیق‌کننده» تشخیص داد و هر دو را حذف کرد:

```
dropped: XAUUSD_D1  (dilutes pool — lowers z_proxy)
         XAUUSD_H8  (dilutes pool — lowers z_proxy)
used:    XAUUSD_H12 (تنها)
```

**قاعدهٔ توقفِ پیش‌ثبت §۵.۵ فعال شد:** کمتر از ۲ عضوِ معتبر ⇒ استخر
تشکیل نمی‌شود ⇒ `compute_rqs2` هرگز فراخوانی نشد ⇒ حکم رسمی
**INCOMPLETE با rqs2 = 0** (تقدیر S325/S330/S782: صفر = «قبل از داوری
مُرد»، نه سنجشِ ضعف). داوریِ تک‌کارتِ H12 مجاز نبود — پیش‌ثبت فقط آزمونِ
استخری را تعریف کرده بود و آزمونِ جایگزین = درجهٔ آزادی پسینی.

## ۲. باگِ شکارشده و ثبت‌شده: BUG-DIRSTR

اجرای اول نشان داد ستونِ `direction` خروجیِ `simulate_trades` رشتهٔ
`'long'/'short'` است نه ۱/−۱؛ مقایسهٔ `==1` بی‌صدا `nL=0` می‌داد و وزنِ
سمت‌ها در perm_ref را خراب می‌کرد (liftهای اجرای اول: +4.30/+10.08/+6.38 —
همه متورم). اصلاح شد (کامیت f128b6f8) و اجرای دوم با وزنِ صحیح همان
تصمیمِ توقف را داد — پس خطا بر نتیجهٔ نهایی بی‌اثر بود، اما به فهرست
گاردهای ارثی اضافه می‌شود: **BUG-DIRSTR — direction رشته است؛ هرگز با
عدد مقایسه نکن.**

## ۳. چرا این مرگِ آموزنده است (تشریحِ گروتندیکی)

فرضیهٔ «فلاتِ زیرآبیِ واحد» (S584 §5) ابطال شد — نه با شکستِ جهت، بلکه
با **ناهمگنیِ شدتِ لبه**: H12 با lift ~8pp تنها ایستاده؛ H8 و D1 با
lift ~1.5–2.7pp فقط سایه‌های رقیقِ همان پدیده‌اند (زیر آستانهٔ 4pp گیت
H3 حتی به‌تنهایی). دریا بالا آمد و معلوم شد سه جزیره نبودند — یک قله بود
و دو تپهٔ رسوبی. مقایسه با استخرهای موفق (S770/S602: اعضا lift همگن
۶–۹pp) الگو را تأیید می‌کند: **استخر فقط برندهٔ هم‌قد جمع می‌کند، نه
خویشاوندِ ضعیف.**

میراثِ عملی برای همهٔ دهگانه‌ها:
- سیگنالِ RVI(14)-cross روی XAUUSD واقعی است اما فقط در H12 غلظتِ کافی
  دارد (+7.95pp full-span) — و همان‌جا هم تک‌کارت زیر پیش‌شرط توان S782
  در اکتشاف مانده بود (S584). خانوادهٔ RVI-cross **بسته** شد.
- گرادیانِ مقیاس (S584: بهترین در وسطِ طیف H12، افت در دو سو) الگویی
  تکرارشونده است — قله‌های تک‌TF را استخر نجات نمی‌دهد.

## ۴. اثبات پرهیز از ۸ خطای رایج

1. **Look-ahead:** سیگنال از closeهای بسته؛ cross با roll(1). ✓
2. **انتخاب پسینی:** اعضا/هندسه/mh قبل از اجرا در پیش‌ثبت قفل (کامیت 39e29a3a). ✓
3. **نشت هندسه:** SL/TP فقط از `_s584_explore.json` خوانده شد (BUG-GEOMDRIFT). ✓
4. **چندگانگی:** n_trials=17 تجمعی؛ فقط یک آزمون تأییدی تعریف شده بود که
   طبق قاعدهٔ توقف هرگز اجرا نشد؛ هیچ آزمون جایگزینی اجرا نشد. ✓
5. **نال نادرست:** نال اندازه‌گیری‌شده در فضای هر کارت، دوسویه، K=500/سمت،
   seed قطعی هر کارت (BUG-NULLUNCOND/PERMK). ✓
6. **جهت‌گیری split:** به مرحلهٔ split نرسید (موتور فراخوانی نشد) — پروتکل
   پیش‌ثبت صدکِ زمان ورود بود (BUG-SPLITDIR). ✓
7. **دیتاست اشتباه:** mt5_full، assert بازه>12y، provenance ثبت شد. ✓
8. **حکم دستی:** هیچ حکمی از موتور جعل نشد — INCOMPLETE/rqs2=0 یعنی
   «موتور فراخوانی نشد»، عیناً تقدیر S325/S782. ✓

## ۵. حکم و بستن پرونده

**INCOMPLETE — rqs2 = 0.** خانوادهٔ RVI-cross (S584+S585) بسته.
n_trials مصرف‌شدهٔ خانواده: ۱۷. هیچ ادعای لبه‌ای باقی نیست.

*— دههٔ S580. صخره غرق شد؛ نقشه صادقانه‌تر شد. ادامه: S586.*
