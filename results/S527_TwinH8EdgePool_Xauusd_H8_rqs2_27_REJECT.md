# S527 — استخرِ دو لبهٔ H8 (S523+S526) — REJECT 27.5 (F2: رقیق‌سازی — و کشفِ هم‌رویدادیِ نهان)

**داور:** RQS2 v2.6 · **نویسنده:** گاوس (S520–S529) · **تاریخ:** 2026-09-02
**پیش‌ثبت:** `results/S527_PREREG_TwinH8EdgePool_Xauusd_H8.md` (کامیت 189dcaf4 — قبل از هر عدد)
**runner:** `tools/s527_twin_h8_pool.py` · **رکورد:** `results/_s527/POOL_verdict.json`
**داده:** `data/full/XAUUSD_H8.csv` ۱۵.۵۹y · n_trials=23789 (وراثت کامل WPR)

---

## ۱) حکم موتور — عیناً

```
S527-POOL | REJECT RQS2=27.5 | n=337 WR=51.93% PF=1.69 lift=+7.76pp z=2.9
گیت‌ها: H3✗ H5✗ — بقیه ✓
اعضا: s523 n=299 (lift=+12.44) · s526-dedup n=134 (lift عضو کامل +14.77)
سیگنال‌ها: s523=495، s526=361، same-bar=172 (۴۸٪ سیگنال‌های s526!)
FIFO: 433 → 337 · سهم پس-از-FIFO: s523=76.9% (نقض C5>50%)
F1: ورودی‌های s526 درون پنجره‌های باز s523: 38.8% (زیر آستانهٔ 40% ولی مرزی)
نول blend شرطی‌شده: unc=44.17 perm_mean=43.22
```

## ۲) داوری ابطال‌گرهای پیش‌ثبت‌شده

- **F2 (رقیق‌سازی): ✅ فعال** — lift استخر +7.76 < min(اعضا)−2pp = +10.44. dedupe+FIFO لبه را خورد. REJECT بدون نجات (طبق پیش‌ثبت هیچ جراحی پس‌ازآن مجاز نیست).
- **F1 (هم‌رویدادی):** 38.8% — زیر 40% اسمی، اما same-bar=48% سیگنال‌ها داستان واقعی را می‌گوید (پایین).
- **F3 (توان):** z=2.9 ≪ 4.61 — حتی بدون رقیق‌سازی هم نمی‌گذشت.
- **C5:** سهم 76.9% > 50% — استخر نامتوازن؛ مشروعیت S431 نداشت.
- **پیش‌بینی شخصی گاوس:** FIFO ~10–20% پیش‌بینی شد، واقعیت 48% same-bar + برش FIFO — **غلط از پایه**. خطا: استقلال مکانیزم‌ها را از استقلال *تعریف* استنتاج کردم، نه از اندازه‌گیری.

## ۳) کشف علمی — چرا دو «رویداد متفاوت» یکی بودند

**هم‌رویدادیِ نهان:** WPR(14)>−13 یعنی «close در ۱۳٪ بالایی دامنهٔ ۱۴ کندل اخیر».
سقفِ تازهٔ ۹۰-کندلی **به‌طور ضمنی** همین را ایجاب می‌کند (سقف ۹۰ ⊃ سقف ۱۴). پس
هر لبهٔ تازهٔ سقف ۹۰ تقریباً همیشه یا هم‌زمان با گذر WPR است (۴۸٪ same-bar) یا
درون پنجرهٔ باز آن (38.8%). **S526 تا حد زیادی زیرخانوادهٔ سخت‌گیرانه‌ترِ همان
رویداد قیمتی S523 است** — نسخهٔ خالص‌تر (lift بالاتر: +14.77 vs +12.44) اما نه مستقل.

**تعمیم قانون S520:** آنجا «چند TF از یک قانون = یک رویداد» بود؛ اینجا
«دو قانونِ سطح-قیمتی روی یک TF» هم می‌تواند یک رویداد باشد. استقلال را باید
**اندازه گرفت** (same-bar% + F1)، هرگز از تفاوتِ فرمول استنتاج نکرد.
استخرهای موفق (S431/S604) اعضایی با مکانیزم‌های *واقعاً* ناهم‌زمان داشتند.

## ۴) پیامد برای خانواده

- **S523-H8 همچنان POWER-LIMITED** — این مسیر نجات بسته شد؛ نجات واقعی فقط دادهٔ آینده (~۱۸ سال) یا لایهٔ هم‌خانوادهٔ ناهم‌زمان.
- **S526-H8 (ACCEPT 88.2) دست‌نخورده و مستقل معتبر می‌ماند** — این استخر آزمایشی جدا بود؛ حکم S526 متکی به آن نیست.
- برای استخرسازان آینده: قبل از پیش‌ثبت استخر، same-bar% اعضا را بسنجید؛ >20% = بی‌فایده.

## ۵) وضعیت

REJECT قطعی. سایت دست‌نخورده (دستور کاربر). دهک: S520✗ S521✗ S522✗ S523(PL) S524✗ S525✗ **S526✅** S527✗ → S528.
