# S1622 — PREREG — رکوردِ هوای پاک (Clear-Air Record): کفِ کندل بالای سقفِ close ۹۰-کندلی — XAUUSD H8/H6/H12 — LONG

**دانشمند:** اقلیدس · بلوک S1620–S1629 · شمارهٔ ۳/۱۰ · 2026-09-09 · **commit قبل از کد و هر عدد.** داور: `compute_rqs2` v2.6 دست‌نخورده. **فقط اکتشاف — سایت هرگز لمس نمی‌شود (دستور کاربر).**

## ۱) پیشینه و بداعت
- S1621 (من، ACCEPT 88.3) نشان داد اطلاعات خانوادهٔ لبهٔ تازه در **قاطعیت شکست** است (حاشیهٔ close بر ATR). سؤال هندسی بعدی: قاطع‌ترین شکل شکست — **کل کندل** (حتی low) بالای سقف ۹۰ close قبلی. این معادل «حاشیه ≥ دامنهٔ زیرِ close» است: شکست بدون هیچ بازگشت به زیر رکورد.
- تمایز از S1511 (نیوتن، ACCEPT): S1511 = `low > max(low[t−90..t−1])` (بالای نوارِ **کف‌ها**؛ شمارش ۳۵۸ در H8، ۲۷٪ هم‌بار با S526). S1622 = `low > max(close[t−90..t−1])` (بالای **رکورد close** — زیرمجموعهٔ سخت‌گیرانه‌ترِ لبهٔ تازهٔ S526، چون close>low>prevmax). رویداد S1622 ⊂ S526 است و ⊄ S1511. ممیزی: `grep -riE "low\[t\] > max\(close|low > prevmax|clear.air|هوای پاک|whole candle above"` ⇒ فقط S1511/S1581 (نوار کف‌ها) و اسناد خودم. بکر.
- تمایز از S1621: در S1621 حاشیه بر ATR نرمال شد؛ در S1622 حاشیه بر *دامنهٔ خود کندل* سنجیده می‌شود (`low ≥ prevmax` ⇔ `close − prevmax ≥ close − low`). دو نرمال‌سازی متفاوت؛ هم‌پوشانی با S1621 و S1520 و S1511 سنجیده و افشا می‌شود.

## ۲) فرضیه (H_clearair)
اگر low کندلِ رکوردزن نیز بالای سقف ۹۰ close قبلی باشد (کندل «در هوای پاک» بالای رکورد معلق است، صفر بازدید از زیر رکورد)، تقاضا بی‌رقیب بوده ⇒ ادامه قوی‌تر از رکوردهایی که کندل حین شکست زیر رکورد هم معامله شده. **پیش‌بینی:** lift_gated > +14.77 (S526-H8) و > lift_counter. ریسک اصلی: نرخ عبور پایین (گپ‌های H8 در طلا کم‌اند) ⇒ قحطی n.

## ۳) قانون منجمد (صفر پارامتر آزاد)
- پایه عیناً S526: `nh = close[t] > prevmax`, `prevmax = max(close[t−90..t−1])`, `~nh[t−1]`.
- **گیت:** `low[t] > prevmax` (بی‌آستانه — تعریف هندسی محض). counter: `low[t] ≤ prevmax`.
- LONG، ورود open کندل بعد، FIFO، بی‌هم‌پوشانی، اسپرد 3.3 — شبیه‌ساز S382 عیناً.
- براکت SL=1.5×median(ATR100)، TP=1.5×SL. کارت‌ها H8 (اصلی)، H6، H12. داده `data/mt5_full/` ۱۵.۶ سال (assert).
- Null شرطی فضای درفت>0، stride {1,3,7} + perm K=2000 seed 20260805 — هارنس S382 دست‌نخورده (رانر `s162x_record_geometry_runner.py`، فقط تابع گیت افزوده می‌شود).
- مسیر B؛ split_bar=0.70·n موتور برای H7.

## ۴) بودجه — صادق
۳ کارت × ۲ حالت = **n_trials = 6**. تنش 50 تشخیصی.

## ۵) گاردها/ابطال‌گرها (منجمد)
- **G1** نرخ عبور ∈ [15%, 75%] پیش از PnL؛ خارج ⇒ REJECT بدون نجات (اگر <15%: «هوای پاک روی H8 گپ‌محور و نادر است» به‌عنوان قانون منفی ثبت می‌شود).
- **P1a** lift_gated(H8) > +14.77 · **P1b** lift_gated > lift_counter — شکست هر یک ⇒ REJECT ولو ۱۱ گیت سبز.
- **P2 هم‌پوشانی:** اگر ACCEPT، اشتراک سیگنال با S1621/S1520/S1511 روی H8 گزارش می‌شود؛ Jaccard>0.6 با هر لایهٔ زنده ⇒ برچسب **ACCEPT-REDUNDANT** و توقف/گزارش «لایهٔ موفق» اعلام نمی‌شود (رویهٔ S1580/S1912).
- **F1** هیچ کارتی lift>+4 ⇒ محور مرده.
- حکم رسمی = موتور روی **H8 gated**.
- **پیش‌بینی شخصی:** نرخ عبور ~20–30٪ (رکوردهای گپ‌دار/شوکی)، n≈50–70 ⇒ به‌احتمال قوی POWER-LIMITED با lift بالا (+18..+22). احتمال ACCEPT ذهنی ۲۰٪؛ POWER-LIMITED ۵۰٪.

## ۶) قلمرو
فقط S1622؛ هیچ فایل دانشمند دیگری و هیچ فایل سایت لمس نمی‌شود.
