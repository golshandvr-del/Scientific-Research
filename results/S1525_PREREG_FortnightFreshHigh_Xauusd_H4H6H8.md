# S1525 — پیش‌ثبت (قبل از هر آزمون): سقفِ تازهٔ دو‌هفته‌ای (Fortnight Fresh High، افق ۱۰ روز معاملاتی) ⇒ LONG

**نویسنده**: Carl Friedrich Gauss — بلوک دوم S1520–S1529، شمارهٔ ۶ از ۱۰
**داور**: `engine/rqs2.py::compute_rqs2` v2.6 (دست‌نخورده) · **مسیر B** (یک تعریف منجمد، صفر پارامتر آزاد)
**داده**: `data/full/*.csv` = gunzip از **`data/mt5_full/`** (۱۵.۶ سال) · **فقط اکتشاف؛ سایت هرگز**

## ۰) درس‌های اعمال‌شده
1. کارت اصلی = پرشمارترین (**H4**، 879 لبه).
2. افق تقویمی، منجمد **پیش** از PnL: ۱۰ روز معاملاتی (H4→60، H6→40، H8→30).
3. رویداد با **لنگر جمعی** (سقف قیمت — قانون بازنویسی‌شدهٔ S1524)، بدون هیچ گیت.
4. فقط ۳ کارت (H12/D1 با این افق کم‌شمار و در S1523 هم ضعیف‌ترین بودند؛ بودجه هدر نمی‌رود).

## ۱) افشا و پرسش علمی
- fetch پس از S1524: هیچ ACCEPT جدید.
- S1523 ثابت کرد سقفِ تازه یک پدیدهٔ **افق تقویمی** است (۳۰ روز ⇒ همگرایی به +12). پرسش طبیعی: **افق کوتاه‌تر** چه؟ آیا «سقف دو هفته» هم لنگر جمعی است (تقویم بازار: دو هفته ≈ فاصلهٔ داده‌های کلان آمریکا / انقضاهای آپشن هفتگی) یا اطلاعات فقط در مقیاس ماهانه است؟ این آزمون مرز پایین **مقیاس افق** را می‌سنجد — نه بازیافت S526.
- **سنجش توصیفی بدون PnL**: افق ۱۰ روز — H4 879 لبه (56/سال)، H6 744، H8 651؛ فقط 51–54% هم‌بار با سقفِ ماهانه ⇒ رویداد **نیمه‌مستقل** از S1523؛ 91–94% در drift(LB)>0 ⇒ نول شرطی درست. افق ۵ روز هم شمرده شد (H4 1218 لبه، فقط 38% هم‌بار با ماهانه، 90% درفت) و افق ۶۵ روز (97% هم‌بار با ماهانه = زیرمجموعهٔ عملی S1523، بی‌فایده). **۱۰ روز پیش از PnL انتخاب و منجمد شد**؛ ۵ روز فقط در صورت شکست این آزمون و با شمارهٔ جدید. هیچ WR/سود تولید نشد.
- تازگی: grep «two-week / 10-day / دو هفته / fortnight» ⇒ هیچ لایهٔ سقف تازه؛ S693 (اکسترمم هفتهٔ قبل با پذیرش، M15/H1، REJECT) رویداد و هندسهٔ متفاوت. **بکر.**

## ۲) تعریف منجمد
| جزء | مقدار |
|---|---|
| افق | ۱۰ روز معاملاتی: **H4→60، H6→40، H8→30** |
| رویداد | `nh = close > max(close[t−LB..t−1])`؛ سیگنال = `nh & ~nh[t−1]` ⇒ **LONG** |
| گیت | هیچ |
| براکت/ورود/هزینه | SL=1.5×median(ATR100)، TP=1.5×SL · open کندل بعد · FIFO · 3.3 پیپ (هارنس S382 عیناً) |
| نول | شرطی در فضای `close[t−1]−close[t−LB] > 0`؛ uncond strides 1/3/7 + جایگشت K=2000 seed=20260805؛ MAX |
| کارت‌ها | **H4** (اصلی)، H6، H8 |

## ۳) بودجهٔ چندگانگی
n_trials = **40** = خانوادهٔ سقف/کف تازهٔ گاوس (34) + ۳ کارت + ۳ افق شمرده‌شده (5/10/65). تنش 50 در MD.

## ۴) ابطال‌گرها و پیش‌بینی‌ها (منجمد)
- **F1**: حکم رسمی = موتور روی **H4(60)**؛ ACCEPT فقط اگر ۱۱/۱۱.
- **P1 (مقیاس افق)**: اگر lift H4(60) ≥ +10 ⇒ لنگر دو‌هفته‌ای وجود دارد؛ اگر lift < +8 ⇒ اطلاعات مقیاس ماهانه است و افق کوتاه‌تر رقیق است (قانون: «لنگر جمعی طلا ماهانه است»).
- **P2 (توان)**: n بزرگ‌تر (پیش‌بینی ~500) ⇒ حتی lift +8 می‌تواند z>3 بدهد. اگر ACCEPT با lift<+8: حکم معتبر اما در MD «لبهٔ کم‌عمق پرشمار» علامت می‌خورد و جانشینی S1523-H4(180) توصیه نمی‌شود.
- پیش‌بینی شخصی: H4(60) n≈480–520، WR 48–51، lift +7..+10، z 2.5–3.5 ⇒ **مرزی (۴۵٪ ACCEPT)**؛ H8(30) lift +7..+9؛ H6 بین آن دو. گاوس انتظار دارد lift از ۳۰ روز **کمتر** باشد (رقیق‌تر) ولی n جبران کند.
- اگر ACCEPT: توقف و گزارش؛ overlap فوری با S1523-H4(180) و S382-H4 لایو (هر دو روی همان کارت). اگر REJECT: گزارش کوتاه ⇒ S1526.
