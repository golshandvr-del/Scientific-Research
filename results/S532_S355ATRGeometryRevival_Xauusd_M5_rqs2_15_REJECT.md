# S532 — احیای S355 با هندسهٔ ATR-مقیاس · XAUUSD-M5 · **REJECT (15.9)**

| | |
|---|---|
| پیش‌ثبت | `results/S532_PREREG_S355_ATRScaledGeometry_PathC.md` (commit `60176ea7` — قبل از هر محاسبه) |
| هارنس | `strategies/s532_s355_atr_geometry.py` (commit `99ee3c2d`) |
| قفل جست‌وجو | `results/_s532/LOCKED.json` (commit `c771b487` — قبل از holdout) |
| حکم | `results/_s532/verdict.json` (commit `f9dd6114`) — **یک** اجرا، بدون بازگشت |
| داده | `data/mt5_full/XAUUSD_M5.csv` · ۱٬۰۸۹٬۵۷۴ ردیف · ۲۰۱۱-۰۱-۰۳ → ۲۰۲۶-۰۸-۰۷ (۱۵.۵۹ سال) |
| n_trials | ۲۵ (تجمعی صادقانه: S530=۱ + S531=۲۰ + S532=۴) · seed=20260816 · K=500 |

---

## ۱. فرضیه (H_geom) و چرا معقول بود

S530 نشان داد S355 روی تاریخ کامل `REGIME-ONLY` است؛ S531 فرضیهٔ رژیم را ابطال
کرد. شاهد تشخیصی جدید: در pre-2023 **۷۲.۶٪** معاملات با timeout (نه لمس براکت)
بسته می‌شدند در برابر ۲۷.۷٪ در دورهٔ اخیر — براکت ثابت 120pip در دورهٔ کم‌نوسان
(میانهٔ ATR14 نیمهٔ اول فقط **7.8pip**!) عملاً لمس‌نشدنی بود.
**H_geom:** براکت شناور `SL=TP=k·ATR14` تز لایه را در هر دو رژیم قابل‌اندازه‌گیری
می‌کند و لبه برمی‌گردد.

## ۲. فاز جست‌وجو (فقط نیمهٔ اول، ۵۴۴٬۷۸۷ کندل تا 2018-11-13)

| k | n | WR | medSL | سهم maxhold | سربه‌سر | t |
|---|---|---|---|---|---|---|
| 2.0 | 57 | 54.39٪ | 18.6 | 0٪ | 58.89٪ | 0.04 |
| 3.3 | 57 | 56.14٪ | 30.6 | 0٪ | 55.39٪ | 0.54 |
| **5.5** | **56** | **57.14٪** | **50.2** | **0٪** | **53.29٪** | **1.19** ← قفل |
| 9.0 | 56 | 50.00٪ | 82.2 | 0٪ | 52.01٪ | 0.05 |

- **P1 (پیش‌بینی مکانیکی) تأیید شد:** سهم خروج timeout از ۷۲.۶٪ به **۰٪** رسید —
  مکانیک درست شد.
- گارد عمل-همانی پاس: medSL برندهٔ k=5.5 در نیمهٔ اول 50.2pip است، دور از 120
  (چون ATR نیمهٔ اول بسیار کوچک‌تر است — دقیقاً همان ناایستایی که فرضیه می‌گفت).
- k=2.0 زیر سربه‌سرِ خودش (54.4 < 58.9) — سنگینیِ هزینهٔ ثابت 3.3pip روی براکت
  تنگ، عیناً طبق پیش‌بینی P2.

## ۳. حکم (کل داده، k=5.5 منجمد)

```
REJECT · RQS2 = 15.9
n=159 · WR=55.35% · PF=1.212 · net=+1224.9pip · expectancy=+17.4pip
lift=+6.12pp · z=1.54 · p_perm=0.0614 · null_ref=49.23% (perm_sd=3.54, K=500)
سربه‌سر هزینه‌دار (medSL=66.8pip, RR=1): 52.47%
دروازه‌های قرمز: H1, H2, H3, H5, H6, H7, H8
maxhold_share کل: 0.0%
```

**زیرپنجره‌ها (اطلاعی):**

| دوره | n | WR | net |
|---|---|---|---|
| pre-2023 | 113 | **49.56٪** | **−144.8pip** |
| recent | 46 | 69.57٪ | +2905.8pip |

## ۴. نتیجهٔ علمی — H_geom ابطال شد

هندسهٔ شناور مشکل مکانیکی را کاملاً حل کرد (۰٪ timeout) اما لبه برنگشت:
pre-2023 همچنان زیر ۵۰٪ و زیان‌ده است. یعنی آنچه در pre-2023 غایب است،
**خودِ محتوای اطلاعاتی سیگنال** است، نه شیوهٔ برداشتش. ترکیب سه آزمون مستقل:

1. **S530** — سیگنال خام روی تاریخ کامل: مرده (REJECT 18.1، REGIME-ONLY)
2. **S531** — گیت رژیم (۴ سنجهٔ استاندارد): جدا نمی‌کند (REJECT 19.5)
3. **S532** — هندسهٔ ATR-مقیاس: مکانیک درست، لبه غایب (REJECT 15.9)

⇒ **مرگ ابدی خانوادهٔ «احیای تمام-تاریخیِ S355»** با ابزارهای گیت/هندسه.
هر احیای آینده باید خودِ event سیگنال را عوض کند (لایهٔ متفاوت = شمارهٔ متفاوت)،
نه بسته‌بندی آن را. وضعیت لایهٔ زندهٔ سایت (S355 روی دادهٔ اخیر) بی‌تغییر —
تصمیم «نگه‌داشتن-با-آگاهی / حذف» همچنان با شماست و S532 گزینهٔ سوم
(نجات با هندسه) را هم حذف کرد.

## ۵. ردپای صداقت

- پیش‌ثبت با پیش‌بینی صادقانهٔ P(موفقیت)≈۲۵٪ **قبل** از هر عدد commit شد (60176ea7).
- جست‌وجو فقط نیمهٔ اول؛ قفل قبل از holdout جدا commit شد (c771b487).
- holdout **یک بار** اجرا شد؛ اسکریپت اجرای دوم را از نظر فنی رد می‌کند.
- گرید بسته (۴ عضو)، RR=1 منجمد (TP≥SL — قانون بودجه)، mh=96 منجمد (درس S360).
- صفرِ اندازه‌گیری‌شده با **همان هندسهٔ شناور** (نه ۵۰٪ نظری): uncond=48.94٪.
- n_trials تجمعی روی پایهٔ S355 گزارش شد (۲۵)، نه فقط سهم این آزمون.
- تمایز با S360 (`DEAD_UNDER_FLOATING_GEOMETRY` روی S341) در پیش‌ثبت مستند شد؛
  نتیجهٔ هر دو حالا هم‌جهت است: شناورسازی هندسه هیچ لایهٔ مرده‌ای را زنده نکرد.
  **قانون مشاهده‌شدهٔ تقویت‌شده: هندسه ناقل لبه نیست؛ لبه در event است.**

— اویلر · S532 · بلوک S530–S539
