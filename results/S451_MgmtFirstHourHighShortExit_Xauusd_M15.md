# S451 — آینهٔ SHORT قاعدهٔ M1: «خروجِ SHORT در شکستِ سقفِ ساعتِ اول» · **REJECT (مرزی)**

| فیلد | مقدار |
|---|---|
| شمارهٔ رکورد | `S451` (مأموریت ۵ — مدیریت معامله) |
| نوع | قاعدهٔ مدیریتِ معامله (حالت ۴) — RQS2 مستقل ندارد؛ موتورِ حاکم پروژه: rqs2 v2.6 |
| پیش‌ثبت | `results/S450_PREREG_MGMT_RULES_PROTOCOL.md` §2 ردیفِ S451 (commit `65a01cd7`) |
| چرا آزمونِ مستقل؟ | هشدارِ صریحِ MISSION_5: M1 فقط برای LONG اثبات شده؛ **تقارن مفت فرض نمی‌شود** |
| بیمار | S344 Brooks trend-from-open first-pullback · XAUUSD-M15 · **SHORT** (ACCEPT rqs2 89) |
| حکم | **REJECT** — سود ۹۴.۳٪ پایه < آستانهٔ پیش‌ثبت‌شدهٔ ۹۵٪ (با وجودِ بهبودِ واقعیِ ریسک) |
| دادهٔ خام | `research/mgmt/S451_paired_S344_M15.json` |
| اسکریپت | `strategies/s451_paired_s344.py` |

## ۱. قاعدهٔ آزموده‌شده (فرمِ ثابت، صفر پارامترِ آزاد)

اگر در پوزیشنِ SHORT هستیم و `close` کندلِ بسته‌شده **بالای سقفِ ساعتِ اولِ
روزِ معاملاتیِ جاری** برود ⇒ خروج در `open` کندلِ بعد. لنگرِ روز = گپ >
(TF+30) دقیقه (لنگرِ اصلاح‌شدهٔ S450). ساعتِ اولِ M15 = ۴ کندل.

تفسیرِ پیشینی (قبل از دیدنِ نتیجه ثبت شد): ساعتِ اولِ M15 = عینِ
opening-range خودِ لایه (n_open=4)؛ بسته‌شدن بالای آن یعنی نفیِ premise
روزِ نزولی — منطقِ Brooks.

## ۲. اعتبارسنجیِ بازتولید

بازتولیدِ پایه از خطِ لولهٔ ACCEPT عیناً: **n=92، WR=64.13٪، PF=2.078** —
هر سه رقم برابرِ سندِ رسمیِ ACCEPT ✓ (بازتولیدِ بیت‌به‌بیتِ جمعیتِ بیمار).

## ۳. نتیجه (pnl بر ۱ لات، USD؛ ۹۲ معاملهٔ جفتی، ۲۶ معامله تغییر کرد)

| متریک | پایه | با قاعده | تغییر |
|---|---|---|---|
| avg | 341.61 | 322.09 | **−5.7٪** (بودجه: −۵٪) ❌ |
| sd | 1298.93 | 1276.87 | −1.7٪ ✓ |
| worst | −2233 | −2233 | = |
| maxDD | 7168 | 6080 | **−15.2٪** ✓ |
| PF | 2.078 | 1.995 | −4.0٪ |
| WR | 64.1٪ | 58.7٪ | −5.4pt |

نیمه‌ها: H1 → ریسک بهتر ولی سود کم (FAIL)؛ H2 → **PASS کامل** (سود و ریسک
هر دو). اما معیارِ پیش‌ثبت هم‌جهتیِ *هر دو* نیمه + سودِ کل ≥۹۵٪ را می‌خواست.

## ۴. حکمِ صادقانه — و چرا آستانه را جابه‌جا نمی‌کنیم

فاصله تا قبولی فقط ۰.۷ واحدِ درصد بود. دقیقاً برای همین لحظه، معیار **قبل
از دیدنِ اعداد** commit شده بود (ضدِ اشتباهِ رایج #8: دورزدنِ معیار).
جابه‌جاییِ آستانه پس از مشاهده = همان تقلبی که پروتکل برای بستنش ساخته شد.
حکم: **REJECT**. قاعده واردِ `trade_manager.ts` نمی‌شود.

## ۵. یافتهٔ علمی: عدم‌تقارنِ LONG/SHORT تأیید شد (دلیلِ داده‌محور)

| | S450 (LONG، ۵ کارت) | S451 (SHORT، S344-M15) |
|---|---|---|
| معاملاتِ تغییریافته در پایه | سودده یا خنثی (+14.9 در S312-H1) | **از قبل بازنده** (−545.69) |
| اثرِ قاعده بر آن‌ها | فاجعه (−261.4؛ خروج در کفِ بازگشت) | تقریباً خنثی (−614.77؛ کمی دیر) |
| مکانیزمِ شکست | dip در سوئینگِ صعودی خریده می‌شود | خروجِ **دیرهنگام** — تا close بالای سقف برود، عمدهٔ ضرر خورده شده |
| اثرِ ریسک | مخلوط (گاهی maxDD بدتر) | بهبودِ واقعی و پایدار (maxDD −15٪، هر دو نیمه) |

هشدارِ مأموریت درست بود: بازار متقارن نیست — آینهٔ SHORT ماهیتاً «حدِ ضررِ
ساختاریِ دیرهنگام» از آب درآمد، نه قفلِ سود. مسیرِ آیندهٔ مشروع (فقط
پیشنهاد، بدونِ اجرا در این رکورد): نسخهٔ سریع‌ترِ ماشه (مثلاً لمسِ intrabar
به‌جای close) یک **قاعدهٔ جدید** است و اگر روزی آزموده شود، پیش‌ثبت و
شمارهٔ خودش را می‌گیرد — نه ویرایشِ پس‌ازمشاهدهٔ همین قاعده.

## ۶. ردِ اشتباهاتِ رایج

| # | اشتباه | چگونه رد شد |
|---|---|---|
| 1 | look-ahead | خروج در open[i+1]؛ سقفِ ساعتِ اول فقط از کندل‌های ≤i؛ تقدمِ SL/TP موتور |
| 2 | تغییرِ جمعیت | بازپخشِ جفتی؛ n=92 در هر دو بازو؛ بازتولید = سندِ ACCEPT بیت‌به‌بیت |
| 3 | فرضِ تقارن | کلِ این رکورد آزمونِ مستقلِ آینه است — و عدم‌تقارن عملاً تأیید شد |
| 7 | جست‌وجوی پارامتر | صفر پارامترِ آزاد؛ فرمِ قاعده پیش‌ثبت‌شده |
| 8 | جابه‌جاییِ معیار | آستانهٔ ۹۵٪ با وجودِ شکستِ ۰.۷pt دست نخورد — REJECT صادقانه |

— AI مأموریت ۵ · 2026-08-14 · وضعیت: **REJECT** (بهبودِ ریسکِ واقعی، اما بهای سود بیش از بودجهٔ پیش‌ثبت‌شده)
