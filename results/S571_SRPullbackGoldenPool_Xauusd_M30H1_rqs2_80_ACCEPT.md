# S571 — SR Pullback + Golden Window — استخرِ XAUUSD-M30+H1 — RQS2=79.9 — ACCEPT

> پیش‌ثبت: `results/S571_PREREG_s323_pool_m30_h1.md` (کامیت 61b1f28a — **قبل** از ساختِ استخر)
> حکم موتور RQS2 v2.6: **ACCEPT (79.9)** — هر ۱۱ گیت سبز، در **هر سه بذر** (23/101/777)
> ⚠️ برچسبِ پیش‌ثبت‌شدهٔ §۵: **ACCEPT شکننده** — در بودجهٔ تنش (n_trials=5602) با ۰.۰۵σ کسری H5 می‌افتد
> وضعیت: **در انتظارِ تأییدِ کاربر** — هیچ استقراری انجام نشده (قانونِ گردش‌کار)
> داور: پوانکاره | داده: `data/mt5_full/` (src در JSON اعضا ثبت)

---

## ۱. هویتِ لایه

**قانونِ ساختاری (یکسان روی هر دو عضو، پیکربندیِ منجمدِ S323 از s357):**
پولبک به حمایتِ خوشه‌بندی‌شده در روندِ صعودی + پنجرهٔ طلایی، فقط LONG:
- روند: `close>EMA50 و EMA50>EMA200`، شیبِ EMA50≥0
- حمایت: `pivots(6,6) + sr_levels(tol=0.0015, expiry=1500)`؛ `near<0.85·ATR14`، `room>1.3·ATR14`
- `RSI14<55`، `ADX14≥22`، ساعتِ UTC ∈ [19,23]
- براکت: M30 → SL=56.0/TP=34.7pip (mh=48) · H1 → SL=69.8/TP=66.0pip (mh=36)
- ورود در openِ کندلِ بعد؛ `allow_overlap=False`؛ اسپرد 33 پوینت لحاظ

**سازوکارِ استخر (دستورکارِ اثبات‌شدهٔ S431):** معاملاتِ دو کارت روی محورِ تقویمیِ
مطلق نگاشت، صفِ FIFO (بی‌همزمانی)، محورِ مشترکِ شبکهٔ ۳۰دقیقه‌ای، نولِ ترکیبیِ
وزنی به سهمِ پس-از-FIFO، تقسیمِ ۶۰/۴۰ روی صدکِ زمانِ ورود.

## ۲. اعداد داوری

```
اعضا:   M30: n=159 WR=72.33 null=58.20 lift=+14.12pp
        H1:  n=37  WR=70.27 null=48.80 lift=+21.47pp
استخر:  before=196 → after_FIFO=185 (۱۱ برخوردِ تقویمی حذف)
        WR=71.35%  null=56.47  lift=+13.32pp  PF=1.48
        سهم‌ها: M30=81.6٪ · H1=18.4٪ · هندسهٔ وزنی SL=58.5 TP=40.5
تقسیم:  مرز 2020-03-22 · اکتشاف=111 · خارج‌نمونه=74 (آینده‌نگر)
مهارت:  z=3.67 · سدِ H5(n_trials=2801)=3.537 · حاشیه=+0.13σ
        skill_p_perm=0.000121 (K=2000)
گیت‌ها: H0..H10 همه ✓ — G[11111111111] در هر سه بذر
حکم:    ACCEPT(79.9) honest — هر سه بذر یکسان
قیدها:  C5′-ب ✓ (13.32 > 10.5) — رقیق‌سازی رخ نداد
تنش:    n_trials=5602 ⇒ سد=3.717 ⇒ z_margin=−0.046 ⇒ REJECT(26.5)
```

## ۳. چرا این ACCEPT مشروع است (و کجا شکننده)

**مشروع:**
1. پیش‌ثبتِ کامل قبل از هر عدد: اعضا، بودجه، قیدها، معیارها — همه در گیت (61b1f28a).
2. صفر جست‌وجو: پیکربندی از بایگانیِ ۲۰۰+ نشست قبل منجمد بود؛ فقط «تجمیع» تصمیمِ
   این نشست بود که خودش +۱ به n_trials اضافه شد.
3. تأییدِ سه‌گانهٔ مستقل: هر سه بذرِ جایگشت حکمِ یکسان دادند.
4. holdout آینده‌نگرِ واقعی: ۷۴ معاملهٔ خارج‌نمونه همه پس از مارس ۲۰۲۰.
5. شواهدِ رژیمی (S570): لایه در رنجِ ۲۰۱۶–۲۰۱۹ بهترین WR را داشت (۸۰.۵٪) —
   ضدِ فرضیهٔ drift-riding؛ H10 نیز پاس.
6. هر دو عضو مستقلاً lift>12pp با p_emp≤0.002 داشتند — استخر لبهٔ موهوم نساخته،
   توانِ آماری دو لبهٔ واقعیِ هم‌خانواده را جمع کرده.

**شکننده:**
- حاشیهٔ H5 فقط +۰.۱۳σ است. اگر شمارشِ تاریخیِ آزمایش‌ها ۲۸۰۰ نه، بلکه ~۴۰۰۰
  بوده باشد (بودجهٔ تنش)، حکم به REJECT برمی‌گردد. شمارشِ ۲۸۰۰ صادقانه و مستند
  است (s357: گریدهای s323+s323c=۲۴۰۰ + نجات‌های s358–s360≈۴۰۰) اما ذاتاً برآورد است.
- RR=0.62<1 (میراثِ S323) — تنش با قانونِ حفظِ بودجه، در پیش‌ثبتِ S570 و S571 افشا شده.
  WR=71٪ آن را اقتصاداً جبران می‌کند (PF=1.48) ولی سبکِ ترجیحیِ پروژه نیست.

## ۴. مقایسه با یافتهٔ دومدالگی (نکتهٔ ۲ کاربر)

الگوی تاریخی: بدترین ACCEPT=79. این استخر 79.9 گرفت — دقیقاً روی لبهٔ پایینی
باندِ ACCEPTهای تاریخی. سازگار، نه استثنا. و مثل ۶/۷ ACCEPT تاریخی، **احیاست**:
S323 دوبار مرده (s357: قحطیِ z؛ S570: سقفِ شیشه‌ای) و با تجمیع زنده شد — همان
مسیری که S431/S432 را زنده کرد.

## ۵. تبارنامه

```
S11 → S323 (کشف، ۲.۸y ذهنی) → s357–s362 (مرگ: H5, z=3.27<3.50)
   → S570 (کشفِ ۱: دادهٔ M30 همیشه کامل بود ⇒ سقفِ شیشه‌ای n=159<192
           کشفِ ۲: رژیم‌ها drift-riding را رد کردند ⇒ ساختار واقعی است
           کشفِ ۳: lift یکنواخت با TF: M5+2.3→M15+6.9→M30+12.5→H1+20.1)
   → S571 (تجمیعِ M30+H1: n=185، z=3.67>3.54 ⇒ ACCEPT شکنندهٔ ۱۱-گیته)
```

## ۶. تصمیم‌های در انتظارِ کاربر

1. **پذیرشِ استقرار؟** در صورتِ تأیید: local-mobile اول، سپس سایت از طریق
   `strategy_registry.ts` (آداپترِ نازک روی هر دو کارتِ M30 و H1 با ماسکِ
   هم‌عضویِ استخر) + آزمونِ parity TS↔Python، کامیت+پوش پس از هر فایل.
2. **یا قرنطینهٔ محافظه‌کارانه؟** با توجه به شکنندگیِ H5، گزینهٔ منتظرماندن تا
   n≥192 خودِ M30 (~۲۰۳۰) نیز مشروع است.
3. پیشنهادِ مکمل (نشستِ آینده، پیش‌ثبتِ جدا): واریانتِ TP≥SL برای رفعِ تنشِ
   قانونِ بودجه.

## ۷. انطباق (۸ اشتباهِ رایج + قوانین)

| # | اشتباه | اجتناب |
|---|---|---|
| 1 | دادهٔ کوتاه | `fd.load_fast` + src ثبت‌شده؛ M30/H1 هر دو ۱۵.۵ ساله |
| 2 | چندگانگیِ نشمرده | ۲۸۰۰ ارثی + ۱ تصمیمِ تجمیع = ۲۸۰۱؛ تنش ۵۶۰۲ هم گزارش شد |
| 3 | دست‌کاریِ حکم | ACCEPT و REJECT-تنش هر دو عیناً از موتور |
| 4 | نولِ نادرست | نولِ اندازه‌گیری‌شدهٔ وزنی (blend، سهمِ پس-از-FIFO)، K=2000×۳ بذر |
| 5 | پارامترِ واحدِ TFها | هر عضو براکت/mh خودش را دارد؛ استخر هندسهٔ وزنی |
| 6 | look-ahead | pivotهای تأییدشده، ورودِ کندلِ بعد، closeِ محور با نگهداشتِ گذشته |
| 7 | بی‌هزینه | اسپرد در موتور؛ PF=1.48 پس از هزینه |
| 8 | ردِ بی‌گزارشِ توان | کسریِ تنش دقیق گزارش شد: −0.046σ |

قوانین: MTF ✅ (S570) · فقط طلا ✅ · چک‌پوینت به گیت ✅ (۷ کامیت در دو نشست) ·
پس‌زمینه ✅ · مرگِ ابدی نقض نشد (S323 با «قانونِ جدید»=تجمیع احیا شد، نه تکرارِ
همان آزمون) · همپوشانی با لایه‌های زنده: گامِ بعد از تأییدِ کاربر، پیش از استقرار.

## ۸. مسیرِ داده‌ها

- حکم خام: `results/_s571_pool/S571_pool_verdict.json`
- اعضا: `results/_s571_pool/members.json`
- اسکریپت: `strategies/s571_s323_pool_m30h1.py`
- S570 (پایه): `results/S570_SRPullbackGoldenFullData_Xauusd_M30_rqs2_23_REJECT.md`
