# S983 — الحاقیهٔ انجماد (پیش از تک‌آزمونِ هولد-اوت)

> **دستورِ جدیدِ کاربر:** «هر یک از این ده شماره رو باید به یک لایه استراتژی اختصاص بدی
> و **حکم رسمی موتور** رو براشون بدست بیاری.» ⇒ بسته‌شدنِ صرفاً توان‌محور کافی نیست؛
> داوریِ رسمیِ `compute_rqs2` اجرا می‌شود ولو تحلیلِ توان شکست را پیش‌بینی کند.
> این الحاقیه **قبل** از اجرای داور کامیت می‌شود.

## پیکربندیِ منجمد (یگانه — از جست‌وجوی نیمهٔ اول)

| قلم | مقدار |
|---|---|
| کارت | **XAUUSD-H8** (بهترین z جست‌وجو: +2.26) |
| بازو | W=34 · θ=0.9 · **main** (TSM: +θ⇒LONG، −θ⇒SHORT) · SL_k=1.2 · RR=1.6 |
| sl_base_pip | منجمد از اسکنِ نیمهٔ اول: مقدارِ `sl_base_pip` در `results/_s983/scan_H8.json` |
| max_hold | 64 · allow_overlap=False |
| split | `split_bar = n_all // 2` (مرزِ ساختاریِ مسیر C) |
| مدلِ صفر | `tools/audit_fast_null.py::build_null_fast` روی کلِ داده، K=600، seed=20260806، دو سمت |
| n_trials صادقانه | **912** (فضای اعلام‌شدهٔ S983؛ 19×48) |
| قانونِ مرگ | حکم هرچه بود (به احتمالِ قوی REJECT طبق تحلیلِ توان) ثبت می‌شود؛ بدونِ آزمونِ مجدد |

**اعترافِ پیشاپیش (صداقتِ علمی):** تحلیلِ توانِ خودم می‌گوید این آزمون تقریباً
قطعاً شکست می‌خورد (n هولد-اوت ≈۷۰ ⇒ سدِ لیفت ≈۱۶pp در برابرِ +13.4pp جست‌وجو که
خودش احتمالاً متورم است). این شلیک برای اجرای دستورِ کاربر (حکمِ رسمیِ موتور برای
هر شماره) انجام می‌شود و نتیجهٔ منفی، مطابقِ پیش‌بینیِ ثبت‌شده، اطلاعات است.
