# S1521 — پیش‌ثبت (قبل از هر آزمون): سقفِ تازهٔ مطلع × رژیمِ آرامِ σ (Informed Fresh High in Calm)

**نویسنده**: Carl Friedrich Gauss — بلوک دوم S1520–S1529، شمارهٔ ۲ از ۱۰
**داور**: `engine/rqs2.py::compute_rqs2` v2.6 (دست‌نخورده، gates_only، ۱۱ گیت) · **مسیر B** (یک تعریف منجمد، صفر پارامتر آزاد)
**داده**: `data/full/XAUUSD_{H8,H12,H6}.csv` (gunzip از mt5_full، ۱۵.۶ سال) · **سایت دست‌نخورده**

## ۰) افشای صادقانه
- پس از S1520 (ACCEPT 90.6)، fetch انجام شد؛ ACCEPTهای جدید موازی: **S1911 کینز** (Kyle shock × calm-σ، H8، 93.9 — گیت آرامش +13pp به S965 افزود)، **S607 کانتور** (شوک انگل × drift × calm، 83.1 — دو گیت با نسبت هم‌خطی ≈1.0 جمع‌پذیر)، S749، S408. هیچ‌کدام سقفِ تازه را با رژیم σ ترکیب نکرده‌اند.
- **سنجش توصیفی بدون PnL** (قانون S529 — هم‌خطی پیش از گیت): نرخ عبور گیت آرامش (σ_t ≤ median σ_{t−233..t−1}) در سیگنال‌های S1520:
  H8 **58.1%** · H12 52.0% · H6 56.1% — در برابر نرخ پایهٔ آرامش در کل داده 55.3%. نسبت هم‌خطی ≈ 1.05 ⇒ گیت **متعامد** با رویداد (نه هم‌خط، نه کم‌چگال). هیچ عدد سود/زیان/WR تولید نشد.
- گیت σ عیناً از `strategies/s605_engle_sigma_regime.py::sigma_series/regime_ratio` (منجمد S605/S606/S1911) import می‌شود — بازنویسی نمی‌شود.

## ۱) فرضیه (Andersen–Bollerslev بر لنگر ساختاری)
قانون S1520: سقفِ تازه «کجا» است، ρ «چگونه». پرسش S1521: «در چه هوایی؟» سقفِ تازهٔ مطلع در بازارِ آرام = خبر واقعی که هنوز در قیمت جذب نشده (اطلاعات‌رسانی، ادامه)؛ در طوفان = یکی از ده‌ها کندل بزرگ خوشهٔ نوسان (پژواک، ادامهٔ ضعیف). S1911 دقیقاً این را روی شوک دامنه دید؛ اینجا روی رویداد سطحی آزموده می‌شود.

## ۲) تعریف منجمد
| جزء | مقدار | منبع |
|---|---|---|
| رویداد | لبهٔ تازهٔ `close > max(close[t−90..t−1])` و ρ=(close−open)/(high−low) ≥ 0.618 | S1520 (ACCEPT 90.6) |
| **گیت آرامش** | `σ²_t = 0.94σ²_{t−1}+0.06r²_{t−1}` (RiskMetrics)؛ ورود فقط اگر `σ_t ≤ median(σ_{t−233..t−1})` (پنجرهٔ بستهٔ گذشته، W=233) | S605/S606/S1911 |
| جهت / براکت / ورود / نول | LONG · SL=1.5×median(ATR100)، TP=1.5×SL · open کندل بعد، FIFO، 3.3 پیپ · نول شرطی در فضای درفت>0 (عیناً S1520 تا lift هم‌مقیاس باشد) | S1520 |
| کارت‌ها | **H8** (داوری اصلی)، H12، H6 (تکرار) | — |

## ۳) بودجهٔ چندگانگی
n_trials = **23** = 17 (خانوادهٔ سقف‌تازه تا S1520) + 3 (بازوهای counter S1520، صادقانه شمرده) + 3 (این آزمون). تنش 50 در MD.

## ۴) ابطال‌گرها و پیش‌بینی‌ها (منجمد)
- **P1 ضدِ سوختن توان**: lift گیت‌شده روی H8 باید **اکیداً** > +19.13 (پایهٔ S1520). در غیر این صورت لایه شکست‌خورده حتی با گیت‌های سبز.
- **P2 بازوی طوفان** (σ_t > median): lift باید کمتر از بازوی آرام باشد؛ اگر storm ≥ calm ⇒ رژیم σ برای این رویداد بی‌اطلاع ⇒ REJECT صریح.
- **F1**: حکم رسمی = خروجی موتور روی H8-calm؛ ACCEPT فقط اگر ۱۱/۱۱ + P1 + P2.
- **خطر اصلی (اعلام‌شده)**: n ≈ 85 روی H8 ⇒ حتی با lift بالاتر، z می‌تواند زیر 3.83 پایه بیفتد (سوختن توان از راه n، نه lift). اگر lift بالاتر اما z پایین‌تر و موتور ACCEPT دهد: حکم موتور معتبر است اما در MD صریح می‌نویسم که S1521 «خالص‌تر ولی کم‌توان‌تر» است و جانشینی S1520 را **توصیه نمی‌کنم** مگر z ≥ 3.83.
- پیش‌بینی شخصی: H8-calm n≈80–90، WR 63–67، lift +22..+27، z 3.4–4.2؛ storm lift +10..+15. H12/H6: POWER-LIMITED.
- اگر ACCEPT: توقف و گزارش؛ overlap با لایو S382-H4 فوری. اگر REJECT: گزارش کوتاه ⇒ S1522.
