# S431 — پیش‌ثبتِ احیای `S351` از راهِ **تجمیعِ چند-کارتی** (مسیرِ `C`)

> ⚠️ **این سند پیش از محاسبهٔ حتی یک عددِ جدید نوشته و commit می‌شود.**
> معیارِ حاکم: `engine/rqs2.py` **v2.6**. مأموریت: `MISSION_4` (بازهٔ S430–S449).
> گامِ قبلی: `S430` (REJECT) — درسش مستقیماً این گام را شکل داد.

---

## ۰. چرا این گام، بعد از شکستِ `S430`

`S430` با انتخابِ نامزد بر اساسِ **`z` بالا** شکست خورد. درسِ ثبت‌شده:

> `z` می‌گوید لبه **واقعی** است؛ `H9` می‌پرسد لبه **بزرگ** است. این دو مستقل‌اند.
> معیارِ درستِ انتخابِ نامزد: **حاشیهٔ `H9` = امیدِ خام − ۲×هزینه**.

ابزارِ `tools/s430_h9_headroom_rank.py` (commitِ قبلی) کلِ بایگانی را با این عینک
بازرتبه‌بندی کرد. نتیجه یک **الگویِ تکرارشوندهٔ** غیرقابلِ‌چشم‌پوشی بود:

| | `S430`/`S73` (شکست‌خورده) | طبقهٔ طلاییِ نو |
|---|---|---|
| حاشیهٔ H9 | **−۰.۳۹ pip** (بودجه ندارد) | **+۳۸ تا +۱۷۳ pip** (بودجهٔ فراوان) |
| `n` | ۱۱۹۴ (فراوان) | ۴۲–۷۷ (کم) |
| دروازهٔ افتاده | `H9` (نابخشودنی) | `H3,H5` (توانی — بخشودنی) |
| بیماری | لبهٔ کوچک | نمونهٔ کم |

⇒ نامزدِ درست **دقیقاً برعکسِ** نامزدِ قبلی است.

---

## ۱. نامزدِ انتخاب‌شده و مکانیزمِ شکستش

**`S351` — «فیلترِ جهتیِ شکستِ ساختارِ لگ-متناسب» (LPSB) روی مولدِ `S333`.**
سند مرجع (فقط ارجاع، دست‌نخورده):
`results/S351_LPSBStructureFilterXauusdM5M15M30H1_Xauusd_M5M15M30H1_rqs2_29_REJECT.md`

قانونِ لایه (صفر-پارامتر، از سندِ `S351` بندِ ۴):

```
ورودِ LONGِ S333 را بپذیر  ⇐⇒  state_LPSB(t) == −1
(با L=8, f=0.33 — عضوِ مرکزیِ پیش‌ثبت‌شدهٔ خانواده)
```

اثرِ فیلتر در **هر چهار کارت یکنواخت و هم‌جهت** بود (سندِ `S351`، جدولِ بندِ ۴):

| کارت | `S333` پایه (WR/PF) | + فیلترِ نزولی | `z` پایه → فیلتر |
|---|---|---|---|
| M5 | ۶۵.۶٪ / ۲.۸۵ | **۷۲.۳٪ / ۳.۹۵** | ۳.۰ → **۳.۴** ✅ (تنها کارتِ عبوری) |
| M15 | ۶۲.۸٪ / ۲.۳۰ | ۶۵.۸٪ / ۲.۴۴ | ۲.۰ → ۲.۱ ❌ |
| M30 | ۶۶.۷٪ / ۲.۴۸ | ۶۷.۹٪ / ۲.۵۶ | ۲.۲ → ۲.۰ ❌ |
| H1 | ۶۲.۲٪ / ۱.۸۵ | ۶۵.۲٪ / ۲.۰۷ | ۲.۱ → **۲.۵** ❌ |

**تشخیص:** یک قانونِ **یکسان** (همان مولد، همان فیلترِ صفر-پارامتر) در چهار
مقیاسِ نمونه‌گیری، چهار بار **جداگانه** داوری شد — هر بار با ~۵۰ معامله. هیچ‌کدام
به‌تنهایی توانِ آماری نداشت، در حالی که همه **هم‌جهت** بودند.

اثباتِ سقفِ شیشه‌ای (با `engine.rqs2.n_required_for_h3`، خودِ تابعِ موتور):

| کارت | `n` | `lift` | `n` لازم برای عبورِ `H3` | زیرِ سقفِ شیشه‌ای؟ |
|---|---|---|---|---|
| XAUUSD-M30 | ۴۲ | +۱۵.۵۸ | ۹۸ | ✅ بله |
| XAUUSD-M15 | ۵۱ | +۱۱.۷۱ | ۱۷۴ | ✅ بله |
| XAUUSD-M5 | ۶۴ | +۱۴.۰۱ | ۱۲۱ | ✅ بله |
| XAUUSD-H1 | ۷۴ | +۱۰.۴۳ | ۲۱۹ | ✅ بله |

⇒ شکستِ `H3` در هر چهار کارت **ریاضیاً ناشی از کمبودِ `n`** است. این دقیقاً
شرطِ `G5`ِ حکمِ `UNPROVEN` در v2.6 است.

---

## ۲. فرضیهٔ مرکزی (پیش‌ثبت‌شده)

> **فرضیهٔ H-431:** چون قانون در چهار کارت **یکی** است و اثرش **هم‌جهت**، تجمیعِ
> معاملاتِ چهار کارت در یک جمعیتِ واحد، `n` را از ~۵۰ به ~۲۳۱ می‌رساند و
> `z` را با `√n` بزرگ می‌کند، **بدونِ افزودنِ هیچ پارامترِ نو** و بنابراین
> **بدونِ افزایشِ هزینهٔ چندگانگی**.

تخمینِ پیشینی (فقط تخمین، نه ادعا):
`z_pooled ≈ ۱.۷۹۶ × √(۲۳۱/۷۴) ≈ ۳.۱۷` در برابرِ سدِ `۳.۰۹`.

**مجوزِ پروژه برای این کار** — یادداشتِ طراحیِ خودِ موتور (`engine/rqs2.py` خط ۲۳۶):

> «… به «اتاقِ انتظار» می‌رود تا با دادهٔ بیشتر یا **تجمیعِ چند-کارتی** نمونه‌اش
> بزرگ شود.»

پس این یک ترفندِ ابتکاریِ من نیست؛ **درمانِ پیش‌بینی‌شدهٔ خودِ معیار** برای همین
بیماری است.

---

## ۳. خطرِ اصلی و **قیدهای ضدِتقلبی که از پیش می‌پذیرم**

تجمیع می‌تواند به‌راحتی تبدیل به حساب‌سازی شود. چهار قیدِ سختِ زیر **قبل** از
دیدنِ نتیجه قبول می‌شوند و نقضِ هرکدام ⇒ اعلامِ شکست:

**C1 — همگنیِ جمعیت (شرطِ لازمِ آماری).**
تجمیعِ جمعیت‌های ناهمگن جرمِ آماری است. پس *قبل* از تجمیع، آزمونِ همگنی:
اگر `lift` کارت‌ها هم‌علامت نباشند یا آزمونِ `χ²` همگنیِ نرخِ برد رد شود
(`p < 0.05`)، **تجمیع انجام نمی‌شود** و گام شکست‌خورده اعلام می‌شود.
⚠️ کارتِ خامِ `S351` روی M15 لیفتِ **منفی** داشت (−۲.۸۵) — ولی آن نسخهٔ *بدونِ
فیلتر* است. تجمیع فقط روی نسخهٔ **فیلترشده** (که در هر ۴ کارت مثبت بود) مجاز است.

**C2 — ممنوعیتِ انتخابِ پس‌رویدادی کارت‌ها.**
مجموعهٔ کارت‌ها **همین الان** قفل می‌شود: `XAUUSD × {M5, M15, M30, H1}` — همان
چهار کارتی که سندِ `S351` روی آن‌ها فیلتر را سنجیده بود. **حق ندارم** بعد از
دیدنِ نتیجه کارتِ ضعیف را حذف کنم تا `z` بالا بیاید (این «cherry-picking» و
نقضِ سپرِ اشتباهِ ۸ است). اگر تجمیعِ ۴-کارتی رد شد، گام **رد** است — نه اینکه
سراغِ تجمیعِ ۳-کارتی بروم.

**C3 — استقلالِ زمانیِ معاملات (کشندهٔ پنهان).**
M5/M15/M30/H1 **همان بازار در همان زمان**اند. اگر یک ورودِ H1 و یک ورودِ M5 در
یک لحظه باز باشند، دو نمونهٔ مستقل نیستند — یک معاملهٔ دوباره‌شمرده‌شده‌اند و
`z` تجمیعی **تقلبی** می‌شود. پس اجباراً اندازه‌گیری می‌شود:
- همپوشانیِ زمانیِ معاملات بینِ کارت‌ها (بر حسبِ درصد)،
- و `n_eff` (نمونهٔ مؤثر) با حذفِ معاملاتِ هم‌زمان.
اگر همپوشانی بالا بود، `z` بر پایهٔ `n_eff` گزارش می‌شود، نه `n` خام.
**این قید مهم‌ترینِ چهار قید است و احتمالِ شکستِ گام عمدتاً از اینجاست.**

**C4 — هزینهٔ چندگانگیِ صادقانه.**
`n_trials` گزارش‌شده = `۷۴` (همان عددِ بایگانیِ `S351`) **به‌علاوهٔ** هر
واریانتی که در این گام می‌آزمایم. هیچ پارامترِ نویی تنظیم نمی‌شود:
`L=8, f=0.33, sl_k=1.618, rr=1.272, hold=12` همه از `S351` **منجمد** می‌آیند.

---

## ۴. آنچه آزموده می‌شود (قفل‌شده)

| # | آزمون | معیارِ عبور |
|---|---|---|
| ۱ | بازتولیدِ چهار کارتِ فیلترشدهٔ `S351` | `n`/WR در ±۱۰٪ اعدادِ سند |
| ۲ | آزمونِ همگنی (`C1`) | هم‌علامتی + `χ²` رد نشود |
| ۳ | اندازه‌گیریِ همپوشانیِ زمانی (`C3`) | گزارشِ `n_eff` |
| ۴ | داوریِ RQS2 v2.6 روی جمعیتِ تجمیعی | ۱۱ دروازه |
| ۵ | مدلِ صفرِ **اندازه‌گیری‌شده** (نه فرضی) | جای‌گشتِ `K=2000`، بذر `20260805` |

**هیچ فیلترِ نویی در این گام اضافه نمی‌شود.** فرضیه صرفاً «همان لبه، نمونهٔ
بزرگ‌تر» است. اگر تجمیع خودش کافی نبود، *سپس* فیلترهای `F1..F9`ِ مأموریت در
گامِ `S432` آزموده می‌شوند (با پیش‌ثبتِ جداگانه).

---

## ۵. شرایطِ اعلامِ شکست (از پیش پذیرفته‌شده)

گام **REJECT** اعلام می‌شود اگر هر یک رخ دهد:
1. آزمونِ همگنیِ `C1` رد شود؛
2. همپوشانیِ زمانی به‌قدری بالا باشد که `n_eff` عملاً به `n`ِ یک کارت برگردد؛
3. `z` بر پایهٔ `n_eff` زیرِ `۳.۰۹` بماند؛
4. هر دروازهٔ **نابخشودنیِ** v2.6 (`H2/H8/H9/H10/H4/H6`) بشکند.

و اگر رد شد، **مثلِ `S430`** با مکانیزمِ کاملِ شکست مستند می‌شود، نه با
جست‌وجوی یک عددِ خوش‌ظاهر.

---

## ۶. ⚠️ الحاقیهٔ اجباری — **تجمیع یک‌بار قبلاً آزموده و شکست خورده است**

این بند **پس از** نوشتنِ بندهای ۱–۵ و **پیش از** محاسبهٔ هر عددِ نو اضافه شد،
به‌محضِ کشفِ `strategies/s351_pool_rescue.py`. پیش‌ثبتی که سابقهٔ شکستِ مشابه را
پنهان کند، پیش‌ثبتِ ناقص است.

**تلاشِ قبلی** (`results/_scan_S351/_pool_rescue.json`):

| عضو | `n` | `lift` |
|---|---|---|
| XAUUSD-D1 | ۷۴ | **+۱۳.۹۰** |
| XAUUSD-H4 | ۵۱۵ | +۰.۸۸ |
| XAUUSD-H1 | ۱۹۳۷ | +۲.۳۲ |
| **تجمیعی** | ۲۰۳۷ (پس از حذفِ همپوشانی از ۲۵۲۶) | **+۲.۴۹** |

نتیجه: `z = ۱.۵۷` · `RQS2 = ۱۱.۱` · **REJECT** (و حتی `power_limited=false`).

### چرا آن شکست، فرضیهٔ `S431` را باطل **نمی‌کند**

آن گام **نسخهٔ خامِ** LPSB را تجمیع کرد؛ من **نسخهٔ فیلترشده** را تجمیع می‌کنم.
تفاوت کیفی است، نه سلیقه‌ای:

| | تجمیعِ قبلی (خام) | تجمیعِ `S431` (فیلترشده) |
|---|---|---|
| اعضا | D1 / H4 / H1 | M5 / M15 / M30 / H1 |
| `lift`ِ اعضا | +۱۳.۹ ، **+۰.۹** ، **+۲.۳** | +۱۴.۰ ، +۱۱.۷ ، +۱۵.۶ ، +۱۰.۴ |
| `n`ِ اعضا | ۷۴ ، **۵۱۵** ، **۱۹۳۷** | ۶۴ ، ۵۱ ، ۴۲ ، ۷۴ |
| همگنی | ❌ ناهمگن | ✅ هم‌مرتبه |

**مکانیزمِ شکستِ قبلی (تشخیصِ من): رقیق‌سازیِ وزنی.** تجمیع، میانگینِ *وزنی به `n`*
می‌گیرد. عضوِ قویِ D1 فقط ۲.۹٪ نمونه را داشت و دو عضوِ تقریباً بی‌لبه ۹۷.۱٪ را.
پس نتیجهٔ `+۲.۴۹` تقریباً همان `lift`ِ H1ِ خام (`+۲.۳۲`) است — تجمیع لبه را
بزرگ نکرد، بلکه **لبهٔ D1 را در نمونهٔ اعضای ضعیف غرق کرد**.

**درسِ ماندگار (ثبتِ پیش از آزمون):** تجمیع فقط وقتی درمانِ کمبودِ توان است که
اعضا **هم‌لبه و هم‌مرتبه** باشند. تجمیعِ یک عضوِ قوی با اعضای بزرگ‌ترِ ضعیف،
درمان نیست — **رقیق‌سازی** است. این دقیقاً همان چیزی است که قیدِ `C1` (همگنی)
از پیش برای جلوگیری‌اش گذاشته شده بود.

### قیدِ نوِ `C5` — سقفِ نسبتِ نمونه

برای اینکه من هم در همان دام نیفتم، **از پیش** می‌پذیرم: هیچ عضوی مجاز نیست
بیش از **۵۰٪** نمونهٔ تجمیعی را داشته باشد. نسبتِ اعضای من
(۶۴/۵۱/۴۲/۷۴ از ۲۳۱ ⇒ حداکثر ۳۲٪) این قید را پاس می‌کند. اگر پس از حذفِ
همپوشانی این قید نقض شد، تجمیع **متوقف** و گام REJECT اعلام می‌شود.

---

_پیش‌ثبتِ `S431` · مسیرِ چندگانگی `C` · معیار: `engine/rqs2.py` v2.6 ·
بذرِ ثابت: `20260805` · `K=2000`_
