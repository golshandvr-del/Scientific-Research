# S781 — Zscore144Ext — XAUUSD-M30 — REJECT (RQS2 = 3.2)

**پژوهشگر:** ابن هیثم (AI) · **تاریخ:** 2026-08-14
**پروتکل چندگانگی:** مسیر C (hold-out) — پیش‌ثبت commit `f8e64956`
(`results/S781_PREREG_zscore144_reversal_m30.md`) پیش از لمس نیمهٔ دوم برای این فرضیه.
**n_trials = 1.**

---

## ۱. فرضیه و پیکربندی (منجمد)

عبور رویدادی `zscore_fib_144` (دستهٔ statistical، پنجرهٔ فیبوناچی 144 — دستهٔ
دست‌نخورده در لایه‌های زنده) از ±3.08 → ورود هم‌جهت (تداوم کشیدگی آماری).

| مؤلفه | مقدار |
|---|---|
| داده | `data/mt5_full/XAUUSD_M30.csv` · نیمهٔ دوم: 90,957 کندل (2018-11-09 تا 2026-08-07) |
| هندسه | ATRmed(34)=34.6pip → SL=77.1، TP=92.6 (2.23×/2.68×؛ TP>SL) |
| اجرا | max_hold=144، تک‌پوزیشن، شبیه‌ساز رسمی، اسپرد کامل 3.3pip |
| مدل پوچ | مختص لایه: بی‌قید دو سمت (stride 1/3/7) + جای‌گشت K=1000، بذر 781 → `results/_s781/null_model.json` |

**بهبود روش نسبت به S780:** رتبه‌بندی اکتشاف با آلفا-نسبت-به-بی‌قیدِ هر سمت
(نه سربه‌سر هندسی) و غربال پایداری ثلثی (T1/T2/T3 × long/short همگی مثبت).

## ۲. نتیجهٔ آزمون تأییدی یگانه

```
S781_Zscore144Ext_XAUUSD_M30 | REJECT  RQS2=3.2 | n=427 WR=47.54% PF=0.99
lift=+2.38pp z=0.8 | H0:✓ H1:✗ H2:✗ H3:✗ H4:✗ H5:✓ H6:✗ H7:✗ H8:✗ H9:✗ H10:✗
net = +69 pip · long: n=233 WR=47.64% · short: n=194 WR=47.42%
```

حکم از موتور رسمی `engine.rqs2.compute_rqs2` (ورودی کامل: sl_pip, tp_pip,
bar_time, null, n_trials=1, split_bar=0.70×n, close).

## ۳. کالبدشکافی — چرا حتی معیار اصلاح‌شده هم کافی نبود؟

| سنجه | نیمهٔ اول | نیمهٔ دوم |
|---|---|---|
| n | 469 | 427 |
| آلفا (نسبت به بی‌قید) | **+7.70pp** (z_α=3.35) | long: +0.3pp / short: +4.9pp |
| net pip | +2257 | +69 |

۱. **سمت long کاملاً محو شد:** WR=47.64٪ در برابر perm_mean=47.34٪ — دقیقاً
   هم‌سطح شانس. کشیدگی آماری صعودی طلا در رژیم ۲۰۱۸–۲۰۲۶ هیچ اطلاع افزوده‌ای ندارد.
۲. **سمت short نیمه‌جان ماند** (+4.9pp نسبت به perm_mean=42.51، z≈1.6) — ولی
   بسیار دورتر از سد 3.09 و n=194 توان کافی ندارد. طبق قانون ضدتقلب، جداسازی
   پس‌رخدادیِ «فقط short» جست‌وجوی پنهان روی hold-out است و انجام نمی‌شود.
۳. **پایداری ثلثیِ درون‌نمونه هم پیش‌بین نبود.** دومین شاهد مستقل (پس از S780)
   که در طلا مرز رژیمی ۲۰۱۸ ساختار بازار را عوض کرده است.

## ۴. تبعیت از پروتکل

- لایه مرده است؛ هیچ بازتنظیم/بازآزمونی روی نیمهٔ دوم انجام نشد و نمی‌شود.
- MTF اجرا نشد (لمس اضافهٔ hold-out؛ فقط برای لایهٔ زنده موضوعیت دارد). EURUSD: هرگز.
- ممیزی هم‌پوشانی: بی‌موضوع (REJECT به سایت راه ندارد).
- حکم دست‌نویس نشد؛ همان خروجی موتور ثبت شد.

## ۵. ردِ ۸ اشتباه رایج

1. زمانی-صرف نبود (سیگنال آماری رویدادی). 2. پیچیدگی کامل (مدل پوچ دوسمته،
hold-out، غربال ثلثی). 3. دستهٔ statistical — نه MA. 4. سایت/local-mobile
دست‌نخورده. 5. حکم فقط برای کارت M30. 6. هندسهٔ ATR همان TF. 7. پارامترهای
غیررُند (3.08، 2.23، 2.68، پنجرهٔ فیبوناچی 144). 8. TP>SL؛ REJECT بی‌کم‌وکاست ثبت شد.

## ۶. درس تجمیعی S780+S781

دو فرضیهٔ مستقل (چرخه‌ای و آماری)، هر دو با اکتشاف صادقانه روی نیمهٔ اول و
پایداری درون‌نمونه‌ای خوب، هر دو در hold-out ۲۰۱۸–۲۰۲۶ فرو ریختند. نتیجهٔ
روش‌شناختی: در طلا **پایداری در رژیم ۲۰۱۱–۲۰۱۸ شاهد ضعیفی برای رژیم فعلی است**.
جست‌وجوی آیندهٔ مسیر C باید یا (الف) نیمهٔ اول را به «رژیم-همگن با نیمهٔ دوم»
نزدیک‌تر کند (مثلاً جست‌وجو روی ۲۰۱۸–۲۰۲۲ و تأیید روی ۲۰۲۲–۲۰۲۶ با پیش‌ثبت مرز)،
یا (ب) به خانواده‌هایی برود که مکانیزم‌شان رژیم-ناوابسته است (ریزساختار جلسه‌ای،
الگوهای کندلی شرطی). شواهد: `results/_s781/`.
