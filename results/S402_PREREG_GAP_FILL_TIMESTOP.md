# S402 — پیش‌ثبت: گپ‌فیلِ M30 + time-stop (حملهٔ دوم به تک‌دروازهٔ H8)

> مسیر C چندگانگی · ثبت **قبل از** اجرای هر ترکیب · مُهرِ زمان = همین کامیت
> والد: S400-M30 REJECT(33.2) فقط-H8 · S401 REJECT فازِ تنظیم (بهترین DD=6.46% > 6.40%)

## ۱. فرضیه

در S401 دو اهرمِ BE-trigger و cooldown افتِ سرمایه را ۲۵٪ کاهش دادند اما ۰.۰۶pp کم
آوردند. باخت‌های باقی‌مانده، باخت‌های *کامل* SL هستند (فاصلهٔ 1.5×|گپ| — بسیار سنگین).
اهرمِ **time-stop** این باخت‌ها را قیچی می‌کند: اگر گپ تا پایانِ بارِ `fb+B` پُر نشده،
خروج در کلوزِ همان بار. باختِ کامل → باختِ جزئی/سربه‌سر ⇒ کاهشِ مستقیمِ DD حسابداریِ داور.

## ۲. لایهٔ پایه (منجمد — هیچ پارامتری از آن تیون نمی‌شود)

XAUUSD · M30 · گپ‌DOWN · آستانهٔ QW70 (رولینگ ۲۵۰روزهٔ جدا-دوشنبه) · X-FILL ·
k_sl=1.5 · حذفِ DOW=0 · ورود = openِ بارِ اولِ روز · TP = کلوزِ روزِ قبل ·
اسپرد 3.3 pip · seed=400 · split M30 = بارِ 90691.

## ۳. فضای جست‌وجو (قفل — ۱۲ ترکیب)

| اهرم | مقادیر | توجیه |
|---|---|---|
| time-stop B (بار) | {7, 13, 19} | 3.5h / 6.5h / 9.5h در M30 — شناور، غیرگرد |
| BE-trigger f | {None, 0.4} | برندهٔ اثرگذارِ S401 |
| cooldown d | {0, 1} | برندهٔ اثرگذارِ S401 |

۳×۲×۲ = **۱۲ ترکیب**. قراردادها عینِ موتور: چکِ TP/SL درون‌بار مقدم بر time-stop؛
time-stop = خروج در **کلوزِ** بارِ `fb+B` (بعد از چکِ exit همان بار)؛ BE فقط از بارِ
بعدِ ورود؛ TP∧SL همزمان = باخت.

## ۴. قاعدهٔ برنده و تعهدها (قفل)

- تنظیم **فقط** روی نیمهٔ اول (بارهای < 90691) با حسابداریِ خودِ داور (`run_capital`).
- واجد: n≥100 ∧ PF>1 ∧ t≥2.5 ∧ **maxDD ≤ 6.40%** (0.8×سقفِ H8 — همان حاشیهٔ S401).
- برنده: کمترین maxDD؛ گره‌گشایی: t بزرگ‌تر.
- اگر هیچ ترکیبی واجد نشد ⇒ **REJECT بدونِ باز‌کردنِ holdout** (مثل S401 — بدونِ استثنا).
- در صورت وجودِ برنده: **یک** آزمونِ رسمی `compute_rqs2` روی کلِ داده با همان برنده.

## ۵. حسابِ چندگانگی (صادقانه، تجمعی)

S400: 216 · S401: +6 = 222 · S402: +12 ⇒ **n_trials = 234**.

## ۶. null (بازو-همتا، عینِ S401)

استخر = همهٔ روزهای gap-DOWN بی‌قید (بدونِ آستانه/DOW) با **همان** اهرم‌های برنده
(B/f/d یکسان) ⇒ lift فقط ارزشِ آستانه+فیلتر را می‌سنجد. K=500 زیرمجموعهٔ هم‌اندازه،
seed=400. هندسهٔ داور: میانهٔ sl_pip/tp_pip اندازه‌گیری‌شده از معاملاتِ واقعی.

## ۷. معیارِ نهایی

RQS2 v2.6 · یازده دروازهٔ H0–H10 · فایلِ نتیجه: `Strategy_Pair_TFs_rqs2_<score>_<STATUS>.md`

*تاریخ ثبت: 2026-08-18 · Mission 1 · نویسنده: AI-S400s (بازهٔ S400–S409)*
