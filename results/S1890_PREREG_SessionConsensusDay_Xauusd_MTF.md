# S1890 — پیش‌ثبت: «روزِ اجماعِ سه‌سشنی» (Three-Session Consensus Day) · XAUUSD

> ⏱ **commit و push پیش از هر اجرا.** موتور: RQS2 v2.6 · نویسنده: جورج سوروس (دههٔ دوم: S1890–S1899) · تاریخ: 2026-09-05 · داده: `data/mt5_full/` ۱۵.۶ سال (src در هر JSON)

## ۰. مرور موازی‌ها و راهبردِ دههٔ جدید
جدید: ACCEPT S1621 (رکورد+حاشیه، H8)، S547 (پیش‌تعطیلات، M30)، S569 (گپ×آرام، M1/M5)، S1580 ACCEPT-REDUNDANT (گیتِ حجم مرده). POWER-LIMITED: S1512، S555. REJECT: ~۱۵. هیچ‌کدام با ایدهٔ زیر هم‌پوشانی ندارند.
**راهبردِ اصلاح‌شده (از نقدِ دههٔ اول):** رویداد-اول · پرتکرار (n≥300) · **صفر فیلترِ کاهنده** · لنگرِ سشن (درسِ S899).
**بکارت:** grep «three sessions / اجماع سشن / full-day / روزِ کامل / session consensus» ⇒ هیچ لایه‌ای. S643 (پوشا، دو-کندلی) و S1512 (کندلِ بیرونِ نوار) شکلی‌اند، نه سشنی. شمارشِ رویداد (بدونِ PnL، فقط از دادهٔ H8): ۹۸۷ روزِ اجماع از ۳۹۹۵ روز (۲۴.۷٪)؛ با جابجاییِ ≥1.0×ATR21: ۶۵۹.

## ۱. فرضیهٔ بازتابی
هر کندلِ H8 (۰۰/۰۸/۱۶ UTC) رأیِ یک جمعیتِ سشنی است (آسیا، لندن، نیویورک) — S899 نشان داد این مرزها اطلاعات دارند. وقتی **سه جمعیتِ مستقل در یک روز هم‌جهت رأی می‌دهند**، «باورِ رایج» در حالِ شکل‌گیری است و روزِ بعد حلقهٔ بازتابی ادامه می‌دهد. برخلافِ شوک (یک جمعیت، حرکتِ بزرگ)، اجماع = سه جمعیت، هر حرکتی. پیش‌بینیِ ابطال‌پذیر: WR(اجماع) > WR(روزِ غیرِاجماع با همان جابجایی) — یعنی *توافق* اطلاعات دارد، نه فقط *بزرگی*.

## ۲. تعریفِ منجمد
- **رویداد (روی H8):** سه کندلِ متوالی با ساعتِ آغازِ ۰۰، ۰۸، ۱۶ UTC از **یک روزِ تقویمی** که `sign(close−open)` هر سه یکسان و ناصفر باشد. سیگنال روی کندلِ ۱۶ (پایانِ روز)؛ ورود open کندلِ بعد (= کندلِ ۰۰ روزِ بعد). جهت = جهتِ اجماع (follow). **بدونِ هیچ آستانهٔ اندازه** (پرتکرار).
- **براکتِ شناور:** SL=1.272×ATR21[i−1]، TP=2.058×ATR21[i−1] (S965، منجمد؛ ATR روی H8 علّی). max_hold=3 کندلِ H8 (= روزِ بعد، کامل) — **یک انتخابِ طراحی، از پیش قفل**: فرضیه دربارهٔ روزِ بعد است. allow_overlap=False. اسپرد 3.3pip.
- **چند-TF (قانون):** رویداد ذاتاً روی H8 تعریف می‌شود. برای رعایتِ قانون، همان رویداد روی **H4** (۶ کندل، سه جفتِ هم‌سشن هم‌جهت — سخت‌تر) و **D1** (کندلِ روز با ρ≥0.618 به‌عنوان پروکسیِ اجماع) هم اجرا می‌شود؛ TFهای ریزتر معنا ندارند (پیش‌اعلام INCOMPLETE). ⇒ ۳ کارت، **n_trials=3**، صفر پارامترِ آزاد.
- **کارتِ رسمیِ لایه = H8.**

## ۳. کنترل‌ها (تشخیصی، بدونِ اثر بر حکم)
- **P1 (توافق در برابر بزرگی):** روی همان کارتِ H8، گروهِ مقایسه = روزهایی که اجماع **ندارند** اما |close16−open00| ≥ میانهٔ جابجاییِ روزهای اجماع؛ همان براکت/هولد؛ فقط IS (۷۰٪ اول). اگر WR(اجماع) ≤ WR(مقایسه) ⇒ توافق بی‌اطلاع است (فرضیه ابطال، ثبت می‌شود).
- **P2:** جهت: اگر بازوی short به‌تنهایی WR<40 داشت، در گزارش ذکر می‌شود؛ حکم روی هر دو جهت با هم (follow) طبقِ تعریف.
- **نالِ کانونیِ هندسه‌همتا:** ۳ قرعهٔ بی‌قید {1890,2890,3890} (ref=max) + جایگشتِ زمانی K=500 بذر 1890 با همان n_sig، هر جهت جدا. `split_bar=0.7N` فقط برای OOS.

## ۴. پیش‌بینیِ شخصی
H8: n≈700–900 (پس از no-overlap)، WR 47–52، lift +4..+9pp، z 2–4. ریسکِ اصلی: PF<1.3 (گیتِ H1) چون رویداد پرتکرار و اثر متوسط است. P(ACCEPT)≈۲۰٪، P(POWER-LIMITED یا REJECT با lift معنادار)≈۵۰٪. H4 سخت‌تر و کم‌شمارتر؛ D1 پروکسی ضعیف.

## ۵. تعهدها
حکمِ موتور (کارتِ H8) عیناً در نامِ فایل · commit تدریجی هر کارت · دادهٔ کامل mt5_full · EURUSD هرگز · هیچ فایلِ دانشمندِ دیگری لمس نمی‌شود · اگر ACCEPT: توقف، حسابرسیِ هم‌پوشانی با S965/S1911/S1621/S526 (بر حسبِ زمانِ ورود)، گزارش به کاربر · شکست ⇒ S1891.
