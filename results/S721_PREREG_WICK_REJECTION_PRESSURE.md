# S721 — پیش‌ثبت: فشارِ ردِ جهت‌دارِ سایه‌ها (Wick-Rejection Pressure) — XAUUSD

**تاریخ:** 2026-08-19 · **مسیر چندگانگی:** C (اکتشاف فقط روی نیمهٔ اول؛ داوری روی دادهٔ کامل با `split_bar = len//2`)
**این سند قبل از هر آزمونی روی نیمهٔ دوم کامیت می‌شود.**

## ۱) فرضیه (منجمد)
سایهٔ پایینیِ کندل = دفاعِ خریدار از کف؛ سایهٔ بالایی = دفاعِ فروشنده از سقف.
`R(W) = (Σlower − Σupper) / (Σlower + Σupper)` روی پنجرهٔ W کندل ∈ [−1,+1].
**ورود لبه‌ای** (رویداد نه حالت — درس S382): عبور R از +thr ⇒ LONG، از −thr ⇒ SHORT (حالت continuation: دفاعِ مکررِ یک طرف ⇒ ادامه در همان جهت). دوطرفه.

**نوبودگی:** grep کامل results/*.md — «wick» فقط در S327 (کلایمکس تک‌کندلی، REJECT). هیچ لایه‌ای عدم‌تقارنِ **تجمعیِ** سایه‌ها را نیازموده. متعامد با S720 (z قیمت)، S382 (Williams%R)، S850 (رزونانس).

## ۲) پارامترهای منجمد و هندسهٔ literal
`W=13، thr=0.35، mode=cont، k=3.4×ATR(89)ِ میانه، RR=2.0، max_hold=13 کندل` (هم‌مقیاس با W — علاج BUG-TFM).

| TF | SL (pip) | TP (pip) | max_hold |
|---|---|---|---|
| H2 | 153.5 | 307.0 | 13 |
| H4 | 227.8 | 455.6 | 13 |
| H6 | 283.1 | 566.2 | 13 |

**خانوادهٔ منجمد: {H2, H4, H6}** — فقط همین ۳ کارت داوری می‌شوند. pip=0.10 (موتور). هزینه: spread=3.3 pip، commission=0.

## ۳) مبنای انجماد (شواهد نیمهٔ اول — اکتشاف، نه شاهد)
- H4: lift ترکیبی +7.77pp؛ **پایداری زمانی Q1/Q2: +7.50 / +7.89pp**؛ هر دو سمت مثبت (L:+8.0، S:+7.4).
- H2: فلات یکنوای thr: −0.72→+0.73→+3.95→+9.16pp؛ Q1/Q2: +5.30/+2.02.
- H6: +3.45pp (Q1 +4.69/Q2 +1.27) — عضو ضعیف، عمدتاً خوراک استخر.
- w=34 مرده (n فروپاشیده یا منفی) ⇒ خارج از خانواده.

## ۴) شمارش صادقانهٔ چندگانگی
فضای کاوش: 216 سلول (3W×4thr×3k×3rr×2mode) × 12 TF = **2592** + کاوشگرهای lift/پایداری (probe1: ۶ سلول×۳ بخش؛ probe2: ۲۴ سلول) ≈ 42 ⇒ **n_trials = 2650**. N_PERM=500/سمت، SEED=20260819. داور: همان معماری s720_adjudicate (null دوسمتی با هندسهٔ خود بازو، perm_k واقعی، z_luck_bound از موتور).

## ۵) پیش‌بینی‌ها (شامل فرضیه‌های رقیب — ثبت صادقانه)
| # | پیش‌بینی | مبنا |
|---|---|---|
| P1 | H4 قوی‌ترین کارت (پایداری Q1/Q2 استثنایی) | probe1 |
| P2 | تک‌کارت‌ها احتمالاً POWER-LIMITED (z تخمینی full-data: H4≈2.8، H2≈1.9) ⇒ استخر لازم | برآورد z |
| P3 | **تنش ثبت‌شده:** H3 در سلول منجمد lift منفی (−3.75pp) بین دو TF مثبت — اگر ساختار واقعی بود انتظار یکنوایی داشتیم؛ این ریسک جدیِ «نویز» است | probe1 |
| P4 | فرضیهٔ رقیب: اگر مصنوع رژیم باشد، H7/H10 می‌کُشد (درس S720) | صادقانه |
| P5 | اگر live شود، همپوشانی با S382 (مومنتوم H4) باید ممیزی و فیلتر فوراً آزموده شود | قانون همپوشانی |

## ۶) مسیر نجات پیش‌ثبت‌شده (فقط در صورت POWER-LIMITED)
اگر کارت‌هایی lift مثبت ولی z<3.09 داشتند و **هیچ گیت ماهوی دیگری** (H1/H4/H5/H8/H9/H10) نشکسته بود، تجمیع رسمی با `engine/rqs2_pool.pool_cards` (هندسهٔ نسبی یکسان ✓ k=3.4,RR=2 · هم‌جهتی lift · FIFO تقویمی · همگنی) و حکم نهایی با همان `compute_rqs2` روی استخر. کارت REJECT با گیت ماهوی شکسته وارد استخر نمی‌شود (قانون S720).

## ۷) معیار موفقیت
ACCEPT فقط با گذر هر ۱۱ گیت RQS2 v2.6 روی دادهٔ کامل (یا استخر قانونی). هر چیز دیگر = ثبت صادقانهٔ شکست.
