# S1515 — پیش‌ثبت: سقفِ تازهٔ ۹۰ (S526 منجمد، بدون ρ) × رژیمِ **STORM** روی TFهای زیرِ H8 (H4, H6) ⇒ LONG — XAUUSD

> **این سند قبل از تولید هر عدد PnL کامیت می‌شود** (قانون ③). داور: `engine/rqs2.py` v2.6 — verbatim.
> نویسنده: Isaac Newton — بلوک S1510–S1519، شمارهٔ ۶ از ۱۰ · 2026-09-05. **فقط اکتشاف — سایت هرگز لمس نمی‌شود.**

## ۱) منشأ: آزمونِ مستقیمِ «قانونِ وارونگیِ زیرِ H8»
دو شاهدِ مستقل: S1521-H6 (S1520×رژیم: calm +4.82 / **storm +17.37, z=3.07 PL**) و S1513 من (S1511×رژیم: H6 storm > calm). هر دو می‌گویند روی H6 لبهٔ تازهٔ ساختاری در **طوفان** زنده است. اما هیچ‌کس این را روی **والدِ خام S526** (بدون گیتِ ρ که n را نصف می‌کند) روی H6 و **H4** نیازموده است:
- S526-H6 خام: REJECT 26.6 (z=2.65, n≈276) — «لبه هست، توان نه».
- S526-H4 خام: POWER-LIMITED 27.7 (z=3.09 روی سد، n=414).
فرضیه: گیتِ storm روی این دو کارت *اطلاعات* می‌افزاید (چون در آرامش، سقفِ تازهٔ H4/H6 نویزِ ریزساختاری است) و به‌رغمِ نصف‌شدنِ n، z را بالای سد می‌برد.
**ریاضیِ توانِ پیش از عدد (درسِ S1513):** H6: n≈115–130 ⇒ ACCEPT فقط اگر lift ≥ +17؛ H4: n≈165–180 ⇒ فقط اگر lift ≥ +14. **پیش‌بینیِ صادقانه: احتمالِ POWER-LIMITED بالاست**؛ این لایه آزمونِ یک قانون است، نه شرط‌بندی روی ACCEPT.
**پیش‌سنجیِ گیت (فقط شمارش):** H4: 696 رویداد → storm 285 (41%) · H6: 472 → storm 196 (42%) — متعامد ✓ (سهمِ پایهٔ storm ≈46%).
**بکر بودن:** S1521 فقط S1520 (با ρ) روی H6 را داشت؛ S1911/S1740 خانواده‌های دیگر. S526×storm روی H4/H6 در هیچ سندی نیست.

## ۲) تعریف منجمد
| جزء | مقدار | منبع |
|---|---|---|
| رویداد | `close[t] > max(close[t−90..t−1])` لبهٔ تازه ∧ `close[t−1]−close[t−90] > 0` ⇒ LONG، ورود open کندل بعد | S526 |
| **گیت** | `reg_t = σ_t / median(σ_{t−233..t−1})` (σ از `s605.sigma_series` import، W=233) · **STORM: reg>1** (بازوی اصلی) · CALM: reg≤1 (بازوی کنترل، رسماً داوری و شمرده می‌شود) | S605/S1521 |
| کارت‌ها | **H4، H6** (زیرِ H8؛ H8 و بالاتر متعلق به قانونِ calm است و اینجا آزموده نمی‌شود) | — |
| براکت | SL=1.5×median(ATR100) هر کارت، TP=1.5×SL؛ اسپرد 3.3 | S382/S526 |
| نول | مشروط در فضای drift>0 ∧ رژیمِ بازو: strides 1/3/7 + perm K=2000، SEED=**20260909** | S523/S1513 |
| داده | `data/mt5_full/` (H4 از `XAUUSD_H4.csv` mt5_full که اکنون موجود است)، گارد E-16 | — |
| n_trials | **8** = ۴ حکم (۲ کارت × ۲ بازو) + ۴ پارامتر وام‌گرفته (90، 1.5، 1.5، W=233) — این خانواده (S526) بدهیِ خودش را در بلوک‌های دیگر دارد؛ من فقط بدهیِ خودم را می‌شمارم | صادقانه |
| مسیر | B — ۴ حکم، بدون جست‌وجو | ③ |

## ۳) ابطال‌گرها (پیش از عدد)
- **P1 وارونگی (قفل‌شده):** روی هر دو کارت **storm > calm** در lift. اگر روی H4 معکوس شد ⇒ قانونِ وارونگی فقط H6 است، نه «زیرِ H8».
- **P2 ضدِ سوختنِ توان:** lift(storm) > lift(پایهٔ خام): H6 > +9.49 و H4 > lift خامِ H4 (اندازه‌گیریِ داخلی). و z(storm) ≥ z(پایه): H6 ≥ 2.65، H4 ≥ 3.09. اگر lift بالا رفت ولی z پایین آمد ⇒ «پالایشِ بدونِ قدرت» (مثل S1513) ثبت می‌شود.
- **P3 متمم:** calm زیرِ پایه.
- **F1:** ACCEPT در هر بازو ⇒ توقف + گزارش + ممیزی هم‌پوشانی (S382-H4 لایو — حیاتی چون H4 است؛ S526-H8، S1520، S1521، S1621، S1511) + overlap-as-filter.
- **F2:** غیر-ACCEPT ⇒ گزارش کوتاه ⇒ fetch ⇒ S1516.
- **پیش‌بینی شخصی:** H6-storm n≈120، lift +14..+18، z 2.6–3.2 (PL محتمل)؛ H4-storm n≈170، lift +12..+15، z 2.8–3.4 (لبِ سد).

## ۴) انطباق
③ ✓ · یک شماره = یک لایه ✓ · TP≥SL ✓ · mt5_full ✓ · بدون EURUSD ✓ · قلمروی دیگران دست‌نخورده (S605 import) ✓ · fetch: S1522 REJECT، S535/S505 پیش‌ثبت (Dual-Numeraire)، S1960 پیش‌ثبت — ACCEPT جدید نیامده ✓ · **سایت هرگز** ✓.

*نیوتن: اگر قانون واقعی است، باید روی والد هم صادق باشد، نه فقط روی فرزندِ پالوده.* 🍎
