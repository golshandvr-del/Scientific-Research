# S624 — پیش‌ثبتِ مسیر چندگانگی (قبل از هر آزمون نهایی)

**تاریخ:** 2026-08-13 · **نویسنده:** اقلیدس (بلوک S620–S629) · **وضعیت:** PREREG

## ۰) ممیزی تازگی (دفتر مشترک تا commit b6ddd613)

متمایز از: S720 (کشیدگی چندمقیاسی)، S900 (شکست مینسکی)، S901 (جاروی نقدینگی—swing)،
S913 (RSI-21)، S560ها (GapOpen)، S582 (TD Sequential)، S680 (مدت اشباع لاگر).
هیچ پیش‌ثبت فعالی «پس‌زنی EMA در پول‌بک» را ادعا نکرده. خانواده‌های بستهٔ خود بلوکم
(LagRSI-خروج، سطح رند) استفاده نمی‌شوند.

## ۱) فرضیه (با درس‌های دفتر مشترک)

- درس S522: لبهٔ طلا جهت‌دار و هم‌سوی روند سکولار صعودی است ⇒ **فقط LONG**.
- درس بلوک خودم: تک‌رویداد، بدون انباشت فیلتر؛ نرخ رویداد بالا در H1 برای n.

**رویداد (پس‌زنی EMA از بالا):** کندلی که (الف) کندل قبلش تماماً بالای EMA(P) بسته،
(ب) low آن EMA را لمس/قطع کند (`low ≤ EMA`)، (ج) close آن دوباره بالای EMA بسته شود.
یعنی نخستین بوسهٔ حمایتی میانگین در روند صعودی — پرایس‌اکشن کلاسیک (Brooks: pullback to MA).

## ۲) مسیر: C (نگه‌داشت) — جست‌وجو فقط `[0, n/2)`؛ آزمون نهایی یک `compute_rqs2` با
`split_bar=n/2`، `n_trials=1` پس از PREREG-2. نگه‌داشت تاکنون بکر است.

## ۳) خانوادهٔ منجمد

| پارامتر | مقادیر | تعداد |
|---|---|---|
| TF | M30, H1, H2, H3 | 4 |
| P (دورهٔ EMA — فیبوناچی) | 89, 144, 233 | 3 |
| k_sl (SL=k×ATR100) | 1.5, 2.0 | 2 |
| RR (TP=RR×SL؛ RR≥1) | 1.0, 1.5 | 2 |

کوشش‌ها: 4×3×2×2 = **48**. سمت: فقط LONG (پیش‌ثبت‌شده؛ آینهٔ SHORT آزموده نمی‌شود —
درس S522 که آینهٔ طلا مرده است). max_hold=64 (H1–H3) و 120 (M30).
بدهی تجمیعی نیمهٔ جست‌وجوی بلوک: 4320+48=4368.

## ۴) معیار انجماد — چهارشرطی (H2 و H8 از روز اول)

(۱) lift ≥ 4pp · (۲) n ≥ n_required(3.09σ) · (۳) exp_pip > 0 پس از هزینهٔ کامل ·
(۴) بیشینهٔ lift·√n میان گذرندگان. هیچ‌کس نگذشت ⇒ سوخته، نگه‌داشت بکر.

## ۵) ثابت‌ها

SEED=20260816 · مبنا: اندازه‌گیری‌شدهٔ بی‌قید LONG با همان هندسه (نمونه‌گیری تصادفی؛
مقایسه با مبنای درفت‌دار طلا یعنی H3 فقط مهارتِ فراتر از درفت را می‌سنجد — درس S346) ·
داده فقط `tools.s434_fast_data` · spread=3.3pip، 10k$، ریسک=1٪ · EURUSD: هرگز ·
بهداشت حافظه (درس OOM) · قانون اندک‌اندک: JSON+commit هر TF.
