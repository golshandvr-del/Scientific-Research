# S983 — عبور از آستانهٔ مومنتومِ سری‌زمانیِ نرمال‌شده به نوسان (TSM Cross) — حکم رسمی موتور

**وضعیت: ❌ REJECT — RQS2 = 12.3 (حکم رسمی `compute_rqs2`, نه دستی)**

- **استراتژی**: S983 TsmVolnormCross — m(t) = (close[t]−close[t−W]) / (ATR100[t]·√W)؛ رویداد = نخستین عبورِ هر اپیزود از ±θ (اپیزود با بازگشتِ m به صفر ریست می‌شود). ریشهٔ علمی: Moskowitz–Ooi–Pedersen (2012).
- **جفت‌ارز**: XAUUSD (طلا) — طبق قانون، صفر ثانیه روی EURUSD.
- **تایم‌فریم داوری**: H8 (منتخبِ منجمدشده از اسکن ۱۹ تایم‌فریم روی نیمهٔ اول)
- **پیکربندی منجمد** (الحاقیهٔ `S983_ADDENDUM_FREEZE.md`، کامیت‌شده *قبل* از اجرای داور): W=34، θ=0.9، جهت main، SL_k=1.2، RR=1.6، max_hold=64، split_bar=n//2
- **TP > SL**: بله — SL=119.63pip، TP=191.40pip (RR=1.6) ✓
- **منبع داده**: `data/mt5_full/XAUUSD_H8.csv` (mt5_full — بانک کامل)، 11,978 کندل
- **پیش‌ثبت**: `S983_PREREG_TSM_VOLNORM_CROSS.md` (کامیت d53cffe3، قبل از هر تستِ جهت‌دار)
- **n_trials صادقانه**: 912 (48 آرم × 19 تایم‌فریم فضای اسکن)

## حکم verbatim موتور

```
S983_TsmCross_XAUUSD-H8    | REJECT     RQS2= 12.3 | n= 136 WR=46.32% PF=1.29 lift=  +8.60pp z= 1.5 | H0:✓ H1:✗ H2:✓ H3:✗ H4:✓ H5:✗ H6:✓ H7:✗ H8:✗ H9:✓ H10:✗
```

## اعداد کلیدی (از `results/_s983/H8_rqs2_verdict.json`)

| متریک | مقدار |
|---|---|
| n_trades (کل بازه) | 136 |
| WR | 46.32% |
| PF | 1.287 (< 1.3 → H1 ✗) |
| lift خالص | +8.60pp |
| **skill_z** | **1.46** (نیاز: ≥3.09) |
| **skill_p_perm** | **0.072098** (نیاز: ≤0.001) → H3 ✗ |
| z_obs vs z_luck_bound(912) | 2.07 vs 3.229 → margin = −1.16 → H7 ✗ |
| **OOS (نیمهٔ دوم، holdout)** | n=68، WR=39.71%، PF=0.978، net=−95.5 → H8 ✗ |
| کالیبراسیون ربع‌ها | [+1207.7, +1062.2, +47.4, −3.1] — فروپاشی زمانی → H5/H10 ✗ |
| null ref (بدترکیبِ blend) | 37.72% (perm_mean long=39.37، short=35.75، K=600، seed=20260806) |

## تفسیر

دقیقاً همان چیزی که تحلیل توانِ پیش‌اعلام‌شده در الحاقیهٔ انجماد پیش‌بینی کرده بود رخ داد: لیفتِ نیمهٔ اول (+13.4pp روی n=68) در نیمهٔ دوم به WR=39.7% (زیر آستانهٔ سربه‌سر 39.52%) فروپاشید. کالیبراسیونِ ربع‌ها نشان می‌دهد سیگنال فقط در دو ربعِ اولِ داده (سال‌های قدیمی‌تر طلا) زنده بود و در رژیم جدید مرده است. الگوی MOP-2012 در مقیاس H8 طلا با هزینهٔ 3.3pip **مهارتِ آماریِ اثبات‌پذیر ندارد**.

## اثبات پرهیز از ۸ خطای مرگبار

1. **Look-ahead**: سیگنال از close[t] و ATR[t]؛ ورود در open[t+1] (موتور استاندارد). ✓
2. **Survivorship/انتخابِ پس‌ازآزمون**: کاندید H8 روی نیمهٔ اول انتخاب و *قبل از* دیدنِ نیمهٔ دوم منجمد شد (کامیت الحاقیه قبل از داور). ✓
3. **چری‌پیکِ n_trials**: کل فضای جست‌وجو 912 آرم صادقانه به موتور گزارش شد؛ Bonferroni p_adj=1.0. ✓
4. **هزینهٔ معاملاتی**: spread=3.3pip در هر معامله لحاظ شد. ✓
5. **مدل صفرِ جعلی**: null از `build_null_fast` واقعی (K=600، seed منجمد 20260806) روی همان دیتا/هندسه؛ هرگز دست‌ساز. ✓
6. **Overlap**: allow_overlap=False. ✓
7. **حکم دستی**: حکم فقط از `compute_rqs2`؛ خط verbatim بالا. ✓
8. **سوزاندنِ holdout مکرر**: فقط یک شلیک؛ نیمهٔ دوم پیش از این آزمون بکر بود. ✓

## نتیجه‌گیری خانواده

- TSM عبور از آستانه (نسخهٔ خام MOP، بدون هموارسازی) در طلا: **مرده در همهٔ ۱۹ تایم‌فریم** — بهترین z اسکن +2.26 << نوارِ شانس، و holdout رسمی H8 هم REJECT.
- قانون تجربی «main > mirror» برای بار پنجم تکرار شد (side_lift: long +11.98pp vs short +4.57pp — هر دو ناکافی).
- شمارهٔ S983 بسته شد با حکم رسمی: **REJECT — RQS2=12.3**.

— آندری کولموگوروف، 2026-08-28

*جایگزین سند موقت `S983_TsmVolnormCross_Xauusd_19TF_rqs2_0_INCOMPLETE.md` (که با این حکم منسوخ شد).*
