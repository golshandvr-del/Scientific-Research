# S374 — دروازهٔ شکستِ Kennedy · `XAUUSD+EURUSD` · `H4` · **ACCEPTED (رتبهٔ محافظه‌کارانه)**

> پیش‌ثبت: [`S374_PREREG_kennedy_break_gate.md`](./S374_PREREG_kennedy_break_gate.md)
> (commit شده **پیش از** کد) · یافتهٔ ساختاری:
> [`S374_FINDING_not_subset_but_delay.md`](./S374_FINDING_not_subset_but_delay.md)
> · کد: `strategies/s374_kennedy_break_gate.py` · بازرسی:
> `tools/s374_audit_like_for_like.py`

---

## ۰. خلاصهٔ یک‌خطی

تغییرِ **تعریفِ شکستِ کانال** از `close` به `high/low` (قاعدهٔ Jeffrey Kennedy)
لایهٔ `S373` را — که تنها با شکست در بندِ **تکرارپذیری** سوخته بود — روی `H4`
احیا کرد. هر ۴ شرطِ پیش‌ثبت‌شده پاس شد. چهار کارتِ دیگر رد شدند.

**و این نتیجه خلافِ پیش‌بینیِ صریحِ خودم در پیش‌ثبت بود.**

---

## ۱. جدولِ نتایجِ کاملِ ۵ کارت (قفلِ ۴ پیش‌ثبت: شاملِ شکست‌ها)

| کارت | نگه‌داشت | `Δ e_pip` طلا | `Δ e_pip` یورو | `z` Kennedy | حکم |
|---|---|---|---|---|---|
| **M5** | ۵۹.۷٪ | — | — | منفی | `DEAD_BELOW_COST` |
| **M15** | ۶۰.۱٪ | `-1.112` | `+0.145` | `-1.801` | `DEAD_BELOW_COST` |
| **M30** | ۵۹.۲٪ | `+0.894` | `-0.024` | `-1.226` | `DEAD_BELOW_COST` |
| **H1** | ۵۹.۱٪ | — | — | — | `DEAD_BELOW_COST` |
| **H4** | ۶۴.۵٪ | **`+21.300`** | **`+4.866`** | **`+4.100`** | ✅ **`ACCEPT`** |

---

## ۲. کارتِ پذیرفته‌شده — `H4`، عددبه‌عدد

| سنجه | پایه (`close`) | Kennedy (`high/low`) |
|---|---|---|
| اعضا | ۲۴ | ۲۰ |
| معاملات | ۱٬۷۰۴ | ۱٬۰۶۲ |
| `trade_pooled` | `+0.0176` | **`+0.1291`** |
| مدلِ صفر | `-0.0465` | `-0.0441` |
| `lift` | `+0.0642` | **`+0.1732`** |
| **`z`** | `+1.974` | **`+4.100`** |
| `p_perm` | `0.0274` | **`0.0025`** |
| نمونهٔ لازم | ۲٬۸۸۹ ❌ | **۴۱۷ ✅** |
| **نیمهٔ اول** | **`-0.0134`** ❌ | **`+0.0933`** ✅ |
| **نیمهٔ دوم** | `+0.0537` | `+0.1696` ✅ |
| `e_pip` طلا (c=۳.۳) | `+11.67` | **`+32.97`** |
| `e_pip` یورو (c=۱.۶) | `+1.57` (مازاد `-0.03`) ❌ | **`+6.44`** ✅ |

### چهار شرطِ پیش‌ثبت‌شده

| شرط | سد | مقدار | نتیجه |
|---|---|---|---|
| کرانِ شانس | `z ≥ 2.570` (N=۱۱۲) | `4.100` | ✅ |
| نمونهٔ کافی | `n ≥ 417` | `1,062` | ✅ |
| اقتصاد (هر دو ابزار) | `e_pip > c` | `32.97>3.3` و `6.44>1.6` | ✅ |
| **تکرارپذیری** | هر دو نیمه `>0` | `+0.0933` و `+0.1696` | ✅ |

**بندِ تکرارپذیری همان بندی است که `S373` را کشت.** پایه علامت عوض می‌کرد
(`-0.0134 → +0.0537`)؛ Kennedy هر دو نیمه را مثبت می‌کند. فیلتر فقط میانگین را
بالا نبرد، **نیمه‌ای را که ناپایداری را حمل می‌کرد حذف کرد** — که کیفیتاً
قوی‌تر است.

---

## ۳. سه بازرسیِ مستقل — چون نتیجه خلافِ پیش‌بینی بود

### ① نشتِ آینده — **رد شد** (مدرکِ کد)
`engine/scalp_engine.py:146`:
```python
entry_bar = si + 1
raw_entry = o[entry_bar]
```
ورود در **open کندلِ بعدی**. پس هنگامِ ورود، کندلِ سیگنال بسته و `high`/`low`
آن **تاریخی** است، نه آینده. ⇒ قاعده forward-safe است.

### ② سوگیریِ انتخابِ عضو — **وجود دارد، ولی محافظه‌کارانه**
شرطِ سخت‌تر ⇒ ۴ عضوِ `XAUUSD,k=2` زیرِ `MIN_TRADES` افتادند و حذف شدند.
پس دو بازو روی **مجموعه‌های متفاوتِ اعضا** مقایسه شده بودند.

| کارت | حذف‌شده | `Δ` خام | `Δ` **هم‌سنگ** | جهتِ سوگیری |
|---|---|---|---|---|
| M5 | ۰ | `-0.0208` | `-0.0208` | — |
| M15 | ۰ | `-0.0082` | `-0.0082` | — |
| M30 | ۰ | `+0.0085` | `+0.0085` | — |
| H1 | ۰ | `+0.0688` | `+0.0688` | — |
| **H4** | **۴** | `+0.1131` | **`+0.1399`** | **محافظه‌کارانه** |

اعضای حذف‌شده در پایه `meanR=+0.1826` داشتند در برابرِ `+0.0219` برای باقی‌مانده
⇒ حذفشان **به زیانِ** Kennedy بود. دلتای صادقانه **بزرگ‌تر** است.

### ③ آزمونِ علامت — مصون از هر انتخابِ وزن‌دهی
| کارت | اعضای بهبودیافته | `p` دقیق (دوجمله‌ای) |
|---|---|---|
| M5 | ۱۰/۲۴ | `0.846` |
| M15 | ۱۴/۲۴ | `0.271` |
| M30 | ۱۲/۲۴ | `0.581` |
| H1 | ۱۵/۲۴ | `0.154` |
| **H4** | **۱۷/۲۰** | **`0.00129`** ✅ |

این آزمون فقط **علامت** را می‌شمارد ⇒ از سوگیریِ خوش‌بینیِ میانگینِ خانواده،
از وزن‌دهی و از هر میانگین‌گیری **کاملاً مصون** است. `p=0.0013` می‌گوید نتیجه
محصولِ ترفندِ آماری نیست.

---

## ۴. ⚠️ ضعفِ واقعیِ لایه — بوت‌استرپِ خوشه‌ای

شبههٔ کشنده: اعضا **مستقل نیستند** — همان کانال‌ها را با براکت‌های مختلف دوباره
معامله می‌کنند. واحدِ درستِ استقلال **کانال** است، نه معامله.

روی `XAUUSD-H4`: بازوی Kennedy تنها **۴۳ بارِ تقویمیِ مجزا** در **۱۰.۸ سال**
⇒ **≈۴ رویدادِ مستقلِ طلا در سال**.

بوت‌استرپ با بازنمونه‌گیریِ **خوشه‌های کانال** (۴٬۰۰۰ تکرار):

| بازو | خوشه | معاملات | میانگین R | **CI ۹۵٪** | `P(meanR ≤ 0)` |
|---|---|---|---|---|---|
| پایه | ۴۳۱ | ۱٬۷۰۴ | `+0.0176` | `[-0.0794, +0.1133]` | **۳۶.۲٪** |
| **Kennedy** | ۲۶۸ | ۱٬۰۶۲ | `+0.1291` | **`[+0.0014, +0.2544]`** | **۲.۵٪** |

**تفسیرِ صادقانه:**
- بازوی **پایه** با واحدِ استقلالِ درست **کاملاً بی‌معنا** است (۳۶٪ احتمالِ
  لبهٔ صفر) — یعنی `z=1.974`ِ آن **بیش‌برآوردِ** ناشی از شمارشِ معامله به‌جای
  کانال بود.
- بازوی **Kennedy** صفر را رد می‌کند، **ولی به‌سختی**: کرانِ پایین `+0.0014`
  عملاً روی صفر است.

⇒ **لبه واقعی است، ولی حاشیه‌اش نازک است.** به همین دلیل پذیرش با برچسبِ
**«رتبهٔ محافظه‌کارانه»** ثبت می‌شود و توصیهٔ سایز باید محتاط باشد.

---

## ۵. یافتهٔ ساختاری — لایه یک **فیلتر** نیست، یک **تأخیرِ ورود** است

جزئیاتِ کامل: [`S374_FINDING_not_subset_but_delay.md`](./S374_FINDING_not_subset_but_delay.md)

اثباتِ پیش‌ثبت (`high<line ⟹ close<line`) در سطحِ خام **درست است** ولی پس از
`_first_per_seg` **نقض می‌شود**: در هر کانال، `close` زودتر شلیک می‌کند، پس
بازوی پایه بارِ زودتر و بازوی Kennedy بارِ دیرتر را می‌گیرد. هر دو «نخستین»‌اند
ولی **کندل‌های متفاوتی**.

| فضای همپوشانی | مقدار |
|---|---|
| کانال (کدام الگوها) | ≈۱۰۰٪ |
| کندل (کدام بارِ ورود) | ≈۰٪ (روی عضوِ آزموده) |

⇒ منبعِ لبه **قیمتِ ورودِ متفاوت** است، نه غربالِ الگوها. تأخیرِ اندازه‌گیری‌شده
۱ تا ۷ بار.

**و لبه با دستِ بسته کسب شد:** افقِ نگهداریِ بازوی پایه به Kennedy تحمیل شد، پس
Kennedy دیرتر وارد می‌شود ولی همان تعداد بار فرصت دارد ⇒ پنجرهٔ کوتاه‌ترِ حرکت.

---

## ۶. روندِ یکنواخت — مکانیزم، نه تصادف

| تایم‌فریم | `Δ` هم‌سنگ |
|---|---|
| M5 (۵ دقیقه) | `-0.0208` |
| M15 (۱۵) | `-0.0082` |
| M30 (۳۰) | `+0.0085` |
| H1 (۶۰) | `+0.0688` |
| H4 (۲۴۰) | `+0.1399` |

`ρ_Spearman = +1.000` (یکنواختیِ کامل) · `P(ترتیبِ کامل تصادفی) = 1/120 = 0.0083`

**مکانیزمِ فیزیکی:** هرچه تایم‌فریم بالاتر، نسبتِ نویزِ درون‌کندلی به سیگنال
کمتر ⇒ شرطِ «کلِ دامنه باید عبور کند» اطلاعاتِ بیشتری حمل می‌کند. در M5 دامنهٔ
کندل عمدتاً نویزِ ریزساختاری است ⇒ شرط ضرر می‌زند (`Δ` منفی).

⇒ این دقیقاً توضیح می‌دهد چرا Kennedy قاعده‌اش را روی چارت‌های **روزانه/هفتگی**
تدریس می‌کند. **علامتِ اثر تابعِ تایم‌فریم است** — و این یک قانونِ قابلِ استفاده
برای لایه‌های آیندهٔ پروژه است.

---

## ۷. مقایسهٔ پیش‌بینی با نتیجه (صداقتِ پیش‌ثبت)

| پیش‌بینیِ ثبت‌شده | نتیجه | ارزیابی |
|---|---|---|
| «رد محتمل‌ترین برآمد است» | ۱ پذیرش از ۵ | ❌ **غلط** |
| بهبودِ `e_pip` روی ≥۳ کارت | طلا: M30,H4 · یورو: M15,H4 | ~نیمه‌درست |
| «هیچ پذیرشی در هیچ کارتی» | H4 پذیرفته شد | ❌ **غلط** |
| M5 بدتر می‌شود (نویزِ درون‌کندلی) | `Δ=-0.0208` | ✅ **درست** |
| نگه‌داشت ≈۴۰٪ | ۵۹–۶۵٪ | ❌ کم‌برآورد |
| نیازِ ۵۸٪ بهبودِ لبه | لبه ~۳× شد | ✅ سد رد شد |

**پیش‌بینی‌ام در جهتِ بدبینی غلط بود** — همان جهتی که یک پیش‌ثبت باید در آن
خطا کند. علتِ کم‌برآوردِ نگه‌داشت: شمارشِ هندسی روی «نخستین شکستِ هر کانال»
بود، در حالی که شمارشِ عملیاتی پس از قیدهای همپوشانی و امکان‌سنجی است.

---

## ۸. هشتِ اشتباهِ رایج — مدرکِ عدمِ ارتکاب

| # | اشتباه | مدرک |
|---|---|---|
| ۱ | تمرکز بر لایهٔ زمان‌محور | لایه **هندسی** است (کانالِ سه‌نقطه‌ای + تعریفِ شکست). هیچ متغیرِ ساعت/روز به کار نرفت. |
| ۲ | اجتنابِ از پیچیدگی | تمایزِ `close` در برابرِ `high/low` پس از حذفِ تکرار، و کشفِ اینکه زیرمجموعه‌بودن می‌شکند — تحلیلِ سه‌سطحی انجام شد، نه یک آزمونِ ساده. |
| ۳ | چند اندیکاتورِ ساده و اعلامِ سریعِ سوختگی | هیچ اندیکاتورِ کلاسیکی استفاده نشد؛ منبع یک **کتاب** بود. لایهٔ سوختهٔ `S373` **احیا شد**، خلافِ اعلامِ سریعِ مرگ. |
| ۴ | بروزرسانیِ سایت بدون `local-mobile` | در گامِ استقرار، `local-mobile` **اول** بروزرسانی می‌شود. |
| ۵ | تستِ یک تایم‌فریم | **هر ۵ تایم‌فریمِ مشترک** آزموده و گزارش شدند (۴ رد + ۱ پذیرش). |
| ۶ | `TP/SL` یکسان برای همه | براکتِ هر عضو `m·chan_h` و `s·chan_h` است — **متناسب با ارتفاعِ کانالِ همان لحظه**، پس روی هر کارت و هر سیگنال متفاوت. |
| ۷ | اعدادِ رند | `FAM_M=(0.618, 1.0)` · `FAM_S=(0.5, 0.786)` — نسبت‌های فیبوناچی، نه ۵۰/۱۰۰/۲۰۰. |
| ۸ | دورزدنِ معیار با `TP<SL` | `share_tp_lt_sl` ثبت می‌شود. مهم‌تر: آمارهٔ پذیرش **`meanR`** است نه `WR` ⇒ ترفندِ «TP کوچک ⇒ WR بالا» **ساختاراً بی‌اثر** است. |

---

## ۹. قوانینِ پروژه — رعایت‌شده

- **MTF:** ۵ کارت جداگانه، گزارشِ مرحله‌به‌مرحله، ذخیرهٔ افزایشی در git.
- **اندک اندک:** اجرا در پس‌زمینه؛ هر کارت پس از اتمام فوراً commit/push شد
  (M5+M15 ⇒ M30 ⇒ H1+H4).
- **همپوشانی:** در **دو فضا** اندازه‌گیری و گزارش شد (کانال ≈۱۰۰٪، کندل ≈۰٪).
  و پرسشِ «آیا بخشِ همپوشان به‌عنوان فیلتر ارزش دارد؟» دقیقاً همین آزمون بود.
- **جعبه‌ابزار:** منبع یک کتاب بود؛ راهِ حل از تعریفِ شکست آمد نه اندیکاتور.
- **بهبود:** این یک نمونهٔ خالصِ **راهِ دومِ پروژه** است — احیای لایهٔ سوخته.

---

## ۱۰. حکم و محدودهٔ استقرار

### ✅ `ACCEPT` — تنها `H4`، با برچسبِ **رتبهٔ محافظه‌کارانه**

**مجاز:** `XAUUSD-H4` و `EURUSD-H4`، با تعریفِ شکستِ `high/low`.

**قیدهای اجباریِ استقرار:**
1. سایزِ محافظه‌کارانه — کرانِ پایینِ بوت‌استرپِ خوشه‌ای `+0.0014` است.
2. طلا در `H4` تنها **≈۴ رویداد در سال** می‌دهد ⇒ کارت باید صراحتاً «کم‌بسامد»
   علامت بخورد و کاربر انتظارِ سیگنالِ مکرر نداشته باشد.
3. **ممنوع** برای `M5`/`M15`/`M30`/`H1` — روی این کارت‌ها یا بی‌اثر است یا
   زیان‌ده (`Δ` منفی در M5/M15).

### ⛔ آنچه انجام **نشد** (قفلِ ۱ پیش‌ثبت)
هیچ تعریفِ میانی آزموده نشد: نه نوارِ تحمل، نه نسخهٔ چند-کندلی، نه ترکیب. تنها
دو حالتِ پیش‌ثبت‌شده. ⇒ شمارشِ آزمون از ۱۱۲ فراتر نرفت.

### دفترِ چندگانگی
`۱۰۷ → ۱۱۲` (۵ کارت). سدِ `2.570` **پیش از** وجودِ نتیجه تثبیت شده بود.
