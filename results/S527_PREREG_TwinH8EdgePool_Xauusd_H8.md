# S527 — پیش‌ثبت (PREREG): استخرِ دو لبهٔ H8 دهکِ گاوس (S523-WPR-گیت‌شده + S526-سقف‌تازه) | XAUUSD-H8

**تاریخ:** 2026-09-02 · **نویسنده:** گاوس (قلمرو S520–S529) · **وضعیت:** منجمد قبل از هر تست
**مسیر:** B (صفر پارامتر آزاد) · **SEED:** 20260805

---

## ۰) مالکیت قلمرو

دهکِ من S520–S529؛ این S527 است (S526+1). هر دو عضو استخر از دهکِ خودم هستند —
هیچ لایهٔ همکاری لمس یا تجمیع نمی‌شود (S602/S770/S950/S965 فقط پیشینهٔ خواندنی).

## ۱) انگیزه — نجاتِ پیش‌ثبت‌شدهٔ S523-H8

- **S523-H8** (WPR-گیت‌شده): n=299، lift=+12.44، z=3.13، **۱۰/۱۱ گیت سبز** — فقط H5
  (توان) شکست. مسیر نجات ثبت‌شده در حکم S523: «~۲.۲× معاملهٔ بیشتر».
- **S526-H8** (سقف تازهٔ ۹۰-کندلی): n=200، lift=+14.77، z=3.23، **ACCEPT 88.2**.
- قانون استخر (S500/S431): تجمیع فقط با lift>0 هم‌جهت و هم‌مرتبه ⇒ +12.44 و +14.77 ✓
- رویدادها **متفاوت‌اند** (گذر WPR از −13 در فضای درفت ≠ لبهٔ تازهٔ سقف ۹۰-کندلی)؛
  برخلاف S520 (مضارب یک رویداد روی TFهای هم‌خانواده) اینجا دو مکانیزم مستقل روی
  **یک** کارت تجمیع می‌شوند — الگوی موفق S431 (قوانین ساختاری مستقل)، نه الگوی مردهٔ S520.
- پیشینهٔ تازه: S604 (استخر دو-عضویِ هم‌راستا، ACCEPT 80.7).

## ۲) قانون استخر (منجمد — عیناً دستورکار اثبات‌شدهٔ S431/S520)

- **عضو ۱ (S523-H8):** WPR(14) گذر بالای −13 & drift90>0 ⇒ LONG؛ SL=1.5×median(ATR100)،
  TP=1.5×SL (هندسهٔ per-card منجمد).
- **عضو ۲ (S526-H8):** لبهٔ تازهٔ `close>max(close,90).shift(1)` ⇒ LONG؛ همان هندسه.
- **ادغام:** نگاشت روی محور تقویمی مطلق، صفِ **FIFO بدون هم‌زمانی** (concurrency=1) —
  اگر سیگنالی هنگام معاملهٔ باز برسد، حذف می‌شود. اگر هر دو عضو در یک کندل سیگنال
  دهند، یک معامله (dedupe به نفع عضو ۱ — قراردادی، بی‌اثر بر PnL چون هندسه یکسان است).
- **نول ترکیبی (blended):** نول شرطی‌شدهٔ هر عضو (در فضای درفت-مثبت، K=2000،
  seed=20260805، سخت‌ترین استراید) با وزنِ سهمِ پس-از-FIFO ترکیب می‌شود
  (واریانس‌ها وزن می‌گیرند — عیناً `blend_pool_null` موجود در s520_pool_adjudicate).
- **داوری:** یک بار compute رسمی RQS2 v2.6 روی استخر. split_bar=0.70 عیناً هارنس.

## ۳) بودجهٔ چندگانگی — صادقانه

عضو ۱ وارث دودمان WPR است ⇒ استخر کل بودجه را به ارث می‌برد:
**n_trials = 23783 (S525) + 5 (S526) + 1 (این استخر) = 23789** ⇒ سد H5 ≈ z=4.61.
هیچ تخفیفی گرفته نمی‌شود. تنش با 30000 نیز گزارش می‌شود.

## ۴) ابطال‌گرها — منجمد

- **F1 (هم‌رویدادی پنهان):** اگر پس از نگاشت تقویمی، >40% ورودی‌های عضو ۲ درون
  پنجره‌های باز عضو ۱ بیفتد (یا برعکس) ⇒ اعضا مستقل نیستند؛ خطر S520؛ استخر فقط
  در صورت z نهایی بالای سد پذیرفته می‌شود و در غیر آن REJECT بدون نجات.
- **F2 (رقیق‌سازی):** اگر lift استخر < min(لیفت اعضا) − 2pp ⇒ FIFO لبه را خورده؛ REJECT.
- **F3 (توان):** اگر z_pool < 4.61 ⇒ POWER-LIMITED/REJECT طبق موتور؛ **بدون** حذف
  عضو، تغییر هندسه، یا هر جراحی پس‌ازآن.
- **پیش‌بینی شخصی گاوس:** FIFO ~10–20% حذف می‌کند (رویدادها متفاوت‌اند)؛
  n_pool ≈ 400–460؛ lift ≈ +12..+14؛ z ≈ 4.2–4.7 — **مرزی**. اگر بالای 4.61 رفت،
  نجاتِ رسمی S523 است؛ اگر نه، سند POWER-LIMITED قوی‌تر می‌شود و راه برای
  آیندهٔ داده باز می‌ماند.

## ۵) گردش‌کار

runner (بازاستفاده: signals هر دو عضو + FIFO + blend_pool_null از s520_pool_adjudicate)
→ اجرا → چک‌پوینت JSON فوری → ممیزی هم‌پوشانی استخر با لایو S382-H4 (قانون) →
MD حکم → گزارش (توقف فقط در صورت ACCEPT).

*کامیت این فایل = مهر زمانی پیش‌ثبت. هیچ عددی قبل از آن محاسبه نشده.*
