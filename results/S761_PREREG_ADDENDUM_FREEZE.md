# S761 — الحاقیهٔ انجماد (پیش از هر تماس با هولدآوت)

> **نویسنده:** آپولونیوس پرگایی · بلوک S760–S769 · تاریخ: 2026-08-26
> جستجو فقط روی ۶۰٪ نخستِ هر کارت انجام شد (`results/_scan_S761/*_search.json` —
> per-TF کامیت‌شده). **۴۰٪ هولدآوت تا این لحظه هرگز شبیه‌سازی نشده است.**

## ۱) کارت‌های منجمد — فقط عبورکنندگان از سدّ lift×√n ≥ 78

| کارت | side | mode | W | θ | n(train) | WR | lift | exp_pip | score |
|---|---|---|---|---|---|---|---|---|---|
| **H1** | short | cont | 21 | 0.555 | 82 | 56.10% | **+15.83pp** | +19.73 | **143.4** |
| **D1** | short | cont | 8 | 0.333 | 39 | 56.41% | **+18.71pp** | +165.1 | **116.9** |

هندسه (از پیش‌ثبت، جستجو نشد): `SL=1.618×ATR(89)` علّی per-bar · `TP=1.5×SL` · `max_hold=34` ·
`allow_overlap=False` · اسپرد از `se.ASSETS`.

## ۲) NO_CANDIDATE — هولدآوت این ۶ TF هرگز لمس نمی‌شود

| TF | بهترین score | دلیل |
|---|---|---|
| H2 | 76.3 (short) | زیر سد (فاصلهٔ ۱.۷ — نه، سد سد است) |
| H3 | 64.6 | زیر سد |
| H4 | 33.0 | زیر سد |
| H6 | 61.0 | زیر سد |
| H8 | 28.4 | زیر سد |
| H12 | 40.2 | زیر سد |

## ۳) مشاهدهٔ ساختاری (پیش از هولدآوت، فقط از TRAIN)

جهتِ برنده در همهٔ TFها **short-cont** بود (غلبهٔ واریانسِ منفی ⇒ ادامهٔ نزولی) — سازگار با
عدم‌تقارنِ شناخته‌شدهٔ «نوسانِ بد» (Patton-Sheppard). سمتِ long ضعیف‌تر بود؛ همین عدم‌تقارن
پیش‌بینیِ نظریه است، نه نقضش.

## ۴) پروتکل هولدآوت (تغییرناپذیر)

- هر کارت **یک** آزمون: `python3 strategies/s761_signed_semivariance.py holdout <TF>`
- فایل نگهبان `{TF}_JUDGED` تستِ دوم را می‌بندد. شکست = مرگ ابدی این پیکربندی‌ها.
- نول: uncond + K=2000 پرموتیشن روی کندل‌های معتبرِ هولدآوت با n_holdout. SEED=20260826+761.
- حکم با **هر دو** `n_trials=1` (Path C) و `n_trials=96` (سخت‌گیرانه) گزارش می‌شود — عیناً از موتور.

— آپولونیوس 📐
