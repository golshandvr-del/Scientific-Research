# S986 — الحاقیهٔ انجماد: حکم رسمی موتور (یک شلیک)

**تاریخ: 2026-09-09 — کامیت‌شده *قبل از* اجرای داور (نظم مسیر C).**
پیش‌ثبت: `results/S986_PREREG_SHOCK_EXTREME_BREAK.md` (ddd5c3d9). اسکنر: `strategies/s986_shock_extreme_break_scan.py` (کامیت پیش از اجرا).
اسکنِ ۱۹ کارت روی نیمهٔ اول کامل و کامیت‌شده است (`results/_s986/scan_*.json`، چک‌پوینت به‌ازای هر کارت). داده: `data/mt5_full/` (assert در کد).

## ۱) نتیجهٔ گام ۱ (اسکنِ نیمهٔ اول) — سدِ توان **رد شد**

| کمیت | مقدار | میله |
|---|---|---|
| بیشینهٔ z مثبت | **+2.55** (D1, θ=1.618, W=8, **mirror**, SL_k=1.8, RR=1.6, n=73, lift=+14.57pp) | `expected_max_z(608)=3.11` (محافظه‌کار 3.42) |
| بهترین بازوی اصلی (main) | H8, θ=2.618, W=8, SL_k=1.8, RR=1.6, n=74, lift=+12.18pp, z=+2.15 | — |
| کارت‌های بی‌بازو (n<30) | MN1, W1 | — |
| n_trials صادقانه | **608** | — |

**پیش‌بینی‌های ثبت‌شده:**
- (الف) زیرِ M30 سمّی — **تأیید** (M1 میانگین z=−213، M3 −55، M15 −9.5، M30 −5.0؛ هفتمین تکرار).
- (ب) main > mirror — **تأیید در قلمروِ H6–H8** (H8: main +0.68 vs mirror −1.50؛ H6: +0.58 vs −1.62) اما **رد در D1** (mirror +1.50 vs main −0.99). روی D1 شکستِ اکسترمم پس از شوکِ روزانه، fade می‌دهد نه ادامه — ولی زیرِ نویز.
- (ج) قلمروِ مثبت H3–H12 — **تأیید ضعیف** (H6/H8/H12/D1 مثبت، H2–H4 صفر).
- (د) **P1 — رد شد (مهم‌ترین یافته):** روی H8 (بهترین کارتِ main) کنترلِ ورودِ فوری در closeِ شوک با همان θ=2.618 و همان هندسه: **lift=+16.91pp, z=+3.58, n=107** در برابرِ بازوی شکست **+12.18pp, z=+2.15, n=74**. همین در H6 (کنترل +9.35 z=2.60 n=185 vs شکست +9.38 z=1.67 n=76) و H4 (کنترل +7.02 z=2.44 vs شکست +0.45). ⇒ شرطِ «عبور از اکسترممِ شوک» **فیلترِ توان‌سوز** است: n را یک‌سوم می‌کاهد و لیفت را نمی‌افزاید. **مکانیزمِ S844 («کندلِ تأیید حرکت را مصرف می‌کند») به تأییدِ قیمتی هم تعمیم می‌یابد.** طبق پیش‌ثبت §۰/۱، لایه در سطحِ پیش‌ثبت شکست‌خورده اعلام می‌شود؛ حکمِ رسمی همچنان صادر می‌شود.
- (ه) θ=2.618 > θ=1.618 — **تأیید** (main H1–H12: +0.49 vs −1.68pp).

**نکتهٔ صادقانه دربارهٔ کنترلِ P1:** z=+3.58 کنترلِ H8 از سدِ نویز می‌گذرد، ولی این کنترل (۱) بازوی داوریِ این لایه نیست و در پیش‌ثبت صریحاً از داوری کنار گذاشته شده، (۲) دقیقاً همان رویدادِ S965-baseline (شوک 2.618×ATR21، follow، بدونِ ρ) است که همکارِ Kyle آن را گزارش کرده (کشفِ H8: lift +11.81pp) و با ρ به ACCEPT رسانده. **هیچ ادعای تازه‌ای روی آن نمی‌کنم و هولد-اوت را برای آن لمس نمی‌کنم.**

## ۲) پیکربندیِ منجمد برای شلیکِ رسمی

قاعدهٔ انتخاب (یکسان با S984/S985): **بازوی با بیشترین z در اسکنِ نیمهٔ اول.**

| پارامتر | مقدار |
|---|---|
| کارت | **XAUUSD-D1** (`data/mt5_full/XAUUSD_D1.csv`) |
| رویداد | `shock_candles(θ=1.618)` → `break_signals(W=8)` — عینِ اسکنر |
| جهت | **mirror** (شکستِ صعودی ⇒ SHORT، شکستِ نزولی ⇒ LONG — fade) |
| SL_k / RR | **1.8 / 1.6** (TP>SL) |
| sl_base_pip | منجمد از `scan_D1.json` (نیمهٔ اول) |
| max_hold | 32 · `allow_overlap=False` |
| split_bar | `n_all//2` |
| مدلِ صفر | `build_null_fast` K=600، seed=20260806، هر دو سمت |
| n_trials | **608** |
| داور | `strategies/s986_judge.py` |

**پیش‌بینیِ از-پیش-اعلام‌شده:** REJECT یا POWER-LIMITED (n≈73 در نیمه؛ z=2.55 زیرِ میله؛ جهتِ mirror خلافِ قانونِ ۶بار-تکرارشدهٔ main>mirror ⇒ احتمالِ بالای نویز). این شلیک برای اجرای دستورِ «حکمِ رسمیِ موتور برای هر شماره» است. پس از حکم هیچ پارامتری تغییر نمی‌کند و S986 بسته می‌شود.

— آندری کولموگوروف
