# S563 — PREREG — تداوم ساعت اول روز با دروازهٔ Drift (XAUUSD، M1/M5/M15/M30/H1)

**تاریخ ثبت**: 2026-08-20 — کامیت **قبل از هر تست** (git timestamp = preregistration).
**دانشمند**: لایب‌نیتس — بلوک S560–S569. **EURUSD حذف به دستور کاربر.**

## 1) فرضیه و منابع

گنجینهٔ Handoff §5.2: «۷۲٪ رشد طلا در اولین ساعت روز معاملاتی رخ می‌دهد». خانوادهٔ GapOpen من (S560/S562، ۴ ACCEPT) فقط زیرمجموعهٔ گپ‌دار این پدیده را برداشت می‌کند (~۲۰٪ روزها). **فرضیهٔ S563**: خودِ ساعتِ اول، مشروط به رژیم صعودی، لبهٔ مستقل دارد:
- **دروازهٔ Drift**: تعریف پذیرفته‌شدهٔ دانشمند موازی S950 (تأییدشده در S523 نیز): روند ۹۰-واحدی علّی مثبت.
- **فیلتر V**: قانون DD-انتقال سه‌بار-تأییدشده (S404/S561/S562).

این لایه «احیا» نیست — رویداد جدید است (ورود بی‌قیدِ گپ در open هر روز). هیچ لایهٔ ACCEPT موجودی day-open بی‌قید را معامله نمی‌کند (بررسی آرشیو: S404=گپ‌فیل مشروط به گپ، S560/S562=مشروط به گپ، S770=state-cross درون‌روز، S800=squeeze).

## 2) تعریف مکانیکی (منجمد)

- شکست روز: `thr=max(1800s, 1.5×TF_sec)` (BUG-BRKTHRESH-safe؛ عین S560).
- سیگنال روی آخرین کندل روز k−1؛ ورود موتور در open اولین کندل روز k (si+1، علّی).
- **Drift(k)**: `close(آخرین کندل روز k−1) > close(آخرین کندل روز k−91)` — ۹۰ روز معاملاتی کامل، اکیداً علّی؛ تا ۹۱ روز تاریخچه → رد.
- **فیلتر V(qv)**: عیناً تابع S562 (`vol_ref` = میانگین دامنهٔ ۱۴ روز کامل قبل > چندک qv رولینگ ۲۵۰روزهٔ علّی، min 60).
- LONG-only. هندسه: قاعدهٔ ثابت V-TIME (SL=TP=q98(|MFE|∪|MAE|) از نیمهٔ اولِ بازوی قفل‌شده؛ خروج زمانی حاکم).

## 3) فضای بازوها

فیلتر ∈ {NAKED (هیچ), D (drift), D+V78, D+V85} × hold ∈ {۶۰min, ۱۲۰min} = **۸ بازو/TF**
hold بر حسب کندل: M1:{60,120}، M5:{12,24}، M15:{4,8}، M30:{2,4}، H1:{1,2}.
**n_trials = 8×5TF = 40** (خانوادهٔ جدید؛ خویشاوندی با GapOpen در گزارش هم‌پوشانی افشا می‌شود).

## 4) مسیر — Path C

قفل هر TF روی نیمهٔ اول فقط (split 2018-10-20)؛ معیار: t نیمهٔ اول، تساوی(<0.01)→DD کمتر؛ تک‌لمس نیمهٔ دوم؛ حکم فقط `compute_rqs2` (gates_only، ۵ ورودی الزامی، null بازو-محور — فیلترها جزو مهارت، استخر perm بی‌قید)، K=500، **seed=20260816**، n_trials=40، split_bar عین S560. غربال توان قبل از داوری؛ n<30 → بدون حکم.

## 5) پیش‌بینی‌ها (قبل از دیدن هر عددی)

- **P1**: بازوی NAKED در قفل نمی‌برد (لبهٔ بی‌قید < 4pp — وگرنه بازار آن را خورده بود).
- **P2**: بازوی drift-دار در حداقل یکی از M5/M15 قفل می‌شود و z≥3.09 کل‌داده می‌گیرد؛ ACCEPT ممکن.
- **P3**: برخلاف خانوادهٔ گپ، قید مقیّد این‌جا **H3 (lift)** خواهد بود نه H8 — چون رویداد روزانه/کم‌هیجان است.

## 6) توقف، صداقت، هم‌پوشانی

بازوی قفل‌شده رد شد → هیچ بازوی دیگری لمس نمی‌شود؛ verdict verbatim. در صورت ACCEPT: هم‌پوشانی روزانه/لحظه‌ای vs S560-M5، S562-M15/H1، S404-M30، S355 + بند 3 (تست بخش هم‌پوش به‌عنوان فیلتر) فوراً اجرا می‌شود. انتظار صادقانه: هم‌پوشانی روزانهٔ بالا با خانوادهٔ گپ در روزهای گپ‌دار؛ ارزش افزوده باید در روزهای بی‌گپ اثبات شود (در گزارش، برش گپ‌دار/بی‌گپ الزامی است).

*لایب‌نیتس — S563 | Path C | seed=20260816*
