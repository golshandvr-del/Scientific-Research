# S503 — پیش‌ثبت (PREREG): قانون Resumption علّی S356 روی تایم‌فریم‌های بکر H2/H3 | XAUUSD

> **این سند قبل از هر آزمون قضاوت‌شده commit می‌شود.** (مسیر C)
> دهه: S500–S509 (قلمرو ارشمیدس). S503 آزاد است (بررسی ۲۰۲۶-۰۸-۳۰).

## ۰) پیش‌نیازهای قانونی
- fetch گیتهاب ✅ (تا head `1da1d1f5` + بررسی S913/S627: هر دو مکانیزم متفاوت‌اند —
  S913 = گذر RSI-21/55 (optimism)، S627 = تقاطع درفت-۹۰ به‌عنوان رویداد. این‌جا
  state-machine کانالِ Brooks (اسپایک→رنج→شکست) است؛ S627 خودش در ممیزی بداعتش
  صراحتاً این تمایز را ثبت کرده ✅
- داده: `data/mt5_full/` unpack و verify شد (سالم=۱۹، ناهمسان=۰) ✅

## ۱) شکاف شواهد (چرا H2/H3؟)
سوییپ ۱۶-کارتی S356-ACCEPT (rqs2=80) گرادیان lift طلا را داد:
`M5:+9.6 → M15:−0.9 → M30:−2.7 → H1:+15.0 → H4:+4.4` — H1 «قلهٔ محلی روی تیغه».
**H2 و H3 هرگز آزموده نشدند** (در زمان S356 داده‌شان موجود نبود؛ mt5_full بعداً با S434 آمد).
اگر قله واقعی است نه artifact، همسایه‌های H2/H3 باید lift مثبتِ هم‌مرتبه نشان دهند؛
اگر صفر/منفی بودند، «تیغه» ادعای S356 تیزتر از تصور است — هر دو نتیجه ارزش علمی دارد.

## ۲) قانون منجمد (همه از S356-ACCEPT، صفر جاروب)
```
سیگنال = build_signals_causal(df, 'XAUUSD', tf, n_open_frac=0.13, late_hour=16,
                              spike_k=0.8, tight_atr=12.0)  ∧  regime_gate(r2_fib_55 ≥ 0.45)
SL = round(1.3 × ATRpip, 1) ، TP = 2×SL (rr=2 ≥ 1 ✅) ، LONG-only ، allow_overlap=False
داده = data/mt5_full/XAUUSD_H2.csv و XAUUSD_H3.csv (کل ۲۰۱۱→۲۰۲۶، ۱۵.۶ سال)
```

### ثابت‌های ساختاری per-TF (پیش‌ثبت — تنها بخش جدید، با فرمول نه جاروب)
- `bars_per_day`: H2=12، H3=8 (واقعیت تقویمی، پارامتر نیست)
- `ATR period` = 21 (هر دو همسایهٔ H1 و H4 در دیکشنری منجمد S354 مقدار 21 دارند ⇒ وراثت یکتا)
- `max_hold`: درون‌یابی لگاریتمی زمانِ نگه‌داری بین H1 (20h) و H4 (40h) بر حسب لگاریتم TF:
  H2 → 20×2^(1/2)≈28.3h ⇒ **mh=14 بار**؛ H3 → 20×2^(log2(3)/2)≈34.6h ⇒ **mh=12 بار**
  (فرمول یکتا و بسته؛ هیچ مقدار جایگزینی آزموده نمی‌شود)

## ۳) طرح آزمون و قضاوت
1. **دروازهٔ بازتولید**: H1 با همین خط لوله باید n=117، WR=51.28 بدهد (بیت‌سازگار با S356/S500). شکست ⇒ STOP.
2. هر کارت (H2، H3) جدا: null کانونی اندازه‌گیری‌شده (`build_null_canonical`، K=2000، seed=20260813)،
   یک فراخوان `compute_rqs2` v2.6 با holdout ۴۰٪ آخر (SPLIT_FRAC=0.60).
3. **استخر شرطی H2+H3** (قید پیش‌ثبت‌شده، درس رقیق‌سازی S500): فقط اگر هر دو lift مثبت و
   هم‌مرتبه (نسبت lift بزرگ به کوچک ≤ 2.5) و χ² همگنی p≥0.05 ⇒ تجمیع FIFO تقویمی +
   نول ترکیبی وزنی (روش S431/S500) + یک فراخوان داور. در غیر این صورت استخر اجرا نمی‌شود.
   عضو H1 به استخر **اضافه نمی‌شود** (لایهٔ زندهٔ S356 است؛ درس S500: تجمیع با آن dilution داد).

## ۴) ابطالگرهای پیش‌ثبت‌شده (self-REJECT بدون داور)
- **F1**: هر کارتی با lift ≤ 0 ⇒ همان کارت مرده (بدون فراخوان داور برای آن کارت).
- **F2**: هر کارتی با n < 60 ⇒ کم‌توان، بدون داوری (گزارش صرف).
- اگر هر دو کارت F1/F2 خوردند ⇒ کل S503 self-REJECT؛ یافتهٔ «تیغهٔ تیز H1» ثبت می‌شود.

## ۵) حسابداری n_trials (صادقانه)
- ارث خانوادهٔ S354/S356/S500: **51**
- S503: ۲ کارت + ۱ استخر شرطی = **3**
- **n_trials = 54** ⇒ کران H5 ≈ 2.35σ — گیت مقید H3 (z≥3.09) است.

## ۶) ممیزی هم‌پوشانی (الزامی پیش از حکم نهایی)
- vs **S356-H1 لایو** (همان مکانیزم!): نگاشت زمانی ورودهای H2/H3 به پنجره‌های سیگنال H1
  (±1 کندل H1) — گزارش درصد و lift زیرگروه مستقل (nonOV).
- vs **S382-H4 لایو**: همان روش (S500 نشان داد H4 با S382 ~69٪ ±2 همپوشان بود؛ H2/H3 باید سنجیده شود).

## ۷) ثابت‌های منجمد
SEED=20260813 · K_PERM=2000 · SPLIT_FRAC=0.60 · pip=0.10 · spread=3.3 · comm=0 · N_TRIALS=54

## ۸) تعهد
REJECT ⇒ گزارش کوتاه + حرکت خودمختار به S504. ACCEPT ⇒ توقف و گزارش به فرمانده.

— ارشمیدس، ۲۰۲۶-۰۸-۳۰
