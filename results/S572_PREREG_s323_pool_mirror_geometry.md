# S572 — پیش‌ثبتِ الزام‌آور: استخرِ S323 با هندسهٔ آینه‌ای (TP≥SL)

**تاریخ:** 2026-08-19 · **نویسنده:** پوانکاره (جلسهٔ S572؛ قلمروِ این دانشمند S570–S579)
**وضعیت:** منجمد — پیش از هر اجرای تعیین‌کننده کامیت می‌شود.
**والد:** S571 (ACCEPT شکنندهٔ 79.9، کامیت 629e2a78) ← S570 (REJECT 23، فقط H5) ← S323.

---

## ۱) انگیزه (از یافته‌های ثبت‌شده، نه از داده‌کاوی جدید)

S571 با هندسهٔ موروثی SL=58.5/TP=40.5 پذیرفته شد، اما دو تنش صادقانه افشا شد:

1. **RR=0.69<1** — در تضاد با قانونِ «حفظ بودجه» (ترجیح TP>SL) مأموریت.
2. **شکنندگی تنش** — با بودجهٔ 5602، z به‌اندازهٔ ۰.۰۴۶σ کم می‌آورد.

پرسش S572: آیا لبهٔ ساختاریِ S323 (پول‌بک به S/R در روند طلایی) از هندسه مستقل است؟
اگر بله، واریانت TP≥SL باید زنده بماند و قانون حفظ بودجه را هم ارضا کند.

## ۲) تعریفِ دقیقِ واریانت — صفر پارامترِ آزاد

**تبدیلِ آینه‌ای:** برای هر عضو، `slMult ↔ tpMult` جابه‌جا می‌شود. هیچ عدد جدیدی
معرفی نمی‌شود؛ اعداد همان اعدادِ منجمدِ DEPLOYED_CFG هستند:

| کارت | SL قدیم | TP قدیم | SL جدید | TP جدید | RR جدید |
|---|---|---|---|---|---|
| XAUUSD-M30 | 2.1·ATR | 1.3·ATR | **1.3·ATR** | **2.1·ATR** | 1.62 |
| XAUUSD-H1 | 1.8·ATR | 1.7·ATR | **1.7·ATR** | **1.8·ATR** | 1.06 |

- سیگنال‌ها: عیناً `signals_backtested` منجمد (S570) — دست نمی‌خورد.
- maxHold: عیناً منجمد (M30=48، H1=36).
- استخر: عیناً دستورکارِ S571 (pool_cards، محور ۳۰دقیقه‌ای مصنوعی، close از H1
  با نگهداشت آخرین مقدار، holdout در صدک ۶۰٪ زمان ورود، هندسهٔ وزنی به سهم
  پس-از-FIFO، نولِ blend وزنی).
- نول: `build_null` با همان SL/TP جدید بازساخته می‌شود (K=2000؛ بذرها 23/101/777؛
  بذر حاکم = 23).

## ۳) بودجهٔ چندگانگی و قیود

- **n_trials = 2802** (میراثِ S571=2801 + این یک تصمیمِ آینه‌ای) ⇒ میلهٔ H5 = **3.5374**.
- **تنش = 5604** ⇒ میله = **3.7167** (گزارش الزامی؛ برچسبِ شکنندگی مثل S571).
- **C5″-الف:** هر عضو باید lift ≥ +4pp و z_proxy > 2 داشته باشد؛ وگرنه از استخر حذف
  (همان منطق pool_cards) و اگر فقط یک عضو ماند ⇒ گزارش تک‌کارت، بدون استخر.
- **C5″-ب:** چون TP دورتر است WR پایه پایین‌تر می‌آید؛ قیدِ رقیق‌سازی روی lift استخر:
  **lift_pool ≥ +8.0pp**، وگرنه REJECT-STOP (بدون داوری RQS2؛ بدون تلاش مجدد).
- **قید RR:** هندسهٔ وزنیِ نهایی باید TP≥SL بدهد (طبق ساخت، تضمینی است؛ چک می‌شود).

## ۴) معیارِ حکم (یک داوری، بدون تکرار)

- داوری با `compute_rqs2` (holdout_mask، allow_overlap=False)، بذر حاکم 23،
  حساسیت 101/777. حکم موتور **عیناً** گزارش می‌شود.
- اگر honest=ACCEPT اما stress=REJECT ⇒ برچسبِ «ACCEPT شکننده» + ارجاع به کاربر
  پیش از هر استقرار (همان بند §۵ پیش‌ثبت S571).
- اگر REJECT ⇒ مرگ ابدیِ این واریانت؛ تجربه ثبت می‌شود؛ ادامه با S573.
- **هیچ EURUSD** — فقط XAUUSD (استثنای صریح مأموریت).

## ۵) چه چیزی این آزمون را از ۸ خطای رایج مصون می‌دارد

1. پیش‌ثبت پیش از اجرا (این فایل، کامیت‌شده). 2. صفر پارامتر آزاد (تبدیل آینه‌ای
قطعی). 3. n_trials صادقانه با میراث کامل. 4. نولِ اندازه‌گیری‌شده با هندسهٔ خودِ
واریانت. 5. holdout زمانی BUG-SPLITDIR-safe. 6. محور مصنوعی BUG-QUANT/BUG-SPAN-safe.
7. قید توقفِ C5″-ب پیش از دیدن نتیجه. 8. تعهد به گزارش عینی حتی در شکست.
