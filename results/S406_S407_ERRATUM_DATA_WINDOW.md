# اصلاحیهٔ رسمی — S406 و S407 روی دادهٔ ناقص M15 اجرا شدند

**تاریخ:** 2026-09-04 · **بلوک:** S400–S409

## خطا
`data/XAUUSD_M15.csv` (نسخهٔ کوتاه پروژه) فقط **150,000 کندل، 2020-02-20 → 2026-07-07** را دارد.
دادهٔ کامل پروژه در `data/mt5_full/XAUUSD_M15.csv.gz` است: **363,778 کندل، 2011-01-03 → 2026-08-07 (15.6 سال)** — همان منبعی که S560/S562 استفاده کرده‌اند.

S406 (REJECT 29.5) و S407 (ACCEPT 98.1) هر دو `se.load_data('data/XAUUSD_M15.csv')` را خوانده‌اند ⇒ فقط ~۶ سال از ۱۵.۶ سال. `SPLIT_BAR['M15']=75000` هم روی همان فایل کوتاه تعریف شده بود (≈2023) و با split M30 (2018-10-25) هم‌تاریخ نیست.

**S400–S405 تحت تأثیر نیستند:** M30/H1 نسخهٔ کوتاه ۱۰۰٪ با mt5_full هم‌پوشان است (MANIFEST: overlapping_bars=181383, identical=100%). S400-M15 (REJECT) هم روی فایل کوتاه بود؛ حکمش REJECT است و تغییر جهت نمی‌دهد اما با همین قید علامت‌گذاری می‌شود.

## اقدام
- **S407 ACCEPT 98.1 ⇒ وضعیت: `ACCEPT-INVALID-DATA` — به‌عنوان لایهٔ سالم شمرده نمی‌شود.** فایل حکم دست‌نخورده می‌ماند (شواهد تاریخی)؛ این اصلاحیه بالای آن قرار می‌گیرد.
- **S406 REJECT 29.5 ⇒ `REJECT-INVALID-DATA`.** (REJECT می‌ماند؛ دلیل مرگ بدون V معتبر است اما اعداد روی داده ناقص‌اند.)
- ممیزی هم‌پوشانی S407 نیز فقط روی پنجرهٔ ۲۰۲۰→۲۰۲۶ معتبر است.
- شماره‌های S406/S407 مصرف شده‌اند و بازپس نمی‌گیرم (قانون: هر شماره یک لایه). n_trials تجمعی (280) نیز کاهش نمی‌یابد — آزمایش انجام شده، شانسش شمرده می‌شود.
- **S408** = همان پرسش S407 روی دادهٔ کامل M15 با پیش‌ثبت مستقل، split هم‌تاریخ با M30 (2018-10-25 11:30 ⇒ bar 180896 در فایل کامل)، n_trials=286.

## درس روش‌شناسی (برای همهٔ بلوک‌ها)
همیشه قبل از تیون، `first_utc/last_utc/bars` دادهٔ بارگذاری‌شده را چاپ و در پیش‌ثبت ثبت کن و با `data/mt5_full/MANIFEST.json` مقایسه کن. از این لایه به بعد این خط در همهٔ اسکریپت‌های بلوک اجباری است.

*هیچ تغییری در سایت. هیچ فایل بلوک دیگری لمس نشد.*
