# S764 — پیش‌ثبت: «کاراییِ مسیرِ رتبه‌بندی‌شدهٔ علّی در شوک» (Causal-Ranked Path Efficiency Shock) · XAUUSD

> **نویسنده:** آپولونیوس پرگایی · **بلوک مجاز:** S760–S769 (S764 = S763+1)
> **تاریخ:** 2026-09-05 · کامیت **پیش از هر کد/عدد**. · **دارایی:** فقط XAUUSD · **داده:** `data/mt5_full/`، src در هر کارت · **داور:** rqs2 v2.6 عیناً.

## ۰) fetch و مرور
پس از S763 هیچ ACCEPT تازه‌ای نرسیده (git log از `1bb178e8`). S1520 (ρ روی سقفِ تازه، H8، 90.6) بازخوانی شد: «کجا» (لنگر) × «چگونه» (کیفیتِ درونی) الگوی برنده است.

## ۱) رابطه با S763 — افشای بدهیِ خانوادگی
S763 نشان داد E = |c−o|/Σ|Δc| در دو TF مستقل بر کنترلِ شوک-تنها غالب می‌شود (P1 ✓) و هولدآوتش مثبت ماند، اما به‌دلیلِ **مقیاس‌ناوردا نبودنِ E** (مخرج ~√N) در H6/H8/H12 رویدادی نگذاشت و n=69 توان نداشت.
S764 **همان بُعد** را با تعریفِ مقیاس‌ناوردا می‌سنجد ⇒ خانوادهٔ مشترک. بدهی: n_trials سخت‌گیرانه = 30 (S763) + 30 (S764) = **60**؛ حکم با n_trials=1 (Path C) و 60 گزارش می‌شود. اگر S764 هم پاس نکند، این بُعد در بلوکِ من **بسته** می‌شود (دو شلیک، نه بیشتر).

## ۲) تعریفِ دقیق (علّی)
- E[t] مانند S763 (زیرکندل M5؛ D1: H1).
- **رتبهٔ علّی:** `Q[t] = سهمِ شوک‌های W=100 شوکِ قبلی (بستهٔ گذشته، بدونِ t) که E‌شان < E[t]` ∈ [0,1] — فقط بین شوک‌ها مقایسه می‌شود، پس مقیاسِ TF حذف می‌شود. warm-up: ۱۰۰ شوکِ قبلی موجود.
- شوک: `high−low ≥ 1.618×ATR21[t−1]`. LONG: شوک ∧ close>open ∧ Q≥q* (+درفت ۶۰روزه اختیاری: H4:360، H6:240، H8:180، H12:120، D1:60). SHORT آینه. follow.
- کنترل: q*=0 (شوک-تنها با همان warm-up تا مقایسه منصفانه باشد).
- ورود openِ t+1.

## ۳) خانوادهٔ منجمد (۳۰)
TF∈{H4,H6,H8,H12,D1} × q*∈{0 کنترل, 0.5, 0.75} × gate∈{none,drift}. سمت‌ها جدا (H4 موتور). هندسه پیشینی: SL=1.272×ATR21[t−1]، TP=1.618×SL، max_hold=16، allow_overlap=False. W=100 منجمد (جستجو نمی‌شود).

## ۴) پیش‌بینی‌های ابطال‌پذیر
- P1: بازویِ q*>0 باید lift > کنترلِ هم‌گیت/هم‌سمت داشته باشد؛ وگرنه بُعد بی‌اطلاع و لایه غیرقابلِ استقرار حتی با حکمِ مثبت.
- P2: q*=0.75 ≥ q*=0.5 در lift (یکنوایی) — S763 نتوانست بسنجد؛ اینجا حتماً n دارد.
- P3: Jaccard با S965 گزارش می‌شود؛ >0.5 ⇒ «مشتق».
- پیش‌بینیِ شخصی: H8 drift-long q*=0.75: n≈۹۰–۱۲۰ در ۶۰٪، lift +8..+12pp. اگر lift<+4pp ⇒ فرضیه مرده.

## ۵) مسیر C
جستجو ۶۰٪؛ سد `lift×√n≥78 ∧ exp>0 ∧ n≥30 ∧ q*>0 ∧ P1`؛ یک کارت per TF per side؛ یک هولدآوت با نگهبان؛ نول uncond + K=2000 با n_holdout؛ SEED=20260905+764.

## ۶) بدترین خبر اول
شوک‌های 1.618ATR روی H8 در ۶۰٪ ≈ ۶۰۰ ⇒ پس از رتبه‌بندی q*=0.75 و درفت ≈ ۷۰–۱۰۰ رویداد؛ برای z≥3.09 با lift +10pp نیاز n≈۱۵۰ روی کل داده — مرزی. D1 قطعاً کم‌شمار.

— آپولونیوس پرگایی 📐
