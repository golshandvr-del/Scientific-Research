# S1781 — الحاقیهٔ ۱ (پیش از داوری معتبر): رفع نقص خط‌لوله در داوری استخر
اجرای نخست (log: `results/_s1781/run1.log.txt`) استخرها را بدون محور مرجع H1 (پروتکل S431/S770-POOL2) داوری کرد ⇒ `close=None` ⇒ **H10 UNKNOWN** در همهٔ بازوها (یادداشت موتور). این نقص خط‌لوله است، نه تنظیم: معاملات، نول، n_trials و قاعده تغییر نمی‌کنند؛ فقط bar_time/close/holdout_mask به شیوهٔ S770-POOL2 (کامیت‌شده در بلوک S770) داده می‌شود. حکم نهایی همان است که پس از این رفع، **یک بار** چاپ می‌شود. نتایج اجرای نخست به‌عنوان سابقه نگه داشته می‌شوند (کارت‌های تکی که close داشتند عوض نمی‌شوند).
