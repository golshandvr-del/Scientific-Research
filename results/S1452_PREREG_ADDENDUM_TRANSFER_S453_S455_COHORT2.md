# S1452 — پیش‌ثبتِ الحاقی: انتقالِ دو قاعدهٔ PASS دههٔ اول (S453 رکودِ زمانی، S455 شکستِ نجاتِ 𝔻𝔻) و ترکیبشان به cohort 2

**تاریخ:** 2026-09-04 · **پروتکلِ دهه:** `results/S1450_PREREG_MGMT_DECADE2_PROTOCOL_AND_TP_EXTENSION.md` §۰–۱ · **معیار:** §۴ مادر · **وضعیت:** کامیت‌شده **قبل از هر اجرا**.

## ۱) انگیزه
- دو قاعدهٔ PASS دههٔ اول هر یک روی **یک** لایه پاس شدند (S453→S382_H4، S455→S312_H1). پرسشِ علمیِ طبیعی: تعمیم‌پذیرند یا خاصِ همان لایه؟ cohort 2 چهار لایهٔ **تازه** با موتورِ s382 (بدونِ mh، ورود close، SL-first) دارد — دقیقاً جنسِ S382_H4 که S453 روی آن پاس شد.
- نقدِ دههٔ اول (ضعفِ ۵): ترکیبِ S453+S455 هرگز اندازه‌گیری نشد. اینجا به‌عنوانِ واریانتِ سوم اندازه‌گیری می‌شود.
- لایه‌های cohort 2 (S526/S1511/S589) در ACCEPTهای خودشان **هیچ خروجِ زمانی** ندارند ⇒ طبقِ درسِ (c) دههٔ اول، S453 روی آن‌ها معنادار است.

## ۲) واریانت‌های قفل‌شده (بدونِ جست‌وجوی پارامتر — همه از برنده‌های قفل‌شدهٔ قبلی)
- **V_AGE:** عینِ S453 برندهٔ S382_H4: در close کندلِ e+15 (تک‌ارزیابی) اگر fl ≤ ۰ ⇒ خروج در close. k=15 برای همهٔ TFها (H4/H6/H8) **ثابت** — عمداً بازتنظیم نمی‌شود (آزمونِ انتقالِ خام).
- **V_DDF:** عینِ S455 V_DDFAIL: در ضرر، 𝔻𝔻 کامل شد ⇒ یک کندل مهلت؛ اگر 𝕌 نیامد و هنوز در ضرر ⇒ خروج در close. الفبای علّیِ ۵۰۰.
- **V_BOTH:** اجتماعِ دو قاعده (هرکدام زودتر شلیک کرد).
- برنده در نیمهٔ اول = بیشترین بهبودِ maxDD میانِ پاس‌شده‌ها. حداقلِ ۲۰ تغییر.

## ۳) بیماران / اجرا
cohort 2 حکم‌ساز: S526_H8, S1511_H6, S589_H8g, S589_H4g. **S382_H4 و S312_H1 در این شماره فقط اطلاع‌رسان** (هولد‌اوتشان برای V_AGE/V_DDF قبلاً لمس شده؛ V_BOTH روی آن‌ها اطلاع‌رسان). موتورِ s382: خروجِ قاعده در close همان کندل (عینِ S453/S455). mt5_full. توازی اجباری؛ مسیرِ C. خروجی: `research/mgmt/S1452_*.json`، `results/S1452_MgmtTransferAgeDdf_Xauusd_H1H4H6H8.md`.

## ۴) پیش‌بینی‌های ابطال‌پذیر
- P1: V_AGE (k=15) روی S589_H4g بیشترین شانس (همان TF و همان خانوادهٔ هندسه‌ی S382_H4).
- P2: روی H8 (S526/S589_H8g) k=15 معادلِ ۵ روز است — احتمالاً خیلی دیر شلیک می‌کند (بازنده‌ها تا آن‌جا SL خورده‌اند) ⇒ کم‌اثر.
- P3: V_BOTH ریسک را بیش از هر تک‌قاعده کم می‌کند اما سود را هم بیشتر می‌خورد؛ اگر پاس شود، اولین «بستهٔ» مدیریتیِ اثبات‌شده است.
- P4: اگر همه رد شوند: قواعدِ PASS دههٔ اول **لایه‌-ویژه** بودند (per-layer, نه فقط per-TF) — درسِ مهم برای کارتِ حالتِ ۴: هر لایه قاعدهٔ خودش را می‌خواهد.

## ۵) ممنوعیت‌ها
هیچ تغییری در سایت · هیچ تغییری در فایل‌های دیگران/موتور · k و الفبا بازتنظیم نمی‌شوند.
