# S1560 — گپِ منفی × σ-CALM در برابر V روی M15/H1 — ✅ ACCEPT 96.9 (M15 DUAL) · ❌ REJECT 27.1 (H1 SIGMA) — **قانونِ «مقیاسِ آرامش» تأیید شد**

**دانشمند:** لایب‌نیتس (بلوک دوم S1560–S1569 — شمارهٔ ۱ از ۱۰) · **تاریخ:** 2026-09-09
**داور:** `engine/rqs2.py::compute_rqs2` v2.6 — دست‌نخورده، gates_only، ۱۱ گیت وتو؛ حکم عیناً از موتور.
**پیش‌ثبت:** `results/S1560_PREREG_GAPOPEN_SIGMA_VS_V_M15H1_XAUUSD.md` (کامیت `633e51d6` — قبل از هر عدد؛ هیچ بندی تغییر نکرد).
**ابزار:** `tools/s1560_gap_sigma_vs_v.py` (کامیت قبل از اجرا؛ پس از حکم فقط گاردِ مجموعهٔ خالی در تابعِ ممیزی افزوده شد — بی‌اثر بر حکم).
**رکوردها:** `results/_s1560_arms/{locked_config,judge_M15,judge_H1,overlap_M15,overlap_H1}.json`.
**داده:** `data/mt5_full/XAUUSD_{M15,H1}.csv` — ۱۵.۶ سال کامل (assert در کد).
**وضعیت:** ⏸ **توقف طبق قانون کاربر — لایهٔ موفق ⇒ گزارش. فقط اکتشاف؛ سایت هرگز لمس نمی‌شود.**

---

## ۰) حکم رسمی موتور — عیناً (n_trials=443)

```
S1560-M15 [DUAL = V85 ∧ σ-CALM]:  ACCEPT  RQS2=96.9
  n=210  WR=71.90%  null_ref=35.79%  lift=+36.11pp  z=10.30  z_luck_bound=3.01  z_margin=7.90
  PF=2.713  net=+$3,243  expectancy=+6.86 pip  maxDD=2.45%  MCL=3/8  recovery=9.73  top_win_share=2.2%
  گیت‌ها: H0✓ H1✓ H2✓ H3✓ H4✓ H5✓ H6✓ H7✓ H8✓ H9✓ H10✓ — هر ۱۱ سبز، notes خالی

S1560-H1 [SIGMA-CALM]:  REJECT  RQS2=27.1
  n=158  WR=62.66%  null_ref=45.28%  lift=+17.38pp  z=4.35  z_margin=1.37
  PF=1.614  net=+$1,383  maxDD=4.39%  recovery=2.72  — رد: H8 (ریکاوری/کارایی سرمایه)
```

**حکم بلوک برای S1560: ACCEPT، بهترین امتیاز 96.9 (M15 DUAL).**
⚠️ **برچسب صداقت:** پالایشِ S560/S562 است، نه آلفای مستقل. S1560-M15 **زیرمجموعهٔ کاملِ S562-M15** است (kept_not_in_V = ۰؛ ۴۸٪ معاملات S562 حفظ شد). حکم‌های تازه: ۲ (M15 DUAL، H1 SIGMA).

## ۱) پرسش علمی و پاسخ — اصلِ لایه اینجاست

**پرسش (پیش‌ثبت §۱):** S569 روی M1/M5 نشان داد σ کوتاه‌مدت > V روزانه. S562 روی M15/H1 نشان داد V کار می‌کند. **آیا روی M15/H1 هم σ > V است (H_self: σ خود-مقیاس همه‌جا برنده) یا V می‌ماند (H_scale: مقیاس آرامش = مقیاس ورود)?**

**پاسخ نیمهٔ اول (پیش از هر تماس با هولدآوت — ثبت در `first_half_answer`):**

| TF | t_fh(BASE) | t_fh(**V**) | t_fh(SIGMA) | t_fh(DUAL) | هم‌خطی V×σ | پاسخ |
|---|---|---|---|---|---|---|
| M15 | 3.65 | **3.61** | 2.37 | 2.66 | 0.943 | **H_scale** |
| H1 | 1.98 | **2.03** | 0.06 | n_fh=29 (حذف) | 0.921 | **H_scale** |

**P1 ✓ در هر دو TF. F1 نزد.** قانون S569 تأیید و اکنون **چهار-TF** است:

| TF ورود | گیت آرامشِ برنده | شاهد |
|---|---|---|
| M1 | σ کوتاه (RiskMetrics روی M1) | S569: σ t 3.52 > V 2.66 |
| M5 | σ ∧ V (DUAL) | S569: 3.95 > V 3.90 > σ 3.54 |
| M15 | V روزانه (σ کمکی، در DUAL DD را می‌کاهد) | S1560: V 3.61 > DUAL 2.66 > σ 2.37 |
| H1 | V روزانه؛ σ **بی‌اثر** | S1560: V 2.03 ≫ σ 0.06 |

**قانون قابل‌حمل (بازنویسی‌شده و دقیق‌تر):** «σ RiskMetrics با W=233 کندل» *خود-مقیاس* است اما مقیاسش را از TF می‌گیرد: روی M1 ≈ ساعت آخر، روی H1 ≈ ۱۰ روز. لبهٔ گپِ بازگشایی به آرامشِ **۱–۱۴ روزهٔ قیمت روزانه** (V) حساس است؛ σ فقط وقتی کمک می‌کند که مقیاسش زیر یک روز باشد (M1/M5) — یعنی «آرامشِ شبِ قبل» اطلاعاتِ جداگانه‌ای دارد، ولی «آرامشِ ۱۰ روزِ بازدهِ H1» با V هم‌معنی و ضعیف‌تر است. **V و σ روی هر چهار TF متعامدند (0.92–0.98)** ⇒ اطلاعات مستقل، اما فقط در مقیاسِ ریز مفید.

## ۲) قاعدهٔ قابل حمل بازوی ACCEPT (M15 DUAL — صفر پارامتر آزاد)

S562-M15 عیناً (گپ منفی q70 با تفکیک آخرهفته؛ V85: میانگین دامنهٔ ۱۴ روز ≤ چندک ۸۵ تاریخچهٔ ۲۵۰ روزه؛ LONG open کندل اول؛ SL=TP=50.9 پیپ، hold=1 کندل) **+** `σ_i ≤ median(σ_{i−233..i−1})` روی کندل سیگنال (σ RiskMetrics λ=0.94 روی بازده M15، کد `s840.ewma_z` منجمد).

## ۳) S1560-M15 در برابر والدها (کل داده)

| سنجه | S560-M15 (پایه) | S562-M15 (V85) | **S1560-M15 (DUAL)** |
|---|---|---|---|
| RQS2 | REJECT (H8) | 95.3 | **96.9** |
| n | 603 | 438 | 210 |
| WR | 67.2 | 70.8 | **71.9** |
| lift | — | +35.0 | **+36.1** |
| z | — | 15.3 | 10.3 |
| PF | — | 2.81 | 2.71 |
| maxDD | — | 3.71% | **2.45%** |
| net | — | $9,086 | $3,243 |
| recovery | — | 12.5 | 9.7 |
| نیمه‌ها WR | — | — | 68.9 → **73.2** (هولدآوت بهتر) |
| سال‌ها مثبت | — | — | 14/16 |

**ممیزی مکمل (`overlap_M15.json`):** آنچه DUAL از S562 حذف کرد (`V_not_in_kept`, n=228): WR 69.7، +8.2 pip، DD 5.1٪ — **هنوز عالی**. یعنی σ روی M15 معاملهٔ بد را حذف نکرد؛ فقط نصفِ معاملات خوب را کنار گذاشت. DD موتور بهتر شد (2.45 vs 3.71) اما به قیمتِ ۶۴٪ سود خالص. **F4 شلیک شد: برچسب «توان‌سوز» — S562-M15 لایهٔ اصلی می‌ماند؛ S1560-M15 فقط شاهدِ تکرارپذیریِ لبه در زیرمجموعهٔ آرام‌تر است.** این با پاسخ H_scale کاملاً سازگار است.

## ۴) H1 — چرا REJECT درست است

σ روی H1 ۶۱٪ معاملات پایه را حذف کرد، اما مکمل (`removed_vs_base`, n=249) WR 66.7 و +10.1 pip داشت — **بهتر از نگه‌داشته‌ها** (62.7، +8.4). و `kept_not_in_V` (n=64: معاملاتی که σ پذیرفت ولی V رد کرد) WR 60.9، +4.6 pip، DD 31٪ — همان دُمِ بدی که V درست حذف می‌کند. σ روی H1 نه‌فقط بی‌اثر بلکه **ضدگزینش** بود. موتور H8 را رد کرد (recovery 2.7). Jaccard با V فقط 0.295. **H1: قانون S562 (V) دست‌نخورده می‌ماند.**

## ۵) پیش‌بینی‌ها

| | پیش‌بینی | نتیجه |
|---|---|---|
| P1 | V ≥ σ در هر دو TF (prior 60٪) | ✅ هر دو |
| P2 | هم‌خطی 0.90–1.10 | ✅ 0.943 / 0.921 |
| P3 | DUAL-H1 n_fh<60 و توان‌سوز؛ DUAL-M15 DD < 3.71 با lift ≥ 35 | ✅ H1 n_fh=29 حذف شد · ✅ M15 DD 2.45، lift 36.1 |
| P4 | عبور σ ۵۵–۷۰٪ | ✗ 43٪ / 38٪ — σ روی TF درشت‌تر سخت‌گیرتر است (W=233 کندل = تاریخچهٔ بلندتر) |
| F4 | ACCEPT اما مکمل خوب ⇒ توان‌سوز | **شلیک شد** روی M15 |
| حکم | prior 70٪ ACCEPT | ✅ (M15) |

## ۶) اثبات ۸ خطای رایج

1. **Look-ahead:** ماسک S560 (گپ در لحظهٔ ورود معلوم)؛ V از ۱۴ روز کامل‌شده؛ σ_i از r_{i−1} و قبل، median از پنجرهٔ بستهٔ گذشته؛ هندسه از قفل ۲۰۱۸ S560. ✓
2. **Overfit:** ۴ بازوی داوری منجمد؛ qv (85/78)، λ، W همه وارثی؛ هندسه بازتنظیم نشد؛ هولدآوت یک بار؛ پس از حکم فقط گاردِ خالی در تابع ممیزی (بی‌اثر بر PnL). ✓
3. **چندگانگی:** n_trials=443 تجمعی خانوادهٔ گپ؛ z_luck 3.01، z_margin 7.9. V دوباره داوری نشد (حکمش موجود بود). ✓
4. **نال:** LONG غیرشرطی هم‌هندسه، 50k + K=500 جایگشت هم‌اندازه. ✓
5. **هزینه:** 3.3 پیپ؛ H9 سبز. ✓
6. **داده:** mt5_full ۱۵.۶ سال با assert؛ S562 بیت‌به‌بیت بازتولید شد (438/255) — BUG-DATASETDRIFT. ✓
7. **سمت:** LONG-only وارثی. ✓
8. **تمرکز:** top_win_share 2.2٪، MCL 3، 14/16 سال مثبت. ✓

## ۷) دفتر بلوک دوم S1560–S1569

| # | لایه | حکم |
|---|---|---|
| **S1560** | GapOpen σ-vs-V M15/H1 (داوریِ قانون مقیاس) | ✅ **ACCEPT 96.9** (M15 DUAL؛ توان‌سوز — S562 اصلی می‌ماند) · H1 REJECT 27.1 |
| S1561–S1569 | — | **تعهد پیش‌ثبت: رویدادِ تازه، نه خانوادهٔ گپ** |

*یادداشت لایب‌نیتس: خانوادهٔ گپ اکنون واقعاً بسته است — نه چون ایده‌ای نمانده، بلکه چون هر گیتِ تازه فقط توان می‌سوزد. قانونش را دارم؛ حالا باید رویدادِ دیگری پیدا کنم.*
