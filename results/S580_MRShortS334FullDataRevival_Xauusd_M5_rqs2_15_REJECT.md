# S580 — بازآزماییِ لایهٔ S334 (Mean-Reversion SHORT) روی دادهٔ کاملِ ۱۵.۶ ساله — ❌ REJECT

> **پیش‌ثبت:** `results/S580_PREREG_MRSHORT_XAUUSD_VIRGIN_HOLDOUT.md` (کامیتِ جداگانه، **پیش از** هر آزمون)
> **مسیرِ چندگانگی:** Path C — تأییدِ hold-out · `n_trials=500` · ۵۰۰ جای‌گشت/بازو · seed=20260812
> **داور:** `tools/s580_adjudicate.py` · موتور: `engine/rqs2.py` v2.6 (حکم خودکار، نه دستی)
> **داده:** `data/mt5_full/XAUUSD_*.csv` (S434) — provenance در هر آرتیفکت ذخیره شده (ضدِ E-16)

---

## ۱) پرسش پژوهش

S334 تنها کارتِ POWER-LIMITEDِ بازآزمایی‌نشدهٔ پروژه بود: fade اشباعِ خرید (z>2.4، RSI>70،
شمعِ نزولی) با گیتِ رژیمِ `Hurst<0.50 & Kurt<1.8`، منجمدشده روی دادهٔ ۲۰۲۳-۲۰۲۶ با
n=47، WR=61.7٪. طبق z∝√n، دادهٔ کامل n را ~۵.۵× می‌کند. **آیا لبه واقعی است؟**
مزیتِ ساختاری: ۸۸۵٬۰۰۳ کندلِ ۲۰۱۱-۲۰۲۳ برای این لایه **دادهٔ کاملاً بکر** بود — یک
hold-out طبیعی که پیش از دیدنِ حتی یک عدد، در پیش‌ثبت قفل شد.

## ۲) نتایجِ بازوهای پیش‌ثبت‌شده (کانفیگ منجمد، صفر جاروب)

| بازو | کارت / برش | n | WR٪ | lift(pp) | z | سدِ شانس | PF | net$ | RQS2 | حکم موتور |
|---|---|---|---|---|---|---|---|---|---|---|
| **A** | M5 بکر ۲۰۱۱-۲۰۲۳ | 218 | 49.08 | +6.00 | 1.79 | 3.053 | 0.90 | **−294** | 9.1 | ❌ REJECT |
| **B** | M5 کامل ۱۵.۶y | 266 | 51.13 | +7.67 | 2.53 | 3.053 | 1.07 | +271 | **14.7** | ❌ REJECT |
| C-M1 | M1 کامل | 1010 | 41.39 | +4.57 | 3.01 | 3.09 | 0.61 | −5144 | 5.2 | ❌ REJECT |
| C-M15 | M15 کامل | 107 | 35.51 | **−10.56** | −2.19 | — | 0.48 | −1129 | 0.0 | ❌ REJECT |
| C-M30 | M30 کامل | 87 | 44.83 | −1.91 | −0.36 | — | 0.56 | −728 | 0.1 | ❌ REJECT |
| C-H1 | H1 کامل | 44 | 34.09 | −12.29 | −1.61 | — | 0.38 | −577 | 0.7 | ❌ REJECT |
| C-H4 | H4 کامل | 14 | — | — | — | — | — | — | — | ⛔ n<30 |
| C-D1 | D1 کامل | 2 | — | — | — | — | — | — | — | ⛔ n<30 |

- `skill_p_perm`: A=0.0368 · B=0.0058 · هر دو بسیار دورتر از آستانهٔ H3 (≤0.001).
- گیت‌های شکسته (بازوی B): H1,H2,H3,H5,H6,H8,H9. بازوی A بدتر: +H4,H10.
- هندسهٔ C-بازوها طبق قاعدهٔ پیش‌ثبت‌شده: نسبت‌های منجمدِ M5 (11.17×/12.70×) × medianATR14(TF).
- EURUSD طبق دستور صریح کاربر مطلقاً آزموده نشد.

## ۳) حکم و تفسیرِ علّی (دقیقاً قاعدهٔ تصمیمِ پیش‌ثبت، بند ۶)

**حکم رسمی کارتِ XAUUSD-M5 = REJECT (RQS2=14.7)** — خروجی خودکار موتور، بازوی B.

**تشخیص: REGIME-ONLY.** بازوی A (بکر) هم آماری مرده (z=1.79) هم اقتصادی (net منفی؛
WR=49.1٪ زیر سربه‌سرِ 48.2٪+هزینه به‌اندازه‌ای نزدیک که چیزی نمی‌ماند). کلِ «حیاتِ»
S334 از پنجرهٔ ۲۰۲۳-۲۰۲۶ می‌آمد (همان دادهٔ تنظیم). این **دقیقاً** الگویی است که
S530 برای S355 یافت — دومین مورد مستند: *لایه‌ای که فقط روی پنجرهٔ برازشِ خودش زنده است.*

**دربارهٔ گیتِ رژیم:** فرضیهٔ S334 این بود که Hurst+Kurt رژیمِ mean-reverting را جدا
می‌کند. دادهٔ بکر نشان داد گیت «چیزی» می‌بیند (lift=+6pp>0) اما نه به‌قدر معنادار و نه
به‌قدرِ پوشش هزینه. گیتِ رژیم n را کم کرده بود، رژیم را واقعاً جدا نکرده بود —
همان خطری که در پیش‌ثبت §۷ ثبت کردم (پیش‌بینی: «احتمال ACCEPT ~۲۵٪» — محقق شد).

**یافتهٔ جانبی M1 (ارزشمند):** z=3.01 با n=1010 — فقط 0.04 زیر سدِ شانس، ولی PF=0.61.
یعنی روی M1 «سایهٔ آماریِ» یک پدیدهٔ ضدپایایی هست اما هزینهٔ ثابت 3.3pip در برابر
ATR=3.9pip اقتصادش را می‌کُشد. **درس: لبهٔ آماری بدون اقتصاد، لایه نیست.** این با
یافتهٔ S510 (RR-geometry روی TFهای پایین) هم‌صداست و برای آیندگان ثبت می‌شود.

**قانونِ مرگِ ابدی:** S334 اکنون روی دادهٔ کامل در تمام TFهای قابل‌اندازه‌گیری مرده
است. مسیر احیای «دادهٔ بیشتر» (ارزان‌ترین مسیر) مصرف شد. احیای مجدد فقط با
مکانیزمِ *نو* (نه تنظیمِ همین) مجاز خواهد بود.

## ۴) قانونِ دووجهی تأیید شد

29→79 خالی می‌ماند: نمرات این مأموریت (0.0 تا 14.7) همه در خوشهٔ پایین.
هیچ عددی در شکاف ننشست. تنظیم نمی‌توانست B (z=2.53) را به 3.09 برساند — تلاش هم نشد.

## ۵) اثباتِ پرهیز از ۸ اشتباه رایج

1. **دادهٔ کوتاه:** ❌ نشد — کلِ کار روی `mt5_full` (provenance در هر JSON؛ گارد DATASETDRIFT با معیارِ بازهٔ >۱۲ سال).
2. **look-ahead:** ❌ نشد — سیگنالِ منجمد importشده از `s334_mr_short_revival.py` (ورود کندلِ بعد)، نال با همان موتور.
3. **تسلیمِ زودهنگام/ابزارِ محدود:** ❌ نشد — ۸ بازو روی ۷ TF؛ گیتِ رژیم از بانک ۴۰۱تایی (hurst/kurt).
4. **گزارشِ گزینشی:** ❌ نشد — هر ۸ بازو (شامل ۲ نامعتبر) با JSON کامل کامیت شدند، شکست‌ها با همان دقتِ موفقیت‌ها.
5. **هزینهٔ نادیده:** ❌ نشد — spread=3.3pip در موتور؛ حتی علتِ مرگِ M1 دقیقاً هزینه بود و صادقانه گزارش شد.
6. **TP/SL یکسان برای همهٔ TFها:** ❌ نشد — قاعدهٔ ATR-مقیاسِ per-TF، پیش‌ثبت‌شده در §۴ **پیش از دیدنِ اعداد**.
7. **اعدادِ رند:** ❌ نشد — هیچ پارامتری انتخاب نشد؛ همه از آرتیفکتِ منجمد (z_win=34 فیبوناچی، thresholds غیررند).
8. **حکمِ دستی/دور زدنِ معیار:** ❌ نشد — verdict فقط از `engine/rqs2.py`؛ `ADMISSION_RULE='gates_only'`؛ TP>SL.

## ۶) ردِ ممیزی و بازتولید

- کانفیگ: `results/_s334_XAUUSD_M5.json` (خوانده‌شده، نه بازنویسی — گارد BUG-GEOMDRIFT)
- آرتیفکت‌ها: `results/_s580_arms/arm_{A,B,C_M1,C_M15,C_M30,C_H1,C_H4,C_D1}.json`
- بازتولید: `python3 tools/s580_adjudicate.py --arms A,B` سپس `--arms C:M1` ... (کشِ رژیم: `results/_s580_regime_cache/`)
- گاردهای ارثی فعال: BUG-PERMK، BUG-NULLUNCOND (نالِ per-arm، **سمتِ short** — تلهٔ تازه‌ای که هنگام کپیِ الگوی long از S437 شکار شد)، BUG-SCOREKEY، BUG-ZBARNEST، BUG-PIPGUESS، قیدِ n≥30، BUG-DATASETDRIFT.

## ۷) میراث برای آیندگان

- **مسیرِ ارزانِ «فقط دادهٔ بیشتر» برای S334 مصرف و بسته شد.** (قانون مرگ ابدی برای این مسیر)
- **الگوی REGIME-ONLY دومین‌بار مستند شد** (S355/S530، حالا S334/S580) ⇒ فرضیهٔ عمومی:
  WRهای بالای برازش‌شده روی ۲۰۲۳-۲۰۲۶ به‌طور سیستماتیک محصولِ رژیمِ اخیرِ طلا هستند.
  هر احیای آینده باید «اول روی بکرِ ۲۰۱۱-۲۰۲۳» آزموده شود، نه آخر.
- **سرنخِ M1:** پدیدهٔ ضدپایایی با z≈3.0 و n>1000 وجود دارد ولی زیر هزینه دفن است.
  اگر روزی TF/هندسه‌ای پیدا شود که همین پدیده را با هزینهٔ نسبیِ کمتر صید کند
  (مثلاً M3/M5 با هولدِ کوتاه‌تر یا RR بزرگ‌تر — سرنخ باز RR>1 روی TFهای پایین)،
  آنجا جای حفاری است. ← ورودیِ مأموریت بعدی (S581).

— S580 · حکم: ❌ REJECT (RQS2=14.7، بازوی B) · صادر شده توسط موتور، پذیرفته‌شده با آرامشِ ریاضی‌دان.
*دریا بالا آمد؛ صخره‌ای نبود — اما بستر دریا نقشه‌برداری شد، و نقشه خود گنج است.*
