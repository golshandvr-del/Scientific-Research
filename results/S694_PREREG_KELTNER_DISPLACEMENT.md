# S694 — پیش‌ثبت: Keltner-Displacement Acceptance | XAUUSD | مسیر C

**تاریخ**: 2026-09-06 · **نویسنده**: گودل (بلوک S690–S699؛ این لایه S694 = S693+1) · **وضعیت**: منجمد قبل از هر تست
**داور**: `engine/rqs2.py::compute_rqs2` v2.6 دست‌نخورده (gates_only) — حکم عیناً از موتور.
**داده**: `data/mt5_full/XAUUSD_*.csv` — ۱۵.۶ سال، ۱۹ TF (تلهٔ E-16 خنثی؛ src در هر JSON).

## ۰) بازخوانی مخزن پیش از این سند (fetch امروز)
- ACCEPTهای تازهٔ همکاران: S604/S605/S606/S607 (خانوادهٔ شوک انگل + گیت‌ها)، S919 (شوک مطلع هم‌راستا H6)،
  S965/S966 (ماندگاری کایل H8)، S1520 (سقف تازهٔ ۹۰ کندلی × کندل مطلع H8)، S770، S800.
- REJECTهای آموزنده: S755 («سطح ثابت + شکست = مرده؛ انبساط خودمقیاس نسبت به مرجع متحرک زنده است»)،
  S684 («نرمال‌سازی شوک به روند محلی، دریفت طلا را حذف می‌کند — دریفت پیام است»)، S906 (شوک پس از سکوت، توان‌محدود).
- بلوک خودم: S690 REJECT · S691 REJECT · S692 REJECT (مهارت واقعی، هندسه‌کُش) · S693 REJECT (مهارت هفتگی در M15، PF کم).

## ۱) مفهوم (تازگی ممیزی‌شده با grep بی‌برش)
**رویداد**: close از باند کلتنر عبور و *بیرون آن بسته* شود:
- LONG: `close[t] > EMA_p[t] + k·ATR_p[t]` و `close[t−1] ≤ EMA_p[t−1] + k·ATR_p[t−1]` (لبه)
- SHORT: قرینه با باند پایین.
- ورود: open کندل t+1 (همسو = acceptance/follow). یک سیگنال per side تا بازگشت close به درون باند (debounce طبیعی لبه).
- **تمایز**: ≠ S906/S602–607 (شوک تک‌کندلی؛ اینجا جابه‌جایی *انباشته* نسبت به مرجع متحرک) · ≠ S800 (بدون پیش‌شرط squeeze) ·
  ≠ S640 KAMA-cross و S662 SuperTrend (عبور از باند جابه‌جایی، نه تقاطع/ratchet) · ≠ S211e/s20 (کلتنر آنجا فیلتر/جزء بود، نه رویداد).
- ادامهٔ وصیت S693: رویداد پرتکرار، جهت درون‌زاد، مرجع خودمقیاس — بدون لنگر تقویمی (خانوادهٔ لنگر تقویمی در بلوک من بسته شد).

## ۲) خانوادهٔ منجمد (مسیر C)
| پارامتر | مقادیر |
|---|---|
| p (EMA و ATR وایلدر، هم‌دوره) | {21, 34, 55} |
| k (پهنای باند) | {1.618, 2.618} |
| k_sl (SL = k_sl·ATR_p) | {1.0, 1.618} |
| rr | {1.0, 1.618, 2.058} |
**اندازه**: 3×2×2×3 = **36 سلول per side**؛ `n_trials = 72` (دو سمت). hold = 48 ساعت (BUG-TFM: زمان ثابت، تبدیل با bars_per_hour).
هزینه = spread 3.3 pip (XAUUSD، بدون کمیسیون)، tp = max(rr·sl, sl) (سپر TP≥SL). ابهام SL&TP در یک کندل ⇒ SL.

## ۳) پروتکل
- SPLIT_FRAC = 0.60؛ جست‌وجو فقط روی TRAIN؛ برنده per side = max lift·√n مشروط به n ≥ n_required_for_h3؛ اگر هیچ سلولی ⇒ REJECT «سقف شیشه‌ای» بدون خرج holdout.
- نول: جایگشت زیرنمونه‌ای با هندسهٔ منجمد برنده، K=500 per side، seed=694.
- یک فراخوان compute_rqs2 per TF با split_bar، bar_time، close، null، n_trials=72.
- ۱۹ TF طلا از M1؛ کامیت افزایشی per TF؛ هیچ EURUSD.

## ۴) پیش‌بینی‌های ابطال‌پذیر
- **P1**: در M1–M5 هندسهٔ SL≈ATR_p زیر ~15 pip ⇒ هزینه مهارت را می‌خورد (سومین/چهارمین تکرار قانون S692).
- **P2**: اگر قانون S755 درست است، در H1–H8 با k=2.618 lift ≥ +6pp ظاهر می‌شود؛ ابطال ⇒ «انبساط خودمقیاس» فقط با پیش‌شرط فشردگی (S800) کار می‌کند.
- **P3**: n در H8+ زیر ~300 ⇒ سقف توان (POWER-LIMITED حداکثر).

## ۵) تعهدات
هیچ تغییر پس‌آزمونی. بلوک‌های دیگر فقط‌خواندنی. REJECT ⇒ گزارش خلاصه و S695 بی‌درنگ (پس از fetch). ACCEPT ⇒ توقف و گزارش به کاربر، هم‌پوشانی معاملاتی با ACCEPTها، بعد لوکال-موبایل سپس سایت.

— گودل 🖋
