# S772 — پیش‌ثبت: انبساطِ دامنه نسبت به لنگرِ هفته (AWR Weekly Range-Expansion Continuation) · XAUUSD

> ⏱ **این سند پیش از هر آزمونِ جهت‌دارِ S772 کامیت و push می‌شود.** مُهرِ گیت = پیش‌ثبت.
> **نویسنده:** دیوفانتوس · بلوک S770–S779 · لایهٔ ۳ از ۱۰ · مسیر چندگانگی C.

## ۰. بررسیِ مخزن پیش از شروع (قانونِ کاربر)
fetch شد (fe96c1c3). ACCEPTهای جدیدِ خوانده‌شده: S1520 (Gauss, Informed Fresh High H8 91)، S526 (Fresh Rolling High H8 88)، S604/S606/S607 (Cantor, Engle shock pools ≤83.1)، S749 (Khayyam, StrongClose H2 82)، S919 (Keynes, Convention-Aligned Shock H6 88.9)، S965/S966 (Kyle, permanence H8 82/86). درسِ مشترکِ همه: **لبه‌های طلا در H6–H8–D1 زنده‌اند و در «لبهٔ تغییرِ حالت» نه حالتِ انباشته** — S772 هم روی state-cross است.
همسایه‌های لنگرِ هفتگی (فقط خوانده شد): S639 (weekly-open به‌عنوان متغیرِ تهی، best +1.18pp)، S693 (PWH/PWL acceptance REJECT 15)، S982 (Weekly-Open Reclaim REJECT 4). **تمایزِ S772:** هیچ‌کدام نرمال‌سازی به **AWR** (میانگینِ دامنهٔ هفتگی) و آستانهٔ **فراکشنیِ انبساط** نداشتند؛ آن‌ها به سطحِ open/extremum نگاه می‌کردند، S772 به «چه کسری از دامنهٔ عادیِ هفته مصرف شده». S639 هشدار است، نه رد: عبورِ صِرف از open (θ=0) تهی است؛ S772 دقیقاً θ>0 را می‌آزماید.

## ۱. فرضیه (خانوادهٔ S770/S771 — مقیاسِ میانی)
S770 (روز) ACCEPT 82.4 با ~۶۹۰ معامله؛ S771 (ماه) REJECT 25.9 با lift بالاتر (+7.9) اما فقط ۲۰۱ معامله (کم‌توان). **پیش‌بینیِ ساختاری:** مقیاسِ هفته باید بینِ دو باشد — تراکمِ رویداد ~۲–۳× ماه (پروبِ فرکانسی: D1 θ=0.35 → ۳۸۵ رویداد در پنجرهٔ ۶۰٪، H8 → ۵۸۵) و lift احتمالاً بین +5.6 و +7.9.
H1: عبورِ `frac = (close − weekOpen)/AWR₁₃` از +θ ⇒ LONG؛ از −θ ⇒ SHORT (state-cross، فقط لبه). ادبیات: Moskowitz/Ooi/Pedersen 2012 (TSM)، Gao/Han/Li/Zhou 2018 (intraday momentum از open).

## ۲. تعریفِ دقیق (همه علّی)
- weekOpen = openِ اولین کندلِ هفتهٔ ISO جاری (شناخته از لحظهٔ باز شدن).
- AWR₁₃ = میانگینِ (high−low) ۱۳ هفتهٔ **کاملِ پیشین** (shift(1) روی هفته).
- frac_t با closeِ کندلِ t؛ سیگنال روی کندلِ t+1 اجرا می‌شود (موتور simulate_trades).
- هندسه (منجمد از S770، **هیچ جستجویی**): SL = 1.272 × ATR₁₀₀ (pip)، TP = 2.058 × SL.
- ثابت‌ها: SEED=20260906 · K_PERM=800 · SPLIT_FRAC=0.60 · AWR_P=13 · ATR_P=100.
- شبکه: THETAS={0.35, 0.50, 0.65} × HOLDS={16, 32} = ۶ سلول × ۲۰ TF = **۱۲۰ سلول**.
- معیارِ انتخابِ سلول (پیش‌ثبت): max zproxy=lift·√n/100 مشروط به exp_net>0 در **هر دو** نیمهٔ اکتشاف. نبودِ چنین سلولی ⇒ REJECT_BY_RULE.

## ۳. پروتکلِ داوری
- ۲۰ کارت (M1…MN1؛ H4 از H1 بازنمونه) روی `data/mt5_full` (verify ۱۹/۱۹؛ assert در load_card — E-16).
- هر کارت حداکثر یک داوری (holdout ۴۰٪ انتهایی، n_trials پایه 120+داوری‌ها).
- حقِ **حداکثر یک استخر** (قاعدهٔ عضویت اعلام می‌شود در الحاقیهٔ پیش از اجرا؛ پیش‌فرضِ اعلام‌شدهٔ اکنون: PF≥1.30 مانند S771).
- پس از پاس: ممیزیِ همپوشانی با تمام لایه‌های زنده + S770 (خویشاوندِ نزدیک — احتمالِ همپوشانیِ بالا؛ اگر Jaccard روزها > ۳۰٪ باشد، به‌عنوانِ «تکرارِ S770» گزارش می‌شود نه لایهٔ مستقل).

## ۴. پیش‌بینی‌های ابطال‌پذیر
1. M1…M30 هزینه‌مرده.
2. بهترین کارت‌ها D1/H12/H8 با θ=0.35 (مانند S771).
3. n استخر بین ۳۵۰ تا ۶۰۰؛ اگر <۳۰۰ ⇒ لنگرِ هفتگی هم کم‌توان است و خانوادهٔ لنگرمند فقط در مقیاسِ روز زنده می‌ماند.
4. اگر ACCEPT اما Jaccard با S770 > ۳۰٪ ⇒ یافته «تکرارِ مقیاسی» است — صادقانه اعلام می‌شود.
