# S516 — پیش‌ثبت: V-TIME نوسانی + فیلتر هم‌راستایی درفت (Trade-Selection Lever)

**تاریخ پیش‌ثبت:** 2026-08-18 (commit قبل از هر آزمون — قانون ③)
**قلمرو عددی:** بلوک S510–S519 ✓
**وارث:** S515 (بهترین بلوک: 8/11 گیت، H3 پاس با z=3.38؛ بیماری = PF متقارن 1.255)
**وام علمی از دیتابیس (پس از fetch):**
- S561 موازی: اهرم شکل‌دهی PnL با استاپ = سوخته (DD بدتر شد) — **آن مسیر را نمی‌روم**
- S562 موازی (DOUBLE ACCEPT): اهرم **انتخاب معامله** (فیلتر علّی) گیت‌ها را درمان کرد
- S950 موازی (ACCEPT 80): فیلتر هم‌راستایی درفت روی رویداد پرش — همان الگو

---

## 1) فرضیه

**H-S516:** در رویداد انبساط نوسان، معاملاتی که خلاف درفت غالب ورود می‌کنند
بازندهٔ سیستماتیک‌اند. حذف علّی آن‌ها (فیلتر هم‌راستایی) PF را از 1.255 و lift
را از +8.74pp بالا می‌برد؛ با کاهش n به ~۲۵۰، z باید از کران شانس 3.7 عبور کند
(چون lift باید ~+12pp شود اگر فرضیه درست باشد).

تمایز از S512 (شکست هم‌خونی): درفت جهت‌دار قیمت از خانوادهٔ **روند/جهت** است،
نه نوسان — استقلال اطلاعاتی از atr_fib_55 (بی‌جهت) ساختاری است.

## 2) پایهٔ منجمد (صفر تغییر)

- کارت XAUUSD-M30 کامل | سیگنال: atr_fib_55 cross↑q90(کشف)/LONG (همان S511)
- خروج: V-TIME **k=4** + براکت نادر SL=TP=q98(کشف) — برندهٔ S515، منجمد
- شبیه‌ساز: sim_vtime اثبات‌شده (parity S515 PASSED)

## 3) خانوادهٔ فیلتر (پیش‌ثبت کامل — H5)

فیلتر هم‌راستایی علّی: سیگنال در بار t نگه داشته می‌شود ⟺ `close[t] > close[t-L]`
(درفت مثبت در L کندل اخیر؛ همهٔ داده‌ها تا close[t] = علّی).

**L ∈ {13, 34, 89, 233} کندل M30** = **4 آزمون**. هیچ پارامتر دیگری جستجو نمی‌شود.
n_trials تجمعی = 5009 (تا S515) + 4 = **5013**

## 4) قاعدهٔ انتخاب (فقط کشف ۶۰٪)

معتبر ⟺ روی کشف: n_filtered ≥ 50 ∧ retention ∈ [0.25, 0.90] ∧
net فیلترشده > net پایه در هر دو نیمهٔ کشف.
**برنده = بیشترین t-آماره در کشف.** هیچ معتبر ⇒ REJECT-by-rule.

## 5) آزمون عملیات-همانی (درس S437 + درس دقت S512)

K=1000 زیرمجموعهٔ تصادفی با همان retention (seed ثابت). **گیت بر حسب p دقیق:**
`P(WR_random ≥ WR_winner) ≤ 0.05` (نه مقایسهٔ چندک گردشده). شکست ⇒ REJECT-by-identity
بدون داوری.

## 6) داوری (مسیر C)

1. null با سیگنال‌های فیلترشده: بی‌قید stride 1/3/7 + جایگشت K=2000، seed=20260822،
   دنیای LONG، p دقیق.
2. حداکثر **یک** compute_rqs2 روی کل نمونه، split_bar کشف، n_trials=5013.

## 7) برنامهٔ اقتضایی

- غیر-REJECT ⇒ ممیزی هم‌پوشانی با همهٔ لایه‌های زندهٔ ACCEPT (S312/S344/S355/S356/
  S382/S404/S431/S432/S560/S562/S571/S572/S950) + آزمون فوری بخش هم‌پوشان به‌عنوان فیلتر.
- ACCEPT ⇒ **توقف و گزارش فوری به کاربر** (قانون جدید کاربر).
- REJECT ⇒ گزارش خلاصه، fetch، و ادامهٔ خودکار با S517 (قانون جدید کاربر).

## 8) ثابت‌ها

SEED=20260822 | WARMUP=400 | SPLIT_FRAC=0.60 | K_VT=4 | Q_BRACKET=0.98
L_GRID={13,34,89,233} | MIN_N=50 | RET=[0.25,0.90] | K_IDENTITY=1000 | K_PERM=2000
N_TRIALS=5013 | PIP=0.10 | COST_PIP=3.3

**امضا: نیوتن — قبل از دیدن هرگونه عدد آزمون S516.**
