# S530 — داوریِ تأییدیِ تمام‌تاریخیِ S355 · XAUUSD-M5 · RQS2 = **18.1 / REJECT** (روی ۱۵.۶ سال)

> | | |
> |---|---|
> | **نوع آزمون** | تأییدی (confirmatory) — قاعدهٔ منجمد، صفر جست‌وجو، `n_trials=1` |
> | **موضوع** | لایهٔ **زندهٔ** `S355` روی کارتِ `XAUUSD-M5` سایت |
> | **حکم روی دادهٔ کامل** | `REJECT` (18.1) — H3 ✗ · H6 ✗ · H8 ✗ |
> | **حکم پیش‌ثبت‌شدهٔ سه‌گانه** | **`REGIME-ONLY`** (تعریف در پیش‌ثبت، §۴) |
> | **پیش‌ثبت** | `results/S530_PREREGISTRATION_S355_FullHistory_Adjudication.md` · commit `442f2e0c` (قبل از اجرا) |
> | **مصنوع خام** | `results/_s530/full_adjudication.json` · commit `342771a9` |
> | **معیار حاکم** | `engine/rqs2.py` v2.6 · نالِ اندازه‌گیری‌شده K=500 · seed=20260813 |

⚠️ **این REJECT حکمِ ACCEPT کارتیِ S355 را باطل نمی‌کند — آن را مشروط می‌کند.**
حکم ACCEPT اصلی روی پنجرهٔ ۲.۸ ساله صادر شد و همین آزمون همان اعداد را سکه‌به‌سکه
بازتولید کرد. آنچه این آزمون اضافه می‌کند: لبهٔ لایه **مختصِ رژیم اخیر** است.

---

## ۱. داده — الزام صریح کاربر (گارد BUG-DATASETDRIFT)

```
مسیر:  data/mt5_full/XAUUSD_M5.csv
ردیف:  1,089,574 کندل
بازه:  2011-01-03 → 2026-08-07  (۱۵.۵۹ سال)
```

## ۲. قاعدهٔ منجمد (هیچ پارامتری در این نشست انتخاب نشد)

```
mask = s333.build_layer(df, BEST_CFG['XAUUSD_M5']) & (lpsb_state(L=8, f=0.33) == -1)
هندسه: sl=120pip · tp=120pip · max_hold=96  (خوانده از BEST_CFG، رونویسی نشد)
شبیه‌ساز: se.simulate_trades(asset='XAUUSD', allow_overlap=False)  — اسپرد 3.3pip، کمیسیون ۰
```

## ۳. جدول دروازه‌ها روی کل ۱۵.۶ سال

| دروازه | عدد اندازه‌گیری‌شده | آستانه | نتیجه |
|---|---|---|---|
| H0 | n=160 · بدون همپوشانی | n≥30 | ✅ |
| H1 | PF=1.573 · top_win_share=0.017 | PF≥1.3 | ✅ |
| H2 | WR=56.88% vs BE_cost=51.38% ⇒ +5.5pp · RR=1.0 | ≥3pp | ✅ |
| **H3** ⭐ | lift=+8.05pp · **z=2.04** · p_perm=0.0208 · K=500 | p≤0.001 (z≥3.09) | ❌ |
| H4 | فقط LONG، همان اعداد | — | ✅ |
| H5 | هر دو نیمه — | — | ✅ |
| **H6** | پوشش تقویمی ناکامل‌مثبت روی ۱۵ سال | همهٔ فصول مثبت | ❌ |
| H7 | UNKNOWN (آزمون تأییدی، split اکتشافی ندارد) | — | ؟ |
| **H8** | **MCL=9** | ≤ حد مجاز | ❌ |
| H9 | exp=+14.69pip در برابر 3.3pip هزینه | — | ✅ |
| H10 | مقاومت رژیمی | — | ✅ |

نال اندازه‌گیری‌شده: `uncond_wr=48.82%` · `perm_mean=48.52` · `perm_sd=3.47` · `perm_max=61.24`.

## ۴. یافتهٔ مرکزی — تجزیهٔ زیرپنجره (ازپیش‌اعلام‌شده در پیش‌ثبت)

| پنجره | n | WR | net (pip) | نسبت به سربه‌سر 51.38% |
|---|---:|---:|---:|---|
| **اخیر** (2023-09 →) | 47 | **72.34%** | **+2,670** | +20.96pp — همان ACCEPT اصلی، بازتولید دقیق |
| **قبل از ۲۰۲۳** (2011 → 2023-09) | 113 | **50.44%** | **−320** | **−0.94pp ⇒ ضررده** |

**نتیجه‌گیری:** لبهٔ S355 «ضعیف‌شده روی تاریخ بلند» نیست — **قبل از رژیم روندی
۲۰۲۳-۲۰۲۶ اصلاً وجود ندارد**. ۱۱۳ معاملهٔ ۱۲.۵ سالِ اول زیر سربه‌سرِ هزینه‌دار
می‌نشینند و کل سود ۱۵ ساله از ۴۷ معاملهٔ ۳ سال اخیر می‌آید.

## ۵. حکم سه‌گانهٔ پیش‌ثبت‌شده ⇒ `REGIME-ONLY`

- CONFIRMED-FULL؟ خیر (H3 روی کل داده رد).
- BURNED؟ خیر (پنجرهٔ اخیر عیناً بازتولید شد: n=47, WR=72.34%، همان z=3.47 محلی).
- **REGIME-ONLY؟ بله** — دقیقاً تعریف پیش‌ثبت: لبه در رژیم اخیر واقعی، در تاریخ بلند غایب.

## ۶. پیامد برای کاربر (تصمیم با شماست — طبق پیش‌ثبت)

`S355` روی کارت `XAUUSD-M5` سایت زنده است. گزینه‌ها:
1. **ابقا با آگاهی**: مادامی که رژیم روندی طلا برقرار است لایه کار می‌کند؛ ریسک = بازگشت رژیم رنج/اره‌ای که طبق ۱۲.۵ سال شواهد این قاعده را ضررده می‌کند.
2. **افزودن دروازهٔ رژیم** (مسیر S530-revival، §۷): اگر موفق شود، هم لایه امن‌تر می‌شود هم شاید ACCEPT تمام‌تاریخی بگیرد.
3. حذف — محافظه‌کارانه‌ترین، ولی لبهٔ رژیم فعلی را هم دور می‌ریزد.

پیشنهاد من: گزینهٔ ۲ را می‌آزمایم؛ تا آن زمان لایه بماند (رژیم فعلاً برقرار است).

## ۷. گام بعدی ازپیش‌اعلام‌شده (از §۵ پیش‌ثبت — بدون تغییر)

احیا به‌عنوان `S530-revival`: دروازهٔ رژیم از خانوادهٔ منجمد S351
(`hurst55 / r2_34 / er_21 / chop_21` با آستانه‌های فیبوناچی) روی S355،
**مسیر C**: جست‌وجو فقط روی نیمهٔ اول ۱۵.۶ سال، یک آزمون روی نیمهٔ دوم.
پیش‌ثبت جداگانه پیش از اجرا.

## ۸. ردپای صداقت

- پیش‌بینی پیش‌ثبت‌شده («محتمل‌ترین: REGIME-ONLY») **قبل از اجرا** commit شد و محقق شد.
- بازتولید متقابل: n=160/WR=56.88% مستقلاً با عدد ثانویهٔ مأموریت ۴ یکی درآمد؛ زیرپنجرهٔ اخیر با ACCEPT اصلی (47/72.34%) یکی درآمد — دو اعتبارسنجی متقاطع رایگان.
- هیچ پارامتری لمس نشد؛ ستون زمان ورود از سورس موتور خوانده شد نه حدس (BUG-CFGKEYS تکرار نشد).

— اویلر · 2026-08-13
