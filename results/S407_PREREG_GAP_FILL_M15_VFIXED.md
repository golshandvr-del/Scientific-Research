# S407 — پیش‌ثبت: گپ‌فیل M15 با فیلتر نوسان **ثابت** · XAUUSD M15 · مسیر C

**تاریخ:** 2026-09-04 · **دانشمند:** بلوک S400–S409 · **داور:** RQS2 v2.6 (`compute_rqs2` — تنها مرجع حکم) · **seed=407**

## ۰) چرا این لایه (زنجیرهٔ استدلال از شواهد قبلی)

- S406 (REJECT 29.5) نشان داد لبهٔ WR گپ‌فیل روی M15 واقعی است (lift +28.7pp, z=9.5σ) اما **بدون V** DD از 1.62% به 16.93% منفجر می‌شود.
- قانون DD-انتقال حالا ۵ نقطه دارد: V فعال ⇒ DD منتقل (S404, S562, S405-tune)؛ V غایب ⇒ DD منفجر (S403, S406).
- در S406 قاعدهٔ قفل‌شدهٔ n≥100 ترکیب‌های V را (n=53–75 در نیمهٔ اول، کیفیت بالاتر) حذف کرد. این خطای **طراحی پیش‌ثبت** بود، نه خطای فرضیه.
- S407 همان پرسش علمی را با طراحی اصلاح‌شده می‌پرسد: **V غیرقابل‌مذاکره در همهٔ بازوها**؛ فضای جست‌وجو فقط روی (آستانه، k_sl).

⚠️ این یک «چرخهٔ دوم روی S406» نیست: S406 هولدآوتش را یک‌بار لمس کرد و حکمش نهایی است. S407 لایه‌ای متمایز است (مجموعهٔ معاملات متفاوت — V در همهٔ بازوها) با پیش‌ثبت مستقل و n_trials صادقانهٔ انباشته. هزینهٔ چندگانگی پرداخت می‌شود.

## ۱) تعریف مکانیکی (منجمد)

- داده: `data/XAUUSD_M15.csv` (همان S400/S406) · روز معاملاتی و گپ: `s400_gap_open.build_days` (منجمد)
- رویداد: گپ منفی روز (open اول − close روز قبل < 0) · |gap| > آستانهٔ علّی روزانه `thresholds_for_day(q)` (منجمد S400)
- DOW ≠ دوشنبه (S405: دوشنبه سوخته)
- **فیلتر V (ثابت در همهٔ بازوها):** `vol_flags` منجمد S404 — ATR14 روز قبل > صدک 0.78 تاریخچهٔ علّی 250 روز (min 60 obs). روز پرچم‌خورده ⇒ سیگنال رد.
- ورود: Buy در open کندل اول روز · TP = close روز قبل (فیل کامل) · SL = k_sl × |gap| · هزینه: SPREAD_PIP منجمد S401 · خروج اجباری در آخرین کندل روز · `sim_trade_be(..., be_f=None)` منجمد
- بدون BE، بدون time-stop، بدون cooldown (دفتر اهرم‌ها: خنثی/مضر)

## ۲) فضای جست‌وجو (قفل — ۶ ترکیب)

q ∈ {60, 70, 80} × k_sl ∈ {1.7, 2.0} — V=ATRq78 در همه.

## ۳) قاعدهٔ برنده (قفل، فقط نیمهٔ اول)

ok ⇔ n ≥ 50 ∧ PF > 1 ∧ t ≥ 2.5 ∧ maxDD ≤ 4.80% ∧ wr_edge ≥ 2pp ∧ rec ≥ 3.
از میان ok: کمینهٔ maxDD؛ tie → بیشترین t. اگر هیچ ok ⇒ REJECT فاز تیون (هولدآوت باکره).
کف n=50 در نیمهٔ اول ⇒ انتظار n_full ≈ 100–150 (H3 موتور تصمیم نهایی می‌گیرد).

## ۴) آزمون رسمی (تک‌لمس)
برنده روی کل داده → `compute_rqs2` (gates_only) · null بازو-محور: ورودهای بی‌قید گپ‌DOWN با همان k_sl **بدون** آستانه/DOW/V (این‌ها مهارت سنجیده‌اند) · K=500 زیرمجموعهٔ هم‌اندازه · seed=407.

## ۵) چندگانگی صادقانه
n_trials تجمعی: 274 + 6 = **280**.

## ۶) هم‌پوشانی
اگر ACCEPT: ممیزی روز-هم‌پوشانی با S404 (M30 فیل) و S562-M15 (لایبنیتس، time-exit). انتظار: هم‌پوشانی بالا با S562-M15 (همان روزها، همان V) ⇒ قانون پرتفوی یک-لایه-گپ-در-روز. ارزش متمایز S407 = هندسهٔ TP=فیل در برابر خروج زمانی.

## ۷) پیش‌بینی‌های ثبت‌شده
- P1: k_sl=2.0 برنده (مرز RR=0.5).
- P2: DD تیون→کل با نسبت ≤ 2× منتقل می‌شود (قانون V).
- P3: قاتل محتمل اگر بمیرد: H3/H7 (n کوچک ~100–130)، نه H8.
- P4: WR کل‌داده 88–95%.

## ۸) قواعد
مسیر C · هیچ فایلی از بلوک‌های S500–S980 لمس نمی‌شود (فقط import منجمد از s400/s401/s404 خودم) · سایت دست‌نخورده.
