# S645 — پیش‌ثبت: «برگشتِ Parabolic SAR» — XAUUSD — مسیرِ C

**نویسنده:** امی نوتر · **دکاد:** S640–S649 · **تاریخ:** 2026-09-03
**وضعیت:** پیش‌ثبت — **پیش از هر محاسبه روی نیمهٔ دوم** commit می‌شود.

## ۰. فرضیه و اعترافِ پیش‌نگر

Parabolic SAR (Wilder 1978؛ step=0.02، max=0.2 — متعارف، **صفر پارامترِ آزاد**). رویداد: برگشتِ SAR
(flip). فرضیهٔ خودِ Wilder: پی‌گیریِ جهتِ جدید (cont) — لانگ در flip صعودی، شورت در flip نزولی.

**اکتشافِ نیمهٔ اول:** همهٔ سلول‌های n≥200 ضعیف‌اند: plain/cont در H3S +2.56، H4L +2.19، H4S +1.89؛
تنها سلولِ قوی H12L +7.27 (n=153) است. استخرِ در-نمونهٔ plain/cont روی {H3,H4,H6,H8,H12} lift≈+1.8، lsn≈80.
با قانونِ نصف‌شدن (S641) انتظار ≈40 ≪ 78. **پیش‌بینیِ ثبت‌شده: REJECT** (احتمالِ ذهنیِ ACCEPT < ۵٪).
این آزمون برای تکمیلِ حکمِ رسمیِ شمارهٔ S645 اجرا می‌شود (دستورِ کاربر: هر شماره یک حکمِ موتور).

## ۱. ممیزیِ برخورد

`grep -ril "psar|parabolic" results/S[5-9]*.md research/S[5-9]*.md` = ۱ (S873 فقط در فهرستِ grep نام برده؛ خانواده‌اش DMI است).
PSAR در هیچ پیش‌ثبتِ S500–S980 خانوادهٔ اصلی نیست. هیچ لایهٔ زنده‌ای PSAR ندارد.

## ۲. مشخصاتِ منجمد

| مؤلفه | مقدار |
|---|---|
| دارایی / داده | XAUUSD · `data/mt5_full/` |
| کارت‌ها | {H3, H4, H6, H8, H12} |
| اندیکاتور | `indicator_bank.psar(df, step=0.02, mx=0.2)` بدونِ تغییر |
| لانگ | SAR[t−1] ≥ close[t−1] **و** SAR[t] < close[t] (flip به صعودی) |
| شورت | آینه (flip به نزولی) |
| SL / TP / hold | 1.5×median(ATR100) نیمهٔ دوم · TP=SL · max_hold=64 · allow_overlap=False |
| دوره | فقط `df.iloc[len//2:]` |
| نول | جایگشتِ هم‌هندسه/هم‌تعداد به تفکیکِ سمت، K=500، seed=645645 |
| استخر / محور / قاضی | `rqs2_pool.pool_cards` · محورِ ۵ دقیقه‌ای · holdout_mask=چندکِ ۵۰٪ · `compute_rqs2` **یک بار**، n_trials=1 |

هیچ گونه/حالت/کارتِ دیگری آزموده نمی‌شود. REJECT → آرشیو؛ **v2 ندارد.**

## ۳. کدِ اجرا

`strategies/s645_final_holdout.py` — پس از این commit، **یک بار**.
خروجی: `results/S645_PsarFlip_Xauusd_H3H4H6H8H12_rqs2_<score>_<VERDICT>.md`
