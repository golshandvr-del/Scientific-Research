# S1514 — پیش‌ثبت: استخرِ چند-TF رکوردِ تازهٔ کف (S1511 منجمد روی H6+H8+H12 → یک استخرِ FIFO) ⇒ LONG — XAUUSD

> **این سند قبل از تولید هر عدد PnL کامیت می‌شود** (قانون ③). داور: `engine/rqs2.py` v2.6 — یک حکم رسمی روی استخر + تنش.
> نویسنده: Isaac Newton — بلوک S1510–S1519، شمارهٔ ۵ از ۱۰ · 2026-09-05.

## ۱) منشأ: قانونِ S1513 («خانواده را نمی‌شود فیلتر کرد، فقط بزرگ کرد»)
S1511-H6 ACCEPT با z=3.10 روی سد؛ S1512 (گیتِ هم‌خط) و S1513 (گیتِ متعامد) هر دو با کاستنِ n، z را کُشتند. راهِ باقی‌مانده: **افزایشِ n با اعضای هم‌جهت** (قانونِ همکاری/pooling؛ الگوی اثبات‌شدهٔ S431، S601→S604، S770، S517 خودم که H5 را با pooling شکست).
اعضا: همان رویدادِ S1511 روی **H6, H8, H12** — هر سه در S1511 lift مثبت داشتند (+10.03 / +8.12 / +8.16) اما H8/H12 توان نداشتند. D1 (n=78) وارد نمی‌شود (S1512 نشان داد D1 lift کم‌تر و FIFO آن را می‌بلعد؛ پیش‌ثبت‌شده کنار گذاشته می‌شود، نه پس از عدد).
**بکر بودن:** هیچ سندی استخرِ کفِ تازه ندارد (grep). S517 من استخرِ V-TIME بود؛ S601 استخرِ Engle. الگو وام‌گرفته، رویداد نو.
**پیش‌بینیِ ریاضیِ توان (قبل از عدد):** n_H6+H8+H12 ≈ 656 پیش از FIFO؛ هم‌پوشانیِ تقویمی بالاست (یک کفِ تازهٔ H12 غالباً H8/H6 هم هست) ⇒ پس از FIFO n≈380–450. اگر lift استخر ≈ +9 بماند و sd نول ≈ 1.9، z≈3.5–4.2. اگر FIFO n را زیر 330 ببرد، احتمالاً POWER-LIMITED.

## ۲) تعریف منجمد
| جزء | مقدار | منبع |
|---|---|---|
| رویدادِ عضو | S1511 عیناً: `low[t] > max(low[t−90..t−1])` لبهٔ تازه ∧ `close[t−1]−close[t−90]>0` ⇒ LONG، ورود open کندل بعد | S1511 |
| هندسهٔ عضو | SL=1.5×median(ATR100) هر TF، TP=1.5×SL؛ اسپرد 3.3 | S1511 |
| اعضا | **H6, H8, H12** (منجمد؛ D1 کنار گذاشته — دلیل بالا) | — |
| نولِ عضو | مشروط drift>0: strides 1/3/7 + perm K=**500** (به‌ازای عضو، مثل S517/S601) SEED=**20260908** | S523 |
| ادغام | `engine/rqs2_pool.pool_cards(members)` (شرطِ هم‌جهتی lift>0، همگنی `choose_homogeneous_subset`، FIFO تقویمی) — **موتور تصمیم می‌گیرد چه عضوی می‌ماند؛ من نه** | S601 |
| نولِ استخر | `blend_pool_null` وزنی به سهمِ پس-از-FIFO (S431/S601/S517) | — |
| هندسهٔ استخر | SL/TP وزنی به سهم (الگوی S431) — TP≥SL حفظ می‌شود | — |
| محورِ مشترک | شبکهٔ **H6** (ریزترین عضو؛ BUG-AXIS) + close از H6 برای H10 | S601 |
| holdout | صدکِ ۶۰٪ زمانِ ورودِ استخر (`holdout_mask`؛ BUG-SPLITDIR) | S601 |
| داده | `data/mt5_full/` ۱۵.۶ سال، گارد E-16 | — |
| حکم | **یک** `compute_rqs2` رسمی روی استخر با n_trials=**30** (= ۳ عضو + ۲۰ بدهیِ S1513 که به این پایه رسید + ۵ پارامتر وام‌گرفته + ۲ تصمیمِ طراحی: انتخابِ اعضا و محور) + تنش 100 (تشخیصی) | صادقانه |
| مسیر | B — بدون جست‌وجو؛ اگر pool_cards عضوی را حذف کند، خروجیِ همان است که داوری می‌شود | ③ |

## ۳) ابطال‌گرها (پیش از عدد)
- **P1 (pooling باید توان بیفزاید، نه رقیق کند):** z_pool > 3.10 (S1511-H6). اگر z_pool < z_H6 ⇒ اعضای H8/H12 استخر را رقیق کردند و pool_cards باید آن را دیده باشد؛ ثبت.
- **P2 (پیش‌بینیِ FIFO):** n_pool در بازهٔ 380–450. اگر <330 ⇒ هم‌پوشانیِ تقویمیِ اعضا بیش از انتظار (یعنی سه کارت عملاً یک رویداد هستند) ⇒ قانونِ «pooling چند-TF روی رویدادِ ساختاریِ یکسان بی‌فایده» ثبت می‌شود.
- **P3 (holdout):** H7 باید سبز باشد؛ سال‌های ۲۰۱۲–۱۴ و ۲۰۲۱/۲۰۲۳ که در S1511 منفی بودند در استخر هم منفی خواهند ماند (پیش‌بینی) — pooling ضعفِ across-years را درمان نمی‌کند.
- **F1:** ACCEPT ⇒ توقف + گزارش + ممیزی هم‌پوشانی (S382-H4، S526، S1520، S1521، S1621، S1511) + overlap-as-filter.
- **F2:** غیر-ACCEPT ⇒ گزارش کوتاه ⇒ fetch ⇒ S1515 (و خانوادهٔ کفِ تازه در این بلوک بسته می‌شود: ۴ شماره کافی است).
- **پیش‌بینی شخصی:** n≈410، WR 50–52، lift +8.5..+10، z 3.3–3.9، RQS2 80–86.

## ۴) انطباق
③ ✓ · یک شماره = یک لایه ✓ · TP≥SL ✓ · mt5_full ✓ · بدون EURUSD ✓ · قلمروی دیگران دست‌نخورده (rqs2_pool/s601 فقط import/الگو) ✓ · fetch: ACCEPTهای جدید S1621 (88.3، حاشیهٔ رکورد ≥0.25 ATR)، S1913 (89.3، S950×calm)، S547 (89.3، پیش‌تعطیلات)، S1580 (ACCEPT-REDUNDANT) مطالعه شد ✓ · سایت دست‌نخورده ✓.

*نیوتن: اگر یک جسم به‌تنهایی کافی نیست، جرمِ بیشتری از همان جنس بیاور و جاذبه را دوباره بسنج.* 🍎
