# S573 — پیش‌ثبتِ الزام‌آور: افزودن عضو M15 به استخر آینه‌ای S323

**تاریخ:** 2026-08-19 · **نویسنده:** پوانکاره (قلمرو S570–S579)
**وضعیت:** منجمد — پیش از هر اجرای تعیین‌کننده کامیت می‌شود.
**والد:** S572 (ACCEPT مستحکم 78.7/78.4، کامیت 2b967d11) ← S571 ← S570 ← S323.

## ۱) انگیزه (از اندازه‌گیری‌های ثبت‌شدهٔ S570، نه داده‌کاوی جدید)

S570 روی هندسهٔ موروثی اندازه گرفت: XAUUSD-M15 با پیکربندی منجمد DEPLOYED_CFG
دارای n=293 و lift=+6.9pp است (یکنوایی TF-lift). پرسش S573: آیا افزودن M15 به
استخر آینه‌ای S572، با بزرگ‌کردن n (~۴۵۰ پیش از FIFO)، z را بالا می‌برد بی‌آنکه
رقیق‌سازی لبه را بکشد؟

## ۲) تعریف دقیق — یک تصمیم، صفر پارامتر آزادِ دیگر

- اعضا: **{M15, M30, H1}** — هر سه با سیگنالِ منجمدِ `signals_backtested` و
  پیکربندی DEPLOYED_CFG (M15: nearMax=0.85, adx=22, slMult=1.8, tpMult=1.5,
  maxHold=96).
- هندسهٔ آینه‌ای برای هر عضو: SL=tpMult·ATRmed، TP=slMult·ATRmed
  (M15: SL=1.5·ATR, TP=1.8·ATR, RR=1.2).
- دستورکار استخر: عیناً S572 (pool_cards با همگنی greedy، محور مصنوعی —
  این‌بار گام = **۱۵ دقیقه** چون ریزترین عضو M15 است — close از H1 با نگهداشت،
  holdout صدک ۶۰٪ زمان ورود، نول blend وزنی، هندسهٔ وزنی).
- نول‌ها: build_null با هندسهٔ آینه‌ایِ هر عضو، K=2000، بذرها 23/101/777.

## ۳) بودجه و قیود توقف

- **n_trials = 2803** (میراث S572=2802 + این تصمیم) ⇒ میلهٔ H5 ≈ 3.5374.
- **تنش = 5606** ⇒ میله ≈ 3.7168.
- **C5⁗-الف (عضویت):** انتخاب همگنی توسط pool_cards (z_proxy=lift·√n،
  حاشیهٔ 0.15) حاکم است. **اگر M15 توسط greedy حذف شود ⇒ استخر همان S572
  می‌شود که قبلاً داوری شده ⇒ توقف بدون داوری مجدد** (ضد double-dip).
- **C5⁗-ب (رقیق‌سازی):** lift_pool ≥ **+8.0pp** (همان S572)، وگرنه REJECT-STOP
  بدون داوری RQS2.
- **قید RR:** هندسهٔ وزنی نهایی باید TP≥SL (assert).
- فقط XAUUSD؛ EURUSD ممنوع.

## ۴) معیار حکم — یک داوری، بدون تکرار

بذر حاکم 23، حساسیت 101/777؛ حکم موتور عیناً گزارش می‌شود. اگر honest=ACCEPT و
stress=REJECT ⇒ برچسب «ACCEPT شکننده» و ارجاع به کاربر. REJECT ⇒ این گسترش
می‌میرد (S572 سرجایش معتبر می‌ماند — این آزمونِ گسترش است، نه بازداوری والد).

## ۵) پیش‌بینی ثبت‌شده (برای ارزیابی صداقت پیشینی)

سهم M15 در استخر بزرگ است (~۶۰٪)؛ اگر lift آینه‌ای M15 حدود +5..7pp باشد،
lift استخر ≈ 8.5..10pp و z ≈ 3.7..4.1. یعنی این آزمون واقعاً می‌تواند به هر
دو سو بیفتد — آزمونی منصفانه است، نه نمایشی.
