# S518 — پیش‌ثبت: درمان اقتصادی استخر V-TIME با پنجرهٔ نشستی علّی — XAUUSD

**قلمرو**: S510–S519 · **لایه**: S518 · **تاریخ**: 2026-08-26 · **SEED=20260824**
**موتور**: RQS2 v2.6 · **مسیر چندگانگی**: C · **این سند پیش از هر عدد commit می‌شود.**

## ۱. فرضیهٔ علمی
S517 سیگنال را اثبات کرد (استخر M15+M30، z=4.4 > سد 3.688، lift=+7.41pp) اما
اقتصادش مرد (PF=1.00): خروج زمانی PnL تقریباً متقارن می‌سازد و امید پیپی
زیر هزینه می‌مانَد. سود واقعی رخداد انبساط-نوسان باید در پنجره‌های نشستیِ
پرجنبش (لندن/نیویورک) متمرکز باشد که «حرکت به‌ازای کندل» بزرگ‌تر از هزینهٔ
ثابت 3.3pip است (سابقهٔ ACCEPT: S404 پنجرهٔ نوسان؛ S312/S432 انتخاب تقویمی).
پنجرهٔ ساعت ورود ۱۰۰٪ علّی است و اطلاعاتش (زمان روز) با سیگنال (انبساط
نوسان نسبی) و با رانه (S516) متعامد است.

## ۲. پایهٔ منجمد (صفر درجهٔ آزادی)
استخر S517 عیناً: اعضای {M15, M30} با قانون منجمد S515
(atr_fib_55 عبور↑q90(کشف)/LONG، k=4، براکت q98 هر کارت)، ادغام FIFO تقویمی،
نول ترکیبی وزنی، محور M15، هندسهٔ وزنی. هیچ پارامتری از پایه تغییر نمی‌کند.

## ۳. خانوادهٔ فیلتر (تنها درجات آزادی — از پیش شمرده: ۴ آزمایش)
ساعت ورود UTC معامله (از t_entry استخر) در یکی از ۴ پنجرهٔ نشستی:
- W1 آسیا: 00–07 · W2 لندن: 07–12 · W3 هم‌پوشانی لندن/NY: 12–17 · W4 NY متأخر: 17–24
**قاعدهٔ انتخاب (فقط روی کشف = ۶۰٪ نخستِ زمان ورود استخر):**
- n_کشف ≥ 60 و retention ∈ [0.20, 0.80]
- میانگین پیپ (net) در پنجره > میانگین پیپ پایه در **هر دو نیمهٔ** کشف
- برنده = بیشینهٔ t-stat پیپ خالص در کشف. اگر هیچ پنجره‌ای معتبر نبود ⇒ توقف
  بدون داوری (REJECT-by-no-candidate؛ بدهی حفظ می‌شود).

## ۴. آزمون هویت اقتصادی (تیغ S512/S516 — این‌بار روی پیپ، نه WR)
K=1000 زیرنمونهٔ تصادفی از معاملات کشفِ پایه با همان retention برنده، بذر SEED.
آماره: **میانگین پیپ خالص**. گیت: `P(rand ≥ obs) ≤ 0.05` (p دقیق).
شکست ⇒ REJECT-by-identity بدون خرج داوری.

## ۵. داوری (یک‌باره)
- فیلتر برنده روی کل استخر (کشف+OOS) اعمال؛ همان محور/هندسه/نول ترکیبی S517
  (نول WR نشست-ناوابسته است؛ تغییر نمی‌کند — محافظه‌کارانه).
- holdout = همان مرز صدک ۶۰٪ زمان ورودِ **پایه** (منجمد از S517).
- **یک** فراخوان رسمی `compute_rqs2(pool_filtered, 'XAUUSD', n_trials=5024)`
  + تنش 8000. **n_trials صادقانه = 5024** = 5019 (بدهی تا S517) + 4 (پنجره‌ها) + 1 (آزمون).

## ۶. پیش‌بینی ابطال‌پذیر
اگر فرضیهٔ تمرکز نشستی درست باشد: پنجرهٔ برنده PF ≥ 1.30 و z ≥ 3.7 نگه می‌دارد.
اگر امید پیپی در همهٔ پنجره‌ها یکنواخت باشد ⇒ فرضیه ابطال؛ اهرم نشستی روی این
هسته می‌سوزد و هسته به وارث بعدی می‌رسد.

## ۷. برنامهٔ اقتضایی (پیش از دیدن هر عدد)
- REJECT ⇒ گزارش خلاصه، fetch، ادامهٔ خودکار با S519 (قانون کاربر).
- غیر-REJECT ⇒ ممیزی هم‌پوشانی فوری با همهٔ ACCEPTهای زنده + آزمون بخش
  هم‌پوشان به‌عنوان فیلتر. ACCEPT ⇒ **توقف و گزارش فوری به کاربر.**

*پایان پیش‌ثبت — از این نقطه هیچ تصمیمی خارج از این سند گرفته نمی‌شود.*
