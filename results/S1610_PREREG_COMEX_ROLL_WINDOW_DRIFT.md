# S1610 — پیش‌ثبت (قبل از هر محاسبهٔ PnL): درفت پنجرهٔ رولِ فیوچرز COMEX (First-Notice) — XAUUSD — LONG-first

**دانشمند:** اواریست گالوا · **بلوک دوم:** S1610–S1619 (قانون +۱۰۰۰) · شمارهٔ ۱ از ۱۰ · **تاریخ:** 2026-09-06
**داور:** `engine/rqs2.compute_rqs2` v2.6 دست‌نخورده · **داده:** `data/mt5_full/` ۱۵.۵۹ سال، `assert 'mt5_full' in src`
**این سند قبل از نوشتن هر کدی که PnL می‌بیند کامیت+push می‌شود.**

## ۰) اصول بلوک دوم (از نقد دههٔ اول — `S610-S619_BLOCK_FINAL_REPORT_Galois.md`)
1. رویداد نادر ساختاری با **لنگر نهادی بیرونی** · TF درشت · LONG-first · نول شرطی/هندسه‌محور.
2. **شمارش رویداد پیش از قفل** (PnL-کور): انجام شد — ۹۴ پنجرهٔ رول در ۱۵.۶ سال (۶.۰/سال)، ۴۷ در هر نیمه؛ ۴۷۰ روز معاملاتی.
3. **پیش‌بینی تیغ‌دار مقایسه‌ای** با یک لایهٔ زندهٔ نام‌برده (§۶).

## ۱) ممیزی قلمرو (grep ثبت‌شده، pull تازه 2026-09-06)
`first.notice|delivery.month|contract.roll|futures.roll|roll.(week|window|date)|تحویل|GC[FGHJKMNQUVXZ]` در `results/S*.md`, `strategies/*.py`: **صفر** لایه (فقط واژهٔ «rolling» در S628 و «تحویل» به‌معنای deliver در S374). S971 «Roll» = Richard Roll 1984 (خودهمبستگی)، بی‌ربط.
همسایگان تقویمی و تمایز: **S895 TOM** (TD−1..TD+3 هر ماه؛ **همهٔ** ماه‌ها؛ REJECT 7.3) — S1610 فقط ۶ ماه پیش‌تحویل و پنجره **TD−5..TD−1** (نه TD−1/TD0)؛ **S547 پیش‌تعطیلات** (ACCEPT 89، Ariel؛ روز قبل از تعطیلات رسمی) — تقاطع فقط چند روز در سال؛ **S312/S432 میان‌ماه** (dom 10/13/20) — هیچ تقاطع؛ **S306/S310 EOM** — پنجرهٔ TD−2..TD0 همهٔ ماه‌ها. تقاطع روزهای S1610 با EOM/TOM محدود به TD−1 است؛ در ممیزی هم‌پوشانی جدا گزارش می‌شود (§۷).
مرور ACCEPTهای تازه (S547، S569، S802، S1740، S1912): همه خوانده شد؛ هیچ‌کدام رول فیوچرز نیست.

## ۲) نظریهٔ اقتصادی (لنگر نهادی)
قرارداد GC فعال در ماه‌های تحویل Feb/Apr/Jun/Aug/Oct/Dec است. **First Notice Day** = آخرین روز کاری ماهِ *قبل از* تحویل. در ~۵ روز معاملاتی پیش از آن، دارندگان long غیرتحویلی (ETFها، CTAها، بانک‌ها) باید front را بفروشند و deferred را بخرند (روزهای «Goldman roll» در کالاها؛ Mou 2011 «Limits to Arbitrage and Commodity Index Investment»؛ Bessembinder et al. 2016 «Liquidity costs of roll»). اثر شناخته‌شده در نفت/کشاورزی: **فشار قیمتی پیش‌بینی‌پذیر** روی spread و اغلب روی سطح قیمت لحظه‌ای. طلا کم‌مطالعه است. XAUUSD اسپات به فیوچرز فعال قفل است ⇒ اگر فشار رول جهت‌دار باشد، در اسپات دیده می‌شود.
**جهت پیشینی:** ادبیات کالا (Mou) نشان می‌دهد در روزهای رول، front تحت فشار فروش است ⇒ **قیمت لحظه‌ای ضعیف در پنجره** و **بازیابی پس از FND**. اما قانون جهت‌مندی طلا (S522/S528/S618) می‌گوید فقط LONG اطلاعات دارد. **دو بازو پیش‌ثبت می‌شوند و هر دو گزارش:** بازوی A = SHORT درون پنجره (TD−5 → TD−1)؛ بازوی B = LONG پس از FND (ورود اولین بار روز TD0 ماهِ پیش‌تحویل، خروج تا ۵ روز معاملاتی بعد). n_trials هر دو را می‌شمارد.

## ۳) قاعدهٔ منجمد
- تقویم: روز معاملاتی = تغییر تاریخ سرور در D1 `mt5_full`. ماه پیش‌تحویل ∈ {1,3,5,7,9,11}. `tdl` = تعداد روزهای معاملاتی باقی تا آخر ماه (۰=آخرین). **پنجرهٔ A:** روزهای tdl∈{5,4,3,2,1} ⇒ سیگنال = آخرین بار TF کارت در روز tdl=6 (یا اولین بار روز tdl=5 − ۱)؛ ورود open بار بعد؛ خروج V-TIME: SL=TP=k×ATR34(Wilder، تا بار سیگنال)، k∈{1.272, 2.058}، اسکن تا آخرین بار روز tdl=1 با تقدم SL، وگرنه close آن بار. **پنجرهٔ B:** سیگنال = آخرین بار روز tdl=1 (FND-eve)؛ ورود open بار بعد (اولین بار FND)؛ همان هندسه؛ اسکن ۵ روز معاملاتی.
- کارت‌ها: TF ∈ {H4, D1} × k ∈ {1.272, 2.058} × بازو {A-short, B-long} ⇒ **۸ نقطه**. کارت اصلی از پیش اعلام‌شده: **B-long H4 k=1.272** (سازگار با قانون جهت‌مندی؛ اگر A برنده شود P1 ابطال و ثبت).
- **فقط یک معامله در هر پنجره** (۹۴ رویداد ⇒ n≈۹۴ هر بازو؛ نیمهٔ اول ۴۷).
- نول (S612): سکهٔ منصف روی همان رویدادها/هندسه K=1000 SEED=20260908 (متقارن؛ برای بازوی LONG علاوه بر آن مرجع **درفت‌شرطی D1** گزارش می‌شود: ورود LONG در هر روز غیررول با درفت ۳۰روزه مثبت — تشخیصی).
- **اکتشاف:** فقط نیمهٔ اول (۴۷ پنجره). **واجد:** n₁≥40 ∧ net₁>0 ∧ lift₁≥z_luck×perm_sd. **n_trials=8+2 (تشخیصی)=10** ⇒ z_luck=Φ⁻¹(1−1/10)=**1.28**… — نه: قانون شخصی، حداقل بودجه ۵۰ برای هر لایهٔ تقویمی (محور تقویم بارها لمس شده؛ S613/S614/S619 من) ⇒ **n_trials=50، z_luck=2.05** برای پیش‌شرط توان؛ compute_rqs2 با n_trials=50.
- مرگ شرافتمندانه ⇒ REJECT 0، هولد‌اوت بکر. واجد ⇒ کل داده روی بهترین کارت + compute_rqs2 (۵ ورودی الزامی، split_bar = مرز نیمه). ACCEPT ⇒ توقف، ممیزی هم‌پوشانی با S547/S895/S312/S432، گزارش.

## ۴) شمارش پیشینی (PnL-کور، انجام‌شده)
۹۴ پنجره / ۴۷+۴۷. H4: ~۶ بار در روز ⇒ پنجرهٔ A ≈ ۳۰ بار H4، B ≈ ۳۰ بار. با k=1.272×ATR34 در ۳۰ بار H4، احتمال رسیدن به SL/TP بالا ⇒ WR سکه ≈ ۴۹–۵۰. ATR34 روی D1 با k=1.272 و ۵ بار: بسیاری در پایان پنجره بسته می‌شوند ⇒ نول هندسه‌محور ضروری (S612).

## ۵) پیش‌بینی‌های ابطال‌پذیر
- **P1 (جهت‌مندی):** بازوی B-long > بازوی A-short در lift (هر TF). ابطال ⇒ فشار رول واقعی و طلا **استثنای** قانون جهت‌مندی؛ ثبت می‌شود.
- **P2 (تیغ مقایسه‌ای با S547 پیش‌تعطیلات — لایهٔ زندهٔ تقویمی هم‌مقیاس):** اگر کارت اصلی ACCEPT شود، باید در هم‌پوشانی روز-ورود با S547 **< ۲۰٪** باشد (تعطیلات رسمی ≈ ۹/سال، رول ۶/سال؛ تقاطع نادر) — وگرنه «زائد» اعلام می‌شود حتی با ۱۱ گیت سبز.
- **P3 (H4 ≥ D1 در lift):** ۵ روز = ۳۰ بار H4 اما فقط ۵ بار D1؛ هندسهٔ D1 برای اثر چندروزه فراخ است.
- **P4 (نقطهٔ صفرِ صادقانه):** اگر هر ۸ نقطهٔ نیمهٔ اول |lift|<3pp ⇒ رول طلا در اسپات **بی‌اثر** است (بازار کارا؛ رول در spread جذب می‌شود نه سطح) — نتیجهٔ علمی معتبر، محور بسته.

## ۶) خروجی
`strategies/s1610_comex_roll.py` (explore/verdict) · `results/_s1610_roll/{calendar_check.json, grid_first_half.json, decision.json[, verdict.json]}` · MD نهایی `S1610_ComexRollWindowDrift_Xauusd_H4D1_rqs2_<score>_<verdict>.md`.

*این پیش‌ثبت قبل از نوشتن هر کد PnL کامیت می‌شود. شمارش رویداد قبلاً بدون PnL انجام شد.* 🔒

## ۷) دستور کاربر (2026-09-06، پیش از کامیت این سند)
**«از این به بعد کار تو فقط اکتشاف است و هرگز نباید چیزی در سایت تغییر بدهی.»** ⇒ هیچ فایلی در `web_tool/`، `local-mobile/`، رجیستری لایه‌ها یا `strategies/sim_strategies.py` توسط من لمس نمی‌شود — در هیچ حالتی، حتی ACCEPT. خروجی من فقط: پیش‌ثبت، اسکریپت پژوهشی مستقل در `strategies/s16xx_*.py`، رکورد در `results/_s16xx_*/`، MD حکم.
