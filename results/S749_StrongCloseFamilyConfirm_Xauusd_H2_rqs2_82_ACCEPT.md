# S749 — تأیید قاعدهٔ منجمد S748 با قیمت‌گذاری کامل چندگانگی خانواده · XAUUSD-H2 · ✅ ACCEPT 82.3

**نویسنده**: عمر خیام نیشابوری (بازهٔ S740–S749 — لایهٔ ۱۰ از ۱۰)
**تاریخ**: 2026-09-04 · **داور**: `engine/rqs2.py::compute_rqs2` v2.6 دست‌نخورده (۱۱ گیت وتو)
**پیش‌ثبت**: `results/S749_PREREG_STRONG_CLOSE_FAMILY_MULTIPLICITY_CONFIRM.md` (کامیت `98f4dc45` — **قبل** از کد و هر عدد)
**کد**: `strategies/s749_family_confirm.py` · **رکورد**: `results/_scan_S749/S749.json` + `run.log`
**داده**: `data/mt5_full/XAUUSD_H2.csv` — 15.59y (2011-01 → 2026-08)، 47,623 کندل، split_bar=28,573
**وضعیت**: ⏸ توقف طبق دستور — هیچ استقراری.

---

## ۰) حکم رسمی موتور — عیناً

```
S749-A-H2 (n_trials=640)      | ACCEPT        RQS2=82.3 | n=271 WR=58.67% PF=1.62 lift=+10.17pp z=3.35 | H0..H10 همه ✓ · notes=[]
S749-A-H2 STRESS (2000)       | REJECT        RQS2=28.0 | فقط H5 ✗ (z_margin −0.10σ)
S749-B-POOL H2+H4 (640)       | POWER-LIMITED RQS2=20.7 | n=391 lift=+9.07pp z=2.76 | H3 ✗ (n_req 290) · H10 نامعلوم
```

**حکم رسمی لایه (قاعدهٔ پیش‌ثبت §۴ = بازوی A با 640): ✅ ACCEPT 82.3.**

## ۱) چرا این ACCEPT مشروع است — و مرزهایش

S748-H2 با n_trials=32 حکم ACCEPT 83.9 داشت اما به‌دلیل قرنطینهٔ TF (holdout H2 قبلاً در S747 با پیکربندی دیگری REJECT 10.0 لمس شده بود) به UNPROVEN کاسته شد. S749 **قاعده را صفر تغییر داد** (پارامترها فقط از JSON S748 خوانده شدند؛ بازتولید بیت‌به‌بیت: n=271, WR=58.67 ✓) و به‌جای قرنطینهٔ «همه یا هیچ»، **تمام ۶۴۰ سلولی که خانوادهٔ strong-close در ۳ لایه × ۲۰ TF آزموده بود** را در n_trials قیمت‌گذاری کرد:

| n_trials | z_luck_bound | z_obs | margin | حکم |
|---|---|---|---|---|
| 32 (S748) | 2.10 | 3.35 | +1.25σ | ACCEPT 83.9 (قرنطینه ⇒ UNPROVEN) |
| **640 (رسمی S749)** | **3.126** | **3.35** | **+0.22σ** | **ACCEPT 82.3** |
| 2000 (تنش) | 3.447 | 3.35 | −0.10σ | REJECT (H5) |

پیش‌بینی P1 پیش‌ثبت («margin به ≈+0.4σ کاهش می‌یابد») **تأیید شد** (+0.22σ). پیش‌بینی P2 («تنش 2000 مرزی/شکست») **تأیید شد**. پیش‌بینی P3 («H6 حذف می‌شود، استخر A را بهبود نمی‌دهد») **تأیید شد**.

**مرزهای صداقت (باید همراه حکم نقل شود):**
1. لبه با margin **+0.22σ** ACCEPT است — مرزی‌تر از S606 (+0.45σ) و مشابه S602 (+0.19σ). با n_trials≈1000 می‌شکند.
2. holdout H2 یک بار پیش (S747، پیکربندی متفاوت، REJECT) دیده شده؛ این در 640 قیمت‌گذاری شده اما «بکر» نیست. ایدهٔ dual-drift از اکتشاف H1 و S604 آمد، نه از holdout H2.
3. p_adj_bonferroni=0.32 (تحت 640) — Bonferroni کامل رد می‌کند؛ موتور با z_luck_bound (بیشینهٔ مورد انتظار از 640 آزمون نویز) داوری می‌کند که معیار رسمی پارادایم است. هر دو عدد گزارش شد.
4. **توصیه: forward-test** قبل از هر استقرار.

## ۲) قاعدهٔ قابل حمل (منجمد، صفر پارامتر آزاد)

| جزء | مقدار |
|---|---|
| کارت | XAUUSD-H2 |
| رخداد | `high−low ≥ 2.5×ATR100` (ATR ساده، causal) |
| موقعیت بسته | `(close−low)/(high−low) ≥ 0.8` و `close>open` |
| رانش دوگانه | `close > SMA100[i−1]` **و** `close > SMA300[i−1]` (~۲۵ روز معاملاتی) |
| جهت | **long_only** |
| ورود/خروج | open کندل بعد · SL=2.5×ATR100 · TP=1.5×SL (میانه 133/200 پیپ) · max_hold=16 · بدون هم‌پوشانی |
| هزینه | 3.3 پیپ |

## ۳) اعداد کامل بازوی A

| سنجه | مقدار |
|---|---|
| n / WR | 271 (همه long) / 58.67٪ |
| BE_cost / null_ref (perm_mean) / perm_max | 40.99٪ / 48.50٪ / 56.37٪ (K=500) |
| lift / skill_z / p_perm | +10.17pp / 3.35 / 4.0e-4 |
| PF (دلاری) / net ($10k، ریسک ثابت) | 1.623 / **+$7,888** |
| MaxDD / RF / MCL | **4.91٪** / 8.65 / 5 از 11 مجاز |
| Expectancy | +21.7 پیپ (+18.4 در ۲×هزینه) |
| تقویم | 4/4 مثبت · top_win_share 5.5٪ |
| H7 (holdout ۴۰٪ دنباله) | ✓ · H10 (بقا در برابر رانش غالب) ✓ |

## ۴) بازوی B — استخر (اطلاع‌رسانی)

`choose_homogeneous_subset` مکانیکی: H4 (lift 10.77) + H2 (10.17) پذیرفته؛ H6 (8.87) به‌دلیل رقیق‌سازی حذف. FIFO: 472 → 391 (H2 233 / H4 158). نتیجه: lift +9.07، z 2.76، **H3 ✗** (n_req=290 — با FIFO n مؤثر افزایش کافی نداشت) ⇒ POWER-LIMITED 20.7. استخر A را بهبود نداد؛ طبق پیش‌ثبت فقط گزارش می‌شود. نکته: H4 با `data/XAUUSD_H4.csv` (نه mt5_full) اجرا شد — همان منبع S747/S748، ۱۵.۵۳ سال، E-16 خنثی ولی ثبت می‌شود.

## ۵) دفتر ۸ خطای رایج

| # | خطا | وضعیت |
|---|---|---|
| ۱ | AXIS/SPAN (E-16) | ✓ src در هر کارت؛ H2 از mt5_full 15.59y |
| ۲ | SPLITDIR | ✓ اکتشاف ۶۰٪ نخست (S748)؛ H7 روی دنباله |
| ۳ | DEFAULTARG | ✓ پارامترها فقط از JSON S748؛ assert روی K_R/K_SL/RR |
| ۴ | look-ahead | ✓ SMA shift(1)، ATR causal، ورود open بعد |
| ۵ | نول | ✓ همان seed=748، K=500، هم‌هندسه، منجمد |
| ۶ | TP≥SL | ✓ RR=1.5 |
| ۷ | n_trials | ✓ **640 = کل خانواده** (نه فقط ۳۲ سلول H2) + تنش 2000 گزارش‌شده |
| ۸ | تغییر قاعده پس از دیدن داده | ✓ قاعدهٔ حکم (A با 640، بدون قرنطینه) در پیش‌ثبت §۴ **قبل** از اجرا ثبت شد؛ قرنطینهٔ S748 لغو نشد — S748 UNPROVEN می‌ماند |

## ۶) معنای علمی

سه دانشمند مستقل به یک پدیده رسیده‌اند: **کندل شوکِ با بدنهٔ قاطع + هم‌راستایی با رانش میان‌مدت ⇒ ادامه** — کایل S965/S966 (ρ≥0.618 + drift-180 روی H8، 86)، کانتور S604 (شوک انگل + drift روی H6/D1، 80.7)، و این خانواده (CLV≥0.8 + dual-SMA روی H2، 82.3). تفاوت TF (H2 در برابر H8) و تعریف شوک (ATR100 در برابر ATR21) نشان می‌دهد اثر به یک پارامتر خاص وابسته نیست. تفسیر Kyle (1985): بستهٔ قوی = اثر قیمتی دائمیِ جریان مطلع؛ رانش دوگانه = تأیید در دو مقیاس زمانی (TSM).

---
*هیچ استقراری. حکم موتور کلمهٔ نهایی: ACCEPT 82.3 با margin +0.22σ — مرزی، صادقانه، forward-test لازم. توقف و گزارش به کاربر.*
