# S513 — جست‌وجوی هندسه برای پایهٔ M30 | XAUUSD-M30 | RQS2=20.2 | REJECT

**وضعیت: REJECT** (5 گیت شکست: H1, H3, H5, H7, H8)
**پیش‌ثبت:** `results/S513_PREREG_M30_GEOMETRY.md` (commit قبل از هر آزمون)
**قلمرو عددی:** بلوک S510–S519 ✓ | **مسیر C** ✓ (یک فراخوان compute_rqs2، n_trials=5003)

---

## 1) نتیجهٔ رسمی

سیگنال منجمد: atr_fib_55 cross↑q90(کشف) / LONG | برندهٔ کشف: **SL_K=2.058, RR=2.058**

| کمیت | مقدار | گیت | نتیجه |
|---|---|---|---|
| n | 350 | H0 ≥100 | ✓ |
| PF | **1.25** | H1 ≥1.30 | ✗ |
| WR | 38.57% (سربه‌سر هزینه: 35.12%) | — | +3.46pp |
| lift | +6.18pp | H3 ≥4pp | جزء اول ✓ |
| z | **2.47** | H3 ≥3.09 | ✗ |
| p_perm دقیق | 0.0068 | — | (درس S512 رعایت شد) |
| بدهی چندگانگی | p_adj بونفرونی=1.0 با 5003 آزمون | H5 | ✗ |
| holdout | — | H7 | ✗ |
| maxDD | **14.12%** | H8 ≤8% | ✗ |

## 2) داستان علمی: هندسه در کشف درخشید، در داوری فروریخت

- کشف (۶۰٪): سلول برنده PF=1.569، WR=45.92%، net=+14.76 — کنترل فقط PF=1.127
- نمونهٔ کامل: PF=1.25، WR=38.57% — یعنی **~۷۰٪ بهبود PF بخار شد**.
- مقایسه با کنترل در داوری S511: همان سیگنال با هندسهٔ کنترل PF=1.273 داشت؛
  هندسهٔ «بهینه» فقط 1.25 گرفت — **بهبودِ کشف عمدتاً برازش هندسه به نویز کشف بود.**

## 3) یافته‌های علمی

1. **قانون جدید (مکمل قانون S510):** «هندسه روی سیگنال بادار هم فقط تا حد
   انتقالِ بودجه کار می‌کند؛ PF اضافیِ کشف که با هندسه آمده، به داوری منتقل
   نمی‌شود.» سیگنال بادار lift خود را نگه داشت (+6.48 → +6.18pp با هندسهٔ
   متفاوت) اما PF هندسی نگه داشته نشد. lift مالِ سیگنال است؛ PF مالِ برهم‌کنش
   سیگنال×هندسه×رژیم است و شکننده‌تر.
2. **SL پهن‌تر DD را متورم کرد:** SL=2.058×ATR ⇒ maxDD از مرز قابل قبول به
   14.12% جهید — H8 که در S511 سبز بود، قرمز شد. بهای «جای تنفس» = ریسک دنباله.
3. **سه تلاش روی این پایه (S511 مستقیم، S512 فیلتر، S513 هندسه) همگی در
   z≈2.5–2.7 متوقف شدند.** طبق قانون مرگ ابدی، خانوادهٔ «atr_fib_55↑q90 تنها
   روی M30» را سوخته اعلام می‌کنم. lift واقعی ~+6pp با n~350 ساختاراً به 3.09
   نمی‌رسد؛ برای عبور یا lift بزرگ‌تر لازم است یا n بزرگ‌تر با همان lift —
   هیچ‌کدام از داخل همین کارت/رویداد به دست نیامد.

## 4) حسابداری صداقت

- انتخاب فقط در کشف ✓ | یک compute_rqs2 ✓ | n_trials=5003 اعلام‌شده ✓
- p دقیق گزارش شد (0.0068) ✓ | هیچ نگاه دوباره ✓

## 5) میراث برای S514

پایهٔ M30-q90 سوخت، اما **دو واقعیت اندازه‌گیری‌شده** باقی ماند:
(الف) خانوادهٔ نوسان/رژیم حامل لبهٔ ناخالص طلاست (S511)؛
(ب) lift این خانواده در رویدادِ گذر q90 حدود +6pp سقف دارد.
مسیر S514 باید **رویداد را عوض کند نه هندسه/فیلتر را**: به‌جای «لحظهٔ گذر»
(که n را کوچک می‌کند)، «رژیمِ حالت» با خروج زمانی/رژیمی — یا کارت دیگری
(M15/H1) از همین خانواده با تعریف رویداد غنی‌تر از سیگنال‌های موجود دیتابیس
دانشمندان موازی (پس از fetch و مرور کارهای جدیدشان — قانون جدید کاربر).

**امضا: نیوتن** — سه شکست متوالی روی یک پایه = یک نقشهٔ دقیق از مرزهای آن پایه.
