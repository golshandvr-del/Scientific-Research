# 🔴 `BUG-ZBARAPPROX` — سدِ چاپ‌شده در `S436` سدِ واقعی نبود

**گام ۱۲۶** · کشف‌شده حین ساختِ غربالِ توانِ `S437`

---

## ۱. چه چیزی غلط بود

ابزارِ داوریِ `S436` در هر اجرا این خط را چاپ می‌کرد:

```python
f'سد z≈{sqrt(2*log(N_TRIALS)):.2f}'      # tools/s436_adjudicate.py:295
```

`√(2·ln N)` **تقریبِ مجانبیِ** `E[max_N]` است، نه خودِ آن. سدی که موتور
واقعاً اعمال می‌کند `engine.rqs2.expected_max_z` است:

```
E[max_N] ≈ (1−γ)·Φ⁻¹(1 − 1/N) + γ·Φ⁻¹(1 − 1/(N·e))
```

| کمیت | مقدار برای `N=1928` |
|---|---|
| تقریبِ چاپ‌شده `√(2·lnN)` | **۳.۸۸۹۵** |
| سدِ واقعیِ موتور `expected_max_z` | **۳.۴۳۷۴** |
| اختلاف | **+۰.۴۵۲σ** ⇒ تقریب **۱۳.۲٪ سخت‌گیرانه‌تر** |

---

## ۲. آیا حکمِ `S436` عوض می‌شود؟ — **خیر**

`B0/M5` با `z = ۰.۴۶`:

| سد | شکاف | حکم |
|---|---|---|
| تقریب ۳.۸۹۰ | −۳.۴۳σ | رد |
| واقعی ۳.۴۳۷ | −۲.۹۸σ | رد |

⇒ حکمِ `REJECT` و مرگِ ابدی **دست‌نخورده** می‌ماند. عددِ «سال‌های لازم» در
`E-17` اصلاح می‌شود ولی نتیجه‌گیری تغییری نمی‌کند:

| محاسبه | با تقریب | با سدِ واقعی |
|---|---|---|
| n لازم | ۳٬۷۸۹ | ۲٬۹۵۹ |
| سالِ داده | ۲۰۰ | **۱۵۷** |

> ⚠️ توجه: عددِ `۱۲۶ سال` که در `E-17` ثبت شد، از سدِ **۳.۰۹** (فقط `H3`)
> آمده بود. سه عدد در گردش بود و هر سه درست‌اند ولی به **سه پرسشِ متفاوت**
> پاسخ می‌دهند: ۱۲۶ سال برای `H3` تنها · ۱۵۷ سال برای سدِ مؤثرِ واقعی ·
> ۲۰۰ سال برای تقریبِ سخت‌گیرانه. جهتِ نتیجه در هر سه یکسان است.

---

## ۳. چرا با این حال باگِ **جدی** است

این نمونه بی‌ضرر بود، ولی **فقط تصادفاً** — چون نامزد آن‌قدر دور از سد بود
که ۱۳٪ تفاوت بی‌اهمیت شد. پنجرهٔ خطرناک دقیقاً بینِ دو سد است:

```
اگر  3.437 < z_obs < 3.890  ⇒  با تقریب: رد    · با سدِ واقعی: پاس
```

یعنی هر لایه‌ای با `z` در این بازه، به‌غلط **کشته** می‌شد. این همان
خانوادهٔ خطاست که در `E-16` (`S435`) ثبت شد: آنجا با `z = ۱.۹۵` در برابرِ
سدِ ۱.۹۶ رد شدم و نوشتم «ناخوشایندترین حقیقت». **جهتِ این باگ محافظه‌کارانه
است** (به ضررِ پذیرش)، ولی محافظه‌کاریِ **تصادفی** جای دقت را نمی‌گیرد.

> 🎓 **درسِ روش‌شناختی — دوباره همان الگو.** تقریب را از حافظه بازسازی کردم
> («جریمهٔ چندگانگی مثلِ ریشهٔ دو لگاریتم رشد می‌کند») به‌جای اینکه تابعِ
> موجودِ موتور را **صدا بزنم**. این ششمین نمونهٔ همان قاعده در این مأموریت
> است. و مثل `BUG-SCOREKEY`، جهتِ خطا **علیهِ** فرضیه‌ام بود ⇒ بررسی نشد.
>
> **قاعدهٔ عملیاتی که اضافه می‌کنم:** هیچ ابزارِ گزارشی نباید آستانه‌ای را
> **دوباره حساب کند** که موتور از قبل صادر می‌کند. سد باید از خودِ موتور
> **خوانده** شود (`z_luck_bound` در خروجی)، نه در ابزار بازتولید شود.

---

## ۴. چرا در `S436` کشف نشد و اینجا شد

در `S436` عددِ ۳.۸۹ فقط **چاپ** می‌شد؛ هرگز با چیزی مقایسه نشد. حکمِ واقعی
را موتور با سدِ درستِ خودش می‌داد، پس **هیچ ناسازگاری‌ای بروز نکرد**.
اینجا کشف شد چون غربالِ توان **مجبور شد سد را صریحاً محاسبه کند** و عدد با
آنچه به‌خاطر داشتم نخواند.

⇒ همان مسیرِ کشفِ `BUG-DAYDTYPE` و `BUG-PERMK`: **مقایسهٔ دو کمیتی که باید
توافق کنند**. عددی که فقط چاپ می‌شود و با چیزی سنجیده نمی‌شود، می‌تواند
سال‌ها غلط بماند.
