# S955 — «جهشِ هم‌راستا در رژیمِ آرام» (S950 Jump-Aftermath × Calm-σ) · XAUUSD-H8 · RQS2=87.7 · ✅ ACCEPT (نیز H12 89.4 · H6 84.9)

**دانشمند:** Robert Merton (باند S950–S959 — شمارهٔ ۶ از ۱۰) · **تاریخ:** 2026-09-09
**داور:** `engine/rqs2.py::compute_rqs2` v2.6 — دست‌نخورده، ۱۱ گیت وتو؛ حکم عیناً از موتور
**پیش‌ثبت:** `results/S955_PREREG_jump_aftermath_calm_regime.md` — کامیت و push **قبل از هر عدد**
**کد:** `strategies/s955_jump_aftermath_calm.py` (کامیت قبل از اجرا؛ پایه عیناً از `s950_jump_aftermath` و گیت عیناً از `s605_engle_sigma_regime` import — هیچ بازنویسی)
**رکوردها:** `results/_scan_S955/{H8,H12,H6,…}.json` (۲۱ TF)، `H8_seed_stability.json`، `H12_H6_seed_stability.json`، `overlap_audit.{py,json}`
**داده:** `data/mt5_full/XAUUSD_*.csv` — ۱۵.۶ سال کامل (src در هر JSON)
**وضعیت:** ⏸ **توقف طبق دستور — لایهٔ موفق ⇒ گزارش و انتظار تأیید. هیچ استقراری (TS/registry/سایت) انجام نشده.**

---

## ۱) حکم موتور — عیناً

```
S955-H8  CALM (n_trials=34):  ACCEPT  RQS2=87.7
  n=112  WR=67.86%  lift=+17.85pp  z=3.78  p_perm=7.9e-05  PF=2.06  maxDD=2.86%  SL=TP=235.7pip
  H0✓ H1✓ H2✓ H3✓ H4✓ H5✓ H6✓ H7✓ H8✓ H9✓ H10✓ — هر ۱۱ سبز، notes خالی
  پایداری بذر (۵ بذر): 87.6 / 87.7 / 87.7 / 87.8 / 87.9 — همه ACCEPT
  تنش چندگانگی: n_trials=200 ⇒ ACCEPT 86.8 · n_trials=1000 ⇒ ACCEPT 86.0

S955-H12 CALM (n_trials=34):  ACCEPT  RQS2=89.4
  n=76  WR=69.74%  lift=+19.86pp  z=3.46  p_perm=2.7e-04  PF=2.31  maxDD=2.43%
  ۴ بذر: 89.2–89.4 همه ACCEPT · تنش n_trials=1000 ⇒ ACCEPT 87.7

S955-H6  CALM (n_trials=34):  ACCEPT  RQS2=84.9
  n=149  WR=65.10%  lift=+15.15pp  z=3.67  p_perm=1.2e-04  PF=1.74  maxDD=4.74%
  ۴ بذر: 84.9 ×۴ · تنش n_trials=1000 ⇒ ACCEPT 83.2

S955-H4  CALM: POWER-LIMITED 26.0 (n=250, lift +7.9, z=2.5 — فقط H3)  ·  H3/H2/H1/M30…M1: REJECT  ·  D1/W1/MN1: NO-CANDIDATE (n<30)
```

**حکم رسمی لایه (کارتِ اصلیِ پیش‌ثبت = H8): ✅ ACCEPT 87.7.** H12 و H6 هم با همان قاعدهٔ منجمد و بدون هیچ تغییری ACCEPT شدند.

## ۲) قاعدهٔ قابل حمل — صفر پارامتر آزاد، همه به ارث

| جزء | مقدار | منبع |
|---|---|---|
| رویداد | `|r_t| > 2.6·σ_BV(89)`؛ σ_BV = √(π/2 · SMA₈₉(|r_i||r_{i−1}|))، علّی با `bv[t]=bv_full[t−2]` | S950 (ACCEPT 80) |
| هم‌راستایی | long فقط اگر `close[t−1] − close[t−90] > 0`؛ short آینه | S950 |
| **گیتِ آرامش** | `σ²_t = 0.94σ²_{t−1} + 0.06·r²_{t−1}` (RiskMetrics)؛ `reg_t = σ_t / median(σ_{t−233..t−1})`؛ ورود فقط اگر `reg_t ≤ 1` | S605/S606 (W=233 عین S1911) |
| براکت | SL = TP = 2.058 × ATR(89) (SMA-TR، شیفت ۱) — RR 1.0 (TP≥SL ✓) | S950 |
| max_hold · overlap · هزینه | 34 کندل · بدون همپوشانی · 3.3 pip | S950 |

## ۳) ابطال‌گرهای پیش‌ثبت — همه با داده جواب دادند

| | H8 | H12 | H6 | نتیجه |
|---|---|---|---|---|
| **P1** lift_calm > lift_base (بی‌گیت = S950) | **+17.85 > +11.19** | +19.86 > +9.38 | +15.15 > +12.57 | **گذشت ۳/۳** — گیت اطلاعات‌افزاست، نه توان‌سوز (درسِ S1912 رعایت شد) |
| **P2** بازوی طوفانی ضعیف‌تر | WR 54.5 / lift +4.6 (vs 67.9 / +17.9) | 50.0 / −0.2 (vs 69.7 / +19.9) | 60.7 / +10.8 (vs 65.1 / +15.2) | **گذشت ۳/۳** |
| **P3** pass-rate ۳۰–۸۵٪ | 49.6٪ | 45.6٪ | 49.8٪ | **گذشت ۳/۳** — گیت زنده |
| پیش‌بینی مکتوب H8 | n 110–150 · WR 64–68 · lift +15…+20 · z 3.0–3.6 | واقعیت: n=112 · 67.9 · +17.9 · 3.78 | | داخل بازه (z اندکی بالاتر) |

**تفکیک آرام/طوفان روی H8 (همان رویداد، همان براکت):** ۲۳۸ جهشِ هم‌راستا → ۱۱۸ آرام (WR 67.9٪) در برابر ۱۲۰ طوفانی (WR 54.5٪): ۱۳ نقطهٔ درصدی.
همان جهش، فقط بستهٔ ولاتیلیته‌اش فرق دارد. این **سومین تکرارِ مستقلِ** قانونِ «شوک × آرامش» است (S606 Engle، S1911 Kyle-range، S1740 strong-close) و **اولین** روی رویدادِ **بازده‌ای** (return jump).
پیش‌بینیِ منفیِ پیش‌ثبت هم درست بود: در H12/D1 پایه REJECT بود؛ در H12 گیت آن را به ACCEPT رساند (lift دو برابر)، در D1 نمونه به ۲۸ افتاد ⇒ NO-CANDIDATE (نه ادعایی).

## ۴) همپوشانی با لایه‌های ACCEPTِ هم‌کارت (`overlap_audit.json`؛ منطقِ هر لایه از ماژولِ منتشرشدهٔ خودش، فقط خواندن)

| کارت | در برابر | همان‌کندل (٪ از S955) | همپوشانیِ پنجرهٔ نگهداری | جهتِ مخالف |
|---|---|---|---|---|
| H8 | **S950 (والد)** | **100٪** (زیرمجموعهٔ ساختاری، 118/238) | 100٪ | 0 |
| H8 | S1911 (93.9) | 33.9٪ | 45.8٪ | 0 |
| H8 | S966 (86) | 23.7٪ | 35.6٪ | 0 |
| H8 | S965 (82) | 33.9٪ | 50.8٪ | 0 |
| H6 | S919 (88.9) | 30.3٪ | 46.5٪ | 0 |
| H12 | S800-H12 (83.6) | 43.6٪ | **92.3٪** | 0 |

**نتیجهٔ همپوشانی:**
- **H8**: S955 = زیرمجموعهٔ S950 ⇒ **دو لایه نباید هم‌زمان روی کارت باشند** (سایزِ دوبرابر روی سیگنالِ مشترک). طبق P4 پیش‌ثبت (ACCEPT>80 و P1 گذشته) پیشنهاد: **S955 جانشینِ S950 روی XAUUSD-H8** شود (همان ۱۱۲ معامله از ۲۲۴، WR 61.6→67.9، PF 1.56→2.06، maxDD 4.9→2.9). با S1911/S965/S966 هم‌جهت و کمتر از نیمی همپوشان ⇒ هم‌افزا (رویدادِ متمایز: بازده در برابر دامنه).
- **H6**: با S919 نیمه‌مستقل (46٪ پنجره‌ای) ⇒ قابلِ افزودن.
- **H12**: با S800 (فشردگی→گشایش) ۹۲٪ همپوشانیِ پنجره‌ای (S800 با ۴۳۶ معامله تقریباً همیشه فعال است) ⇒ **S955-H12 اطلاعاتِ افزودهٔ کمی روی کارتِ H12 دارد** — پیشنهاد: به‌عنوانِ «شاهدِ تکرار» ثبت شود، استقرار فقط اگر کاربر بخواهد.

## ۵) مشاهدهٔ جنبی (ادعای لایه نیست)
بازوی **base** (قاعدهٔ S950 بی‌گیت) روی H6 با n_trials=34: ACCEPT 90.3 (n=311، lift +12.6). در اسکنِ اصلیِ S950، H6 REJECT بود چون آن‌جا «بهترین عضوِ خانوادهٔ ۲۴تایی» داوری می‌شد، نه قاعدهٔ نهاییِ درفت‌هم‌راستا. این یک **مشاهدهٔ کنترل** است، پیش‌ثبت نداشته و لایه اعلام نمی‌شود؛ ثبت می‌کنم تا اگر خواستید در شماره‌ای مجزا با پیش‌ثبت داوری شود.

## ۶) گرادیانِ مقیاس (پنجمین تکرار در باند)
lift_calm: H12 +19.9 → H8 +17.9 → H6 +15.2 → H4 +7.9 → H2 +6.7 → H1 +2.0 → M30 +2.1 → M12 +0.3 → M3 −2.8 → M1 −11.0.
گیتِ آرامش در H1–M10 هنوز P1/P2 را می‌گذراند (اطلاعات دارد) اما لبهٔ خام آن‌جا وجود ندارد که تقویت شود.

## ۷) صداقتِ روش
- پیش‌ثبت قبل از هر عدد؛ کد قبل از اجرا کامیت شد؛ **یک آزمونِ منجمد در هر کارت** — صفر جست‌وجو، صفر تیون، صفر دورِ بهبود.
- n_trials=34 = ۳۳ تجمعیِ S950 (همان رویداد) + ۱؛ تنش تا 1000 هم ACCEPT.
- null کانونیِ جای‌گشتِ علامت (K=2000) — همان null که در ERRATUM S951/S952 اصلاح شد؛ ۵ بذر.
- همهٔ ۲۱ چک‌پوینت (۱۵ REJECT، ۳ NO-CANDIDATE، ۱ PL، ۳ ACCEPT، M2 بی‌داده) کامیت شدند. H6 و H12 «کارتِ اصلی» نبودند؛ ACCEPTشان با همان قاعده و بدون انتخابِ پس‌نگر آمده و همین‌طور گزارش می‌شود.

**وضعیت باند:** S950 ✅80 · S951 ❌ · S952 ⏸PL · S953 ❌ · S954 ❌ · **S955 ✅ 87.7 (H8) / 89.4 (H12) / 84.9 (H6)** · بعدی پس از تأیید شما: S956
