# S561 — PREREG — احیای H8 لایهٔ GapOpen در M15/M30/H1 (XAUUSD)

**تاریخ ثبت**: 2026-08-19 — این فایل **قبل از هر تستی** کامیت می‌شود (git timestamp = preregistration).
**دانشمند**: لایب‌نیتس — بلوک S560–S569. هیچ فایل/شماره‌ای خارج بلوک لمس نمی‌شود.
**EURUSD**: به دستور کاربر حذف.

## 1) انگیزه و فرضیه

S560 (کامیت 0f0eab53 → گزارش eee04c3c) لبهٔ GapOpen-NegGap را در هر ۵ TF با z=8–22 اثبات کرد، اما M15/M30/H1 **فقط** گیت H8 (maxDD≤8%، MCL، recovery≥3) را شکستند:
- M15: dd=12.40%, MCL=10/9 | M30: dd=12.79% | H1: dd=9.45%

**علت ساختاری**: SL=TP=q98 (51–101 pip) یعنی زیانِ منفرد بسیار بزرگ در TF درشت → DD دلاری از سقف 8% عبور می‌کند.

**فرضیهٔ S561**: کوچک‌کردن براکت متقارن (چندک پایین‌تر از q98) زیان منفرد را کَپ می‌کند و DD را زیر 8% می‌آورد، در حالی که لبهٔ جهتی (که از خودِ سیگنال می‌آید نه هندسه) زنده می‌ماند. ریسک شناخته‌شده: درس فاز 19 («استاپ قیمتی لایهٔ گپ را می‌کُشد») — اما آن درس دربارهٔ SL=q30(MAE)≈1pip بود؛ ناحیهٔ q85–q95 آزمون‌نشده است.

## 2) ثابت‌ها (از S560 قفل‌شده — تغییرناپذیر)

سیگنال عیناً S560: شکست روز با `thr=max(1800,1.5×TF_sec)` (BUG-BRKTHRESH-safe)، گپ منفی، چندک انبساطی عِلّی (min 20 نمونه)، تفکیک ویکند sw=True، ورود open کندل si+1. پیکربندی سیگنال هر TF عیناً از `locked_config.json` S560: M15(q70,sw,hold1)، M30(q80,sw,hold1)، H1(q80,sw,hold2). **M1/M5 لمس نمی‌شوند (ACCEPT قطعی‌اند).**

## 3) فضای پارامتر جدید (فقط هندسهٔ خروج)

- براکت متقارن SL=TP=qX(|MFE|∪|MAE|) از **نیمهٔ اول فقط**، X ∈ {85, 90, 95} → 3 بازو
- براکت نامتقارن SL=qX(|MAE|), TP=q98(|MFE|)، X ∈ {85, 90} → 2 بازو
- hold عیناً قفل S560 (تغییر نمی‌کند).
- **n_arms جدید = 5 بازو × 3 TF = 15**

## 4) مسیر چندگانگی — Path C با حسابداری صادقانهٔ انباشته

- قفل انتخاب بازو روی **نیمهٔ اول فقط** (split 2018-10-20، همان split_bar های S560).
- معیار قفل: t-stat نیمهٔ اول؛ در تساوی، DD نیمهٔ اول کمتر.
- تک‌لمس نیمهٔ دوم برای بازوی قفل‌شدهٔ هر TF.
- **n_trials انباشته = 400 (S560) + 15 (S561) = 415** — چون نیمهٔ دوم برای این خانوادهٔ سیگنال قبلاً یک بار در S560 لمس شده، چندگانگیِ کل خانواده صادقانه انباشته اعلام می‌شود.
- حکم فقط از `compute_rqs2` (gates_only، همهٔ ۵ ورودی اختیاری الزامی: sl_pip، tp_pip از موتور، bar_time، null بازو-محور K=500 seed=20260814، n_trials=415، split_bar). غربال توان قبل از داوری.

## 5) پیش‌بینی‌های ثبت‌شده (قبل از دیدن هر عددی)

- **P1**: حداقل یکی از M15/M30 با براکت q85–q90 از H8 عبور می‌کند (DD<8%) و ACCEPT می‌گیرد.
- **P2**: کوچک‌کردن براکت WR را پایین می‌آورد (استاپ‌خوردن‌های بیشتر) اما z همچنان > bar می‌ماند، چون لبه جهتی است.
- **P3**: H1 حتی با q85 هم رد می‌شود (نوسان دلاری ذاتی H1 با hold=2 بزرگ است).

## 6) معیار توقف و صداقت

- اگر بازوی قفل‌شده رد شد، **هیچ بازوی دیگری روی نیمهٔ دوم تست نمی‌شود** (تک‌لمس مطلق). نتیجه هرچه بود، verbatim گزارش می‌شود.
- نام‌گذاری خروجی: `Strategy`-فرمت `S561_GapOpenH8Revival_Xauusd_<TFs>_rqs2_<score>_<verdict>.md`.
- Overlap: سیگنال عین S560 است → هم‌پوشانی لحظه‌ای با S355 قبلاً صفر اندازه‌گیری شده؛ هم‌پوشانی S561–S560(M5) بین-TF گزارش می‌شود (رویداد یکسان، TF متفاوت — انتظار هم‌پوشانی روزانهٔ بالا؛ برای پرتفوی فقط یکی از TFها per-day فعال خواهد بود و این در گزارش تصریح می‌شود).

*لایب‌نیتس — S561 | Path C | seed=20260814*
