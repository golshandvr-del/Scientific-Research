# S621 — پیش‌ثبتِ مسیر چندگانگی (قبل از هر آزمون نهایی)

**تاریخ:** 2026-08-13 · **نویسنده:** اقلیدس · **وضعیت:** PREREG
**پیشینه:** S620 (UNPROVEN) تناقض توان را نشان داد: lift در نردبان TF یکنواخت صعود می‌کند
(H1:1.7 → H3:3.7 → H6:6.2 → H12:8–12 → D1:17pp) اما n نزول. نتیجهٔ هندسی: لبهٔ چرخش
SHORT واقعی است و **حاملش بافت TF بالاست**، نه خود رویداد TF پایین.

## ۱) فرضیه

روی XAUUSD، سیگنال SHORTِ «خروج Laguerre RSI از اشباع خرید» در TF پایه (H1/H2/H3)
**فقط وقتی** TF بالاتر (H12/D1) نیز در ناحیهٔ اشباع خرید باشد، مهارتی ≥4pp فراتر از
مبنای بی‌قید دارد. (تلاقی MTF: n از TF پایه، lift از بافت TF بالا — تجمیع نمونه به‌جای انباشت فیلتر؛ فقط ۱ دروازه.)

فقط SHORT (شواهد S620 یک‌سویه بود؛ LONG در TFهای بالا اثری نشان نداد). لایهٔ جدید — احیای هیچ لایه‌ای نیست.

## ۲) مسیر چندگانگی: C (نگه‌داشت) — همان مرز S620

- جست‌وجو فقط روی `[0, n/2)` هر TF پایه؛ نگه‌داشت `[n/2, n)` **هنوز بکر است** (S620 آن را مصرف نکرد) و تا انجماد لمس نمی‌شود.
- آزمون نهایی: **یک** فراخوانی `rqs2.compute_rqs2` روی کل داده، `split_bar=n/2`، `n_trials=1`، پس از PREREG-2.

## ۳) خانوادهٔ جست‌وجو (منجمد)

| پارامتر | مقادیر | تعداد |
|---|---|---|
| TF پایه | H1, H2, H3 | 3 |
| γ پایه | 0.4, 0.5 | 2 |
| th پایه (عبور به پایینِ 100−th) | 15, 20 | 2 |
| TF تأیید | H12, D1 | 2 |
| γ تأیید | 0.5 (ثابت) | 1 |
| آستانهٔ تأیید (LagRSI_TFبالا ≥ c) | 70, 80 | 2 |
| k_sl (SL=k×ATR(100) پایه) | 1.5, 2.0 | 2 |
| RR (TP=RR×SL؛ RR≥1) | 1.0, 1.5 | 2 |

کوشش‌ها: 3×2×2×2×2×2×2 = **192**. سمت: فقط SHORT.
تأیید MTF به‌صورت **forward-safe**: مقدار LagRSI تأیید از آخرین کندلِ *بسته‌شدهٔ* TF بالا پیش از کندل سیگنال.

## ۴) ثابت‌ها

SEED=20260813 · مبنا: اندازه‌گیری‌شده (بی‌قید نمونه‌گیری‌شده، همان هندسه، همان الگوی S620) ·
داده فقط `tools.s434_fast_data` · max_hold: H1/H2/H3=64 · حساب: spread=3.3pip، سرمایه=10k$، ریسک=1٪ · EURUSD: هرگز.

## ۵) معیار انجماد (همان بند ۵ سختگیرانهٔ S620)

(۱) lift ≥ 4pp در نیمهٔ جست‌وجو، (۲) n ≥ n_required از فرمول 3.09σ، (۳) بیشینهٔ lift·√n.
اگر هیچ نامزدی نگذشت ⇒ سوخته، بدون لمس نگه‌داشت. شمارش صادقانه: این ۱۹۲ کوشش روی همان نیمهٔ
جست‌وجوی مصرف‌شدهٔ S620 است؛ نگه‌داشت همچنان فقط یک آزمون خواهد دید.

## ۶) قانون اندک‌اندک

خروجی هر TF پایه فوراً JSON + commit + push در `results/_scan_S621/`.
