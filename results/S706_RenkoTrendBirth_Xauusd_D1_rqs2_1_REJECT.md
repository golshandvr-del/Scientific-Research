# S706 — تولد روند رنکو (Renko Trend-Birth) · XAUUSD-D1 · REJECT 0.7

**دانشمند:** آگوستین-لویی کوشی (بلوک S700–S709) · **داور:** `engine/rqs2.py::compute_rqs2` v2.6 دست‌نخورده
**پیش‌ثبت:** `results/S706_PREREG_RENKO_TREND_BIRTH.md` (کامیت 1bf2376b — قبل از هر عدد)
**کد:** `strategies/s706_renko_scan.py` (3d2f03e2) · `strategies/s706_final_adjudication.py` (1ee2a768 — قبل از اجرای آزمون نهایی)
**رکوردها:** `results/_s706/` — unit_test + ۱۹ scan + final_null_D1 + final_verdict
**داده:** `data/mt5_full/XAUUSD_D1.csv` (۱۵.۵۹ سال، src در JSON) · SEED=706

## ۱) حکم موتور — عیناً
```
S706-D1 OFFICIAL | REJECT RQS2=0.7 | n=218 WR=39.45% | H0:✓ H1:✗ H2:✗ H3:✗ H4:✗ H5:✗ H6:✗ H7:✗ H8:✗ H9:✗ H10:✗
PF=0.943 net=−$884 lift=+0.04pp z=0.01 p_perm=0.50 maxDD=33.1%
half_nets=[+3191, −3068]  OOS: n=146 WR=30.82% PF=0.65 net=−$3,068
SL=179.05pip TP=268.58pip (RR 1.5)  brick=179.05pip  n_trials=304
```

## ۲) قاعدهٔ منجمد که آزموده شد
آجر `b = 1.0 × median(ATR55 علّی نیمهٔ جست‌وجو) = 179pip`؛ رنکوی کلاسیک بر close با وارونگی دو-آجری؛ رویداد = لبهٔ تازهٔ `run=2`؛ `cont`؛ SL=b، TP=1.5b؛ max_hold=16.

## ۳) جست‌وجو (نیمهٔ اول — صادق)
۳۰۴ سلول. ۴ واجد فریز (همه در D1/H12؛ M1–M30 یکسره منفی — P2 ✅؛ H1–H8 مثبت ولی ضعیف). برنده به قاعدهٔ `α·√n`: D1 با n=72، α=+18.0pp، PF=1.88، z=3.13.

## ۴) چرا مُرد — ریشهٔ ساختاری، نه بدشانسی
- نیمهٔ اول: n=72 معامله ⇒ نیمهٔ دوم: n=**146** — دو برابر. آجرِ منجمدِ 179pip در ۲۰۱۹–۲۰۲۶ (طلا 1300→3400، ATR روزانه ۲–۳ برابر) عملاً به «نیم‌آجر» تبدیل شد؛ رنکو به نویز-شمار تبدیل شد و رویدادهای «تولد روند» انبوه و بی‌معنا شدند.
- **درس تازه (L-S706 — «مرگِ مقیاسِ مطلق»):** هر لایه‌ای که آستانهٔ خود را به **پیپِ مطلق** منجمد می‌کند (آجر، SL ثابت، فاصلهٔ ثابت) در دارایی‌ای با روند بلندمدت قیمت/نوسان، در هولد-اوت خودبه‌خود «کوچک» می‌شود و شکست می‌خورد. برندگان موازیان (S526/S1520/S965/S919) همه آستانهٔ **نسبی و غلتان** دارند (ATR علّی همان کندل، سقف ۹۰-کندلی، ρ بی‌بعد). آجرِ رنکوی سازگار (ATR-غلتان) رنکوی کلاسیک نیست و در پیش‌ثبت من نبود — پس آزموده نشد و طبق بند مرگ در S70x آزموده نخواهد شد.
- cal_nets: فقط ۱ از ۴ سطلِ تقویمی مثبت؛ counter_drift: exp_counter=−37pip در برابر exp_aligned=+17pip — همان قانون S692 «هندسه مهارت را می‌بلعد».
- P1 نیمه‌درست (cont>0 فقط در D1/H12 و فقط در نیمهٔ اول)؛ P3 ✅ (m=3 lift کمتر با n کمتر — رنکو m=3 در D1 n=53).

## ۵) کارنامهٔ صداقت
- پیش‌ثبت و کد قبل از هر عدد؛ نیمهٔ دوم فقط یک‌بار و فقط توسط موتور خوانده شد.
- آزمون واحد مصنوعی پیش از اسکن (run دقیق، نویز یک‌آجری بی‌اثر، چند-آجری در یک کندل).
- BUG-EPOCH رعایت؛ E-16 assert شد؛ داده پس از ریست سندباکس از gz در `mt5_full` بازسازی شد (src درج).
- **بند مرگ:** خانوادهٔ رنکو در بلوک S70x بسته شد. هیچ استخر نجاتی — شرط L-S705 (lift≥10pp در ≥۲ TF) ظاهراً برقرار بود (D1 و H12) اما ریشهٔ مرگ رقّت نیست، **مقیاس مطلق** است؛ استخر آن را درمان نمی‌کند.
