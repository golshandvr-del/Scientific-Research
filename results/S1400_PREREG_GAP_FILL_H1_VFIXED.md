# S1400 — پیش‌ثبت: گپ‌فیل H1 با V ثابت · XAUUSD H1 · دادهٔ کامل ۱۵.۶ ساله · مسیر C

**تاریخ:** 2026-09-09 · **بلوک دوم:** S1400–S1409 (شمارهٔ ۱ از ۱۰) · **داور:** RQS2 v2.6 `compute_rqs2` · **seed=1400**

## ۰) پرسش و قید بلوک
نقشهٔ TF خانوادهٔ گپ‌فیل (TP=close قبل، SL=2×|gap|، V): M30 ✅ S404 · M15 ✅ S408 · **H1 = تنها حفرهٔ باقی‌مانده**. S400-H1 با هندسهٔ X-BAR (نه فیل) و بدون V مُرد (H2/H7/H10)؛ S562-H1 لایبنیتس با براکت متقارن + V روی H1 ACCEPT 96.0 گرفت؛ S1560-H1 نشان داد روی H1 فقط V کار می‌کند (σ بی‌اثر). پس فیل‌هندسه با V روی H1 هرگز حکم موتور نگرفته.
**قید خودتحمیلی بلوک دوم (گزارش نهایی S400 §۴):** حداکثر ۲ شماره برای یک خانواده مگر ACCEPT مستقل. S1400 **آخرین** شمارهٔ خانوادهٔ گپ‌فیل در این بلوک است، هر نتیجه‌ای بگیرد. ممیزی مفهومی: S1400 با S404/S408 هم‌روز خواهد بود (همان روزها) — پس **پیش‌اپیش اعلام می‌کنم که آلفای مستقل نیست**؛ ارزشش تکمیل نقشهٔ TF و پاسخ به «آیا رزولوشن درشت‌تر لبه را می‌کُشد؟» است.

## ۱) داده (assert در کد)
`data/mt5_full/XAUUSD_H1.csv` — 91,331 کندل · 2011-01-03 00:00 → 2026-08-07 23:00. split_bar = **45581** (اولین کندل ≥ 2018-10-25 11:30 = 12:00 UTC؛ هم‌تاریخ S404/S408).

## ۲) تعریف مکانیکی (عین S404/S408، منجمد)
گپ منفی روز · |gap| > QW q علّی · DOW ≠ دوشنبه · V ثابت (ATR14 روز قبل ≤ صدک 0.78 غلتان 250، min 60) · Buy در open کندل اول (H1) · TP = close روز قبل · SL = k_sl×|gap| · بدون BE/time-stop/cooldown · خروج اجباری کندل آخر روز · `sim_trade_be(be_f=None)` منجمد.
نکتهٔ H1: کندل اول H1 یک ساعت را می‌پوشاند ⇒ TP/SL هم‌زمان در همان کندل محتمل‌تر است (قرارداد موتور: باخت). این جریمهٔ ساختاری رزولوشن درشت است و همان چیزی است که آزموده می‌شود.

## ۳) فضای جست‌وجو (قفل — ۶ ترکیب)
q ∈ {60, 70, 80} × k_sl ∈ {1.7, 2.0} · V ثابت.

## ۴) قاعدهٔ برنده (قفل، فقط نیمهٔ اول)
ok ⇔ n ≥ 50 ∧ PF > 1 ∧ t ≥ 2.5 ∧ maxDD ≤ 4.80% ∧ wr_edge ≥ 2pp ∧ rec ≥ 3. کمینهٔ maxDD؛ tie → t. هیچ ok ⇒ REJECT تیون.

## ۵) آزمون رسمی (تک‌لمس)
کل داده → `compute_rqs2` gates_only · null بازو-همتا: گپ‌DOWN بی‌قید، همان k_sl، بدون آستانه/DOW/V · K=500 · seed=1400.

## ۶) چندگانگی
n_trials تجمعی بلوک‌های من: 292 + 6 = **298**.

## ۷) هم‌پوشانی
اگر ACCEPT: روز-هم‌پوشانی با S404 (M30) و S562-H1 (آرتیفکت منجمد read-only).

## ۸) پیش‌بینی‌ها
- P1: نرخ TP∧SL هم‌زمان (باخت قراردادی) در H1 > M30 ⇒ WR کل‌داده < S404 (90.5%)؛ حدس 80–87%.
- P2: k_sl=2.0 برنده.
- P3: اگر بمیرد، قاتل H2 (wr_excess<3pp به‌خاطر باخت‌های هم‌زمانی) یا H7.
- P4: DD تیون→کل ≤ 2× (قانون V).

## ۹) قواعد
مسیر C · هیچ فایل S500+/S1500+ نوشته نمی‌شود · فقط اکتشاف — سایت هرگز لمس نمی‌شود.
