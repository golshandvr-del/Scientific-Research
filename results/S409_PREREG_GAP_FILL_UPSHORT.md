# S409 — پیش‌ثبت: گپ‌فیلِ **گپ مثبت / سمت Sell** با V ثابت · XAUUSD M30 · دادهٔ کامل ۱۵.۶ ساله · مسیر C

**تاریخ:** 2026-09-06 · **بلوک:** S400–S409 (آخرین شماره) · **داور:** RQS2 v2.6 (`compute_rqs2`) · **seed=409**

## ۰) پرسش علمی
آینهٔ S404: آیا گپ **مثبت** (open اول > close قبل) هم به‌سمت close قبل پر می‌شود و با V قابل برداشت است؟
شواهد موجود همه **اکتشافی و بدون حکم موتور**‌اند: فاز ۱۲ («گپ-بالا لبه ندارد، حتی منفی»)، مأموریت ۱ («روزهای گپ-بالا را وارد نکن») و پیش‌ثبت S400 (§۱ «gap UP: لبهٔ صفر/منفی»). آن اکتشاف‌ها **بدون V، بدون DOW، با هندسهٔ X-BAR** بودند. S404/S408 ثابت کردند که V و k_sl=2.0 لایهٔ مردهٔ گپ‌منفی را زنده کردند؛ پس این پرسش که آیا همان دو اهرم گپ مثبت را هم زنده می‌کنند، **هرگز با حکم رسمی موتور بسته نشده**. S409 آن را می‌بندد — هر جهتی که بیفتد، دانش است (فاز ۵: بازار نامتقارن).

## ۱) داده (اجباری، assert در کد)
`data/mt5_full/XAUUSD_M30.csv` (gunzip) — 182,145 کندل · 2011-01-03 00:00 → 2026-08-07 23:30. split_bar = اولین کندل ≥ 2018-10-25 11:30 UTC (هم‌تاریخ S404/S408). اسکریپت bars/first/last/split را چاپ و assert می‌کند.

## ۲) تعریف مکانیکی (آینهٔ دقیق S404، منجمد)
گپ **مثبت** روز (gap > 0) · |gap| > آستانهٔ علّی QW q (`thresholds_for_day` — روی |gap| تعریف شده، تقارن‌پذیر) · DOW ≠ دوشنبه · **V ثابت** (ATR14 روز قبل ≤ صدک 0.78 غلتان 250، min 60) · **Sell** در open کندل اول · TP = close روز قبل · SL = entry + k_sl×|gap| · بدون BE/time-stop/cooldown · خروج اجباری کندل آخر روز · SPREAD_PIP=3.3 · قرارداد موتور: TP∧SL همزمان = باخت.
`sim_trade_short` = آینهٔ خط‌به‌خط `sim_trade_be` (S401) با be_f=None؛ خودم می‌نویسم و با یک تست تقارن (وارونه‌سازی قیمت ⇒ pnl یکسان) صحت‌سنجی می‌کنم قبل از تیون.

## ۳) فضای جست‌وجو (قفل — ۶ ترکیب)
q ∈ {60, 70, 80} × k_sl ∈ {1.7, 2.0} · V=ATRq78 در همه.

## ۴) قاعدهٔ برنده (قفل، فقط نیمهٔ اول)
ok ⇔ n ≥ 50 ∧ PF > 1 ∧ t ≥ 2.5 ∧ maxDD ≤ 4.80% ∧ wr_edge ≥ 2pp ∧ rec ≥ 3. از ok: کمینهٔ maxDD؛ tie → بیشترین t. هیچ ok ⇒ REJECT تیون (هولدآوت باکره).

## ۵) آزمون رسمی (تک‌لمس)
کل داده → `compute_rqs2` gates_only · null بازو-همتا: ورودهای بی‌قید **Sell در گپ‌UP** با همان k_sl بدون آستانه/DOW/V · K=500 · seed=409.

## ۶) چندگانگی
n_trials تجمعی: 286 + 6 = **292**.

## ۷) هم‌پوشانی
اگر ACCEPT: روزهای S409 (گپ مثبت) با S404/S408 (گپ منفی) **به‌تعریف ناهم‌پوشان**‌اند؛ ممیزی فقط با S560/S562 (لایبنیتس، فقط‌خواندنی) که گپ منفی‌اند ⇒ انتظار هم‌پوشانی ≈ 0.

## ۸) پیش‌بینی‌های ثبت‌شده
- P1 (اصلی، بر پایهٔ فاز ۵/۱۲): لبهٔ پرشدن گپ مثبت **ضعیف‌تر** از گپ منفی است: wr_edge نیمهٔ اول < نصفِ S404 (+9.9pp) ⇒ احتمالاً < +5pp.
- P2: اگر لایه بمیرد، قاتل H1/H2 (لبهٔ ناکافی) است نه H8 — چون V، DD را کنترل می‌کند.
- P3: اگر زنده بماند، WR کل‌داده < S404 (90.5%).
- P4: نال Sell-uncond WR < نال Buy-uncond (52–58%) — درفت صعودی طلا علیه Sell است.

## ۹) قواعد
مسیر C · هیچ فایل S500–S980 نوشته نمی‌شود · سایت دست‌نخورده.
