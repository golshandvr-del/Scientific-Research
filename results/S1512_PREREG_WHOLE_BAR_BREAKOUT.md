# S1512 — پیش‌ثبت: کندلِ کاملاً بیرونِ نوار (Whole-Bar Breakout: سقفِ تازهٔ close ∧ کفِ تازهٔ low هم‌بار) ⇒ LONG — XAUUSD H3/H6/H8/H12/D1

> **این سند قبل از تولید هر عدد PnL کامیت می‌شود** (قانون ③). داور: `engine/rqs2.py` v2.6 — حکم verbatim.
> نویسنده: Isaac Newton — بلوک S1510–S1519، شمارهٔ ۳ از ۱۰. تاریخ: 2026-09-05.

## ۱) منشأ: شکستِ P2 در S1511 (ACCEPT 84.8)
S1511 نشان داد «کفِ تازه» (غیابِ عرضه) *به‌تنهایی* کم‌اطلاع است (+3.96pp بدون S526)، اما در حضورِ «سقفِ تازه» (S526، حضورِ تقاضا) به +11.33 می‌رسد. فرضیهٔ اصلاح‌شدهٔ نیوتن: لبه در **هم‌زمانیِ دو نیرو در یک کندل** است — کندلی که *تمامش* (از low تا close) بالای نوارِ ۹۰-کندلی ایستاده است. این با قانونِ S1520 هم‌خوان است: «رویدادِ ساختاری × کیفیتِ همان-کندل هم‌افزاست؛ رویداد × تاریخِ دریفت هم‌خط است (S529)».

**بکر بودن:** grep `whole bar`/`entire candle`/«کل کندل بیرون»/`fresh low & fresh high` ⇒ هیچ سندی. S707 (کندل بی‌سایهٔ open)، S789/S884 (خمودگی→اکسترمم)، S528 (کفِ تازهٔ SHORT) متفاوت‌اند.
**شمارشِ خام پیش از PnL (بدون خروجی):** رویدادِ تازه: H3 735 · H6 376 · H8 285 · H12 199 · D1 109؛ ~۹۹٪ داخل drift>0.

## ۲) تعریف منجمد (صفر پارامتر آزاد پس از این کامیت)
| جزء | مقدار | منبع |
|---|---|---|
| رویداد (حالت) | `WB = (close[t] > max(close[t−90..t−1])) ∧ (low[t] > max(low[t−90..t−1]))` | S526 + S1511 |
| سیگنال | `WB & ~WB.shift(1)` (**فقط لبهٔ تازه**) `& drift90>0` | S963 |
| گیت دریفت | `close[t−1] − close[t−90] > 0` (تعریفِ فضای نول، هم‌ارز با S526/S1511) | S523 |
| جهت / ورود | LONG-only · open کندل بعد · FIFO · `allow_overlap=False` | S526 |
| براکت | SL = 1.5×median(ATR100) · TP = 1.5×SL (TP≥SL ✓) | S382/S526 عیناً |
| هزینه | اسپرد 3.3 پیپ | حساب |
| کارت‌ها | **H3, H6, H8, H12, D1** (H3 اضافه شد چون S1740 نشان داد H2 هم می‌تواند زنده باشد؛ H4 در mt5_full اکنون موجود است ولی برای ثبات با S1511 وارد نمی‌شود) | — |
| داده | `data/mt5_full/` (۱۵.۶ سال) با گارد `assert 'mt5_full'` | E-16 |
| نول | مشروط در فضای drift>0: uncond strides 1/3/7 + جایگشت K=2000، SEED=**20260906**؛ مرجع=MAX | S523/S526 |
| n_trials | **15** = ۵ کارت + ۴ پارامتر وام‌گرفته + ۶ بازوی تشخیصی | صادقانه |
| مسیر | B — یک حکم به‌ازای هر کارت؛ بدون جست‌وجو | ③ |

## ۳) ابطال‌گرها (پیش از عدد)
- **P1 هم‌افزایی (ضدِ سوختنِ توان):** روی H6، lift(S1512) باید **اکیداً >** lift(S1511-H6)=+10.03 **و** > lift(S526-H6 بازتولید داخلی). اگر S1512 فقط زیرمجموعهٔ کم‌توان‌تر باشد (lift ≈ والدین، n کمتر) ⇒ «فیلترِ حذفی» و بی‌ارزش حتی اگر موتور ACCEPT دهد — در سند صریح نوشته می‌شود.
- **P2 متمم:** بازوی «سقفِ تازه **بدون** کفِ تازه» (S526-only) باید ضعیف‌تر از WB باشد. اگر معکوس شد، کفِ تازه *ضدِ* اطلاع است.
- **P3 نرخِ عبور (S529):** WB/S526-fresh باید در ۳۰–۸۵٪ باشد (پیش‌سنجی: H6 376/491=77٪ ✓، H8 285/361=79٪ ✓ — گیت زنده ولی نزدیکِ سقف؛ ثبت شد).
- **F1:** ۱۱/۱۱ روی ≥۱ کارت ⇒ ACCEPT ⇒ توقف + گزارش + ممیزی هم‌پوشانی (S382-H4، S526، S1520، S1521، S589، S1511، S919) + overlap-as-filter.
- **F2:** REJECT ⇒ گزارش کوتاه ⇒ fetch ⇒ S1513 خودکار. هیچ تغییرِ پارامتری پس از عدد.
- **پیش‌بینی شخصی:** H6 n≈230–260، WR 52–56، lift +11..+15، z 3.0–3.8؛ H8 n≈170–200، lift +12..+18، z 2.5–3.5. اگر H8 این‌بار هم رد شود، «H6 = TF طبیعیِ کف» به‌عنوان قانون ثبت می‌شود.

## ۴) انطباق
قانون ③ ✓ · یک شماره = یک لایه ✓ · TP≥SL ✓ · mt5_full ✓ · بدون EURUSD ✓ · قلمروی دیگران دست‌نخورده ✓ · ACCEPTهای جدید مطالعه شد: S802 (92.5, D1, S800×ρ)، S589 (88.3, S526×RVOL_slot)، S1521 (89.3, S1520×calm-σ)، S1740 (79.8, H2 strong-close×calm)، S955 (87.7, S950×calm) ✓ · سایت دست‌نخورده ✓.

*نیوتن: دو نیرو در یک نقطه و یک لحظه — برآیندشان را می‌سنجم.* 🍎
