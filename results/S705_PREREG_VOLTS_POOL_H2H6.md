# S705 — پیش‌ثبت: استخر دوکارتهٔ عبور ساختار زمانی نوسان {H2, H6} — قبل از هر آزمون

**دانشمند:** آگوستین-لویی کوشی (بلوک S700–S709) · **SEED=705** · **مسیر C**
**والد:** S704 (REJECT 18.1 تک‌کارت H2 — فرضیه سالم، توان ناکافی: n=336 vs n_req=392)

---

## ۱) چرا این آزمون مشروع است (نه بازیافتِ شکست)

S704 هر سه پیش‌بینی‌اش را درست از آب درآورد و lift واقعی نشان داد (z_obs=2.86، p_emp=0.0025) اما تک‌کارت در گیرهٔ نُدرت مُرد. الگوی اثبات‌شدهٔ پروژه — S602→S604 (80.7)، S770 (82.4)، S431 (93) — می‌گوید: **رویداد نادرِ درشت‌مقیاس را با استخر چندکارته به توان می‌رسانند.** بند مرگ S704 فقط «تک‌کارت» را بست؛ استخر طرح متفاوتی است (همان تمایز S701/S431 از اعضایشان).

تفاوت با شکست S701 (استخر آرون): اعضای S701 در جست‌وجو PF<1.3 داشتند (درسِ ثبت‌شده: «سلول زیر 1.3 در هولد-اوت می‌میرد»). اینجا **هر دو عضو در جست‌وجو PF≥1.34 دارند** — دقیقاً شرطی که از S701/S702 آموختیم.

## ۲) اعضای منجمد (از آرتیفکت‌های از-قبل-کامیت‌شدهٔ S704 — هیچ جست‌وجوی تازه‌ای نشد)

آماره‌های نیمهٔ جست‌وجو (کامیت‌های checkpoint اسکن S704، پیش از این پیش‌ثبت در origin/main):

| کارت | θ | mode | k_sl | n_search | α_search | PF_search | z |
|---|---|---|---|---|---|---|---|
| XAUUSD-H2 | 1.6 | cont | 1.618 | 183 | +8.73pp | 1.344 | 2.41 |
| XAUUSD-H6 | 1.3 | cont | 1.618 | 112 | +9.76pp | 1.420 | 2.11 |

قاعدهٔ هر عضو عیناً قاعدهٔ S704: VR=ATR8/ATR89 علّی، عبور صعودی از θ پس از ≥۳ کندل زیر آن؛ جهت = دریفت ۳۴ کندلی؛ cont؛ SL=1.618×median(ATR55 نیمهٔ جست‌وجوی همان کارت)؛ TP=1.5×SL؛ hold: H2=64، H6=32؛ ورود open کندل بعد.

**معیار گزینش (منجمد):** فقط سلول‌هایی از جست‌وجوی S704 که هر سه معیار فریز والد را با PF≥1.3 دارند — H2/θ1.6 (تنها واجد رسمی) + H6/θ1.3 (واجد α و PF؛ n=112 vs n_req/2=119.5 به‌فاصلهٔ ۷ معامله — طبق سنت S770 «الحاقیهٔ استخر» وارد می‌شود و همین‌جا اعلام و شمارش می‌شود). **هیچ عضو دیگری مجاز نیست.**

## ۳) پروتکل ادغام (عین s431/s701 — بدون درجهٔ آزادی تازه)

- بازسازی هر عضو روی دادهٔ کامل خودش؛ ادغام FIFO تقویمی، concurrency=1 (وام از `strategies/s701_aroon_slowpool.py`).
- نول: per-member دوطرفه K=1000 (ورود همه‌کندلی هم‌هندسه، allow_overlap=True)؛ ترکیب با `blend_pool_null` وزن‌دار به سهم پس-از-FIFO.
- محور داوری: زمان واقعی معاملات (BUG-EPOCH: cast دومرحله‌ای [s]→[ns]).
- مرز holdout: `split` در بیشینهٔ زمانِ نیمهٔ جست‌وجوی دو کارت (سنت S701).
- **یک** فراخوان `compute_rqs2` با **n_trials = 236** = 228 (جست‌وجوی S704) + 4 (زیرمجموعه‌های ممکن استخر {H2},{H6},{H2,H6},{H2,H3,H6}) + 4 (حاشیهٔ صداقت).

## ۴) پیش‌بینی‌های ابطال‌پذیر

- P1: n استخر ≥ 500 (H2≈336 + H6≈220 منهای برش FIFO) ⇒ سد نُدرت شکسته می‌شود.
- P2: اگر مهارت واقعی است z≥3.09 با lift≥+7pp؛ اگر lift زیر +6pp افتاد، رقّت هولد-اوت حاکم است و لایه می‌میرد.
- P3: خطر اصلی H8 است (DD تک‌کارت H2 برابر 16% بود)؛ تنوع دوکارته باید DD را زیر 8% بیاورد — اگر نیاورد، REJECT صادقانه.
- **بند مرگ نهایی:** این آخرین آزمون خانوادهٔ vol-termstructure در بلوک S70x است — هر نتیجه‌ای بیاید، خانواده برای همیشه بسته می‌شود.
