# S913 — پیش‌ثبتِ رسمی · «بازگشت خوش‌بینی» (RSI-21 گذر صعودی از ۵۵) · XAUUSD

> **تاریخ ثبت:** 2026-08-14 · **نویسنده:** Builder-AI (نقش: جان مینارد کینز)
> **وضعیت:** کامیت **پیش از اجرای هر تست** — مطابق §H5 در `docs/RQS2_SPEC.md`.
> **منطق انتخاب:** سه REJECT متوالی (S910–S912) نشان داد ساختارهای کندلی خام در طلا
> پس از هزینه قدرت اثبات ندارند. تنها ACCEPT اسیلاتوریِ تاریخ پروژه (S382: گذر
> Williams %R از آستانه، صفر فیلتر، n=869) الگوی «تجمیع نمونه» را اثبات کرده است.
> S913 عضو جدیدی از همان خانوادهٔ مفهومی است — اندیکاتور، آستانه، TF و ماهیت گذرِ
> متفاوت ⇒ لایهٔ نو، نه احیای S382.

## ۱) مفهوم

انتظارات جمعی وقتی از تردید به خوش‌بینی می‌چرخند، خودتقویت‌شونده‌اند (روحیهٔ بازار
به‌مثابهٔ convention کینزی). گذرِ RSI به بالای میانهٔ متمایل-به-قدرت = لحظهٔ چرخش
convention. در داراییِ رونددوستِ طلا، **فقط Long**.

## ۲) تعریف سیگنال (منجمد — صفر جاروب)

- `RSI(21)` کلاسیک (Wilder) روی close — دورهٔ ۲۱ فیبوناچی.
- **سیگنال:** `RSI[i−1] < 55` و `RSI[i] ≥ 55` (گذر صعودی از ۵۵ — فیبوناچی).
- ورود در **کندل بعد** (بدون نگاه به آینده). **صفر فیلتر دیگر.**
- آستانهٔ ۵۵ و دورهٔ ۲۱ همین‌جا منجمدند؛ هیچ جاروبی مجاز نیست.

## ۳) مسیر چندگانگی: **مسیر C — تأیید هولد-اوت**

- تقسیم ۶۰/۴۰ (train/holdout) — قرارداد ثابت S910–S912.
- **کارت اصلی: XAUUSD-H2.** جست‌وجوی مجاز فقط در train: هندسه با قاعدهٔ ثابت
  `TP=q60(MFE)` · `SL=q30(MAE)` · قید `TP≥SL` · `max_hold=55` (فیبوناچی، منجمد).
- شرط ورود به داوری (از پیش): لبهٔ train مثبت و بالای هزینه؛ وگرنه سوختن در train
  بدون لمس هولد-اوت.
- **تست نهایی روی هولد-اوت H2: دقیقاً ۱ اجرا** ⇒ `n_trials=1`.
- خانواده: پیکربندی منجمد روی ۱۹ TF طلا، فقط ناحیهٔ train (توصیفی)؛ کارت غیر-اصلی
  در صورت داوری مستقل `n_trials=19`. EURUSD مطلقاً خارج از دامنه.

## ۴) قیود و حسابرسی

- null اختصاصی بازو (الگوی s437)، `K_PERM=500`، گارد BUG-PERMK.
- در صورت حکم غیر-REJECT: حسابرسی هم‌پوشانی با ۵ لایهٔ سایت (به‌ویژه S382-H4 که
  هم‌خانوادهٔ مفهومی است و S356-H1 همسایهٔ TF) با `engine/trade_simulator.py`؛
  بخش هم‌پوشان به‌عنوان فیلتر هم آزموده می‌شود — به تعویق نمی‌افتد.
- حکم موتور عیناً گزارش می‌شود در
  `results/S913_OptimismResumption_Xauusd_<TFs>_rqs2_<score>_<verdict>.md`.
- محدودهٔ شماره‌گذاری این جلسه S910–S919 است؛ به فایل‌های دانشمندان موازی
  (S500–S909، S920–S980) هیچ دستی زده نمی‌شود.
