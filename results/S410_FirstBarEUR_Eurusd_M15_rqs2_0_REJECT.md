# S410 — کندلِ اولِ روزِ EURUSD · M15 · **REJECT (پیش از آزمون، قاعدهٔ توقفِ پیش‌ثبت‌شده)**

> مأموریت: `MISSION_2` · بازه: S410–S419 · معیار: RQS2 **v2.6**
> پیش‌ثبت: `results/S410_PREREG_FIRSTBAR_EUR.md` (commit `06cf7a29`، **قبل از هر تنظیمی**)
> کد: `strategies/s410_firstbar_eur.py` · خروجی خام: `results/_scan_S410/tune.json`
> حکم: **REJECT** — دروازهٔ کشنده: **H9 (نابخشودنی)**، اثبات‌شده روی نیمهٔ اول؛
> **نیمهٔ دومِ داده هرگز باز نشد** (holdout بکر ماند). rqs2_score=0 (داوری رسمی اجرا نشد چون قاعدهٔ توقف زودتر آتش کرد).

---

## ۱. فرضیه و بازتولید (وظیفهٔ ۱ مأموریت — انجام شد)

| کمیت | مقدارِ سندِ مأموریت | بازتولیدِ این جلسه |
|---|---|---|
| E کندلِ اول | +0.63 pip | **+0.63 pip** ✅ |
| t | +6.81 | **+6.81** ✅ |
| P(up) | 0.603 | **0.603** ✅ |
| n | 2094 | **2094** ✅ |
| corr با لایهٔ XAU (فاز ۱۸ §D) | +0.04 | **+0.041** ✅ |

لبه **واقعی** است. پرسشِ این گام هرگز «آیا لبه هست؟» نبود؛ «آیا لبه **به‌اندازهٔ
هزینه بزرگ** است؟» بود — همان پرسشی که H9 می‌پرسد.

## ۲. کارتِ اقتصادی (استخراجی، نه فرضی)

`engine/scalp_engine.py :: ASSETS['EURUSD']` → spread=1.0 + 2×slip=0.3 ⇒
**هزینهٔ کامل 1.6 pip**. سدِ H9: `exp_net > 0.5×spread` **و** `exp_net − 1.6 > 0`.

## ۳. پروتکل (مسیرِ C — مو‌به‌مو اجرا شد)

- تقسیم: `split_bar=100000` از ۲۰۰٬۰۰۰ کندلِ M15؛ تنظیم فقط روی نیمهٔ اول (2018-06 → 2022-04).
- فضای بسته: ۸ فیلتر × ۲ نگه‌داری × ۳ هندسه + ۶ سلولِ دوتایی = ۵۴ سلولِ اجراشده؛ `n_trials=80` اعلامی (با ۲۴ سلولِ ارثیِ فاز ۱۴).
- تابعِ انتخابِ منجمد: `argmax exp@2c` مشروط `n≥200` و `exp@2c>0`؛ **در غیابِ سلولِ واجد ⇒ توقفِ کامل**.

## ۴. نتیجهٔ اسکن (نیمهٔ اول، ۱۰۴۹ رویدادِ لنگر)

بهترین سلول‌ها به حاشیهٔ H9:

| سلول | n | WR | exp خالص | **exp@2c** | t |
|---|---|---|---|---|---|
| **F3+F1 · h=1.5 · SL18 (بدون TP)** | 453 | 66.2 | **+1.515** | **−0.085** | +5.94 |
| F3+F1 · h=1.5 · SL12 | 453 | 65.3 | +1.435 | −0.165 | +5.73 |
| F3 · h=1.5 · SL18 | 627 | 61.7 | +1.228 | −0.372 | +5.17 |
| F1 · h=1.5 · SL18 | 603 | 62.4 | +1.142 | −0.458 | +4.96 |
| F0 (پایهٔ خالص) · h=1.5 · SL18 | 1049 | 55.3 | +0.586 | −1.014 | +3.11 |

الگوهای قطعی:
1. **نگه‌داریِ ۱h در همه‌جا منفی، ۱.۵h در همه‌جا مثبت** ⇒ دریفت در ساعتِ اول هنوز شکل نگرفته — سازگار با پروفایلِ hour0 (اوج در کندل ۴–۶).
2. ۹۱–۱۰۰٪ خروج‌ها زمانی‌اند ⇒ سدها تزئینی (تأییدِ مستقلِ قانونِ دسترس‌پذیریِ سد).
3. فیلترها به‌ترتیبِ حاشیهٔ H9: F3 (مومنتوم آخرین کندل منفی) > F1 (dip4) > F2 > F7 > F5 > F6 > F4. ترکیبِ F3+F1 بهترین.
4. **هیچ سلولی exp@2c>0 ندارد** ⇒ قاعدهٔ توقفِ بندِ ۵ پیش‌ثبت آتش کرد. آزمونِ نهایی، مدلِ صفر، و M30/H1 **اجرا نشدند** — عمداً؛ ادامه‌دادن یعنی سوزاندنِ holdout برای لایه‌ای که از قبل مردهٔ اقتصادی است. (M30/H1 نمونهٔ کمتر با همان لبهٔ زیرِ سد می‌دادند — همان رویداد، تجمیعِ درشت‌تر.)

## ۵. ممیزی هم‌پوشانی (وظیفهٔ ۴ مأموریت — انجام شد)

اندازه‌گیریِ رویدادمحور روی نیمهٔ اول:

| کمیت | مقدار |
|---|---|
| رویدادهای سلولِ برترِ S410 (F3∩F1) | 453 |
| رویدادهای خانوادهٔ S430/S73 (لنگر0+dip4) | 603 |
| اشتراک | **453 = ۱۰۰٪ از S410** · Jaccard = **75.1٪** |

⇒ S410 **زیرمجموعهٔ سختِ** خانوادهٔ S430/S73 است — همان رویداد، فیلترِ اضافه.

**آزمونِ بخشِ هم‌پوشان به‌عنوانِ فیلترِ احیا (بلافاصله انجام شد):**
افزودنِ F3 به خانوادهٔ S430، حاشیهٔ H9 را از **−0.458 به −0.085** رساند
(۸۱٪ کاهشِ فاصله تا سد) با WR 62.4→66.2. یعنی F3 **بهترین فیلترِ شناخته‌شده
برای این خانواده** است — اما هنوز ~۰.۰۹ pip کم می‌آورد. **یافتهٔ قابلِ‌اقدام
برای آینده:** این خانواده فقط با (الف) کاهشِ هزینهٔ فرضی (بروکرِ ارزان‌تر از
1.6pip) یا (ب) یک فیلترِ متعامدِ جدید خارج از فضای امتحان‌شده زنده می‌شود؛
تنظیمِ بیشتر در همین فضا فقط چندگانگی می‌خرد، نه لبه.

## ۶. اثباتِ عدمِ ارتکابِ ۸ اشتباهِ رایج

| # | اشتباه | دفاع |
|---|---|---|
| ۱ | تنظیم روی کلِ داده | تنظیم فقط روی نیمهٔ اول؛ نیمهٔ دوم **هرگز خوانده نشد** (کد: `dfa = df.iloc[:split]`) |
| ۲ | چندگانگیِ پنهان | فضا قبل از اجرا در PREREG با commit جدا منجمد شد (`06cf7a29`)؛ ۵۴ سلولِ اجراشده ⊂ ۸۰ اعلامی |
| ۳ | look-ahead | سیگنال روی آخرین کندلِ دیروز، ورود در openِ 00:00 توسطِ خودِ موتور؛ F3/F4 فقط دادهٔ بستهٔ گذشته؛ corr فاز۱۸ با `shift(1)` |
| ۴ | نادیده‌گرفتنِ هزینه | تابعِ انتخاب = خودِ حاشیهٔ H9؛ pnl شبیه‌ساز از قبل هزینه‌دار |
| ۵ | survivorship در انتخابِ نامزد | نامزد از پیش‌ثبتِ مأموریت آمد، نه از اسکنِ نتایجِ خوب |
| ۶ | max_hold بر حسبِ کندل (BUG-TFM) | `hold_hours × bars_per_hour(df)` اندازه‌گیری‌شده |
| ۷ | هم‌پوشانیِ اندازه‌گیری‌نشده | بندِ ۵ همین سند؛ ۱۰۰٪ تودرتویی با S430 صریحاً اعلام شد |
| ۸ | TP<SL برای WRِ جعلی | هندسه‌ها SL≤TP (G3 متقارن، G1/G2 بدونِ TP)؛ سپرِ H2/H1 موتور دست‌نخورده |

## ۷. جمع‌بندی

لبهٔ کندلِ اولِ EURUSD **واقعی ولی کوچک** است: بهترین پیکربندیِ قابلِ‌دفاع
+1.515 pip می‌سازد و سدِ اقتصادی 1.6 pip است. کسری فقط **۰.۰۸۵ pip** است —
نزدیک‌ترین شکستِ ثبت‌شده روی این خانواده (S430: −0.39). حکم طبق قاعدهٔ
پیش‌ثبت: **REJECT پیش از آزمون؛ holdout بکر ماند** و برای هر تلاشِ آیندهٔ
این خانواده قابلِ‌استفاده است — این باارزش‌ترین داراییِ این گام است.

*ثبت: 2026-08-13 · AI مأمورِ MISSION_2*
