# S607 — پیش‌ثبت: شوکِ انگل با دو گیتِ متعامد (روند × رژیم σ) — قبل از هر محاسبه

**دانشمند:** گئورگ کانتور · **دهه:** S600–S609 (شماره‌ی ۸ از ۱۰) · **دارایی:** XAUUSD فقط · سایت دست‌نخورده
**والدها:** S604 (ACCEPT 80.7؛ گیتِ جهتِ روند TSM، K=60روز) · S606 (ACCEPT 80.1؛ گیتِ رژیم CALM، W=233)
**مرور بالادست پیش از این پیش‌ثبت:** S919 کینز (ACCEPT 88.9، H6، شوک S965 × گیتِ قرارداد S604 — نشانه‌ی حمل‌پذیریِ گیت دریفت)، S1520 گاوس (ACCEPT 90.6، H8)، قانون S529 (pass-rate >75% = گیت مرده)، قانون S525 (ضدِ سوختنِ توان). دهه‌های دیگر فقط خوانده شدند.

## ۱. پرسش
دو گیتِ S604 (جهت: drift = close[t-1] − close[t-1-K] هم‌جهت با معامله) و S606 (سطح: σ_t ≤ median σ_{t-W..t-1}) از دو بُعدِ متفاوتِ فرایندِ قیمت می‌آیند (بردارِ روند vs اسکالرِ نوسان). **آیا ترکیبِ AND آن‌ها اطلاعاتِ افزایشی دارد، یا هم‌خطی است و فقط توان می‌سوزاند؟**

## ۲. تعریفِ منجمد — **صفر پارامترِ آزاد**
- ماشین: عیناً S604/S605/S606 (توابع فریز S840؛ هیچ تابعی بازنویسی نمی‌شود).
- گیتِ DUAL روی کارت‌های H8, H12, H6 = `drift_gate(K_S604) AND calm_gate(W_S606)` با پارامترهای **از پیش منجمد** (بدون انتخاب):
  - H8: K=60روز (=180 کندل) · W=233
  - H12: K=30روز (=60 کندل) · W=89
  - H6: K=60روز (=240 کندل) · W=233
  - D1: خام (در S604 و S605 هیچ گیتی به معیار احیا نرسید).
- **سنجشِ هم‌خطی پیش از داوری (قانون S529):** برای هر کارت، pass-rate هر گیتِ تکی و pass-rate DUAL و نسبتِ `PR_dual / (PR_drift × PR_calm)` (≈1 ⇒ مستقل؛ ≫1 ⇒ هم‌خطی). گزارش می‌شود؛ حکم را تغییر نمی‌دهد.
- اعضای نامزد: {D1 خام, H8-DUAL, H12-DUAL, H6-DUAL}؛ انتخاب‌گر رسمی margin=0.15؛ وتوی پس‌ازFIFO 15%.
- داوری: FIFO تقویمی، محور H1 مصنوعی، close از H1، holdout=صدک60% زمانِ ورود، نولِ منجمد S840 وزن‌دار با سهمِ پس‌ازFIFO.
- **بودجه:** n_trials = 5176 (S606) + 1 = **5177** رسمی؛ تنش 8000. SEED=20260823. TP≥SL.

## ۳. پیش‌بینی‌های ابطال‌پذیر
- **P1 (ضدِ سوختنِ توان — S525):** لیفتِ استخر DUAL > max(لیفت S604=+11.39, لیفت S606=+12.49) = **> +12.49pp**. اگر گیت‌ها سبز اما P1 ✗ → «ACCEPT زائد» (لایه‌ی مستقل نیست).
- **P2 (توان):** n استخر ≥ 150؛ اگر <150، خطرِ H3/H5 اعلام‌شده است.
- **P3 (استقلال):** نسبتِ هم‌خطی در H8 بین 0.8 و 1.25 (گیت‌ها تقریباً مستقل). اگر >1.5، گیت‌ها هم‌خطی‌اند و ترکیب توجیهِ نظری ندارد — این خود یافته است.
- **پیش‌بینیِ شخصیِ کانتور (ثبت برای نقدِ پایان دهه):** DUAL روی H8 لیفت را افزایش می‌دهد اما n را به ~120 می‌کاهد؛ استخر نهایی احتمالاً POWER-LIMITED یا ACCEPT با حاشیه‌ی نازک. یعنی انتظارِ من **شکستِ محترمانه** است؛ اگر ACCEPT قوی شد، پیش‌بینی‌ام غلط بوده و ثبت می‌شود.

## ۴. تعهدات
- ACCEPT با P1 ✓ → ایست و گزارش؛ ممیزی هم‌پوشانی. هیچ استقراری.
- REJECT/POWER-LIMITED/زائد → MD رسمی، خلاصه، حرکت به S608 (پس از fetch و مرور).
- حکم موتور نهایی. هیچ عددی دست‌نویس نمی‌شود.

*کانتور — پیش از دیدن حتی یک عدد. commit اول، اجرا بعد.*
