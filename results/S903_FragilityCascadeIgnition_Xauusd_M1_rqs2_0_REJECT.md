# S903 — Fragility Cascade Ignition · XAUUSD M1 · RQS2 v2.6 → **REJECT (0.0)**

> **مینسکی:** «ثبات، بی‌ثباتی می‌زاید — اما ظاهراً سه کندلِ هم‌جهت با رنجِ فزاینده، در M1 فقط بی‌ثباتیِ حسابِ معامله‌گر را می‌زاید.»

## شناسنامه
| | |
|---|---|
| لایه | S903 — اشتعال آبشار شکنندگی (k کلوز هم‌جهت + TR اکیداً صعودی، یال‌محور) |
| پیش‌ثبت | `results/S903_PREREGISTRATION.md` (کامیت f72ca0cc، **قبل** از هر آزمون) |
| مسیر تعدد | Path C (hold-out) · discovery فقط ۶۰٪ اول · n_trials=16 |
| داده | `data/mt5_full/XAUUSD_M1.csv` · 5,000,000 کندل · 14.34 سال |
| هزینه‌ها | spread=3.3pip · CONTRACT_SIZE=100 · commission=0 · margin=40$/lot |
| null | perm K=1000, seed=903 (جهت‌های تصادفی روی بازده hold-بار) |

## کانفیگ قفل‌شده (از discovery روی ۶۰٪ اول)
`c3_k2.5_rr2.0_h13` → k=3، SL=2.5×ATR14 (med=10.2pip)، RR=2.0 (TP med=20.3pip)، hold=13
- بهترینِ ۱۶ کمبو با معیار پیش‌ثبت‌شده `wr+0.001·net` و کف ۴۰۰ معامله — **همهٔ ۱۶ کمبو در discovery عمیقاً منفی بودند** (net از −264k تا −1.3M pip).

## آزمون نهایی (یکتا، کل داده، split_bar=3,000,000)
| متریک | مقدار |
|---|---|
| n_trades | 130,278 |
| WR | 32.49٪ (breakeven لازم: 44.16٪) |
| **lift** | **−17.5pp** · z=**−126.35** · p_perm=1.0 |
| PF | 0.499 · expectancy=−3.47pip |
| OOS (۴۰٪ آخر) | WR=36.77٪، PF=0.578 — همان جهنم، فقط ولرم‌تر |
| گیت‌ها | H0..H10 همگی FAIL → score=0.0 |

## Verdict: **REJECT** — rqs2_0

## تفسیر مینسکی
1. **رویداد آناتومیک بود، درسِ S882 رعایت شد** — هیچ آستانهٔ توزیعیِ منجمدی وجود نداشت؛ پس این REJECT اطلاعاتِ خالص دربارهٔ خودِ رویداد است، نه شکنندگیِ آستانه.
2. **جهتِ اشتباه؟** lift دو طرف تقریباً متقارن منفی است (long −17.2 / short −17.8). یعنی بعد از سه کندلِ شتاب‌گیرنده، ادامهٔ حرکت *بدتر از سکهٔ شیر-خط* است — بوی **بازگشت** می‌دهد، اما تجربهٔ S901 (فیدِ رویداد S900) نشان داد فیدِ post-hoc بدون پیش‌ثبتِ نو ممنوع و معمولاً باز هم زیر دیوار هزینه دفن می‌شود: WR فیدِ ضمنی ≈67.5٪ لازم دارد 44.2٪ برای برد؛ فید یعنی WR≈67.5 که وسوسه‌انگیز است ولی cost آن سمت هم 3.3pip می‌بلعد و RR معکوس می‌شود. قضاوت به آزمون MTF موکول.
3. **سقف توان شکست** — با hold کوتاه (13) به n=130k رسیدیم؛ مشکل S900–S902 (n≈150 در H8) اینجا وجود ندارد. پس این بار مرگ از کم‌توانی نیست؛ از **نبود سیگنال** است.

## یادداشت مهندسی
شبیه‌سازی با `sim_chunked` انجام شد (هم‌ارزیِ معامله‌به‌معامله با موتور یکپارچه روی H1 در ۳ کانفیگ اثبات و کامیت شد) — چون ۲۷۲هزار یالِ k=2 در M1، سندباکس ۹۸۵MB را می‌کشت.

*ادامه: batch روی ۱۸ تایم‌فریم دیگر با چک‌پوینتِ per-TF.*
