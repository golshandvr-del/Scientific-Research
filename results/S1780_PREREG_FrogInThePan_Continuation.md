# S1780 — پیش‌ثبت: تداوم «قورباغه در قابلمه» (Frog-in-the-Pan Continuation) · XAUUSD · مسیر C

**پژوهشگر:** ابن هیثم (دههٔ S1780–S1789) · **تاریخ:** 2026-09-06 · **وضعیت:** منجمد پیش از هر عدد

## ۱) مالکیت و عدم‌مداخله
S780–S789 من تمام شد (۰ ACCEPT / ۱ POWER-LIMITED / ۹ REJECT). دههٔ جدید S1780–S1789 آزاد است (grep: هیچ فایلی). هیچ فایل خارج از دهه لمس نمی‌شود.

## ۲) ممیزی بکارت (پس از fetch f37a6edf)
- «frog|discreteness|FIP|share of up|up-days» → **۰ فایل** در results/ و strategies/. مکانیزم بکر است.
- نزدیک‌ترین‌ها: S787 (رگهٔ ۵ کندلی هم‌جهت، POWER-LIMITED 22.8 — حالت حدّی ID=−1)، S842 (drift burst استانداردشده، POWER-LIMITED؛ فقط بزرگی رانش، بدون پیوستگی)، S526 (سقف تازه، ACCEPT؛ رویداد قیمتی نه مسیر). تمایز S1780: **کیفیت مسیر** (نسبت کندل‌های هم‌جهت)، نه بزرگی یا اکسترمم.

## ۳) فرضیه (Da, Gurun & Warachka 2014, RFS)
اطلاعاتی که به‌تدریج (گام‌های کوچک پیوسته) وارد قیمت می‌شود، کمتر توجه جلب می‌کند و بیش‌تر کم‌واکنشی می‌آفریند ⇒ ادامهٔ روند پس از پنجره‌های **پیوسته** قوی‌تر از پنجره‌های **گسسته** (چند پرش بزرگ) است.
پیش‌بینی جهت‌دار: در طلا (LONG-جهت‌دار، قانون S522) پنجره‌های صعودی پیوسته ادامه می‌یابند.

## ۴) قاعدهٔ منجمد (صفر پارامتر آزاد — همه از پیشینهٔ فیبوناچی/برندگان)
- پنجره W=21 کندل. R_W = close[i]/close[i−21] − 1. σ_W = std(بازده‌های تک‌کندلی، 89 کندل، shift 1)·√21.
- **پایه (بازوی کنترل P1):** R_W ≥ 1.618·σ_W.
- **رویداد رسمی (FIP):** پایه **و** نسبت کندل‌های صعودی در پنجره ≥ 0.618 (≥13 از 21). سیگنال = **لبهٔ ورود به حالت** (بار قبل شرط برقرار نبود — قانون S963).
- جهت: **LONG-only**. ورود open کندل بعد. SL = TP = 2.058·ATR(89) علّی per-bar (RR=1؛ هندسهٔ S950/S787). max_hold = 21. allow_overlap=False. اسپرد 3.3.
- کارت‌ها: **H8, H12, D1** (۳ کارت). هیچ جست‌وجوی پارامتر، هیچ TF دیگر.

## ۵) داوری
- دادهٔ کامل mt5_full (assert 'mt5_full' in src)، `compute_rqs2` **یک بار** برای هر کارت، split_bar = int(0.60·n) (پروتکل برندگان S602/S770/S950).
- نول متعارف داور S78x: uncond = میانگین ۴۰ نمونهٔ تصادفی هم‌حجم؛ جایگشت K=500، seed=1780.
- **n_trials = 6** (۳ کارت × ۲ بازو). حکم رسمی لایه = بهترین کارت بازوی FIP؛ بازوی کنترل فقط برای P1 گزارش می‌شود و حکم نمی‌گیرد.
- **P1 (گیت باید اطلاعات‌افزا باشد):** lift_FIP − lift_control ≥ +3pp در کارت رسمی؛ وگرنه حتی با حکم سبز، لایه «پیوستگی» را اثبات نکرده و فقط drift-burst است (به S842 ارجاع می‌شود).
- **F1 (ابطال):** اگر در هیچ کارتی lift_FIP ≥ +8pp نباشد، مکانیزم در طلا رد است. بدون تعمیر، بدون تنظیم مجدد.

## ۶) پیش‌بینی شخصی
H8: n≈120–200، lift +8..+14pp، احتمال ACCEPT ~30٪ (خطر H3 مانند S787). D1: n<80 (توان‌محدود). انتظار صادقانه: POWER-LIMITED در H8.

*این سند پیش از نوشتن کد و هر عدد کامیت می‌شود.*
