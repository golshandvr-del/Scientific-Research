# S792 — Shock-Candle Continuation — XAUUSD H3 — RQS2 = 3.5 — REJECT

**src**: `/home/user/webapp/data/mt5_full/XAUUSD_H3.csv` (داده کامل ۱۵.۶ ساله: 2011-01-03 → 2026-08-07، 31806 کندل H3)
**دانشمند**: ابوریحان بیرونی — بلوک S790–S799
**پیش‌ثبت**: `strategies/S792_PREREG.md` — commit `2bb25319` (قبل از هر نگاهی به نیمهٔ دوم)
**داور**: `strategies/s792_final.py` (commit `ca766f07`) → `strategies/s792_final_result_H3.json`
**حکم**: خروجی مستقیم موتور `engine/rqs2.compute_rqs2` — دست‌نویس نیست.

---

## 1) فرضیه (پیش‌ثبت‌شده)

نهادهای بزرگ سفارش‌هایشان را تقطیع می‌کنند (Kyle 1985; Almgren–Chriss): یک «شمع شوک» با
`range ≥ 2.618×ATR89` که close آن در انتهای extreme کندل بسته شود (`clv ≥ 0.764` برای لانگ،
`clv ≤ 0.236` برای شورت) ردپای شروع اجرای سفارش است و باید **ادامه** پیدا کند.

**کارت منجمد (Path C)**: XAUUSD-H3، r=2.618، q=0.236، SL=TP=1.618×ATR89 متقارن، mh=21 کندل،
ورود در open کندل بعد، همپوشانی ممنوع. ۴ پارامتر، همگی فیبوناچی. اسپرد 3.3 پیپ، pip=0.10.

## 2) شواهد اکتشاف (فقط نیمهٔ اول)

- یکنوایی در TF: M15 همه منفی → H1 +5.07p → H3 +10.03p (WR 56.7٪، n=312) → H6 +18.83p
- همسایگی H3: هر ۹ ترکیب (r×q) مثبت
- سال‌به‌سال: 5/8 سال مثبت

## 3) آزمون یگانهٔ نیمهٔ دوم (hold-out)

| متریک | کل | **Hold-out (نیمهٔ دوم)** |
|---|---|---|
| n | 651 | **339** |
| WR | 52.69٪ | **48.97٪** |
| WR سربه‌سر (با هزینه) | 51.54٪ | 51.54٪ |
| PF | 1.033 | **0.894** |
| خالص (پیپ) | +1285 (gross) / امید −4.33p | **−1693.1** |

**بند ابطال پیش‌ثبت فعال شد**: «WR hold-out < 51.5٪ ⇒ فرضیهٔ تقطیع سفارش روی طلا مرده و در
بلوک S79x تکرار نمی‌شود.»

### دروازه‌های RQS2 v2.6

| گیت | نتیجه | توضیح |
|---|---|---|
| H0 | ✅ | داده/ساختار سالم |
| H1 | ❌ | PF=1.033 < 1.3 |
| H2 | ❌ | امید ریاضی −4.33 پیپ (در 2×هزینه: −7.63) |
| H3 | ❌ | lift=3.19pp < 4pp؛ **skill_z=1.10، p_perm=0.136** (نیاز: z≥3.09, p≤0.001)؛ K=500 پرم |
| H4–H10 | ❌ | آبشاری پس از شکست هسته |

null متعارف (هر دو سمت، K=500، seed=792، حفظ تعداد nL/nS):
long: uncond=50.22, perm_mean=50.22, sd=2.79 — short: uncond=48.59, perm_mean=48.72, sd=3.03

### تشخیص counter_drift (همان قاتل S790)

| گروه | n | WR | امید (پیپ) |
|---|---|---|---|
| هم‌جهت رژیم ۲۸۰روزه | 302 | 54.64٪ | **+3.48** |
| خلاف رژیم | 304 | 50.33٪ | **−15.31** |

هر ذرهٔ سود ظاهری از هم‌جهتی با دریفت رژیم می‌آید = **بتای رژیم، نه آلفا**. عدم تقارن سمتی
(long lift +5.53pp / short +0.64pp) نیز همین را می‌گوید: در دههٔ صعودی طلا، «تداوم لانگ» خودبه‌خود
برنده به نظر می‌رسد.

## 4) اثبات پرهیز از ۸ خطای رایج

1. **Overfitting**: ۴ پارامتر منجمد فیبوناچی، بدون بهینه‌سازی روی نیمهٔ دوم؛ همسایگی ۹/۹ مثبت در IS.
2. **Look-ahead**: ATR89 با شیفت ۱ کندل؛ ورود در open کندل بعد از سیگنال؛ سیگنال فقط از کندل بسته‌شده.
3. **Survivorship/انتخاب داده**: کل ۱۵.۶ سال `data/mt5_full` (تلهٔ E-16 رد شد)؛ src در بالای سند.
4. **نادیده‌گرفتن هزینه**: اسپرد 3.3 پیپ در هر معامله؛ حتی تست 2×هزینه گزارش شد (−7.63p).
5. **Multiplicity**: مسیر C — جستجو فقط در نیمهٔ اول، پیش‌ثبت در گیت (2bb25319) قبل از آزمون
   یگانهٔ نیمهٔ دوم؛ n_trials=100 به موتور اعلام شد؛ pooling فقط برای POWER-LIMITED پیش‌اعلام
   شده بود و چون حکم REJECT است **اجرا نشد** (rescue پس از شکست = خطای چندگانگی).
6. **P-hacking**: حکم = خروجی موتور RQS2، دست‌نویس نیست؛ p_perm=0.136 صادقانه گزارش شد.
7. **Regime beta به‌جای آلفا**: تشخیص counter_drift اجرا و گزارش شد — و دقیقاً همین گیت لایه را کشت.
8. **اجرای غیرواقعی**: SL/TP هم‌زمان ⇒ بدترین حالت (ضرر)؛ ورود open بعدی؛ همپوشانی ممنوع.

## 5) اثبات پیروی از قوانین پروژه

- **قانون داده کامل**: 31806 کندل کامل H3، src صریح. ✅
- **قانون بلوک**: همهٔ فایل‌ها s792_* / S792_* — داخل بلوک S790–S799، بدون دست‌زدن به کار دیگران. ✅
- **قانون چک‌پوینت**: هر فایل بلافاصله commit+push (recon dd57c6f0، prereg 2bb25319→11fe7303،
  داور ca766f07→fc6af45a، این سند در commit همین فایل). ✅
- **قانون ذره‌ذره**: اجرای نهایی در background با لاگ. ✅
- **EURUSD ممنوع**: فقط XAUUSD. ✅
- **حکم از موتور**: verdict/score مستقیم از compute_rqs2. ✅

## 6) درس‌آموخته برای دیتابیس علمی

- **الگوی «تداوم» روی طلا در هر شکلی (S790 fade، S791 ORB، S792 shock) در hold-out می‌میرد**؛
  عامل مشترک: سودِ IS از هم‌جهتی با دریفت بلندمدت طلاست و در نیمهٔ دوم که ساختار رژیم عوض
  می‌شود، فرو می‌ریزد.
- یکنوایی TF (M15→H6) در IS فریبنده بود — یکنوایی ≠ علیّت.
- خانوادهٔ فرضیهٔ order-splitting روی XAUUSD در بلوک S79x بسته شد (بند ابطال).

**لایهٔ بعد: S793 — فرضیهٔ نو، پس از fetch و مرور یافته‌های تازهٔ دانشمندان موازی.**
