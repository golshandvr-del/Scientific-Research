# S404 — پیش‌ثبت: گپ‌فیلِ M30 در پنجرهٔ مجازِ H2 + فیلترِ نوسان (حملهٔ چهارم به H8)

> مسیر C چندگانگی · ثبت **قبل از** اجرای هر ترکیب · مُهرِ زمان = همین کامیت
> والدها: S400-M30(فقط-H8) · S401(6.46%) · S402(time-stop خنثی) · S403(REJECT 35.2: H2+H8)

## ۱. دو درسِ S403 که این پیش‌ثبت را شکل می‌دهد

1. **پنجرهٔ H2**: در X-FILL، RR=1/k_sl دقیقاً ⇒ سپرِ RR≥0.5 یعنی **k_sl≤2.0**.
   ناحیهٔ آزمون‌نشده: k_sl∈(1.5, 2.0].
2. **انتقال‌ناپذیریِ DD**: نیمهٔ اول 2.51% → کل‌داده 10.07% (۴×). پس سقفِ فازِ
   تنظیم باید بسیار سخت‌گیرتر از 6.40% باشد و یک اهرمِ *انتخابِ معامله* (نه فقط
   شکلِ pnl) برای کوچک‌کردنِ خوشه‌های باخت لازم است.

## ۲. لایهٔ پایه (منجمد به‌جز اهرم‌ها)

XAUUSD · M30 · گپ‌DOWN · آستانهٔ QW70 · X-FILL · حذفِ DOW=0 · ورود = openِ بارِ اول ·
TP = کلوزِ روزِ قبل · اسپرد 3.3 pip · seed=400 · split M30 = بارِ 90691.
**BE-trigger حذفِ دائم** (S403: در استاپ‌های پهن wr_edge را منفی می‌کند).

## ۳. فضای جست‌وجو (قفل — ۱۲ ترکیب)

| اهرم | مقادیر | توجیه |
|---|---|---|
| k_sl | {1.7, 1.85, 2.0} | درونِ پنجرهٔ H2؛ شناور/غیرگرد |
| فیلترِ نوسان V | {None, ATRq78} | ردِ روزِ سیگنال‌دار اگر ATR14 روزِ قبل > صدکِ ۷۸ٍ رولینگِ ۲۵۰روزهٔ علّی — خوشه‌های باخت در رژیمِ پرنوسان جمع می‌شوند |
| cooldown d | {0, 1} | اهرمِ اثباتاً مفیدِ S401 |

۳×۲×۲ = **۱۲ ترکیب**. قراردادِ فیلترِ V علّی است: فقط ATRهای روزهای < k.
RR≥0.5 به‌ساخت برقرار است (k_sl≤2.0).

## ۴. قاعدهٔ برنده و تعهدها (قفل)

- تنظیم **فقط** روی نیمهٔ اول با حسابداریِ خودِ داور (`run_capital`).
- واجد: n≥100 ∧ PF>1 ∧ t≥2.5 ∧ **maxDD ≤ 4.80%** (0.6×سقفِ H8 — سخت‌گیرتر به‌خاطرِ
  درسِ انتقال‌ناپذیری) ∧ wr_edge ≥ +2pp ∧ rec ≥ 3.
- برنده: کمترین maxDD؛ گره‌گشایی: t بزرگ‌تر.
- هیچ ترکیبی واجد نشد ⇒ **REJECT بدونِ باز‌کردنِ holdout** — بدونِ استثنا.
- در صورت وجودِ برنده: **یک** آزمونِ رسمی `compute_rqs2` روی کلِ داده.

## ۵. حسابِ چندگانگی (صادقانه، تجمعی)

S400:216 · S401:+6 · S402:+12 · S403:+12 · S404:+12 ⇒ **n_trials = 258**.

## ۶. null (بازو-همتا)

استخر = همهٔ روزهای gap-DOWN بی‌قید (بدونِ آستانه/DOW/فیلترV) با همان k_sl/cooldownِ
برنده. K=500، seed=400. هندسهٔ داور: میانهٔ sl_pip/tp_pip اندازه‌گیری‌شده.
(فیلترV جزوِ «مهارت» است پس در null اعمال نمی‌شود — lift ارزشِ آستانه+DOW+V را می‌سنجد.)

## ۷. معیارِ نهایی

RQS2 v2.6 · فایلِ نتیجه: `Strategy_Pair_TFs_rqs2_<score>_<STATUS>.md`

*تاریخ ثبت: 2026-08-18 · Mission 1 · نویسنده: AI-S400s (بازهٔ S400–S409)*
