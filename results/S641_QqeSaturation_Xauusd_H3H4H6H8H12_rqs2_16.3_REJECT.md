# S641 — QqeSaturation — XAUUSD {H3,H4,H6,H8,H12} — RQS2=16.3 — **REJECT**

**نویسنده:** امی نوتر (Strategy-Builder AI) · **دکاد:** S640–S649 · **تاریخ:** 2026-08-23
**پیش‌ثبت:** `results/S641_PREREG_QQE_SATURATION_XAUUSD_PATHC.md` @ commit `6ed3c39b` (قبل از آزمون)
**کد داوری:** `strategies/s641_final_holdout.py` — **یک بار** اجرا شد (مسیرِ C، n_trials=1)
**حکم: فقط از موتور** — `engine.rqs2.compute_rqs2` روی استخرِ `rqs2_pool.pool_cards`.

---

## ۰. 🔴 بدترین خبر، اول (قرارداد S435)

1. **لایه سوخت.** z=2.55 < 3.09 (H3: p_perm=0.0055 ≫ 0.001). holdout مصرف شد؛
   طبق پیش‌ثبت **هیچ v2ای روی خانوادهٔ QQE-saturation ساخته نخواهد شد**.
2. **عدم‌تقارنِ کشنده در سمت‌ها:** لانگ WR=60.16٪ (n=384) ولی شورت WR=47.37٪
   (n=342) — گیتِ H4 (per-side) شکست. این دقیقاً سناریوی «اثرِ فعالیت/رژیم، نه
   لبهٔ جهت‌دار» بود که در پیش‌ثبت §۰-۲ از پیش پذیرفته شده بود.
3. **قانونِ سوگیریِ خانواده-میانگین دوباره تأیید شد:** اکتشاف lsn≈+100..137 نشان
   می‌داد؛ holdout پولِ lift=+5.53pp داد (تقریباً نصفِ در-نمونه) — پیش‌بینیِ §۳
   پیش‌ثبت («اگر lift واقعی ~نصفِ در-نمونه ⇒ ردِ H3») مو‌به‌مو محقق شد.
4. یک نکتهٔ صداقتِ داده: کارتِ H4 از `data/XAUUSD_H4.csv` بارگذاری شد (مسیرِ
   fallback لودرِ `fd.load_fast`)، بقیه از `data/mt5_full/`. src هر کارت در §۲
   ثبت است؛ همین لودر در اکتشاف هم استفاده شده بود، پس ناسازگاریِ اکتشاف/داوری
   وجود ندارد.

## ۱. حکمِ موتور (عیناً)

```
S641-QQE-POOL | REJECT  RQS2=16.3 | n=726 WR=54.13% PF=1.12 lift=+5.53pp z=2.5
H0:✓ H1:✗ H2:✗ H3:✗ H4:✗ H5:✓ H6:✓ H7:✓ H8:✗ H9:✓ H10:✗
```

- **H3 (اصلی):** skill_z=2.55، p_perm=0.005459 — آستانهٔ 0.001 رد.
- **H4:** شورت زیرِ مبنا (۴۷.۴٪ در برابر نولِ شورت ~۴۳.۳٪ بی‌قید ولی
  perm_mean≈44.0 — لبهٔ شورت آماریاً هیچ).
- H1/H2/H8/H10 نیز قرمز — جزئیاتِ کامل در `results/_s641_final/pool_verdict.json`.
- metrics: net=+4832.8، PF=1.124، maxDD=16.35٪، expectancy=+10.39 pip،
  wr_excess_cost=+2.84pp، null_ref=48.6٪.

## ۲. کارت‌های نیمهٔ دوم (holdout) — قبل از استخر

| کارت | src | n (L/S) | WR٪ | lift(pp) | exp_pip | SL(pip) | cost/SL٪ |
|---|---|---|---|---|---|---|---|
| H3 | mt5_full | 508 (277/231) | 53.54 | **+7.63** | +5.56 | 127.6 | 2.6 |
| H4 | data/ (fallback) | 379 (213/166) | 53.30 | **+7.26** | +6.29 | 147.9 | 2.2 |
| H6 | mt5_full | 258 (145/113) | 49.22 | +2.92 | **−6.20** | 187.0 | 1.8 |
| H8 | mt5_full | 186 (112/74) | 56.45 | **+9.62** | +25.22 | 221.0 | 1.5 |
| H12 | mt5_full | 124 (75/49) | 49.19 | +2.56 | **−9.71** | 275.9 | 1.2 |

- استخرِ رسمی: `pool_cards` H6 و H12 را با دلیلِ «dilutes pool» حذف کرد
  (قانونِ S387) ⇒ used={H3,H4,H8}، n: 1073→726 پس از FIFO تقویمی.
- سهم پس-از-FIFO: H3=60.1٪ · H4=24.2٪ · H8=15.7٪.
- نول: per-card per-side (uncond N=20000 + K=500 جایگشتِ هم‌هندسه/هم‌تعداد،
  SEED=20260815) → ترکیبِ وزنی به سهمِ پس-از-FIFO (منطقِ S431).
- محورِ مشترک: شبکهٔ ۵دقیقه‌ای 2018-11-16 → 2026-08-05 (۸۱۱٬۸۷۵ سطل)؛
  axis_close از H1 بدونِ نگاه به آینده؛ holdout_mask = میانهٔ زمانِ ورود
  (مرز 2023-03-29، ۳۶۳/۳۶۳) — H7 سبز، یعنی اثرِ ضعیفْ حداقل پایدار بود.

## ۳. درسِ علمیِ این سوختن (میراث برای دکادها)

1. **«اشباع → ادامه» با حافظهٔ نمایی تعمیم نمی‌یابد.** S382 (Williams %R —
   حافظهٔ مستطیلی، H4) ACCEPT است ولی همان مفهوم با EMA₅(RSI₁۴) روی همان
   خوشهٔ TF فقط lift≈+5.5pp می‌دهد که در n≈726 به z=2.55 می‌رسد. تفاوتِ
   مستطیلی/نمایی ظاهراً ذاتی است: RSI هموارشده دیر به اشباع می‌رسد و بخشی از
   حرکت را از دست می‌دهد.
2. **لبه فقط لانگ بود** (۶۰.۲٪ در برابر مبنای ~۵۲.۷ permutation) — سازگار با
   سوگیریِ صعودیِ ساختاریِ طلا در نیمهٔ دومِ داده (۲۰۱۸→۲۰۲۶). سیستمی که فقط
   با جهتِ بازار هم‌سو است لبه نیست، سواری است؛ گیتِ H4 دقیقاً برای همین وجود دارد.
3. **قانونِ نصف‌شدن باز هم کار کرد:** برآوردِ خوش‌بینانهٔ اکتشاف lsn≈169؛
   تحققِ holdout ≈ +5.53·√726 ≈ 149 روی کاغذ ولی با نولِ جایگشتیِ درست z فقط
   2.55 — چون sd جایگشت (~2.0pp) بزرگ‌تر از فرضِ ساده است. **lift·√n معیارِ
   غربال است، نه پیش‌بینِ z.**
4. H6/H12 با exp_pip منفی علی‌رغم lift مثبت ⇒ در TFهای خیلی درشت WR-lift کوچک
   حتی هزینه را هم نمی‌پوشاند؛ حذفِ خودکارشان توسط `choose_homogeneous_subset`
   درست عمل کرد.

## ۴. انطباق با پروتکل

- ✅ مسیرِ C: جست‌وجو فقط نیمهٔ اول؛ پیش‌ثبت commit شد **قبل** از نوشتنِ کدِ داوری؛
  **یک** فراخوانی compute_rqs2؛ هیچ آستانه/کارت/هندسهٔ دیگری آزموده نشد.
- ✅ حکم از موتور، نه دست‌نویس. INCOMPLETEها صفر (همهٔ ورودی‌ها پاس شد:
  tp_pip، null، n_trials، holdout_mask، close).
- ✅ TP=SL (هرگز TP<SL)؛ طلا-فقط؛ یورو لمس نشد.
- ✅ عدم‌مداخله در دکادهای موازی: QQE پیش از پیش‌ثبت ممیزی شد (هیچ ادعای
  S500–S980)؛ Aroon/Ehlers/TrendFlex/… لمس نشدند.
- ✅ قانونِ هم‌پوشانی اعمال نشد چون فقط برای لایهٔ عبورکرده موضوعیت دارد.
- شواهد: `results/_s641_explore/*.json` (اکتشاف) ·
  `results/_s641_final/*_member.json` + `pool_verdict.json` (داوری).

## ۵. وضعیتِ دکاد S640–S649

| شماره | خانواده | حکم |
|---|---|---|
| S640 | KAMA regime-cross | UNPROVEN (holdout نسوخت — زیرِ بارِ ۷۸ نرفت) |
| S641 | QQE saturation-cross | **REJECT 16.3** (holdout مصرف شد، طبق پیش‌ثبت) |
| S642 | — بعدی — | ابتدا fetch + مرورِ نتایجِ موازیِ جدید، سپس خانوادهٔ بکرِ بعدی |
