# S771 — الحاقیهٔ پیش‌ثبت ۱: استخرِ اعضای قوی (تنها و آخرین آزمونِ استخرِ S771)

> ⏱ این سند **پیش از اجرای استخر** کامیت می‌شود. حقِ دو استخر در پیش‌ثبتِ پایه (3c9aa0ed) ذخیره شده بود؛ اینجا **فقط یک** استخر مصرف می‌شود و دومی داوطلبانه واگذار می‌گردد.

## ۱. وضعیتِ پیش از این آزمون (ثبت‌شدهٔ صادقانه)
۲۰ کارتِ تکی همه REJECT/REJECT_BY_RULE. بهترین‌ها: D1 22.0 (PF 1.451, lift +5.57, z 1.14)، W1 20.3 (PF 1.439)، H12 20.2 (PF 1.345). هیچ کارتی H3 را پاس نکرد (کم‌توانی؛ n هر کارت ≤ ۳۱۶ در حالی که n_required≈۴۳۰).
H6 با lift +6.08 و z=1.67 اما PF 1.258 و DD 10.8% — «هیجانِ آماری بدونِ اقتصاد».

## ۲. قاعدهٔ عضویت (منجمد، عینی، پیش از اجرا)
**عضو = کارتی که در داوریِ تکی PF ≥ 1.30 داشته باشد.** (سخت‌گیرتر از S770 که 1.2 بود، چون S771 عضوِ H6 با DD بالا دارد و درسِ S770 گفت اعضای ضعیف اقتصادِ استخر را می‌کُشند.)
⇒ اعضا: **{W1, D1, H12}** — با پیکربندیِ منجمدِ داوریِ تکی (θ=0.35، hold=16 برای هر سه).
هیچ عضوِ دیگری اضافه/کم نمی‌شود؛ choose_homogeneous_subset موتور اجازه دارد بر اساس POOL_ADD_MARGIN عضوی را حذف کند — حکمِ آن پذیرفته است.

## ۳. پروتکل (کلمه‌به‌کلمه S770-POOL2)
- `engine/rqs2_pool.pool_cards` با FIFO تقویمی (concurrency=1).
- نولِ استخر: `blend_pool_null` وزن‌دار به سهمِ کارت.
- holdout_mask = زمانِ ورود ≥ کوانتیلِ ۶۰٪ (پروتکلِ S431).
- محورِ مرجعِ H1 با close برای H10.
- **n_trials = 120 + 20 داوری + 1 استخر = 141** (صادقانه؛ برای S771 مستقل از S770 شمرده می‌شود چون فرضیه و داده‌ی فیچر متفاوت است، اما در MD نهایی خویشاوندی با S770 ذکر می‌گردد).

## ۴. تعهد
این **آخرین** آزمونِ S771 است. هر حکمی که موتور بدهد، حکمِ رسمیِ S771 است. پس از آن → گزارش کوتاه → fetch گیت‌هاب → S772.
