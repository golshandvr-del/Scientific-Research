# S834 — موجِ شاخصِ نیرو (Elder Force-Index Surge, Symmetric Follow) — XAUUSD-H1 — حکم نهایی

**حکم رسمی موتور: REJECT (امتیاز 8.1)** · `engine/rqs2.py::compute_rqs2` v2.6 · مسیر C (هولد‌اوت)
پژوهشگر: جیم سایمونز · دههٔ S830–S839 · تاریخ: 2026-09-04
پیش‌ثبت: `results/S834_PREREG_FORCE_SURGE_HOLDOUT.md` (کامیت `aae11b19` — قبل از آزمون)
داور: `strategies/s834_force_surge.py` · شواهد: `results/_scan_S834/`

## ۱) فرضیه (خلاصه)

Force Index = EMA13(volume×Δclose)، z-نرمال با sd غلتانِ 144. عبورِ تازه از ±2.0 =
جریانِ سفارشِ فعالِ یک‌سو ⇒ ادامه. آینه‌ای کامل، جهت درون‌زاد، هندسه‌ی ساده
(SL=2×ATR34، TP=1.3×SL، hold=21، بدون trail/BE). قلمروِ بکر (صفر لایه‌ی Force در بانک).

## ۲) اعداد

| | اکتشاف (۶۰٪) | هولد‌اوت (از 2020-05-27) |
|---|---|---|
| n معامله | 1,114 | **801** (L=390 S=411) |
| WR | 47.85% | **45.44%** |
| نالِ هم‌هندسه K=500 | 44.35%±1.43 | **44.78%±1.68** |
| INFO-lift | +3.49pp (z=2.4) | **+0.67pp (z=0.40)** |
| PF | 1.096 | **1.069** |
| exp | +3.40 pip | +5.83 pip |

سرشماریِ کورِ پیش‌ثبت (L=434, S=469) دقیقاً تکرار شد. دروازه‌ها: H0 ✓، H9 ✓، H10 ✓؛
H1 (PF≥1.3) ✗، H2 ✗، H3 (lift≥4pp & p≤0.001) ✗، H4–H8 ✗. یادداشت موتور: `PF=1.011<1.3`.

## ۳) تشریحِ مرگ

1. **پیش‌بینیِ «محتمل» پیش‌ثبت محقق شد** (lift ∈ [+2,+5] پیش‌بینی، واقعیت +0.67 —
   حتی پایین‌ترِ آن). REJECT بود، نه POWER-LIMITED: با n=801 توان کافی بود؛
   سیگنال کوچک است.
2. **این بار مرگِ رژیمی *نبود*.** برخلاف S830/S831/S833 که lift از +9..+14 به ~0
   فروریخت، اینجا اکتشاف هم فقط +3.5pp بود و هولد‌اوت +0.7 — تفاوت در حدِ ۱.۵
   sd. لبه‌ی حجمی در هر سه رژیمِ اکتشاف مثبت بود و در 2020+ هم منفی نشد؛
   فقط **همیشه کوچک** بود. آزمونِ سه‌رژیمی (درسِ S833) کارش را کرد: این نخستین
   لایه‌ی من است که در هولد‌اوت *معکوس* نشد.
3. **درسِ تازه**: ناورداییِ رژیمی ≠ بزرگیِ لبه. سیگنالِ حجمیِ z-نرمال پایدار است
   اما اطلاعاتش ~۱–۳pp است — زیرِ هزینه‌ی ۳.۳pip اسپرد در H1. اگر جایی زنده
   باشد، در TFهای بالاتر (H8/H12/D1 — همان‌جا که ACCEPTهای موازی S965/S950/S770
   زندگی می‌کنند) و با هندسه‌ی بزرگ‌تر است؛ در کاوش ۲، H8/FORCE k=−2.0 lift=+8.2pp
   (n=75) و VOLRAT H8 +11.2pp (n=66) دیده شد ولی توانِ کافی نداشت.
4. **هیچ چرخشِ پس-از-دیدن.** یک آزمون، قفلِ `S834_HOLDOUT_SPENT.lock`.

## ۴) درس‌های بانک‌شده برای S835+

- چهار لایه‌ی H1 (S830–S834): سه مرگِ رژیمی + یک لبه‌ی پایدارِ کوچک. **H1 برای
  لایه‌ی مستقلِ اسپردی 3.3pip مقدورترین TF نیست** — نسبتِ هزینه/ATR بالاست و
  اطلاعاتِ جهتیِ خالص در آن اندک. S835 به H8/H12/D1 می‌رود (طبق MTF-law همیشه
  همه‌ی TFها بعداً بررسی می‌شوند، ولی اکتشافِ اولیه از بالا).
- ابزارهای اثبات‌شده که می‌مانند: تقارنِ کامل، نالِ هم‌هندسه، سه رژیمِ واقعی،
  سرشماریِ کور، هندسه‌ی بدون trail.

## ۵) ۸ اشتباهِ رایج — گواهی

1. Overfitting/چرخش: ✗ — پیش‌ثبتِ منجمد، یک آزمون، قفل. 2. Look-ahead: ✗ — ورود
open کندلِ بعد؛ z با sd تا t. 3. TF-snooping: ✗ — فقط H1 پیش‌ثبت. 4. هزینه: ✗ — اسپرد/
اسلیپیجِ موتور. 5. WR vs 50%: ✗ — نالِ جای‌گشتیِ هم‌هندسه. 6. چندگانگی: ✗ — مسیر C،
n_trials=1، ~۱۴۶ سلولِ اکتشاف همه در پنجره‌ی جدا و شمرده‌شده. 7. E-16: ✗ — assert +
verify 19/19. 8. شفافیت: ✗ — لاگ/JSON/قفل در git.

MTF/همپوشانی: فقط برای بازمانده‌ها — اجرا نشد.

— جیم سایمونز · S834 · 2026-09-04
