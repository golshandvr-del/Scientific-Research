# S1911 — پیش‌ثبت: «شوکِ مطلع در آرامش — خبرِ واقعی در سکوت» (Kyle Shock × Calm-σ Regime)

**دانشمند:** جان مینارد کینز · **بلوک دوم:** S1910–S1919 · شمارهٔ ۲ از ۱۰
**وضعیت:** پیش‌ثبت — **هیچ عددی محاسبه نشده.** کامیت قبل از هر اجرا.
**داور:** `engine/rqs2.py::compute_rqs2` v2.6 · **داده:** `data/mt5_full/` ۱۵.۶ سال کامل · XAUUSD فقط.

## ۰) سؤال باز و ممیزی تازگی

دیتابیس دو **گیت رژیم** مستقل روی شوک انگل ثابت کرده: جهت روند (S604، K=60d) و **آرامش σ** (S606: `σ_t ≤ median(σ_{t−233..t−1})`، P2 گذشت: lift +10.2→+12.5). S607 نشان داد دو گیت متعامدند (نسبت هم‌خطی ≈1.0–1.07) و AND آن‌ها بالاترین نمرهٔ دهه را داد (83.1).
روی **شوک کایل** (S965: `rng≥2.618·ATR21`, `ρ≥0.618`) فقط گیت جهت آزموده شده (S966-H8، S919-H6، S1910-H12/D1 — و S1910 نشان داد گیت جهت روی H12 بی‌اطلاع است). **گیت آرامش σ روی شوک کایل هیچ‌جا آزموده نشده** (grep `calm|σ ≤ median|آرام` روی S96x/S97x/S15x/S19x: S967 «آرام» را برای کندلِ *بعد* از شوک تعریف کرد — مفهومی دیگر و REJECT).
نزدیک‌ترین‌ها: S606، S607، S965، S966، S919، S967.

**روایت کینزی:** قرارداد بازار در دورهٔ آرام «محکم» است؛ شوک مطلع در آرامش یعنی خبری که قرارداد را می‌شکند و جمعیت هنوز واکنش نداده ⇒ ادامه. در طوفان، شوک فقط یک کندلِ دیگر از خوشهٔ نوسان است.

## ۱) قاعدهٔ منجمد (صفر پارامتر آزاد — به ارث)

| جزء | مقدار | منبع |
|---|---|---|
| شوک/retention/جهت/براکت/mh | عیناً S965: θ=2.618، ρ≥0.618، follow، SL 1.272/TP 2.058 ×ATR21[t−1]، mh=16 | S965 |
| **گیت آرامش** | `σ²_t = 0.94σ²_{t−1} + 0.06·r²_{t−1}` (RiskMetrics، r = log-return کندل، علّی — σ_t فقط از ≤t−1)؛ ورود فقط اگر `σ_t ≤ median(σ_{t−233..t−1})` | S606 |
| ورود/overlap/هزینه | open کندل بعد · allow_overlap=False · 3.3 پیپ | استاندارد |

هیچ گیت جهتی نیست — این لایه گیت σ را **تنها** می‌سنجد (AND با جهت = S1912 احتمالی، فقط اگر این یکی اطلاع نشان دهد).

## ۲) کارت‌ها و n_trials

- **کارت‌ها: H8 (اصلی) و H6.** H8 چون S606 آرامش را روی H8 ثابت کرد و S965 پایه دارد؛ H6 چون S919 نشان داد کارت زنده است.
- ⚠️ تلهٔ S603: H8 و H6 هر یک ACCEPT بی‌گیت/با گیت جهت دارند (S965، S966، S919). گیت σ روی آن‌ها **زیرمجموعه** می‌سازد. مشروعیت فقط از **P1** می‌آید: lift گیت‌شده باید از بی‌گیت *بالاتر* باشد (ملاک S606-P2 / S603). اگر ACCEPT گرفت ولی P1 نگذشت ⇒ در MD «افزونهٔ بی‌ارزش» علامت می‌خورد، مانند S603.
- قاعده منجمد ⇒ یک لمس کل داده، n_trials = 2. تنش گزارشی تا 620 (S966) و 5176 (S606).
- قانون بلوک n<150: pass-rate S606 ≈ 67٪ ⇒ H8 n≈95، H6 n≈160. علامت‌گذاری طبق S919 §۳.

## ۳) ابطال‌گرها

- **P1 (اصلی):** WR/lift گیت‌شده > بی‌گیت follow روی همان کارت. بی‌گیت فقط گزارش.
- **P3:** بازوی «طوفان» (`σ_t > median`) باید ضعیف‌تر باشد؛ اگر طوفان بهتر بود، روایت غلط است و «خبر در طوفان» ثبت می‌شود.
- **P5 (هم‌خطی، قانون S529/S607):** pass-rate گیت σ روی رویدادهای کایل گزارش می‌شود؛ اگر >85٪ گیت مرده است (همه را رد می‌کند یا هیچ را)؛ اگر <30٪ توان‌سوز.
- **P2:** بدون z≥3.09 ⇒ REJECT بی‌تیون.

## ۴) پیش‌بینی شخصی

H8: n≈90–110، lift +14–20، z 2.6–3.4 — مرزی؛ اگر ACCEPT، P1 با lift S965 (+12.8) مقایسه می‌شود. H6: n≈150–170، lift +6–10، z 1.8–2.6 ⇒ REJECT. پیش‌بینی P3: طوفان WR≈45. احتمال ACCEPT با P1 گذشته: ~35٪.

## ۵) ۸ اشتباه — تعهد

Lookahead (σ_t از r تا t−1، median پنجرهٔ بسته t−233..t−1، ρ از کندل t، ورود t+1) · snooping (صفر پارامتر، پیش‌ثبت قبل عدد) · survivorship (کل mt5_full، src در JSON) · multiplicity (n_trials=2 + تنش) · نول هم‌هندسه K=500 perm_k=pa.size · هزینه 3.3 · cherry-pick (هر دو کارت + هر سه بازو منتشر) · حکم فقط موتور.

— ج. م. کینز
