# S562 — PREREG — فیلتر نوسان علّی روی GapOpen (M15 و H1 فقط، XAUUSD)

**تاریخ ثبت**: 2026-08-20 — کامیت **قبل از هر تست** (git timestamp = preregistration).
**دانشمند**: لایب‌نیتس — بلوک S560–S569. **EURUSD حذف به دستور کاربر.**

## 1) انگیزه — تلاقی دو شاهد مستقل

- **S561 (من)**: اهرم شکلِ-pnl (براکت تنگ) روی GapOpen درشت‌TF شکست خورد و سوخته اعلام شد؛ DD بدتر شد (M15: 12.4→27.3%).
- **S404 (دانشمند موازی، ACCEPT 96.8)**: H8 با **فیلتر انتخاب معامله** شکسته شد — حذف روزهای پرنوسان با ATR علّیِ روزِ قبل > چندک رولینگ. DD او 13.35→2.63% شد **و منتقل شد**.

**فرضیهٔ S562**: همان قانون روی GapOpen-NegGap من (سیگنال S560، هندسهٔ برندهٔ V-TIME q98) کار می‌کند: خوشه‌های باخت GapOpen در رژیم پرنوسان زندگی می‌کنند؛ فیلتر علّی V آن‌ها را حذف و H8 را سبز می‌کند بدون کشتن z.

## 2) دامنه — چرا فقط M15 و H1؟

- M1/M5: ACCEPT قطعی S560 — ممنوع‌اللمس.
- **M30 حذف**: S404 (دانشمند دیگر) دقیقاً روی روزهای گپ منفی M30 با فیلتر V لایهٔ ACCEPT مستقر دارد؛ ساخت نسخهٔ تقریباً هم‌پوشان، ارزش علمی ندارد و تداخل قلمرو است.
- می‌ماند: **M15 و H1**.

## 3) ثابت‌ها (منجمد — تغییرناپذیر)

- سیگنال: عیناً S560 (`locked_config.json` S560): M15(q70,sw,hold1)، H1(q80,sw,hold2)؛ شکست روز BUG-BRKTHRESH-safe؛ چندک انبساطی علّی min20؛ ورود open کندل si+1.
- هندسه: عیناً برندهٔ S560 — V-TIME، SL=TP=q98 نیمهٔ اول (M15: 50.9، H1: 101.4)، hold قفل. **هیچ چندک هندسهٔ جدیدی محاسبه نمی‌شود.**

## 4) فضای پارامتر جدید (فقط فیلتر)

فیلتر V (الگوی S404): ردِ سیگنال اگر «نوسان مرجعِ علّیِ قبل از ورود» > چندک رولینگ 250-روزهٔ علّی (min 60 نمونه). نوسان مرجع = میانگین True Range روزانهٔ 14 روزِ *قبل* (فقط داده تا پایان روز قبل).
- qv ∈ {70, 78, 85} → **3 بازو × 2 TF = 6**
- **n_trials انباشتهٔ خانوادهٔ GapOpen = 415 + 6 = 421**

## 5) مسیر — Path C

- قفل qv هر TF روی **نیمهٔ اول فقط** (split 2018-10-20)؛ معیار: t نیمهٔ اول؛ تساوی(<0.01) → DD نیمهٔ اول کمتر.
- تک‌لمس نیمهٔ دوم؛ حکم فقط `compute_rqs2` (gates_only، ۵ ورودی الزامی، null بازو-محور با فیلتر جزو مهارت — استخر perm بدون فیلتر، الگوی S404 §4: آستانه/فیلتر جزو skill است)، K=500، **seed=20260815**، n_trials=421، split_bar همان S560. غربال توان قبل از داوری؛ n<30 → بدون حکم (قید ۲).

## 6) پیش‌بینی‌ها (قبل از دیدن هر عددی)

- **P1**: M15 با یکی از qv ها H8 را پاس می‌کند (DD<8%) و ACCEPT می‌گیرد — چون DD پایهٔ آن (12.4%) به سقف نزدیک است و مکانیزم S404 در M30ِ همسایه اثبات شده.
- **P2**: فیلتر n را ~20–35% کم می‌کند اما z بالای bar می‌ماند (لبه جهتی است).
- **P3**: H1 مرزی است (DD پایه 9.45%)؛ شانس عبور کمتر از M15.

## 7) معیار توقف و صداقت

بازوی قفل‌شده رد شد → هیچ بازوی دیگری روی نیمهٔ دوم تست نمی‌شود؛ نتیجه verbatim گزارش می‌شود. اگر هر دو TF رد شدند، خانوادهٔ GapOpen درشت‌TF با ۳ شکست (q98، براکت، فیلتر) برای این نسل بسته اعلام می‌شود. Overlap: در صورت ACCEPT، هم‌پوشانی روزانه با S404-M30 (رویداد خویشاوند در TF متفاوت) و S355 اندازه‌گیری و بند 3 اجرا می‌شود.

*لایب‌نیتس — S562 | Path C | seed=20260815*
