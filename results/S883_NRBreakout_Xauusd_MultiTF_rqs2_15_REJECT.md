# S883 — شکست فشردگی NR — XAUUSD — حکم نهایی: ❌ REJECT

**داور**: `engine/rqs2.py` v2.6 — حکم را موتور صادر کرده.
**پیش‌ثبت**: `results/S883_PREREG_NRBreakout_PathC.md` (commit `bb9ec536` قبل از هر شبیه‌سازی)
**اسکریپت حکم**: `strategies/s883_verdict.py` (commit قبل از اجرا)
**بهترین امتیاز**: H8 = **15.0** → `rqs2_15` | داده: ۱۵.۶ سال mt5_full، Path C، n_trials=36، seed=883

## ۱. فرضیه
NR-m (دامنهٔ کندل اکیداً کوچک‌تر از m−1 کندل قبل — رویداد آناتومیک نسبی، **بدون چندک منجمد**؛ درسِ مرگ سه لایهٔ قبلی) + اولین close خارج از دامنه در kc کندل → continuation.
پیش‌بینی‌های ابطال‌پذیر: (الف) لبه دوطرفه؛ (ب) m بزرگ‌تر → لبهٔ قوی‌تر.

## ۲. IS (نیمهٔ اول، ۳۶ پیکربندی، ۲۰ TF)
- M1…H2: **مرده — دیوار اسپرد (تکرار پنجم)**. H6/H12/D1/MN1: بدون نامزد.
- نامزدها: H3 (m13, n=747, lift=+1.22)، **H4 (m5, n=1140, lift=+5.20, score=175.6)**، H8 (m13, n=265, lift=+4.66)، W1 (m5, n=40, lift=+1.46).
- پیش‌بینی (ب) همین‌جا مشکوک شد: بهترین نامزد m=5 بود نه m=13 — پرچم قرمز ثبت شد.

## ۳. حکم hold-out (یک شلیک، K=500 per-side)

| TF | n OOS | WR IS→OOS | p_perm | امتیاز | حکم |
|----|------|-----------|--------|--------|-----|
| H3 | 923 | 41.5→39.0 | 0.402 | 0.4 | ❌ |
| H4 | 1525 | 51.8→48.2 | 0.252 | 1.8 | ❌ |
| H8 | 333 | 42.6→44.1 | **0.089** | 15.0 | ❌ |
| W1 | 47 | 57.5→51.1 | 0.621 | 5.0 | ❌ |

## ۴. تشخیص مرگ
1. لبهٔ IS کوچک (lift ≤5pp) روی n بزرگ → در OOS به‌سمت صفر میل کرد؛ H4 با n=1525 فقط lift≈+1.6pp داشت — سیگنال NR در XAUUSD تقریباً هم‌ارز نمونه‌گیری تصادفی است.
2. H8 تنها بازمانده (p=0.089) اما ۱۱ برابر دورتر از آستانهٔ 0.001 — شانس، نه مهارت.
3. تأیید پرچم قرمز: مکانیزم «فشردگی» با m عمیق‌تر قوی‌تر نشد — رویداد NR روی طلای پرنوسان معنای انباشت سفارش ندارد (برخلاف سهام که Crabel رویش کار کرد).

## ۵. اثبات ۸ خطا: (۱) بدون look-ahead — تأیید close، ورود open بعدی؛ (۲) ۱۵.۶ سال + assert فعال mt5_full؛ (۳) Path C پیش‌ثبت bb9ec536، n_trials=36=کل فضا؛ (۴) حکم فقط از موتور؛ (۵) null کانونی K=500 per-side داخل OOS؛ (۶) هیچ چندکی وجود نداشت که نشت کند، sl/tp منجمد از نیمهٔ اول، گرم‌سازی صفر شد؛ (۷) پارامترها فیبوناچی 5,8,13/34,55,89؛ (۸) tp≥sl همیشه.
**نجات پس از شکست**: هیچ — طبق تعهد §6. ممیزی هم‌پوشانی: N/A.

## ۶. درس‌ها
- NR/compression روی XAUUSD حتی به‌صورت آناتومیک هم لبه ندارد — نویز طلا در همهٔ رژیم‌ها فشردگی واقعی نمی‌سازد؛ خانوادهٔ range-compression در کنار خانوادهٔ moment بایگانی شود.
- لایه‌های ACCEPT همگی **لنگر ساختاری بیرونی** دارند (گپ روزانه، جهش، سطح رند، سشن) نه صرفاً شکل کندل. S884 باید رویدادی با لنگر ساختاری بیرونی و بکر در بلوک S880-889 باشد.
