# S422 — پیش‌ثبتِ لایهٔ **Exuberance SHORT** (آینهٔ سلولِ مقابلِ ماتریسِ نامتقارن)

> ⏱ این فایل **پیش از هر بک‌تستِ رسمیِ S422** commit/push می‌شود. مُهرِ git = پیش‌ثبت.

**لایه:** S422 — SHORT سوئینگ در سلولِ «روندِ صعودی + نوسانِ Q4» (شیدایی/blow-off).
**نسبت با S420:** فرضیهٔ **متفاوت و مستقل** — سلولِ دیگرِ همان ماتریسِ تأییدشدهٔ
فاز ۱۹/۲۰. S420 (سلولِ خرید) در پنجرهٔ بکر REJECT شد؛ این پیش‌ثبت هیچ پارامتری
از آن قرض نمی‌گیرد جز زیرساختِ اجرایی.
**معیار:** RQS2 v2.6 · **محدوده:** S420–S429 · **کد:** `strategies/s422_exuberance_short.py`

## ⚖️ افشای کاملِ وضعیتِ داده (شرطِ صداقتِ این پیش‌ثبت)

- پنجرهٔ بکرِ ۲۰۱۱–۲۰۱۹ قبلاً **یک بار** برای فرضیهٔ S420 (LONG در سلولِ
  تسلیم) باز شده. تنها اطلاعِ من از آن پنجره = خروجیِ ۴۱ معاملهٔ همان سلول
  (شکستِ اقتصادی؛ ۲۰۱۳ سالِ بد برای LONGهای تسلیم).
- سلولِ «روندِ صعودی + Q4-vol» و رفتارِ SHORT در آن، در ۲۰۱۱–۲۰۱۹ توسطِ
  **هیچ‌کس** (شاملِ خودِ من) دیده نشده — رویدادهایش مجموعهٔ جدایی از رویدادهای S420 است.
- ⚠️ سوگیریِ بالقوه: دانستنِ «۲۰۱۳ خرسی بود» می‌تواند انتظارِ موفقیتِ SHORT را
  بالا ببرد. این دانش در **هیچ پارامتری** دخالت داده نشده (گرید از منطقِ پیشینی
  می‌آید، بندِ ۲) و حکم را فقط دروازه‌های RQS2 می‌دهند، نه انتظارِ من.
- چندگانگیِ بین-فرضیه‌ای: این **دومین** فرضیه‌ای است که از این پنجره تأیید
  می‌گیرد ⇒ در گزارش، علاوه بر n_trials درون-فرضیه، `EFDR_cross = p_emp × 2`
  نیز اعلام می‌شود (سخت‌گیرانه‌تر از الزامِ صریحِ v2.6).

## ۱. مسیر: C معکوس (همان منطقِ S420)

| نقش | بازه | کاربرد |
|---|---|---|
| کشف/تیون | 2020-01-01 → پایانِ داده | گریدِ بندِ ۲؛ قفل به JSON؛ commitِ جداگانه |
| تأیید (بکر برای این فرضیه) | 2011-01-03 → 2019-12-31 | **یک** اجرای RQS2 |

قاعدهٔ مرگِ زودهنگام: اگر در پنجرهٔ کشف **هیچ** ترکیبی exp>0 ∧ n≥30 نداشت،
کارت در همان مرحله می‌میرد و پنجرهٔ بکر **اصلاً باز نمی‌شود** (حفظِ داده).

## ۲. قاعدهٔ سیگنال و فضای کشف (تثبیت‌شده)

روزِ معاملاتی/بازده/vol/چارکِ علّی: **همان تعاریفِ S420** (زیرساختِ مشترک، نه پارامتر).

```
سیگنال (روزِ i): trend_5d > 0  ∧  vol_5d ≥ چارکِ q علّی  ∧  شتاب:
                 |ret_i| ≥ a × mean(|ret| دو روزِ قبل)
⇒ SHORT در openِ اولین کندلِ H1 روزِ بعد · خروجِ فقط-زمانی پس از hold روزِ معاملاتی
· بدونِ استاپِ قیمتی (براکتِ مجازی ±2000$/oz؛ راستی‌آزماییِ ۰-برخورد در هر اجرا)
```

| محور | مقادیر | منشأ پیشینی |
|---|---|---|
| `W` | 5 (ثابت) | هفتهٔ معاملاتی — همان قراردادِ پروژه |
| `q` | `0.70, 0.75` | همان دو چارکِ مجازِ سندِ مأموریت |
| `a` شتاب | `1.0, 1.5` | آینهٔ منطقیِ decel: a=1.0 «هنوز کند نشده»، a=1.5 «شتابِ آشکار» (placeboی فاز۲۰ بدونِ این قید بود؛ قید از منطقِ blow-off می‌آید نه از داده) |
| `hold` | `5, 8` | کوتاه‌تر از LONG (سقوط‌ها سریع‌تر از رالی‌ها تخلیه می‌شوند — عدمِ تقارنِ شناخته‌شدهٔ vol)؛ ۸ همان عددِ اکتشافیِ ماتریس |
| جهت | SHORT-only (ثابت) | خودِ فرضیه |

**فضا = ۲×۲×۲ = ۸ ⇒ `n_trials=8`.** انتخاب: بیشینهٔ (WR−50)·√n مشروط exp>0 ∧ n≥30
(همان قاعدهٔ S420؛ tie-break: ترکیبِ ساده‌تر a=1.0, hold=8). مدلِ صفر: جای‌گشتِ
زمانیِ SHORT با K=500 (همان سازوکارِ S420، سمتِ short). H7: split درون-پنجره‌ایِ ۶۰/۴۰.

## ۳. ریسکِ اعلام‌شده

SHORTِ بدونِ استاپ در دارایی‌ای با دمِ صعودیِ ۱۱۸× ذاتاً خطرناک است؛ اگر
پنجرهٔ کشف (که ابربازارِ صعودی است) این را تحمل نکند، کارت همان‌جا می‌میرد —
که خود یک نتیجهٔ علمیِ گزارش‌کردنی است (سلولِ شیدایی حتی برای SHORT هم لبه ندارد).

*پیش‌ثبت‌کننده: AI مأموریتِ ۳ · S420–S429 · RQS2 v2.6 · 2026-08-16*
