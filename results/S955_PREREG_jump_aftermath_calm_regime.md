# S955 — پیش‌ثبت: «جهشِ هم‌راستا در رژیمِ آرام» (S950 Jump-Aftermath × Calm-σ Gate)

**دانشمند:** Robert Merton (باند S950–S959) · **تاریخ:** 2026-09-09 · **پیش از هر آزمون ثبت شد.**

## ۰. بررسی پیش از طراحی (fetch + مرور ACCEPTهای جدید — فقط خواندن، هیچ فایلی از دیگران لمس نشد)
- **S1911** (کینز، H8 93.9): شوکِ Kyle × گیت آرامشِ σ (RiskMetrics λ=0.94، reg=σ_t/median(σ_{t−233..t−1}) ≤ 1). P1 گذشت.
- **S1740** (خیام، H2 79.8): بستهٔ قوی × رانش × همان گیتِ آرامش. P1 گذشت در ۳/۳ کارت.
- **S606** (Engle، D1/H8 80.1): شوکِ Engle × آرامش. **S1912**: گیتِ دوگانه (آرام AND رانش) روی Kyle ⇒ ACCEPT-REDUNDANT (P1 شکست: dual < calm-only).
- **S589** (H8/H4 88): سقفِ تازه × حجم. **S802** (D1 92): S800 × ρ.
- **قانونِ مخزن (۳ تکرارِ مستقل)**: «شوک × رژیمِ آرام» اطلاعات‌افزاست. هیچ‌کدام این گیت را روی **جهشِ بازده (return jump, S950)** نیازموده‌اند — رویدادِ S950 (r>2.6σ_BV) از رویدادِ Kyle (range≥2.618·ATR21 با ρ≥0.618) متمایز است: S950 «بازدهِ بسته‌به‌بسته» را می‌سنجد، Kyle «دامنهٔ درون‌کندلی».
- درسِ S954 (خودم): شاخصِ رژیمِ نرم باید **گیت** باشد نه ماشه. درسِ S1912: گیتِ دوگانه خطرِ REDUNDANT دارد ⇒ من **فقط یک گیت جدید** روی پایهٔ منجمد می‌گذارم.

## ۱. فرضیه (Merton 1976 jump-diffusion + Engle ARCH)
در رژیمِ آرام، جهشِ بازده «غافلگیرکننده» است ⇒ نسبتِ اطلاعات به نویزِ آن بالاتر و ادامهٔ پس‌ازجهش قوی‌تر (Kyle: در بازارِ آرام
معامله‌گرِ مطلع هویتش را کمتر پنهان می‌کند). در رژیمِ طوفانی، همان جهش بخشی از خوشهٔ ولاتیلیته است و ادامهٔ ضعیف‌تری دارد.

## ۲. قاعدهٔ منجمد (صفر پارامترِ آزاد — همه به ارث)
| جزء | مقدار | منبع |
|---|---|---|
| رویداد | `|r_t| > 2.6·σ_BV(89)`، σ_BV علّی (bv[t]=bv_full[t−2]) | S950 (ACCEPT 80) — عیناً `s950_jump_aftermath.features/member_signals` |
| هم‌راستایی | long فقط اگر `c[t−1] − c[t−90] > 0`؛ short آینه | S950 |
| **گیتِ جدید** | `σ²_t = 0.94σ²_{t−1} + 0.06 r²_{t−1}`؛ `reg_t = σ_t / median(σ_{t−W..t−1})`؛ ورود فقط اگر `reg_t ≤ 1` | S605/S606 — `sigma_series`, `regime_ratio` **import عیناً**، W=233 (عین S1911) |
| هندسه | SL=TP=2.058×ATR(89) (SMA-TR شیفت ۱)، max_hold=34، بدونِ همپوشانی | S950 |
| ورود/هزینه | open کندلِ بعد (موتور)، spread 3.3pip | استاندارد |

**فقط یک آزمونِ منجمد در هر کارت** (کارتِ اصلی H8 = کارتِ S950؛ H6 و H12 و D1 گزارشی). n_trials کلِ باند تا این‌جا برای این رویداد:
S950=33 + این لایه ۱ کارت × ۱ آزمون ⇒ **n_trials=34** به موتور داده می‌شود (شمارشِ تجمعیِ صادقانهٔ پایهٔ منجمد + آزمونِ حاضر).
null: جای‌گشتِ علامتِ کانونی (K=2000) — عینِ S950/S951-fixed. اسکنِ ۲۱ TF طبق قانونِ MTF انجام می‌شود (گزارشی؛ n_trials هر کارت همان 34).

## ۳. ابطال‌گرها (پیش از دیدنِ عدد)
- **P1 (گیت باید اطلاعات‌افزا باشد، نه توان‌سوز)**: در H8، `lift_calm > lift_S950_ungated (+11.6pp طبق S950)`. اگر lift_calm ≤ lift پایه ⇒ **REDUNDANT** حتی با ACCEPT موتور (درس S1912) ⇒ استقرار نمی‌شود.
- **P2 (بازوی مکمل)**: بازوی `reg_t > 1` (طوفانی) باید WR/lift کمتری از calm داشته باشد. اگر طوفانی برنده شد ⇒ روایت غلط.
- **P3 (pass-rate زنده)**: نسبتِ سیگنال‌های S950 که از گیت می‌گذرند باید ۳۰–۸۵٪ باشد (اگر <30٪ گیت عملاً لایه را می‌کشد).
- **P4 (همپوشانی)**: معاملاتِ S955 زیرمجموعهٔ S950 هستند به‌طورِ ساختاری ⇒ اگر ACCEPT شد، S955 **جانشینِ** S950 روی کارتِ H8 پیشنهاد می‌شود فقط اگر P1 گذشته باشد و RQS2 > 80؛ در غیر این صورت S950 می‌ماند و S955 «شاهدِ تکرار» است.

## ۴. پیش‌بینیِ مکتوب
H8: n≈110–150 (از ۲۲۴)، WR 64–68٪، lift +15…+20pp، z≈3.0–3.6 ⇒ ACCEPT مرزی یا PL. H6/H12/D1: پایهٔ S950 آن‌جا REJECT بود ⇒ گیت معجزه نمی‌کند (REJECT). میکرو: REJECT/NO-CANDIDATE.

## ۵. داده/اقتصاد
`data/mt5_full/` ۱۵.۶ سال، ۲۱ TF، spread 3.3pip، contract=100، $10k، ریسک ۱٪. هیچ پارامتر تیون نمی‌شود؛ اگر نتیجه بد بود همان گزارش می‌شود.
