# S585 — پیش‌ثبت: داوریِ استخریِ سیگنالِ فریزِ RVI(14) روی {H8, H12, D1}

**تاریخ:** 2026-08-26 · **دانشمند:** S58x (دههٔ S580–S589) · **دارایی:** XAUUSD
**نوع:** Path-C ادامهٔ زنجیره — داوریِ استخریِ تک‌باره، **صفر درجهٔ آزادی تازه**
**پیش‌نیاز:** این سند **قبل از هر محاسبه/آزمون** commit می‌شود.

---

## ۱. خاستگاه و زنجیرهٔ شواهد (بدون هیچ نگاه تازه به داده)

S584 (`results/S584_RviDirectionalVol_Xauusd_H6H8H12D1_rqs2_0_INCOMPLETE.md`)
روی **نیمهٔ اولِ** داده سه کارتِ هم‌جهتِ net-مثبت پیدا کرد که تک‌تک از
پیش‌شرط توان (قانون S782) رد شدند اما الگوی متاِ برنده‌های اخیر
(S770/S800/S602/S950: رویدادِ وضعیت-نوسان روی TFهای درشت، اغلب استخری)
را کامل بازتولید می‌کنند. §۵ همان سند این S585 را قفل کرد:

> «S585 = پیش‌ثبت استخر {H8, H12, D1} همین سیگنال فریز با هندسهٔ فریز
> rr=1.618 (بدون هیچ جست‌وجوی جدید — صفر درجهٔ آزادی تازه)، داوری استخری
> تک‌باره روی کل داده با null استخری؛ n_trials تجمعی صادقانه = 16+1=17.»

استعارهٔ گروتندیک: سه جزیرهٔ کم‌توان که از یک فلاتِ زیرآبی واحد
برخاسته‌اند — به‌جای بلندکردن هر جزیره، **دریا را بالا می‌بریم**: ادغامِ
تقویمی، n مؤثر را از {254, 170, 82} به یک نمونهٔ واحدِ سریالی می‌رساند.

## ۲. بررسیِ قلمرو (انجام‌شده قبل از این پیش‌ثبت)

- `git fetch` + rebase تا `531d349c` انجام شد؛ همهٔ کامیت‌های جدید مرور شدند.
- grep مرزِ کلمه‌ای `\brvi\b|rvi_vol`: فقط S346 (فهرست ابزار، دههٔ دیگر،
  probe عمومی — نه آزمونِ سیگنالِ cross) و S363 (فهرست خانوادهٔ حجم در
  addendum). **هیچ دهگانه‌ای cross هفتاد/سیِ RVI را آزموده یا claim نکرده.**
- درس‌های تازهٔ جذب‌شده: S525 (گیت توان می‌سوزاند ⇒ استخرِ بی‌گیت)،
  S517/S533 (الگوی استخر با subset رسمی و null ترکیبی)، S543/S962
  (اطلاعات در وضعیتِ نوسان است نه سطوحِ عمومی — سازگار با فرضیه).

## ۳. سیگنالِ فریز (عیناً S584 — بازنویسی ممنوع)

- `rvi = ib.rvi_vol(df, 14)` از `engine/indicator_bank.py` (فریز).
- LONG: عبورِ rvi به بالای 70.0 (prev≤70 → rvi>70).
- SHORT: عبورِ rvi به زیرِ 30.0 (prev≥30 → rvi<30).
- هر دو سمت با هم در یک فراخوانِ `se.simulate_trades` (دوسویه، بدون گزینش).
- **هیچ گیت، هیچ فیلتر، هیچ پارامترِ تازه.**

## ۴. اعضای استخر و هندسهٔ فریز (گاردِ BUG-GEOMDRIFT: از آرتیفکت)

منبعِ یکتای هندسه: `results/_s584_explore.json` (میانهٔ ATR نیمهٔ اول؛
هیچ عددی اینجا از نو محاسبه نمی‌شود، فقط **خوانده** می‌شود):

| کارت | sl_pip | tp_pip (rr=1.618) | mh | مبنا در آرتیفکت |
|------|--------|-------------------|----|------------------|
| XAUUSD-H8  | 118.79 | 192.20 | 21 | بهترین نقطهٔ H8 در grid منتشرشدهٔ S584 |
| XAUUSD-H12 | 151.09 | 244.46 | 34 | برندهٔ رسمیِ قفل‌شدهٔ S584 |
| XAUUSD-D1  | 215.61 | 348.86 | 34 | بهترین نقطهٔ D1 در grid منتشرشدهٔ S584 |

انتخابِ mh به‌ازای هر کارت از نقاطِ **از قبل منتشرشده در سند S584** است
(نه جست‌وجوی تازه)؛ تفاوتِ mh21/34 در H8 عملاً صفر بود (12.57 در برابر
12.37 pip) — این انتخاب هیچ قدرتِ گزینشی حمل نمی‌کند و در n_trials
تجمعی (§۷) از قبل حساب شده است.

اعتبارِ استخر (۴ شرطِ `engine/rqs2_pool.py`): (۱) هندسهٔ نسبیِ یکسان
1.272×ATR(21)، rr=1.618 ✓؛ (۲) هم‌جهتی: lift>0 هر عضو — در اجرا
سنجیده می‌شود؛ (۳) ادغامِ FIFO تقویمی (همزمانی=۱) توسط `pool_cards` ✓؛
(۴) همگنیِ قدرت: `choose_homogeneous_subset` رسمی ✓.

## ۵. پروتکلِ داوری (یک آزمونِ تأییدی، بدونِ استثنا)

1. **داده:** `fd.load_fast('XAUUSD', tf)` برای H8/H12/D1 — گاردِ
   BUG-DATASETDRIFT: assert بازه >12 سال + ثبتِ src/rows/span.
2. **اجرای کامل‌داده:** هر کارت روی **کل ۱۵.۶ سال** با SL/TP مطلقِ فریزِ
   جدولِ §۴ (همان مقادیری که روی نیمهٔ اول مدیان‌گیری شدند — عمداً مطلق
   نگه داشته می‌شوند تا هیچ بازمحاسبه‌ای رخ ندهد).
3. **نالِ هر کارت** (گارد BUG-NULLUNCOND — نال در فضای همان کارت): برای
   هر سمت جداگانه، K=500 جای‌گشت با همان تعدادِ سیگنالِ همان سمت،
   جایگذاریِ تصادفی روی اندیس‌های معتبر (warmup=250، انتهای mh کنار
   گذاشته)، `allow_overlap=False`؛ به‌علاوه uncond_wr هر سمت با نمونهٔ
   بزرگِ `allow_overlap=True` (الگوی s580). گاردِ BUG-PERMK: perm_k =
   اندازهٔ آرایهٔ جای‌گشت‌ها.
4. **liftِ عضو:** WRِ مشاهده‌شدهٔ کارت (دو سمت با هم) منهای میانگینِ
   وزنیِ perm_mean دو سمت (وزن = سهمِ تریدهای هر سمت). عضو با lift≤0
   توسط `pool_cards` حذف می‌شود (شرط هم‌جهتی).
5. **قاعدهٔ توقف:** اگر کمتر از ۲ عضوِ معتبر بماند ⇒ استخر تشکیل نمی‌شود؛
   حکمِ رسمی INCOMPLETE (rqs2=0، موتور فراخوانی نمی‌شود) و خانواده بسته.
6. **استخر:** `engine/rqs2_pool.pool_cards` رسمی (subset همگن + FIFO
   تقویمی). قید C5 (سابقهٔ S431/S540): سهمِ هیچ عضوی پس از FIFO > 50٪
   نباشد؛ نقض ⇒ توقف بدون دستکاری.
7. **محورِ مشترک:** گریدِ ۱ساعته (ریزتر از ریزترین عضو H8)؛ close محور
   از XAUUSD-H1 کامل (fd). الگوی عینی S540 (ضد BUG-AXIS/QUANT/SPAN).
8. **نالِ استخر:** ترکیبِ وزنی نال‌های اعضا با سهمِ پس-از-FIFO — تابعِ
   `blend_pool_null` عیناً از `tools/s540_pool_adjudicate.py`.
9. **هندسهٔ استخر برای موتور:** میانگینِ وزنیِ sl/tp اعضا به سهمِ پس-از-FIFO
   (الگوی S540).
10. **تقسیم:** hold-out = ۴۰٪ انتهایی به **صدکِ ۶۰٪ زمانِ ورودِ معاملات**
    (گاردِ BUG-SPLITDIR، سابقهٔ S770/S517/S540) — `holdout_mask` به موتور.
11. **حکم:** فقط و فقط یک فراخوانِ `engine/rqs2.compute_rqs2` با
    `n_trials=17`؛ حکم و امتیاز عیناً از موتور. هیچ حکمِ دستی.

## ۶. ثابت‌ها

- SEED = 20260826 · K_PERM = 500/سمت/کارت (درس S435: ≥500)
- N_TRIALS = **17** (۱۶ نقطهٔ اکتشاف S584 + ۱ آزمونِ استخریِ حاضر)
- SPLIT_FRAC = 0.60 · C5_MAX_SHARE = 0.50 · STEP = 1h
- گارد n≥30 به‌ازای هر کارت (کمتر ⇒ همان کارت MEASUREMENT-LIMITED و حذف).

## ۷. حسابداریِ صداقت و ابطال‌پذیری

- این **تنها** آزمونِ تأییدیِ خانوادهٔ RVI-cross است؛ نتیجه هرچه باشد
  خانواده بسته می‌شود (ACCEPT ⇒ توقف و گزارش به کاربر؛ غیر از آن ⇒ مرگ/
  INCOMPLETE ثبت و عبور به S586).
- پیش‌بینیِ ابطال‌پذیر P1: استخرِ FIFO حداقل ۲ عضوِ هم‌جهت نگه می‌دارد.
- P2: اگر پدیده واقعی است، z استخری باید از zهای تکی (که تک‌تک زیر سد
  بودند) بالاتر برود؛ اگر z استخری ≤ بیشینهٔ z تکی، فرضیهٔ «فلاتِ واحد»
  ابطال شده — صادقانه ثبت می‌شود.
- prior شخصی: ~۲۵٪ موفقیت (استخرهای مشابه: S770 ✓، S602 ✓، S520 ✗، S533 ?).

## ۸. پرهیز از ۸ خطای رایج (تعهد پیشینی)

1. **Look-ahead:** سیگنال فقط از closeهای بستهٔ گذشته؛ cross با prev=roll(1).
2. **انتخابِ پسینی:** اعضا و هندسه پیش از هر اجرا در این سند قفل شدند.
3. **نشتِ هندسه:** SL/TP از آرتیفکتِ منتشرشدهٔ S584 خوانده می‌شود.
4. **چندگانگی:** n_trials=17 تجمعیِ صادقانه؛ یک آزمونِ تأییدی.
5. **نالِ نادرست:** نالِ اندازه‌گیری‌شده در فضای هر کارت + ترکیبِ وزنی.
6. **جهت‌گیریِ split:** صدکِ زمانِ ورود، نه تقویمِ محورِ دلخواه.
7. **دیتاستِ اشتباه:** assert بازه>12y + ثبتِ provenance.
8. **حکمِ دستی:** حکم فقط از `compute_rqs2`.

*— دههٔ S580. دریا بالا می‌آید؛ یا صخره غرق می‌شود یا نقشه صادقانه‌تر می‌شود.*
