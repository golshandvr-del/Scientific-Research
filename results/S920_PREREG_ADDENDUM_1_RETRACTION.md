# S920 — الحاقیهٔ ۱: عقب‌نشینیِ پیش‌ثبتِ «نظمِ خودجوش» (آنتروپی-سقوط)

> **تاریخ:** 2026-08-14 · الگوی رویه: `results/S870_PREREG_ADDENDUM_1_RETRACTION.md`

## چه شد

پس از commitِ پیش‌ثبتِ S920 (`570f91db`)، در همگام‌سازی با `origin/main` کشف شد
که یک نشستِ موازی قبلاً **همین مفهوم** را پیش‌ثبت و آغاز کرده است:

- `results/S880_PREREG_EntropyCollapse_PathC.md` (commit `874f0a59`) —
  «فروریزشِ آنتروپی → تولدِ ساختار»: همان ماشهٔ سقوطِ آنتروپیِ شانونِ بازده
  + جهت از علامتِ رانش، همان مسیرِ C، همان دارایی.
- چک‌پوینتِ D1 آن روی نیمهٔ اول اجرا شده (`results/_scan_S880/XAUUSD_D1.json`).

پیش‌ثبتِ من مستقل نوشته شد ولی از نظرِ مفهومی **مضاعف** است. دو پیش‌ثبتِ
موازی روی یک مفهوم = تورمِ پنهانِ چندگانگی (همان چیزی که H5 برای بستنش
ساخته شده).

## تصمیم

- پیش‌ثبتِ S920 نسخهٔ ۱ (آنتروپی-سقوط) **پس گرفته می‌شود**؛ مالکیتِ مفهوم با S880 می‌ماند.
- **صفر آزمونِ جهت‌دار** برای S920v1 اجرا شد — فقط شمارشِ نرخِ آتشِ رویداد روی
  نیمهٔ اول (بدونِ هیچ اطلاعی از نتیجهٔ معاملات). پس هیچ نشتِ داده‌ای رخ نداده
  و بودجهٔ hold-out دست‌نخورده به S880 می‌رسد. (راستی‌آزماییِ برداری‌سازیِ
  `entropy` علیه بانک نیز غیرجهت‌دار بود و به نفعِ S880 باقی می‌ماند.)
- S920 با **مفهومِ نو و بکر** در پیش‌ثبتِ نسخهٔ ۲ ادامه می‌یابد:
  «تصحیحِ خودجوش» — CRSI افراطی در رژیمِ ضدپایای هرست (grep کامل:
  `crsi` در هیچ لایه‌ای از تاریخِ پروژه استفاده نشده است).

— فریدریش هایک · 2026-08-14
