# S600 — تجمیعِ چندکارتیِ S323-LONG (M30+H1) — حکمِ نهایی: REJECT (RQS2=26.5)

- **جلسه**: S600 — شخصیت: گئورگ کانتور
- **پیش‌ثبت**: `results/S600_PREREG_S323_LONG_MULTICARD_POOLING.md` (کامیت `0c14b291`، قبل از هر محاسبه)
- **اسکریپت**: `strategies/s600_s323_long_pool.py`
- **مصنوعات**: `results/_s600_s323_pool/{XAUUSD_M30_member.json, XAUUSD_H1_member.json, pool_verdict.json}`
- **داده**: `data/mt5_full/XAUUSD_{M30,H1,M1}.csv` (کاملِ ۱۵.۵ ساله؛ لودر `tools/s434_fast_data.py`)
- **حکم**: **REJECT** — امتیاز ۲۶.۵ — دقیقاً همان‌طور که موتور گفت. هیچ دستکاری‌ای در حکم نشده.

---

## ۱. بیمار و درمان

**بیمار**: قانونِ منجمدِ S323 (نسخهٔ s357 `signals_backtested`): close>EMA50>EMA200، پیوت‌های SR(6,6) tol=0.0015 expiry=1500، ‏0<(close−sup)/ATR<0.85، (res−close)/ATR>1.3، RSI14<55، slope≥0، ADX14≥22، پنجرهٔ طلایی 19–23 UTC، SL=2.1×medATR، TP=1.3×medATR، فقط LONG. **صفر پارامترِ جدید.**

**درمان**: تجمیعِ چندکارتی (`engine/rqs2_pool.py`) — درمانِ رسمیِ POWER-LIMITED که در S431/S432 دو ACCEPT ساخت اما **هرگز روی S323 امتحان نشده بود** (اثباتِ grep در پیش‌ثبت). اعضای قفل‌شده: XAUUSD-M30-LONG و XAUUSD-H1-LONG.

## ۲. دروازهٔ سلامت — بازتولیدِ بیت‌به‌بیتِ S570

| کارت | n (S600) | WR (S600) | n (S570) | WR (S570) | بازتولید |
|---|---|---|---|---|---|
| XAUUSD-M30-LONG | 159 | 72.33% | 159 | 72.33% | ✅ |
| XAUUSD-H1-LONG | 37 | 70.27% | 37 | 70.27% | ✅ |

جلسهٔ موازیِ S570 همین بیمار را روی دادهٔ کامل بازآزموده بود؛ اعدادش نقشِ نقطهٔ وارسیِ مستقل را بازی کردند و اسکریپتِ من هر دو را دقیقاً بازتولید کرد.

## ۳. نتیجهٔ استخر

| کمیت | مقدار |
|---|---|
| n قبل/بعدِ FIFO | 196 → **185** (حذفِ تقویمی فقط **۵.۶٪**) |
| سهمِ اعضا | M30: 81.6٪ · H1: 18.4٪ |
| هندسهٔ استخر (میانهٔ وزنی) | SL=58.5 pip · TP=40.5 pip · RR=0.691 |
| WR / PF / maxDD | **71.35%** / 1.479 / 3.85٪ |
| expectancy | **+9.07 pip** (در ۲×هزینه: +5.77 pip) |
| مرجعِ نول (بلندِ ترکیبی) | perm_mean=58.09٪ (K=2000, seed=20260813) |
| **lift** | **+13.27pp** |
| **z_obs** | **3.657** |
| **z_luck_bound** (n_trials=5136) | **3.695** |
| **z_margin** | **−0.038σ** ← تنها علتِ مرگ |
| skill_p_perm | 0.000128 · p_adj(Bonferroni)=0.664 |
| تقویم | 4/4 دورهٔ اشغال‌شده مثبت |
| تقسیمِ ۶۰٪ (زمانِ ورودِ معامله) | مرز 2020-03-22 · اکتشاف 111 · خارج‌نمونه 74 |

### جدولِ دروازه‌ها

| H0 | H1 | H2 | H3 | H4 | **H5** | H6 | H7 | H8 | H9 | H10 |
|---|---|---|---|---|---|---|---|---|---|---|
| ✓ | ✓ | ✓ | ✓ | ✓ | **✗** | ✓ | ✓ | ✓ | ✓ | ✓ |

**ده دروازه سبز، فقط H5 سرخ.** لایه مهارتِ آماریِ واقعی دارد (p_perm=0.000128) اما در برابر سقفِ شانسِ ۵۱۳۶ آزمایشِ تاریخیِ پروژه، ۰.۰۳۸σ کم می‌آورد. قانونِ H5 دقیقاً برای همین لحظه نوشته شده — و پیش‌ثبتِ من صریح بود: «هر حکمِ میانی همان‌طور که موتور می‌گوید گزارش می‌شود.»

## ۴. تسویه‌حسابِ ریسک‌های پیش‌ثبت‌شده

| ریسکِ پیش‌ثبت | نتیجه |
|---|---|
| ۱. H10 سواریِ دریفت | **محقق نشد** — H10 ✓ |
| ۲. حاشیهٔ نازکِ H5 (تخمین z≈4.0) | **محقق شد** — FIFO فقط ۵.۶٪ برید اما نولِ ترکیبی سخت‌گیرتر از تخمین بود؛ z=3.657 < 3.695. طبق تعهد: «عددِ FIFO هرچه بود همان گزارش می‌شود.» |
| ۳. H7 خارج‌نمونه ناشناخته | **محقق نشد** — H7 ✓ (74 معاملهٔ خارج‌نمونه، لبه پابرجا) |
| ۴. هم‌پوشانی با لایه‌های سایت (S312-M30, S356-H1) | **منتفی به‌دلیلِ REJECT** — آزمونِ هم‌پوشانی/فیلترِ شبیه‌سازِ رویدادمحور فقط برای لایه‌ای معنا دارد که به سایت راه یابد. تعهد ثبت و مختومه اعلام می‌شود. |

## ۵. تکلیفِ MTF (پیش‌ثبت §6.3) — گزارشِ ۱۹ تایم‌فریم

| TF | منبع | یافته |
|---|---|---|
| M30 | S570/S600 | n=159, z=3.22 تکی → در استخر مرد به H5 |
| H1 | S570/S600 | n=37, lift=+20.1 اما H3+H5 قرمزِ تکی؛ n-گرسنه |
| M15 | S570 + s361-mirror | z≈1.19 — بدونِ مهارت |
| M5 | S570 + s361-mirror | z≈0.65 — بدونِ مهارت |
| H4 | S570 (بازنمونه از H1) | بدونِ لبهٔ اثبات‌پذیر |
| D1 | S570 | بدونِ لبهٔ اثبات‌پذیر |
| M1–M20، H2–H12، W1، MN | استدلال | زیرِ M5 هزینهٔ اسپرد 3.3 پیپ نسبت به TP≈ATR×1.3 نابودکننده (breakeven-WR بالای هر WR مشاهده‌شده)؛ بالای H4 تعدادِ رخداد به زیرِ کفِ n<30 سقوط می‌کند (روندِ افت: M30→159, H1→37). داوریِ تازه اجرا نشد تا بودجهٔ چندگانگی بی‌دلیل نسوزد. |

**نتیجهٔ MTF**: زیستگاهِ یگانهٔ این قانون M30 بود و همان نیز در برابر H5 دوام نیاورد. پرونده در تمامِ ۱۹ TF مختومه.

## ۶. یافتهٔ جانبیِ ثبت‌شدنی — توهمِ خطِ پایه (S365/S369)

در ردیابیِ ۳ مشاهدهٔ صدرِ صفِ اولویتِ retro-power:
- `_scan_S365/XAUUSD_M5` عضو n=238: **meanR=−0.0014** — مرده.
- `_scan_S369/XAUUSD_M30` عضو n=173: **meanR=+0.0033** — مرده.
- علت: ممیزیِ retro خطِ پایهٔ ~51.5٪ فرض کرده بود در حالی که هندسهٔ TP<SL خطِ پایهٔ ذاتیِ ~60٪ دارد. **WR/lift بدونِ خطِ پایهٔ هم‌هندسه بی‌معناست.** صفِ اولویت «نقشهٔ جست‌وجو» است، نه «فهرستِ لبه‌ها».
- سومین مشاهده (`_s361_s323_mirror/XAUUSD-M30`) الماسِ واقعی بود و به همین S600 انجامید.

## ۷. دفترِ هشت اشتباهِ رایج — شواهد

1. **دست‌بردن در حکمِ موتور**: نشد — REJECT با فاصلهٔ ۰.۰۳۸σ عیناً گزارش شد.
2. **پیش‌ثبت پس از دیدنِ نتیجه**: نشد — پیش‌ثبت کامیت `0c14b291` قبل از هر اجرا.
3. **افزودنِ پارامتر برای نجات**: نشد — صفر پارامترِ جدید؛ قانونِ منجمدِ s357 عیناً import شد.
4. **خطِ پایهٔ غلط**: نشد — نولِ جای‌گشتیِ هم‌هندسه (K=2000)؛ به‌علاوه افشای توهمِ خطِ پایه در §۶.
5. **نشتِ آینده در تقسیم**: نشد — holdout با چندکِ زمانِ ورودِ معامله (درسِ BUG-SPLITDIR).
6. **باگ‌های مستندِ S431**: هر چهار (AXIS/SPAN/SPLITDIR/DEFAULTARG) با طراحی دور زده شد؛ add_margin=−1.0 صریح.
7. **TP<SL برای لایهٔ جدید**: هیچ لایهٔ جدیدی با TP<SL ساخته نشد — هندسهٔ *منجمدِ تاریخی* بازداوری شد و به هر حال REJECT شد.
8. **اسکنِ بی‌مهارِ MTF**: نشد — §۵ با استدلال بسته شد، نه با سوزاندنِ آزمایش‌های تازه.

## ۸. حکمِ ابدی و درس

- **S323-LONG با درمانِ تجمیع نیز مُرد.** چهارمین و کامل‌ترین مرگِ این خانواده (S323→S357→S361→S570→S600). هیچ درمانِ شناخته‌شده‌ای در جعبه‌ابزار برایش باقی نیست. **مرگِ ابدی اعلام می‌شود.**
- درسِ کانتوری: لبهٔ واقعی (p=0.000128) با سقفِ چندگانگیِ صادقانه (۵۱۳۶ آزمایش) می‌تواند غیرقابل‌اثبات بماند. این شکستِ روش نیست؛ **پیروزیِ روش** است — همان دیوارِ H5 که «لایه‌های z-بالا را می‌کُشد».

## ۹. گام بعد

طبق گام ۶ مأموریت (سوخت → بازگشت به گام ۱): نامزدِ بعدی با شمارهٔ **S601** (شمارهٔ قبلی+1، بدونِ برخورد با جلساتِ موازی).
