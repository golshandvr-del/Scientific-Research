# S760 — الحاقیهٔ انجماد (پیش از هر آزمونِ نهایی روی نیمهٔ hold-out)

> **تاریخ:** 2026-08-13 · مُهرِ زمانیِ git = گواه. تا این لحظه **هیچ معامله‌ای از
> نیمهٔ دوم (hold-out) هیچ کارتی شبیه‌سازی یا دیده نشده است** — فازِ جستجو فقط
> روی `[0, 0.60·n)` اجرا شد (`results/_scan_S760/*_search.json`).

## ۱) نتیجهٔ فازِ جستجو (خلاصه)

* M1..M6 (شاملِ M2 بازنمونه‌گیری‌شده از M1): لیفت ≤ 0 یا exp منفی ⇒ **بدونِ نامزد**
  (پیش‌بینیِ قانونِ S383 در پیش‌ثبت درست بود).
* M10..M30: لیفتِ کوچکِ مثبت ولی exp منفی ⇒ هزینه‌خورده؛ نامزدِ قابلِ دفاع ندارند.
* W1/MN1/D1: n<30 ⇒ بدونِ نامزد (کم‌رویدادی، طبقِ انتظار).
* **لبه در کارت‌های ساعتی است** — هم‌خوان با لایهٔ ACCEPTِ H4ِ S382 که نشان داد
  «ساعتی» ناحیهٔ طبیعیِ لبهٔ اندیکاتوریِ طلاست.

## ۲) پیکربندی‌های منجمد برای آزمونِ یگانهٔ hold-out (per-card)

آستانه‌ها **عدداً** منجمدند (از توزیعِ نیمهٔ جستجو) — روی hold-out بازمحاسبه
نمی‌شوند (ضدِ نشتی). هندسه SL=k×ATR(100) و TP=RR×SL (TP≥SL همیشه). `mh`=max_hold.

| کارت | سمت | TSI(long,short) | آستانهٔ منجمد | k | RR | mh | جستجو: n / lift(pp) / exp(pip) |
|---|---|---|---|---|---|---|---|
| **H8** | SHORT | (25,13) | گذر به زیرِ **−21.729** | 1.0 | 1.0 | 64 | 71 / **+17.98** / +31.6 |
| **H12** | SHORT | (34,13) | گذر به زیرِ **−19.536** | 1.0 | 1.0 | 64 | 45 / +13.28 / +32.0 |
| **H6** | LONG | (55,21) | گذر به بالای **+17.282** | 1.0 | 1.0 | 64 | 52 / +14.18 / +11.4 |
| **H4** | SHORT | (55,21) | گذر به زیرِ **−15.838** | 1.5 | 1.5 | 64 | 77 / +13.29 / +36.7 |
| **H3** | LONG | (34,13) | گذر به بالای **+22.389** | 1.5 | 1.0 | 64 | 164 / +9.67 / +15.9 |
| **H2** | LONG | (55,21) | گذر به بالای **+17.939** | 1.0 | 1.0 | 64 | 147 / +10.12 / +7.4 |
| **H1** | LONG | (55,21) | گذر به بالای **+27.938** | 1.5 | 1.0 | 64 | 137 / +9.39 / +6.5 |

**اولویتِ داوری: H8-SHORT** (بیشترین score=lift·√n=151.5). بقیهٔ کارت‌ها نیز هر
یک آزمونِ یگانهٔ خود را دارند (پیش‌ثبت: داوریِ per-TF طبقِ قانونِ MTF).

## ۳) پروتکلِ اجرا (منجمد)

1. سیگنال‌ها با پیکربندیِ منجمد روی **کلِ** سری محاسبه؛ شبیه‌سازی روی کلِ داده
   (اسکلتِ رسمیِ GUIDE) و `split_bar = int(0.60·n)` به `compute_rqs2` داده
   می‌شود تا H7 روی همین مرز داوری کند.
2. null با الگوی `build_null_side` (s351): مبنای بی‌قیدِ هم‌هندسه + جای‌گشتِ
   **K=2000** زیرمجموعه‌های هم‌اندازه از کندل‌های واجد (finite ATR، پس از warmup)،
   با همان هندسه/mh/allow_overlap=False. `SEED=20260812`.
3. `n_trials`: طبقِ تعهدِ پیش‌ثبت **هر دو سد** گزارش می‌شود — N=1 (تعریفِ مسیرِ C،
   خانوادهٔ منجمدِ این الحاقیه) و N=96 (کلِ خانوادهٔ جستجوی per-TF). اگر حکم
   بینِ دو سد عوض شود، **سخت‌گیرانه‌تر** ملاک است.
4. ورودی‌های الزامیِ `compute_rqs2`: `sl_pip, tp_pip, bar_time, null, n_trials,
   split_bar, close` — همه داده می‌شوند؛ INCOMPLETE ممنوع.
5. حکمِ موتور عیناً گزارش می‌شود؛ هر کارت فقط **یک بار** داوری می‌شود؛ شکست =
   مرگِ فرضیه روی آن کارت.
6. پس از داوری: ممیزیِ هم‌پوشانی با لایه‌های مستقر (به‌ویژه S382-H4-LONG که
   هم‌TF با H4-SHORTِ ماست — سمتِ مقابل، ولی اندازه‌گیری الزامی است) با
   `engine/trade_simulator.py`؛ بخشِ مشترک بی‌درنگ به‌عنوانِ فیلتر آزموده می‌شود.
