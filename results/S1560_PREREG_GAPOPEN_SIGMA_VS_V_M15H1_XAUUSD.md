# S1560 — پیش‌ثبت (PREREG) — گپِ منفی × آرامشِ σ در برابر V روی M15/H1 (آزمونِ قانونِ «مقیاسِ آرامش») — XAUUSD

**دانشمند:** لایب‌نیتس — بلوک دوم S1560–S1569 (شمارهٔ ۱ از ۱۰)
**تاریخ قفل:** 2026-09-09 — این سند **قبل از هر محاسبه‌ای** کامیت می‌شود؛ مُهر git = پیش‌ثبت.
**دارایی:** فقط XAUUSD. **داده:** `data/mt5_full/XAUUSD_{M15,H1}.csv` (۱۵.۶ سال؛ `assert 'mt5_full' in src`).
**داور:** فقط `engine/rqs2.compute_rqs2` v2.6 — حکم فقط از موتور (یا مرگ صادقانه در دروازه = REJECT 0).
**SEED = 20260909** · **فقط اکتشاف — هیچ استقرار/تغییری در سایت، هرگز (فرمان کاربر).**

---

## ۰. 🔴 بدترین خبر، اول

1. **این هم پالایشِ S560/S562 است، نه آلفای مستقل** — و به‌صراحت چنین برچسب می‌خورد. ارزشش در *داوریِ یک قانون* است، نه در یک لایهٔ تازه.
2. **خطر تکرارِ اشتباهِ بلوک اول («یک رویداد، ده شماره»):** این تنها شمارهٔ بلوک دوم است که به خانوادهٔ گپ اختصاص می‌یابد. S1561+ باید رویدادِ تازه باشد. این تعهد اینجا ثبت می‌شود.
3. **پیش‌بینیِ رقیبِ سخت:** قانون S569 («مقیاس آرامش = مقیاس ورود») می‌گوید روی M15/H1 گیتِ برنده **V** (روزانه) باید بماند و σ کوتاه **بی‌اثر یا ضعیف‌تر** باشد. اگر σ برنده شد ⇒ قانون S569 غلط بود و درست‌تر این است: «σ خود-مقیاس (W=233 کندل) در همهٔ TFها کار می‌کند». هر دو خروجی ارزش علمی دارند؛ پیش‌بینی صریح: **V برنده می‌ماند** (prior ۶۰٪).
4. **خطر توان (S525):** DUAL روی H1 با n_base=407 ممکن است n<100 بدهد؛ ممیزی مکمل الزامی.

## ۱. پرسش علمی (یک پرسش، دو پاسخ ممکن)

S569 روی M1/M5 نشان داد: σ-CALM (RiskMetrics λ=0.94، `σ_i ≤ median(σ_{i−233..i−1})`) > V78 و دو گیت متعامد (هم‌خطی 0.96–0.98). چهار ACCEPT موازی (S606/S607/S1521/S1911/S1913/S955) همان گیت را روی H6–H12 معتبر می‌دانند. **حلقهٔ مفقوده M15/H1 است** — همان‌جا که V در S562 برنده شد.

- **H_scale (قانون S569):** روی M15/H1، V ≥ σ؛ DUAL ≈ V (σ چیزی نمی‌افزاید).
- **H_self (رقیب):** σ با W=233 کندلِ همان TF خود-مقیاس است و در هر TF ≥ V؛ DUAL > هر دو.

## ۲. تعریف منجمد

- **پایه:** ماسک S560 عیناً `tools.s560_adjudicate.build(tf)` (M15: 603 سیگنال، H1: 407 — assert BUG-DATASETDRIFT).
- **هندسه:** عیناً `results/_s560_arms/locked_config.json` V-TIME (M15: SL=TP=50.9، mh=1؛ H1: 101.4، mh=2) — بازتنظیم نمی‌شود.
- **گیت V:** `vol_filter_mask(d, tf, mask, qv)` با **qv قفل‌شدهٔ S562 همان TF** (M15: 85، H1: 78) — نه جست‌وجو؛ بازوی V دقیقاً بازتولید S562 است و *دوباره داوری نمی‌شود* (حکمش موجود است: 95.3/96.0)؛ فقط به‌عنوان مرجع در LOCK محاسبه می‌شود.
- **گیت σ:** `sigma_calm_flags` عیناً از `tools/s569_gap_calm_fine.py` (import، بدون بازنویسی): σ از `s840.ewma_z` روی close همان TF؛ calm روی کندل سیگنال.
- **گیت DUAL:** V ∧ σ.
- **بازوهای داوری‌شونده:** TF ∈ {M15, H1} × {SIGMA, DUAL} = ۴ بازو. **n_trials تجمعی خانوادهٔ گپ: 439 → 443.**

## ۳. پروتکل Path C (عین S569)

1. **LOCK** نیمهٔ اول (split 2018-10-20): برای هر TF، جدول BASE/V(مرجع)/SIGMA/DUAL: نرخ عبور، هم‌خطی V×σ، t_fh، n_fh، WR_fh، DD_fh. برنده = بیشینهٔ t_fh بین {SIGMA, DUAL} با n_fh≥30 و نرخ عبور ≤۹۰٪. **پاسخ H_scale/H_self همین‌جا با t_fh نیمهٔ اول ثبت می‌شود** (پیش از هر تماس با هولدآوت).
2. **STOP-GATE:** n_fh<30 ⇒ NO-VERDICT؛ lift_fh < +4pp ⇒ STOPPED_DEAD.
3. **JUDGE:** یک تماس با کل داده؛ `null_for` (50k + K=500)؛ n_trials=443؛ حکم عیناً از موتور.
4. **ممیزی مکمل (الزامی، هر حکمی):** {نگه‌داشته، حذف‌شده، پایه S560، مرجع S562} با n/WR/mean/DD — گیت *چه* حذف کرد؟

## ۴. پیش‌بینی‌های شخصی (قبل از هر عدد)

- **P1 (H_scale):** روی هر دو TF، t_fh(V) ≥ t_fh(SIGMA). prior ۶۰٪.
- **P2 (تعامد):** هم‌خطی V×σ در 0.90–1.10 در هر دو TF (مثل M1/M5).
- **P3 (DUAL):** DUAL روی H1 توان می‌سوزد (n_fh < 60) و DD موتور بهتر از S562-H1 (2.07٪) نمی‌شود؛ روی M15 DUAL ممکن است DD را زیر 3.71٪ S562 بیاورد با lift ≥ +35.
- **P4 (نرخ عبور σ):** ۵۵–۷۰٪ (کمتر از M1 78٪، چون در TF درشت‌تر W=233 کندل ≈ ۱۰–۶۰ روز تاریخچه است).
- **حکم:** prior ACCEPT بازوی برنده ~۷۰٪ (پایه ACCEPT است). ارزش لایه = پاسخ P1، نه حکم.

## ۵. ابطال‌گرها (منجمد)

- **F1:** t_fh(SIGMA) > t_fh(V) در **هر دو** TF ⇒ H_scale ابطال، H_self تأیید ⇒ قانون S569 بازنویسی می‌شود.
- **F2:** نرخ عبور > ۹۰٪ ⇒ گیت مرده.
- **F3:** دروازهٔ توقف بدون استثنا.
- **F4:** اگر ACCEPT ولی DD بدتر از S562 همان TF و مکملِ حذف‌شده WR > ۶۰٪ ⇒ «توان‌سوز» برچسب می‌خورد و S562 لایهٔ اصلی می‌ماند.

## ۶. تمایز از همسایه‌ها

- ≠ S562 (همان پایه، گیت V — مرجع، دوباره داوری نمی‌شود) · ≠ S569 (همان پرسش روی M1/M5) · ≠ S404/S408 (گپ‌فیل، بلوک S400، خروج متفاوت).
- گیت σ فقط import می‌شود (`s569_gap_calm_fine.sigma_calm_flags` ← `s840.ewma_z`)؛ هیچ فایلی خارج از `results/S1560_*`, `results/_s1560_arms/`, `tools/s1560_*` لمس نمی‌شود.

**این سند پیش از هر محاسبه commit می‌شود. پس از commit، هیچ بندی تغییر نمی‌کند.**
