# S436 — بندِ سومِ قانونِ همپوشانی: تحلیلِ فیلتر

**گام ۱۲۲** · کارت: `XAUUSD-M5` · لایهٔ زنده: `S355` · نامزد: `S214/B0`
`n_trials=1930` · `n_perm=500` · سدِ z ≈ **۳.۸۹**

---

## ۱) چرا این آزمون اجرا شد

قانونِ همپوشانیِ پروژه صریح است: هر جا لایهٔ نو با لایه‌های موجود همپوشانی
داشته باشد، **باید همان‌جا** — نه در مرحله‌ای بعد — امکانِ استفاده از بخشِ
همپوشان به‌عنوانِ فیلترِ تأیید بررسی شود.

اندازه‌گیریِ همپوشانی (گام ۱۲۱):

| کمیت | مقدار |
|---|---|
| روزهای فعالِ `S355` | ۴۵ |
| روزهای فعالِ نامزد (`B0`) | ۴۲ |
| روزهای **مشترک** | **۵** (۱۱.۱٪ از `S355`) |

> ⚠️ **تصحیحِ پیش‌ثبت.** در گام ۱۱۸ نوشتم پوشش ۴۲.۲٪ است. آن رقم متعلق به
> بازوی `A1` (بدونِ قابِ تقویمی، ۲۴۰ روز) بود که در گام ۱۱۲ سنجیده شد.
> آزمونِ فیلتر روی `B0` اجرا شد — تنها بازو با لیفتِ مثبت — و پوششِ `B0`
> برابرِ ۱۱.۱٪ است. هر دو عدد درست‌اند ولی به دو پرسشِ متفاوت پاسخ می‌دهند.
> این ناسازگاری **قبل از** خواندنِ حکم ثبت شد، نه بعد از آن.

---

## ۲) نتایجِ خام

| نسخه | n | برنده | WR | انتظار (pip) | PF | خالص | maxDD | لیفت | z | RQS2 |
|---|---|---|---|---|---|---|---|---|---|---|
| `BASE` (S355 خام) | ۴۷ | ۳۴ | ۷۲.۳۴٪ | ۵۶.۸۱ | ۳.۹۵۱ | \$۲۴۶۹.۰ | ۱.۹۸٪ | +۲۰.۲۵ | ۲.۷۸ | **۲۶.۰** |
| `CONFIRM` | ۵ | — | — | — | — | — | — | — | — | ⛔ n<30 |
| `VETO` | ۴۲ | ۳۰ | ۷۱.۴۳٪ | ۵۵.۴۰ | ۴.۱۳۸ | \$۲۱۳۳.۲ | ۱.۸۳٪ | +۱۹.۶۱ | ۲.۵۴ | ۲۵.۳ |

دروازه‌های شکست‌خورده در هر سه نسخه یکسان: `H3`, `H5`, `H7` · هیچ دروازهٔ نامعلومی نیست.

**گاردِ افراز پاس شد:** ۶۰ میلهٔ سیگنال = ۹ (`CONFIRM`) + ۵۱ (`VETO`).

---

## ۳) داوریِ سه‌شرطیِ پیش‌ثبت‌شده

معیار در گام ۱۱۸ **قبل از اجرا** داخلِ ابزار نوشته شد و هر سه شرط لازم است:

| شرط | `CONFIRM` | `VETO` |
|---|---|---|
| ۱ — RQS2 بالاتر از پایه | — | ❌ ۲۵.۳ < ۲۶.۰ |
| ۲ — n ≥ ۳۰ | ❌ n=۵ | ✅ n=۴۲ |
| ۳ — لیفت بالاتر از پایه | — | ❌ ۱۹.۶۱ < ۲۰.۲۵ |

**حکم: هیچ‌یک از دو جهت، فیلترِ مفید نیست.**

---

## ۴) 🎓 یافتهٔ اصلی: علامتِ فیلتر برعکسِ فرضیه بود

`VETO` دقیقاً ۵ معامله حذف کرد. ترکیبِ آن‌ها تشخیصی است:

```
حذف‌شده: ۵ معامله = ۴ برنده + ۱ بازنده
WR بخشِ حذف‌شده  = ۸۰.۰٪    (کلِ لایه: ۷۲.۳۴٪)
انتظارِ بخشِ حذف‌شده = $۶۷.۲/معامله  (کلِ لایه: $۵۲.۵/معامله)
```

فرضیهٔ `VETO` این بود که روزهای نامزد رژیمی‌اند که `S355` در آن بد عمل
می‌کند. **عکسش درست است:** روزهای مشترک بهترین روزهای `S355` بودند — هم
نرخِ بردِ بالاتر، هم انتظارِ ۲۸٪ بیشتر.

⇒ پس جهتِ `CONFIRM` از نظرِ **علامت** درست است. ولی ۵ معامله در ۱۵.۵ سال
تولید می‌کند و قیدِ ۲ آن را — به‌درستی — از داوری محروم می‌کند.

**تمایزِ عمدی و مهم:** این «فیلتر بی‌فایده است» نیست؛ این
«جهتِ سودمند وجود دارد ولی توانِ آماری‌اش صفر است» است. همان تفکیکی که
در گام ۱۱۷ بینِ *MEASUREMENT-LIMITED* و *ابطال‌شده* گذاشتم.

---

## ۵) ⚠️ دامِ PF: چرا «بهبود» نبود

`VETO` نسبتِ سود را از ۳.۹۵۱ به ۴.۱۳۸ **بالا برد** و دراوداون را از ۱.۹۸٪
به ۱.۸۳٪ **پایین آورد**. اگر تنها همین دو عدد گزارش می‌شد، «بهبود» خوانده
می‌شد.

ولی در همان حال سودِ خالص ۳۳۵.۸ دلار **کم** شد و لیفت و z هر دو افت کردند.
مکانیزم: چهار برندهٔ حذف‌شده کوچک و یک بازندهٔ حذف‌شده بزرگ بود، پس نسبتِ
مجموعِ سود به مجموعِ زیان به‌طورِ حسابی بهتر شد در حالی که **مهارت** بدتر شد.

> این دقیقاً شکلِ اشتباهِ رایجِ ۸ است، فقط با لباسِ PF به‌جای WR: بهبودِ یک
> نسبت با کوچک‌کردنِ مخرج، نه با افزودنِ لبه. معیارِ سه‌شرطی به همین دلیل
> شرطِ لیفت را داشت — و همان شرط این نسخه را رد کرد.

---

## ۶) وضعیتِ بندِ سوم

بندِ سومِ قانونِ همپوشانی **اجرا و بسته شد**، با اندازه‌گیری نه با ادعا:

- هر دو جهتِ ممکن آزموده شد، نه فقط جهتی که دوست داشتم.
- معیار پیش از اجرا ثبت شد و بعد از دیدنِ نتیجه تغییر نکرد.
- هر دو جهت گزارش شد، صرف‌نظر از نتیجه.
- علامتِ واقعیِ اثر کشف و ثبت شد، حتی با اینکه خلافِ فرضیهٔ خودم بود.
