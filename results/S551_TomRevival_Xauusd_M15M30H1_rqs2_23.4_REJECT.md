# S551 — احیای S141 «رانش چرخش ماه» روی تمام‌طول — REJECT

- **پیش‌ثبت:** `results/S551_PREREG_S141_TOM_FULLSPAN_H1.md` (commit `b39cef49`، پیش از هر عدد)
- **اسکریپت:** `strategies/s551_tom_revival.py` · خام: `results/_scan_S551/*.json` (+ زمان‌های ورود برای ممیزی همپوشانی)
- **قاعده (صفر جست‌وجو):** LONG · اولین روز معاملاتی ماه · hour∈7..12 UTC (SESSION_OPEN) · بدون فیلتر اندیکاتوری · SL=TP متقارن · overlap=False
- **آمار:** SEED=20260817 · K_PERM=2000 · n_trials=299 · نول اندازه‌گیری‌شده با هندسهٔ خود لایه · داده: `data/mt5_full/` تمام‌طول ~۱۵٫۵y

## حکم: REJECT — کارت حکم‌ساز H1 دروازه‌های H3/H5/H10 را شکست

| کارت | نقش | هندسه | n | WR | lift | z | rqs2 | دروازه‌های ✗ |
|---|---|---|---|---|---|---|---|---|
| M15 | اطلاع | 295/295/48 | 203 | 58.13% | +8.31pp | 2.4 | 24.6 | H3,H5 |
| M30 | اطلاع | 295/295/36 | 201 | 55.22% | +4.58pp | 1.3 | 22.4 | H3,H5,H8,H10 |
| **H1** | **حکم‌ساز** | 395/395/24 | **195** | 55.90% | +4.45pp | **1.2** | 23.4 | **H3,H5,H10** |

## تحلیل توان (قانون گزارش n_needed)

- H1: `n_needed = 195·(3.09/1.2)² ≈ 1,293` معامله. اما این قاعده ساختاراً **~۱ معامله per ماه** می‌دهد (روز اول ماه، بدون همپوشانی) ⇒ سقف نمونه در ۱۵٫۵ سال ≈ ۱۸۷ و برای ۱٬۲۹۳ معامله ~۱۰۷ سال داده لازم است. **هیچ مقدار انتظاری نجاتش نمی‌دهد** — این REJECT توان‌محورِ ابدی برای این پیکربندی است، نه POWER-LIMITED موقت.
- M15 (نزدیک‌ترین): `n_needed = 203·(3.09/2.4)² ≈ 337` ⇒ کمبود ~۱۳۴ ماه ≈ ۱۱ سال دادهٔ آینده. ثبت شد برای آیندگان؛ ادعایی برنمی‌خیزد (کارت اطلاع‌رسان + انتخابش پس از دیدن نتیجه می‌بود = H5).

## یافته‌ها

1. **مهارت واقعی اما زیر کران:** lift مثبت در هر ۳ کارت و PF>1.49 در همه — اثر TOM روی طلا «وجود دارد» اما در چگالی ~۱۲ معامله/سال هرگز به z≥3.09 نمی‌رسد. این همان درسِ گزارش اشتراکات است: پذیرش‌ها با **تجمیع نمونه** ساخته می‌شوند؛ TOM ذاتاً کم‌نمونه است و تجمیع تایم‌فریمی هم نمونهٔ مستقل نمی‌افزاید (قضیهٔ S540 بلوک موازی: عبورهای تقویمیِ درشت یک رویداد تقویمی‌اند — FIFO حذف‌شان می‌کند).
2. **مقایسه با پذیرش قدیمی S306:** S306 روی ۶٫۴ سالِ M15 با RQS+ قدیمی ACCEPT گرفته بود؛ زیر v2.6 و روی ۱۵٫۵ سال، همان خانواده z=2.4 می‌دهد. تأییدی بر سخت‌گیریِ درست معیار جدید.
3. **H6 پاس در هر ۳ کارت** (برخلاف S550): اثر TOM در پنجره‌های تقویمی پایدار است — مشکل فقط چگالی نمونه است.
4. **افشای همپوشانی:** با S312/S432 ساختاراً صفر (روز ۱ ∉ {10,13,20}). زمان‌های ورود در JSONها ذخیره شد؛ چون لایه رد شد، ممیزی همپوشانی با لایو‌ها موضوعیت استقراری ندارد.

## پرهیز از ۸ اشتباه — شواهد
جست‌وجوی صفر (همهٔ پارامترها ارثی/پیش‌ثبت)؛ زیرمجموعهٔ ساعتیِ پس‌ازنتیجهٔ S306 صریحاً رد شد؛ فیلترانباشت S222c رد شد؛ هر ۳ کارت گزارش شد؛ هندسه per-TF؛ بدون عدد گرد؛ TP=SL؛ دادهٔ تمام‌طول.

**وضعیت: REJECT — مرگ ابدی برای این قاعده/هندسه (کمبود توان ساختاری، نه قابل‌جبران با داده). گام بعد: S552.**
