# S587 — پیش‌ثبت: احیای کارتِ RVI-H12 با گیتِ دریفتِ TSM (قانون S604)

**تاریخ:** 2026-08-27 · **دانشمند:** دههٔ S580–S589 · **دارایی:** XAUUSD-H12
**نوع:** مسیر ۲ (احیای سوخته) — **یک** آزمونِ تأییدیِ تک‌کارته، صفر جست‌وجو
**این سند قبل از هر محاسبهٔ گیت‌خورده commit می‌شود.**

---

## ۱. افشای کاملِ اطلاعاتِ موجود (صداقتِ پیشینی)

آنچه از قبل **می‌دانم** (همه تحت پروتکل‌های پیش‌ثبت‌شدهٔ قبلی محاسبه و
منتشر شده):
- S584 (اکتشاف نیمهٔ اول): برندهٔ H12/rr1.618/mh34 با lift ناکافی (5.09
  در برابر 11.78) ⇒ INCOMPLETE.
- S585 (استخر، طبق پیش‌ثبت روی کل داده اجرا شد اما موتور هرگز فراخوانی
  نشد): H12 کامل‌داده n=386، WR=45.85، lift=+7.95pp — **تنها بازماندهٔ**
  انتخاب‌گرِ همگنی؛ استخر با <۲ عضو فروپاشید.

آنچه **نمی‌دانم** (موضوع این آزمون): رفتارِ همین کارت زیر گیتِ دریفتِ
علّی. هیچ نسخهٔ گیت‌خورده‌ای هرگز اجرا نشده.

## ۲. مبنای علمی و وراثت

- **S604 (ACCEPT 80.7، دههٔ کانتور — فقط الهام با ذکر منبع، کد و قلمروشان
  دست‌نخورده):** گیتِ TSM دریفت K=60 روزِ تقویمی، علّی (`close[t−1]` در
  برابر `close[t−1−K]`)، liftِ هر ۴ کارتِ شوک را بالا برد و کارتِ سوختهٔ
  H6 را احیا کرد.
- Moskowitz, Ooi & Pedersen 2012 (*Time Series Momentum*): سود ادامه‌روی
  ۱۲ماهه در ۵۸ بازار؛ نسخهٔ کوتاه‌ترِ K=60d برای طلا در S604/S950 تثبیت شد.
- S525 (درسِ مخالف): گیت می‌تواند توان بسوزاند (z افت کند) — ابطال‌گرِ
  F2 همین‌جا پیش‌ثبت می‌شود.
- سیگنالِ پایه: S513 — lift مالِ سیگنال است و منتقل می‌شود.

## ۳. قاعدهٔ فریز (صفر درجهٔ آزادی)

- **سیگنال:** RVI(14) cross 70/30 دوسویه (عیناً S584/S585؛ بازنویسی ممنوع).
- **گیت (تنها عنصر تازه، خودش فریز):** K = 60 روزِ تقویمی = **120 کندلِ
  H12**. LONG فقط اگر `close[t−1] > close[t−1−120]`؛ SHORT فقط اگر
  `close[t−1] < close[t−1−120]`. هیچ جست‌وجویی روی K نمی‌شود — K=60d
  پرتکرارترین برندهٔ S604 (H8 و H6 هر دو K=60) با ذکر منبع.
- **هندسه (BUG-GEOMDRIFT — از آرتیفکت):** sl=151.09، tp=244.46 (rr=1.618)،
  mh=34 از `results/_s584_explore.json`.
- **داده:** `fd.load_fast('XAUUSD','H12')` کامل ۱۵.۶ سال؛ assert>12y.

## ۴. پروتکلِ داوری (یک فراخوانِ موتور)

1. سیگنالِ گیت‌خورده روی کل داده → `simulate_trades` (دوسویه، overlap=False).
2. گارد n≥30 وگرنه MEASUREMENT-LIMITED.
3. **نالِ شرطی در فضای گیت (قانون S523/S525):** جای‌گشت‌ها فقط روی
   کندل‌های مجازِ همان سمت (گیتِ همان جهت باز)، K_PERM=500/سمت، هندسهٔ
   همان کارت، seed قطعی؛ uncond هر سمت هم در فضای گیتِ همان سمت
   (allow_overlap=True، نمونهٔ بزرگ). گاردهای BUG-PERMK/NULLUNCOND/DIRSTR.
4. split = صدکِ ۶۰٪ زمانِ ورود (BUG-SPLITDIR) → holdout_mask.
5. **حکم: یک فراخوانِ `compute_rqs2` با n_trials=18** (حسابِ صادقانه:
   ۱۶ اکتشافِ S584 + ۱ استخرِ S585 + ۱ همین آزمون). امتیاز/حکم فقط از موتور.

## ۵. ابطال‌گرها (پیش‌ثبت)

- **F1 (گیت می‌سوزاند، S525):** lift گیت‌خورده ≤ +7.95pp (نسخهٔ بی‌گیت) ⇒
  قانونِ S604 روی سیگنال‌های غیرشوک تعمیم نمی‌یابد — ثبت و مرگ.
- **F2 (توان):** n گیت‌خورده <150 ⇒ حتی lift بالا هم بی‌دادگاه می‌ماند؛
  حکم موتور هرچه بود همان (احتمالاً UNPROVEN/POWER-LIMITED).
- **F3 (P1):** اگر واقعی است، z باید از 3.09 عبور کند (H3) — تک‌آزمونه،
  بدون تلاش مجدد.
- نتیجه هرچه شد خانوادهٔ RVI برای همیشه بسته می‌شود (این دومین و آخرین
  فرصتِ مجازِ احیا طبق مسیر ۲ است).

## ۶. ثابت‌ها

SEED=20260828 · K_PERM=500/سمت · N_TRIALS=18 · SPLIT_FRAC=0.60 ·
K_DRIFT=120 کندل H12 (=60 روز) · هندسه از `_s584_explore.json`.

## ۷. تعهد ۸-خطا

Look-ahead: گیت با t−1 علّی ✓ · انتخاب پسینی: K و همه‌چیز اینجا قفل ✓ ·
هندسه از آرتیفکت ✓ · چندگانگی: n_trials=18 تجمعی ✓ · نال شرطی در فضای
گیت ✓ · split صدکِ زمان ورود ✓ · دیتاست assert>12y ✓ · حکم فقط موتور ✓.

*— دههٔ S580. جزر و مد را با جریانِ اقیانوس هم‌جهت می‌کنیم؛ اگر موج بلندتر نشد، قانونِ S604 مرز خورده است.*
