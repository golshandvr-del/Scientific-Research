# S672 — PREREG-2: فریزِ کاندیدا قبل از لمسِ holdout

> پیش‌ثبتِ اصلی: `results/S672_PREREG_NOSUPPLY_PULLBACK_VSA_PATHC.md` (کامیت f9dcde61)
> اسکنِ ۱۹ TF کامل و per-TF کامیت شد (`results/_scan_S672/`). این سند **قبل از هر لمسِ holdout** کامیت می‌شود.

## نتیجهٔ ارزیابیِ ۶ معیارِ فریز روی هر ۱۹ TF

- **H8 پاس شد** (تنها TF). D1 (PF=1.12)، H6 (PF=1.20)، H1/H2/H3 (PF≈1.0)، همهٔ TFهای دقیقه‌ای (PF<0.9، هزینه‌مرگ — پیش‌بینیِ P1 تأیید)، H12 (lsn=57)، W1 (n=3)، MN1 (صفر) — سوختند.
- پیش‌بینیِ P2 نیمه‌تأیید: لبه در H6–H8 ظاهر شد (نه M30–H1).

## کاندیدای منجمد (دست نمی‌خورد)

| TF | pt | α | side | k_sl | RR | max_hold | نیمهٔ جستجو |
|---|---|---|---|---|---|---|---|
| **H8** | 50 | 0.7 | **short** (No Demand) | 1.0×ATR100 | 1.5 | 32 | n=252, WR=?, lift=+8.73pp, PF=1.51, lsn=138.5, n_req=306≤403, q1=+4914/q2=+1885 |

**معیارِ (e) پیوستگی:** ۸ سلولِ مجاور در H8-short همگی PF 1.34–1.61 (کامیت‌شده در JSON) — خوشهٔ پیوسته، نه جزیره. H6-short هم‌جهت (+5.5pp) اما زیرِ آستانهٔ PF.

## پروتکلِ حکمِ نهایی (منجمد)

- دقیقاً **یک** `compute_rqs2` روی کل‌داده با `split_bar = n_full//2`.
- مدلِ صفرِ **روند-شرطی**: ورودهای تصادفیِ short فقط از بارهای `valid ∧ (close<SMA50)`؛ همان هندسه/موتور/max_hold؛ K_PERM=1000؛ SEED=20260823.
- **n_trials = 11** (۸ فینالِ S670 + ۲ فینالِ S671 + ۱ فینالِ S672 — بدهیِ انباشتهٔ صادقانهٔ بلوکِ S670–S679).
- خروجی: `results/_final_S672/H8.json` + trades.csv؛ سپس MD حکم.
- هرچه داور گفت همان است؛ بدونِ v2، بدونِ فیلترِ پس‌ازنتیجه.
