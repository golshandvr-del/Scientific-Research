# S610 — پیش‌ثبت (قبل از هر محاسبهٔ تازه): جداشدگی طلا از دلار — H8/D1 — فقط LONG — نول شرطیِ درفت‌مثبت — XAUUSD

**دانشمند:** اواریست گالوا · **دهه:** S610–S619 · **تاریخ:** 2026-09-05
**داور:** `engine/rqs2.compute_rqs2` v2.6 دست‌نخورده · این سند قبل از نوشتن/اجرای هر کدی کامیت می‌شود.

## ۰) چرا S610 (بازپس‌گیری شماره)
پیش‌ثبت قبلی S610 (`S610_PREREG_S334_FULLDATA_REJUDGE.md`) **پیش از هر محاسبه** باطل شد (S580 موازی همان آزمون را اجرا کرد؛ بودجهٔ صفر خرج شد). قانون کاربر: «هر یک از ده شماره باید لایهٔ استراتژی و حکم رسمی موتور داشته باشد.» پس S610 اکنون لایهٔ واقعی می‌گیرد. هیچ عددی از پیش‌ثبت باطل‌شده به این لایه راه ندارد.

## ۱) افشای کامل آلودگی
- **S618 (خودم، REJECT 0)** همین رویداد را روی **H1/H4** با **دو جهت** و **نول سکه** روی نیمهٔ اول (۲۰۱۹-۱۲ → ~۲۰۲۳-۰۳) سنجید: H4 k=2.058 بازوی LONG WR=58.8 (n=131)، بازوی SHORT 56.2؛ H1 ≈ سکه. S618 §۵ صریحاً نوشت: «مسیر آینده: H8/D1، فقط LONG، نول شرطی در فضای درفت‌مثبت.» **این لایه دقیقاً همان مسیر است ⇒ فرضیه‌ای *پس از دیدنِ* نیمهٔ اول H4 است.** جبران: (الف) نیمهٔ اول **هیچ ادعایی** نمی‌سازد؛ (ب) کارت‌های H8/D1 در S618 هرگز شبیه‌سازی نشدند (بکر از نظر محاسبه، نه از نظر فرضیه)؛ (ج) بودجهٔ چندگانگی ارثی کامل S617+S618 (۶۲۰) به n_trials افزوده می‌شود؛ (د) مرحلهٔ تأیید فقط روی نیمهٔ دوم.
- **بازهٔ داده (افشا):** DXY فقط از ۲۰۱۹-۱۲-۱۳ موجود است ⇒ اشتراک ۶.۶ سال. طلا از `data/mt5_full/` کامل بارگذاری می‌شود (گارد `assert 'mt5_full' in src`)؛ **ATR و درفت روی تاریخ کامل ۱۵.۶ ساله محاسبه می‌شوند**؛ فقط فضای رویداد به بازهٔ DXY محدود است. این محدودیت داده‌ای است نه انتخابی و در MD نهایی برجسته می‌شود.
- مرور ACCEPTهای تازه: S1511، S1520/S1521، S1911، S589، S749، S606/S607، S919 — همه ادامه‌روی/شوک روی **خودِ طلا** با نول شرطی؛ S610 لنگر **خارجی** (DXY) دارد و با هیچ‌کدام هم‌رویداد نیست. هیچ لایه‌ای در S5xx–S9xx یا S1xxx روی DXY کار نمی‌کند (grep `dxy|decoupl`: فقط S427 موازی که **EURUSD-پسماند ۵روزهٔ بازگشتی** روی H1 است — رویداد، جهت، دارایی مرجع و افق متفاوت؛ دست‌نخورده).

## ۲) نظریه
تقاضای طلا **مستقل از ارز** (پناهگاه، بانک مرکزی) وقتی آشکار می‌شود که طلا **همراهِ** دلار بالا می‌رود. چنین جریان‌هایی نهادی و چندروزه‌اند ⇒ ادامه در مقیاس H8/D1. قانون جهت‌مندی طلا (S522/S528/S618/S1511): اطلاعات ادامه‌روی فقط در LONG.

## ۳) قاعدهٔ منجمد (صفر پارامتر تازه)
- رویداد روی **بار H8 طلا** (ساعت بستهٔ آن `T`): `z_g = r_g/sd34_prev(r_g)` روی H8 طلا (rolling-STD ۳۴ بار، تا بار قبل)؛ `z_d` = همان روی بازدهٔ DXY-H1 **تجمعی در همان پنجرهٔ ۸ ساعته** `[T−8h, T]` تقسیم بر sd34_prev بازده‌های تجمعی ۸ساعتهٔ DXY (ساخته از H1، غیرهم‌پوشان، تا پنجرهٔ قبل). رویداد = `z_g ≥ +1.0 ∧ z_d ≥ +1.0`. **فقط LONG.**
- **درفت-گیت ساختاری (تعریف نول، نه فیلتر انتخابی):** `close[t−1] > close[t−1−90]` روی H8 (منجمد S526/S1520؛ ۹۰ کندل H8 = ۳۰ روز). رویدادها **درون** این فضا تعریف می‌شوند و نول هم از همین فضا نمونه می‌گیرد (استاندارد S523).
- کارت D1: همان قاعده با H8→D1 (`z_g` روی D1، `z_d` روی بازدهٔ روزانهٔ DXY از H1، درفت ۳۰ بار D1).
- ورود `open[sig+1]`؛ **هندسه منجمد S526/S1520:** SL=1.5×median(ATR100 کل تاریخ) ، TP=1.5×SL (RR 1.5)، hold=16 بار (H8) / 6 بار (D1)، تقدم SL، allow_overlap=False (FIFO).
- کارت‌ها: **H8 (اصلی، از پیش اعلام‌شده)**، D1 (MTF). ۲ نقطه.

## ۴) نول (استاندارد S523/S526 — شرطی)
- `uncond`: ورود در **هر** بار فضای درفت‌مثبت (stride 1) با همان هندسه.
- `perm`: K=1000 نمونهٔ تصادفی به اندازهٔ n از فضای درفت‌مثبت، همان هندسه، SEED=20260907. مرجع = max(uncond, perm_mean). (سخت‌ترین: مهارت ورای بتای صعودی.)

## ۵) بودجه و دو مرحله
- n_trials = **622** (۳۰۰ S617 + ۳۲۰ S618 + ۲ کارت). سد بخت موتور ≈ Φ⁻¹(1−1/622) = **3.98σ**.
- **مرحلهٔ تأیید (نیمهٔ دوم فضای رویداد — بکر):** کارت H8: n₂≥40 ∧ net₂>0 ∧ lift₂≥2.0×perm_sd₂. شکست ⇒ **REJECT 0** (حکم رسمی). گذشت ⇒ مرحلهٔ حکم.
- **مرحلهٔ حکم:** کل بازه روی H8 و D1؛ compute_rqs2 با trades DataFrame (pnl_pip, outcome, sl_pip, entry_bar, exit_bar, direction) + tp_pip + null + n_trials=622 + split_bar (مرز نیمه) + bar_time + close. حکم موتور کلمهٔ نهایی. اگر ACCEPT ⇒ توقف، ممیزی هم‌پوشانی فوری با S526/S1520/S589/S966/S602–S607 (همه H8/D1 LONG)، گزارش به کاربر.

## ۶) پیش‌بینی‌ها
- **P1:** lift₂ کارت H8 در [+5, +15] (اگر شاهد S618 واقعی باشد). lift₂≤0 ⇒ شاهد S618 آرتیفکت رژیم ۲۰۱۹–۲۰۲۳ (چهارمین REGIME-ONLY دهه).
- **P2:** مرجع نول شرطی **بالاتر** از سکه (≈۵۰) خواهد بود (بتای صعودی)؛ پیش‌بینی ≈۵۲–۵۶ با RR 1.5 (BE≈40.7).
- **P3:** رویداد H8 در بازهٔ ۶.۶ سال: ۱۲۰–۳۰۰ (P≈2–4٪ از ۵۱۴۶ بار H8 پس از گیت درفت).
- **P4:** H8 ≥ D1 در lift (n کافی در H8؛ D1 کم‌نمونه).

## ۷) خروجی
`strategies/s610_decoupling_h8.py` (confirm/verdict) · `results/_s610_decoup/{confirm_half2.json, decision.json[, verdict_H8.json, verdict_D1.json]}` · MD نهایی `S610_GoldDxyDecouplingLong_Xauusd_H8D1_rqs2_<score>_<verdict>.md`.

*این پیش‌ثبت قبل از نوشتن یا اجرای هر کدی کامیت می‌شود.* 🔒
