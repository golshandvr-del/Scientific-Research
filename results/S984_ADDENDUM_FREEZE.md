# S984 — الحاقیهٔ انجماد: حکم رسمی موتور (یک شلیک)

**تاریخ: 2026-09-04 — کامیت‌شده *قبل از* اجرای داور (نظم مسیر C).**
پیش‌ثبت: `results/S984_PREREG_DAILY_OPEN_RECLAIM.md` (fdf04f52). اسکنر: `strategies/s984_daily_open_reclaim_scan.py`.
اسکن ۱۹ کارت روی نیمهٔ اول کامل و کامیت‌شده است (`results/_s984/scan_*.json`، آخرین چک‌پوینت b2756c9f).

## ۱) نتیجهٔ گام ۱ (اسکنِ نیمهٔ اول) — سدِ توان **رد شد**

| کمیت | مقدار | میله |
|---|---|---|
| بیشینهٔ z مثبت | **+2.11** (H8, q=0.5, main, SL_k=1.8, RR=1.3, n=298, lift=+6.06pp) | `expected_max_z(456)=3.02`، با بدهیِ خانواده `3.42` |
| لیفتِ لازم برای هولد-اوت با n≈300, p0≈0.44 | **≈8.9pp** | لیفتِ جست‌وجو 6.06pp |
| کارت‌های بی‌بازو (n<30) | D1, W1, MN1 | — |

**پیش‌بینی‌های ثبت‌شده:**
- (الف) زیرِ M30 سمّی — **تأیید** (M1 z=−54.8 … M20 z=−2.75؛ پنجمین تکرارِ گرادیانِ مقیاس).
- (ب) main > mirror — **تأیید** (میانگین z روی M30–H12: main −0.60 در برابر mirror −2.77؛ ششمین تکرار).
- (ج) قلمروِ مثبت M30–H3 — **رد شد**: M30 (−1.56) و H1 (+0.61) بی‌اثر؛ تنها بازوهای مثبت روی H3–H8 هستند (رویدادِ درون‌روزی روی TFهای درشت با q=0.5 هنوز پرتعداد است — تنشِ پیش‌بینی‌شده نادرست بود).
- (د) لیفتِ q یکنواخت صعودی — **تأیید ضعیف در تجمیع** (q=0.5: −0.29، q=1.3: +0.19، q=2.1: +0.48pp) اما **در H8 برعکس** (فقط q=0.5 مثبت).

## ۲) پیکربندیِ منجمد برای شلیکِ رسمی (طبق دستور کاربر: هر شماره حکم موتور می‌گیرد)

| پارامتر | مقدار |
|---|---|
| کارت | **XAUUSD-H8** (`data/mt5_full/XAUUSD_H8.csv`, 11,978 کندل) |
| رویداد | `reclaim_signals(df, atr_arr(df), q=0.5)` — عینِ اسکنر |
| جهت | **main** (عبورِ صعودی ⇒ LONG، نزولی ⇒ SHORT) |
| SL_k / RR | **1.8 / 1.3** (TP>SL) |
| sl_base_pip | **99.69** (منجمد از `scan_H8.json`، نیمهٔ اول) ⇒ SL=179.44pip, TP=233.27pip |
| max_hold | 64 · `allow_overlap=False` |
| split_bar | `n_all//2 = 5,989` |
| مدلِ صفر | `build_null_fast` K=600، seed=20260806، هر دو سمت |
| n_trials | **456** (فضای اعلام‌شدهٔ این لایه؛ بدهیِ خانواده در MD ذکر می‌شود) |
| داور | `strategies/s984_judge.py` |

**پیش‌بینیِ از-پیش-اعلام‌شده:** REJECT (لیفتِ جست‌وجو 6.06 < 8.9pp لازم). این شلیک برای اجرای دستورِ «حکمِ رسمیِ موتور برای هر شماره» انجام می‌شود، نه به امیدِ ACCEPT. پس از حکم، هیچ پارامتری تغییر نمی‌کند و شمارهٔ S984 بسته می‌شود.

— آندری کولموگوروف
