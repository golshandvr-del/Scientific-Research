# S1742 — پیش‌ثبت: بستهٔ قوی × رانش دوگانه × ماندگاری بدنه (Strong-Close Dual-Drift × Kyle Body-Retention ρ≥0.618) — Path C

**نویسنده**: عمر خیام نیشابوری (بلوک دوم S1740–S1749 — ۳ از ۱۰) · **تاریخ**: 2026-09-04 — **کامیت قبل از هر خط کد.**
**دارایی**: XAUUSD · **داده**: فقط `data/mt5_full` (enforce) · **فقط اکتشاف — هیچ تغییری در سایت.**

## ۰) مالکیت
S1742 آزاد (`grep S1742` خالی). فایل دیگران لمس نمی‌شود.

## ۱) درس‌های بازبینی (ادامهٔ همین ساعت)
- **S1741 من**: گیت حجم روی رخداد رنج‌محور **مرده** بود (pass 87٪). درس: گیت باید بُعدی را بسنجد که رخداد **از قبل تثبیت نکرده**. رخداد من رنج (≥2.5ATR) و موقعیت بسته (CLV≥0.8) را تثبیت می‌کند — ولی **اندازهٔ بدنه** را نه.
- **نقص ساختاری رخداد**: `CLV≥0.8 ∧ close>open` هم شامل ماروبوزو (open نزدیک low، ρ≈0.9) است و هم **چکش** (open نزدیک high، بدنهٔ کوچک، سایهٔ پایینی بلند، ρ≈0.2). چکش در تفسیر کایل = جریان نویزی که درون کندل برگشته (اثر گذرا)؛ ماروبوزو = اثر دائمی. مخلوط‌کردنشان lift را رقیق می‌کند.
- **S965/S1911/S919/S802**: ρ≥0.618 روی سه خانوادهٔ مستقل (شوک ATR21 کایل، اسکوییز S800، پرش S950) counter را کشت و lift را بالا برد. روی خانوادهٔ strong-close آزموده نشده.
- **گیت جلسه/ساعت** روی شوک‌ها: S654, S899 REJECT ⇒ رد.

## ۲) فرضیه
میان رخدادهای S749-H2، آن‌هایی با `ρ = |close−open|/(high−low) ≥ 0.618` (بدنهٔ غالب) اثر قیمتی دائمی دارند و ادامه می‌یابند؛ رخدادهای ρ<0.618 (چکش/سایهٔ بلند) ضعیف‌تر یا برگشتی‌اند. پیش‌بینی: گیت lift را بالا می‌برد و counter ضعیف‌تر است.

## ۳) قاعدهٔ منجمد — همه به ارث، صفر پارامتر آزاد
| جزء | مقدار | منبع |
|---|---|---|
| رخداد + رانش دوگانه + براکت + long_only | عیناً S749/S748-H2 (`_scan_S748/H2.json`, assert) | S749 |
| **گیت ماندگاری (جدید)** | `ρ_t = |close_t − open_t| / (high_t − low_t) ≥ 0.618` روی همان کندل رخداد (causal: کندل بسته شده؛ ورود open بعد) | S965 (ثابت طلایی، قفل) |
| گیت آرامش/حجم | استفاده نمی‌شود (سنجش مستقل) | |

## ۴) کارت‌ها و n_trials
- **داوری: H2**. **n_trials_official = 649** (640 + 3 S1740 + 3 S1741 + 3 این لایه). تنش 2000/5176.
- تأییدی: H4, H6 (P1/P3 فقط).
- هیچ آستانهٔ دیگری (0.618 قفل).

## ۵) ابطال‌گرها
- **P1**: lift_vs_BE بازوی ρ≥0.618 > بی‌گیت (+17.68 روی H2). شکست ⇒ REDUNDANT ⇒ ناموفق.
- **P3**: counter ρ<0.618 ضعیف‌تر (WR/lift).
- **P5**: pass-rate ۳۰–۸۵٪ (اگر >۸۵٪، گیت مرده مثل S1741 — صریح گزارش می‌شود).
- **P6**: هم‌پوشانی با calm (S1740) — برای تصمیم ترکیب در لایهٔ بعد.

## ۶) قاعدهٔ تصمیم
H2 ACCEPT ∧ P1 ✓ ⇒ موفق، توقف. در غیر این صورت ناموفق ⇒ S1743. هیچ تیونی.

## ۷) تعهدات
سند قبل از کد · کد قبل از اجرا · src enforce · ۸ خطا · هیچ استقراری/تغییر سایت.
