# S580 — پیش‌ثبتِ مسیرِ تکثّر: بازآزماییِ لایهٔ منجمدِ S334 (MR-SHORT) روی دادهٔ کامل ۱۵.۶ ساله

> **تاریخ پیش‌ثبت:** 2026-08-12 · **وضعیت:** 🔒 قفل‌شده **پیش از اجرای هر آزمون**
> **مسیر انتخابی: Path C — تأییدِ hold-out** (`docs/GUIDE_FOR_STRATEGY_BUILDER_AI.md`)
> این سند در کامیتِ جداگانه و **قبل از** نخستین اجرای داور ثبت می‌شود — الزامِ راهنما.

---

## ۱) کاندیدا و چرایی (استدلالِ پیش از داده)

- **لایه:** S334 — Mean-Reversion **SHORT** (منشأ s122) با گیتِ رژیمِ Hurst+Kurtosis.
- **کارت:** XAUUSD-M5 · **حکم فعلی:** POWER-LIMITED (n=47 روی دادهٔ کوتاه ~۲.۸ سال).
- **تریاژ:** از ۴ کارتِ POWER-LIMITED در `AUDIT_LEDGER.json`، سه‌تا بسته‌اند
  (S312→نجات با S432؛ S323→مرگِ ابدیِ اثبات‌شده؛ S354→جایگزین‌شده با S356).
  **S334/XAUUSD-M5 تنها کارتِ POWER-LIMITEDِ بازآزمایی‌نشده روی دادهٔ کامل است.**
- **منطقِ z∝√n:** n از ۴۷ به ~۲۵۰+ می‌رسد ⇒ اگر لبه واقعی باشد z از مرز 3.09σ عبور
  می‌کند؛ اگر برازشِ درون‌نمونه بوده باشد، دادهٔ بکر آن را افشا می‌کند.
  **هر دو خروجی اطلاعِ خالص است.**

## ۲) کانفیگِ منجمد — منبعِ واحد (درسِ BUG-GEOMDRIFT)

کانفیگ در زمانِ اجرا از آرتیفکتِ رسمی `results/_s334_XAUUSD_M5.json` **خوانده** می‌شود
(هرگز در کدِ داور بازنویسی نمی‌شود). مقادیرِ ثبت‌شده در آن آرتیفکت:

```
ورود SHORT : z_win=34, z_thr=2.4, RSI(14)>70, شمعِ نزولی (close<open و close<close[-1])
گیتِ رژیم  : Hurst(دریچهٔ بانک) < 0.50  AND  Kurtosis < 1.8
خروج       : SL=110 pip, TP=125 pip (TP>SL ✅), maxHold=20 کندل
اجرا       : allow_overlap=False · اسپرد 3.3 pip · کمیسیون 0 · contract=100
```

**هیچ پارامتری تنظیم نمی‌شود. هیچ جاروبی وجود ندارد. یک کانفیگ، آزمون‌های شمرده.**

## ۳) مرزِ دادهٔ دیده/بکر (اندازه‌گیری‌شده، نه حدسی)

- دادهٔ قدیمی که S334 روی آن قفل شد: `data/XAUUSD_M5.csv` — از epoch **1695076500**
  (2023-09-18 22:35 UTC) تا 2026-07-16.
- دادهٔ کامل: `data/mt5_full/XAUUSD_M5.csv` — ۱,۰۸۹,۵۷۴ کندل، 2011-01-03 → 2026-08-07.
- ⇒ **بخشِ بکر: ۸۸۵,۰۰۳ کندل (2011-01-03 → 2023-09-18)** — هرگز توسط این لایه دیده نشده.
- بارگذاری فقط با مسیرِ رسمی: `tools/s434_fast_data.load_fast('XAUUSD', tf)` و گزارشِ
  `d['src']` در هر خروجی (ضدِ تلهٔ E-16).

## ۴) بازوهای پیش‌ثبت‌شده — نه بیشتر

| بازو | داده | نقش |
|---|---|---|
| **A (اصلی — Path C)** | فقط بخشِ بکر `time < 1695076500` | **یک** آزمونِ تأییدی روی hold-out طبیعی |
| **B (کارتِ رسمی)** | کلِ ۱۵.۶ سال | داوریِ رسمیِ RQS2 v2.6 کارتِ XAUUSD-M5 |
| **C1..C6 (قانونِ MTF)** | کلِ داده، هر TF جدا: M1, M15, M30, H1, H4, D1 | نقشهٔ زیستگاه (EURUSD به دستورِ کاربر حذف) |

**قاعدهٔ هندسه برای C1..C6 (ثبت پیش از دیدنِ اعداد):** روی هر TF،
`SL = ratio_sl × medianATR14(TF)` و `TP = ratio_tp × medianATR14(TF)` که
`ratio_sl = 110/medianATR14(M5)` و `ratio_tp = 125/medianATR14(M5)` — یعنی همان
مضرب‌های M5، بدون هیچ جاروب. `maxHold=20` ثابت. آستانه‌های رژیم ثابت (h<0.50, k<1.8).
(همان قاعدهٔ مرحلهٔ `mtf` خودِ S334 — اشتباهِ رایج #۶ «TP/SL یکسان برای همه TF» رخ نمی‌دهد.)

## ۵) پارامترهای داوری (کپیِ عینی از رابطِ تأییدشدهٔ `tools/s437_adjudicate.py`)

- `n_trials = 500` — سخاوتمندانه‌ترین شمارش: کلِ جست‌وجوی تاریخیِ S334 (باقی‌ماندهٔ
  finetune ~۴۰۵ ترکیب) + تریاژِ ۴ کاندیدا + ۸ بازوی این مأموریت ≤ ۵۰۰.
  چون `expected_max_z(567)=3.09` و H3 خودش z≥3.09 می‌خواهد، هر شمارشی ≤۵۶۶ سدِ
  یکسان می‌دهد ⇒ **این صداقت رایگان است** (کامنتِ داورِ S437).
- `N_PERM = 500` جای‌گشت به‌ازای هر بازو (درسِ S435: زیرِ ۵۰۰، حکمِ H3 نوسانی است).
- `SEED = 20260812` (ثابت، بازتولیدپذیر).
- **نال مخصوصِ هر بازو با هندسهٔ همان بازو** (گاردِ BUG-NULLUNCOND) و — نکتهٔ حیاتی که
  هنگام خواندنِ رابط شکار شد — چون لایه **SHORT** است، ماسک‌های جای‌گشتی به آرگومانِ
  `short_sig` موتور داده می‌شوند و دیکشنریِ نال در کلیدِ `'short'` پر می‌شود
  (نسخهٔ S437 برای لایهٔ long بود؛ کپیِ کورکورانه = باگِ جدید).
- نال **کامل** به موتور پاس می‌شود (هرگز `['short']` جدا نمی‌شود — BUG-NULLUNWRAP).
- `split_bar = int(len(df)·0.70)`، `initial_capital=10000`، `allow_overlap=False`.
- کلیدهای خروجی عیناً از نگاشتِ S437: `rqs2_score`، `verdict`، `metrics['z_luck_bound']`.
- قیدِ ۲: زیرِ ۳۰ معامله حکم صادر نمی‌شود ⇒ MEASUREMENT-LIMITED.

## ۶) قاعدهٔ تصمیم (پیش از دیدنِ هر عدد)

1. حکمِ رسمیِ کارت = خروجیِ خودکارِ `engine/rqs2.py` روی **بازوی B**. هیچ حکمی دستی نوشته نمی‌شود.
2. بازوی A تفسیرِ علّی می‌دهد: اگر A زنده و B زنده ⇒ لبه ساختاری است؛ اگر A مرده و
   B فقط به‌واسطهٔ بخشِ دیده زنده ⇒ برازشِ درون‌نمونه، و حکمِ صادقانه همان است که موتور بدهد.
3. اگر ACCEPT ⇒ ممیزیِ هم‌پوشانی با ۵ لایهٔ زندهٔ سایت (به‌ویژه S355 روی همین کارتِ M5)
   با شبیه‌سازِ event-driven؛ بخشِ هم‌پوشان **بلافاصله** به‌عنوان فیلتر آزموده می‌شود (قانونِ هم‌پوشانی).
4. نتیجه — هرچه باشد — در `results/S580_..._rqs2_<score>_<verdict>.md` ثبت و push می‌شود.

## ۷) پیش‌بینیِ ثبت‌شده (فروتنیِ علمی)

WR منجمد ۶۱.۷٪ روی ۲.۸ سالِ اخیر است؛ سابقهٔ S355 (۷۲.۳٪→۵۶.۹٪ پس از گسترشِ داده)
هشدار می‌دهد که رژیم‌های ۲۰۱۱-۲۰۲۳ (شاملِ روندهای بزرگِ طلا) برای یک لایهٔ fade
دشوارترند. گیتِ Hurst دقیقاً برای همین ساخته شد — این آزمون، آزمونِ *خودِ گیت* است.
**پیش‌بینی‌ام: WR کاهش می‌یابد؛ زنده‌ماندنِ حکم به این بستگی دارد که گیتِ رژیم واقعاً
رژیم را جدا کند نه اینکه صرفاً n را کم کرده باشد. احتمالِ ذهنی ACCEPT: ~۲۵٪.**

— S580 · پژوهشگر: در مقامِ گروتندیک، دریا را بالا می‌آوریم تا صخره خودش غرق شود.
