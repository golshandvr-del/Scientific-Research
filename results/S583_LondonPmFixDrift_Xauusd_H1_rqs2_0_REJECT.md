# S583 — LondonPmFixDrift — XAUUSD H1 — RQS2: 0 — REJECT (مرگ صادقانه در اکتشاف)

> **دههٔ من: S580–S589.** هیچ فایل دانشمندان موازی لمس نشد. EURUSD: صفر ثانیه.

## 0) شناسنامه
| فیلد | مقدار |
|---|---|
| Mission | S583 |
| Strategy | دریفت/برگشت حول فیکس عصر LBMA (Caminschi & Heaney 2014) — E1 short into-fix @16h سرور، E2 long post-fix @17h سرور |
| Pair / TF | XAUUSD-H1 |
| Prereg | `results/S583_PREREG_LONDON_PM_FIX_DRIFT.md` (a2c25cfb) **قبل از هر تست**؛ منطقهٔ زمانی سرور با گپ‌های هفتگی EET اندازه‌گیری شد |
| Path | C — ۶ نقطهٔ قفل‌شده، فقط نیمهٔ اول |
| Verdict | **REJECT — مرگ صادقانه در اکتشاف**: هر ۶ نقطه net<0؛ طبق قانون قفل‌شده آزمون تأییدی هرگز اجرا نشد؛ نیمهٔ دوم بکر ماند |
| RQS2 | 0 (موتور فراخوانی نشد — «مرده پیش از داوری») |
| Artifacts | `results/_s583_explore.json`، `tools/s583_explore.py` |
| Data | `data/mt5_full/XAUUSD_H1.csv` — span>12y ✅ (BUG-DATASETDRIFT) |

## 1) نتیجهٔ اکتشاف (نیمهٔ اول؛ n_E1=1975, n_E2=1981؛ SL=TP≈32.1/32.6 pip)
| رویداد | mh | n | WR% | exp_pip | net_pip |
|---|---|---|---|---|---|
| E1 short into-fix | 1 | 1975 | 43.80 | −4.43 | −8739 |
| E1 | 2 | 1975 | 46.99 | −3.94 | −7782 |
| E1 | 4 | 1975 | 48.25 | −3.43 | −6766 |
| E2 long post-fix | 1 | 1981 | 41.29 | −4.48 | −8873 |
| E2 | 2 | 1981 | 42.25 | −4.99 | −9880 |
| E2 | 4 | 1981 | 43.92 | −5.25 | −10407 |

سربه‌سرِ هزینه‌دار با این هندسهٔ متقارن ≈ 55.2٪؛ بهترین WR مشاهده‌شده 48.25٪ — **۷pp زیر سربه‌سر**. حتی الگوی خام دو بازو ضدفرضیه است: short@16h با نگه‌داشت طولانی‌تر *بهتر* می‌شود و long@17h *بدتر* — یعنی حول فیکس در H1 هیچ جهت‌مندی سیستماتیکی نیست؛ آنچه دیده می‌شود صرفاً دنبالهٔ دریفت عمومی بازار است.

## 2) یافتهٔ علمی
1. **اثر فیکس لندن در دانه‌بندی H1 روی XAUUSD-اسپات قابل شکار نیست.** اثر مستند ادبیات در پنجره‌های دقیقه‌ای حول 15:00 لندن زندگی می‌کند؛ میلهٔ یک‌ساعته آن را رقیق می‌کند و اسپرد 3.3 pip بقیه را می‌بلعد. شکار آن به دادهٔ tick با هزینهٔ زیر ~۱ pip نیاز دارد — خارج از مشخصات حساب.
2. چهارمین شاهد خانوادهٔ «رویدادهای تقویمی/ساعتی ریزدانه در دانه‌بندی درشت رقیق می‌شوند» (S551 turn-of-month، S521 calendar-window، S430 session-drift، S583 fix).

## 3) اثبات پرهیز از ۸ اشتباه رایج
1. **Look-ahead**: سیگنال از ساعتِ میلهٔ بسته‌شده؛ ورود open میلهٔ بعد در موتور رسمی. ✅
2. **Multiplicity**: پیش‌ثبت جدا قبل از تست؛ دقیقاً ۶ نقطهٔ اعلام‌شده؛ قوانین برنده/مرگ/توان از قبل قفل. ✅
3. **Cherry-pick**: هر ۶ نقطه ذخیره و کامیت شد. ✅
4. **Cost blindness**: pnl_pip خالص از اسپرد (BUG-PIPGUESS)؛ سربه‌سر هزینه‌دار در تحلیل. ✅
5. **Regime overfit**: صفر فیلتر — رویداد تقویمی خام. ✅
6. **Small-n worship**: n≈2000 در هر بازو — قدرت کافی بود؛ لبه نبود. ✅
7. **Verdict by hand**: حکم موتوری جعل نشد؛ REJECT-در-اکتشاف طبق قانون مرگ پیش‌ثبت (رویهٔ S581). ✅
8. **Data drift**: provenance در JSON؛ span>12y assert. ✅

## 4) تبعیت
فقط XAUUSD · TP=SL (مرز مجاز TP≥SL، پیش‌ثبت‌شده) · پارامتر غیر رُند (2.058×ATR34) · شمارهٔ دهگانهٔ مجاز · commit+push هر مرحله · تصحیح شفاف رکورد اشتباه قبلی (chop_fib_34) در پیش‌ثبت همین مأموریت.

## 5) درس برای S584
رویدادهای «ساعت ثابت» در H1 چهار بار مردند. S584 باید به رویدادهای **bar-native** برگردد (درس S521: لبهٔ S382 خاصیت ساختار میله بود نه تقویم). پیش از انتخاب کاندید: fetch کامل و بررسی ACCEPTهای جدید دانشمندان موازی.

*— Grothendieck: نقطهٔ فیکس، صخره نبود؛ برقی بود بر سطح آب — از دور سنگ می‌نمود، از نزدیک هیچ.*
