# S1581 — پیش‌ثبت: کفِ تازهٔ ۹۰-کندلی (S1511) × تأییدِ حجمِ نسبیِ هم‌اسلات (S589)

**تاریخ:** 2026-09-09 · **دانشمند:** بلوک S1580–S1589، شمارهٔ ۲ از ۱۰ · **مسیر:** ۱ (بهبود لایهٔ سالم S1511 با گیت متعامد)
**دارایی:** XAUUSD · **داده:** `data/mt5_full/XAUUSD_{H6,H8,H4}.csv` (هارد-گارد `mt5_full` در لودر) · **این سند قبل از هر عدد کامیت می‌شود.**
**قید مأموریت:** فقط اکتشاف — هیچ تغییری در سایت/استقرار.

## ۱) فرضیه — آزمونِ مستقیمِ قانون S1580

S1580 قانون داد: *گیت حجم هم‌اسلات روی رویدادهای «سطح» (fresh-high S589: ✅) متعامد و اطلاع‌رسان است؛ روی رویدادهای «دامنه» (شوک: ✗ زائد).* S1511 (ACCEPT 84.8، H6) رویداد سطحیِ دیگری است: `low[t] > max(low[t−90..t−1])` — «هوای پاک زیر کندل». اگر قانون درست باشد، همان گیت باید روی H6 توان بیفزاید (P1) و بازوی کم‌حجم باید بمیرد (P2). این یک **پیش‌بینیِ ابطال‌پذیرِ بیرونی** است، نه جست‌وجو.

## ۲) بکارت قلمرو (پس از fetch `633e51d6`)
`grep -iE 'volume|حجم|rvol'` روی S1511/S1513/S1621: **صفر**. S1513 (σ-calm روی fresh-floor) REJECT شد — گیتِ رژیم روی این رویداد کار نکرد؛ حجم متغیر دیگری است. لایه‌های S1511/S1513/S1621 دانشمندان دیگرند؛ **دست‌نخورده**.

## ۳) قاعدهٔ فریز (صفر پارامتر آزاد — همه ارثی)
| جزء | مقدار | منبع |
|---|---|---|
| رویداد | `low[t] > max(low[t−90..t−1])` و کندل قبل چنین نبود (لبهٔ تازه) | S1511 |
| گیت درفت | `close[t−1] − close[t−90] > 0` (فضای نول) | S1511/S523 |
| **گیت حجم** | `RVOL_slot[t] = volume[t]/median(volume of prev 30 same-hour bars, shift(1), min_periods 20) ≥ 1.0` | S589 |
| جهت / ورود | LONG-only · open کندل بعد · allow_overlap=False | S1511 |
| براکت | SL = 1.5×median(ATR100) · TP = 1.5×SL | S382/S526 |
| نول | شرطی در فضای drift>0: uncond strides 1/3/7 + جایگشت K=2000 seed=20260905، مرجع=MAX | S1511 |
| holdout | هارنس S382 (`split_bar=0.70·len`) | S382 |

هارنس = کپیِ عینیِ `strategies/s1511_fresh_floor.py` (خود کپیِ S526/S382) با **افزودنِ فقط** گیت RVOL؛ بازوها: `gated`، `counter` (RVOL<1)، و `ungated` (=S1511 خام همان داده، برای P1).

## ۴) کارت‌ها و شمارش
**اصلی:** H6 (خانهٔ S1511). **ثانوی:** H8، H4. **n_trials صادقانه = 3** (۳ کارت، بازوی gated داوری می‌شود؛ ungated/counter گزارشی). تنش 50 روی هر ACCEPT.

## ۵) پیش‌بینی‌ها و ابطال‌گرها (قبل از عدد)
- **P1 ضدِ سوختنِ توان:** H6 gated lift > +10.03 و z > 3.10 (پایهٔ S1511). ✗ → گیت فقط n برید.
- **P2:** counter < gated در H6. ✗ (counter ≥ gated) → **قانون S1580 ابطال می‌شود** و باید بازنویسی شود («فقط fresh-high، نه هر رویداد سطحی»).
- **P3 نرخ عبور < 75٪** (S529). پیش‌بینی: 60–72٪ (fresh-floor مثل fresh-high با کندل معمولی رخ می‌دهد، نه شوک).
- **P4 هم‌پوشانی** با S1511 (باید >80٪ — جانشین‌گونه) و با S589-H8 (اشتراک ورودی؛ S1511 خود ۶۵٪ با S526 هم‌پوشان بود).
- پیش‌بینی شخصی H6: n≈180–210، WR 53–56، lift +12..+15، z 3.2–3.8. prior ACCEPT ≈ ۴۰٪.
- ACCEPT با P3 پاس → توقف و گزارش (بدون استقرار). ACCEPT با گیت مرده → REDUNDANT. غیر از آن → گزارش خلاصه، S1582.

## ۶) پرهیز از ۸ خطای رایج (تعهد پیشینی)
1. Look-ahead: rolling().shift(1)، RVOL shift(1) درون گروه ساعت، ورود open بعدی. 2. چندگانگی: صفر جست‌وجو؛ n_trials=3 + تنش 50. 3. نال: شرطی هم‌هندسه K=2000. 4. Holdout: هارنس S382. 5. هزینه: 3.3 pip، TP>SL. 6. داده: `data/mt5_full` با assert. 7. BUG-DIRSTR/GEOMDRIFT: هارنس ارثی. 8. حکم: فقط `compute_rqs2`؛ برچسب REDUNDANT فقط از قاعدهٔ پیش‌ثبت.

*— بلوک دوم، موج دوم: قانونی که موج اول نوشت، حالا باید خودش را روی کفِ تازه ثابت کند یا بشکند.*
