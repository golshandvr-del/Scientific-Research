# S985 — آغازِ سمّیتِ جریان با طبقه‌بندیِ حجمِ توده‌ای (BVC Flow-Toxicity Onset) — حکم رسمی موتور

**وضعیت: ❌ REJECT — RQS2 = 6.7 (حکم رسمی `compute_rqs2` v2.6، نه دستی) · رتبه D**

| | |
|---|---|
| پیش‌ثبت | `results/S985_PREREG_BVC_FLOW_TOXICITY_ONSET.md` (f8b29a17 — قبل از اسکنر) |
| الحاقیهٔ انجماد | `results/S985_ADDENDUM_FREEZE.md` (قبل از داور) |
| اسکنر / داور | `strategies/s985_bvc_toxicity_scan.py` / `strategies/s985_judge.py` |
| داده (src) | `data/mt5_full/XAUUSD_H6.csv` — 15,966 کندل (۱۵.۵۹ سال)؛ ستونِ `volume` = تیک‌ولومِ MT5 |
| مسیر چندگانگی | C — جست‌وجو نیمهٔ اول (7,983 کندل)، هولد-اوت نیمهٔ دوم، **یک** شلیک |
| خروجی موتور | `results/_s985/H6_rqs2_verdict.json`, `H6_trades.csv`, `judge_H6.log` |
| اسکنِ ۲۰ کارت | `results/_s985/scan_*.json` (۱۹ TF + H4 بازنمونه‌شده) |

## ۱) خط حکم موتور (verbatim)

```
S985_BvcToxicityOnset_XAUUSD-H6 | REJECT     RQS2=  6.7 | n= 176 WR=46.59% PF=1.07 lift=  +3.61pp z= 0.7 | H0:✓ H1:✗ H2:✗ H3:✗ H4:✗ H5:✗ H6:✗ H7:✗ H8:✗ H9:✓ H10:✗
```

## ۲) اعداد کلیدی

| کمیت | مقدار |
|---|---|
| پیکربندی منجمد | H6 · L=21 · κ=1.5 (θ=0.5945) · **mirror** (fade) · SL=1.8×ATR(100)=150.7pip · TP=196.0pip (RR 1.3) · max_hold 64 |
| n (کل بازه) | 176 (long 82 / short 94) |
| WR / PF / net | 46.59% / 1.067 / +747 pip |
| skill lift / z | +3.61pp / 0.68 |
| **skill_p_perm** | **0.248** (K=600، seed 20260806) |
| null_ref_wr | 42.98% |
| n_trials / z_luck_bound / z_margin | 960 / 3.243 / **−2.28** |
| n لازم برای H3 با این لیفت | ≈1,799 (داریم 176) |
| هولد-اوت (OOS, n=108) | WR 38.89% (لازم 44.43%) · PF 0.799 · net −1,322 pip |
| نیمه‌ها (net) | +2,396 / −1,322 — **معکوس** |
| کالیبراسیون ۴ربع | +1,215 / +1,038 / −653 / −742 (فقط ۲ از ۴ مثبت) |
| هم‌راستایی با درفت | aligned: WR 51.6%, exp +22.4pip · counter: WR 41.5%, exp −10.2pip (۹۴ از ۱۵۸ معامله counter) |
| maxDD / recovery / MCL | 17.5% / 0.33 / 7 |

## ۳) تفسیر

- **سدِ توان پیش از شلیک رد شده بود** (max z=+2.39 < 3.24) و شلیک فقط برای اجرای دستورِ «حکمِ رسمی برای هر شماره» انجام شد. حکم دقیقاً مطابقِ پیش‌بینیِ الحاقیه: REJECT. لیفتِ نیمهٔ اول (+14.4pp, n=68) در نیمهٔ دوم ناپدید شد (OOS WR 38.9% — زیرِ بی‌قید). یک بیشینهٔ نویز از ۹۶۰ آزمایش.
- **یافتهٔ ماندگار ۱ — TOX پروکسیِ انبساط است، نه اطلاعات:** پیش‌بینیِ (د) رد شد: لیفتِ main با κ **کاهش** می‌یابد (κ=1.5: −2.1pp، 2.5: −3.2، 3.5: −3.2). اگر TOX واقعاً جریانِ مطلع را می‌سنجید، آستانهٔ بالاتر باید اطلاعاتی‌تر بود. روی تیک‌ولومِ MT5، `Σ|OI|/ΣV` عملاً همان «نوسانِ تحقق‌یافته / نوسانِ EWM» وزن‌دار است — با S770/S800 هم‌جنس، اما بدونِ فیلترِ تداومِ آن‌ها. **طبقه‌بندیِ BVC هیچ اطلاعاتِ جهت‌داری فراتر از انبساطِ ساده به طلا اضافه نمی‌کند.**
- **یافتهٔ ماندگار ۲ — اولین شکستِ «main > mirror»:** در ۶ لایهٔ پیشینِ رنجِ من main پیوسته بهتر از mirror بود؛ اینجا میانگینِ z روی H1–H12 برابر است (−0.45 vs −0.46). تفسیر: علامتِ NF (جریانِ خالصِ ۲۱–۵۵ کندل) روی طلا **فاقدِ محتوای جهت‌دار** است — نه ادامه دارد، نه بازگشت. این با REJECTهای S942/S943/S946/S947/S975 (همهٔ خانوادهٔ حجم) هم‌خوان است: **تیک‌ولومِ MT5 روی XAUUSD در هیچ تبدیلی (surge، OBV، MFI، record، و اکنون BVC) سیگنالِ جهت‌دار نمی‌دهد.** خانوادهٔ حجم-جهت روی طلا بسته می‌شود.
- **یافتهٔ ماندگار ۳ — گرادیانِ مقیاس، ششمین تکرار:** M1 z≈−92 → M15 −3.9 → H1 −1.1 → H12 +0.1. هر رویدادِ ریزساختاریِ پرتکرار زیرِ M30 روی طلا سمّی است؛ این اکنون قانونِ سایت است، نه فرضیه.
- **درسِ روش‌شناختی:** پیش‌ثبت ۵ خطرِ «بدترین خبر اول» را فهرست کرد؛ خطرِ ۲ («خویشاوندی با انبساطِ نوسان») دقیقاً همان چیزی بود که رخ داد. ارزشِ پیش‌ثبتِ صادقانه در همین است — نتیجه قابلِ تفسیر است نه فقط «سوخت».
- پیش‌بینی‌های ثبت‌شده: (الف) **تأیید**؛ (ب) **رد ضعیف**؛ (ج) تأیید ضعیف (مثبت‌ها فقط H1–H12 ولی همه زیرِ نویز)؛ (د) **رد**.

## ۴) اثبات ۸ اشتباه

1. **نشتِ آینده؟** خیر — σ_{t−1} با شیفتِ ۱؛ TOX/NF فقط تا کندلِ جاری؛ ورودِ شبیه‌ساز در openِ کندلِ بعد؛ گرم‌شدنِ SIGMA_SPAN+L.
2. **جست‌وجو در هولد-اوت؟** خیر — اسکن ساختاری `iloc[:n//2]`؛ نیمهٔ دوم فقط یک بار با پیکربندیِ کامیت‌شده در الحاقیه (قبل از اجرا).
3. **حکمِ دستی؟** خیر — verbatim از `compute_rqs2` با ۵ ورودیِ اجباری (bar_time, close, null دو‌سویه, n_trials, split_bar).
4. **هزینه؟** spread 3.3pip + slip در `se.ASSETS`؛ expectancy@2×cost = +3.35pip (بی‌معنا با p=0.25).
5. **n_trials کم‌شمرده؟** خیر — ۹۶۰ (H4 بازنمونه‌شده هم شمرده شد، نه ۹۱۲).
6. **بازوی دوم؟** خیر — پس از حکم هیچ پارامتری لمس نشد.
7. **داده؟** `mt5_full` (assert در داور)؛ `vol_zero_frac=0` روی همهٔ کارت‌ها.
8. **دخالت در کارِ دیگران؟** خیر — فقط `S985*`, `results/_s985/`, `tools/s985_run_all.sh`.

## ۵) هم‌پوشانی

بدونِ ACCEPT ممیزیِ هم‌پوشانی لازم نیست. برای ثبت: خویشاوندیِ ریاضی با S770/S800 (انبساطِ نوسان) در پیش‌ثبت اعلام شده بود.

## ۶) وضعیتِ شماره

S985 **سوخت** (REJECT 6.7، ۳ یافتهٔ ماندگار). لایهٔ بعدی طبقِ قانونِ «+۱»: **S986**.
بلوک‌های S500–S979 متعلق به دانشمندانِ موازی و دست‌نخورده.

— آندری کولموگوروف، 2026-09-04
