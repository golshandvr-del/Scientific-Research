# S985 — الحاقیهٔ انجماد: حکم رسمی موتور (یک شلیک)

**تاریخ: 2026-09-04 — کامیت‌شده *قبل از* اجرای داور (نظم مسیر C).**
پیش‌ثبت: `results/S985_PREREG_BVC_FLOW_TOXICITY_ONSET.md` (f8b29a17). اسکنر: `strategies/s985_bvc_toxicity_scan.py` (f49f3932).
اسکن روی نیمهٔ اول کامل و کامیت‌شده است (`results/_s985/scan_*.json`، ۲۰ کارت: ۱۹ + H4 که لودر از H1 بازنمونه می‌کند).

## ۱) نتیجهٔ گام ۱ (اسکنِ نیمهٔ اول) — سدِ توان **رد شد**

| کمیت | مقدار | میله |
|---|---|---|
| بیشینهٔ z مثبت | **+2.39** (H6, L=21, κ=1.5, **mirror**, SL_k=1.8, RR=1.3, n=68, lift=+14.39pp) | `expected_max_z(960)=3.24` |
| بازوی اصلیِ برتر (main) | H1, L=55, κ=1.5, SL_k=1.8, RR=1.6, n=312, lift=+4.19pp, z=+1.51 | — |
| کارت‌های بی‌بازو (n<30) | MN1, W1, D1 | — |
| n_trials صادقانه | **960** (48 × ۲۰ کارت — H4 هم اسکن شد) | — |
| کالیبراسیونِ آماره | TOX میانگین ≈0.47 روی همهٔ TFها (پیش‌بینیِ H0: 0.5؛ کمی زیرِ آن = دنبالهٔ پهن) · `vol_zero=0` | ✓ |

**پیش‌بینی‌های ثبت‌شده:**
- (الف) زیرِ M30 سمّی — **تأیید** (M1 z=−92 میانگین، M3 −24، M5 −14 … M15 −3.9؛ ششمین تکرارِ گرادیانِ مقیاس).
- (ب) main > mirror — **مبهم/رد ضعیف**: بازوی برترِ کل mirror است (H6)، ولی میانگینِ z روی H1–H12: main −0.45 در برابر mirror −0.46 — تفاوتی نیست. اولین لایه در رنجِ من که main برتری نداشت.
- (ج) قلمروِ مثبت H3–H12 — **تأیید ضعیف**: همهٔ بازوهای z>+1 در H1–H12 هستند؛ اما هیچ‌کدام از سدِ نویز نمی‌گذرد.
- (د) لیفت یکنواخت در κ — **رد شد**: κ=1.5: −2.11pp، κ=2.5: −3.24pp، κ=3.5: −3.15pp (main، H1–H12). ⇒ طبق قاعدهٔ از-پیش-اعلام‌شده: **«TOX فقط پروکسیِ انبساط است، نه اطلاعات».**

## ۲) پیکربندیِ منجمد برای شلیکِ رسمی

قاعدهٔ انتخاب (همان S984): **بازوی با بیشترین z در اسکنِ نیمهٔ اول.**

| پارامتر | مقدار |
|---|---|
| کارت | **XAUUSD-H6** (`data/mt5_full/XAUUSD_H6.csv`) |
| رویداد | `toxicity_signals(oi, v, L=21, kappa=1.5)` — θ=0.5945 — عینِ اسکنر |
| جهت | **mirror** (NF>0 ⇒ SHORT، NF<0 ⇒ LONG — fade) |
| SL_k / RR | **1.8 / 1.3** (TP>SL) |
| sl_base_pip | منجمد از `scan_H6.json` (نیمهٔ اول) |
| max_hold | 64 · `allow_overlap=False` |
| split_bar | `n_all//2` |
| مدلِ صفر | `build_null_fast` K=600، seed=20260806، هر دو سمت |
| n_trials | **960** |
| داور | `strategies/s985_judge.py` |

**پیش‌بینیِ از-پیش-اعلام‌شده:** REJECT یا POWER-LIMITED (n≈68 در نیمه ⇒ n≈140 کل؛ لیفتِ +14pp با این n در سدِ نویزِ ۹۶۰ آزمایش تشخیص‌ناپذیر است؛ جهتِ mirror با ۶ تکرارِ قبلیِ main>mirror ناسازگار ⇒ احتمالِ بالای نویز). این شلیک برای اجرای دستورِ «حکمِ رسمیِ موتور برای هر شماره» است. پس از حکم هیچ پارامتری تغییر نمی‌کند و S985 بسته می‌شود.

— آندری کولموگوروف
