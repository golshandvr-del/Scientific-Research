# S744 — ادغامِ چندکارتهٔ «ردِ سطحِ رند» — XAUUSD (H12+H3+H6+D1) — **REJECT (12.0)**

**پیش‌ثبت**: `results/S744_PREREG_ROUND_LEVEL_POOL_PATH_C.md` (کامیت پیش از کد)
**کد**: `strategies/s744_round_pool.py` · **چک‌پوینت**: `results/_scan_S744/pool.json`
**داده**: `data/mt5_full/` (src در چک‌پوینت per-card) · **n_trials=48** · K_perm=500

## ۱) خلاصهٔ اجرا
- رویدادِ منجمد: `s743.build_signals(step=50, rej_k=0.25)`؛ اعضای منجمد {H2,H3,H6,H8,H12,D1}؛ W1 و M5 قرنطینه.
- اکتشاف (< T*=2020-06-08): برنده `k_sl=2.5, rr=1.5` با اعضای FIFO-باقی‌ماندهٔ
  {H12,H3,H6,D1} → n=333, WR=51.65, PF=1.337, lift=+11.0pp, z=4.09 — از آستانه‌ها گذشت.
- آزمونِ نهاییِ منجمدِ یگانه (کلِ ۱۵.۶ سال، FIFO تقویمی، نالِ ترکیبیِ اندازه‌گیری‌شده):
  - استخر: n_before=1717 → **n=696** پس از FIFO (سهم: H3≈۴۳٪, H6≈۲۸٪, H12≈۱۸٪, D1≈۱۰٪)
  - WR=49.14٪ · PF=1.11 · exp≈+۱۲pip · lift=+2.53pp · **z=1.2 · skill_p_perm≈0.12 (z=1.2)**
  - **حکم موتور: REJECT · RQS2=12.0**
  - دروازه‌ها: `H0:✓ H1:✗ H2:✓ H3:✗ H4:✗ H5:✗ H6:✓ H7:✗ H8:✗ H9:✓ H10:✗`

## ۲) تشخیصِ علمی — چرا شکست؟ (رقیق‌سازیِ کلاسیک، درسِ S351 تکرار شد)
liftهای **کلِ داده** نسبت به نالِ اندازه‌گیری‌شدهٔ هر عضو:
H12=+2.28 · H3=+1.41 · H6=+0.95 · D1=+2.22 (همه زیر ۳pp).
یعنی liftِ ۱۱ppِ اکتشاف عمدتاً **بختِ پنجرهٔ اکتشاف** بود (نیمهٔ اولِ داده)،
نه لبهٔ پایدار: در نیمهٔ دوم (holdout، ۳۶۲ معامله) H7 شکست. سطوحِ رندِ
۵۰دلاری در طلای ۱۵۰۰–۳۵۰۰ دلاریِ سال‌های اخیر همان چگالی/معنای دورهٔ
۱۲۰۰دلاری را ندارند — ناایستاییِ مقیاسِ قیمت، مکانیزمِ مشکوکِ فروپاشی.

## ۳) بستنِ خانواده (مرگِ ابدی)
طبق پیش‌ثبت §7: خانوادهٔ «سطوحِ رند با این تعریفِ رویداد» (S743+S744) برای
همیشه بسته است. W1 (که ۷/۱۱ دروازه گرفت) نیز holdoutاش مصرف شده — احیای آن ممنوع.

## ۴) اثباتِ پرهیز از ۸ خطای رایج
| # | خطا | شاهدِ پرهیز |
|---|---|---|
| ۱ | look-ahead | سیگنال از کندلِ بسته؛ ورود کندلِ بعد (queue_rr)؛ close محور با searchsorted(right)-1 |
| ۲ | survivorship در گزینش | حکمِ نهایی روی استخرِ FULL (همهٔ اعضای lift>0)، نه زیرمجموعهٔ گزینش‌شده |
| ۳ | نادیده‌گرفتنِ هزینه | spread=3.3pip در همهٔ شبیه‌سازی‌ها + H2/H9 |
| ۴ | بیش‌برازشِ چندگانه | مسیر C، شبکهٔ منجمدِ ۴تایی، n_trials=48 (24 موروثیِ S743) |
| ۵ | نالِ فرضی | نالِ اندازه‌گیری‌شده per-card (K=500، همان هندسه، صفِ اشغال) + ترکیبِ وزنیِ w² |
| ۶ | آلودگیِ holdout | T* تقویمیِ منجمد پیش از اکتشاف؛ W1/M5 قرنطینه؛ فقط یک آزمونِ نهایی |
| ۷ | تورمِ همپوشانی | FIFO تقویمی (n 1717→696) + allow_overlap=False + محورِ مشترکِ ۲h (بدونِ کلیپ) |
| ۸ | TP<SL پنهان | queue_rr: tp=max(rr·sl, sl) |

## ۵) میراث برای S745+
- خطِ «ساختارِ قیمتیِ ایستا (سطحِ دلاریِ ثابت)» در دارایی‌ای با روندِ سکولارِ
  قوی محکوم به ناایستایی است ⇒ رویدادهای بعدی باید **نسبی/تطبیقی** تعریف شوند.
- ادغامِ چندکارته سالم اجرا شد (FIFO، نالِ ترکیبی، holdout تقویمی) — ابزار
  معتبر ماند؛ مشکل لبهٔ خام بود، نه روشِ تجمیع.

— عمر خیام نیشابوری · S744 بسته شد · ۲۰۲۶-۰۸-۱۹
