# S382 — نامزد: `Williams %R` گذر به بالای −۱۳ | XAUUSD_H4 | lift خام +۷.۸۳

> **نوعِ سند:** **لایهٔ پذیرفته‌شده.**
> **حکم: ACCEPT — rqs2 = ۸۳.۵ | هر ۱۱ دروازه ✅ | rank_tier = A**
> **صفر فیلتر.** بودجهٔ معامله دست‌نخورده.
> سومین لایهٔ پذیرفته‌شدهٔ تاریخِ پروژه — و **نخستین با نمونهٔ کافی.**

---

## ۱. چیستیِ نامزد

| مشخصه | مقدار |
|-------|-------|
| قاعده | `Williams %R (14)` گذر به **بالای** −۱۳ |
| کارت | `XAUUSD_H4` (بازه: ۱۵.۵۳ سال) |
| جهت | **long** |
| SL | ۱.۵ × ATR(100) = **۱۲۲.۸۵ pip** |
| TP | ۱.۵ × SL = **۱۸۴.۲۸ pip** |
| فیلتر | **هیچ** |

### اعدادِ اندازه‌گیری‌شده

| کمیت | مقدار |
|------|-------|
| n (معاملات، بدونِ هم‌پوشانی) | **۸۶۹** |
| نرخ | ۵۵.۹ در سال |
| WR | ۴۸.۹۱٪ |
| سربه‌سرِ هزینه‌دار | ۴۱.۰۷٪ |
| **lift** | **+۷.۸۳** |
| میانگینِ مدتِ اشغال | ۸.۵ کندل |

---

## ۲. چرا این نامزد از هر چیزِ قبلیِ پروژه متمایز است

### ۲.۱ توانِ آماری، برای اولین بار، **کافی** است

S380 نشان داد ۶۴٪ ردهای آرشیو هیچ اطلاعاتی حمل نمی‌کنند، چون نمونه‌شان
حتی لبهٔ ۹ واحدی را نمی‌دید. آستانه‌ها:

| لبه | n لازم (توان ۸۰٪) | نامزد دارد؟ |
|-----|-------------------|-------------|
| ۵ واحد | ۷۸۴ | ✅ ۸۶۹ |
| ۹ واحد | ۲۴۲ | ✅ ۸۶۹ |

این **اولین بار** در تاریخِ پروژه است که نامزدی پیش از آزمون، توانِ کافی
دارد. یعنی نتیجه — هرچه باشد — **اطلاعات حمل می‌کند**، برخلافِ آن ۹ لایهٔ
سوختهٔ آرشیو.

### ۲.۲ بودجهٔ معامله **دست‌نخورده** است

S379 قانونِ هزینهٔ فیلتر را ثابت کرد: یک فیلترِ جهت‌متضاد ۹۹.۷٪ بودجه را
می‌خورد. این نامزد **هیچ فیلتری ندارد**، پس تمامِ بودجه‌اش باقی است. اگر
بعداً فیلتر لازم شود، از یک نقطهٔ شروعِ سالم خرج می‌شود نه از یک نقطهٔ
ورشکسته.

### ۲.۳ lift خام ~۳.۲× بزرگ‌ترِ S353

| لایه | lift خام (پیش از دروازه) |
|------|--------------------------|
| S166 → S353 (XAUUSD_D1) | +۲.۴۳ |
| **S382 (این نامزد)** | **+۷.۸۳** |

---

## ۳. بارِ چندگانگی — بی‌رحمانه‌ترین آزمون

کلِ جاروب: **۲۳٬۷۵۵ آزمون** (۲۴۸ قاعده × ۲ ضریبِ SL × ۳ rr × ۲ جهت × ۸ کارت).
با این حجم، انتظار داریم چند موردْ شانسی lift بالا بدهند. پس تصحیح لازم است.

### ۳.۱ توزیعِ زمینه (نویزِ مرجع)

| کمیت | مقدار |
|------|-------|
| میانهٔ lift کلِ ۲۳٬۷۵۵ آزمون | **−۴.۴۲** |
| میانگین | −۵.۱۶ |
| انحرافِ معیار | ۴.۳۲ |
| کمینه / بیشینه | −۲۷.۷۷ / +۱۴.۳۸ |
| کسرِ مثبت | **۶.۸٪** |

**تفسیرِ مهم:** میانهٔ منفی تصادفی نیست — هزینهٔ ۳.۳ pip اسپرد به‌طور
سیستماتیک همه را منفی می‌کند. این تأییدی است که شبیه‌ساز هزینه را
واقعاً اعمال می‌کند و خوش‌بینیِ دروغین تولید نمی‌کند.

### ۳.۲ نتیجهٔ تصحیحِ بونفرونی

| SL_k | rr | n | lift | z | p یک‌طرفه | **p بونفرونی** | حکم |
|------|----|----|------|---|-----------|----------------|-----|
| **۱.۵** | **۱.۵** | ۸۶۹ | **+۷.۸۳** | **۴.۶۹** | ۱.۳۴e−۶ | **۰.۰۳۱۹** | ✅ **PASS** |
| ۲.۰ | ۱.۰ | ۸۴۴ | +۶.۶۹ | ۳.۸۹ | ۵.۰۱e−۵ | ۱.۰۰۰۰ | ❌ |
| ۱.۵ | ۲.۰ | ۷۹۱ | +۶.۱۰ | ۳.۶۲ | ۱.۵۰e−۴ | ۱.۰۰۰۰ | ❌ |
| ۲.۰ | ۱.۵ | ۷۱۸ | +۵.۸۵ | ۳.۱۹ | ۷.۱۱e−۴ | ۱.۰۰۰۰ | ❌ |
| ۲.۰ | ۲.۰ | ۶۳۲ | +۵.۳۹ | ۲.۸۶ | ۲.۱۰e−۳ | ۱.۰۰۰۰ | ❌ |

---

## ۴. تصحیحِ صادقانهٔ ادعایِ خودم

هنگامِ دیدنِ اولیهٔ نتایج، من گفتم «lift در سه هندسه مثبت و بالای ۶ است،
پس پایدار است». **این ادعا را تضعیف می‌کنم.**

پس از محاسبهٔ بونفرونی، تنها **یک** هندسه از پنج، معناداری تصحیح‌شده دارد.
بقیه p بونفرونیِ ۱.۰ می‌دهند. پس صورتِ درستِ ادعا این است:

> ✅ **جهتِ** lift در هر پنج هندسه یکسان و مثبت است (شاهدِ ضعیفِ اصالت)
> ❌ **معناداریِ تصحیح‌شده** فقط در یک هندسه برقرار است

این تفاوت اهمیتِ عملی دارد: نمی‌توان گفت «هر هندسه‌ای کار می‌کند». تنها
`k=1.5, rr=1.5` مدرکِ آماریِ کافی دارد و بقیه تنها هم‌جهت‌اند.

### ۴.۱ محدودیتِ بونفرونی — در جهتِ مخالف

بونفرونی فرض می‌کند ۲۳٬۷۵۵ آزمون **کاملاً مستقل**‌اند. این غلط و
شدیداً بدبینانه است، چون:

- همان قاعده با ۶ هندسه تکرار شده (۶ آزمونِ شدیداً همبسته)
- `rsi14`, `rsi21`, `rsi27` تقریباً یک چیز را می‌سنجند
- کارت‌های H1/H4 از همان سریِ قیمت مشتق شده‌اند

پس آزمون‌های **مؤثر** بسیار کمتر از ۲۳٬۷۵۵ است. بونفرونی یک **کرانِ
بالای بدبینانه** است، و اینکه نامزد از آن هم گذشته، شاهدِ قوی‌تری است تا
اگر با تصحیحِ دقیق‌تر (مثلِ `effective_trials` در `engine/rqs2.py`) سنجیده
می‌شد.

---

## ۵. چرا Williams %R > −۱۳؟ منطقِ اقتصادی

**نکتهٔ ظریفِ ضدِ شهود:** `%R > −۱۳` یعنی قیمت در **۱۳٪ بالاییِ** دامنهٔ
۱۴-کندلی است — یعنی ناحیهٔ **اشباعِ خرید**. و ما در آن نقطه **long**
می‌زنیم، نه short.

این خلافِ خواندنِ کلاسیکِ Williams %R است (که اشباعِ خرید = سیگنالِ فروش).
پس این نامزد یک **سیگنالِ مومنتومی** است، نه بازگشتی: خریدنِ قدرت.

**و این دقیقاً چیزی است که طراحیِ ابزار اجازهٔ کشفش را داد** —
`side_of()` هر دو جهت را می‌آزماید و اجازه می‌دهد **داده** تصمیم بگیرد،
نه پیش‌داوریِ سبک. اگر خواندنِ کلاسیک را فرض کرده بودیم (short در اشباعِ
خرید)، این نامزد هرگز دیده نمی‌شد.

اینکه روی طلا مومنتوم کار می‌کند و بازگشت نه، با یافتهٔ کلیِ جاروب
سازگار است: liftهای مثبت شدیداً روی **طلا** خوشه بسته‌اند
(۹۲۴ روی H4 طلا در برابرِ **صفر** روی M30 و M15 یورو).

---

## ۶. نتیجهٔ کاملِ rqs2 — هر ۱۱ دروازه پاس

| دروازهٔ rqs2 | وضعیت | مقدارِ اندازه‌گیری‌شده |
|--------------|-------|----------------------|
| حجمِ نمونه | ✅ | n = ۸۶۹ (آستانهٔ توانِ ۸۰٪ = ۷۸۴) |
| سربه‌سرِ هزینه‌دار | ✅ | lift = +۷.۸۳ |
| چندگانگی (بونفرونی) | ✅ | p_adj = ۰.۰۲۸۱ روی ۲۳٬۷۵۵ آزمون |
| **جایگشت (۲۰۰۰ تکرار)** | ✅ | z = ۴.۷۵۴ در برابر کرانِ شانسِ ۴.۰۶۷ ⇒ حاشیه +۰.۶۸۷ |
| **خریدارِ کور (مدلِ صفر)** | ✅ | آلفا = ۴۸.۹۱ − ۴۰.۹۸ = **+۷.۹۳** |
| **پایداریِ پنجره‌های تقویمی** | ✅ | هر ۴ پنجره سودده (۵۸۳/۲۸۴۴/۱۰۱۴۷/۱۳۲۱۲) |
| **پایداریِ دو نیمه** | ✅ | هر دو نیمه سودده (۳۵۷۵ / ۳۶۹۶۷) |
| **بیشینهٔ زیانِ متوالی** | ✅ | ۱۲ در برابر مجازِ ۱۶ |
| **افتِ سرمایه** | ✅ | ۵.۶٪ | 
| **تمرکزِ سود** | ✅ | بزرگ‌ترین برد = ۰.۲۴٪ سودِ ناخالص |
| **مقاومتِ هزینه** | ✅ | انتظار در هزینهٔ **دوبرابر** هنوز +۲۴.۰۶ pip |
| **خارج‌ازنمونه** | ✅ | WR = ۴۹.۷۳٪ روی n=۳۶۴ (**بهتر** از داخل‌نمونه) |
| **رانشِ ضدِ رژیم** | ✅ | §۶.۱ |
| شبیه‌سازِ رویدادمحورِ کامل | ✅ | PF = ۱.۴۶۷، net = ۵۴٬۰۹۸.۸ |

### ۶.۱ رانشِ ضدِ رژیم — قاطع‌ترین شاهدِ «آلفا، نه بتا»

جدی‌ترین ایرادِ ممکن به این لایه: *«فقط-خرید روی طلاست که ۱۵ سال صعود
کرده — پس این بتای بازار است نه مهارت.»*

شبیه‌ساز، ۸۶۹ معامله را بر حسبِ رانشِ رژیمِ ۲۸۰ روزه تفکیک کرد:

| زیرمجموعه | n | WR | انتظار (pip) |
|-----------|---|-----|--------------|
| **ضدِّ رانش** (خلافِ روندِ ۲۸۰ روزه) | **۲۴۰** | **۵۰.۰۰٪** | **۳۰.۷۱** |
| هم‌جهت با رانش | ۵۵۸ | ۴۸.۲۱٪ | ۲۵.۲۱ |
| غیرقابلِ داوری | ۷۱ | — | — |

**لایه در ضدِّ روند بهتر عمل می‌کند، نه بدتر:** WR بالاتر (+۱.۷۹) و انتظارِ
بالاتر (+۵.۵۰ pip، یعنی ۲۲٪ بیشتر). اگر لبه صرفاً سواری بر رانشِ صعودی
بود، زیرمجموعهٔ ضدِّ رانش باید فرومی‌ریخت. نریخت — **بهتر شد.**

این ایراد را نه با استدلال، بلکه با تفکیکِ عددیِ ۲۴۰ معاملهٔ ضدِّ رژیم
ابطال می‌کند، و مکملِ سه مرجعِ همگرای مستقل روی ~۴۱٪ است
(سربه‌سرِ هندسی ۴۱.۰۷، خریدارِ کور ۴۰.۵۴، میانگینِ ۲۰۰۰ جایگشت ۴۰.۹۸).

**هدفِ سایت:** نرخِ ۵۵.۹/سال ⇒ حدودِ یک سیگنال هر ۴.۵ روز. کفِ معیار
(۵۰/سال) را می‌زند ولی از هدفِ روزی-۱-سیگنال (۲۵۲/سال) **۴.۵× دور** است.
هیچ‌یک از ۲۳٬۷۵۵ آزمون هم‌زمان lift≥۵ و نرخ≥۲۵۲ نداشت.

---

## ۷. رعایتِ ضدِ اشتباهاتِ رایج

| اشتباه | مدرکِ عددی |
|--------|-----------|
| #۱ تنها لایهٔ زمان‌محور | هیچ شرطِ زمانی در نامزد نیست؛ Williams %R یک نوسان‌سنجِ دامنه‌ای است |
| #۲ اجتنابِ پیچیدگی | جهتِ ضدِ شهودی (long در اشباعِ خرید) پذیرفته شد چون داده گفت، نه شهود |
| #۳ چند اندیکاتورِ ساده | ۲۴۸ قاعده از ۶ خانوادهٔ متفاوت جاروب شد، نه ۲-۳ اندیکاتور |
| #۴ سایت بدونِ local mobile | هیچ تغییرِ سایتی — نامزد هنوز تأیید نشده |
| #۵ تک تایم‌فریم | هر ۸ کارتِ قابلِ استفاده جاروب شد (۲ ارز × ۴ تایم‌فریم) |
| #۶ TP/SL یکسان | SL = k×ATR(100) **همان کارت** ⇒ H4 طلا ۱۲۲.۸۵ pip، M30 ۶۱.۱ pip |
| #۷ اعدادِ رند | آستانهٔ −۱۳ (نه −۲۰)، BB مضربِ ۲.۶۱۸، دوره‌های ۲۷/۳۳/۴۷/۵۵/۶۷/۱۱۲/۱۳۵/۱۷۰ |
| #۸ دورزدنِ معیار | rr≥۱ همیشه؛ برندهٔ نهایی rr=۱.۵ یعنی **TP > SL**؛ و داوری با سربه‌سر نه WR خام |

---

## ۸. حکمِ نهایی و جایگاه در پروژه

```
S382_WilliamsR_XAUUSD_H4 | ACCEPT | rqs2 = 83.5 | rank_tier = A
n=869  WR=48.91%  PF=1.467  lift=+7.83pp  alpha=+7.93pp  z=4.754
H0..H10: ✓✓✓✓✓✓✓✓✓✓✓  (11/11)   n_fail=0  n_unknown=0  power_limited=False
```

### چرا این پذیرش از دو پذیرشِ قبلیِ پروژه متمایز است

| | S354 / S356 | S355 | **S382** |
|---|---|---|---|
| نمونه | کم | کم | **۸۶۹** |
| توانِ کافی برای دیدنِ لبهٔ خودش؟ | مرزی | مرزی | **بله (۷۸۴ لازم)** |
| تعدادِ فیلتر | چند | چند | **صفر** |
| بارِ چندگانگی پرداخت‌شده؟ | جزئی | جزئی | **کاملِ ۲۳٬۷۵۵ آزمون** |
| مدلِ صفرِ ساخته‌شده؟ | خیر | خیر | **بله (۳ مرجعِ مستقل)** |

**صفر فیلتر** یعنی این لایه بودجهٔ معاملهٔ خود را دست‌نخورده نگه داشته و
طبق قانونِ هزینهٔ فیلتر (S379) هیچ‌یک از ۹۹.۲٪ اتلافِ بودجهٔ تاریخیِ پروژه
را مرتکب نشده. همچنین یعنی **جای خالی برای بهبود دارد**: هر فیلترِ مؤثری
که بعداً کشف شود، می‌تواند روی این لایه سوار شود.

### محدودیتِ صریح

نرخِ ۵۵.۹/سال کفِ معیار (۵۰/سال) را می‌زند ولی از هدفِ روزی-۱-سیگنال
(۲۵۲/سال) **۴.۵× دور** است. این محدودیت با S383 و S384 توضیح داده شد:
هدفِ ۲۵۲ با ساختارِ هزینهٔ حساب در تضاد است، نه فقط سخت‌تر از معیار.

**راهِ باقی‌مانده برای رسیدن به هدفِ سایت: تجمیعِ پرتفوی (راهِ ۲).**
این لایه سنگ‌بنایِ آن است: اگر ۴ لایهٔ مستقلِ هم‌کیفیت روی کارت‌های
مختلف پیدا شود، مجموع به ۲۵۲ می‌رسد **بدونِ** پرداختِ جریمهٔ اشغال که
راهِ ۱ را کشت.

---

## 🔌 اتصال به سایت و `local-mobile` — انجام شد

> این بخش **بعد از** پذیرش نوشته شد و مدرکِ اتصال را ثبت می‌کند، نه ادعای آن.

**فایل‌های تغییریافته:**

| فایل | نقش |
|------|-----|
| `web_tool/src/williams_momentum_s382.ts` | ماژولِ لایه (پورتِ verbatim از پایتون) |
| `web_tool/src/strategy_registry.ts` | `import` + آداپترِ `s382Layer` + اتصال به `XAUUSD-H4` |
| `local-mobile/app.bundle.mjs` | بازسازی: ۵۱۹٬۰۵۴ → ۵۳۶٬۸۹۰ بایت (+۱۷.۸KB) |
| `local-mobile/_parity_s382_williams.mjs` | آزمونِ صحتِ عددی (دروازهٔ اتصال) |
| `local-mobile/_probe_s382_wiring.mjs` | بازرسِ اتصال در مسیرِ تصمیمِ زنده |

**اولویتِ نخستِ کارت** شد. سه دلیلِ اندازه‌گیری‌شده:

1. **توانِ آماری در نقطهٔ استفاده:** ۸۶۹ رویدادِ مستقل (۵۵.۹/سال). لایهٔ
   پیشینِ اولویت‌دار (`S374`) شمارِ خامِ بزرگ‌تری (۱٬۰۶۲) دارد ولی سندِ
   خودش ~۴ رویدادِ **مستقل** در سال و ۴۳ کندلِ متمایز در ۱۰.۸ سال ثبت
   کرده ⇒ شواهدش **خوشه‌ای** است، شواهدِ این لایه نیست.
2. **تنها لایه‌ای که زیرِ کرانِ نو بازداوری و تأیید شد** (`S395`):
   `M_eff = ۲۷۸٬۴۴۷` ⇒ سد `۴.۶۰۸۶`؛ این لایه `z = ۵.۰۲۳۶` ⇒ حاشیهٔ `+۰.۴۱۵۰`.
3. **صفر فیلتر** ⇒ بودجهٔ معامله دست‌نخورده (قانونِ هزینهٔ فیلترِ `S379`).

### مدرکِ صحتِ عددی — `PARITY PASS`

| آزمون | نتیجه |
|-------|-------|
| `williamsR(14)` در برابرِ مرجعِ **مستقل** | بیشینه اختلاف `0.00e+0` روی ۲۳٬۷۴۲ کندل |
| `atrWilder(100)` (α=1/p) در برابرِ مرجع | بیشینه اختلاف `1.28e-13` روی ۲۳٬۷۵۵ کندل |
| «گذر» رویداد است نه حالت | ۱۴۷۷ گذر در برابرِ ۳۷۸۸ حالت = **۲.۵۶×** |
| نشتِ آینده (۱۲ نقطهٔ بریده‌شده) | **صفر** ناسازگاری |
| `TP > SL` | ساختاراً تضمین‌شده (`rr = ۱.۵`) |
| هر سه حالت زنده‌اند | `ENTRY=۲` · `APPROACHING=۲` · `NEUTRAL=۹۶` روی ۱۰۰ نمونه |

⚠️ مرجع‌ها **عمداً** نسخهٔ دومِ ساده‌نویسی‌شده داخلِ خودِ آزمون‌اند، نه
فراخوانیِ توابعِ ماژول — آزمونی که کدِ خودش را صدا بزند هیچ‌چیز را اثبات نمی‌کند.

### مدرکِ اتصال در مسیرِ تصمیمِ زنده

۳۰۰ بازپخش روی تاریخِ بریده‌شده، **صفر خطا**؛ لایه در **۲۱ مورد `ENTRY`** و
۹ مورد `APPROACHING` در خروجیِ `runCard` ظاهر شد. کارتِ `XAUUSD-H4` از
۳ لایه به **۴** رسید. نمونهٔ ورود: `entry=3113.36 · TP=3142.84 · SL=3093.70 · rr=1:1.50`.

پرسشِ «چرا در تستِ زنده `otherLayers=0` بود؟» با اندازه‌گیری پاسخ گرفت:
`%R` جاری `−۶۷.۹۰` است و ۵۴.۹ واحد **زیرِ** آستانه ⇒ حالتِ `NEUTRAL` تنها
خروجیِ صادقانه است، و `otherLayers` طبقِ طراحی فقط `ENTRY`/`APPROACHING` را
حمل می‌کند. یعنی **رفتارِ صحیح** بود نه باگِ اتصال.

### ⚠️ کشفِ همین مرحله — نکتهٔ صداقتِ هندسه

اندازه‌گیری روی ۲۳٬۷۵۵ کندلِ همین کارت:

| کمیت | مقدار |
|------|-------|
| میانهٔ `ATR(100)` | `۸.۱۹۰` ⇒ `SL = ۱۲۲.۹` pip ✔ **عیناً عددِ قفل‌شدهٔ سند** |
| `ATR(100)` جاری | `۳۸.۷۷۶` ⇒ `SL` آزاد `۵۸۱.۶` pip = **۴.۷۳×** |
| داخلِ بندِ `[۰.۶۰×, ۱.۶۰×]` | **۷۱.۱٪** تاریخ |
| بالای بند / زیرِ بند | `۱۶.۴٪` / `۱۲.۵٪` |

میانهٔ اندازه‌گیری‌شده عددِ `۱۲۲.۸۵۴` سند را **مستقلاً تأیید** می‌کند.
ولی وضعیتِ **فعلی** بیرونِ بند است. خطر در **هر دو جهت** واقعی است:

* `ATR` بسیار بالا ⇒ بستنِ `SL` روی سقفِ بند = استاپی **تنگ‌تر از نوسانِ
  جاری** ⇒ خروجِ تصادفی؛ `WR` اندازه‌گیری‌شده منتقل نمی‌شود.
* `ATR` بسیار پایین ⇒ استاپی **پهن‌تر از لازم** ⇒ ریسکِ دلاریِ بیش از
  پروفایلی که لایه با آن پذیرفته شد.

**تصمیم:** بند حفظ شد (رهاکردنِ براکت تا ۵۸۱ pip حکمِ اندازه‌گیری‌شده را
باطل می‌کرد) **و** وضعیت به کاربر **صریحاً گفته می‌شود** — هم سطرِ
«تطابقِ نوسانِ جاری با بازهٔ آزموده‌شده» در جدولِ شاخص‌ها (با `status=bad`)
و هم داخلِ متنِ دلیلِ ورود همراه با اقدامِ پیشنهادی (کوچک‌کردنِ حجم یا صبر).

**چرا این ناقضِ قانونِ «هیچ اطلاعِ اضافه» نیست؟** آن قانون سودِ انباشته و
شمارِ استراتژی‌های آزموده را ممنوع می‌کند — اطلاعاتی که **تصمیم را عوض
نمی‌کنند**. این هشدار تصمیم را **عوض می‌کند**: به کاربر می‌گوید حجم را کم
کند یا صبر کند. اطلاعی که رفتار را تغییر می‌دهد، اضافه نیست. و پنهان‌کردنش
یعنی کاربری که براکتی را کنارِ نمرهٔ `۸۳.۵` می‌بیند، گمان کند آن براکت
آزموده شده — در حالی که نشده.

---

_وضعیت: ✅ **پذیرفته‌شده و متصل.** هر ۱۱ دروازه پاس (و زیرِ کرانِ نو بازتأیید).
سومین لایهٔ پروژه، نخستین با نمونهٔ کافی، و اولویتِ نخستِ کارتِ `XAUUSD-H4`._
