# S626 — Round-Number Rejection NATIVE-COARSE + Drift — XAUUSD H1/H2/H4 — RQS2=0 — UNPROVEN

**دانشمند:** اقلیدس (بلوک S620–S629) · **تاریخ:** 2026-08-21
**PREREG:** `results/S626_PREREG_XAUUSD_round_number_native_coarse_drift_pathC.md` (commit `e527f08b` — قبل از هر کد)
**Path C:** جستجو فقط `[0, n/2)` · هولد-اوت **بکر ماند** · تست نهایی اجرا نشد
**Checkpoints:** `6f311032` (H4), `4fdfecbf` (H1/H2) · SEED=20260818 · n_trials=48

---

## ۱. خلاصهٔ حکم

**سوخته در جستجو — اما با ارزشمندترین نوع مرگ بلوک: قحطی-n با سیگنال ساختارمند.**
هیچ‌یک از ۴۸ پیکربندی هر ۵ شرط فریز را هم‌زمان رد نکرد. نزدیک‌ترین: H2 G=$50 c=0.1 k=2 RR=1.5 → lift=+10.42pp، exp=+1.54 ✓ ولی **n=13 در برابر n_req=177** — شرط ۲ با فاصلهٔ ۱۴ برابر شکست.

## ۲. نتایج کلیدی (نیمهٔ جستجو، null درفت‌-شرطی)

| TF | بهترین cfg | n | lift(pp) | exp_pip | n_req | حکم |
|---|---|---|---|---|---|---|
| H1 | G=50 c=0.1 k=1.5 RR=1.5 | 23 | **+15.43** | −2.45 | 72 | ✗n ✗exp |
| H2 | G=50 c=0.1 k=2 RR=1.5 | 13 | **+10.42** | **+1.54** | 177 | ✗n (تنها exp مثبت) |
| H4 | G=50 c=0.1 k=1.5 RR=1.5 | 15 | **+11.00** | −4.32 | 136 | ✗n ✗exp |

رویدادها در نیم‌پنجره: H1: 14–50 · H2: 8–29 · H4: 9–27. مبناهای درفت‌-شرطی WR≈22–30٪ (بسته به هندسه) با exp منفی — یعنی lift ها نسبت به «ورود تصادفی در رژیم صعودی» سنجیده شده‌اند، نه بتای درفت (درس S346 رعایت شد).

## ۳. یافتهٔ علمی: تمایز بنیادی با S624

این مرگ از جنس S624 (قلهٔ منزوی) **نیست**:

- **پیوستگی همسایگی برقرار است:** در c=0.1، هر ۳ TF × هر ۴ هندسه lift مثبت +5 تا +15pp دارند. سیگنال در کل همسایگی پارامتری هم‌علامت است — دقیقاً همان معیاری که S624 در آن مرد.
- **جهت اثر با نظریه سازگار است:** بومی‌سازی درشت، اطلاعات S622 را زنده نگه داشت (بر خلاف پیوند S623 که آن را کشت). c=0.1 (تلورانس سخت‌گیرتر) همه‌جا از c=0.2 قوی‌تر است — انتخاب‌گری واقعی سطح.
- **مشکل صرفاً power است:** اعداد رند $50/$100 در نیم‌پنجرهٔ H1–H4 آن‌قدر کم لمس می‌شوند (۸–۵۰ رویداد) که هیچ حجم نمونه‌ای ممکن نیست.

**قضیهٔ تنگنای رند-درشت (چهارمین قضیهٔ بلوک):** برای رویدادهای سطح-محور روی طلا، شمار رویداد با گرید سطح و درشتی TF هر دو معکوس می‌افتد؛ در ناحیه‌ای که هزینه/ATR قابل‌تحمل می‌شود (H1+)، نرخ رویداد به زیر آستانهٔ power می‌افتد. خانوادهٔ اعداد رند در **هر دو انتهای طیف** به بن‌بست فیزیکی می‌خورد: ریز = دفن زیر هزینه (S622)، درشت = قحطی نمونه (S626). پیوند بین‌شان هم مرده (S623).

⇒ **خانوادهٔ اعداد رند با سه شکست متعامد برای همیشه بسته شد** (طبق بند ابطال‌پذیری پیش‌ثبت).

### چرا n-extension نمی‌زنم
وسوسه: «نیمهٔ دوم را هم اضافه کن تا n برسد.» ممنوع — نیمهٔ دوم هولد-اوت Path C است؛ بازکردنش برای شکار n یعنی آلودگی کامل (درس S501: revival بدون معیار مستقل = مرگ). هولد-اوت بکر می‌ماند.

## ۴. ارزیابی پنج شرط فریز

| شرط | نتیجه |
|---|---|
| ۱. lift≥4pp | ✓ چندین مورد (تا +15.4) |
| ۲. n≥n_required | ✗ **همه شکست** (بهترین نسبت: 13/177) |
| ۳. exp_pip>0 | ✗ جز یک مورد (H2: +1.54) |
| ۴. پیوستگی همسایگی | ✓ برقرار در c=0.1 |
| ۵. بیشینهٔ lift·√n | H1: 74 ≪ 309 |

**هیچ‌کس هر ۵ شرط را نگذراند ⇒ سوخته، هولد-اوت بکر، ادامه به S627.**

## ۵. ترازنامهٔ بلوک

- ۷ لایه (S620–S626)، ۷ مرگ تمیز، ۰ آلودگی هولد-اوت
- ۴ قضیه: سقف خانواده · مرز معامله‌پذیری · فروپاشی افق · تنگنای رند-درشت
- ۲ دکترین: انزوای پارامتری (S624) · بی‌اطلاعی شکلی (S625)
- بدهی جستجوی تجمعی: 4344 + 48 = **۴۳۹۲ trial** (همه پیش‌ثبت، همه نیمهٔ اول)

## ۶. رعایت قوانین

✅ PREREG قبل از کد (`e527f08b`) · ✅ Path C، هولد-اوت دست‌نخورده · ✅ null اندازه‌گیری‌شدهٔ **شرطی‌شده بر درفت** (S346) · ✅ TP≥SL · ✅ شمارش صادقانه ۴۸ · ✅ checkpoint per-TF · ✅ EURUSD و شماره‌های دیگران لمس نشد

**حکم نهایی: UNPROVEN — rqs2=0 — خانوادهٔ اعداد رند برای همیشه بسته (S622+S623+S626).**
