# S1891 — پیش‌ثبت: «نردبانِ شوکِ بدنه» (Body-Shock Ladder) · XAUUSD

> ⏱ **commit و push پیش از هر اجرا.** موتور: RQS2 v2.6 · نویسنده: جورج سوروس (S1890–S1899) · تاریخ: 2026-09-05 · داده: `data/mt5_full/` ۱۵.۶ سال (src در هر JSON)

## ۰. مرور موازی‌ها و انگیزه
جدید: ACCEPT S1913/S955 (پرشِ S950 × آرامش، H8)؛ S1514 ACCEPT-REDUNDANT؛ POWER-LIMITED S535/S977؛ REJECT S1513/S1522/S548/S931. الگوی غالب: پایهٔ نادر (n≈100–150) × گیتِ رژیم. **هیچ‌کس پایهٔ پرتکرارتر با بزرگیِ متوسط را نساخته.**
درسِ S1890: پرتکرار بدونِ بزرگی = صفر. درسِ دههٔ اول: نادر + فیلتر = بی‌توان. **حدِ وسط:** بزرگی را با **بدنه** (نه دامنه) بسنجیم — بدنه خودش ρ را حمل می‌کند (بدنهٔ ≥1.272×ATR به‌ندرت ρ<0.618 دارد: ۷۳۹ از ۸۱۸)، پس یک شرط کمتر و n چند برابر.
**بکارت:** grep «body shock / شوکِ بدنه / |close−open| ≥ k×ATR / large body» ⇒ فقط S545 (شتابِ بدنه، نسبتِ دو بدنه) و S968 (مسیرِ هموار). هیچ لایه‌ای «قدرِ مطلقِ بدنهٔ H8 نسبت به ATR21» را به‌عنوانِ رویدادِ اصلیِ follow نیازموده. شمارش (فقط از دادهٔ H8، بدونِ PnL): k=1.272 → ۸۱۸، k=1.618 → ۳۹۴، k=2.0 → ۱۹۷ (S965: ۱۴۸).

## ۱. فرضیه
اطلاعاتِ کایل در **جابجاییِ خالصِ سشن** (بدنه) است، نه در دامنه؛ دامنهٔ بزرگ با بدنهٔ کوچک = نبردِ بی‌نتیجه، دامنهٔ متوسط با بدنهٔ کامل = پذیرشِ یک‌طرفه. پیش‌بینیِ ابطال‌پذیر (نردبان): lift باید در k **یکنوا** باشد (k بزرگ‌تر ⇒ lift بیشتر، n کمتر)؛ اگر یکنوایی شکست، «بزرگی» توضیح‌دهنده نیست.

## ۲. تعریفِ منجمد
- **رویداد (H8 لنگرشده ۰۰/۰۸/۱۶):** `|close−open| ≥ k × ATR21[i−1]` (ATR علّی)، k ∈ **{1.272, 1.618, 2.0}** (نردبان؛ اعدادِ فیبوناچی‌واره، نه گِرد). جهت follow. ورود open کندلِ بعد.
- **براکت/هولد (S965، منجمد):** SL=1.272×ATR21[i−1]، TP=2.058×ATR21[i−1]، max_hold=16، allow_overlap=False، اسپرد 3.3pip.
- **چند-TF (قانون):** همان رویداد روی H6، H12، H4، D1 هم (پیش‌بینی: مرگ در غیرِ H8 طبق S898/S899)؛ TFهای ریزتر با hold=16 معنای سشنی ندارند ⇒ INCOMPLETE پیش‌اعلام. ⇒ ۵ TF × ۳ k = **۱۵ کارت، n_trials=15**.
- **قفلِ کارتِ رسمی (مسیر C):** روی H8، k از میانِ ۳ نردبان با بیشینهٔ `t_pnl` در ۷۰٪ اول انتخاب و قفل می‌شود؛ یک `compute_rqs2` روی دادهٔ کامل با `split_bar`. اهلیت: n_IS≥150 و exp@2×cost_IS>0؛ بدونِ اهل ⇒ UNPROVEN. **حکمِ رسمیِ لایه = کارتِ H8 قفل‌شده.** کارت‌های دیگر اطلاعی، همه گزارش می‌شوند.
- **P1 (نردبان، IS):** lift(k=2.0) ≥ lift(1.618) ≥ lift(1.272). **P2:** lift(k قفل‌شده) در برابر S965 (R≥2.618∧ρ≥0.618) روی همان IS — اگر بدنه‌محور با n بزرگ‌تر lift هم‌اندازه داشت، تعریفِ بهتری از «مطلع» است.
- نالِ کانونیِ هندسه‌همتا: ۳ قرعهٔ بی‌قید {1891,2891,3891} (ref=max) + جایگشتِ زمانی K=500 بذر 1891 با همان n_sig، هر جهت جدا.

## ۳. پیش‌بینیِ شخصی
H8: k=1.618 قفل می‌شود (تعادلِ n≈390/lift)؛ lift +8..+12pp، z 2.5–4، PF 1.35–1.5. P(ACCEPT)≈۳۰٪ — بهترین شانسِ هر دو دهه. k=1.272: lift +5..+8 (رقیق). H6/H12: lift نیمه؛ D1: n کم.

## ۴. تعهدها
حکمِ موتور عیناً در نامِ فایل · commit تدریجی هر کارت · دادهٔ کامل mt5_full · EURUSD هرگز · هیچ فایلِ دانشمندِ دیگری لمس نمی‌شود · اگر ACCEPT: توقف، حسابرسیِ هم‌پوشانی با S965/S966/S1911/S919 (زمانِ ورود)، گزارش به کاربر · شکست ⇒ S1892.
