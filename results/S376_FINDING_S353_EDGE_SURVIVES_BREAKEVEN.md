# S376 — یافته: لبهٔ S353 زیرِ حسابداریِ سربه‌سر **زنده می‌مانَد**

> **نوعِ سند:** اعتبارسنجیِ مستقلِ یک لایهٔ POWER-LIMITED با معیارِ سربه‌سرِ سندِ S375.
> **Δ سودِ خالص = ۰** (هنوز هیچ لایه‌ای مستقر نشد). رکورد دست‌نخورده.

---

## ۰. چرا این آزمون لازم بود

سندِ **S375** ثابت کرد که «لیفت» نسبت به نرخِ بردِ بی‌قید (~۵۲٪) یک مرجعِ نادرست
است، و هر ۱۸ آزمونی که در صدرِ صفِ بازآزمایی نشسته بودند مصداقِ اشتباهِ رایجِ #۸
بودند: TP کوچک‌تر از SL ⇒ نرخِ بردِ بالا **بدونِ** لبه.

پرسشِ فوریِ بعدی: **آیا لایه‌های POWER-LIMITEDِ پروژه هم همین بیماری را دارند؟**
اگر بله، «اتاقِ انتظار» پروژه پر از توهم است. اگر نه، آن‌ها لبهٔ واقعی‌اند و فقط
نمونه‌شان کم است — که تشخیصِ کاملاً متفاوتی است.

S353 (`HalfTrend + دروازهٔ رژیمِ r2·hurst·entropy`) برای این آزمون انتخاب شد چون
پرقوی‌ترین لبهٔ ادعاشدهٔ اتاقِ انتظار را دارد: `lift = +8.97` روی EURUSD_D1.

---

## ۱. هندسهٔ واقعیِ S353 — نقطهٔ عطف

از `results/_scan_S353/{EURUSD,XAUUSD}_D1.json`، فیلدِ `best`:

| کارت | SL (pip) | TP (pip) | **rr** | n | WR | PF | net |
|---|---:|---:|---:|---:|---:|---:|---:|
| EURUSD_D1 | ۱۲۶.۶ | ۱۸۹.۹ | **۱.۵** | ۸۶ | ۵۰.۰۰٪ | ۱.۵۲۲ | +$۲,۳۸۸.۷ |
| XAUUSD_D1 | ۴۴۹.۱ | ۶۷۳.۷ | **۱.۵** | ۷۱ | ۵۷.۷۵٪ | ۱.۹۳۵ | +$۲,۴۷۹.۴ |

**`rr = 1.5` یعنی TP بزرگ‌تر از SL** — دقیقاً **عکسِ** الگویِ اشتباهِ #۸.

### محاسبهٔ نقطهٔ سربه‌سر (با هزینهٔ واقعیِ حسابِ دمو، ۳.۳ pip)

```
BE = (SL + cost) / (TP + SL)

EURUSD_D1: (126.6 + 3.3) / (189.9 + 126.6) = 129.9 / 316.5 = 0.4104 → 41.04٪
XAUUSD_D1: (449.1 + 3.3) / (673.7 + 449.1) = 452.4 / 1122.8 = 0.4029 → 40.29٪
```

### لیفتِ واقعی

| کارت | BE | WR | **لیفتِ واقعی** | لیفتِ مستندِ md |
|---|---:|---:|---:|---:|
| EURUSD_D1 | ۴۱.۰۴٪ | ۵۰.۰۰٪ | **+۸.۹۶** | **+۸.۹۷** ✅ منطبق |
| XAUUSD_D1 | ۴۰.۲۹٪ | ۵۷.۷۵٪ | **+۱۷.۴۶** | +۵.۰۸ (محافظه‌کارتر) |

---

## ۲. سه نتیجهٔ قاطع

### ۲.۱ ✅ S353 از ابتدا **درست** می‌سنجید

محاسبهٔ مستقلِ من برای EURUSD_D1 عددِ `+8.96` داد و سندِ S353 عددِ `+8.97` را
نوشته بود. **تطبیق تا دو رقمِ اعشار.** این تصادفی نیست: یعنی اسکنرِ S353 لیفت را
از همان ابتدا نسبت به نقطهٔ سربه‌سرِ هندسهٔ خودش می‌سنجیده، نه نسبت به ۵۲٪.

⇒ **لایهٔ S353 آلوده به بیماریِ S375 نیست.**

### ۲.۲ ✅ لبه اقتصاداً **واقعی** است

- `rr = 1.5` (TP > SL) ⇒ نمی‌تواند محصولِ عدمِ تقارنِ براکت باشد.
- `PF = 1.52` و `1.94` ⇒ هر دو بالای ۱، با net مثبت.
- WR = ۵۰٪ و ۵۷.۷٪ **بالای** BE = ۴۱٪ و ۴۰.۳٪ ⇒ سودآوری ساختاری، نه آماری.

این تفاوتِ بنیادی با S363 است:

| | S363 (P0 XAUUSD-H1) | S353 (D1) |
|---|---|---|
| rr | ۰.۳۵۷ (TP≪SL) | **۱.۵ (TP>SL)** |
| BE | ۷۵.۷۴٪ | **۴۰–۴۱٪** |
| WR | ۸۱.۰۶٪ | ۵۰–۵۸٪ |
| لیفتِ ظاهریِ ابزارِ من | +۲۹.۱ ❌ توهم | — |
| **لیفتِ واقعی** | **+۵.۳** | **+۸.۹۶ / +۱۷.۵** |
| حکم | مصداقِ اشتباهِ #۸ | **لبهٔ واقعی** |

نکتهٔ زیبا: S363 نرخِ بردِ **بالاتری** دارد (۸۱٪ در برابرِ ۵۰٪) و لبهٔ **کمتری**.
این تجسمِ عددیِ اشتباهِ رایجِ #۸ است.

### ۲.۳ ✅ خطای مستندسازی به **ضررِ** پروژه بود

برای XAUUSD_D1، محاسبهٔ من `+17.46` داد ولی md `+5.08` نوشته بود — یعنی مستندِ
پروژه لبه را **کم‌تر** از واقع گزارش کرده. این جهتِ خطا نشانهٔ سلامتِ روش است:
هفت باگِ ابزارِ حسابرسیِ من همه به **نفعِ** پروژه بودند، ولی مستندسازیِ S353 به
**ضررِ** خودش بود ⇒ نشانهٔ اینکه برای ساختنِ نتیجهٔ مطلوب تنظیم نشده.

---

## ۳. تشخیصِ نهایی: مانع فقط **کمبودِ نمونه** است

از سندِ S353: `z = lift·√n / 50`، نیاز به `lift·√n ≥ 150`.

| کارت | lift | n | √n | lift·√n | نیاز | کمبود |
|---|---:|---:|---:|---:|---:|---|
| EURUSD_D1 | ۸.۹۶ | ۸۶ | ۹.۲۷ | **۸۳** | ۱۵۰ | ۴۵٪ |
| XAUUSD_D1 | ۱۷.۴۶ | ۷۱ | ۸.۴۳ | **۱۴۷** | ۱۵۰ | **۲٪** ⚠️ |

**⚠️ کشفِ درجه‌دو:** با لیفتِ **واقعیِ** ۱۷.۴۶ (نه ۵.۰۸ مستند)، کارتِ XAUUSD_D1
به `lift·√n = 147` می‌رسد — یعنی **تنها ۲٪ زیرِ آستانهٔ ۱۵۰**. سندِ S353 گزارش
کرده بود «بیشترین مقدار تنها ۸۳ شد»، که با لیفتِ کم‌گزارش‌شده درست بود.

تعدادِ معاملهٔ لازم با لیفتِ واقعی: `n ≥ (150/17.46)² = 73.8` ⇒ **۷۴ معامله**.
کارت **۷۱** معامله دارد. **کمبود = ۳ معامله.**

---

## ۴. کارِ باقی‌مانده (برای مرحلهٔ بعد)

1. **بازخوانیِ عددِ ۵.۰۸:** باید فهمید سندِ S353 آن را چگونه محاسبه کرده. اگر
   لیفتِ پس از دروازه نسبت به مرجعِ دیگری است، کدام مرجع؟ تا این روشن نشود،
   عددِ ۱۷.۴۶ یک محاسبهٔ مستقل است و نه جانشینِ تأییدشده.
2. **آزمونِ ۳ معاملهٔ کم:** آیا با شلْ‌کردنِ حداقلیِ دروازه (بدونِ لمسِ منطق) و
   بدونِ افزودنِ درجهٔ آزادی، n از ۷۱ به ۷۴+ می‌رسد؟ **خطرِ p-hacking جدی است**
   و باید پیش‌ثبت شود.
3. **تجمیعِ دو کارتِ D1:** با لیفت‌های واقعی (۸.۹۶ و ۱۷.۴۶) به‌جای مستندها،
   آزمونِ همگنی باید از نو اجرا شود — رد قبلی با لیفت‌های نادرست انجام شده بود.

---

## ۵. رعایتِ اشتباهاتِ رایج

- **#۷ (اعدادِ رند):** هیچ عددِ رندی اختراع نشد. `COST=3.3` از مشخصاتِ حسابِ دمو
  (۰.۳۳ $/oz) می‌آید و SL/TPها (۱۲۶.۶، ۱۸۹.۹، ۴۴۹.۱، ۶۷۳.۷) از فایلِ آرشیو.
- **#۸ (دورزدنِ معیار):** این سند **کلاً درباره‌ی** کشفِ همان دورزدن است.
  و S353 در سمتِ درستِ آن ایستاده (rr=۱.۵ > ۱).
- **#۵ (تکِ تایم‌فریم):** S353 روی ۷ تایم‌فریمِ هر دو ارز آزموده شده بود
  (نردبانِ فراکتالیِ PF از M5 تا W1).

---

_وضعیت: ✅ اعتبارسنجی کامل شد. **S353 لبهٔ واقعی دارد** و تشخیصش
POWER-LIMITED است، نه توهمِ هندسه. کارِ باقی‌مانده در بخشِ ۴._
