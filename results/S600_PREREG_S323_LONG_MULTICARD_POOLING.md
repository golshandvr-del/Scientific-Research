# S600 — پیش‌ثبتِ احیای `S323-LONG` از راهِ **تجمیعِ چند-کارتی** (مسیرِ `C`)

> ⚠️ **این سند پیش از محاسبهٔ حتی یک عددِ جدید نوشته و commit می‌شود.**
> معیارِ حاکم: `engine/rqs2.py` **v2.6** (`gates_only`). شماره‌گذاری از S600 به
> دستورِ حاکمیتیِ کاربر آغاز می‌شود.
> همهٔ اعدادِ این سند از **بایگانیِ موجود** آمده‌اند (`results/_s361_s323_mirror/`،
> `results/S323_*.md`، `results/S431_*.md`) — هیچ اجرای تازه‌ای انجام نشده.

---

## ۰. زنجیرهٔ انتخابِ نامزد — چرا S323، چرا حالا

مسیرِ رسیدن به این نامزد (برای ممیزیِ چندگانگی شفاف ثبت می‌شود):

۱. سندِ `FINDING_RETRO_POWER_94PCT` سه مشاهدهٔ کور-داوری‌شده با
   `lift>+۱۲pp` را صف کرده بود. ردیابیِ من به منبع:
   - `S365-M5 n=238` و `S369-M30 n=173` ⇒ **توهمِ خطِ پایه** بودند
     (`WR=۶۵٪` ولی `meanR≈۰` چون هندسهٔ `TP<SL` خطِ پایهٔ ذاتیِ ~۶۰٪ دارد؛
     خانواده‌ها `DEAD_NO_SKILL`). ❌ دفن شدند — این خودش یک یافتهٔ ضدِ
     اشتباهِ رایج است: **WR و lift بدونِ مبنایِ هم‌هندسه بی‌معناست.**
   - مشاهدهٔ سوم (`n=269, M30`) به آینهٔ `s361_s323_short_mirror` می‌رسد و
     در دلِ آن، سمتِ **LONG** با `z=3.258` روی nullِ اندازه‌گیری‌شده زنده است.
۲. یافتهٔ بایمودال (سندِ مشاور): ۶ از ۷ ACCEPTِ پروژه **احیا** بودند، همه طلا،
   و تنها جداکنندهٔ معتبر `z≥۳.۰۹` است. S323-LONG-M30 این شرط را **از قبل**
   ارضا می‌کند.
۳. درسِ `S430→S431`: نامزدِ درست، لبهٔ **بزرگِ کم‌نمونه** است که فقط دروازهٔ
   توانی می‌شکند، نه لبهٔ کوچکِ پرنمونه. S323-LONG دقیقاً همین است.

## ۱. نامزد و مکانیزمِ شکستِ تاریخی‌اش

**`S323` — پولبک به حمایت در روندِ صعودی (SR-Pullback، نسخهٔ مستقرِ ممیزی‌شدهٔ
`s357`) — فقط سمتِ LONG.**

سندِ مرگ: `results/S323_SRPullbackGoldenXauusdM5M15M30H1_Xauusd_M15_rqs2_22_POWER-LIMITED.md`

اعدادِ بایگانی (از `results/_s361_s323_mirror/summary.json`، دادهٔ ۱۵.۵ سالهٔ
کاملِ M30/H1 — `data/XAUUSD_M30.csv` و `H1` از ابتدا full بودند؛ E-16 اینجا
صادق نیست):

| کارت | سمت | n | WR | ref (nullِ جای‌گشتی) | lift | z |
|---|---|---|---|---|---|---|
| XAUUSD-M30 | LONG | ۱۶۰ | ۷۲.۵۰٪ | ۵۹.۸۸٪ | **+۱۲.۶۲** | **۳.۲۵۸** |
| XAUUSD-H1 | LONG | ۳۷ | ۷۰.۲۷٪ | ۴۹.۹۴٪ | **+۲۰.۳۳** | ۲.۴۷۳ |
| XAUUSD-M30 | SHORT (آینه) | ۱۰۹ | ۵۱.۳۸٪ | ۶۰.۳۹٪ | −۹.۰۱ | −۱.۹۲۴ |

**مکانیزمِ شکست:** M30-LONG از `H3` (۳.۰۹) گذشته بود ولی زیرِ سدِ `H5`
(`zbar=3.5143` با بودجهٔ ۲۵۶۸ آزمایش) ماند — کسریِ فقط ۰.۲۶σ. چهار مسیرِ
نجاتِ آن دوران (فیلتر/افق/آینه/ممیزی) همه به صورت یا به **همان ۱۶۰ معامله**
حمله کردند. حکمِ «مرگِ ابدی» نوشته شد **پیش از آن‌که** ابزارِ تجمیعِ
چند-کارتی (`engine/rqs2_pool.py`، عصرِ S431) وجود داشته باشد.

**اثباتِ بکر بودنِ مسیر:** `grep` روی کلِ `strategies/` و `results/`: هیچ
اسکریپتِ pool به `s323` ارجاع ندارد. این حمله به مخرج از راهِ **کارتِ دوم**
هرگز اجرا نشده.

## ۲. فرضیهٔ مرکزی (پیش‌ثبت‌شده)

> **فرضیهٔ H-600:** قانونِ S323-LONG روی M30 و H1 (هر دو با دادهٔ کاملِ
> ~۱۵.۵ سال، هندسهٔ منجمدِ آرشیو، بدونِ هیچ پارامترِ نو) لیفت‌های هم‌مرتبه
> (+۱۲.۶ و +۲۰.۳) دارد. تجمیعِ تقویمیِ FIFO این دو، n را از ۱۶۰ به ~۱۹۷
> می‌رساند و z را از ۳.۲۶ به بالای سدِ H5 می‌برد — بدونِ هزینهٔ چندگانگیِ نو.

تخمینِ پیشینی (فقط تخمین، نه ادعا):
`lift_pool ≈ (۱۶۰×۱۲.۶۲ + ۳۷×۲۰.۳۳)/۱۹۷ ≈ +۱۴.۱pp` و
`z_pool ≈ ۱۴.۱×√۱۹۷/σ̂ ≈ ۴.۰` (σ̂ از M30 برون‌یابی شده).

## ۳. اعضای استخر — **قفل‌شده پیش از اجرا**

```
POOL_MEMBERS = ['XAUUSD-M30 LONG', 'XAUUSD-H1 LONG']
```

- قاعدهٔ سیگنال: نسخهٔ **مستقرِ** ممیزی‌شده (`s357_s323_v24_rejudge.signals_deployed`
  یا معادلِ لانگِ `s361`) — همان که اعدادِ بایگانی از آن آمده.
- هندسه: براکتِ منجمدِ همان بایگانی — M30: `SL=55.8/TP=34.5/hold=48`؛
  H1: `SL=69.6/TP=65.7/hold=36`. **هیچ عددی تغییر نمی‌کند.**
- M15 و M5 عمداً عضو نیستند: لیفت‌شان (+۵.۸ و +۲.۶) هم‌مرتبهٔ اعضا نیست ⇒
  شرطِ همگنیِ ۴ از `rqs2_pool` نقض می‌شد (درسِ رقیق‌سازیِ `s351_pool_rescue`).
  این حذف **الان و از روی اعدادِ قدیمی** اعلام می‌شود، نه پس از دیدنِ نتیجه.
- سمتِ SHORT عضو نیست: آینهٔ `s361` آن را با lift منفی رد کرد. لایه
  **ساختاراً یک‌سویه** ثبت می‌شود (مانندِ S344 که SHORT-only و ACCEPT است).
- EURUSD به دستورِ صریحِ کاربر از کلِ این مأموریت خارج است.

## ۴. حسابِ چندگانگی (قیدِ ضدتقلب)

- بودجهٔ تاریخیِ S323 (ممیزیِ `s362`): **۲۵۶۸ آزمایش** ⇒ `zbar≈3.514`.
- این گام هیچ پارامتری جست‌وجو نمی‌کند؛ ولی خودِ تصمیمِ «تجمیع» را مثلِ S431
  یک درجهٔ آزادی می‌شمارم ⇒ بودجهٔ داوری: `n_trials = 2568 × 2 = 5136`
  ⇒ سدِ تقریبی `zbar ≈ 3.7`.
- **معیارِ موفقیت:** حکمِ `compute_rqs2` روی استخر با `n_trials=5136` —
  هر ۱۱ دروازه. من فقط ACCEPT را موفقیت می‌شمارم؛ هر حکمِ میانی همان‌طور
  که موتور می‌گوید گزارش می‌شود (اشتباهِ رایجِ دست‌بردن در حکم ممنوع).

## ۵. خطرهایی که از پیش می‌پذیرم (ابطال‌پذیری)

۱. **سواریِ رانش (H10):** عدمِ تقارنِ شدیدِ آینه (شورت −۹pp) طبق درسِ S323
   «تقارن⇒ساختار» یعنی مشکوک به سواریِ رانشِ صعودیِ طلا. اگر H10 بیفتد،
   حکم همان است که موتور بدهد — دفاعی نمی‌سازم.
۲. **حاشیهٔ نازکِ H5:** تخمین ۴.۰ در برابرِ ۳.۷ فقط ۰.۳σ حاشیه دارد؛ اگر
   FIFO همپوشانیِ تقویمیِ M30/H1 را حذف کند و n<۱۹۷ شود، z پایین‌تر می‌آید.
   عددِ FIFO هرچه بود همان گزارش می‌شود.
۳. **H7 (holdout):** بایگانی تفکیکِ نیمه‌ها را برای LONG-M30 گزارش نکرده؛
   اگر لبه فقط در نیمهٔ اول باشد، H7 می‌کشد و این یافته ثبت می‌شود.
۴. **همپوشانی با سایت:** M30 و H1 روی سایت لایهٔ مستقر دارند (S312، S356).
   ممیزیِ همپوشانی با `engine/trade_simulator.py` (تنها سنجهٔ معتبر) اجرا
   و اگر همپوشانی دیده شد، **همان لحظه** به‌عنوانِ فیلتر آزموده می‌شود.

## ۶. برنامهٔ اجرا (قانونِ اندک‌اندک)

۱. بازتولیدِ بیت‌به‌بیتِ اعدادِ بایگانیِ دو عضو (سنجهٔ سلامت؛ اگر بازتولید
   نشد ⇒ توقف و ریشه‌یابی، نه ادامه).
۲. ساختِ استخر با `engine/rqs2_pool.pool_cards` + داوری با `compute_rqs2`
   (`n_trials=5136`, `K_PERM=2000`, `SEED=20260813`).
۳. جدولِ MTF: اجرای قانونِ LONG روی **همهٔ ۱۹ تایم‌فریمِ طلای** `data/mt5_full/`
   از M1 (گزارشی، نه انتخابی — اعضای استخر قفل‌اند). checkpoint و commit
   به‌ازای هر TF.
۴. ممیزیِ همپوشانی (بندِ ۵.۴) و آزمونِ فوریِ overlap-as-filter در صورتِ وجود.
۵. MD نهایی با نام‌گذاریِ قانونی + گزارشِ جامعِ ۸ اشتباه.

_نویسنده: گئورگ کانتور (پرسونای مأموریت) — ۲۰۲۶-۰۸-۱۳_
