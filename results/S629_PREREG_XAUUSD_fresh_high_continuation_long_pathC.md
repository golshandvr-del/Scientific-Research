# S629 — PREREG — Fresh-High Continuation LONG (naked 55-bar-high crossing) — XAUUSD H6/H8/H12 — Path C

**دانشمند:** اقلیدس (بلوک S620–S629 — **آخرین شمارهٔ بلوک**) · **تاریخ:** 2026-08-21 · قبل از هر کد commit می‌شود
**SEED:** 20260821

---

## ۱. انگیزه — هم‌راستایی با خوشهٔ برندگان + سنتز دو یافتهٔ بلوک

تمام ACCEPTهای مستقل ۱۵.۶ساله در یک خوشه‌اند: **تداوم/شوک درشت‌مقیاس هم‌جهت با روند** (S770 ADR expansion D1/H8=82 · S800 squeeze expansion=91 · S602 Engle pool=76 · S950 jump aftermath H8=80 · S562 GapOpen=95/96). بلوک من دو قطعهٔ مکمل افزود:
- **S626:** سطوح *نمایان روانی* در TF درشت سیگنال پیوسته دارند (+10..15pp) — فقط سطح رند کم‌یاب بود.
- **S628:** سطح باید نمایان باشد؛ ولی «لمس کف در رژیم صعودی» با گیت درفت ناسازگار هندسی است.

⇒ سطح نمایانی که با درفت صعودی **سازگار** است: **سقف ۵۵-کندلی**. رویداد گذر تازه از آن (fresh high) در رژیم صعودی مکرر است (سازگاری گیت×رویداد ✓ — درس S628) و مکانیزمش همان تداوم درشت‌مقیاس خوشهٔ برنده است.

## ۲. ممیزی بداعت (ضد-تصادم — پس از fetch تازهٔ db5f9ff7)

- **S543 (قلمرو ۵۴۰):** break-**retest**-hold سقف ۵۵ با ماشین حالت ۳فازی — REJECT. بند مرگ ابدی‌شان صریحاً «با این ماشین حالت» است. لایهٔ من گذر خالص بدون فاز retest — رویدادِ مکملی که آن‌ها نیازموده‌اند (آن‌ها ورود را به بازگشت به سطح مشروط کردند؛ من دقیقاً معاملاتی را می‌گیرم که برنمی‌گردند).
- **S553 (قلمرو ۵۵۰، در جریان):** تداوم streak علامت بدنه — متغیر حالت متفاوت (توالی بدنه، نه سطح قیمتی).
- **S544:** run==4 up-streak — متغیر متفاوت.
- **S346:** کانال ATR تطبیقی D1 — سطح ATRمحور، نه Donchian خالص. REJECT.
- **S373:** کانال native H4 — قلمرو دیگر، REJECT.
- **S72:** breakout فارکس (غیر طلا).
- **S620 خودم:** D1 lift 17–19pp با n کوچک در LagRSI — نشانهٔ مستقل قدرت سیگنال‌های درشت.
- grep «fresh high / naked donchian crossing»: هیچ سند حکمی. بکر. شماره‌گذاری داخل بلوک من.

## ۳. تعریف منجمد

با `Hmax[i] = rolling_max(high, 55)` روی `[i−55, i−1]` (علّی؛ LOOK=55 منجمد — همان عدد S543 برای مقایسه‌پذیری علمی، بدون جستجو):

**رویداد (LONG-only، فقط گذرِ نو):**
- `close[i] > Hmax[i]` (بستهٔ قاطع بالای سقف ۵۵)
- `close[i−1] ≤ Hmax[i−1]` (crossing-only — ضد خوشه‌شدن)
- **گیت درفت (منجمد S950):** `close[i−1] > close[i−90]`
- ورود: open کندل i+1 + slip

**خانوادهٔ جستجو (12 trial):**
- TF ∈ {H6, H8, H12} — قلب خوشهٔ برنده؛ H8 مرجع S950/S602
- k_sl ∈ {1.5, 2}×ATR100 · RR ∈ {1, 1.5} — بودجه TP≥SL ✓
- max_hold: H6=56, H8=42, H12=28 (منجمد؛ ~۲ هفته)
- **n_trials = 3×2×2 = 12** · تجمعی بلوک پس از این: 4428+12=4440

**هزینه:** spread=3.3 + 2×slip=1.0 pip · شبیه‌ساز رسمی `engine/scalp_engine.simulate_trades`

**null اندازه‌گیری‌شده (S346+S612):** ورودهای بی‌قید از بارهای معتبر **drift>0** با همان هندسهٔ منجمد per-(k_sl,rr). lift = برتری «لحظهٔ فتح سقف» بر نقطهٔ تصادفی همان رژیم صعودی.

## ۴. مسیر چندگانگی و شرایط فریز

**Path C:** جستجو فقط `[0, n/2)` · هولد-اوت بکر تا تک‌تست `compute_rqs2` (`split_bar=n/2`, `n_trials=1`, K_PERM≥1000).

**پنج شرط فریز:** (۱) lift≥4pp · (۲) n≥n_required(3.09σ) · (۳) exp_pip>0 · (۴) پیوستگی همسایگی k_sl/RR · (۵) بیشینهٔ lift·√n. عدم گذر ⇒ سوخته، هولد-اوت بکر، بلوک تمام.

## ۵. ابطال‌پذیری صریح

- P1: اگر lift<4pp همه‌جا ⇒ «فتح سقف» در فضای درفت-مثبت اطلاع افزوده ندارد (هم‌ارز قضیهٔ حالت-نه-گذار S627 برای سطح) — و بلوک با ۱۰ مرگ تمیز و ۶ قضیه بسته می‌شود.
- P2: خطر شناخته: فتح سقف = خرید در گران‌ترین نقطه؛ ممکن است exp منفی شود حتی با lift مثبت (الگوی S622).
- پیش‌بینی صادقانه: P(گذر از فریز) ≈ ۲۵٪.

## ۶. تعهدات

commit قبل از کد ✓ · checkpoint per-TF · انحراف = سوختن خودکار · پس از حکم (هر نتیجه): گزارش جامع پایان بلوک.
