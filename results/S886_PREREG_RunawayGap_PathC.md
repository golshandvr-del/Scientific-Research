# S886 — پیش‌ثبت Path C: «شکاف فرار» (Runaway Overlap Gap) — XAUUSD

**تاریخ ثبت**: 2026-09-04 — **قبل از هر شبیه‌سازی معامله** (قانون پروژه).
**دانشمند**: کانمن — بلوک S880–S889 — شمارهٔ ۷ از ۱۰. هیچ شماره‌ای خارج از بلوک لمس نمی‌شود.
**داور**: engine/rqs2.py v2.6 — حکم فقط از موتور.

## ۰) ممیزی بکارت (پس از fetch تازهٔ 9b92125d)
- شکاف **بین‌روزی/آخر هفته** (open − close قبلی): S400–S407، S560 ACCEPT، S566، S810 — خانوادهٔ دیگری با لنگر *زمان تقویمی*؛ اینجا عمداً **حذف** می‌شود (dt > 1.5×TF فیلتر).
- شوکِ *اندازه* (range ≥ θ×ATR): S965/S966/S602/S606/S840/S950 — لنگر بزرگی است. S886 لنگر **جابه‌جایی هندسی** است: کندلی که با کندل قبلی هم‌پوشانی ندارد/کم دارد — می‌تواند کوچک باشد. grep روی `overlap|no.overlap|runaway|شکاف دامنه` هیچ لایهٔ درون‌روزی نیافت. **بکر.**
- Engulf/Outside (S643 REJECT) عکسِ این رویداد است (دامنهٔ بزرگ‌تر شامل‌کننده).

## ۱) فرضیه
هم‌پوشانی ≤ θ با دامنهٔ کندل قبلی = عدم پذیرشِ کاملِ قیمت‌های قبلی در یک بازهٔ کوتاه (نشانهٔ سفارش‌های یک‌سویهٔ اطلاعاتی، Kyle-سبک اما نه بر مبنای بزرگی). جهت: follow (کندل بالاتر → long: `low[t] > low[t−1] ∧ close[t] > high[t−1]`؛ short آینه‌ای).

## ۲) فضای منجمد (Fibonacci، غیرگرد) — ۴۸ کانفیگ
- θ ∈ {0.236, 0.382} — نسبت هم‌پوشانی به دامنهٔ کندل قبلی (θ=0 در TF درشت کم‌رویداد بود؛ در امکان‌سنجی دیده شد، **بدون معامله**)
- gate ∈ {none, drift90}: drift90 = long فقط اگر `close[t−1] > close[t−1−90]`؛ short آینه‌ای (90 منجمد از S526/S950، جست‌وجو نشده)
- b ∈ {1.0, 1.618} (TP = b×SL، همیشه TP≥SL)
- hold ∈ {21, 34, 55}
- side ∈ {both, long-only}
- SL = 1.6 × median(ATR89) در واحد پیپ (از نیمهٔ اول)؛ اسپرد 3.3 پیپ؛ CONTRACT_SIZE=100.
- سیگنال روی کندل t، ورود open کندل t+1؛ allow_overlap=False.
- شکاف زمانی (dt > 1.5×TF) حذف — آخر هفته/تعطیلات به این لایه تعلق ندارند.

## ۳) مسیر چندگانگی — Path C
- جست‌وجوی آزاد فقط روی نیمهٔ اول (n//2 کندل)؛ معیار انتخاب: بیشینهٔ lift×√n با شرط wr > be_wr، n ≥ 30، lift > 0 (lift نسبت به WR غیرشرطی همان side/هندسه).
- انجماد (θ, gate, b, hold, side, sl, tp) → **یک** آزمون روی نیمهٔ دوم. warm = 90 + 5.
- null کانونی: K=500 قرعهٔ هم‌اندازه/هم‌هندسه per-side، فقط درون OOS. n_trials=48. seed=886.
- compute_rqs2 با هر ۵ ورودی (sl_pip, tp_pip, bar_time, null, n_trials, split_bar, close).

## ۴) پیش‌بینی‌های ابطال‌پذیر (ثبت قبل از داده)
1. **گیت اطلاعات‌افزا**: در IS، برای هر (θ,b,hold) ثابت، WR کانفیگ drift90 باید > WR کانفیگ none باشد (نه فقط n کمتر). اگر نبود، گیت توان‌سوز است و مکانیزم مشکوک (درس S964/S603/S885).
2. **یکنوایی θ**: θ=0.236 (جابه‌جایی تمیزتر) lift بیشتری از θ=0.382 دارد. نقض = «جابه‌جایی» اطلاعات نیست.
3. قلهٔ لبه در H4–H8 (همان‌جا که خانوادهٔ شوک قله دارد)؛ زیر H1 دیوار اسپرد (تکرار هشتم).
4. در OOS (ابرروند) long ≥ short.

## ۵) شرط توقف
هر حکمی جز REJECT → توقف و گزارش به کاربر، هیچ استقراری. REJECT → گزارش کوتاه، S887.

**این سند قبل از نوشتن اسکریپت اسکن و هر شبیه‌سازی معامله کامیت می‌شود.**
