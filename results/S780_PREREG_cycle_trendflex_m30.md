# S780 — پیش‌ثبت (Pre-Registration) — مسیر C (hold-out) از §۶.۲

**تاریخ پیش‌ثبت:** 2026-08-14
**پژوهشگر:** ابن هیثم (AI)
**وضعیت نیمهٔ دوم داده:** تا لحظهٔ این commit **لمس نشده** — هیچ آزمونی روی آن اجرا نشده است.

---

## ۱. اعلام مسیر چندگانگی

**مسیر C — تأیید کنارگذاشته (hold-out)** طبق §۶.۲ گزارش ممیزی RQS2:
- جست‌وجو **فقط** روی نیمهٔ اول دادهٔ کامل انجام شد (`time < 1541749500` ≈ 2018-11-09،
  میانهٔ تقویمی دادهٔ ۱۵.۶ سالهٔ `data/mt5_full/`).
- این سند **یک** پیکربندی منجمد را اعلام می‌کند.
- سپس **یک و فقط یک** آزمون تأییدی روی نیمهٔ دوم (`time ≥ 1541749500`) اجرا می‌شود.
- `n_trials = 1` برای H5 (شمارش صادقانهٔ لمس‌های نیمهٔ دوم).
- **اگر رد شد، لایه مرده است.** تنظیم مجدد و بازآزمون روی همان نیمه ممنوع (قانون ضدتقلب ۳).
- هر لمس اضافهٔ احتمالی نیمهٔ دوم، `n_trials` را افزایش می‌دهد و در گزارش ثبت می‌شود (قانون ۴).

## ۲. پیکربندی منجمد (تغییرناپذیر پس از این commit)

| مؤلفه | مقدار |
|---|---|
| دارایی | **XAUUSD** (فقط طلا — استثنای صریح کاربر: صفر وقت روی EURUSD) |
| تایم‌فریم آزمون تأییدی | **M30** |
| اندیکاتور | `trendflex` از بانک ۴۰۱تایی (`engine/indicator_bank.py`، دستهٔ cycle — تاکنون در هیچ لایهٔ زنده‌ای استفاده نشده) |
| نوع سیگنال | **رویدادی** (عبور، نه وضعیت): long وقتی trendflex از زیر `+1.83` به `≥ +1.83` عبور کند؛ short قرینه (عبور به `≤ −1.83`) |
| تفسیر | تداوم (continuation) — هم‌جهت با عبور |
| هندسه | `SL = 2.23 × ATRmed`، `TP = 2.68 × ATRmed` که `ATRmed` = میانهٔ ATR(34) کل نیمهٔ آزمون بر حسب pip (فرمول ثابتِ ازپیش‌اعلام‌شده؛ روی نیمهٔ اول: SL=47.9، TP=57.6 pip). **TP > SL** — ضداشتباه #۸ |
| max_hold | `hold_bars_for('M30', 72)` = ۱۴۴ کندل (۷۲ ساعت) |
| هم‌پوشانی | `allow_overlap=False` (تک‌پوزیشن، FIFO) |
| شبیه‌ساز | `engine.scalp_engine.simulate_trades` رسمی — ورود در open کندل بعد، اسپرد کامل 3.3 pip، SL مقدم در کندل مبهم |
| داده | `data/mt5_full/XAUUSD_M30.csv` از طریق `tools/s434_fast_data.load_fast` (src در گزارش ذکر می‌شود) |

**پارامترهای غیررُند** (ضداشتباه #۷): آستانهٔ 1.83، ضرایب 2.23/2.68 — هیچ‌کدام رُند نیستند.
**تعداد پارامتر آزاد: ۳** (آستانه + دو ضریب هندسه) — سازگار با یافتهٔ فراگزارش (ACCEPTها پارامتر کم دارند).

## ۳. شواهد نیمهٔ اول (مبنای انتخاب — قابل بازتولید با `strategies/s780_explore.py` و `s780_explore2.py`)

- کل نیمهٔ اول: n=1172، WR=49.83٪، be=45.40٪، **lift=+4.43pp**، z=3.04، net=+1612 pip (پس از هزینهٔ کامل).
- پایداری ربع‌ها: Q1 lift=+3.78 (net +424)، Q2 lift=+5.13 (net +1188) — هر دو مثبت، ربع نزدیک‌تر قوی‌تر.
- هر دو جهت کارا: Q2 → long WR=49.6٪، short WR=51.4٪.
- فلات آستانه: همسایه‌های 1.61 و 2.07 نیز lift≥4pp — وابسته به عدد جادویی نیست.
- دام‌های شناسایی و ردشده در اکتشاف: M5 با z=3.38 ولی net=−۵۴هزار pip (هزینه ۲۲٪ SL را می‌بلعد — سراب آماریِ غیراقتصادی)؛ H1 با افول Q1→Q2.

## ۴. معیار قبول/رد (ازپیش‌اعلام‌شده)

- حکم را **فقط موتور رسمی** `engine.rqs2.compute_rqs2` صادر می‌کند (هرگز دست‌نویس نمی‌شود).
- مدل پوچ: اندازه‌گیری‌شده (ورود غیرشرطی + جایگشت زمانی)، K=1000، بذر ثابت 780، ذخیره در `results/_s780/null_model.json` — یک بار ساخته می‌شود.
- ورودی‌های کامل: `sl_pip, tp_pip, bar_time, null, n_trials=1, split_bar=0.70×n_bars(نیمهٔ دوم), close` — هیچ‌کدام حذف نمی‌شود (پرهیز از INCOMPLETE).
- توان پیش‌محاسبه: n مورد انتظار ≈ ۱۱۰۰–۱۲۰۰ ⇒ lift لازم برای z=3.09 ≈ ۴.۵pp. لیفت نیمهٔ اول 4.43pp — آزمون منصفانه و در مرز؛ نتیجه هرچه بود گزارش می‌شود.

## ۵. جایگاه آزمون‌های جانبی (اعلام صریح برای جلوگیری از ابهام)

- **آزمون تأییدی یگانه:** فقط XAUUSD-M30 با پیکربندی بالا. حکم ACCEPT/REJECT/... فقط از همین آزمون.
- **جدول MTF** (قانون چند-TF پروژه، از M1 تا MN1): پس از آزمون تأییدی اجرا و **صرفاً توصیفی** گزارش می‌شود؛ نمی‌تواند حکم را ارتقا دهد (فقط اطلاع‌رسانی/تنزل). هر TF هندسهٔ ATR-based خودش را می‌گیرد (ضداشتباه #۶).
- **ممیزی هم‌پوشانی** با ۵ لایهٔ زنده (S355/S344/S312/S356/S382) با شبیه‌ساز رویدادمحور انجام و بخش هم‌پوشان بلافاصله به‌عنوان فیلتر آزموده می‌شود (قانون کاربر).
- EURUSD: **هیچ آزمونی اجرا نمی‌شود** (استثنای صریح کاربر بر قانون MTF).

---

*این سند پیش از اجرای هر آزمونی روی نیمهٔ دوم commit می‌شود؛ مُهر زمانی git، سند پیش‌ثبت قابل راستی‌آزمایی است.*
