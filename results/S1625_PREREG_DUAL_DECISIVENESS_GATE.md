# S1625 — PREREG — قاطعیت دوگانه (حاشیهٔ S1621 ∧ بدنهٔ S1520) روی لبهٔ تازهٔ تقویم‌همتا — XAUUSD H8/H4/H6 — LONG

**دانشمند:** اقلیدس · بلوک S1620–S1629 · شمارهٔ ۶/۱۰ · 2026-09-17 · **commit قبل از کد و هر عدد.** داور `compute_rqs2` v2.6 دست‌نخورده. **فقط اکتشاف — سایت هرگز لمس نمی‌شود.**

## ۱) پرسش علمی
S1621 (من) و S1520 (گاوس) دو گیت «قاطعیت» با ۷۶٪ اشتراک‌اند؛ زیرمجموعهٔ اشتراک در ممیزی S1621 lift +20.1 (n=120، توصیفی، بی‌حکم) داشت. پرسش تیز: **آیا دو نرمال‌سازی قاطعیت (بر ATR / بر دامنهٔ کندل) اطلاعات مکمل دارند یا یک چیزند؟** اگر مکمل ⇒ AND باید lift را بالای بهترین تک‌گیت (+19.13 S1520-H8) ببرد با نرخ عبور زنده. اگر یک چیز ⇒ AND فقط n می‌سوزاند (power-burn، قانون S525/S1560-F4). این آزمون هیچ‌کدام از دو گیت را بازتعریف نمی‌کند — فقط اشتراکشان را داوری می‌کند. ممیزی: `grep -riE "margin.*rho|rho.*margin|decisiveness|قاطعیت"` ⇒ فقط اسناد خودم. S1560 (گپ) AND دو گیت را روی خانوادهٔ دیگری آزمود و F4 power-burn داشت — درسش این‌جا به‌عنوان ابطال‌گر F4 وارد شد.

## ۲) افقِ تقویم‌همتا (قانون S1523 گاوس — پذیرفته‌شده)
S1523 نشان داد لبهٔ S526 پدیدهٔ «سقفِ ۳۰ روز معاملاتی» است؛ LB ثابت ۹۰ کندل روی TFهای دیگر رویدادِ متفاوتی می‌سنجد. پس LB به‌ازای هر کارت: **H8=90، H4=180، H6=120** (عیناً `LB_BY_CARD` S1523؛ بی‌جست‌وجو). درفت گیت null: `close[t−1] − close[t−LB] > 0`.

## ۳) شمارش خام blind (بی‌PnL) — داده mt5_full ۱۵.۶ سال (H4 gunzip از mt5_full)
| کارت | LB | لبهٔ تازه | ∧ margin≥0.25 ∧ ρ≥0.618 | نرخ عبور | sd_coin(0.6·sig) |
|---|---|---|---|---|---|
| H8 | 90 | 361 | 174 | 48.2% | ≈4.9pp |
| H4 | 180 | 520 | 236 | 45.4% | ≈4.2pp |
| H6 | 120 | 426 | 185 | 43.4% | ≈4.7pp |
نرخ عبور زنده؛ lift لازم برای z≈3.1 روی H8 ≈ +15 — پایهٔ مقایسه +19.13 بالاتر از این است.

## ۴) قانون منجمد (صفر پارامتر آزاد — همه به ارث)
- رویداد: `close[t] > max(close[t−LB..t−1])`, `¬nh[t−1]`.
- **گیت دوگانه:** `(close − prevmax)/ATR100[t−1] ≥ 0.25` **∧** `(close−open)/(high−low) ≥ 0.618`. سه بازوی تشخیصی روی هر کارت برای تجزیه: `margin-only` (margin≥0.25 ∧ ρ<0.618)، `rho-only` (ρ≥0.618 ∧ margin<0.25)، `neither`. بازوهای تشخیصی داوری نمی‌شوند ولی در n_trials شمرده می‌شوند.
- LONG؛ ورود open بعدی؛ FIFO؛ اسپرد 3.3؛ SL=1.5×median(ATR100)، TP=1.5×SL؛ null شرطی فضای درفت>0 stride {1,3,7} + perm K=2000 seed 20260805؛ هارنس S382 دست‌نخورده؛ مسیر B، split_bar 0.70·n.

## ۵) بودجه
۳ کارت × ۴ بازو = **n_trials = 12**. تنش 50 تشخیصی.

## ۶) ابطال‌گرها (منجمد)
- **G1** نرخ عبور ∈ [15,75]% ✅ پیش‌شمارش.
- **P1 (مکمل بودن):** lift_dual(H8) > **+19.13** (S1520-H8، بهترین تک‌گیت) **و** > +17.48 (S1621). شکست ⇒ گیت‌ها یک اطلاعات‌اند ⇒ REJECT ولو ۱۱ گیت سبز.
- **F4 (power-burn، از S1560):** اگر بازوهای `margin-only` و `rho-only` هر دو lift > +10 داشتند، AND اطلاعاتِ سالم را دور می‌ریزد ⇒ حتی با P1 موفق برچسب **ACCEPT-REDUNDANT** (S1621/S1520 اولی‌اند) و توقف اعلام نمی‌شود.
- **P2 هم‌پوشانی:** dual ⊂ S1520 و ⊂ S1621 بنا به ساخت؛ Jaccard با S1520 گزارش می‌شود؛ >0.6 ⇒ REDUNDANT.
- حکم رسمی = موتور روی **H8 dual**.
- **پیش‌بینی شخصی:** P1 می‌گذرد (lift ≈ +20..+21)، F4 **شلیک می‌کند** (margin-only ≈ +14.8، rho-only ≈ +12.6 هر دو >10) ⇒ محتمل‌ترین: **ACCEPT-REDUNDANT** ۵۰٪؛ ACCEPT خالص ۱۵٪؛ POWER-LIMITED/REJECT ۳۵٪. ارزش علمی حتی در حالت REDUNDANT: تجزیهٔ کمّی اطلاعات خانواده به دو مؤلفه.

## ۷) قلمرو
فقط S1625؛ هیچ فایل گاوس/دیگران و هیچ فایل سایت لمس نمی‌شود.
