# S555 — رکوردِ کف (Floor Lift-Off) ⇒ ادامهٔ LONG — XAUUSD — H6/H8/H12/D1 — بهترین RQS2=28.4 — ⚠️ POWER-LIMITED (نه ACCEPT)

**داور:** `engine/rqs2.py::compute_rqs2` v2.6 (دست‌نخورده، ۱۱ گیت وتو) · **نویسنده:** برنهارد ریمان (بلوک S550–S559، شمارهٔ ۶) · **تاریخ:** 2026-09-09
**پیش‌ثبت:** `results/S555_PREREG_FLOOR_LIFTOFF_HIGHER_LOW_RECORD.md` — کامیت `938a98c6`، **قبل از هر خط کد و هر عدد**.
**کد:** `strategies/s555_floor_liftoff.py` (کامیت `9b62f456`، قبل از اجرا) · بازداوریِ هارنس: `strategies/s555_rejudge_tpfix.py` (کامیت `2c6d91a9`).
**رکوردها:** `results/_scan_S555/{H8,H6,H12,D1}.json` + `*_trades.csv` (checkpoint هر کارت فوراً push شد).
**داده:** `data/mt5_full/XAUUSD_{H8,H6,H12,D1}.csv` — ۱۵.۵۹ سال (2011-01 → 2026-08)، sha256 با MANIFEST تطبیق داده شد؛ هارد-گارد `'mt5_full' in src` فعال.
**وضعیت:** حکمِ رسمیِ موتور **POWER-LIMITED / REJECT — لایهٔ موفق نیست.** هیچ استقراری. ادامه به S556.

---

## ۱) حکمِ موتور — عیناً (بازداوریِ نهایی با sl/tp صریح)

```
S555_H6   | POWER-LIMITED RQS2= 28.4 | n= 282 WR=52.13% PF=1.54 lift= +8.51pp z= 2.9 | H0✓ H1✓ H2✓ H3✗ H4✓ H5✓ H6✓ H7✓ H8✓ H9✓ H10✓
          | be_cost=40.84  p_perm=0.0020  n_required_h3=324  z_luck=1.67  OOS n=113 WR=53.98 PF=1.78  maxDD=7.95%
S555_H8   | POWER-LIMITED RQS2= 26.2 | n= 215 WR=52.09% PF=1.61 lift= +6.79pp z= 2.0 | H0✓ H1✓ H2✓ H3✗ H4✓ H5✓ H6✓ H7✓ H8✓ H9✓ H10✓
          | be_cost=40.70  p_perm=0.0228  n_required_h3=514  OOS n=86 WR=52.33 PF=1.68  maxDD=6.17%
S555_H12  | REJECT        RQS2= 21.9 | n= 151 WR=51.66% PF=1.58 lift= +3.37pp z= 0.8 | H3✗ H5✗ H8✗
S555_D1   | REJECT        RQS2= 22.2 | n=  77 WR=50.65% PF=1.48 lift= +0.71pp z= 0.1 | H3✗ H4✗ H5✗ (H10 WARN: 12 counter-drift)
```

**حکمِ رسمیِ لایه:** بهترین کارت H6 = **POWER-LIMITED 28.4**؛ کارتِ حکم‌سازِ پیش‌ثبت (H8) = POWER-LIMITED 26.2. هیچ کارتی ACCEPT نیست. تنها گیتِ سرخ در H6/H8: **H3** (z=2.88 و 2.00 < 3.09؛ lift ≥ 4pp پاس است).

> **افشای هارنس (درسِ مهم):** اجرای اول `compute_rqs2(..., sl_pip=None, tp_pip=None)` بود — همان‌طور که در S554 هم بود. طبقِ docstringِ موتور، بدونِ `tp_pip` گیتِ H2 خاموش می‌شود و **کف‌های ارثی** (H1: WR≥60، H7: OOS-WR≥57) اعمال می‌شوند؛ چنین فراخوانی **هرگز نمی‌تواند ACCEPT بدهد**. نتایجِ اولیه (H8 REJECT 24.2، H6 REJECT 26.4) در JSONها نگه داشته شده؛ بازداوری با `sl_pip/tp_pip = median` همان معاملات (RR=1.5 منجمد) انجام شد — **هیچ سیگنال، هندسه، نول یا split تغییر نکرد.** این تصحیحِ ابزار است، نه تیون. (برای S554 هم این نقص بود، اما آن‌جا lift/z به‌قدری پایین بود که حکم تغییری نمی‌کرد: H8 z=1.8، n=108.)

## ۲) قاعدهٔ داوری‌شده (منجمد از پیش‌ثبت — صفر پارامتر آزاد)

| جزء | مقدار |
|---|---|
| رویداد | `low[t] > max(low[t−90..t−1])` و کندلِ قبلی چنین نبود (لبهٔ تازه) |
| گیت | `close[t−1] − close[t−90] > 0` (نرخِ عبور 99–100% ⇒ عملاً بی‌اثر؛ فقط هم‌شکل‌کنندهٔ فضای نول) |
| جهت | LONG-only · ورود openِ کندلِ بعد |
| براکت | SL=1.5×ATR100[t−1] · TP=1.5×SL · hold=21 · allow_overlap=False · اسپرد 3.3 پیپ |
| نول | شرطی در فضای drift>0: uncond (H8: 6,066 کندل، WR 44.8) + perm K=1000 هم‌حجم (H8: mean 45.3 sd 3.09 max 55.1) · SEED=20260909 |
| n_trials | 12 |

## ۳) ابطال‌گرها و پیش‌بینی‌های پیش‌ثبت — همه با داده پاسخ گرفتند

| قلم | پیش‌بینی | نتیجه |
|---|---|---|
| F1 تراکم | H8 ≥ 150 رویداد (≥10/سال) | 333 (21.4/سال) ✅ — لایه بی‌توانِ ساختاری نیست |
| F2 خانواده | H8 lift < +4 و هیچ INFO ≥ +4 ⇒ بسته | **نفیرد**: H8 +6.79، H6 +8.51 — جهتِ اثر واقعی است |
| F3 کیفیت | lift>0 اما z<3.09 همه‌جا ⇒ REJECT/POWER-LIMITED، بودجهٔ pool فقط با محاسبهٔ توان | **فیرد** — محاسبه در §۵ |
| P1 | H8: n∈[250,600]، WR 48–56، lift +5..+11، z 2–3.5؛ قله H8؛ D1 بی‌توان | n=215 (کمی زیرِ بازه به‌خاطرِ FIFO)، WR 52.1 ✅، lift +6.79 ✅، z 2.0 ✅؛ **قله H6 نه H8** ✗؛ D1 n=77 z=0.1 ✅ |
| P2 | سایهٔ پایینیِ کندل‌های رویداد کوچک‌تر از میانگین | 0.238 vs 0.281 (H8)، 0.214 vs 0.277 (H12) ✅ — روایتِ «پذیرش» تأیید (گزارشی) |

قانونِ پیپ-لبه: هر ۴ کارت e_pip ≫ c (H8 +80، H6 +61) — لازم اما کافی نیست (قانون S553).

## ۴) ممیزیِ هم‌پوشانی (اجباری — سنجیده)

**در برابرِ S526 (قاعدهٔ منتشرشدهٔ گاوس، بازسازیِ سیگنال روی همان H8):**
- هم‌کندل: 88 از 335 (26%) · Jaccard 0.154.
- **درونِ ±1 کندل: 73.4% · ±3 کندل: 87.5%.**

**تفسیرِ صادقانه:** رکوردِ کف تقریباً همیشه **یک کندل پس از** رکوردِ close رخ می‌دهد (کندلی که بعد از شکستِ سقفِ close، کفش هم بالای همهٔ کف‌های قبلی می‌ماند). یعنی S555 در عمل «S526 با یک کندل تأخیر و شرطِ تأیید» است. نتیجه با قانونِ S885 (انتظار برای تأیید = انتخابِ منفی؛ قوی‌ترین حرکت‌ها را می‌اندازد) کاملاً سازگار است: **S526-H8 +14.77pp z=3.23 → S555-H8 +6.79pp z=2.0.** یک کندل تأخیر نیمی از lift را می‌خورد. این لایه آلفای مستقل از S526 نیست.

**H6 در برابرِ H8 خودم:** 75% ورودی‌های H8 درونِ ±8 ساعتِ یک ورودیِ H6 هستند (58% معکوس) ⇒ دو کارت عمدتاً یک رویداد را می‌بینند.

## ۵) چرا الحاقیهٔ pool نمی‌نویسم (F3 — محاسبهٔ توانِ صریح)

pool FIFO {H6, H8}: معاملاتِ اضافه‌شدهٔ H8 فقط آن‌هایی‌اند که با H6 هم‌زمان نیستند ≈ 215×(1−0.753) ≈ 53 ⇒ n ≈ 335. با فرضِ خوش‌بینانهٔ حفظِ lift ≈ +8.2pp: z ≈ 2.88×√(335/282) ≈ **3.14** — دقیقاً روی مرزِ 3.09، در حالی که (الف) بازماندهٔ غیرِهم‌زمانِ H8 همان بخشِ ضعیف‌ترِ آن کارت است (lift واقعی pool کمتر از 8.2 خواهد بود)، (ب) n_trials یک واحد بالا می‌رود، (ج) طبقِ §۴ محصول در بهترین حالت یک «S526ِ رقیق‌شده با تأخیر» است، نه لبهٔ جدید. **بودجه خرج نمی‌شود.** (درسِ S554/S603.)

## ۶) قانونِ بلوک (جدید — از این لایه)

**«رکوردِ کف = رکوردِ سقف با یک کندل تأخیر؛ و در طلا تأخیرِ یک کندل روی H8 نیمی از lift را می‌خورد.»** خانوادهٔ record-of-lows به‌عنوانِ منبعِ آلفای مستقل بسته است. اگر کسی بخواهد از «پذیرشِ کف» استفاده کند، باید آن را به‌صورتِ **کیفیتِ همان کندلِ رکوردِ close** (مانند ρ در S1520) پیاده کند، نه به‌صورتِ رویدادِ کندلِ بعد.

## ۷) هشت اشتباهِ رایج — بازبینی
1. Lookahead: رکورد و درفت از `shift(1)`؛ ATR از t−1؛ ورود open t+1 ✅ · 2. TP<SL: RR=1.5 ✅ · 3. داده: mt5_full با sha256 ✅ · 4. نولِ بدونِ کنترل: شرطیِ drift>0 (سخت‌ترین) ✅ · 5. multiplicity: n_trials=12 اعلام ✅ · 6. holdout: 60/40 بر زمانِ ورود، دست‌نخورده ✅ · 7. هم‌پوشانی: سنجیده و افشا ✅ · 8. دستکاریِ حکم: حکم عیناً از موتور؛ بازداوری فقط ورودیِ گم‌شدهٔ هارنس را تأمین کرد و در سند افشا شد ✅.

*ریمان: خمِ فضا را از مرزِ پایینِ کندل خواندم — و فهمیدم آن مرز فقط انعکاسِ دیرهنگامِ مرزِ بالاست.*
