# S1624 — PREREG — رکورد در رکورد (Nested-Record Gate): لبهٔ تازهٔ ۹۰ که همزمان سقف ۳۶۰ را می‌شکند — XAUUSD H8/H6/H12 — LONG

**دانشمند:** اقلیدس · بلوک S1620–S1629 · شمارهٔ ۵/۱۰ · 2026-09-09 · **commit قبل از کد و هر عدد.** داور `compute_rqs2` v2.6 دست‌نخورده. **فقط اکتشاف — سایت هرگز لمس نمی‌شود.**

## ۱) منطق و بداعت
محورهای «تاریخچه» (S1620 ✗)، «شدت» (S1621 ✓ سقف؛ S1622 ✗ حد؛ S1623 ✗ تعمیم) بسته شدند. بُعد هندسی باقی‌مانده: **موقعیت رکورد در ساختار بلندتر**. لبهٔ تازهٔ ۹۰ دو نوع است: (الف) رکوردی که همچنان زیر سقف ۳۶۰-کندلی است (بازیابی درون یک اصلاح بلند — «رکورد نسبی»)؛ (ب) رکوردی که هم‌زمان سقف ۳۶۰ را هم می‌شکند («رکورد مطلق» در افق ۴× — قلمرو بکر قیمت، بی‌عرضهٔ گیرافتاده). فرضیه: (ب) ادامهٔ قوی‌تری دارد چون بالای آن هیچ خریدار زیان‌دیده‌ای برای فروش در سربه‌سر نیست (منطق «هوای پاک» S1511 در بُعد *زمانی* به‌جای *قیمتی*).
- ۳۶۰ = ۴×۹۰ (تنها ضریب ساده؛ همان افق «آهسته» S627 من). بی‌جست‌وجو.
- بداعت: `grep -riE "nested record|record.in.record|all.time high|ATH|multi.horizon record|fresh high.*(360|250)"` ⇒ S1510 (رکورد *مومنتوم*)، S1740 (بستهٔ قوی)، مدیریت — هیچ‌کدام سقف ۹۰ درون سقف ۳۶۰ نیست. بکر.

## ۲) شمارش خام blind (بی‌PnL)
| کارت | لبهٔ تازهٔ ۹۰ | ∧ close>max(close[t−360..t−1]) | نرخ عبور | sd_coin (n≈0.55·sig) |
|---|---|---|---|---|
| H8 | 361 | 179 | 49.6% | ≈5.0pp |
| H6 | 491 | 246 | 50.1% | ≈4.3pp |
| H12 | 248 | 109 | 44.0% | ≈6.5pp |
نرخ عبور سالم؛ H8 n≈100 ⇒ lift لازم برای z≈3.1 حدود +15..+16pp (پایه +14.77) — مرزی؛ صادقانه: احتمال POWER-LIMITED بالاست.

## ۳) قانون منجمد
- پایه عیناً S526: `nh90 = close > max(close[t−90..t−1])`, `¬nh90[t−1]`.
- **گیت:** `close[t] > max(close[t−360..t−1])` (رکورد مطلق در افق ۳۶۰). counter: `≤`.
- LONG؛ ورود open بعدی؛ FIFO؛ اسپرد 3.3؛ SL=1.5×median(ATR100)، TP=1.5×SL. هارنس S382 دست‌نخورده (رانر s162x، فقط گیت).
- کارت‌ها H8 (اصلی)، H6، H12؛ داده mt5_full ۱۵.۶ سال؛ null شرطی فضای درفت>0، stride {1,3,7} + perm K=2000 seed 20260805؛ مسیر B، split_bar 0.70·n.

## ۴) بودجه
۳ × ۲ = **n_trials = 6** (G0 رانر در S1620 و S1623 دو بار تأیید شده). تنش 50 تشخیصی.

## ۵) گاردها/ابطال‌گرها
- **G1** نرخ عبور ∈ [15,75]% ✅ پیش‌شمارش‌شده.
- **P1a** lift_gated(H8) > +14.77 · **P1b** lift_gated > lift_counter — شکست ⇒ REJECT ولو گیت‌ها سبز.
- **P2 هم‌پوشانی** با S1621/S1520 روی H8؛ Jaccard>0.6 ⇒ ACCEPT-REDUNDANT.
- **F1** هیچ کارتی lift>+4 ⇒ مرده.
- حکم رسمی = موتور روی **H8 gated**.
- **پیش‌بینی شخصی (کالیبره پس از دو خطا):** P1b می‌گذرد (Δ +3..+5pp — رکورد مطلق قوی‌تر از نسبی)، اما z زیر 3.09 به‌خاطر n≈100 ⇒ **POWER-LIMITED ۵۰٪**، ACCEPT ۲۵٪، REJECT ۲۵٪. اگر P1b شکست ⇒ «افقِ رکورد بی‌اهمیت است؛ فقط شدت مهم است» — قانون منفی.

## ۶) قلمرو
فقط S1624؛ هیچ فایل دیگری و هیچ فایل سایت.
