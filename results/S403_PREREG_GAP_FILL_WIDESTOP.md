# S403 — پیش‌ثبت: گپ‌فیلِ M30 + استاپِ پهن (حملهٔ سوم به تک‌دروازهٔ H8)

> مسیر C چندگانگی · ثبت **قبل از** اجرای هر ترکیب · مُهرِ زمان = همین کامیت
> والدها: S400-M30 REJECT(33.2) فقط-H8 · S401 REJECT (6.46%) · S402 REJECT (6.47%؛ time-stop خنثی)

## ۱. فرضیه

دو حملهٔ قبلی به دیوارِ DD≈6.46% خوردند؛ ریشه: خوشهٔ باخت‌های *کاملِ* SL که قبل از
مسلح‌شدنِ BE رخ می‌دهند. اهرمِ ساختاریِ آزمون‌نشده: **پهنای خودِ استاپ (k_sl)**.
زیرِ حسابداریِ درصد-ثابتِ `run_capital`:
(الف) استاپِ پهن‌تر ⇒ برخوردِ کمتر ⇒ خوشهٔ باختِ کامل کوچک‌تر؛
(ب) هر باختِ جزئی/کلوزِ روز، به‌نسبتِ sl_pip بزرگ‌تر، دلاریِ *کوچک‌تر* می‌شود.
ریسک‌ها (صادقانه): H2 سخت‌تر می‌شود (breakeven بالا می‌رود) و recovery ممکن است بیفتد.
به همین دلیل قاعدهٔ واجدشدن، نگهبانِ H2 و recovery دارد (§۴).

## ۲. لایهٔ پایه (منجمد به‌جز k_sl)

XAUUSD · M30 · گپ‌DOWN · آستانهٔ QW70 (رولینگ ۲۵۰روزهٔ جدا-دوشنبه) · X-FILL ·
حذفِ DOW=0 · ورود = openِ بارِ اولِ روز · TP = کلوزِ روزِ قبل ·
اسپرد 3.3 pip · seed=400 · split M30 = بارِ 90691.

## ۳. فضای جست‌وجو (قفل — ۱۲ ترکیب)

| اهرم | مقادیر | توجیه |
|---|---|---|
| k_sl | {2.2, 2.7, 3.3} | شناور/غیرگرد؛ بیرونِ بازهٔ جست‌وجوشدهٔ S400 ({1.0,1.5}) |
| BE-trigger f | {None, 0.4} | برندهٔ اثرگذارِ S401 |
| cooldown d | {0, 1} | برندهٔ اثرگذارِ S401 |

۳×۲×۲ = **۱۲ ترکیب**. قراردادها عینِ موتور (کپیِ S401): چکِ TP/SL درون‌بار؛
TP∧SL همزمان = باخت؛ BE فقط از بارِ بعدِ ورود؛ خروجِ پایانِ روز در کلوزِ آخرین بار.
time-stop حذف شد (S402 اثباتِ خنثی‌بودن).

## ۴. قاعدهٔ برنده و تعهدها (قفل)

- تنظیم **فقط** روی نیمهٔ اول (بارهای < 90691) با حسابداریِ خودِ داور (`run_capital`).
- واجد: n≥100 ∧ PF>1 ∧ t≥2.5 ∧ **maxDD ≤ 6.40%** ∧ **نگهبانِ H2**:
  WR نیمهٔ اول ≥ breakeven+2pp که breakeven = (sl̄+cost)/(sl̄+tp̄) با میانهٔ
  sl_pip/tp_pip همان ترکیب و cost=3.3 pip ∧ **نگهبانِ recovery**: net/maxDD_دلاری ≥ 3.
- برنده: کمترین maxDD؛ گره‌گشایی: t بزرگ‌تر.
- هیچ ترکیبی واجد نشد ⇒ **REJECT بدونِ باز‌کردنِ holdout** (بدونِ استثنا — مثل S401/S402).
- در صورت وجودِ برنده: **یک** آزمونِ رسمی `compute_rqs2` روی کلِ داده.

## ۵. حسابِ چندگانگی (صادقانه، تجمعی)

S400: 216 · S401: +6 · S402: +12 · S403: +12 ⇒ **n_trials = 246**.

## ۶. null (بازو-همتا، عینِ S401/S402)

استخر = همهٔ روزهای gap-DOWN بی‌قید (بدونِ آستانه/DOW) با **همان** اهرم‌های برنده
(k_sl/f/d یکسان) ⇒ lift فقط ارزشِ آستانه+فیلتر. K=500 زیرمجموعهٔ هم‌اندازه، seed=400.
هندسهٔ داور: میانهٔ sl_pip/tp_pip اندازه‌گیری‌شده.

## ۷. معیارِ نهایی

RQS2 v2.6 · یازده دروازهٔ H0–H10 · فایلِ نتیجه: `Strategy_Pair_TFs_rqs2_<score>_<STATUS>.md`

*تاریخ ثبت: 2026-08-18 · Mission 1 · نویسنده: AI-S400s (بازهٔ S400–S409)*
