// ============================================================================
// indicators/bank.ts — بانکِ گستردهٔ اندیکاتورها (User Note: ۴۰۰+ اندیکاتورِ پیچیده)
// ----------------------------------------------------------------------------
// از این نسخه به بعد، بدنهٔ اندیکاتورها به فایل‌های دسته‌بندی‌شدهٔ زیرِ پوشهٔ bank/
// منتقل شده است (هر دسته یک فایلِ TS مستقل). این فایل صرفاً نقشِ «تجمیع‌کننده/نمای
// سازگار» را دارد و آرایهٔ BANK را (به همان ترتیبِ ثبتِ منطقی) صادر می‌کند، تا
// importهای موجود مثلِ `import { BANK } from './bank'` در registry.ts دست‌نخورده بماند.
//
// چرا تقسیم شد؟ برای خواناییِ علمی و نگه‌داری: هر دستهٔ اندیکاتور (روند/مومنتوم/
// نوسان/حجم/آماری/چرخه‌ای/ساختاری/ترکیبی/الگوی‌کندلی/بسطِ variant) اکنون در فایلِ
// اختصاصیِ خودش قابلِ مرور و ویرایش است. راهنمای استفادهٔ هر اندیکاتور در
// docs/INDICATOR_BANK_GUIDE.md مستند شده است.
//
// قوانینِ حاکم (بدون تغییر نسبت به نسخهٔ یکپارچهٔ قبلی):
//   1) همهٔ توابع *بدونِ look-ahead*اند: فقط از دادهٔ تا اندیسِ i استفاده می‌کنند.
//   2) همهٔ ورودی‌ها با `active:false` ثبت می‌شوند — «وجود» دارند اما تا وقتی یک لایهٔ
//      استراتژی صریحاً صدایشان نزند، محاسبه/فعال نمی‌شوند (کشِ تنبلِ رجیستری).
//   3) additive/Strangler-Fig: مسیرِ /api/decision و برابریِ بیت‌به‌بیتِ snapshotِ طلایی
//      دست‌نخورده می‌ماند؛ این فایل هیچ رفتارِ تصمیمِ موجودی را تغییر نمی‌دهد.
//   4) XAU حجمِ واقعی ندارد ⇒ اندیکاتورهای حجمی از volume (tick-volume) پروکسی می‌گیرند.
//
// ✅ برابری تأییدشده: تستِ بیت‌به‌بیت روی ۱۵۰۰ کندلِ واقعیِ XAUUSD M5 →
//    ۴۰۱/۴۰۱ اندیکاتور، ۰ اختلافِ مقدار/متادیتا، ۴۰۱ نامِ یکتا، مجموعهٔ نام یکسان.
// ============================================================================

export { BANK_ALL as BANK } from './bank/_aggregate'
