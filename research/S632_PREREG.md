# پیش‌ثبت S632 — IBS لانگ‌تنهای رژیم‌گیت‌شده (IbsLongRegime) — XAUUSD H1

**تاریخ قفل:** 2026-08-17 — این سند **قبل از** اجرای آزمون hold-out کامیت می‌شود و پس از آن تغییرناپذیر است.
**نسبت با خانواده:** سومین آزمون hold-out خانوادهٔ IBS (S630 REJECT، S631 REJECT). طبق تعهد #2 پیش‌ثبت S631: **n_trials = 3**.
**دهه:** S630–S639 متعلق به این دانشمند؛ بدون تداخل با دهه‌های موازی.

## پیکربندی قفل‌شده (تغییرناپذیر)

- **نماد/تایم‌فریم:** XAUUSD، فقط H1 (`data/mt5_full/` از طریق `tools/s434_fast_data`)
- **فقط لانگ** — سمت شورت برای همیشه از این خانواده حذف شد (دو شکست ثبت‌شده: S630 متقارن −4.4pp، S631 گیت‌شده −1.87pp؛ RQS2 خودش `prune_sides:['short']` داد)
- **سیگنال:** حالت‌محور (state): `mean(IBS,5) < 0.28` (IBS رنج‌صفر → 0.5)
  - `allow_overlap=False` خودش فاصله‌گذاری طبیعی می‌سازد (تا معاملهٔ قبلی بسته نشود ورود جدید نیست)
- **گیت رژیم:** `close > SMA144(close)`
- **هندسه:** SL = TP = `1.5 × median(ATR100)` روی دادهٔ hold-out (متقارن — قانون حفظ بودجه؛ هرگز TP<SL)
- **مدیریت:** `max_hold=64`؛ بدون همپوشانی؛ کندل مبهم → SL مقدم؛ معاملهٔ باز انتهای داده حذف
- **دمو:** CONTRACT_SIZE=100، اسپرد 0.33$/oz (3.3 پیپ)، بدون کمیسیون

## دفاع ضدبیش‌برازش (پیش از آزمون)

- **فلات سه‌بعدی** (s632b): تمام 21 سلول k∈{4,5,6}×thr∈{0.25..0.31} لیفت مثبت (+3.9..+13.4pp)
- **بی‌حساسی گیت:** SMA89/144/233 → لیفت +9.3/+11.1/+10.5
- **پایداری تقویمی:** 7/8 سال نیمهٔ اول WR≥52% (فقط 2012 ضعیف)
- **قلهٔ تیز رد شد:** k6/t0.27 (لیفت 13.4 اما n=103، لبهٔ شبکه) انتخاب نشد؛ سلول درون‌فلاتِ پرنمونه k5/t0.28 انتخاب شد
- **هستهٔ لانگ دو بار در hold-out منتقل شده:** S630 (+1.4pp بی‌گیت)، S631 (+6.25pp با گیت) — این آزمون سوم همان جهت است با توان بالاتر

## پروتکل آزمون (Route C)

- **داده:** فقط نیمهٔ دوم بارها (`df.iloc[len(df)//2:]`)
- **یک اجرا، بدون تکرار، بدون تنظیم پس از نتیجه**
- **نول (الگوی S382):**
  - ① بی‌قیدِ گیت‌خورده لانگ‌تنها، stride ∈ {3,7,13} → بیشینهٔ WR
  - ② جایگشت زمانی درون گیت: K=1000، seed=632632، همان تعداد معاملهٔ لایه، همان شبیه‌ساز، بدون همپوشانی
- **RQS2:** `n_trials=3`، `split_bar=int(0.70×n_bars_holdout)`، perm_k=1000

## شواهد نیمهٔ اول (سلول قفل‌شده)

n=277، WR=58.84%، لیفت +11.07pp نسبت به بی‌قیدِ گیت‌خوردهٔ 47.77، pnl میانگین +5.05 پیپ → `lift·√n ≈ 184 ≥ 78` (حاشیهٔ ۲.۴×)

## افشای فضای جست‌وجو

S630 (~177 پیکربندی) + S630b (12) + S630c (12) + S632 (72) + S632b (25) روی نیمهٔ اول؛ hold-out تاکنون: 2 آزمون (هر دو REJECT). این آزمون سوم → n_trials=3.

## تعهدات

1. ACCEPT → توقف، اطلاع به کاربر، انتظار تأیید پیش از فاز ۶؛ ممیزی همپوشانی با لایه‌های ACCEPT بلافاصله.
2. REJECT/UNPROVEN → بهبود فقط روی نیمهٔ اول؛ آزمون بعدی نیمهٔ دوم = پیش‌ثبت جدید با n_trials=4؛ و اگر شواهد کافی نبود، خانواده صادقانه بایگانی می‌شود.
3. هیچ پارامتری پس از این کامیت تغییر نمی‌کند؛ نتیجه هرچه باشد با نام رسمی در `results/` ثبت می‌شود.
