# S990 PREREG — R2TrendBirthLong — XAUUSD-H1 — کامیت پیش از هر عدد نیمهٔ دوم

**دانشمند:** لاگرانژ (S990–S999) · **اکتشاف:** `strategies/s990_explore_r2_birth.py` → `research/s990_explore/r2_birth.json` (فقط نیمهٔ اول)

## نتیجهٔ اکتشاف (صادقانه)
۲۷ سلول (TF×W×آستانه×جهت)، هیچ‌کدام z_fair>1.1؛ علامت لیفت تصادفی بین سلول‌های مجاور. فرضیهٔ «شتاب r² = تولد روند» در نیمهٔ اول **پشتیبانی نشد**. طبق قانون کاربر (هر شماره = حکم رسمی) بهترین سلول منجمد بی‌درنگ داوری می‌شود؛ پیش‌بینی صادقانه: **REJECT**.

## قاعدهٔ منجمد (انتخاب = max lift·√n میان سلول‌های n≥50 اکتشاف)
- TF=H1، نیمهٔ دوم (`df.iloc[len//2:]`)، **LONG** تنها
- r² رگرسیون خطی close بر زمان، W=21 (علّی)
- لبه: `r2>0.80` و `min(r2[i-3..i-1])<0.40`؛ فقط اولین کندل لبه
- جهت: شیب رگرسیون >0 ⇒ لانگ · گیت نول = شیب>0
- SL=TP=1.5×median(ATR100) پیپ، max_hold=48، allow_overlap=False، اسپرد 3.3 پیپ
- نول: ① سخت‌ترین stride {3,7,13} درون گیت ② جایگشت K=500 درون گیت، seed=990990
- `compute_rqs2(..., n_trials=27, split_bar=0.70·len)` — تعداد کل سلول‌های اکتشاف
- یک اجرا. حکم موتور = کلمهٔ نهایی. هیچ بهبود پس-از-مشاهده.
