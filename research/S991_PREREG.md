# S991 PREREG — AutocorrMomLong — XAUUSD-H8 — کامیت پیش از هر عدد نیمهٔ دوم

**دانشمند:** لاگرانژ (S990–S999) · **اکتشاف:** `strategies/s991_explore_autocorr_regime.py` → `research/s991_explore/autocorr_regime.json` (فقط نیمهٔ اول)

## نتیجهٔ اکتشاف (صادقانه)
۱۵۹ سلول (TF{H1,H4,H8,D1}×W{34,89,233}×θ{0.1,0.2}×q{0,0.5}×رژیم×جهت). **فقط ۱۲ سلول لیفت مثبت**؛ بهترین z=+0.99. پنج سلول با z≤−3.3 (ضدمهارت قوی: مومنتوم H1/H4 با ρ₁>θ). فرضیهٔ «رژیم ρ₁ غلتان» در نیمهٔ اول **رد شد**؛ نشانهٔ روشن: پنجرهٔ خودهمبستگی رژیم را وقتی تأیید می‌کند که تمام شده (انتگرال ≠ مشتق — قانون S616). پیش‌بینی صادقانه: **REJECT**.

## قاعدهٔ منجمد (max lift·√n میان سلول‌های n≥50 = H8 W34_th0.2_q0.5_mom_long)
- TF=H8، نیمهٔ دوم، **LONG** تنها
- r = diff(log close)؛ ρ₁ = corr(r, r.shift(1)) غلتان W=34، shift(1) (علّی)
- رژیم/گیت: ρ₁ > 0.20 · فیلتر: |r_t| > median(|r|)_34 (shift(1)) · جهت: r_t > 0 ⇒ لانگ
- SL=TP=1.5×median(ATR100) پیپ، max_hold=21، allow_overlap=False، اسپرد 3.3
- نول: ① سخت‌ترین stride {3,7,13} درون گیت ② جایگشت K=500 درون گیت، seed=991991
- `compute_rqs2(..., n_trials=159, split_bar=0.70·len)`؛ یک اجرا؛ هیچ بهبود پس-از-مشاهده
