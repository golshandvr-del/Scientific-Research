# S992 PREREG — ErShockLong — XAUUSD-H1 — کامیت پیش از هر عدد نیمهٔ دوم

**دانشمند:** لاگرانژ (S990–S999) · **اکتشاف:** `strategies/s992_explore_er_shock.py` (92 سلول) + `strategies/s992b_refine_er.py` (54 سلول) → `research/s992_explore/` (فقط نیمهٔ اول)

## نتیجهٔ اکتشاف (صادقانه)
- خانوادهٔ ER کافمن در بانک بکر بود. ابطال‌گر P1 (S965) گذشت: لبهٔ ER (پرش lo→hi در ≤k) در H1/H8 همه‌جا به‌مراتب بهتر از حالت (ER>hi) است ⇒ اطلاعات در «پرش» است، نه سطح (قانون S963).
- H1 لانگ W13: پلاتوی ۶ سلولی (۳ باند × ۲ RR) همه lift ∈ [+6.9, +31.8]، سال‌های مثبت 5–7/8. قلهٔ z=3.42 فقط n=29 ⇒ **گیلاس‌چینی نمی‌کنم** (قانون S633)؛ سلول پرجمعیت پلاتو منجمد می‌شود.
- H8 شورت W8: مثبت اما 4/8 سال، z≤1.8 ⇒ رد در انجماد. H8 لانگ همان لبه: ضدمهارت قوی (عدم‌تقارن جهتی S522 — درفت سکولار).
- پیش‌بینی صادقانه: **REJECT یا POWER-LIMITED** (z منصفانهٔ سلول منجمد 1.5 < 3.09؛ اما پلاتو واقعی است).

## قاعدهٔ منجمد
- TF=H1، نیمهٔ دوم (`df.iloc[len//2:]`)، **LONG** تنها
- ER = |close−close.shift(13)| / Σ|Δclose|₁₃ · لبه: ER>0.70 و min(ER[i−3..i−1])<0.30 · فقط اولین کندل لبه
- جهت: close−close.shift(13) > 0 ⇒ لانگ · گیت نول = همان
- SL=1.5×median(ATR100) پیپ، **TP=1.5×SL** (قانون بودجه)، max_hold=48، allow_overlap=False، اسپرد 3.3
- نول: ① سخت‌ترین stride {3,7,13} درون گیت ② جایگشت K=500 درون گیت، seed=992992
- `compute_rqs2(..., n_trials=146, split_bar=0.70·len)` (92+54 سلول)؛ یک اجرا؛ هیچ بهبود پس-از-مشاهده
