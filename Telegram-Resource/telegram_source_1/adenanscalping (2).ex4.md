# بررسیِ منبع: `adenanscalping (2).ex4`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر، مارجین `40$/لات`.

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | Expert Advisor (EA) کامپایل‌شدهٔ MT4 (`.ex4`) — سورس بسته. |
| **نام** | «Adenanku scalping» — **نسخهٔ کپیِ دومِ** فایلِ `adenanscalping.ex4`. |
| **حجم** | ~۳۲KB (**بایت‌به‌بایت مشابهِ نسخهٔ اصلی؛ حجم و رشته‌ها یکسان**). |

## ۲. تشخیص: نسخهٔ تکراری

این فایل از نظرِ حجم (۳۱۹۹۲ بایت) و رشته‌های داخلی **کاملاً یکسان** با `adenanscalping.ex4`
است (تنها پسوندِ `(2)` نشان‌دهندهٔ کپیِ دانلودِ دوم است). بنابراین منطق، پارامترها و تصمیم
دقیقاً همان است.

## ۳. منطق و تصمیم (ارجاع)

- منطقِ کامل، پارامترها (LotMultiplikator، Distance، MaxTrades، Martingale on/off، …) و
  تصمیمِ نهایی در فایلِ **`adenanscalping.ex4.md`** مستند شده است.
- **کلاسِ استراتژی:** ⚠️ مارتینگل-گرید.
- **تصمیم:** ❌ **رد** — ناسازگار با مدیریتِ ریسکِ پروژه (خطرِ نابودیِ حساب، سودِ خالصِ منفیِ بلندمدت).

## ۴. ارتباط با پرتفویِ فعلی

- **همپوشانی:** ۱۰۰٪ با `adenanscalping.ex4` (فایلِ یکسان). لبهٔ نو یا فیلترِ اضافه‌ای ندارد.

## ۵. نتیجهٔ بک‌تست

**N/A** — نسخهٔ تکراری؛ طبق دستورِ نشستِ جاری فقط فایلِ md بررسی ساخته شد. Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده (کپیِ تکراریِ adenanscalping؛ مارتینگل، رد). موردِ بعدی بررسی می‌شود._
