# بررسیِ منبع: `SentinelAI MT4.ex4`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر، مارجین `40$/لات`.

**Expert Advisor بی‌سورس با برندینگِ "AI" (MT4).**

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | EA کامپایل‌شدهٔ MT4 (`.ex4`) — سورس بسته. |
| **نام** | SentinelAI (برندینگِ «AI»). |
| **حجم** | ~۷۸۷KB (**بسیار بزرگ** ⇒ احتمالاً شاملِ مدل/جدولِ داده یا منطقِ سنگینِ چندماژولی). |
| **رشته‌های استخراج‌شده** | ❌ هیچ رشتهٔ معناداری. |
| **تایم‌فریم/دارایی هدف** | نامشخص. |

## ۲. منطقِ استخراج‌شده (استنتاجِ محتاطانه)

- سورس بسته ⇒ منطقِ دقیق قابلِ استخراج نیست.
- حجمِ بسیار بزرگ (~۷۸۷KB) در EAها معمولاً یا به‌خاطرِ **جدولِ ضرایب/مدلِ درون‌ساخت** است یا
  **گرافیک/داشبوردِ سنگین**. برچسبِ «AI» در بازارِ EAها معمولاً بازاریابی است و لزوماً مدلِ
  یادگیریِ واقعی نیست.

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

- هیچ منطقِ اختصاصیِ قابلِ استخراج وجود ندارد.

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ بی‌سورس.
- **فیلترِ تأیید:** ❌ چیزی برای استخراج ندارد.
- **جمع‌بندی:** منبعِ کم‌ارزش (EAِ بستهٔ «AI»‌مارک‌دار).

## ۵. نتیجهٔ بک‌تست

**N/A** — طبق دستورِ نشستِ جاری فقط فایلِ md بررسی ساخته می‌شود؛ بک‌تست نشد. Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده (EAِ بی‌سورسِ «AI»، کم‌ارزش). موردِ بعدی بررسی می‌شود._
