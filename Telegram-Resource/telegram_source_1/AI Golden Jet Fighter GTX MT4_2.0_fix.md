# بررسیِ منبع: `AI Golden Jet Fighter GTX MT4_2.0_fix`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر.

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | EAِ MT4 کامپایل‌شده (`.ex4`) — سورس بسته، همراهِ فایل‌های تبلیغاتی. |
| **نام** | `AI Golden Jet Fighter GTX` — بازارگرمیِ «AIِ طلا»؛ دارایی هدف: **XAUUSD**. |
| **همراهان** | `INFO TRADER.txt` (فقط لینک‌های تلگرام/بروکر/VPS)، صفحهٔ HTMLِ «FREE PREMIUM EAS»، docx/pdfِ عمومی. |
| **رشته‌های `.ex4`** | ❌ obfuscated — هیچ پارامتر/منطقی خوانده نشد. |

## ۲. منطقِ استخراج‌شده

❌ **قابلِ استخراج نیست.** باینریِ بسته؛ فایل‌های همراه صرفاً تبلیغاتی‌اند (کانالِ تلگرام
`@jonavyfxtrade`/`@axleas`، بروکرهای Vantage/Exness با لینکِ افیلیت، هشدارِ «۹۵٪ تریدرها
ضرر می‌کنند»). این الگویِ توزیعِ «EA رایگانِ AIِ طلا» تقریباً همیشه جعبهٔ سیاهِ
grid/martingale است که ادعایِ عملکردش اثبات‌نشده است.

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

❌ **هیچ.** بدونِ منطقِ قابلِ استخراج و بدونِ توضیحِ فنی، فرضیهٔ بک‌تست‌پذیری ساخته نمی‌شود.

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ — **فیلترِ تأیید:** ❌. جعبهٔ سیاهِ تبلیغاتی.

## ۵. نتیجهٔ بک‌تست

**N/A** — طبق User Note فقط md بررسی ساخته شد. Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده و کنار گذاشته شد (EAِ obfuscatedِ تبلیغاتی، بدونِ اطلاعاتِ قابلِ استخراج). موردِ بعدی بررسی می‌شود._
