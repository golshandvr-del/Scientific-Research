# بررسیِ منبع: `170. Instant Buy Sell sl/`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر، مارجین `40$/لات`.

سی‌وهفتمین موردِ بررسی‌شده (ترتیبِ الفباییِ `LC_ALL=C`).

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | سیستمِ سیگنالِ «Instant Buy Sell» MT4 — پنج اندیکاتورِ بسته + تمپلیت. |
| **اجزا** | `Indicators/001.ex4 … 005.ex4` (نام‌های بی‌معنیِ obfuscated)، `Template/buysell.tpl`. |
| **فایلِ همراه** | `LEER_PASWORD (Protegido).txt` (تبلیغِ کانالِ AXE)، `CONTRIBUCIONES.txt`. |
| **رشته‌های استخراج‌شده** | ❌ هیچ (ex4ها obfuscated؛ tpl باینری/غیرِمتنی). |

## ۲. منطقِ استخراج‌شده

- سیستمی که با نصبِ تمپلیت، ۵ اندیکاتور را روی چارت می‌چیند و **فلشِ آنیِ Buy/Sell**
  می‌سازد. نام‌گذاریِ `001..005` عمداً obfuscate شده تا مهندسیِ معکوس سخت شود.
- نه سورس، نه مستنداتِ قانونِ ورود/خروج، نه پارامترِ قابلِ خواندن ⇒ **منطقِ عددی صفر**.
- txt فقط لینکِ تبلیغاتی و درخواستِ رمز است.

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

- ❌ چیزی برای ترجمه به قانونِ بک‌تست‌پذیر نیست (جعبهٔ سیاهِ obfuscated).

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ هیچ.
- **فیلترِ تأیید:** ❌ هیچ.

## ۵. نتیجهٔ بک‌تست

**N/A** — اندیکاتورهای obfuscated و بدونِ مستندات. Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده (سیستمِ فلشِ بستهٔ «Instant Buy Sell»؛ بدونِ لبهٔ قابلِ استخراج). موردِ بعدی بررسی می‌شود._
