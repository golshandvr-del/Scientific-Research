# بررسیِ منبع: `Alchemy Pulse Indicateor.ex5`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر.

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | **اندیکاتورِ MT5 کامپایل‌شده (`.ex5`)** — سورس بسته. |
| **نام** | `Alchemy Pulse Indicator` (املای فایل: "Indicateor") — نامِ تجاری؛ «Pulse» معمولاً به momentum/نوسانِ ضربانی اشاره دارد. |
| **سورس** | ❌ بسته؛ بدونِ مستندِ همراه ⇒ منطق قابلِ اثبات نیست. |

## ۲. منطقِ استخراج‌شده

⚠️ **ناقص.** اندیکاتورهای با نامِ «Pulse» عموماً یک اسیلاتورِ momentum (مانندِ ترکیبِ RSI/MACD/تغییرِ نرخ) هستند که «ضربانِ» بازار را نشان می‌دهند و اغلب سیگنالِ خرید/فروش با تغییرِ رنگ/آستانه می‌دهند. اما بدونِ سورس یا مستند، این صرفاً حدس است.

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

⚠️ **کم.** مفهومِ momentum-oscillator از پیش در ابزارهای استانداردی (RSI, MACD, ROC) موجود و قابلِ آزمون است؛ این فایل چیزِ اثبات‌شده‌ای اضافه نمی‌کند.

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ — **فیلترِ تأیید:** ❌ (بدونِ منطقِ قابلِ استخراج).

## ۵. نتیجهٔ بک‌تست

**N/A** — `.ex5` بستهٔ بی‌مستند. Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده و کنار گذاشته شد (`.ex5` بستهٔ بی‌مستند؛ منطق قابلِ اثبات نیست). موردِ بعدی: `BB AO EMA V2.6.1 OFFICIAL trial_fix.ex5`._
