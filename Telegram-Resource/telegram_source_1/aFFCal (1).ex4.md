# بررسیِ منبع: `aFFCal (1).ex4`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر، مارجین `40$/لات`.

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | اندیکاتورِ MT4 کامپایل‌شده (`.ex4`) — **نسخهٔ کپیِ دومِ** `aFFCal.ex4`. |
| **حجم** | ~۲۹KB (**بایت‌به‌بایت مشابهِ نسخهٔ اصلی**، حجم یکسان ۲۹۱۹۶). |

## ۲. تشخیص: نسخهٔ تکراری

این فایل از نظرِ حجم و ماهیت **کاملاً یکسان** با `aFFCal.ex4` است (تنها پسوندِ `(1)` نشان‌دهندهٔ
کپیِ دانلودِ دوم). منطق، منبعِ داده و کاربرد دقیقاً همان است.

## ۳. منطق و تصمیم (ارجاع)

- ابزارِ **تقویمِ اقتصادیِ Forex Factory** (فیدِ XML؛ نمایشِ اخبارِ High/Medium/Low روی چارت).
- منطقِ کامل و ایدهٔ **فیلترِ News-blackout** در فایلِ **`aFFCal.ex4.md`** مستند شده است.

## ۴. ارتباط با پرتفویِ فعلی

- **همپوشانی:** ۱۰۰٪ با `aFFCal.ex4`.
- **فیلترِ تأیید:** ✅ همان کاندیدِ فیلترِ News-blackout (به `aFFCal.ex4.md` مراجعه شود).

## ۵. نتیجهٔ بک‌تست

**N/A** — نسخهٔ تکراری؛ فقط فایلِ md بررسی ساخته شد. Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده (کپیِ تکراریِ aFFCal). موردِ بعدی بررسی می‌شود._
