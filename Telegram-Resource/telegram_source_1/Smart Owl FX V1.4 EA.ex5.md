# بررسیِ منبع: `Smart Owl FX V1.4 EA.ex5`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر، مارجین `40$/لات`.

بررسیِ نشستِ جاری (ترتیبِ الفبایی — پس از `SWEEP_FVG EA`).

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | Expert Advisor (EA) کامپایل‌شدهٔ MT5 (`.ex5`) — سورس بسته. |
| **نام** | Smart Owl FX V1.4. |
| **حجم** | ~۲۴۴KB (**بزرگ** ⇒ چند‌ماژولی: احتمالاً منطقِ ورود + مدیریتِ لات/گرید + داشبورد + منابعِ گرافیکی مثلِ `logo_owl.bmp`). |
| **رشته‌های استخراج‌شده** | فقط برندِ توزیع `https://t.me/ebfe90` و `Images\logo_owl.bmp`. هیچ نام‌پارامتر/منطقِ داخلی لو نرفت. |
| **تایم‌فریم/دارایی هدف** | نامشخص؛ EAِ عمومیِ خودکار. |

## ۲. منطقِ استخراج‌شده (استنتاجِ محتاطانه)

- سورس بسته ⇒ منطقِ دقیق قابلِ استخراج نیست.
- حجمِ بزرگ در EAهای خودکار معمولاً به‌معنیِ **منطقِ مدیریتِ پوزیشنِ سنگین** است (گرید/
  مارتینگل/averaging) یا داشبوردِ گرافیکیِ حجیم. برچسبِ «Smart» بازاریابی است.
- بدونِ سورس، قاعدهٔ ورود/خروج و اینکه آیا از گرید/مارتینگلِ پرریسک استفاده می‌کند نامعلوم است.

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

- هیچ منطقِ اختصاصیِ قابلِ استخراج از باینری وجود ندارد.
- اگر EA گرید/مارتینگل باشد، طبقِ اصولِ پروژه (تمرکز بر سودِ خالصِ پایدار + WR≥40)، این کلاس
  ریسکِ drawdownِ فاجعه‌بار دارد و به‌طورِ پیش‌فرض جذاب نیست.

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ بی‌سورس.
- **فیلترِ تأیید:** ❌ چیزی برای استخراج ندارد.
- **جمع‌بندی:** منبعِ کم‌ارزش (EAِ خودکارِ بستهٔ برنددار، احتمالِ گرید/مارتینگل). برای کامل‌شدنِ فهرست ثبت شد.

## ۵. نتیجهٔ بک‌تست

**N/A** — طبق دستورِ نشستِ جاری (User Note) فقط فایلِ md بررسی ساخته می‌شود؛ بک‌تست نشد.
Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده (EAِ خودکارِ بی‌سورس؛ کم‌ارزش). موردِ بعدی بررسی می‌شود._
