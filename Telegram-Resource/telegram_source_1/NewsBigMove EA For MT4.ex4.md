# بررسیِ منبع: `NewsBigMove EA For MT4.ex4`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر، مارجین `40$/لات`.

**سورس بسته، اما نامِ صریح ⇒ همان کلاسِ News-Straddle/Breakout.**

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | Expert Advisor (EA) کامپایل‌شدهٔ MT4 (`.ex4`) — سورس بسته و **محافظت‌شده**. |
| **نام** | NewsBigMove EA — معاملهٔ حرکتِ بزرگِ خبری. |
| **حجم** | ~۳۸KB. |
| **رشته‌های استخراج‌شده** | ❌ هیچ رشتهٔ معناداری. |

## ۲. منطقِ استخراج‌شده (استنتاج از نام)

- کلاسِ «News Big Move» = معاملهٔ **حرکتِ ناگهانیِ بزرگ حولِ خبر**، شبیهِ `News Catcher Pro`:
  یا استریدلِ pending دو-طرفه، یا breakout روی رنجِ پیش‌ازخبر.
- منطقِ دقیق و پارامترها بسته‌اند.

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

- همانندِ `News Catcher Pro`: نیازمندِ **تقویمِ خبریِ زمان‌دار** که در `data/` نیست ⇒ بک‌تستِ مستقیمِ straddle ممکن نیست.
- ایدهٔ قابلِ استفاده: **breakout روی رنجِ فشرده** (بدونِ نیاز به خبر) — تشخیصِ فشردگیِ دامنه و ورود در شکست؛
  این نسخهٔ دارایی-محور (نه خبر-محور) روی `data/` بک‌تست‌پذیر است.

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ بدونِ سورس + وابسته به دادهٔ خبری.
- **فیلترِ تأیید:** △ فقط «فیلترِ اجتنابِ خبر» (مثلِ موردِ قبل) قابلِ بهره‌برداری است.
- **جمع‌بندی:** هم‌کلاسِ `News Catcher Pro`؛ لبهٔ مستقلِ جدیدی نمی‌افزاید.

## ۵. نتیجهٔ بک‌تست

**N/A** — طبق دستورِ نشستِ جاری فقط فایلِ md بررسی ساخته می‌شود؛ بک‌تست نشد. Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده (news big-move EA بی‌سورس؛ هم‌کلاسِ News Catcher؛ نیازمندِ فیدِ خبری). موردِ بعدی بررسی می‌شود._
