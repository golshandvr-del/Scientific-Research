# بررسیِ منبع: `SIGNAL DETECTOR.ex5`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر، مارجین `40$/لات`.

بررسیِ نشستِ جاری (ترتیبِ الفبایی — پس از `SIDEWAYS TREND`).

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | اندیکاتور/سیگنالِ کامپایل‌شدهٔ MT5 (`.ex5`) — سورس بسته. |
| **نام** | Signal Detector. |
| **حجم** | ~۲۶KB. |
| **رشته‌های استخراج‌شده** | فقط رشته‌های بازاریابی UTF-16: `CLICK HERE` و `https://t.me/paidfxtools` (کانالِ توزیعِ ابزارهای پولیِ فارکس). هیچ نام‌پارامتر/منطقِ داخلی لو نرفت. |
| **تایم‌فریم/دارایی هدف** | نامشخص؛ اندیکاتورِ عمومیِ چند‌بازاره. |

## ۲. منطقِ استخراج‌شده (استنتاجِ محتاطانه)

- نام عام «Signal Detector» ⇒ یک **اندیکاتورِ فلش/پیکانِ خرید‌فروش** است که روی چارت
  پیکانِ Buy/Sell رسم می‌کند. این کلاسِ محصول در آرشیوِ تلگرام بارها دیده شده و تقریباً
  همیشه بر پایهٔ یکی از این‌هاست: تقاطعِ MA، اشباعِ RSI/Stoch، شکستِ کانال، یا الگوی کندلی.
- ⚠️ ریسکِ بسیار بالای **repaint**: اندیکاتورهای «Signal/Arrow» تلگرامی معمولاً پیکان را
  روی کندلِ بسته‌نشده جابه‌جا می‌کنند و در بک‌تستِ چشمی «۱۰۰٪ برنده» به‌نظر می‌رسند.
- برچسبِ کانالِ `paidfxtools` نشان می‌دهد این نسخهٔ رایگان/تبلیغاتیِ یک ابزارِ پولی است.

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

- هیچ منطقِ اختصاصیِ قابلِ استخراج از باینری وجود ندارد؛ فقط «کلاسِ عام» = سیگنالِ پیکانی.
- تنها ترجمهٔ عام: یک ژنراتورِ سیگنالِ تقاطعی/اشباعی که **جزوِ استراتژی‌های استانداردِ
  پیش‌تر‌آزموده** است — چیزِ نو ندارد.

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ بی‌سورس + ریسکِ repaint.
- **فیلترِ تأیید:** ❌ چیزی برای استخراج به‌عنوان فیلتر ندارد.
- **جمع‌بندی:** منبعِ کم‌ارزش (اندیکاتورِ پیکانیِ بستهٔ تبلیغاتی). صرفاً برای کامل‌شدنِ فهرست ثبت شد.

## ۵. نتیجهٔ بک‌تست

**N/A** — طبق دستورِ نشستِ جاری (User Note) فقط فایلِ md بررسی ساخته می‌شود؛ بک‌تست نشد.
Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده (اندیکاتورِ سیگنالِ پیکانیِ بی‌سورس، ریسکِ repaint؛ کم‌ارزش). موردِ بعدی بررسی می‌شود._
