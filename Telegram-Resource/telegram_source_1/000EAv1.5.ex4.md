# بررسیِ منبع: `000EAv1.5.ex4`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر.

پنجمین موردِ بررسی‌شده (ترتیبِ الفباییِ `LC_ALL=C`).

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | **EA (Expert Advisor) MT4 کامپایل‌شده (`.ex4`)** — سورس بسته. |
| **نام** | 000EA v1.5 (نامِ عمومی/بی‌معنا — پیشوندِ `000` معمولاً برای بالا آمدن در فهرست). |
| **حجم** | ~۱۳KB. |
| **رشته‌های قابلِ استخراج** | ❌ هیچ — خروجیِ `strings` کاملاً نویزِ باینری/obfuscated است؛ حتی نامِ پارامتر یا متنِ راهنما پیدا نشد. |

## ۲. منطقِ استخراج‌شده

❌ **قابلِ استخراج نیست.** فایلِ `.ex4`ِ کامپایل‌شده و به‌ظاهر obfuscated است. هیچ نامِ
پارامتر، سطحِ عددی، تایم‌فریمِ هدف یا متنِ توضیحی قابلِ خواندن نیست. نامِ فایل (`000EA`)
هم هیچ سرنخی از استراتژی نمی‌دهد.

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

❌ **هیچ.** بدونِ منطقِ قابلِ استخراج و بدونِ حتی نامِ معنادار، هیچ فرضیهٔ بک‌تست‌پذیری
از این فایل قابلِ ساخت نیست.

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ — **فیلترِ تأیید:** ❌. جعبهٔ سیاهِ بدونِ اطلاعات.

## ۵. نتیجهٔ بک‌تست

**N/A** — طبق دستورِ نشستِ جاری (User Note) فقط فایلِ md بررسی ساخته می‌شود؛ بک‌تست نشد.
منبع منطقِ قابلِ استخراج ندارد. Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده و کنار گذاشته شد (EAِ کامپایل‌شدهٔ obfuscated، بدونِ اطلاعاتِ قابلِ استخراج). موردِ بعدی بررسی می‌شود._
