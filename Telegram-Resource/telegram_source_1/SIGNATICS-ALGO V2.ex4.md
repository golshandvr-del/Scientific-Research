# بررسیِ منبع: `SIGNATICS-ALGO V2.ex4`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر، مارجین `40$/لات`.

بررسیِ نشستِ جاری (ترتیبِ الفبایی — پس از `SIGNAL DETECTOR`).

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | اندیکاتور/سیستمِ سیگنالِ کامپایل‌شدهٔ MT4 (`.ex4`) — سورس بسته. |
| **نام** | Signatics-Algo V2. |
| **حجم** | ~۱۳۰KB (نسبتاً بزرگ ⇒ احتمالاً چند‌ماژولی: داشبورد + پیکان + شاید فیلترِ چند‌تایم‌فریم). |
| **رشته‌های استخراج‌شده** | فقط برندِ توزیع UTF-16: `https://t.me/FxCyberneticsCodesREALaccount`. هیچ نام‌پارامتر/منطقِ داخلی لو نرفت. |
| **تایم‌فریم/دارایی هدف** | نامشخص؛ سیستمِ عمومیِ چند‌بازاره. |

## ۲. منطقِ استخراج‌شده (استنتاجِ محتاطانه)

- «Algo V2» + حجمِ متوسطِ بالا ⇒ یک **سیستمِ سیگنالِ ترکیبی** (بسته‌بندیِ چند اندیکاتور
  زیرِ یک پیکان/داشبورد). برندِ FxCybernetics در آرشیو با چند ابزارِ مشابه دیده می‌شود.
- الگوی رایجِ این کلاس: تجمیعِ چند تأییدیه (روند + مومنتوم + فیلترِ نوسان) و صدورِ یک
  سیگنالِ نهایی. جزئیاتِ دقیق بدونِ سورس قابلِ تأیید نیست.
- ⚠️ ریسکِ repaint و ادعای «no-repaint/high-win» طبقِ الگوی این خانوادهٔ محصولاتِ تلگرامی.

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

- تنها ایدهٔ عام: **رأی‌گیریِ چند‌اندیکاتوره (confluence voting)** — چند تأییدیهٔ مستقل
  باید هم‌جهت شوند تا سیگنال صادر شود. این مفهوم پیش‌تر در پروژه به‌عنوانِ منطقِ «لایه‌بندی»
  حاضر است؛ چیزِ نو ندارد.
- هیچ پارامتر یا وزنِ اختصاصیِ قابلِ استخراج از باینری نیست.

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ بی‌سورس.
- **فیلترِ تأیید:** ⚠️ مفهومِ «confluence voting» بالقوه مفید است اما عمومی و پیش‌تر لمس‌شده؛
  از این باینری چیزِ عملی استخراج نمی‌شود.
- **جمع‌بندی:** منبعِ کم‌ارزش (سیستمِ سیگنالِ ترکیبیِ بستهٔ برنددار). برای کامل‌شدنِ فهرست ثبت شد.

## ۵. نتیجهٔ بک‌تست

**N/A** — طبق دستورِ نشستِ جاری (User Note) فقط فایلِ md بررسی ساخته می‌شود؛ بک‌تست نشد.
Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده (سیستمِ سیگنالِ ترکیبیِ بی‌سورس؛ کم‌ارزش). موردِ بعدی بررسی می‌شود._
