# بررسیِ منبع: `165. Massive-Fx-Profit-indicator/`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر، مارجین `40$/لات`.

سی‌وچهارمین موردِ بررسی‌شده (ترتیبِ الفباییِ `LC_ALL=C`).

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | اندیکاتورِ سیگنالِ فارکس MT4 (`Massive-Fx-Profit-indicator.ex4`) — سورس بسته. |
| **سازنده** | AXE Corporation / GCC Incorporation (کانالِ تلگرام `@EAIndicatorFX`). |
| **فایل‌های همراه** | `LEER PRIMERO- Read First.txt` (فقط تبلیغِ کانال + درخواستِ رمز)، `DONACIONES.txt` (درخواستِ کمکِ مالی). |
| **رشته‌های استخراج‌شده** | ❌ هیچ منطقِ عددی (کامپایلِ بسته؛ txtها فقط لینکِ تبلیغاتی). |
| **نسخهٔ دوم** | `165. Massive-Fx-Profit-indicator (1)` — فایلِ ex4 **بایت‌به‌بایت یکسان** (md5 مشترک `baae163a…`). |

## ۲. منطقِ استخراج‌شده

- «Massive Fx Profit» یک اندیکاتورِ **فلشِ Buy/Sell** تجاری است که با ادعای «سودِ عظیم»
  بازاریابی می‌شود. مثلِ اکثرِ محصولاتِ AXE، سورس بسته و فقط با «رمزِ کانال» فعال می‌شود.
- txtِ «Read First» هیچ قانونِ ورود/خروج یا تنظیماتِ اندیکاتور ندارد؛ صرفاً لینکِ گروه/کانال
  و روشِ گرفتنِ رمز است.
- بدونِ سورس یا مستندات، **منطقِ عددی قابلِ استخراج نیست.**
- ⚠️ نامِ «profit/signals» در این خانوادهٔ محصولات تقریباً همیشه با repaint همراه است؛ تأییدناپذیر.

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

- ❌ چیزی برای ترجمه به قانونِ بک‌تست‌پذیر نیست (جعبهٔ سیاهِ بدونِ مستندات).

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ هیچ.
- **فیلترِ تأیید:** ❌ هیچ.

## ۵. نتیجهٔ بک‌تست

**N/A** — سورس بسته و بدونِ مستندات. Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده (فلشِ تجاریِ بستهٔ خانوادهٔ AXE؛ بدونِ لبهٔ قابلِ استخراج). موردِ بعدی بررسی می‌شود._
