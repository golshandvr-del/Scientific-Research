# بررسیِ منبع: `2018-profits.ex4`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر، مارجین `40$/لات`.

پنجاه‌وششمین موردِ بررسی‌شده (ترتیبِ الفباییِ `LC_ALL=C`). **فایلِ کامپایل‌شدهٔ obfuscated — جعبهٔ سیاه.**

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | اندیکاتور/EA کامپایل‌شدهٔ MT4 (`.ex4`) — سورس بسته. |
| **نام** | «2018-profits» (نامِ بازاریابی‌شده با ادعای سود). |
| **رشته‌های خوانا** | ❌ تقریباً هیچ — فقط ردِ `~MA` دیده شد؛ بقیهٔ رشته‌ها obfuscated/رمزگذاری‌شده‌اند. |

## ۲. منطقِ استخراج‌شده

- خروجیِ `strings` هیچ رشتهٔ معناداری (نه پارامتر، نه Copyright، نه لینک) نمی‌دهد؛ فایل
  به‌شکلِ عمدی مبهم‌سازی (obfuscate) شده تا مهندسیِ معکوس سخت شود.
- تنها ردِ ممکن `~MA` است که می‌تواند اشاره به استفاده از میانگینِ متحرک باشد — اما این
  حدس تأییدناپذیر است.
- **بدونِ سورس و بدونِ مستندات، منطقِ عددی قابلِ استخراج نیست.**

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

- ❌ چیزی برای ترجمه به قانونِ بک‌تست‌پذیر نیست (جعبهٔ سیاهِ مبهم‌سازی‌شده).

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ هیچ.
- **فیلترِ تأیید:** ❌ هیچ.

## ۵. نتیجهٔ بک‌تست

**N/A** — سورس بسته و obfuscated. Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده (ex4 مبهم‌سازی‌شده با نامِ بازاریابیِ «سود»؛ بدونِ لبهٔ قابلِ استخراج). موردِ بعدی بررسی می‌شود._
