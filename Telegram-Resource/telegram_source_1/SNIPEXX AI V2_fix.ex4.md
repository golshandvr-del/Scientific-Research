# بررسیِ منبع: `SNIPEXX AI V2_fix.ex4`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر، مارجین `40$/لات`.

بررسیِ نشستِ جاری (ترتیبِ الفبایی — پس از `SMC AND BINARY SYSTEM`).

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | Expert Advisor (EA) کامپایل‌شدهٔ MT4 (`.ex4`) — سورس بسته. |
| **نام** | SnipeXX AI V2 (نسخهٔ `_fix`). |
| **حجم** | ~۷۶KB. |
| **رشته‌های استخراج‌شده** | فقط `Copyright 2026, Fx Cybernetics Codes` (UTF-16). هیچ نام‌پارامتر/منطقِ داخلی لو نرفت. |
| **تایم‌فریم/دارایی هدف** | نامشخص؛ نامِ «Snipe» به ورودِ نقطه‌ایِ دقیق (اسکالپ) اشاره دارد. |

## ۲. منطقِ استخراج‌شده (استنتاجِ محتاطانه)

- «Snipe» + «AI» ⇒ یک EAِ اسکالپ که خود را «ورودِ تک‌شلیکِ دقیق» بازاریابی می‌کند.
  برندِ FxCybernetics (هم‌خانوادهٔ `SIGNATICS-ALGO V2` در همین آرشیو).
- برچسبِ «AI» در این خانواده تقریباً همیشه بازاریابی است؛ منطقِ محتمل: ترکیبِ ساده از
  مومنتوم/روند + مدیریتِ لات، با SL/TP ثابت یا ATR-based.
- بدونِ سورس هیچ قاعدهٔ ورود/خروجِ دقیقی قابلِ تأیید نیست؛ ریسکِ تطبیقِ منحنی بالاست.

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

- تنها ایدهٔ عام: ورودِ اسکالپِ مومنتومی با خروجِ سریع — که جزوِ استراتژی‌های استانداردِ
  پیش‌ترآزموده است. چیزِ نو یا پارامترِ اختصاصیِ قابلِ استخراج وجود ندارد.

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ بی‌سورس + برچسبِ AIِ اثبات‌نشده.
- **فیلترِ تأیید:** ❌ چیزی برای استخراج ندارد.
- **جمع‌بندی:** منبعِ کم‌ارزش (EAِ بستهٔ «AI»‌مارک‌دار، هم‌خانوادهٔ SIGNATICS). برای کامل‌شدنِ فهرست ثبت شد.

## ۵. نتیجهٔ بک‌تست

**N/A** — طبق دستورِ نشستِ جاری (User Note) فقط فایلِ md بررسی ساخته می‌شود؛ بک‌تست نشد.
Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده (EAِ اسکالپِ بی‌سورسِ «AI»؛ کم‌ارزش). موردِ بعدی بررسی می‌شود._
