# بررسیِ منبع: `ARRAY4EX.tpl`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر.

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | **تمپلیتِ MT4** (`.tpl`) — فقط چیدمانِ چارت؛ فایلِ اندیکاتورِ همراه در این پوشه نیست. |
| **اندیکاتورهای ارجاع‌شده** | `Array4exEntry` (سیگنالِ ورود)، `ArrayDash1` و `ArrayDash2` (دو داشبورد). |
| **رشتهٔ قابلِ استخراج** | فقط نامِ اندیکاتورها؛ **باینریِ اندیکاتورها موجود نیست** ⇒ منطق قابلِ استخراج نیست. |

## ۲. منطقِ استخراج‌شده

❌ **ناقص.** تمپلیت فقط می‌گوید سیستمِ «Array4ex» شاملِ یک اندیکاتورِ ورود
(`Array4exEntry`) و دو داشبورد (`ArrayDash1/2` — احتمالاً نمای چند-جفت/چند-تایم‌فریم) است.
بدونِ فایلِ `.ex4/.mq4`ِ همراه، قانونِ ورود/خروج قابلِ استخراج نیست.

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

❌ **هیچ (فعلاً).** فقط نام در دست است. اگر در جایِ دیگرِ آرشیو باینریِ `Array4ex` پیدا شد،
دوباره بررسی می‌شود. مفهومِ کلیِ «داشبوردِ چند-تایم‌فریم برای تأییدِ ورود» ایدهٔ عمومیِ
شناخته‌شده‌ای است اما بدونِ جزئیات قابلِ آزمون نیست.

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ — **فیلترِ تأیید:** ❌ (بدونِ منطق).

## ۵. نتیجهٔ بک‌تست

**N/A** — تمپلیتِ تنها، بدونِ اندیکاتورِ همراه. Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده و کنار گذاشته شد (تمپلیتِ بدونِ باینریِ همراه؛ منطق قابلِ استخراج نیست). موردِ بعدی بررسی می‌شود._
