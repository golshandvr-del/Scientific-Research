# بررسیِ منبع: `158. winmaxpips/` (`Winmaxpips.ex4`)

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.

بیست‌وهفتمین موردِ بررسی‌شده (ترتیبِ الفباییِ `LC_ALL=C`).

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | اندیکاتورِ MT4 کامپایل‌شده (`.ex4`، ~۵.۷KB) — سورس بسته. |
| **نام** | WINMAXPIPS FOREX (`www.winmaxpips.com`). |
| **فایلِ همراه** | `LEER PRIMERO- Read First.txt` (تبلیغِ کانالِ AXE، بدونِ مستنداتِ فنی). |
| **رشته‌های استخراج‌شده** | فقط نام و دامنهٔ سایت؛ **هیچ منطقی خوانا نبود**. |

## ۲. منطقِ استخراج‌شده

- یک اندیکاتورِ **سیگنالِ تجاریِ بسته** با برندینگِ سایت. حجمِ بسیار کوچک (~۵.۷KB) نشان
  می‌دهد که هستهٔ ساده‌ای (احتمالاً یک اندیکاتورِ پایه مثل MA/کانال با فلشِ Buy/Sell) دارد،
  اما **بدونِ سورس، منطقِ دقیق قابلِ استخراج نیست.**
- ⚠️ اندیکاتورهای «max pips» معمولاً فلشِ repaint می‌دهند؛ بدونِ سورس نمی‌توان تأیید کرد.

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

- ❌ چیزی برای ترجمه به قانونِ بک‌تست‌پذیر وجود ندارد (جعبهٔ سیاهِ کوچک، بدونِ مستندات).

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ هیچ (جعبهٔ سیاه، بدونِ منطقِ قابلِ استخراج).
- **فیلترِ تأیید:** ❌ هیچ.

## ۵. نتیجهٔ بک‌تست

**N/A** — سورس بسته و بدونِ مستندات؛ چیزی برای آزمون نیست. Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده (سیگنالِ تجاریِ بسته و کوچک؛ بدونِ منطقِ قابلِ استخراج). موردِ بعدی بررسی می‌شود._
