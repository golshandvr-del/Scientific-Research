# بررسیِ منبع: `New_Tuyul_Online_V7.00_m1.ex5`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر، مارجین `40$/لات`.

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | Expert Advisor (EA) کامپایل‌شدهٔ MT5 (`.ex5`) — سورس بسته و **محافظت‌شده**. |
| **نام** | New Tuyul Online V7.00 (تایم‌فریمِ صریح **M1**). «Tuyul» اصطلاحی اندونزیایی برای EAهای اسکالپِ سریع. |
| **حجم** | ~۶۲KB. |
| **رشته‌های استخراج‌شده** | ❌ فقط بایتِ نامفهوم. |

## ۲. منطقِ استخراج‌شده (استنتاجِ محتاطانه)

- سورس بسته ⇒ منطقِ دقیق قابلِ تأیید نیست.
- کلاسِ «Tuyul M1 online» معمولاً **اسکالپرِ فرکانس‌بالا روی اسپرد/تیک** با ریسکِ گرید/مارتینگلِ پنهان است؛
  اغلب روی اسپردِ واقعی و کمیسیون زیان‌ده و به latency حساس است ⇒ **نامناسب برای مدلِ ما** (اسپردِ طلا 3.3pip قابلِ‌توجه).

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

- هیچ منطقِ اختصاصیِ قابلِ استخراج نیست؛ ماهیتِ M1-tick-scalp با هزینهٔ واقعیِ ما (اسپردِ 3.3pip طلا) سازگار نیست.

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ بدونِ سورس + کلاسِ پرریسک.
- **فیلترِ تأیید:** ❌ چیزی برای استخراج ندارد.
- **جمع‌بندی:** منبعِ کم‌ارزش و پرریسک (M1 tick-scalper بی‌سورس؛ احتمالِ گرید/مارتینگل).

## ۵. نتیجهٔ بک‌تست

**N/A** — طبق دستورِ نشستِ جاری فقط فایلِ md بررسی ساخته می‌شود؛ بک‌تست نشد. Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده (ex5 بستهٔ M1 tick-scalper؛ کم‌ارزش/پرریسک). موردِ بعدی بررسی می‌شود._
