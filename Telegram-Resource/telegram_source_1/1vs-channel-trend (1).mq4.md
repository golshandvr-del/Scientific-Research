# بررسیِ منبع: `1vs-channel-trend (1).mq4`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر، مارجین `40$/لات`.

پنجاه‌وچهارمین موردِ بررسی‌شده (ترتیبِ الفباییِ `LC_ALL=C`). **نسخهٔ تکراریِ (duplicate) موردِ ۵۳ام.**

---

## ۱. چیستی

- **کپیِ بایت‌به‌بایتِ `1vs-channel-trend.mq4`** — تأییدِ md5 مشترک:
  `e683390f3a9bdf8eacbdc6a717973599` برای هر دو فایل.
- همان اندیکاتورِ `1VS_ChanelTrend_V1.0` (Vladimir Sitnikov, 2010) — ترسیمِ خودکارِ خطِ
  روند/کانالِ pivot-based.

## ۲. منطقِ استخراج‌شده

- **بدونِ تفاوت** با موردِ ۵۳؛ برای منطقِ کامل به `1vs-channel-trend.mq4.md` رجوع شود.
- خلاصه: خطِ روندِ کم‌شیب که همهٔ کندل‌های میانی را دربر می‌گیرد + کانالِ موازی؛ ابزارِ
  ترسیمی بدونِ سیگنالِ خودکار؛ بدونِ look-ahead.

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

- همان دو ایدهٔ موردِ ۵۳: trendline-breakout و channel-touch (mean-reversion). چیزِ تازه‌ای نیست.

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** 🔸 مانندِ موردِ ۵۳ (تکراری).
- **فیلترِ تأیید:** 🔸 «فاصله تا خطِ روند» (تکراری).

## ۵. نتیجهٔ بک‌تست

**N/A** — نسخهٔ تکراری. Δ سودِ خالص = ۰. برای آزمون به موردِ ۵۳ رجوع شود.

_وضعیت: ✅ بررسی‌شده (duplicate موردِ ۵۳؛ md5 یکسان). موردِ بعدی بررسی می‌شود._
