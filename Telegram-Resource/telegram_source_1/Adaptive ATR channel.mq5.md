# بررسیِ منبع: `Adaptive ATR channel.mq5`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر.

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | **اندیکاتورِ MT5 (سورسِ باز `.mq5`)** — بالاترین ارزشِ تحقیقاتی (منطق کاملاً قابلِ استخراج). |
| **سازنده** | mladen, 2018 (`mladenfx@gmail.com`) — برنامه‌نویسِ شناخته‌شدهٔ اندیکاتورهای تطبیقی. |
| **پلات‌ها** | ۳ خط: `Adaptive EMA` (میانه) + `Channel up` + `Channel down`. |
| **ورودی‌ها** | `inpPeriod=20`، `inpPrice=PRICE_TYPICAL`، `inpMultiplier=1` (پهنای کانال). |

## ۲. منطقِ استخراج‌شده (کاملاً از سورس)

این یک **کانالِ ATR حولِ یک EMAِ تطبیقی از نوعِ KAMA (Kaufman)** است. سازوکارِ ریاضی:

1. **Efficiency Ratio (ER):** برای هر کندل:
   - `signal = |price[i] − price[i−period]|` (حرکتِ خالصِ جهت‌دار در بازهٔ period).
   - `noise  = Σ |price[k] − price[k−1]|` روی همان بازه (مجموعِ نوسانِ گام‌به‌گام).
   - `ER = signal / noise` (عددِ ۰ تا ۱؛ ۱ = روندِ خالص، ۰ = رنجِ پرنویز).
2. **دورهٔ هموارسازیِ تطبیقی:**
   - `fastEnd = period/2`، `slowEnd = period*5`.
   - `averagePeriod = ER·(slowEnd − fastEnd) + fastEnd`.
   - یعنی وقتی بازار رونددار است (ER بالا) هموارسازی **سریع** می‌شود و وقتی رنج است (ER پایین) **کند** — دقیقاً منطقِ KAMA.
3. **خطِ میانی و کانال (با ضریبِ هموارسازیِ تطبیقی `α = 2/(1+averagePeriod)`):**
   - `val[i]  = val[i−1] + α·(price − val[i−1])`  → EMA تطبیقی.
   - `atr[i]  = atr[i−1] + α·((high−low) − atr[i−1])` → ATR تطبیقی (با همان α).
   - `chanUp = val + multiplier·atr` ، `chanDn = val − multiplier·atr`.

**نکتهٔ کلیدی:** هم خطِ میانی و هم پهنای کانال با **همان دورهٔ تطبیقی** نفس می‌کشند — در روند کانال باریک‌تر/چابک‌تر و در رنج پهن‌تر/آرام‌تر می‌شود. این یک ابزارِ ریاضیِ تمیز و بدونِ repaint است (فقط از دادهٔ گذشته).

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

دو ترجمهٔ بک‌تست‌پذیرِ مستقل روی XAUUSD/EURUSD:

> **الف) Breakout (پیروِ روند):** ورودِ Long وقتی close از `chanUp` عبور کند، Short وقتی از `chanDn` — با فیلترِ `ER > آستانه` (مثلاً 0.3) تا فقط در روندهای واقعی وارد شویم. خروج: بازگشت به خطِ میانی `val`.
>
> **ب) Mean-reversion (در رنج):** وقتی `ER < 0.2` (بازارِ رنج)، لمسِ `chanUp`/`chanDn` سیگنالِ بازگشت به `val` می‌دهد.

**ارزشِ ویژه:** خودِ **Efficiency Ratio** به‌تنهایی یک **فیلترِ رژیمِ بازار (trend/range)** بسیار باارزش است — می‌تواند مستقل از کانال، به‌عنوان سوییچِ انتخابِ لایه استفاده شود.

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ✅ **بالقوه** — منطقِ KAMA/Efficiency-Ratio ممکن است در پرتفوی نباشد؛ ارزشِ آزمونِ مستقل دارد.
- **فیلترِ تأیید (مهم):** ✅✅ — **Efficiency Ratio به‌عنوان فیلترِ رژیم** می‌تواند روی لایه‌های سوختهٔ فعلی سوار شود: لایه‌های روندی فقط وقتی `ER>آستانه` فعال، لایه‌های reversal فقط وقتی `ER<آستانه`. این دقیقاً همان «فیلترِ صحتِ سیگنال» است که می‌تواند WR لایه‌های زیرِ ۴۰٪ را بالا ببرد.

## ۵. نتیجهٔ بک‌تست (نشستِ S346 — پورت و آزمونِ کامل انجام شد)

پورتِ verbatim در `strategies/s346_adaptive_channel.py` (شاملِ نکتهٔ ظریفِ mladen:
α تطبیقی **هم** روی خطِ میانه **و هم** روی خودِ ATR).

### ۵.۱ لبهٔ خامِ ابزار — تقریباً تصادفی، ولی فوق‌العاده پرتکرار

| کارت | حالت | رویداد | معاملهٔ پس‌از‌صف | WR | PF |
|---|---|---|---|---|---|
| XAU-M15 | breakout/both p=34 m=1.272 h=5 | **۶۹٬۰۲۱** | **۳۰٬۰۱۰** | ۴۷.۳۵٪ | ۰.۸۰۶ |
| XAU-M15 | breakout/both p=21 m=1.272 h=5 | ۶۵٬۷۴۷ | ۲۹٬۷۱۳ | ۴۷.۳۲٪ | ۰.۷۹۶ |
| XAU-M15 | fade/long p=21 m=1.272 h=34 | ۶٬۹۱۴ | ۲٬۷۸۲ | ۵۱.۴٪ | ۰.۹۰ |
| XAU-H4 | بهترین خامِ اسکن | — | ۶۰ | ۵۵.۰٪ | ۱.۴۵۹ (RQS=۲۵.۱ REJECT) |

**نتیجه‌گیریِ علمی:** ابزار به‌تنهایی **لبه ندارد** (PF<۱ در همهٔ تنظیماتِ پرتکرار)،
اما دقیقاً همین «بی‌طرفیِ آماری + N نجومی» آن را به **بهترین بسترِ ممکن برای
انباشتِ فیلتر** تبدیل می‌کند: ۳۰٬۰۰۰ معامله بودجه‌ای است که اجازه می‌دهد فیلترها
n را ۱۰–۲۰ برابر آب کنند و هنوز n نهاییِ چهار‌رقمی بماند.

### ۵.۲ سه سطحِ آزمون‌شده

| سطح | تلاش | سقفِ WR رسیده | حکم |
|---|---|---|---|
| هندسهٔ خام | ۱۰٬۰۰۰ ترکیب (p×mult×ER×SL×RR×hold×tp_mode) | صفر ترکیبِ تکرارپذیر با WR≥۵۸ | ❌ کافی نیست |
| فیلترِ تک‌بعدی | ۷۲ ابزارِ بانک × ۵ چارک | ≈ ۵۳٪ | ❌ کافی نیست |
| انباشتِ چندفیلتری | ۱۵۲ ویژگی، انباشتِ حریصانه | ۶۴.۲٪ با PF=۱.۷۸ | ✅ کار می‌کند |

### ۵.۳ ER به‌عنوان فیلترِ رژیم — تأییدِ تجربی

`CHAN:er` و خانوادهٔ `er_lucas_*` به‌طور مکرر در فهرستِ کاندیداهای غربال ظاهر شدند،
و ویژگیِ **منحصربه‌فردِ این منبع** — `CHAN:atr_slope` (سرعتِ تغییرِ پهنای کانالِ
تطبیقی) — قوی‌ترین فیلترِ تک‌بعدیِ کشف‌شده بود:

```
+F4 CHAN:atr_slope ≤ −0.0312 | D n=109 WR=62.39 PF=1.48 | H n=39 WR=69.23 PF=2.28
```
یعنی «کانالِ در حالِ باریک‌شدن» شرطِ لازمِ موفقیتِ بازگشت به میانه است — این
گزاره از **خودِ ریاضیاتِ mladen** بیرون می‌آید و در هیچ اندیکاتورِ استانداردی نیست.

### ۵.۴ درسِ روش‌شناختیِ حاصل از این منبع (ارزشِ فرا-تحقیقاتی)

آزمونِ این ابزار یک **باگِ روش‌شناختی در خودِ فرایندِ تحقیق** را آشکار کرد:
اکتشاف با `allow_overlap=True` انجام می‌شد ولی داوریِ RQS+ با `allow_overlap=False`
⇒ تابعِ هدفِ اشتباه بهینه می‌شد (شکافِ WR ۶۳.۱٪ → ۵۸.۲٪). رفعِ آن
(`select_non_overlap` + تستِ برابریِ صف با ۰ اختلاف) از این پس به **همهٔ** لایه‌های
آیندهٔ پروژه سود می‌رساند. ⇒ ارزشِ این منبع فراتر از خودِ ابزار بود.

_وضعیت: ✅ بررسی‌شده و **آزمون‌شده**. سورسِ باز، منطقِ ریاضیِ کاملاً استخراج‌شده
(KAMA + کانالِ ATR تطبیقی + Efficiency Ratio). لبهٔ خام: ندارد. ارزشِ واقعی:
(الف) بسترِ پرتکرارِ بی‌طرف برای انباشتِ فیلتر، (ب) ویژگیِ نوِ `atr_slope`،
(ج) ER به‌عنوان سوییچِ رژیم. حکمِ نهایی در انتهای این فایل ثبت می‌شود._
