# بررسیِ منبع: `_ICT Order blocks( with adr)(3).ex4`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر، مارجین `40$/لات`.

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | اندیکاتورِ MT4 کامپایل‌شده (`.ex4`) — **نسخهٔ کپیِ سومِ** ICT Order Blocks. |
| **حجم** | ~۱۳۶KB (**بایت‌به‌بایت مشابه**، حجم یکسان ۱۳۵۷۴۸). |

## ۲. تشخیص: نسخهٔ تکراری

این فایل از نظرِ حجم و ماهیت **کاملاً یکسان** با `_ICT Order blocks( with adr).ex4` است
(پسوندِ `(3)` = کپیِ دانلود). منطق و کاربرد دقیقاً همان است.

## ۳. منطق و تصمیم (ارجاع)

- سبکِ **ICT / Smart Money**: تشخیصِ Order Block (پس از BOS) + فیلترِ ADR.
- منطقِ کامل و ایده‌های قابلِ آزمون (لایهٔ OB-retest + فیلترِ ADR) در فایلِ
  **`_ICT Order blocks( with adr).ex4.md`** مستند شده است.

## ۴. ارتباط با پرتفویِ فعلی

- **همپوشانی:** ۱۰۰٪ با نسخهٔ اصلی. کاندیدِ لایهٔ OB-retest و فیلترِ ADR (به md اصلی مراجعه شود).

## ۵. نتیجهٔ بک‌تست

**N/A** — نسخهٔ تکراری؛ فقط فایلِ md بررسی ساخته شد. Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده (کپیِ تکراریِ ICT Order Blocks). موردِ بعدی بررسی می‌شود._
