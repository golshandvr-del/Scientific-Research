# بررسیِ منبع: `Painel_EMA_Volume_20_v4_2_MT4 @free_fx_pro/`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر، مارجین `40$/لات`.

**پنلِ (داشبوردِ) EMA + Volume — بی‌سورس؛ همراه با فایل‌های تبلیغاتیِ url.**

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | اندیکاتورِ کامپایل‌شدهٔ MT4 (`.ex4`) — سورس بسته + فایل‌های `.url`/`.txt` تبلیغاتی (رفرال بروکر). |
| **نام** | Painel EMA Volume 20 v4.2 (پرتغالی «Painel» = پنل/داشبورد). |
| **رشته‌های استخراج‌شده** | ❌ (ex4 محافظت‌شده)؛ فایل‌های جانبی صرفاً لینکِ رفرال‌اند. |
| **تایم‌فریم/دارایی هدف** | نامشخص؛ عمومیِ فارکس. |

## ۲. منطقِ استخراج‌شده (استنتاج از نام)

- پنلی که وضعیتِ دو مؤلفه را نمایش می‌دهد:
  - **EMA(20)** به‌عنوانِ فیلترِ جهتِ روند (قیمت بالای/زیرِ EMA20).
  - **Volume** (در MT = tick-volume) به‌عنوانِ تأییدِ فعالیت/مومنتوم.
- خروجی احتمالاً یک داشبوردِ رنگی «Buy/Sell/Neutral» بر پایهٔ ترکیبِ این دو است.

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

- منطقِ ساده و کاملاً بک‌تست‌پذیر (مستقل از فایل): **«جهت با EMA20 + تأییدِ اسپایکِ tick-volume»**.
  - قاعدهٔ کاندید: ورود در جهتِ EMA20 فقط وقتی tick-volumeِ کندل > `k×` میانگینِ N کندل.
  - پارامترها: `EMA∈{20}`, `volMult∈{1.3,1.5,2.0}`, `avgLen∈{20,50}`.
- ⚠️ یادآوری: در فارکس/طلا حجم = tick-volume (پراکسیِ فعالیت)، نه حجمِ نهادی.

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ EMA20 + volume مفاهیمِ پایه‌اند؛ لبهٔ نو بعید است.
- **فیلترِ تأیید:** ✅ **فیلترِ «اسپایکِ tick-volume در جهتِ EMA20»** کاندیدِ خوبی برای تأییدِ
  ورودِ لایه‌های روند/بریک‌اوت و بالابردنِ WR است. ثبت‌شده برای آزمونِ آینده.
- **جمع‌بندی:** فایل بی‌سورس اما مفهومِ ترکیبیِ آن (روند+حجم) کاندیدِ فیلترِ قابلِ آزمون است.

## ۵. نتیجهٔ بک‌تست

**N/A** — طبق دستورِ نشستِ جاری فقط فایلِ md بررسی ساخته می‌شود؛ بک‌تست نشد. Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده (پنلِ EMA20+Volume بی‌سورس؛ ایدهٔ فیلترِ حجم ثبت شد). موردِ بعدی بررسی می‌شود._
