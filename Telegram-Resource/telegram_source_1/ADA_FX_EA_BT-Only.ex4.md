# بررسیِ منبع: `ADA_FX_EA_BT-Only.ex4`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر.

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | EAِ MT4 کامپایل‌شده (`.ex4`) — سورس بسته. |
| **نام** | `ADA_FX_EA_BT-Only` — پسوندِ `BT-Only` یعنی **فقط برای بک‌تست** (احتمالاً نسخهٔ دمو که در حسابِ زنده معامله نمی‌کند). |
| **حجم** | ~۲۰KB. |
| **رشته‌های قابلِ استخراج** | ❌ هیچ متنِ معنادار — obfuscated. |

## ۲. منطقِ استخراج‌شده

❌ **قابلِ استخراج نیست.** باینریِ بسته؛ هیچ نامِ پارامتر، اندیکاتورِ پایه یا قانونِ
ورود/خروج خوانده نشد. تنها سرنخ، پسوندِ `BT-Only` است که ماهیتِ محدودشدهٔ EA را نشان
می‌دهد (نسخهٔ نمایشی).

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

❌ **هیچ.** بدونِ منطقِ قابلِ استخراج، فرضیهٔ بک‌تست‌پذیری ساخته نمی‌شود.

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ — **فیلترِ تأیید:** ❌. جعبهٔ سیاه.

## ۵. نتیجهٔ بک‌تست

**N/A** — طبق User Note فقط md بررسی ساخته شد. Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده و کنار گذاشته شد (EAِ کامپایل‌شدهٔ obfuscated، نسخهٔ BT-Only بدونِ اطلاعاتِ قابلِ استخراج). موردِ بعدی بررسی می‌شود._
