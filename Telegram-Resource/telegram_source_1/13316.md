# بررسیِ منبع: پوشهٔ `13316/` (`ClockAnalog.mq5`)

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.

بیست‌ودومین موردِ بررسی‌شده (ترتیبِ الفباییِ `LC_ALL=C`).

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | اندیکاتورِ MT5 با سورسِ باز — اما **ابزارِ تزئینی**. |
| **نام/سازنده** | ClockAnalog — **Copyright 2015, MetaQuotes Software Corp.** (نمونهٔ رسمیِ SDK). |
| **محتوا** | `clockanalog.mq5` + کلاس‌های کمکی `CStockClock`, `CGraphBase`, `CGButton`, `CGLabel`… |
| **ورودی‌ها** | موقعیت (TopLeft/TopHigh)، طرحِ رنگ، رنگِ عقربه‌ها. |

## ۲. منطقِ استخراج‌شده

- این فایل صرفاً یک **ساعتِ آنالوگِ گرافیکی** روی چارت رسم می‌کند (نمایشِ زمانِ سرور/محلی
  با عقربه). یک نمونهٔ آموزشیِ **رابطِ گرافیکی (Canvas/GUI)** از خودِ MetaQuotes است.
- **هیچ منطقِ معاملاتی، سیگنال، فیلتر یا اندیکاتورِ قیمتی ندارد.**

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

- ❌ هیچ. تنها ارتباطِ دور: «زمانِ روز» که در سیستم‌های session/killzone به‌کار می‌رود، اما
  این فایل چنین منطقی ندارد و صرفاً نمایشی است.

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ هیچ.
- **فیلترِ تأیید:** ❌ هیچ.

## ۵. نتیجهٔ بک‌تست

**N/A** — ابزارِ تزئینی؛ فاقدِ منطقِ معاملاتی. Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده (ساعتِ آنالوگِ تزئینیِ MetaQuotes؛ بی‌ربط به تحقیق). موردِ بعدی بررسی می‌شود._
