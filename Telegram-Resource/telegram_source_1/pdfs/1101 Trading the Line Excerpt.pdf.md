# بررسیِ منبع (کتاب): `1101 Trading the Line Excerpt.pdf`

> قانونِ شمارهٔ ۱ پروژه: هدف فقط **سودِ خالصِ بیشتر** (XAUUSD + EURUSD) است؛ WR تابعِ
> هدف نیست و فقط کفِ ۴۰٪ برای هر لایه اجباری است.

---

## ۱. چیستی

- **نوع:** eBook آموزشی (بخشِ نخستِ یک کتابِ ۴۳-صفحه‌ای — این فایل **excerpt** است).
- **عنوان:** _Trading the Line — 5 Ways You Can Use Trendlines to Improve Your
  Trading Decisions_
- **نویسنده:** **Jeffrey Kennedy** — تحلیلگرِ ارشدِ کالای Elliott Wave International.
- **حجمِ این فایل:** ۱۷ صفحه = **فصلِ ۱ (Defining Trendlines)** + **فصلِ ۲
  (Drawing Trendlines)** — کاملاً استخراج شد.
- **دارایی/تایم‌فریمِ هدفِ سازنده:** آگنوستیک. مثال‌های کتاب: Google (روزانه/هفتگی)،
  قهوه، پنبه (هفتگی)، اوراقِ قرضه (هفتگی)، و **E-Mini Dow روی ۶۰-دقیقه** (شکلِ ۲-۱۷)
  ⇒ نویسنده صراحتاً قاعده را روی **تایم‌فریمِ داخل‌روزی هم** اعمال می‌کند.

---

## ۲. منطقِ استخراج‌شده

### ۲-۱. تعریفِ خطِ روند (فصلِ ۱)
- خط = اتصالِ **دو نقطه** (سقف یا کف). «اهمیتِ خطِ روند **متناسبِ مستقیم** با
  اهمیتِ نقطهٔ ۱ و نقطهٔ ۲ است.»
- افقی ⇒ حمایت/مقاومت. عمودی ⇒ لحظاتِ زمانی (تحلیلِ چرخه). مورب ⇒ **هم قیمت هم زمان**.
- شیبِ خط، شدتِ خوش‌بینی/بدبینیِ بازار را نشان می‌دهد.

### ۲-۲. تکنیکِ **موازیِ Kennedy** (فصلِ ۲ — هستهٔ عملیاتیِ کتاب)
این تکنیک سه‌مرحله‌ای و **کاملاً هندسی** است:

```
۱) خطی از یک کفِ مهم به یک سقفِ مهم بکش (یا برعکس)     ← «خطِ پایه»
۲) موازیِ آن را بردار و روی *افراطی‌ترین نقطهٔ درونِ همان حرکت* بگذار  ← «خطِ ثانویه»
۳) نفوذِ قیمت به خطِ ثانویه ⇒ «حرکتِ قبلی تمام شد؛ حرکتِ جدید آغاز می‌شود»
```

نقلِ صریحِ متن (شکلِ ۲-۱۴): _«خطی از کفِ مهم به سقفِ مهم کشیدم، سپس موازیِ آن را از
پایین‌ترین حرکتِ درونِ توالیِ قیمت گرفتم. **آن خط، پایانِ یک روند و آغازِ روندِ جدید را
مشخص می‌کند.**»_

نکتهٔ مهمِ روش‌شناختی (شکلِ ۲-۱۳): نویسنده اعتراف می‌کند خطِ پایه **به‌تنهایی اطلاعاتِ
چندانی نمی‌دهد**؛ ارزش در **خطِ موازیِ ثانویه** است. ⇒ آزمونِ ما باید **خطِ ثانویه** را
بیازماید، نه خطِ پایه.

### ۲-۳. ⭐ **تعریفِ شکست — لبهٔ اصلیِ این کتاب** (صفحات ۱۳–۱۵)
این صریح‌ترین و عددی‌ترین ادعای کتاب است و **با تعریفِ رایج تفاوت دارد**:

> _«بعضی فکر می‌کنند **بسته‌شدن** بالای یا زیرِ خطِ روند نشانهٔ شکست است. من ترجیح
> می‌دهم حرکتِ قیمت را بر مبنای **high یا low** ببینم... **تا وقتی high کندل زیرِ خطِ
> روند نباشد، شکستِ واقعیِ خطِ روندِ قبلی رخ نداده است.»_ (شکلِ ۲-۱۹)

و در شکلِ ۲-۲۰ با مثال روشن‌ترش می‌کند: در آن نمودار **زیرِ خط بسته شد** ولی
**دو-سه کندل بعد** بود که `high` زیرِ خط تشکیل شد ⇒ فقط آن نقطه شکستِ مشروع است.

**فرمولِ دقیق:**

| جهت | تعریفِ رایج (**رد**شده توسطِ Kennedy) | تعریفِ Kennedy |
|---|---|---|
| شکستِ نزولی | `close < line` | **`high < line`** |
| شکستِ صعودی | `close > line` | **`low > line`** |

⇒ شرطِ Kennedy **اکیداً سخت‌تر** است: `high < line ⟹ close < line` (چون `close ≤ high`)
ولی عکسش برقرار نیست. یعنی مجموعهٔ سیگنال‌های Kennedy **زیرمجموعهٔ محضِ** سیگنال‌های
`close` است.

### ۲-۴. نکاتِ فرعیِ قابلِ استخراج
- **خطِ میانی (midline):** شکلِ ۲-۴ — نقطهٔ میانیِ کانالِ موازی در **چهار ناحیهٔ
  متفاوت** مقاومت داد ⇒ نویسنده همیشه خطِ میانی را هم رسم می‌کند.
- **دوگانیِ رسم:** شکل‌های ۲-۱۱/۲-۱۲ — همان حمایت را می‌توان با «کف-به-کف» یا با
  «سقف-به-سقف + موازی از کفِ میانی» یافت ⇒ دو مسیرِ هندسیِ متفاوت به یک سطح.
- **پایداریِ زمانی:** خطی که ماه‌ها/سال‌ها پیش رسم شده، بارها مؤثر بوده (شکلِ ۲-۶/۲-۷).

---

## ۳. ایده‌های قابلِ آزمون برای پروژهٔ ما

### ⭐ ایدهٔ ۱ (اولویتِ اول) — «دروازهٔ `high/low` به‌جای `close`»
این **بهترین چیزِ این کتاب برای پروژهٔ ما** است، به دلیلی راهبردی:

پروژه در چرخهٔ `S370…S373` ثابت کرد گلوگاهش **«لبهٔ ناخالص» (`e_pip`)** است، نه نمونه
(⇒ `docs/TRIAGE_POWER_ALL_LAYERS.md`). قانونِ لبهٔ پیپی: `E[R] = (e_pip − c)/SL`.
یک فیلترِ **اکیداً سخت‌تر** نمونه را کم می‌کند ولی می‌تواند `e_pip` را **بالا ببرد** —
یعنی مستقیماً به گلوگاهِ واقعی حمله می‌کند، نه به گلوگاهِ خیالی.

**قاعدهٔ بک‌تست‌پذیر:** هر لایهٔ شکست-محورِ موجود را برداشته، شرطِ ماشه را از
`close` به `high/low` تغییر بده و **دو نسخه را روی همان بارها** مقایسه کن:

```
close-break :  short اگر close < line     |  long اگر close > line
KENNEDY     :  short اگر high  < line     |  long اگر low   > line
```

معیارِ سنجش **باید `e_pip` باشد، نه `z`** — چون `z` با کم‌شدنِ نمونه پایین می‌آید
حتی وقتی لبه بهتر شده. این تمایز مستقیماً از قانونِ لبهٔ پیپی می‌آید.

### ایدهٔ ۲ — «کانالِ موازیِ Kennedy» (پایه: کف↔سقف، ثانویه: موازی از افراطی‌ترین نقطه)
این با کانالِ `S366` (که از پیوت‌های هم‌نوع ساخته می‌شد: سقف-سقف-سقف) **متفاوت** است:
خطِ پایهٔ Kennedy **کف را به سقف** وصل می‌کند (خطِ *ضدِ* روند)، سپس موازی می‌شود.
⇒ هندسهٔ کاملاً متفاوت ⇒ کاندیدِ **لبهٔ نو**.

### ایدهٔ ۳ — «خطِ میانیِ کانال» به‌عنوان فیلتر
شکلِ ۲-۴ ادعا می‌کند midline در چهار ناحیه مقاومت داد. قابلِ آزمون به‌عنوان
**فیلترِ ردِ سیگنال**: ورودهایی که هدفشان آن‌سویِ midline است رد شوند.

---

## ۴. ارتباط با پرتفویِ فعلی

- **فیلترِ تأییدِ سیگنال (ارزشمندترین):** ایدهٔ ۱ یک **دروازهٔ عمومی** است که روی
  **هر** لایهٔ شکست-محور قابلِ اعمال است — از جمله لایه‌های سوختهٔ `S364…S373`. طبقِ
  قانونِ جعبه‌ابزار و قانونِ بهبود، این دقیقاً یک «بهبود» است.
- **لبهٔ نو (بالقوه):** ایدهٔ ۲ هندسهٔ کانالیِ نیازموده است.
- **هم‌راستاییِ راهبردی:** برخلافِ همهٔ تلاش‌های `S370` (که نمونه را زیاد می‌کرد)،
  این ایده نمونه را **کم** و لبه را **زیاد** می‌کند ⇒ جهتی که تریاژِ سراسری تجویز کرد.
- **هشدارِ پیشینی:** چون شرط اکیداً سخت‌تر است، نمونه قطعاً کم می‌شود. طبقِ قانونِ
  `n_needed` باید **پیش از** رد کردن، عددِ لازم گزارش شود.

---

## ۵. نتیجهٔ بک‌تست روی شبیه‌سازِ رویدادمحور

### ۵-۱. ایدهٔ ۱ (دروازهٔ `high/low`) ⇒ لایهٔ **S374** ⇒ ✅ **ACCEPTED روی `H4`**

پیش‌ثبت: [`results/S374_PREREG_kennedy_break_gate.md`](../../../results/S374_PREREG_kennedy_break_gate.md)
حکمِ نهایی: [`results/S374_KennedyBreakGate_XauEur_H4_rqs2-ACCEPTED.md`](../../../results/S374_KennedyBreakGate_XauEur_H4_rqs2-ACCEPTED.md)

**طرحِ آزمون:** مقایسهٔ جفتی روی هارنسِ `S373` با **تنها یک تفاوت**: عبارتِ بولیِ ماشه.
افقِ نگهداری، براکت‌ها، خانوادهٔ پارامتر، مدلِ پوچ و منطقِ جهت همه **قفل** بودند.

| کارت | نگه‌داشت | Δ`e_pip` طلا | Δ`e_pip` یورو | حکم |
|---|---|---|---|---|
| `M5`  | ۵۹.۷٪ | — (زیرِ هزینه) | — | ❌ `DEAD_BELOW_COST` |
| `M15` | ۶۰.۱٪ | ‎−۰.۴۲‎ | ‎+۰.۱۱‎ | ❌ `DEAD_BELOW_COST` |
| `M30` | ۵۹.۲٪ | ‎**+۰.۸۹**‎ | ‎−۰.۰۲‎ | ❌ `DEAD_BELOW_COST` |
| `H1`  | ۵۹.۱٪ | ‎+‎ | ‎+‎ | ❌ `DEAD_BELOW_COST` |
| **`H4`** | ۶۴.۵٪ | **۱۱.۶۷ → ۳۲.۹۷** | **۱.۵۷ → ۶.۴۴** | ✅ **`ACCEPT`** |

**کارتِ پذیرفته‌شده (`H4`, `n_perm=400`):**

| شرطِ RQS2 | مقدار | سدّ | نتیجه |
|---|---|---|---|
| شانس | `z = +4.100` | `2.570` (N=112) | ✅ |
| نمونه | `n = 1,062` | `n_needed = 417` | ✅ |
| اقتصاد | طلا `+32.97` / `c=3.3` · یورو `+6.44` / `c=1.6` | هر دو مستقل | ✅ |
| تکرارپذیری | `h1 = +0.0933` · `h2 = +0.1696` | هر دو مثبت | ✅ |

⭐ **بندِ تکرارپذیری همان بندی است که `S373` را کشته بود** (`h1=−0.0134`, `h2=+0.0537`
— تغییرِ علامت). دروازهٔ Kennedy آن را **حل کرد**، نه اینکه فقط میانگین را بالا ببرد.

**بازرسی‌های خصمانه (هر سه انجام شد چون نتیجه خلافِ پیش‌بینیِ خودم بود):**
1. **نشتِ آینده ⇒ منتفی.** موتور در `open` کندلِ *بعدی* پر می‌کند
   (`entry_bar = si + 1`)، پس `high`/`low` کندلِ سیگنال **تاریخی** است نه آینده.
2. **سوگیریِ انتخابِ عضو ⇒ وجود دارد ولی محافظه‌کارانه.** در `H4` چهار عضوِ طلا زیرِ
   کفِ معامله افتادند و حذف شدند؛ آن‌ها در بازوی پایه **قوی‌ترین** بودند
   (`+0.1826` در برابر `+0.0219`) ⇒ حذفشان به **زیانِ** Kennedy است.
   دلتای هم‌سنگ `+0.1399` است، **بزرگ‌تر** از دلتای خام `+0.1131`.
3. **آزمونِ علامت (مصون از هر وزن‌دهی) ⇒ `17/20` عضوِ مشترک بهبود یافتند،
   `p_exact = 0.00129`.**

**روندِ یکنواختِ کامل روی تایم‌فریم** (`ρ_Spearman = +1.000`، شانسِ تصادفی `1/120`):
```
M5: −0.0208 → M15: −0.0082 → M30: +0.0085 → H1: +0.0688 → H4: +0.1399
```
⇒ **قانونِ قابلِ‌استفادهٔ مجدد:** علامتِ اثرِ دروازهٔ Kennedy **تابعِ تایم‌فریم** است.
هرچه تایم‌فریم بالاتر، نسبتِ نویزِ درون‌کندلی به سیگنال کمتر، پس شرطِ «کلِ کندل باید
عبور کند» اطلاعاتِ بیشتری حمل می‌کند. در `M5` دامنهٔ کندل عمدتاً نویزِ ریزساختاری است
و شرط **زیان‌بار** می‌شود. ⇒ همین توضیح می‌دهد چرا نویسنده قاعده را روی نمودارهای
**روزانه/هفتگی** آموزش می‌دهد.

### ۵-۲. ⚠️ تصحیحِ ادعای کتاب (و ادعای خودم): **این «فیلتر» نیست، «تأخیر» است**

سند: [`results/S374_FINDING_not_subset_but_delay.md`](../../../results/S374_FINDING_not_subset_but_delay.md)

بندِ ۲-۳ همین فایل نتیجه گرفت سیگنال‌های Kennedy «زیرمجموعهٔ محضِ» سیگنال‌های `close`
هستند. **اندازه‌گیری این را در سطحِ عملیاتی نقض کرد:**

| سطح | زیرمجموعه؟ |
|---|---|
| بولیِ خام (`h<line` در برابر `c<line`) | ✅ بله (اثباتِ ریاضی برقرار) |
| پس از منطقِ جهت | ✅ بله (۱۴۳ ⇒ ۷۴) |
| **پس از حذفِ تکرار (اولین شکستِ هر کانال)** | ❌ **نه — هیچ اشتراکی!** |

**مکانیزم:** شرطِ `close` **زودتر** در همان کانال شلیک می‌کند. قاعدهٔ «اولین شکستِ هر
کانال» برای بازوی `close` کندلِ زودتر را نگه می‌دارد و برای بازوی Kennedy کندلِ دیرتر
را. هر دو در بازوی خودشان «اولین»‌اند ولی **کندل‌های متفاوتی** هستند (تأخیرِ اندازه‌گیری‌شده:
۱ تا ۷ کندل).

⇒ **بازتعریفِ ماهیتِ لایه:** این یک **قاعدهٔ زمان‌بندیِ ورود** است، نه فیلترِ ردِ سیگنال.
فیلتر همان الگوها را کمتر معامله می‌کند؛ این **همان کانال‌ها** را در کندلِ دیرتر معامله
می‌کند. پس منبعِ لبه نمی‌تواند «حذفِ الگوهای بد» باشد — باید **قیمتِ ورودِ متفاوت** باشد.

⇒ **ارزشِ این تصحیح برای پروژه بیشتر است، نه کمتر:** فیلتر فقط می‌تواند نمونه را کم
کند، ولی قاعدهٔ زمان‌بندی می‌تواند لبه را **بدون از دست دادنِ هیچ الگویی** بالا ببرد.
این یک **محورِ بهبودِ کاملاً نیازموده** برای پروژه باز می‌کند: **«چه وقت» وارد شویم،
نه «آیا» وارد شویم.**

⇒ **همپوشانی در دو فضا گزارش می‌شود** (چون یک عدد برای این لایه بی‌معناست):
فضای کانال ≈ **تقریباً کامل** · فضای کندل ≈ **تقریباً صفر**.

### ۵-۳. ضعفِ واقعیِ لایه (صادقانه، بدونِ پنهان‌کاری)

اعضای خانواده **مستقل نیستند** — همان کانال‌ها را با براکت‌های مختلف دوباره معامله
می‌کنند. واحدِ استقلالِ درست **کانال** است نه معامله. روی طلای `H4` بازوی پذیرفته‌شده
روی **۴۳ کندلِ مجزا در ۱۰.۸ سال** استوار است ⇒ **~۴ رویدادِ مستقل در سال**.

**بوت‌استرپِ خوشه‌ای (بازنمونه‌گیری از کانال‌ها، نه معاملات):**

| بازو | خوشه | میانگینِ R | CI ۹۵٪ | `P(≤0)` |
|---|---|---|---|---|
| `close` (پایه) | ۴۳۱ | `+0.0176` | `[−0.0794, +0.1133]` | **۳۶.۲٪** ← شاملِ صفر |
| **Kennedy** | ۲۶۸ | `+0.1291` | `[+0.0014, +0.2544]` | **۲.۵٪** ← صفر را رد می‌کند |

⇒ بازوی پایه با واحدِ استقلالِ درست **کاملاً بی‌معنا** است (معناداریِ گزارش‌شده‌اش
محصولِ شمردنِ معامله به‌جای کانال بود). بازوی Kennedy از صفر عبور می‌کند ولی
**به‌سختی** (کرانِ پایین `+0.0014` تقریباً روی صفر). ⇒ **پذیرشِ رتبهٔ محافظه‌کارانه**
با اجبارِ خویشتن‌داری در حجم و برچسبِ **کم‌بسامد**.

### ۵-۴. ایده‌های ۲ و ۳ — وضعیت

- **ایدهٔ ۲ (کانالِ موازیِ Kennedy):** ⏳ **نیازموده.** هندسه‌اش (کف↔سقف + موازی از
  افراطی‌ترین نقطه) با کانالِ هم‌نوعِ `S366` متفاوت است ⇒ کاندیدِ لبهٔ نو برای نشستِ بعد.
- **ایدهٔ ۳ (خطِ میانیِ کانال به‌عنوان فیلترِ رد):** ⏳ **نیازموده.**
- **قانونِ قفلِ تعریف رعایت شد:** فقط دو حالتِ `close` و `high/low` آزموده شد؛ هیچ
  نسخهٔ میانی (نوارِ تلورانس، چند-کندلی، ترکیبی) امتحان **نشد**.

---

## ۶. جمع‌بندیِ ارزشِ این منبع برای پروژه

| خروجی | نوع | وضعیت |
|---|---|---|
| دروازهٔ شکستِ `high/low` روی `H4` | **لایهٔ پذیرفته‌شده** (`S374`) | ✅ استقرار |
| «علامتِ اثر تابعِ تایم‌فریم است» | **قانونِ عمومی** | ✅ ثبت‌شده |
| «حذفِ تکرار، فیلتر را به تأخیر تبدیل می‌کند» | **قانونِ روش‌شناختی** | ✅ ثبت‌شده |
| محورِ بهبودِ «زمان‌بندیِ ورود» | **ابزارِ احیای لایه‌های سوخته** | ✅ باز شد |
| کانالِ موازی + خطِ میانی | ایدهٔ نیازموده | ⏳ نشستِ بعد |

**این منبع، با وجودِ اینکه فقط ۱۷ صفحه و صرفاً دو فصلِ نخستِ کتاب است، تنها منبعی
است که تا این لحظه یک لایهٔ پذیرفته‌شده به پروژه اضافه کرد.**

---

_وضعیت: ✅ **کلِ این فایل (فصلِ ۱ + فصلِ ۲) استخراج و آزموده شد** — این excerpt فقط
دو فصلِ نخستِ کتابِ ۴۳-صفحه‌ای است و فصل‌های ۳ تا ۵ («۵ راه») در فایلِ موجود
**نیستند**؛ اگر نسخهٔ کاملِ کتاب به پروژه اضافه شود، سه فصلِ باقی‌مانده جای تحقیقِ
تازه دارند._

## 🏁 حکمِ نهاییِ منبع: **ACCEPTED**
