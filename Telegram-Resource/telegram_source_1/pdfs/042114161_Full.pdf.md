# بررسیِ منبع (کتاب/پایان‌نامه): `042114161_Full.pdf`

> قانونِ شمارهٔ ۱ پروژه: هدف فقط **سودِ خالصِ بیشتر** (XAUUSD + EURUSD) است؛ WR تابعِ
> هدف نیست و فقط کفِ ۴۰٪ برای هر لایه اجباری است.

> **این یک کتاب است.** طبق پروتکل، در یک نشست تمام نمی‌شود؛ هر بخش یک مرحلهٔ تحقیق
> است و این فایل به‌مرور کامل می‌شود.

---

## ۱. چیستی

- **نوع:** پایان‌نامهٔ دانشگاهی (Skripsi) — مطالعهٔ تجربیِ اعتبارسنجیِ یک اندیکاتور.
- **عنوان:** _Analisis Keakuratan Penggunaan Indikator Relative Strength Index
  Periode 21 Hari…_ (تحلیلِ دقتِ کاربردِ اندیکاتورِ RSI با دورهٔ ۲۱ روز به‌عنوان
  راهنمای سیگنالِ خرید و فروش).
- **نویسنده:** Benidiktus Yomo Subarkah — Universitas Sanata Dharma (۲۰۰۹).
- **زبان:** اندونزیایی. **حجم:** ۸۲ صفحه.
- **دارایی/تایم‌فریمِ هدفِ سازنده:** سهامِ صنعتِ معدنِ بورسِ اندونزی (BEI)، دادهٔ
  **روزانه (Daily)**، بازهٔ مارس ۲۰۰۶ – فوریه ۲۰۰۸.

---

## ۲. منطقِ استخراج‌شده (این بخش از کتاب: فصلِ روش‌شناسی + نتیجه)

**اندیکاتورِ پایه:** Relative Strength Index (RSI).

- فرمولِ کلاسیکِ Wilder: `RSI = 100 − 100/(1+RS)`، که
  `RS = میانگینِ صعودِ close / میانگینِ نزولِ close`.
- **نوآوریِ ادعاییِ کتاب:** استفاده از **دورهٔ ۲۱** به‌جای ۱۴ استانداردِ Wilder —
  با این استدلال که «سیگنالِ کاذبِ (false signal) کمتری تولید می‌کند» (ص. روش‌شناسی).
- **قانونِ سیگنال (به نقل از Murphy 1999:242):**
  - `RSI > 70` ⇒ **overbought** ⇒ سیگنالِ **فروش (Sell)**.
  - `RSI < 30` ⇒ **oversold** ⇒ سیگنالِ **خرید (Buy)**.
  - این یک قاعدهٔ **mean-reversion** خالص است (خلافِ حرکتِ افراطی).

**نتیجهٔ خودِ کتاب (بسیار مهم):** پژوهش به‌صراحت نتیجه می‌گیرد که
**«RSI دورهٔ ۲۱ روزه برای سهامِ معدنِ اندونزی دقیق نیست» (_tidak akurat_)** —
یعنی سیگنال‌های RSI با حرکتِ واقعیِ قیمت هم‌خوانیِ معناداری نداشتند. این یک شاهدِ
مستقلِ بیرونی است که با تجربهٔ پروژهٔ ما (لبه‌نداشتنِ اندیکاتورهای خام) هم‌راستاست.

---

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

پروژه تاکنون RSI را فقط با **دورهٔ ۱۴** آزموده (`s01_bb_rsi_meanrev`، `s71_forex_
meanreversion` — هر دو RSI-14 + Bollinger). فرضیهٔ **خاص و نیازموده** این کتاب:

> آیا **RSI با دورهٔ ۲۱** (به‌جای ۱۴) به‌عنوان قاعدهٔ mean-reversionِ خالص
> (خرید زیر ۳۰، فروش بالای ۷۰) روی `XAUUSD_M15` / `EURUSD_M15` لبه دارد؟ یا
> دستِ‌کم به‌عنوان **فیلترِ تأیید** برای لایه‌های موجود، سیگنال‌های کاذب را کم می‌کند؟

ترجمه به قانونِ بک‌تست‌پذیر:
- `rsi21 = RSI(close, 21)` (shift-safe، فقط از گذشته).
- **Long** وقتی `rsi21` از زیرِ ۳۰ به بالا برگردد (خروج از oversold).
- **Short** وقتی `rsi21` از بالای ۷۰ به پایین برگردد (خروج از overbought).
- گریدِ آستانه‌ها {30/70, 25/75, 20/80} و SL/TP چندگانه؛ گیتِ سخت‌گیرانهٔ ضدِ overfit.

---

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو (بالقوه):** پارامترِ دورهٔ ۲۱ (نه ۱۴) روی طلا/یورو نیازموده است.
- **فیلترِ تأیید (بالقوه، ارزشمندتر):** اگر لبهٔ مستقل نداشته باشد، ممکن است
  `rsi21` به‌عنوان فیلترِ رژیمِ اشباع، WR لایه‌های سوختهٔ mean-reversion را بالا ببرد.
- **هشدارِ پیشینی:** نتیجهٔ خودِ کتاب «tidak akurat» است ⇒ انتظارِ پایه پایین است؛
  اما «فیلترِ رد سیگنال» حتی از یک اندیکاتورِ ضعیف هم می‌تواند ارزش بیرون بکشد.

---

## ۵. نتیجهٔ بک‌تست — ⛔ رد شد (لبهٔ ناپایدار)

قاعدهٔ **RSI-21 mean-reversion** (cross-back) روی `XAUUSD_M15` و `EURUSD_M15`
آزموده شد (**۱۲۰ واریانت**: ۲ دارایی × ۲ جهت × ۳ آستانه × ۵ SL/TP × ۲ max_hold).

| دارایی | واریانت | net مثبت | بهترین net | حکم |
|---|---:|---:|---|---|
| XAUUSD | ۶۰ | ۲۰ | +$1,337 (long, WR ۵۳٪، PF ۱.۱۸) | ⛔ ناپایدار |
| EURUSD | ۶۰ | ۰ | −$463 (همه PF<۱) | ⛔ شکست |

**آزمونِ walk-forwardِ بهترین لایهٔ طلا:** پنجرهٔ W3 **منفی** (−$354، PF ۰.۷۳) و ۹۶٪
سودِ کل در W4 متمرکز بود ⇒ لبه در طولِ زمان پایدار نیست (artifactِ یک بازه).

- **EURUSD کاملاً شکست خورد** ⇒ تأییدِ مستقلِ نتیجهٔ خودِ کتاب («_tidak akurat_»).
- **Δ سودِ خالص = ۰**؛ رکورد دست‌نخورده = **+$221,895**.
- مستندِ کامل: [`results/S167_RSI21_MeanReversion_NetProfit_1337_REJECTED.md`](../../../results/S167_RSI21_MeanReversion_NetProfit_1337_REJECTED.md)
  · کد: `strategies/s167_rsi21_mean_reversion.py` + `strategies/s167_rsi21_walkforward_validate.py`

**نتیجهٔ ارتباط با پرتفوی:** دورهٔ RSI ۲۱ به‌جای ۱۴ سیگنال را کمی تمیزتر کرد اما لبهٔ
پایدار نساخت (WR>۵۰٪ بدونِ پایداریِ زمانی = WinRate-Trap). شاید فقط به‌عنوان فیلترِ
رژیمِ اشباع در ترکیب با لایه‌های دیگر ارزش داشته باشد؛ به‌تنهایی لبه ندارد.

_وضعیت: ✅ **بخشِ اولِ کتاب** (فصلِ روش‌شناسیِ RSI-21) بررسی و بک‌تست شد (رد). بخش‌های
بعدیِ کتاب در نشست‌های آینده بررسی می‌شوند. رکورد بدون تغییر (**+$221,895**)._
