# بررسیِ منبع: `3X Combo AI @free_fx_pro`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر، مارجین `40$/لات`.

پنجمین موردِ بررسی‌شده در این نشست. **بستهٔ تبلیغاتیِ کانالِ `@free_fx_pro` — فقط `.ex5` کامپایل‌شده + لینک‌های افیلیت.**

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | EAِ کامپایل‌شدهٔ MT5 (`3X Combo AI @free_fx_pro.ex5`) — **سورس بسته**. |
| **محتوایِ پوشه** | یک فایلِ `.ex5` + چند `.url`/`.txt` تبلیغاتی (رجیستریِ بروکر، rebate، «BEST SCALPER FOR GOLD»). |
| **دارایی/TF هدفِ ادعایی** | Gold scalping (طبقِ نامِ لینک‌ها)؛ بدونِ مستنداتِ فنی. |

## ۲. منطقِ استخراج‌شده

- **هیچ منطقِ قابلِ استخراجی وجود ندارد** — سورس بسته (`.ex5`) و هیچ توضیحِ استراتژی/`.set`
  همراه نیست. نام «Combo AI» و «3X» صرفاً بازاریابی است.
- بقیهٔ فایل‌ها لینک‌های افیلیتِ بروکر (RoboForex/Tickmill/Forex4you) برای rebate هستند،
  نه محتوایِ فنی.

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

- **هیچ.** بدونِ سورس یا توضیحِ قاعده، چیزی برای ترجمه به قانونِ بک‌تست‌پذیر نیست.
- طبقِ یادآوریِ صداقتِ علمیِ README، ادعاهایِ «AI / best gold scalper» بدونِ بک‌تستِ
  مستقل ارزشِ صفر دارند.

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ⛔ خیر. **فیلتر:** ⛔ خیر. (سورس بسته، بدونِ منطقِ قابلِ مشاهده.)

## ۵. نتیجهٔ بک‌تست

**N/A** — موردِ غیرقابلِ استخراج (کامپایل‌شده + تبلیغاتی). Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده (بستهٔ تبلیغاتیِ ex5؛ بدونِ سورس/توضیح؛ ارزشِ تحقیقیِ صفر). موردِ بعدی بررسی می‌شود._
