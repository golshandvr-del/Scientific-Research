# بررسیِ منبع: `2019 TREND IYIOLA.ex4`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر، مارجین `40$/لات`.

پنجاه‌وهشتمین موردِ بررسی‌شده (ترتیبِ الفباییِ `LC_ALL=C`). **کامپایل‌شدهٔ obfuscated — جعبهٔ سیاه.**

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | اندیکاتورِ روندِ کامپایل‌شدهٔ MT4 (`.ex4`، ~۴۱KB) — سورس بسته. |
| **نام** | «2019 TREND IYIOLA» (احتمالاً سازنده: IYIOLA — نامِ نیجریه‌ای رایج در جامعهٔ FX). |
| **رشته‌های خوانا** | ❌ هیچ — همهٔ رشته‌ها رمزگذاری/مبهم‌سازی‌شده‌اند (نه پارامتر، نه Copyright). |

## ۲. منطقِ استخراج‌شده

- از نام برمی‌آید که یک **اندیکاتورِ روند** (فلش یا خطِ رنگی) است، اما هیچ رشتهٔ خوانایی
  برای تأییدِ اندیکاتورهای پایه، پارامترها یا قانونِ ورود/خروج وجود ندارد.
- `strings` فقط بایت‌های تصادفیِ obfuscate‌شده می‌دهد. **منطقِ عددی قابلِ استخراج نیست.**
- ⚠️ اندیکاتورهای «trend» با نامِ سازنده معمولاً بر پایهٔ MA/HMA/رنگِ کندل‌اند و ریسکِ
  repaint دارند؛ اما بدونِ سورس تأییدناپذیر است.

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

- ❌ چیزی برای ترجمه به قانونِ بک‌تست‌پذیر نیست (جعبهٔ سیاهِ مبهم‌سازی‌شده).

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ هیچ.
- **فیلترِ تأیید:** ❌ هیچ.

## ۵. نتیجهٔ بک‌تست

**N/A** — سورس بسته و obfuscated. Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده (اندیکاتورِ روندِ کامپایل‌شده و مبهم؛ بدونِ لبهٔ قابلِ استخراج). موردِ بعدی بررسی می‌شود._
