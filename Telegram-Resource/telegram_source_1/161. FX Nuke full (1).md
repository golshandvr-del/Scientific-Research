# بررسیِ منبع: `161. FX Nuke full (1)/`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.

سی‌امین موردِ بررسی‌شده (ترتیبِ الفباییِ `LC_ALL=C`).

---

## ⚠️ نسخهٔ تکراری

این پوشه با `161. FX Nuke full/` **بایت‌به‌بایت یکسان است** (`diff -rq` = بدونِ تفاوت).
تحلیلِ کاملِ منطق (Currency Power Meter سه-سبکه DT/SC/SW، ایدهٔ فیلترِ قدرتِ دلار/DXY برای
طلا) در فایلِ زیر آمده است:

➡️ **[`161. FX Nuke full.md`](<161. FX Nuke full.md>)**

## نتیجه

- **لبهٔ نو:** ⚠️ (طبقِ نسخهٔ اصلی — ایدهٔ Currency-Strength/DXY).
- **فیلترِ تأیید:** ✅ کاندیدِ فیلترِ جهتِ DXY (ثبت‌شده در فایلِ اصلی).
- **بک‌تست:** N/A (طبق User Note فقط md). Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده (نسخهٔ تکراری؛ ارجاع به فایلِ اصلی). موردِ بعدی بررسی می‌شود._
