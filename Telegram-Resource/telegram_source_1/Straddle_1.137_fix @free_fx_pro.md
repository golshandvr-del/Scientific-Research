# بررسیِ منبع: `Straddle_1.137_fix @free_fx_pro`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر، مارجین `40$/لات`.

بررسیِ نشستِ جاری (ترتیبِ الفبایی — پس از `Smart Owl FX V1.4`).

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | **پوشهٔ بستهٔ EA** برای MT5 — فایلِ اصلی `Straddle_1.137_fix.ex5` (سورس بسته) + چند فایلِ `.url`/`.txt` که همگی **تبلیغِ بروکر/کانال** (RoboForex, TickMill, Forex4you rebate) هستند و ارزشِ تحقیقاتی ندارند. |
| **نام** | Straddle v1.137. |
| **حجم فایلِ اصلی** | ~۵۳۶KB (بسیار بزرگ ⇒ داشبورد/گرافیک + منطقِ مدیریتِ سفارش). |
| **رشته‌های استخراج‌شده** | فقط برندِ توزیع `https://t.me/ebfe90` و `StraddleLogo85.bmp`. |
| **تایم‌فریم/دارایی هدف** | معمولِ straddle: پیش از اخبار / روی شکستِ نوسان. |

## ۲. منطقِ استخراج‌شده

- استراتژی **Straddle** است؛ منطقِ آن پیش‌تر در فایلِ `STRADDLE EA V1.0 MT4.ex4.md` به‌طورِ
  کامل مستند شد: دو سفارشِ معلق (Buy-Stop بالا، Sell-Stop پایین) پیش از انفجارِ نوسان؛ یک طرف
  تریگر و طرفِ مقابل کنسل می‌شود.
- این نسخه (`v1.137`, MT5) صرفاً پیاده‌سازیِ دیگری از همان مفهوم است؛ به‌احتمالِ زیاد با
  پارامترهای مدیریتِ سفارش/انقضای غنی‌تر (به‌خاطرِ حجمِ بزرگ‌تر).

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

- همان ایدهٔ ثبت‌شده در `STRADDLE EA V1.0`: **Breakout-Straddle روی squeeze** (فشردگیِ نوسان)
  با فاصلهٔ `k×ATR`، تست روی XAUUSD/EURUSD با هزینهٔ واقعیِ ۳.۳pip طلا.
- این فایل ایدهٔ **جدیدی نسبت به `STRADDLE EA V1.0` اضافه نمی‌کند** (هم‌مفهوم؛ سورس بسته).

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ تکرارِ مفهومِ straddleِ قبلی؛ چیزِ نوی اختصاصی ندارد.
- **فیلترِ تأیید:** ⚠️ همان فیلترِ «squeeze» که در فایلِ straddleِ قبلی یادداشت شد.
- **همپوشانی:** ~۱۰۰٪ مفهومی با `STRADDLE EA V1.0 MT4.ex4` (هر دو straddle). یک ایدهٔ
  بک‌تستِ straddle کافی است؛ لازم نیست دو بار آزموده شود.
- **جمع‌بندی:** منبعِ کم‌ارزش (نسخهٔ بستهٔ دومِ همان مفهوم + عمدتاً فایل‌های تبلیغاتی). ثبت برای کامل‌شدنِ فهرست.

## ۵. نتیجهٔ بک‌تست

**N/A** — طبق دستورِ نشستِ جاری (User Note) فقط فایلِ md بررسی ساخته می‌شود؛ بک‌تست نشد.
Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده (نسخهٔ دومِ straddleِ بی‌سورس؛ هم‌مفهوم با STRADDLE EA V1.0). موردِ بعدی بررسی می‌شود._
