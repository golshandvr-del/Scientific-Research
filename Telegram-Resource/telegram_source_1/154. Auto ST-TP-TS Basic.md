# بررسیِ منبع: `154. Auto ST-TP-TS Basic/`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.

بیست‌وپنجمین موردِ بررسی‌شده (ترتیبِ الفباییِ `LC_ALL=C`).

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | نسخهٔ **Basic** ابزارِ مدیریتِ خودکارِ معامله MT4 (`.ex4`، سورس بسته). |
| **نام/سازنده** | Auto ST-TP-TS Basic — AXE Corporation / GCC Incorporation. |
| **فایل‌های همراه** | `LEER PRIMERO- Read First.txt` (تبلیغِ کانال) + `parametros.png` (اسکرین‌شاتِ پارامترها). |

## ۲. منطقِ استخراج‌شده

- **هم‌خانواده و نسخهٔ سبک‌ترِ** موردِ ۲۴ (`154. AXE Auto SL-TS-TP`). همان سه‌گانهٔ مدیریتِ
  خروج: **Stop-Loss خودکار، Take-Profit خودکار، Trailing-Stop.**
- تحلیلِ کاملِ منطق و کاربردِ پروژه در فایلِ زیر آمده است:

  ➡️ **[`154. AXE Auto SL-TS-TP.md`](<154. AXE Auto SL-TS-TP.md>)**

- تفاوت: نسخهٔ «Basic» احتمالاً پارامترهای کمتر و فقط trailing/BE سادهٔ ثابت‌پیپ دارد
  (بدونِ حالت‌های پیشرفتهٔ نسخهٔ کامل).

## ۳. ایدهٔ قابلِ آزمون / کاربردِ پروژه

- مانندِ موردِ ۲۴: الگوی **Trailing-Stop و Break-Even ثابت‌پیپ** برای **بخشِ مدیریتِ معاملهٔ
  سایت**. نسخهٔ Basic مرجعِ سادهٔ پیاده‌سازیِ حداقلی است.

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ (سیگنال ندارد).
- **کاربرد:** ✅ الگوی مدیریتِ خروجِ ساده برای سایت (تکراری با موردِ ۲۴، سطحِ Basic).

## ۵. نتیجهٔ بک‌تست

**N/A** — طبق دستورِ نشستِ جاری فقط md بررسی ساخته می‌شود. Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده (نسخهٔ Basicِ Trade Manager؛ ارجاع به موردِ ۲۴). موردِ بعدی بررسی می‌شود._
