# بررسیِ منبع: `165. Massive-Fx-Profit-indicator (1)/`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر، مارجین `40$/لات`.

پنجاهمین موردِ بررسی‌شده (ترتیبِ الفباییِ `LC_ALL=C`). **نسخهٔ تکراریِ (duplicate) موردِ ۳۴ام.**

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | اندیکاتورِ سیگنالِ فارکس MT4 (`Massive-Fx-Profit-indicator.ex4`) — سورس بسته. |
| **سازنده** | AXE Corporation / GCC Incorporation (کانالِ تلگرام `@EAIndicatorFX`). |
| **فایل‌های همراه** | `LEER PRIMERO- Read First.txt` (فقط تبلیغِ کانال + درخواستِ رمز)، `DONACIONES.txt` (درخواستِ کمکِ مالی). |
| **رابطه با موردِ اصلی** | **کپیِ کاملِ `165. Massive-Fx-Profit-indicator`** — ساختارِ پوشه، دو txt و فایلِ `.ex4` همگی یکسان‌اند (تکرارِ آرشیو با پسوندِ ` (1)`). |

## ۲. منطقِ استخراج‌شده

- این پوشه **هیچ محتوای تازه‌ای** نسبت به نسخهٔ اصلی (موردِ ۳۴) ندارد؛ همان اندیکاتورِ
  فلشِ Buy/Sell تجاریِ خانوادهٔ AXE با سورسِ بسته است.
- txtِ «Read First» فقط لینکِ گروه/کانالِ تلگرام و روشِ گرفتنِ رمز را دارد؛ **هیچ قانونِ
  ورود/خروج یا تنظیماتِ اندیکاتور** در آن نیست.
- بدونِ سورس یا مستندات، **منطقِ عددی قابلِ استخراج نیست.**
- ⚠️ نامِ «profit/signals» در این خانوادهٔ محصولات تقریباً همیشه با repaint همراه است؛ تأییدناپذیر.

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

- ❌ چیزی برای ترجمه به قانونِ بک‌تست‌پذیر نیست (جعبهٔ سیاهِ بدونِ مستندات؛ ضمناً تکراری).

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ هیچ.
- **فیلترِ تأیید:** ❌ هیچ.

## ۵. نتیجهٔ بک‌تست

**N/A** — سورس بسته، بدونِ مستندات، و نسخهٔ تکراری. Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده (duplicate موردِ ۳۴؛ فلشِ تجاریِ بستهٔ خانوادهٔ AXE؛ بدونِ لبهٔ قابلِ استخراج). موردِ بعدی بررسی می‌شود._
