# بررسیِ منبع: `sharp ai EA V1.0 MT4.ex4`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر، مارجین `40$/لات`.

بررسیِ نشستِ جاری (ترتیبِ الفباییِ معکوس — از انتهای فهرست به ابتدا).

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | Expert Advisor (EA) کامپایل‌شدهٔ MT4 (`.ex4`) — سورس بسته. |
| **نام** | Sharp AI EA v1.0. |
| **حجم** | ~۵۷KB. |
| **رشته‌های استخراج‌شده** | ❌ هیچ رشتهٔ معناداری (کامپایلِ فشرده)؛ فقط قطعه‌های بی‌معنی. استنتاج از نام و نوع. |
| **تایم‌فریم/دارایی هدف** | نامشخص؛ EAِ عمومی با برچسبِ بازاریابیِ «AI». |

## ۲. منطقِ استخراج‌شده (بر اساسِ نام و کلاسِ محصول)

- برچسبِ «AI» در EAهای تلگرامی تقریباً همیشه **ادعای بازاریابی** است، نه شبکهٔ عصبیِ واقعی؛
  معمولاً پشتِ آن یک ترکیبِ ساده از اندیکاتورها (MA/RSI/ATR) + مدیریتِ لات نهفته است.
- «Sharp» به سیگنال‌گیریِ سریع/اسکالپ اشاره دارد؛ محتمل‌ترین منطق: **ورودِ مومنتومی روی
  کندلِ تند** با SL/TP ثابت یا ATR-based.
- بدونِ سورس، هیچ قاعدهٔ ورود/خروجِ دقیقی قابلِ تأیید نیست؛ ریسکِ repaint/تطبیقِ منحنی بالاست.

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

- تنها ایدهٔ عمومیِ قابلِ ترجمه: **ورودِ مومنتومی روی شکستِ کندلِ پرشتاب** (بدنهٔ کندل >
  k×ATR) با خروجِ ATR-based — که این خود جزوِ خانوادهٔ استراتژی‌های استانداردِ آزموده‌شده است.
- هیچ پارامتر یا منطقِ اختصاصیِ قابلِ استخراج از این باینری وجود ندارد.

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ برچسبِ «AI» بدونِ سورس ارزشِ اطلاعاتی ندارد.
- **فیلترِ تأیید:** ❌ چیزی برای استخراج به‌عنوان فیلتر ندارد.
- **جمع‌بندی:** منبعِ کم‌ارزش (سورس بسته + ادعای AIِ اثبات‌نشده). صرفاً برای کامل‌شدنِ فهرست ثبت شد.

## ۵. نتیجهٔ بک‌تست

**N/A** — طبق دستورِ نشستِ جاری (User Note) فقط فایلِ md بررسی ساخته می‌شود؛ بک‌تست نشد.
Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده (EAِ «AI» بی‌سورس؛ کم‌ارزش). موردِ بعدی بررسی می‌شود._
