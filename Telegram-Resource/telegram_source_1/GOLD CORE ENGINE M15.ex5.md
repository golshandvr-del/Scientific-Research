# بررسیِ منبع: `GOLD CORE ENGINE M15.ex5`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر.

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | **EA/اندیکاتورِ MT5 کامپایل‌شده (`.ex5`)** — سورس بسته. |
| **نام** | `GOLD CORE ENGINE` by `LifeInDreamsWorld` — مخصوصِ **طلا در M15**. مستقیماً روی داراییِ اصلیِ تحقیق و **تایم‌فریمِ پیشنهادیِ سایتِ ما**. |
| **داراییِ هدفِ سازنده** | **XAUUSD M15** — بالاترین تطبیق با کارتِ `XAUUSD:M15` سایت. |
| **سورس** | ❌ بسته؛ منطق از نوع و تایم‌فریم استنتاج می‌شود. |

## ۲. منطقِ استخراج‌شده (موتورِ طلای M15 — استنتاج)

نامِ «Core Engine M15» معمولاً به یک موتورِ ترکیبیِ چندمؤلفه اشاره دارد که برای نوسان‌گیریِ سویینگِ M15 طلا ساخته شده — احتمالاً ترکیبی از:
- فیلترِ روند (MA/ADX)،
- تشخیصِ ساختار/سطح (S/R یا سوینگ)،
- تریگرِ مومنتوم یا شکست.
چون M15 (نه M1) است، به اسپردِ ۳.۳pip حساسیتِ کمتری دارد و برای تحقیقِ ما مناسب‌تر از اسکالپرهای M1.

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

> **بالاترین ربطِ عملی به سایت:** تایم‌فریمِ M15 طلا دقیقاً همان کارتِ اصلیِ ماست.
> - می‌توان یک «موتورِ ترکیبی M15» ساخت: `فیلترِ روند (EMA/ADX) + سطحِ سوینگ + تریگرِ کندلِ تأیید`، با SL/TP مبتنی بر ATR و RR≥۱.۵.
> - چون M15 است، TP معمولی (۱۰۰–۳۰۰ point طلا) به‌راحتی اسپردِ ۳.۳pip را پوشش می‌دهد → سودِ خالصِ واقعی محتمل‌تر.

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ⚠️ سورس بسته است پس نمی‌توان لبهٔ دقیق را استخراج کرد؛ اما تمرکزِ M15 طلا با موتورِ فعلیِ سایت هم‌راستاست.
- **فیلترِ تأیید:** ⚠️ بدونِ سورس، فقط به‌عنوان یادآوریِ «معماریِ موتورِ ترکیبیِ M15 طلا» ثبت می‌شود.

## ۵. نتیجهٔ بک‌تست

**N/A در این نشست** (طبق User Note فقط ساختِ md). **ربطِ بالا به کارتِ XAUUSD:M15 سایت**، اما سورس بسته؛ فقط الهامِ معماری.

_وضعیت: ✅ بررسی‌شده (md ساخته شد). سورس بسته؛ طلا M15 (هم‌راستا با کارتِ اصلیِ سایت). موردِ بعدی: `GOLD-PIP MINER EA V3 for MT4.ex4`._
