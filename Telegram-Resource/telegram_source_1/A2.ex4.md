# بررسیِ منبع: `A2.ex4`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر.

ششمین بلوکِ بررسی (اولین موردِ نشستِ جاری؛ ادامهٔ ترتیبِ الفباییِ `LC_ALL=C` از اندیس ۶۵).

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | اندیکاتور/EAِ MT4 کامپایل‌شده (`.ex4`) — سورس بسته. |
| **نام** | `A2` (نامِ عمومی/بی‌معنا؛ هیچ سرنخی از استراتژی نمی‌دهد). |
| **حجم** | ~۱۹۵KB. |
| **رشته‌های قابلِ استخراج** | ❌ هیچ متنِ معنادار — خروجیِ `strings` کاملاً نویزِ باینریِ obfuscated است (نامِ پارامتر، تایم‌فریمِ هدف یا متنِ راهنما پیدا نشد). |

## ۲. منطقِ استخراج‌شده

❌ **قابلِ استخراج نیست.** فایلِ `.ex4`ِ کامپایل‌شده و obfuscated است. هیچ نامِ پارامتر،
سطحِ عددی، تایم‌فریمِ هدف یا متنِ توضیحی قابلِ خواندن نیست. نامِ فایل (`A2`) نیز
بی‌معناست.

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

❌ **هیچ.** بدونِ منطقِ قابلِ استخراج و بدونِ نامِ معنادار، هیچ فرضیهٔ بک‌تست‌پذیری
قابلِ ساخت نیست.

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ — **فیلترِ تأیید:** ❌. جعبهٔ سیاهِ بدونِ اطلاعات.

## ۵. نتیجهٔ بک‌تست

**N/A** — طبق دستورِ نشستِ جاری (User Note) فقط فایلِ md بررسی ساخته می‌شود؛ بک‌تست نشد.
منبع منطقِ قابلِ استخراج ندارد. Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده و کنار گذاشته شد (EA/اندیکاتورِ کامپایل‌شدهٔ obfuscated، بدونِ اطلاعاتِ قابلِ استخراج). موردِ بعدی بررسی می‌شود._
