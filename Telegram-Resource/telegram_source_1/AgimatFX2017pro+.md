# بررسیِ منبع: `AgimatFX2017pro+`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر.

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | **نصب‌کنندهٔ ویندوز (`.exe`)** برای سیستمِ تجاریِ Agimat FX 2017 Pro+. |
| **محتوا** | فقط `AgimatFX2017pro+.exe` (installer) — فایلِ اجراییِ باینری، **قابلِ اجرا/بررسی در این سندباکس نیست و ارزشِ امنیتی/تحقیقاتیِ اجرا ندارد**. |
| **سورس** | ❌ بسته کامل. |

## ۲. منطقِ استخراج‌شده

⚠️ **از دانشِ عمومی دربارهٔ Agimat FX:** یک سیستمِ تجاریِ non-repaint (ادعای سازنده) با فلشِ خرید/فروش که ترکیبی از **چند اندیکاتورِ روند/momentum** را در یک داشبورد جمع می‌کند و برای فارکس و باینری‌آپشن بازاریابی می‌شود. مثلِ اکثرِ محصولاتِ Agimat، ادعای دقتِ بالا دارد اما **منطقِ داخلی مخفی و اثبات‌نشده** است.

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

❌ **هیچ منطقِ قابلِ استخراج.** installer باینری است و در سندباکسِ لینوکسی اجرا نمی‌شود؛ حتی اگر می‌شد، سورسِ اندیکاتورها همچنان مخفی می‌ماند. مضمونِ «داشبوردِ چند-اندیکاتوری non-repaint» یک ایدهٔ عمومی است، نه چیزی مختصِ این محصول.

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ — **فیلترِ تأیید:** ❌ (بدونِ دسترسی به منطق).

## ۵. نتیجهٔ بک‌تست

**N/A** — installer باینری، بدونِ منطقِ قابلِ استخراج. Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده و کنار گذاشته شد (installerِ `.exe`ِ تجاری؛ منطق مخفی و غیرقابلِ استخراج). موردِ بعدی: `Aj indicator`._
