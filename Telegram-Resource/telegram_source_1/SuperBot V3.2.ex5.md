# بررسیِ منبع: `SuperBot V3.2.ex5`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر، مارجین `40$/لات`.

بررسیِ نشستِ جاری (ترتیبِ الفبایی — پس از `Strong Gradient Channel`).

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | Expert Advisor (EA) کامپایل‌شدهٔ MT5 (`.ex5`) — سورس بسته. |
| **نام** | SuperBot V3.2 (برند تجاری `SuperBot.Live`). |
| **حجم** | ~۳۷KB. |
| **رشته‌های استخراج‌شده** | فقط `SuperBot` و `https://SuperBot.Live` (UTF-16). هیچ نام‌پارامتر/منطقِ داخلی لو نرفت. |
| **تایم‌فریم/دارایی هدف** | نامشخص؛ EAِ خودکارِ تجاری. |

## ۲. منطقِ استخراج‌شده (استنتاجِ محتاطانه)

- سورس بسته ⇒ منطقِ دقیق قابلِ استخراج نیست. محصولِ تجاریِ اشتراکی (`SuperBot.Live`).
- نامِ عام «SuperBot» هیچ سرنخِ استراتژیک نمی‌دهد؛ محتمل‌ترین کلاس: EAِ خودکار با ترکیبی
  از اندیکاتورهای استاندارد + مدیریتِ پوزیشن.

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

- هیچ منطقِ اختصاصیِ قابلِ استخراج از باینری وجود ندارد.

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ بی‌سورس.
- **فیلترِ تأیید:** ❌ چیزی برای استخراج ندارد.
- **جمع‌بندی:** منبعِ کم‌ارزش (EAِ تجاریِ بسته). برای کامل‌شدنِ فهرست ثبت شد. (نسخهٔ بعدیِ V4.0 نیز در همین آرشیو موجود است و جداگانه بررسی می‌شود.)

## ۵. نتیجهٔ بک‌تست

**N/A** — طبق دستورِ نشستِ جاری (User Note) فقط فایلِ md بررسی ساخته می‌شود؛ بک‌تست نشد.
Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده (EAِ تجاریِ بی‌سورس؛ کم‌ارزش). موردِ بعدی بررسی می‌شود._
