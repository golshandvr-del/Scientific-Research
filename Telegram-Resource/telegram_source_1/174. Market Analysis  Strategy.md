# بررسیِ منبع: `174. Market Analysis  Strategy/`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر، مارجین `40$/لات`.

سی‌ونهمین موردِ بررسی‌شده (ترتیبِ الفباییِ `LC_ALL=C`).

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | بستهٔ چند-اندیکاتوریِ «تحلیلِ بازار» MT4 (سورس بسته) + تمپلیت `22.tpl`. |
| **سازنده** | بازبسته‌بندیِ AXE Corporation (`@EAIndicatorFX`). |
| **اجزا** | `Launche.ex4`, `Market_An.ex4`, `RD-Combo.ex4`, `Similar Fx erts V4.ex4`, `_v_1.ex4`, `iClock.ex4` (نام‌ها بریده/obfuscated). |
| **رشته‌های استخراج‌شده** | ✅ فقط `Launche.ex4`: *«Channel is Powerful — S\R Levels In Action»*؛ `iClock` = ساعتِ چارت. بقیه بی‌رشته. |

## ۲. منطقِ استخراج‌شده

- تنها سرنخِ معنادار: **`Launche.ex4`** یک اندیکاتورِ **کانال + سطوحِ حمایت/مقاومت (S/R)**
  است («Channel … S\R Levels In Action»). یعنی هستهٔ سیستم، معاملهٔ **برخورد/شکستِ کانال و
  سطوحِ S/R** است.
- `iClock` صرفاً نمایشِ ساعت/سشن است (ابزارِ جانبی، نه سیگنال).
- `Market_An`, `RD-Combo`, `Similar Fx alerts V4`, `_v_1`: obfuscated و بدونِ رشتهٔ قابلِ استخراج.
- بدونِ مستنداتِ قانونِ ورود/خروج (txtها فقط تبلیغِ AXE).

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

- ایدهٔ «کانال + سطوحِ S/R» مفهومِ استانداردی است که پروژه از راهِ MA-Confluence و
  Squeeze→Breakout بخشی از آن را پوشش داده. بدونِ پارامترهای دقیقِ کانال، لبهٔ نوِ قابلِ
  بازتولید ندارد.

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ محتمل نیست (هم‌پوشانیِ مفهومی با ابزارهای کانال/سطحِ موجود).
- **فیلترِ تأیید:** 🔸 فقط در حدِ ایدهٔ کلیِ «فیلترِ نزدیکیِ قیمت به سطحِ S/R» — نیازمندِ
  پیاده‌سازیِ مستقل، نه استخراج از این فایل.

## ۵. نتیجهٔ بک‌تست

**N/A** — سورس بسته؛ فقط یک رشتهٔ توصیفی استخراج شد. Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده (بستهٔ «تحلیلِ بازار» بستهٔ AXE با هستهٔ کانال/S-R؛ بدونِ لبهٔ قابلِ استخراج). موردِ بعدی بررسی می‌شود._
