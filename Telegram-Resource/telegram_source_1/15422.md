# بررسیِ منبع: پوشهٔ `15422/` (`renkolivechart_v600_indi.mq4` + `renko_range_detector.mq4`)

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر، مارجین `40$/لات`.

بیست‌وششمین موردِ بررسی‌شده (ترتیبِ الفباییِ `LC_ALL=C`). پوشهٔ دو-فایلی با **سورسِ باز**.

---

## ۱. چیستی

| فایل | نوع | نقش |
|---|---|---|
| `renkolivechart_v600_indi.mq4` | اندیکاتورِ MT4 (سورسِ باز، MetaQuotes 2016) | ساختِ **چارتِ آفلاینِ Renko زنده**. |
| `renko_range_detector.mq4` | اندیکاتورِ MT4 (سورسِ باز، File45 2012) | نمایشِ برچسبِ وضعیتِ Renko روی چارت. |

### پارامترهای کلیدی
```
RenkoBoxSize   = 3.0   ← اندازهٔ آجرِ Renko (برحسبِ pip)
RenkoBoxOffset = 0
RenkoTimeFrame = 7     ← تایم‌فریمِ آفلاینِ خروجی
ShowWicks      = false ← نمایشِ سایه‌ها
EmulateOnLineChart = true
```

## ۲. منطقِ استخراج‌شده

- **RenkoLiveChart** یک اندیکاتورِ کلاسیکِ MT4 است که از تیک/کندلِ زنده یک **چارتِ آفلاینِ
  Renko** با اندازهٔ box مشخص می‌سازد؛ سایه‌ها اختیاری. این همان مفهومِ موردِ ۲۱ (`1299/renko`)
  است اما با پیاده‌سازیِ live/offline معروف‌تر (نسخهٔ v600).
- **renko_range_detector** صرفاً یک **برچسبِ متنی** (رنگ/فونت/موقعیت) برای نمایشِ وضعیت
  است — ابزارِ کمکیِ بصری، بدونِ منطقِ سیگنال.

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

- تأییدِ دوبارهٔ ارزشِ **بازسازیِ سریِ Renko در پایتون** (رجوع به موردِ ۲۱). این نسخه
  پارامترِ عملیِ `BoxSize=3pip` و گزینهٔ **حذفِ سایه (ShowWicks=false)** را پیشنهاد می‌دهد
  که برای طلا (نوسانِ بالا) باید متناسب‌سازی شود (box بزرگ‌تر).
- قانونِ بک‌تست‌پذیر مانندِ موردِ ۲۱: reversal/continuation روی آجرهای کامل‌شدهٔ Renko.

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ⚠️ همان ایدهٔ Renkoِ موردِ ۲۱ (تکراری از نظرِ مفهوم)؛ لبهٔ نوی مستقلِ اضافه
  ندارد اما پیاده‌سازیِ مرجعِ استاندارد را تأیید می‌کند.
- **فیلترِ تأیید:** ✅ رنگ/جهتِ آجرِ Renko به‌عنوان فیلترِ رژیمِ روند (تکراری با موردِ ۲۱،
  ثبت‌شده برای آزمونِ آینده).

## ۵. نتیجهٔ بک‌تست

**N/A** — طبق دستورِ نشستِ جاری فقط md بررسی ساخته می‌شود. Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده (RenkoLiveChart v600 — تکرارِ مفهومِ Renkoِ موردِ ۲۱ + برچسبِ بصری). موردِ بعدی بررسی می‌شود._
