# بررسیِ منبع: `Rx King Vip V 0.2.ex4`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر، مارجین `40$/لات`.

**اندیکاتور/EA بی‌سورس (نامِ تجاری VIP).**

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | اندیکاتور/EA کامپایل‌شدهٔ MT4 (`.ex4`) — سورس بسته. |
| **نام** | Rx King VIP v0.2 (نامِ تجاری «VIP»). |
| **حجم** | ~۲۰۸KB (بزرگ ⇒ احتمالاً چند اندیکاتور/موتورِ تشخیصِ سیگنالِ ترکیبی). |
| **رشته‌های استخراج‌شده** | ❌ هیچ رشتهٔ معناداری. |
| **تایم‌فریم/دارایی هدف** | نامشخص. |

## ۲. منطقِ استخراج‌شده

- سورس بسته ⇒ منطقِ ورود/خروج قابلِ استخراج نیست.
- حجمِ بزرگ محتمل است به‌معنایِ موتورِ سیگنالِ فلش‌محورِ ترکیبی باشد؛ اما بدونِ سورس هیچ قاعده‌ای
  قابلِ تأیید نیست. اینگونه اندیکاتورهای «VIP» اغلب repaint دارند و ادعایشان باید با شک نگریسته شود.

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

- هیچ منطقِ اختصاصیِ قابلِ استخراج وجود ندارد.

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ بی‌سورس.
- **فیلترِ تأیید:** ❌ چیزی برای استخراج ندارد.
- **جمع‌بندی:** منبعِ کم‌ارزش (اندیکاتورِ بستهٔ تجاری؛ احتمالِ repaint).

## ۵. نتیجهٔ بک‌تست

**N/A** — طبق دستورِ نشستِ جاری فقط فایلِ md بررسی ساخته می‌شود؛ بک‌تست نشد. Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده (اندیکاتورِ بی‌سورسِ تجاری، کم‌ارزش). موردِ بعدی بررسی می‌شود._
