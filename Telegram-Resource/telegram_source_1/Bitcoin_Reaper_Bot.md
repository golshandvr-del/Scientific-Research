# بررسیِ منبع: `Bitcoin_Reaper_Bot`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر.

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | **EA MT4 کامپایل‌شده (`.ex4`)** برای **بیت‌کوین** + بستهٔ تبلیغاتی (HTML/txt/docx با لینک‌های تلگرام و بروکر). |
| **داراییِ هدف** | BTC — **نه** XAUUSD/EURUSD (داراییِ اصلیِ تحقیق). |
| **سورس** | ❌ بسته؛ مستندِ همراه فقط تبلیغِ کانال/بروکر است (`INFO TRADER.txt` = صرفاً لینک‌های تبلیغاتی). |

## ۲. منطقِ استخراج‌شده

❌ **هیچ.** فایلِ همراه هیچ توضیحِ استراتژیِ فنی ندارد — فقط لینکِ تلگرام و بروکرِ ارجاعی. EA بسته است و داراییِ هدفش بیت‌کوین است که مشخصاتِ نوسان/اسپردِ آن کاملاً با طلا/فارکس متفاوت است.

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

❌ **هیچ.** بی‌ربط به داراییِ تحقیق، سورس بسته، بدونِ منطقِ مستند.

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ — **فیلترِ تأیید:** ❌.

## ۵. نتیجهٔ بک‌تست

**N/A** — EA بیت‌کوینِ بسته و تبلیغاتی. Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده و کنار گذاشته شد (EA بیت‌کوینِ بسته؛ بی‌ربط به XAUUSD/EURUSD و بدونِ منطقِ مستند). موردِ بعدی: `Boom & Crash Quasimodo_Pattern.ex5`._
