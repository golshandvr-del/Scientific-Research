# بررسیِ منبع: `164. Ultimate-Trend-Signals/`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر، مارجین `40$/لات`.

سی‌وسومین موردِ بررسی‌شده (ترتیبِ الفباییِ `LC_ALL=C`).

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | اندیکاتورِ سیگنالِ روند MT4 (دو نسخه: `v1-500`, `v2-600`) — سورس بسته. |
| **فایلِ همراه** | `DONACIONES.txt` (درخواستِ کمکِ مالی). |
| **رشته‌های استخراج‌شده** | ❌ هیچ (کامپایلِ فشرده). |

## ۲. منطقِ استخراج‌شده (از نام)

- «Ultimate Trend Signals» یک اندیکاتورِ **فلشِ روندی** است — معمولاً ترکیبِ چند
  MA/اسیلاتور که فلشِ Buy/Sell در جهتِ روند تولید می‌کند. دو نسخهٔ 500/600 احتمالاً
  تنظیماتِ حساسیتِ متفاوت.
- بدونِ سورس یا مستندات، **منطقِ عددی قابلِ استخراج نیست.**
- ⚠️ نامِ «signals/ultimate» تقریباً همیشه با repaint همراه است؛ تأییدناپذیر.

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

- ❌ چیزی برای ترجمه به قانونِ بک‌تست‌پذیر نیست (جعبهٔ سیاهِ بدونِ مستندات).

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ هیچ.
- **فیلترِ تأیید:** ❌ هیچ.

## ۵. نتیجهٔ بک‌تست

**N/A** — سورس بسته و بدونِ مستندات. Δ سودِ خالص = ۰.
(نسخهٔ `164. Ultimate-Trend-Signals (1)` بایت‌به‌بایت یکسان است.)

_وضعیت: ✅ بررسی‌شده (فلشِ روندِ بستهٔ تجاری؛ بدونِ لبهٔ قابلِ استخراج). موردِ بعدی بررسی می‌شود._
