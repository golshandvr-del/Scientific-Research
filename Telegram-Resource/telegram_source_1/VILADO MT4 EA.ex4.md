# بررسیِ منبع: `VILADO MT4 EA.ex4`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر، مارجین `40$/لات`.

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | Expert Advisor (EA) کامپایل‌شدهٔ MT4 (`.ex4`) — سورس بسته. |
| **نام** | VILADO MT4 EA. |
| **حجم** | ~۱۲.۵KB (بسیار کوچک ⇒ منطقِ ساده). |
| **رشته‌های استخراج‌شده** | ❌ هیچ رشتهٔ معناداری. استنتاج از نام/حجم. |
| **تایم‌فریم/دارایی هدف** | نامشخص. |

## ۲. منطقِ استخراج‌شده

- حجمِ بسیار کوچک (~۱۲.۵KB) نشان‌دهندهٔ **EAِ ساده** است: احتمالاً یک قانونِ ورودِ تک‌اندیکاتوری
  (مثلاً کراسِ MA یا سطحِ RSI) با TP/SL ثابت.
- بدونِ سورس/رشته، هیچ قاعدهٔ اختصاصیِ بک‌تست‌پذیر قابلِ استخراج نیست.

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

- هیچ ایدهٔ اختصاصی و قابلِ استخراجی ندارد؛ منطقِ محتمل (کراسِ MA/RSI ساده) از قبل جزوِ
  استراتژی‌های پایهٔ آزموده‌شدهٔ پروژه است.

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌.
- **فیلترِ تأیید:** ❌.
- **جمع‌بندی:** منبعِ کم‌ارزش (EAِ سادهٔ بی‌سورس).

## ۵. نتیجهٔ بک‌تست

**N/A** — طبق دستورِ نشستِ جاری فقط فایلِ md بررسی ساخته می‌شود؛ بک‌تست نشد. Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده (EAِ سادهٔ بی‌سورس؛ کم‌ارزش). موردِ بعدی بررسی می‌شود._
