# بررسیِ منبع: `#U00a9Pips Meter.ex4`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.

دومین موردِ بررسی‌شده (ترتیبِ الفباییِ `LC_ALL=C`).

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | اندیکاتورِ MT4 **کامپایل‌شده (`.ex4`)** — سورس بسته. |
| **نام** | Pips Meter |
| **سازنده** | کپی‌رایت + لینک `facebook.com/dippylohan.uk` (تنها رشته‌های قابلِ استخراج). |
| **ماهیت (از نام و رشته‌ها)** | یک **ابزارِ اندازه‌گیری/داشبوردِ pip** — نمایشِ فاصلهٔ pip/سود جاری، نه استراتژیِ ورود/خروج. |

## ۲. منطقِ استخراج‌شده

❌ **قابلِ استخراج نیست.** فایل `.ex4`ِ کامپایل‌شده است؛ `strings` فقط یک کپی‌رایت و
یک URL بازگرداند. هیچ قانونِ ورود/خروج/فیلترِ عددی در دسترس نیست.

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

❌ **هیچ.** ابزارهای «Pips Meter» صرفاً فاصله/سودِ جاری را روی چارت نمایش می‌دهند؛
یک ابزارِ کمکیِ بصری‌اند، نه منبعِ لبهٔ معاملاتی. چیزی برای بک‌تست ندارد.

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ — **فیلترِ تأیید:** ❌. یک utility است، نه سیگنال‌ساز.

## ۵. نتیجهٔ بک‌تست

**N/A** — بک‌تست نشد (منبع منطقِ معاملاتیِ قابلِ استخراج ندارد). Δ سودِ خالص = ۰؛
رکورد دست‌نخورده = **+$221,895**.

_وضعیت: ✅ بررسی‌شده و کنار گذاشته شد (ابزارِ اندازه‌گیریِ کامپایل‌شده، بدونِ لبه). موردِ بعدی بررسی می‌شود._
