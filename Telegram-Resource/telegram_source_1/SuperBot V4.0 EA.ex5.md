# بررسیِ منبع: `SuperBot V4.0 EA.ex5`

> قانونِ شمارهٔ ۱: تابعِ هدف = **سودِ خالصِ بیشتر** (XAUUSD + EURUSD)؛ WR کفِ ۴۰٪ دارد.
> مشخصاتِ حساب: `CONTRACT_SIZE=100`، طلا `3.3pip`، کمیسیون=صفر، مارجین `40$/لات`.

بررسیِ نشستِ جاری (ترتیبِ الفبایی — پس از `SuperBot V3.2`).

---

## ۱. چیستی

| ویژگی | مقدار |
|---|---|
| **نوع** | Expert Advisor (EA) کامپایل‌شدهٔ MT5 (`.ex5`) — سورس بسته. |
| **نام** | SuperBot V4.0 (برند تجاری `SuperBot.Live`). |
| **حجم** | ~۶۹KB (بزرگ‌تر از V3.2 ⇒ منطق/داشبوردِ توسعه‌یافته‌تر). |
| **رشته‌های استخراج‌شده** | فقط `SuperBot` و `https://SuperBot.Live` (UTF-16). هیچ نام‌پارامتر/منطقِ داخلی لو نرفت. |
| **تایم‌فریم/دارایی هدف** | نامشخص؛ EAِ خودکارِ تجاری. |

## ۲. منطقِ استخراج‌شده (استنتاجِ محتاطانه)

- سورس بسته ⇒ منطقِ دقیق قابلِ استخراج نیست. نسخهٔ جدیدترِ همان محصولِ تجاریِ `SuperBot.Live`
  (هم‌خانوادهٔ `SuperBot V3.2` در همین آرشیو).
- افزایشِ حجم نسبت به V3.2 صرفاً به افزوده‌شدنِ قابلیت/داشبورد اشاره دارد، نه سرنخِ استراتژیک.

## ۳. ایدهٔ قابلِ آزمون برای پروژهٔ ما

- هیچ منطقِ اختصاصیِ قابلِ استخراج از باینری وجود ندارد. هم‌مفهوم با نسخهٔ V3.2 (بدونِ افشای منطق).

## ۴. ارتباط با پرتفویِ فعلی

- **لبهٔ نو:** ❌ بی‌سورس.
- **فیلترِ تأیید:** ❌ چیزی برای استخراج ندارد.
- **همپوشانی:** با `SuperBot V3.2.ex5` (نسخهٔ قدیمی‌ترِ همان محصول).
- **جمع‌بندی:** منبعِ کم‌ارزش (EAِ تجاریِ بسته). برای کامل‌شدنِ فهرست ثبت شد.

## ۵. نتیجهٔ بک‌تست

**N/A** — طبق دستورِ نشستِ جاری (User Note) فقط فایلِ md بررسی ساخته می‌شود؛ بک‌تست نشد.
Δ سودِ خالص = ۰.

_وضعیت: ✅ بررسی‌شده (EAِ تجاریِ بی‌سورس؛ کم‌ارزش، هم‌خانوادهٔ V3.2). موردِ بعدی بررسی می‌شود._
