# 📱 اجرای سایتِ کامل روی گوشی با Termux (راهنمای کامل و ساده)

این پوشه به شما اجازه می‌دهد **کلِ سایتِ دستیارِ تصمیمِ معاملاتی** را — با **همان منطقِ
کاملِ web_tool** (رجیستریِ ماژولار، `/api/decision`، مدیریتِ معامله و …) — مستقیماً روی
گوشیِ خودتان و بدونِ اینترنتِ بیرونی (به‌جز دریافتِ قیمتِ زنده) اجرا کنید.

> ✅ **این نسخهٔ ساده‌شده نیست.** دقیقاً همان اپِ Hono در `web_tool/` است که فقط طوری
> بسته‌بندی شده تا روی Node/Termux اجرا شود. مغزِ تصمیم همان `runCard`ِ رجیستری است.
> **بخشِ `local-mobile` اولویتِ اصلیِ کاربر است و همیشه هم‌گام با سایت به‌روز می‌شود.**

> ## 🔴🔴 تغییرِ بزرگِ فعلی — پاک‌سازیِ `S396`: از **۹ کارت** به **۵ کارت**
>
> **این بلوک بر همهٔ بلوک‌های پایین‌ترِ این فایل اولویت دارد.**
>
> ### چه اتفاقی افتاد؟
> کلِ **۱۸۱ لایهٔ** پروژه با معیارِ سخت‌گیرانهٔ `RQS2 v2.4` بازداوری شد. نتیجه:
> **تنها ۵ لایه** قبول شدند. بقیه با معیارِ **قدیمی و بازنشستهٔ `RQS+`** مجوز گرفته
> بودند — معیاری که ثابت شد **رانشِ طبیعیِ صعودیِ طلا را با مهارتِ استراتژی اشتباه
> می‌گیرد** (یک لانگِ کاملاً تصادفی روی نمودارِ روزانه رایگان ۵۲.۴٪ می‌بَرد و آن
> معیارِ قدیمی همان هدیه را «مهارت» می‌شمرد).
>
> ⇒ سایت از **۹ کارت / ۲۹ لایه** به **۵ کارت / ۵ لایه** رسید.
>
> ### کارت‌هایی که اکنون می‌بینید
> | کارت | چه‌کار می‌کند (زبانِ ساده) |
> |---|---|
> | **طلا — M5** | روندِ صعودی + دروازهٔ «ساختارِ لگ-متناسب» تأیید کند ⇒ بخر |
> | **طلا — M15** | اگر بازار از قیمتِ بازشدن تندی رفت و اولین اصلاح رسید ⇒ **بفروش** |
> | **طلا — M30** | رانشِ آماریِ نیمهٔ ماه (تصمیم بر پایهٔ **تاریخ**، نه نمودار) |
> | **طلا — H1** | روزِ «ازسرگیریِ روند» به‌سبکِ بروکس، فقط بعد از ساعتِ ۱۶ UTC |
> | **طلا — H4** | `Williams %R` از `−۱۳` رد شد ⇒ بخر (فراوان‌ترین لایه: ۸۶۹ معامله) |
>
> ### ⚠️ کارت‌های یورو (EURUSD) حذف شدند
> هر سه کارتِ یورو برداشته شدند، چون **هیچ‌یک** از لایه‌هایشان معیارِ جدید را پاس
> نکرد. این ضعف نیست، **صداقت** است: ما ترجیح می‌دهیم چیزی به شما نشان **ندهیم**
> تا اینکه سیگنالی بدهیم که خودمان به آن اطمینان نداریم. اگر روزی لایهٔ یورویی
> قبول شود، کارتش برمی‌گردد.
>
> ### 🔴 یک باگِ مهم هم پیدا و تعمیر شد (`BUG-TFM`)
> بخشِ **«مدیریتِ معامله»** — همان بخشی که بعد از ثبتِ معامله به شما می‌گوید حد ضرر
> را کجا ببرید — برای **هر پنج کارت** از نمودارِ **۱۵ دقیقه‌ای** استفاده می‌کرد!
>
> یعنی اگر روی کارتِ **H4** معامله باز می‌کردید، سایت نوسانِ بازار را
> **۴ برابر کوچک‌تر** از واقعیت می‌دید (۸.۲۸ به‌جای ۳۳.۰۱) و در نتیجه حد ضرر را
> **بسیار تنگ‌تر** از لازم پیشنهاد می‌داد ⇒ معاملهٔ سالمِ شما با یک نوسانِ معمولی
> بسته می‌شد.
>
> ✅ تعمیر شد: اکنون هر کارت نوسانِ **تایم‌فریمِ خودش** را می‌بیند.
>
> | کارت | نوسانی که اکنون می‌بیند | قبلاً می‌دید |
> |---|---|---|
> | M5 | ۳.۱۵ | ۸.۲۸ ❌ |
> | M15 | ۸.۲۸ | ۸.۲۸ ✅ |
> | M30 | ۱۳.۱۸ | ۸.۲۸ ❌ |
> | H1 | ۱۸.۴۶ | ۸.۲۸ ❌ |
> | H4 | ۳۳.۰۱ | ۸.۲۸ ❌ |
>
> ### نتیجهٔ جانبیِ خوب: اپ **۲۷٪ سبک‌تر** شد
> حجمِ `app.bundle.mjs` از **۵۲۴.۳ KB** به **۳۸۱.۵ KB** رسید، چون کدِ ۲۴ لایهٔ
> حذف‌شده دیگر اصلاً به گوشیِ شما فرستاده نمی‌شود.

> ## ⭐⭐ تازه‌ترین تغییر — لایهٔ نوِ `S382` روی کارتِ **`طلا / دلار — H4`**
>
> **چه چیزی اضافه شد؟** لایه‌ای به نامِ **«مومنتومِ Williams %R — خریدِ قدرت»**
> (`S382`) روی کارتِ چهارساععتهٔ طلا. این کارت از **۳ لایه به ۴ لایه** رسید و این
> لایه **اولویتِ نخستِ** کارت شد؛ یعنی وقتی همزمان چند لایه سیگنال بدهند، این یکی
> را می‌بینید.
>
> **این لایه چه می‌کند؟ (به زبانِ ساده)** یک شاخص به نامِ `Williams %R` می‌گوید قیمت
> در کجای دامنهٔ ۱۴ کندلِ اخیر ایستاده. تقریباً همهٔ کتاب‌ها می‌گویند «وقتی این شاخص
> بالا رفت، بازار **اشباعِ خرید** است ⇒ **بفروش**». این لایه **دقیقاً برعکس** عمل
> می‌کند: وقتی شاخص از `−۱۳` عبور کرد — یعنی قیمت وارد **۱۳٪ بالاییِ** دامنه شد —
> این لایه **می‌خرد**. چون اندازه‌گیری روی ۱۵.۵ سال داده نشان داد در طلا، ورودِ
> قیمت به آن ناحیه نشانهٔ **ادامهٔ قدرت** است نه پایانِ آن.
>
> **چرا باید به این لایه اعتماد کرد؟ (اعداد، نه ادعا)**
> - **۸۶۹ معامله** در ۱۵.۵۳ سال (**~۵۶ معامله در سال**) ⇒ فراوان‌ترین لایهٔ پرتفوی.
> - **تنها لایهٔ پروژه که با صفر فیلتر هر ۱۱ آزمونِ سخت‌گیرانه را پاس کرد** (`rqs2 = ۸۳.۵`).
> - نرخِ برد `۴۸.۹۱٪` در برابرِ نقطهٔ سربه‌سرِ `۴۱.۰۷٪` (با اسپردِ واقعیِ حسابِ شما).
> - سودِ خالصِ بک‌تست **$۵۴٬۰۹۸** با بیشینه افتِ سرمایهٔ فقط **۵.۶٪**.
> - **آزمونِ سخت‌گیرانه‌ترین:** ۲۰۰۰ بار ترتیبِ معامله‌ها را تصادفی کردیم؛ شانسِ محض
>   **هرگز** به نرخِ بردِ این لایه نرسید (بهترین تلاشِ شانس: `۴۴.۵۹٪`).
> - **۳۰٪ آخرِ داده دست‌نخورده** نگه داشته شد و لایه روی آن **بهتر** بود
>   (`۴۹.۷۳٪` در برابرِ `۴۸.۹۱٪`) ⇒ نشانهٔ اینکه روی داده بیش‌برازش نشده.
>
> **⚠️ نکتهٔ مهمی که کارت به شما می‌گوید:** نوسانِ فعلیِ طلا **۴.۷ برابرِ** نوسانِ
> میانگینِ تاریخی است. لایه حد ضررش را از `ATR` می‌سازد، و در چنین وضعیتی حد ضرر
> به سقفِ مجاز بسته می‌شود که از نوسانِ فعلیِ بازار **تنگ‌تر** است. کارت این را
> **صریحاً** به شما می‌گوید (هم در جدولِ شاخص‌ها و هم در متنِ دلیلِ ورود) و
> پیشنهاد می‌کند **حجم را کوچک‌تر بگیرید** یا منتظرِ آرام‌شدنِ بازار بمانید.
> این هشدار پنهان نشده، چون براکتی که اکنون می‌بینید **عیناً همان براکتی نیست**
> که آن نمرهٔ ۸۳.۵ با آن به‌دست آمد.
>
> **بقیهٔ کارت‌ها دست‌نخورده‌اند.** فقط کارتِ `طلا / دلار — H4` تغییر کرد. هیچ لایهٔ
> دیگری حذف یا جابه‌جا نشد و منطقِ هیچ کارتِ دیگری عوض نشد.
>
> جزئیاتِ کامل: [`results/S382_WilliamsR_Xauusd_H4_rqs2-83.md`](../results/S382_WilliamsR_Xauusd_H4_rqs2-83.md)

> ## ⭐ تغییرِ پیشین — کارتِ **جدیدِ `EURUSD-H4`** + لایهٔ نوِ `S374`
>
> **چه چیزی اضافه شد؟** یک لایهٔ نو به نامِ **«دروازهٔ شکستِ Kennedy»** (`S374`) که
> معیارِ پروژه را پاس کرد، و یک **کارتِ کاملاً نو**: `یورو / دلار — H4 (چهارساعته)`.
> پیش‌تر هیچ کارتِ یوروی چهارساعته وجود نداشت، چون هیچ لایه‌ای روی آن افق تأیید نشده بود.
>
> **این لایه چه می‌کند؟ (به زبانِ ساده)** تقریباً همهٔ سیستم‌ها می‌گویند «اگر قیمتِ
> بسته‌شدن از خطِ کانال عبور کرد، شکست رخ داده». این لایه می‌گوید **این کافی نیست** —
> باید **کلِ دامنهٔ کندل** (سایه‌ها هم) آن‌سویِ خط باشد. یعنی کمی دیرتر وارد می‌شویم،
> ولی فقط وقتی شکست واقعاً قطعی شده است.
>
> **چرا این تغییرِ کوچک مهم بود؟** همین یک تغییر باعث شد یک استراتژیِ **سوخته** احیا شود:
> نسخهٔ قبلی (با تعریفِ `close`) در نیمهٔ اولِ تاریخ **زیان‌ده** و در نیمهٔ دوم سودده بود
> ⇒ بی‌ثبات و ردشده. با تعریفِ جدید **هر دو نیمه سودده** شدند. سودِ ناخالصِ طلا از
> **۱۱.۶۷** به **۳۲.۹۷** پیپ و یورو از **۱.۵۷** به **۶.۴۴** پیپ رسید (هزینهٔ رفت‌وبرگشتِ
> طلا ۳.۳ و یورو ۱.۶ پیپ) ⇒ هر دو ارز **مستقلاً** بالای هزینه.
>
> **برای شما چه تغییری می‌کند؟**
> - یک **کارتِ تازه** در فهرست می‌بینید: `یورو / دلار — H4`.
> - روی کارتِ `طلا / دلار — H4`، این لایه به دو لایهٔ قبلی **اضافه** شده؛ آن‌ها
>   **دست‌نخورده** کار می‌کنند و هیچ چیزی حذف نشده است.
> - حالتِ **«احتمالِ نزدیک شدن به سیگنال»** اینجا معنای دقیقی دارد: وقتی قیمت با
>   بسته‌شدن از خط عبور کرده ولی کلِ دامنه نه، کارت صریحاً می‌گوید چه تأییدی لازم است
>   و **چقدر فاصله** باقی مانده.
>
> ⚠️ **دو هشدارِ صادقانه که باید بدانید:**
> ۱. این لایه **کم‌بسامد** است — روی طلا حدوداً **۴ سیگنالِ مستقل در سال**. سیگنالش کم
>    می‌آید ولی وقتی می‌آید، قوی‌ترین لبهٔ اندازه‌گیری‌شدهٔ پروژه را دارد.
> ۲. رتبهٔ آن **محافظه‌کارانه** است: در سخت‌گیرانه‌ترین آزمونِ آماری (بوت‌استرپِ خوشه‌ای
>    روی کانال‌ها، نه معاملات)، لبه از صفر عبور می‌کند ولی **به‌سختی**. پس در حجم
>    **خویشتن‌دار** باشید.
>
> ⚠️ **فقط `H4`.** هر ۵ تایم‌فریمِ مشترک آزموده شد و در `M5`/`M15`/`M30`/`H1` این قاعده
> زیرِ هزینه یا حتی **زیان‌بار** بود. علتش زیباست: در تایم‌فریم‌های پایین، دامنهٔ کندل
> بیشتر **نویز** است تا اطلاعات، پس شرطِ «کلِ دامنه عبور کند» چیزی به ما نمی‌گوید.
>
> جزئیاتِ کامل: [`results/S374_KennedyBreakGate_XauEur_H4_rqs2-ACCEPTED.md`](../results/S374_KennedyBreakGate_XauEur_H4_rqs2-ACCEPTED.md)

> ✅ **روی گوشی نیازی به build یا `npm install` نیست.** فایلِ آمادهٔ `app.bundle.mjs`
> که کلِ منطقِ سایت + خودِ Hono را در خود دارد، از قبل در گیت است. شما فقط کلون/آپدیت
> می‌کنید و اجرا می‌کنید.

> ## ⚰️ تازه‌ترین تغییر — لایهٔ `S327` از **۵ کارت** حذف شد
> باندلِ این پوشه بازسازی شد تا لایهٔ «کلایمکسِ فروشِ Brooks» (`S327`) که با معیارِ
> جدیدِ `RQS2 v2.4` **روی هر ۷ کارت مرد**، دیگر به شما سیگنال ندهد.
>
> **چرا حذف شد؟** این لایه با `TP` کوچک‌تر از `SL` (نسبتِ ۰.۳۷) نرخِ بردِ **۸۸.۶٪**
> نشان می‌داد، اما وقتی همان لایه را با نسبتِ **قانونیِ** ۰.۵۲ سنجیدیم، نرخِ برد به
> **۷۵.۰٪** افتاد و شواهدِ آماری از `4.43σ` به `2.83σ` سقوط کرد. یعنی آن نرخِ بردِ
> چشمگیر، **هندسه** بود نه مهارت. هفت مسیرِ مستقلِ نجات آزموده شد و هیچ‌کدام جواب نداد.
>
> **برای شما چه تغییری می‌کند؟** روی `XAUUSD` کارت‌های `M5`/`M30`/`H1`/`H4` و روی
> `EURUSD` کارتِ `M30`، دیگر سیگنالی از این لایه نمی‌آید. بقیهٔ لایه‌های هر کارت
> **دست‌نخورده** کار می‌کنند و هیچ کارتی خالی نشده است.
>
> جزئیاتِ کامل: [`results/S327_SellClimaxReversalRejudged_XauEur_M5M15M30H1H4_rqs2-29.md`](../results/S327_SellClimaxReversalRejudged_XauEur_M5M15M30H1H4_rqs2-29.md)
>
> برای گرفتنِ این تغییر فقط کافی است `./update.sh` یا `git pull` بزنید.

> ## 🏆 ارتقای پیشین — کارتِ `XAUUSD-M5` حالا **دروازهٔ ساختار** دارد
> باندلِ این پوشه بازسازی شد تا نخستین لایهٔ پروژه که **هر ۱۱ دروازهٔ `RQS2`** را پاس
> کرده (`S355`, `RQS2 = 83.9`) واردِ مغزِ تصمیم شود.
>
> **برای شما چه تغییری می‌کند؟** روی کارتِ `XAUUSD-M5`، سیگنالِ خریدِ لایهٔ pullback
> دیگر مستقیم به «ورود به معامله» نمی‌رسد؛ اول باید **حالتِ ساختارِ خُردِ بازار** تأیید
> کند. اگر تأیید نکند، کارت در حالتِ **«احتمالِ نزدیک شدن به سیگنال»** می‌ماند و صریحاً
> می‌گوید منتظرِ چه تأییدی است. نتیجهٔ اندازه‌گیری‌شده: WR از **۶۵.۶٪ → ۷۲.۳٪** و
> `PF` از **2.85 → 3.95**، به قیمتِ حذفِ ~۲۶٪ از سیگنال‌ها.
>
> ⚠️ **فقط `XAUUSD-M5`.** سه کارتِ دیگرِ طلا (`M15`/`M30`/`H1`) این آزمون را پاس
> **نکردند**، پس عمداً دست‌نخورده ماندند — قانونِ پروژه اجازه نمی‌دهد منطقی که
> معیار را پاس نکرده به کارتِ کاربر برسد.
>
> برای گرفتنِ این ارتقا فقط کافی است `./update.sh` یا `git pull` بزنید.

---

## 🧩 در این پوشه چه چیزهایی هست؟

| فایل | کار |
|---|---|
| `server.mjs` | سرورِ سبک (فقط با ماژول‌های داخلیِ Node، بدونِ وابستگی). سایت را روی شبکهٔ محلی سرو می‌کند. |
| `app.bundle.mjs` | **باندلِ آمادهٔ کلِ منطقِ سایت** (همهٔ روترها/لایه‌ها + Hono). این را دستی ویرایش نکنید. |
| `cf-shim.mjs` | پلی که `serveStatic` کلادفلر را روی Node ممکن می‌کند (فقط هنگامِ ساختِ باندل لازم است). |
| `build.mjs` | ساختِ دوبارهٔ `app.bundle.mjs` (فقط روی کامپیوتر/سندباکس؛ روی گوشی لازم نیست). |
| `start.sh` | **راه‌اندازیِ سرور روی گوشی** (نصبِ Node + wake-lock + اجرا). |
| `update.sh` | **به‌روزرسانیِ دستی** بدونِ کلونِ دوباره. |
| `stop.sh` | توقفِ کاملِ سرور + آزادسازیِ wake-lock. |

---

## 🚀 مرحلهٔ ۱ — نصبِ ابزارها در Termux (فقط بارِ اول)

Termux را باز کنید و این‌ها را خط‌به‌خط بزنید (بعدِ هر خط Enter):

```bash
pkg update -y && pkg upgrade -y
pkg install -y git nodejs termux-api
```

- `git` : برای گرفتنِ پروژه.
- `nodejs` : موتورِ اجرای سرور.
- `termux-api` : برای wake-lock (تا با خاموش‌شدنِ صفحه، سرور قطع نشود).

> اگر هنگامِ نصب سؤالی پرسید، `y` بزنید و Enter کنید.

---

## 📥 مرحلهٔ ۲ — کلونِ پروژه (فقط بارِ اول)

```bash
cd ~
git clone https://github.com/golshandvr-del/Scientific-Research.git
```

حالا کلِ پروژه در پوشهٔ `~/Scientific-Research` است.

> اگر ریپو خصوصی بود و نامِ کاربری/رمز خواست، به‌جای رمز باید یک
> **GitHub Personal Access Token** بزنید (از GitHub → Settings → Developer settings →
> Personal access tokens بسازید).

---

## ▶️ مرحلهٔ ۳ — اجرای سایت

```bash
cd ~/Scientific-Research/local-mobile
bash start.sh
```

بعد از چند ثانیه چیزی شبیهِ این می‌بینید:

```
  روی همین گوشی      : http://localhost:8080
  در شبکهٔ محلی (LAN) — این آدرس را به دوستتان بدهید:
      http://192.168.1.42:8080
```

- **روی همین گوشی:** مرورگرِ گوشی را باز کنید و بروید به `http://localhost:8080`
- سرور تا وقتی `Ctrl+C` نزنید یا Termux را نبندید، **روشن می‌ماند**.

> `start.sh` به‌صورتِ خودکار wake-lock می‌گیرد؛ یعنی حتی با خاموش‌شدنِ صفحهٔ گوشی،
> سرور قطع نمی‌شود.

---

## 👥 مرحلهٔ ۴ — دیدنِ سایت توسطِ دوستتان (شبکهٔ محلی / LAN)

اگر شما و دوستتان به **یک مودم/وای‌فای مشترک** وصل باشید:

1. سرور را با `bash start.sh` اجرا کنید.
2. آدرسِ `http://192.168.x.x:8080` را که در خروجی چاپ می‌شود به دوستتان بدهید
   (شمارهٔ دقیق در گوشیِ شما ممکن است فرق کند — همان که زیرِ «در شبکهٔ محلی» چاپ شد).
3. دوستتان همان آدرس را در مرورگرِ گوشیِ خودش باز کند.

> **نکته:** سرور روی `0.0.0.0` گوش می‌دهد، پس روی کلِ شبکهٔ محلی در دسترس است.
> اگر دوستتان نتوانست وصل شود، معمولاً به‌خاطرِ **حالتِ AP/Client Isolation** مودم است
> (بعضی مودم‌ها ارتباطِ مستقیمِ دو گوشی را می‌بندند)؛ آن گزینه را در تنظیماتِ مودم خاموش کنید.

---

## 🌙 مرحلهٔ ۵ — اجرای دائم در پس‌زمینه

**راهِ ساده (توصیه‌شده):** کافی است `start.sh` را اجرا کنید و بگذارید Termux باز بماند.
با wake-lock، حتی اگر صفحه خاموش شود سرور زنده می‌ماند. برای اینکه اندروید Termux را
نکُشد، یک‌بار این را هم بزنید (اکتیو نگه‌داشتنِ سرویسِ Termux):

```bash
termux-wake-lock
```

و در تنظیماتِ اندروید، برای Termux **بهینه‌سازیِ باتری (Battery optimization)** را روی
«محدود نشود / Don't optimize» بگذارید تا سیستم آن را نبندد.

**راهِ کاملاً پس‌زمینه (اختیاری، پیشرفته‌تر):** اگر می‌خواهید حتی با بستنِ پنجرهٔ Termux
هم سرور بماند، از `nohup` استفاده کنید:

```bash
cd ~/Scientific-Research/local-mobile
termux-wake-lock
nohup bash start.sh > server.log 2>&1 &
```

- خروجی در فایلِ `server.log` ذخیره می‌شود (`cat server.log` برای دیدن).
- برای بستنِ کامل بعداً: `bash stop.sh`

---

## 🔄 مرحلهٔ ۶ — به‌روزرسانیِ دستی (دفعاتِ بعد)

هر وقت پروژه تغییر کرد و خواستید نسخهٔ جدید را بگیرید، **دیگر کلون نکنید**؛ فقط:

```bash
cd ~/Scientific-Research/local-mobile
bash update.sh      # فقط تغییراتِ جدید را می‌گیرد (git pull)
bash start.sh       # سرور را دوباره اجرا/ری‌استارت می‌کند
```

- `update.sh` هیچ‌چیز را خودکار به‌روز نمی‌کند؛ **کاملاً دستی** است، دقیقاً هر وقت خودتان بخواهید.
- چون باندلِ آمادهٔ سایت هم در گیت است، بعد از `update.sh` منطقِ جدیدِ سایت به‌طورِ خودکار می‌آید.

---

## ⏹️ توقفِ سرور

- اگر در همان پنجره اجرا کرده‌اید: کلیدهای **Ctrl + C**.
- اگر در پس‌زمینه (nohup) اجرا کرده‌اید:

```bash
cd ~/Scientific-Research/local-mobile
bash stop.sh
```

---

## ❓ رفعِ اشکالِ سریع

| مشکل | راه‌حل |
|---|---|
| `node: command not found` | `pkg install -y nodejs` |
| `git: command not found` | `pkg install -y git` |
| `EADDRINUSE ... 0.0.0.0:8080` (پورت اشغال است) | دیگر رخ نمی‌دهد: سرور خودکار پورتِ بعدی (8081، 8082، ...) را می‌گیرد و بالا می‌آید. فقط به خطِ «روی همین گوشی: http://localhost:PORT» نگاه کنید تا پورتِ واقعی را ببینید. اگر خواستید همه را ببندید: `bash stop.sh` |
| `fuser: Bad system call` | بی‌خطر است و دیگر رخ نمی‌دهد؛ اسکریپت‌ها به‌جای `fuser` از `pkill` استفاده می‌کنند. اگر هنوز دیدید، یک‌بار `bash update.sh` بزنید تا اسکریپت‌ها به‌روز شوند |
| دوستم وصل نمی‌شود | مطمئن شوید هر دو روی یک وای‌فای هستید + Client Isolation مودم خاموش باشد |
| قیمت‌ها نمی‌آید | اینترنتِ گوشی روشن باشد (قیمتِ زنده از Yahoo گرفته می‌شود) |
| با خاموشیِ صفحه قطع می‌شود | `pkg install termux-api` و اجرای `termux-wake-lock`؛ و خاموش‌کردنِ بهینه‌سازیِ باتری برای Termux |
| صفحه بعد از چند دقیقه هنگ می‌کرد/از دسترس خارج می‌شد | ✅ رفع شد (نشتیِ حافظهٔ فرانت‌اند). فقط `bash update.sh` بزنید؛ چون این رفع در فایل‌های `public/static` است، **نیازی به ساختِ دوبارهٔ باندل نیست** |

---

## 🔧 برای توسعه‌دهنده: ساختِ دوبارهٔ باندل (فقط روی کامپیوتر/سندباکس)

اگر کدِ `web_tool/src/` را تغییر دادید، باندلِ آماده را دوباره بسازید (نه روی گوشی):

```bash
cd web_tool && npm install          # فقط یک‌بار، برای داشتنِ esbuild و hono
cd ../local-mobile && node build.mjs # app.bundle.mjs را می‌سازد
git add app.bundle.mjs && git commit -m "rebuild bundle" && git push
```

سپس روی گوشی فقط `bash update.sh` می‌زنید و نسخهٔ جدید می‌آید.

---

## ⚡ سرعتِ بارگذاری روی گوشی (مهم)

قبلاً روی گوشی، صفحه گاهی تا **۲ دقیقه** خالی می‌ماند (پس‌زمینه می‌آمد ولی کارت‌ها
ظاهر نمی‌شدند). علت این بود که مرورگر تا کاملِ **همهٔ کارت‌ها** هیچ کارتی نشان
نمی‌داد و هر کارت مستقیم از Yahoo داده می‌گرفت (بدونِ کش، چون کشِ `cf:` فقط روی
Cloudflare کار می‌کند نه روی Node/Termux). با اسکلتِ فوری از `/api/assets` (۷ کارت)
و پنج بهبودِ زیر این حل شد:

| # | بهبود | اثر |
|---|-------|-----|
| ۱ | **کشِ حافظه‌ایِ مستقل از محیط** (`cache.ts`: TTL + SWR + single-flight) | داده‌های تکراری فقط یک‌بار از شبکه گرفته می‌شوند |
| ۲ | **de-dup درخواست‌ها** | چند کارتِ طلا که یک تایم‌فریم می‌خواهند، **یک** fetchِ مشترک دارند |
| ۳ | **timeout روی هر fetch (۶ ثانیه)** | یک منبعِ کند دیگر کلِ صفحه را هنگ نمی‌کند |
| ۴ | **رندرِ تدریجی + کارت‌های مخفی بدونِ fetch** | کارت‌های سریع فوراً می‌آیند؛ هر کارتی که کاربر مخفی کند، یک دسته درخواستِ کمتر |
| ۵ | **پیش‌گرم‌سازی (pre-warm) در استارتِ سرور** | همان اولین کاربر هم کارت‌ها را تقریباً فوری می‌بیند |

**نتیجهٔ اندازه‌گیری‌شده روی همین باندل:**
- فهرستِ کارت‌ها (`/api/assets`) ≈ **۱۵ms** (فوری، بدونِ هیچ درخواستِ شبکه)
- هر کارت با کشِ گرم ≈ **۰.۱ تا ۰.۴ ثانیه** (قبلاً چند ثانیه، با ریسکِ تا ۲ دقیقه)

> نکته: منطقِ تصمیم‌گیریِ استراتژی‌ها **اصلاً تغییر نکرد**؛ فقط لایهٔ داده‌گیری
> سریع‌تر شد. خروجیِ هر کارت دقیقاً همان تصمیمِ قبلی است.

**اگر گوشی‌ات قوی است (رمِ زیاد):** می‌توانی همهٔ کارت‌ها را نمایان بگذاری؛ کش
فشارِ شبکه را مدیریت می‌کند. اگر شبکه‌ات ضعیف است، کارت‌های غیرِضروری را مخفی کن تا
حتی سریع‌تر شود.

---

## 🧠 معماریِ ماژولارِ گره‌محور (نو — طبقِ `webplan.md`)

سایت در حالِ بازچیده‌شدن به یک معماریِ **ROS2-مانند** است: هر مسئولیت یک **گرهِ مستقل**
با **قراردادِ پیامِ نسخه‌دار** (versioned contract). این باندل همهٔ گره‌های زیر را در خود دارد
و روی گوشی هم دقیقاً مثلِ سایت فعال‌اند:

| گره | فایل | کار | حالت |
|---|---|---|---|
| 🚌 **EventBus** | `bus/` | pub/sub رویدادها (`bar.closed@…`) — پلِ بینِ replay و live | فعال |
| 💹 **Price Feed** | `price/` | `PriceFeed@v1` + `HistoryStore` (Disk/Memory) + Heartbeatِ کهنگیِ داده | فعال |
| 📊 **Indicators** | `indicators/` | `IndicatorSnapshot@v1` + رجیستریِ کش‌دار (۱۴ اندیکاتور، شاملِ Alligator/GMMA/Ichimoku) | فعال |
| 🛰️ **Regime Radar** | `regime/` | تشخیصِ رژیمِ بازار (روند/رنج/نوسان) با آستانه‌های **خودکالیبرِ صدکیِ per-TF** | **سایه‌ای** |
| ⚙️ **Runtime** | `runtime/` | مرزِ رسمیِ اجرای لایه‌ها (`CardDecision@v1`) | فعال |
| 🏛️ **Layer Council** | `council/` | رأی‌گیریِ اجماعیِ لایه‌ها (اجماع/اکثریت/تضاد) | **سایه‌ای** |
| 📒 **Live RQS Ledger** | `ledger/` | ثبتِ نتیجهٔ واقعیِ معاملهٔ کاربر ⇒ RQS+ **زنده** + بایگانیِ خودکارِ لایهٔ افت‌کرده (`/api/ledger/*`) | فعال |
| 🔬 **Indicator Scanner** | `scanner/` | همبستگیِ Spearman هر اندیکاتور با حرکتِ بعدیِ قیمت + p-value ⇒ کاندیدِ فیلترِ احیا (`/api/scanner/*`) | **پژوهشی** |
| 🟧 **UI Badges** | `public/static/ui/` | نوارِ heartbeat (دادهٔ کهنه) + نشانِ شورا | فعال |

> **«حالتِ سایه‌ای» یعنی چه؟** گره‌های رژیم و شورا هم‌اکنون فقط **گزارش** می‌دهند و در پاسخِ
> `/api/decision` به‌صورتِ فیلدهای `regime` و `council` دیده می‌شوند — اما **هیچ تصمیمی را
> تغییر نمی‌دهند**. تصمیمِ نهاییِ هر کارت هنوز دقیقاً همان `runCard`ِ رجیستری است. این کار
> با یک **هارنسِ تستِ برابری** تضمین شده که خروجیِ تصمیم بیت‌به‌بیت ثابت مانده است
> (parity hash = `bf0615639cd1e59d`؛ روشِ ایمنِ Strangler-Fig: افزودن بدونِ شکستن).
>
> **«حالتِ پژوهشی» (Scanner) یعنی چه؟** کاوشگرِ اندیکاتور فقط از راهِ `/api/scanner/:asset`
> فراخوانده می‌شود و یک **گزارشِ آماری** (`ScanReport@v1`) برای AI/تحقیق برمی‌گرداند: کدام
> اندیکاتور با حرکتِ بعدیِ قیمت همبستگیِ **معنادار** (p<0.05) دارد ⇒ کاندیدِ فیلترِ احیا.
> این خروجی هرگز روی صفحه یا تصمیمِ کاربر اثر نمی‌گذارد؛ ابزارِ فازِ کشف/احیاست و مستقیماً
> علیهِ «اشتباهِ رایجِ #۳» (غفلت از اندیکاتورهای پیچیده) طراحی شده.

---

_این راهکار طوری طراحی شده که **منطقِ کاملِ سایت** روی گوشی اجرا شود، در شبکهٔ محلی به
اشتراک گذاشته شود، در پس‌زمینه پایدار بماند و **دستی** (نه خودکار) به‌روز شود._
