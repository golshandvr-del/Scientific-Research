# 🐞 گزارشِ باگ‌ها (Bug Report) — دفترچهٔ زندهٔ ممیزی

> این فایل طبقِ **User Note** ساخته شده: تمرکز بر **پیدا کردنِ باگ‌ها** (حتی ریزترین‌ها و
> هر چیزِ غیرمنطقی) در **سایت (`web_tool`)**، **`local-mobile`**، و **استراتژی‌ها/انجین**.
>
> **روشِ کار (شبیه دفترچه یادداشت):** به‌محضِ پیدا شدنِ هر باگ، همان‌جا نوشته می‌شود و بلافاصله
> commit + push می‌شود؛ سپس سراغِ باگِ بعدی. تا یک باگ ثبت نشود، به‌سراغِ بعدی نمی‌رویم.

## راهنمای شدت
- 🔴 **CRITICAL** — منطقِ تصمیم/معامله را اشتباه می‌کند یا کرش/داده‌ی غلط به کاربر می‌دهد.
- 🟠 **HIGH** — رفتارِ نادرست در شرایطِ خاص؛ می‌تواند سیگنال/عدد را غلط کند.
- 🟡 **MEDIUM** — ناسازگاری/نقصِ منطقی که معمولاً بی‌خطر است اما باید اصلاح شود.
- 🔵 **LOW** — جزئی/کیفیتِ کد/پیامِ گمراه‌کننده.

## وضعیت
- 🆕 ثبت‌شده (هنوز رفع نشده)
- ✅ رفع‌شده
- ⏭️ نیازمند تصمیم/بررسیِ بیشتر

---

## فهرستِ باگ‌ها

### BUG-001 — 🟠 HIGH — ناسازگاریِ معیارِ «شکستِ روند» در مدیریتِ خروجِ لایهٔ S214 🆕
**فایل‌ها:** `web_tool/src/gold_m5_router.ts` (تابع `manageGoldM5Scalp`, خطوط ~339-344) +
`web_tool/src/gold_m5_late_entry.ts` (خطوط 33-34).

**شرح:** موتورِ مدیریتِ لحظه‌ایِ اسکالپ (`manageGoldM5Scalp`) هنگامِ تشخیصِ «شکستِ روند»
(برای خروجِ `wrong`) از `EMA_FAST=20` و `EMA_SLOW=100` استفاده می‌کند (خطوط ۳۹–۴۰ همان فایل).
اما وقتی سیگنالِ ورود از **لایهٔ S214 (Late-Entry)** آمده باشد، آن لایه رژیمِ روند را با
`EMA20/EMA50` تعریف می‌کند (`gold_m5_late_entry.ts` خطوط ۳۳–۳۴). در نتیجه معاملهٔ S214 با
TP/SL مخصوصِ خودش (۳۰۰/۱۵۰) مدیریت می‌شود — که درست است — **اما** شرطِ «شکستِ روند» با
EMA100 (متعلق به لایهٔ اسکالپِ پیش‌فرض S79) سنجیده می‌شود، نه EMA50 که خودِ S214 برایش تعریف
شده. این یعنی خروجِ زودهنگام/دیرهنگامِ ناسازگار با منطقِ ورود.

**چرا مهم است:** طبقِ اصلِ «هر لایه TP/SL و منطقِ مدیریتِ مخصوصِ خود را دارد» (User Note،
اشتباهِ رایج «TP/SL یکسان برای همه»)، معیارِ شکستِ روند هم باید مخصوصِ لایه باشد. الان
`manageGoldM5Scalp` هیچ پارامتری برای دورهٔ EMA نمی‌گیرد ⇒ همیشه EMA100.

**پیشنهادِ رفع:** افزودنِ پارامترهای اختیاریِ `emaFast`/`emaSlow` به `ScalpManageInput` و
پاس‌دادنِ (۲۰،۵۰) هنگام مدیریتِ معاملهٔ S214 (فرانت‌اند باید بداند سیگنال از کدام لایه آمده).

**وضعیت:** 🆕 ثبت‌شده (بررسیِ بیشتر برای مسیرِ پاس‌دادنِ منشأ لایه لازم است).

---

### BUG-002 — 🟡 MEDIUM — اسپردِ تخمینیِ طلا ۰.۳۵$ به‌جای ۰.۳۳$ (مشخصاتِ حساب) 🆕
**فایل:** `web_tool/src/trade_manager.ts` خط ۲۰۲.

**شرح:** `const spreadEst = price >= 100 ? 0.35 : ...`. اما مشخصاتِ رسمیِ حسابِ دمو (در
README و پرامپت) اسپردِ واقعیِ طلا را **۰.۳۳$/oz** تعریف می‌کند. اینجا ۰.۳۵ استفاده شده.
این عدد در محاسبهٔ `neutralBand` (ناحیهٔ خنثیِ نویز/اسپرد) به‌کار می‌رود ⇒ ناحیهٔ خنثی کمی
بزرگ‌تر از واقعیت می‌شود. اثرش کوچک است اما ناسازگار با مشخصاتِ دقیقِ حساب.

**پیشنهادِ رفع:** ۰.۳۵ ⇒ ۰.۳۳ (هم‌راستا با `engine/market_spec.py`).

**وضعیت:** 🆕 ثبت‌شده.

---

### BUG-003 — 🟠 HIGH — فیلدِ `pnlUsd` واحدِ قیمت است نه دلار (CONTRACT_SIZE نادیده) 🆕
**فایل:** `web_tool/src/trade_manager.ts` خطوط ۱۸۳، ۵۵–۵۶ (تعریفِ `TradeStatus`).

**شرح:** فیلد `pnlUsd` (نامش «سود/زیان به دلار») این‌گونه محاسبه می‌شود:
`const pnlUsd = round2(rawMove)` که `rawMove` صرفاً **اختلافِ قیمت** (price − entry) است،
نه سودِ دلاری. طبقِ مشخصاتِ حساب `CONTRACT_SIZE=100` (هر ۱.۰۰$ حرکت = ۱۰۰$/لات). پس
سودِ دلاریِ واقعیِ ۱ لات = `rawMove × 100`. الان اگر قیمتِ طلا ۲۰ دلار حرکت کند،
`pnlUsd=20` نمایش داده می‌شود در حالی که سودِ ۱ لات = ۲۰۰۰$ است. کامنتِ خطِ ۵۵ هم مبهم
است («به ازای ۱ لات پایه … نسبت به فاصله»). این فیلد یا باید به `priceMove` تغییرِ نام
دهد یا در CONTRACT_SIZE ضرب شود تا با واقعیتِ حساب هم‌خوان باشد.

**نکته:** UI کنونی شاید این فیلد را نمایش ندهد (باید در `app.js` بررسی شود)، اما هر
مصرف‌کننده‌ای که فرض کند این «دلار» است، عددِ ۱۰۰ برابر غلط می‌گیرد.

**وضعیت:** 🆕 ثبت‌شده (بررسیِ مصرفِ فیلد در فرانت‌اند لازم است).

---

### BUG-004 — 🔴 CRITICAL — ناسازگاریِ تایم‌فریم: لایهٔ Monday (S140⁺⁺، «M5») روی کندلِ M15 اجرا می‌شود 🆕
**فایل‌ها:** `web_tool/src/router.ts` (لایهٔ Monday، خطوط ۴۵۶–۵۷۸) + `web_tool/src/index.tsx`
(خطوط ۵۷۴–۵۷۶، فراخوانیِ `decide` برای کارتِ `XAUUSD`) + `web_tool/src/monday_drift.ts`
(خطوط ۳۱–۳۳).

**شرح:** طبقِ README و کامنت‌های `monday_drift.ts`، لایهٔ **S140⁺⁺** در نشستِ S190–S195 از
M15 به **M5** ارتقا یافت و پارامترهایش مخصوصِ M5 کالیبره شدند:
`MONDAY_SL_PIP=100`, `MONDAY_TP_PIP=200`, `MONDAY_MAX_HOLD=288` (کامنتِ خطِ ۳۳: «۲۴ ساعت
روی M5 = ۲۸۸×۵ دقیقه»).

**اما** این لایه داخلِ تابعِ `decide()` قرار دارد و `decide()` در `index.tsx` **فقط برای
کارتِ `XAUUSD` (تایم‌فریم M15)** صدا زده می‌شود (خط ۵۷۶). کارتِ `XAUUSD-M5` تابعِ جداگانهٔ
`decideGoldM5` را صدا می‌زند که Monday را **ندارد**. نتیجه:

1. سیگنالِ Monday عملاً روی دادهٔ **M15** تولید می‌شود، نه M5 — برخلافِ استراتژیِ اثبات‌شده.
2. `MONDAY_MAX_HOLD=288` که در `manage.maxHoldBars` می‌رود، روی کارتِ M15 معادلِ
   288×15دقیقه = **۷۲ ساعت** است، نه ۲۴ ساعت که طراحی شده (چون `barsHeld` کاربر بر مبنای
   کندلِ همان کارت شمرده می‌شود).
3. TP/SL کالیبرهٔ M5 (100/200 pip) روی نوسانِ بزرگ‌ترِ M15 اعمال می‌شود ⇒ رفتارِ متفاوت از
   بک‌تست.

**چرا CRITICAL:** این یعنی لایه‌ای که در سند به‌عنوان «تأییدشده و افزایشیِ +$1,553 روی M5»
ثبت شده، در محصولِ زنده روی تایم‌فریمِ اشتباه اجرا می‌شود ⇒ رفتارِ واقعیِ سیگنال با
بک‌تست هم‌خوان نیست. (همین ایراد احتمالاً برای S139/S141 نیز صادق است چون آن‌ها هم داخلِ
`decide` روی کارتِ M15 هستند در حالی که برخی pip/max-hold مخصوصِ تایم‌فریمِ دیگرند —
نیازمندِ بررسیِ جداگانه.)

**پیشنهادِ رفع:** یا لایهٔ Monday به مسیرِ `decideGoldM5` منتقل شود (اجرای واقعی روی M5)،
یا اگر عمداً روی M15 می‌ماند، `MONDAY_MAX_HOLD` و TP/SL برای M15 بازتعریف شوند و سند
اصلاح شود. نیاز به تصمیمِ معماری دارد.

**وضعیت:** ⏭️ ثبت‌شده — نیازمندِ تصمیمِ معماری/بررسیِ S139/S141 مشابه.

---

### BUG-005 — 🟠 HIGH — سیگنالِ ساعت-محورِ S73 روی کندلِ M5 چند بار در ساعت تکرار می‌شود 🆕
**فایل:** `web_tool/src/eurusd_router.ts` (`decideEurusd`, خطوط ۳۱۸، ۴۵) + `index.tsx`
(کارتِ `EURUSD` با `tf='5m'`, خطوط ۵۰۳، ۶۲۸–۶۳۱).

**شرح:** کارتِ اصلیِ EURUSD در S187–S189 از M15 به **M5** ارتقا یافت (`tf='5m'`). اما شرطِ
ورودِ S73 هنوز ساعت-محور است: `if (nowUtcHour === ENTRY_HOUR_UTC && pb.met)` که
`ENTRY_HOUR_UTC=0`. روی دادهٔ M5، ساعتِ ۰ UTC شاملِ **۱۲ کندلِ M5** است (۰۰:۰۰، ۰۰:۰۵،
… ۰۰:۵۵). یعنی تا وقتی `pb.met` (pullbackِ ۴ کندلِ اخیر نزولی) برقرار بماند، سیگنالِ ENTRY
برای **هر کدام از این ۱۲ کندل** صادر می‌شود — نه یک‌بار در ابتدای سشن.

در بک‌تستِ S73 روی M5 (که +$8,911 داد) ورود معمولاً «open اولین کندلِ ساعتِ ۰» است، نه هر
کندلِ داخلِ ساعت. اگر منطقِ زنده هر کندلِ ساعتِ ۰ را سیگنال بگیرد، رفتارِ سیگنال با بک‌تست
هم‌خوان نیست و کاربر ممکن است چند سیگنالِ متوالی در یک ساعت ببیند.

**تبصره:** `signal_latch`/فرانت‌اند شاید سیگنال را قفل کند (باید بررسی شود)، اما منطقِ سرور
ذاتاً باید فقط **اولین کندلِ ساعت** را ماشه کند (مثلاً با چک‌کردنِ اینکه کندلِ قبلی ساعتِ
متفاوتی داشته: `prevHour !== ENTRY_HOUR_UTC`).

**وضعیت:** 🆕 ثبت‌شده — نیازمندِ افزودنِ گیتِ «اولین کندلِ ساعت».

---

### BUG-006 — 🟡 MEDIUM — S164 روی کندلِ M5 هم چند بار در ساعتِ ۱۳ تکرار می‌شود 🆕
**فایل:** `web_tool/src/eurusd_router.ts` (`decideEurusd`, خطوط ۲۵۴، ۳۸).

**شرح:** مشابهِ BUG-005: شرطِ ورودِ S164 `s164Day && nowUtcHour === S164_ENTRY_HOUR_UTC`
(۱۳ UTC) است. روی کارتِ EURUSD که اکنون M5 است، ساعتِ ۱۳ شاملِ ۱۲ کندلِ M5 است ⇒ سیگنالِ
SHORT برای هر کندلِ ساعتِ ۱۳ در آن روزِ خاص تکرار می‌شود. بک‌تستِ S164 روی M15 کالیبره شده
(`S164_MAX_HOLD_BARS=12` = «۳ ساعت» با کندلِ M15). روی M5 این ۱۲ کندل = ۱ ساعت، نه ۳ ساعت
⇒ هم تکرارِ سیگنال و هم ناسازگاریِ maxHold مثلِ BUG-004.

**وضعیت:** 🆕 ثبت‌شده — همان ریشهٔ BUG-004/005 (لایهٔ ساعت/M15-کالیبره روی کندلِ M5).

---

### BUG-007 — 🟠 HIGH — VWAP روی دادهٔ Yahoo GC=F با volumeِ ناقص/صفر بی‌اعتبار می‌شود 🆕
**فایل:** `web_tool/src/signal.ts` (`anchoredVWAP`, خطوط ۷۱–۸۳) + `index.tsx` (کندلِ زنده با
`volume: 0`).

**شرح:** `anchoredVWAP` وزن‌دهیِ قیمت را با `volume` انجام می‌دهد و در نبودِ حجم به `close`
سقوط می‌کند (`out[i] = cumV > 0 ? cumPV / cumV : c[i].close`). دو مشکل:
1. دادهٔ Yahoo `GC=F` اغلب volume را برای کندل‌های داخل‌روزیِ M15 ناقص یا صفر می‌دهد؛ در آن
   صورت VWAP عملاً برابرِ close می‌شود ⇒ فیلترِ `vwap_dist_atr` (وزنِ ۰.۳۵ در امتیازِ سیگنال)
   بی‌اثر/غلط می‌شود.
2. کندلِ زندهٔ در حالِ شکل‌گیری در `rebaseFuturesToSpot`/`mergeLiveQuote` با `volume: 0`
   ساخته می‌شود؛ حتی اگر بقیهٔ کندل‌ها volume داشته باشند، کندلِ جاری در جمعِ VWAP سهمِ صفر
   دارد (هرچند این به closedBars حذف می‌شود، در `/api/analysis` که closedBars ندارد باقی است).

**اثر:** `a.probability` که مستقیماً گیتِ ورودِ S67 (خط ۱۱۲۸ router: `p >= P_MIN`) است، از
یک VWAPِ احتمالاً بی‌اعتبار تغذیه می‌شود ⇒ احتمالِ سیگنالِ نادرست.

**پیشنهادِ رفع:** اگر مجموعِ volume روز ~۰ بود، به VWAPِ بدونِ وزن (میانگینِ typical-price)
یا EMA سقوط کن و در UI شفاف اعلام کن که VWAP در دسترس نیست.

**وضعیت:** 🆕 ثبت‌شده.

---

### BUG-008 — 🟡 MEDIUM — قفلِ سیگنال (latch) می‌تواند سیگنالِ لایهٔ ساعت-محورِ منقضی را ۳ رفرش زنده نگه دارد 🆕
**فایل:** `web_tool/public/static/signal_latch.js` (خطوط ۵۶–۷۳، `NEUTRAL_TOLERANCE=3`).

**شرح:** مکانیزمِ ضدِ flicker وقتی سرور از ENTRY به NEUTRAL می‌رود، تا `NEUTRAL_TOLERANCE=3`
نمونهٔ متوالی سیگنالِ ENTRY را در حالتِ `_fading` زنده نگه می‌دارد. برای لایه‌های
**رژیم-محور** این منطقی است (نوسانِ گذرا). اما برای لایه‌های **ساعت-محور** (S73/S139/S140/
S141/S164) که پنجرهٔ ورودشان دقیقاً با گذرِ ساعت **بسته** می‌شود، این یعنی سایت ممکن است تا
۳ رفرش پس از بسته‌شدنِ پنجره هنوز «BUY پایدار» نشان دهد — یعنی سیگنالِ منقضی. با BUG-005/006
(تکرارِ سیگنال روی M5) تعامل می‌کند.

**پیشنهادِ رفع:** برای تصمیم‌هایی که `timeGate.windowOpen===false` شده، latch باید فوراً
باطل شود (نه صبر تا ۳ نمونه).

**وضعیت:** 🆕 ثبت‌شده.

---

### BUG-009 — 🔴 CRITICAL — ناسازگاریِ RSI بین انجینِ لایو (TS) و بک‌تستِ پایتون: در نبودِ ضرر، RSI=100 به‌جای NaN 🆕
**فایل‌ها:** `web_tool/src/indicators.ts` (تابع `rsi`, خطوط ۷۴–۸۷) در برابر
`engine/indicators.py` (تابع `rsi`, خطوط ۱۷–۲۴).

**شرح:** فرمولِ RSI در دو سمتِ پروژه رفتارِ متفاوت در حالتِ لبه (edge case) دارد:

- **پایتون (مرجعِ بک‌تست):**
  ```python
  rs = avg_gain / avg_loss.replace(0, np.nan)   # وقتی avg_loss=0 ⇒ rs=NaN
  return 100 - (100 / (1 + rs))                 # ⇒ 100 - NaN = NaN
  ```
  یعنی وقتی در پنجرهٔ اخیر **هیچ کندلِ نزولی‌ای** نبوده (avg_loss=0)، RSI برابرِ **NaN**
  می‌شود (و در شرط‌های `rsi > X` نادیده/False می‌شود).

- **TypeScript (انجینِ لایو):** خط ۸۳–۸۴:
  ```ts
  const rs = al[i] === 0 ? NaN : ag[i] / al[i]
  out[i] = isNaN(rs) ? 100 : 100 - 100 / (1 + rs)
  ```
  یعنی همان حالت (avg_loss=0) به **RSI=100** ترجمه می‌شود، نه NaN.

**چرا CRITICAL:** این دقیقاً همان حالتی است که در **روندِ صعودیِ قوی/بدونِ پولبک** رخ می‌دهد
(چند کندلِ پیاپیِ صعودی). در آن لحظه:
- بک‌تستِ پایتون: RSI=NaN ⇒ شرطِ اشباعِ خرید `rsi >= 70` **برقرار نمی‌شود** (چون NaN>=70 غلط است).
- لایوِ TS: RSI=100 ⇒ شرطِ `rsi >= 70` **برقرار می‌شود** (مثلاً `confirmation_filter.ts:94`
  که بازهٔ مجازِ RSI را `35..70` می‌گیرد ⇒ در RSI=100 فیلتر **رد** می‌کند؛ اما در پایتون
  همان کندل NaN بوده و رفتارِ فیلتر متفاوت می‌شد).

یعنی سیگنال/فیلترِ زنده در نقاطِ حساسِ روندِ خالص، با نتیجهٔ بک‌تست هم‌خوان نیست — نمونه‌ای
دیگر از واگراییِ لایو-از-بک‌تست (هم‌خانوادهٔ BUG-004/005). حالتِ دوم (هم gain و هم loss صفر،
بازارِ کاملاً flat) نیز در پایتون NaN و در TS مقدارِ ۱۰۰ می‌دهد — که حتی از نظرِ نظری هم غلط
است (بازارِ بی‌حرکت باید RSI≈۵۰ یا نامشخص باشد، نه ۱۰۰).

**پیشنهادِ رفع:** خطِ ۸۴ به‌جای بازگرداندنِ ۱۰۰، همان رفتارِ پایتون را بازتولید کند: در
`al[i]===0` مقدار را NaN بگذارد (و مصرف‌کننده‌ها با `Number.isFinite` مدیریت کنند)، یا اگر
عمداً ۱۰۰ می‌خواهیم، بک‌تستِ پایتون هم باید همین‌طور شود تا هم‌راستا بمانند. تصمیمِ نهایی باید
مرجعِ واحد داشته باشد.

**وضعیت:** 🆕 ثبت‌شده.

---

### BUG-010 — 🟠 HIGH — کامنتِ گمراه‌کننده + ناسازگاریِ محاسبهٔ ATR در فیلترِ تأیید (SMA به‌جای EWM/Wilder) 🆕
**فایل:** `web_tool/src/confirmation_filter.ts` (تابع `atrRaw`, خطوط ۴۰–۵۹، مصرف در ۷۳–۷۴، ۸۷–۸۹).

**شرح:** تابعِ `atrRaw` داخلِ فیلترِ تأیید، ATR را با **میانگینِ متحرکِ سادهٔ (SMA)** روی
TrueRange حساب می‌کند (خطوط ۵۱–۵۷: پنجرهٔ لغزانِ ساده). کامنتِ خطِ ۵۰ صریحاً می‌گوید:
«میانگینِ متحرکِ ساده روی TR (**سازگار با ind.atr پروژه که SMA(TR) است**)». اما این ادعا
**غلط** است: تابعِ رسمیِ `ind.atr` در `indicators.ts` (خطوط ۱۰۰–۱۰۲) از
`ewmAlpha(trueRange(c), 1/period)` استفاده می‌کند — یعنی **EWM با آلفای Wilder (1/period)**،
نه SMA. همچنین معادلِ پایتونِ آن (`engine/indicators.py` خط ۳۵) نیز `tr.ewm(alpha=1/period)`
است.

**چرا مهم است:** شرطِ سومِ فیلتر (`ATR14 > ATR100`، رژیمِ نوسانِ فعال) که یکی از ۵ مؤلفهٔ
امتیازِ تأیید است، با یک ATRِ SMA-محور سنجیده می‌شود که مقدارش با ATRِ EWM-محورِ بقیهٔ پروژه
(و بک‌تست) **متفاوت** است. SMA به‌ویژه در ATR100 نسبت به EWM واکنشِ بسیار متفاوتی به
شوک‌های نوسان دارد ⇒ گیتِ score ممکن است در برخی کندل‌ها متفاوت از آن‌چه در بک‌تستِ فیلتر
کالیبره شده باز/بسته شود. این مستقیماً روی تصمیمِ ENTRY لایه‌های Monday/Turn-of-Month اثر
می‌گذارد (همان‌هایی که این فیلتر برای بالا بردنِ WRشان اضافه شد).

**پیشنهادِ رفع:** یا `atrRaw` را به EWM(1/period) تبدیل کن تا با `ind.atr` و پایتون یکی شود،
یا اگر عمداً SMA است، کامنت اصلاح شود و در سندِ کالیبراسیونِ فیلتر ثبت گردد که آستانهٔ score
با ATRِ SMA کالیبره شده (که با بک‌تستِ EWM هم‌خوان نیست). ترجیحِ فنی: EWM برای هم‌راستایی
با بک‌تست.

**وضعیت:** 🆕 ثبت‌شده.

---

### BUG-011 — 🟠 HIGH — لایهٔ «TP/SL ثابت» با heuristicِ روندِ معکوس زودهنگام بسته می‌شود (ترتیبِ شرط‌ها) 🆕
**فایل:** `web_tool/src/trade_manager.ts` (بلوکِ `overallAction`, خطوط ۴۲۲–۴۵۰؛ به‌ویژه ۴۲۹ در برابر ۴۳۱).

**شرح:** در جمع‌بندیِ `overallAction`، ترتیبِ `else if`ها این است:
```
... reachedTp / reachedSl / closeForMaxHold ...
else if (trendAgainst && inRealLoss)      → overallAction='close'   // خط ۴۲۹
else if (managePlan?.style==='fixed-tp-sl') → overallAction='hold'  // خط ۴۳۱
```
چون شرطِ `trendAgainst && inRealLoss` **قبل** از شرطِ `fixed-tp-sl` بررسی می‌شود، معامله‌ای که
از یک لایه با سبکِ **TP/SL ثابت** باز شده، به‌محضِ اینکه روندِ لحظه‌ایِ سایت معکوس شود و کمی در
ضرر برود (`inRealLoss`)، پیشنهادِ کلیِ **«ببند» (close)** می‌گیرد — دقیقاً برخلافِ فلسفهٔ
همان لایه که خطِ ۴۳۱ توضیح می‌دهد: «TP/SL ثابت دارد؛ به همان پلنِ اولیه پایبند بمان و جابه‌جا
نکن».

**چرا مهم است:** لایه‌های `fixed-tp-sl` (طبقِ اصلِ پروژه) عمداً باید **فقط** با برخوردِ
TP/SL خودشان یا maxHold بسته شوند، نه با سیگنالِ روندِ heuristic که به هیچ‌یک از پارامترهای
بک‌تستِ آن لایه مربوط نیست. این یعنی کاربر ممکن است معامله‌ای که در بک‌تست تا TP دویده را
زودهنگام ببندد ⇒ سودِ خالصِ واقعی < بک‌تست. این هم‌خانوادهٔ «اشتباهِ رایج: TP/SL یکسان/مدیریتِ
غیرِ لایه-محور» است.

**پیشنهادِ رفع:** شرطِ `managePlan?.style==='fixed-tp-sl'` باید **قبل** از
`trendAgainst && inRealLoss` بیاید (یا داخلِ آن شرط، حالتِ fixed مستثنا شود)، تا مدیریتِ
لایه‌ای بر heuristicِ روند اولویت داشته باشد — همان‌طور که در بخشِ `layerAwareAdvices`
(خطوط ۲۷۱–۲۷۷) برای باکس‌های جزئی رعایت شده اما در جمع‌بندیِ کلی نشده.

**وضعیت:** 🆕 ثبت‌شده.



