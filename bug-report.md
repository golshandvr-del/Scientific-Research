# 🐞 گزارشِ باگ‌ها (Bug Report) — دفترچهٔ زندهٔ ممیزی

> این فایل طبقِ **User Note** ساخته شده: تمرکز بر **پیدا کردنِ باگ‌ها** (حتی ریزترین‌ها و
> هر چیزِ غیرمنطقی) در **سایت (`web_tool`)**، **`local-mobile`**، و **استراتژی‌ها/انجین**.
>
> **روشِ کار (شبیه دفترچه یادداشت):** به‌محضِ پیدا شدنِ هر باگ، همان‌جا نوشته می‌شود و بلافاصله
> commit + push می‌شود؛ سپس سراغِ باگِ بعدی. تا یک باگ ثبت نشود، به‌سراغِ بعدی نمی‌رویم.

## راهنمای شدت
- 🔴 **CRITICAL** — منطقِ تصمیم/معامله را اشتباه می‌کند یا کرش/داده‌ی غلط به کاربر می‌دهد.
- 🟠 **HIGH** — رفتارِ نادرست در شرایطِ خاص؛ می‌تواند سیگنال/عدد را غلط کند.
- 🟡 **MEDIUM** — ناسازگاری/نقصِ منطقی که معمولاً بی‌خطر است اما باید اصلاح شود.
- 🔵 **LOW** — جزئی/کیفیتِ کد/پیامِ گمراه‌کننده.

## وضعیت
- 🆕 ثبت‌شده (هنوز رفع نشده)
- ✅ رفع‌شده
- ⏭️ نیازمند تصمیم/بررسیِ بیشتر

---

## فهرستِ باگ‌ها

### BUG-001 — 🟠 HIGH — ناسازگاریِ معیارِ «شکستِ روند» در مدیریتِ خروجِ لایهٔ S214 🆕
**فایل‌ها:** `web_tool/src/gold_m5_router.ts` (تابع `manageGoldM5Scalp`, خطوط ~339-344) +
`web_tool/src/gold_m5_late_entry.ts` (خطوط 33-34).

**شرح:** موتورِ مدیریتِ لحظه‌ایِ اسکالپ (`manageGoldM5Scalp`) هنگامِ تشخیصِ «شکستِ روند»
(برای خروجِ `wrong`) از `EMA_FAST=20` و `EMA_SLOW=100` استفاده می‌کند (خطوط ۳۹–۴۰ همان فایل).
اما وقتی سیگنالِ ورود از **لایهٔ S214 (Late-Entry)** آمده باشد، آن لایه رژیمِ روند را با
`EMA20/EMA50` تعریف می‌کند (`gold_m5_late_entry.ts` خطوط ۳۳–۳۴). در نتیجه معاملهٔ S214 با
TP/SL مخصوصِ خودش (۳۰۰/۱۵۰) مدیریت می‌شود — که درست است — **اما** شرطِ «شکستِ روند» با
EMA100 (متعلق به لایهٔ اسکالپِ پیش‌فرض S79) سنجیده می‌شود، نه EMA50 که خودِ S214 برایش تعریف
شده. این یعنی خروجِ زودهنگام/دیرهنگامِ ناسازگار با منطقِ ورود.

**چرا مهم است:** طبقِ اصلِ «هر لایه TP/SL و منطقِ مدیریتِ مخصوصِ خود را دارد» (User Note،
اشتباهِ رایج «TP/SL یکسان برای همه»)، معیارِ شکستِ روند هم باید مخصوصِ لایه باشد. الان
`manageGoldM5Scalp` هیچ پارامتری برای دورهٔ EMA نمی‌گیرد ⇒ همیشه EMA100.

**پیشنهادِ رفع:** افزودنِ پارامترهای اختیاریِ `emaFast`/`emaSlow` به `ScalpManageInput` و
پاس‌دادنِ (۲۰،۵۰) هنگام مدیریتِ معاملهٔ S214 (فرانت‌اند باید بداند سیگنال از کدام لایه آمده).

**وضعیت:** 🆕 ثبت‌شده (بررسیِ بیشتر برای مسیرِ پاس‌دادنِ منشأ لایه لازم است).

---

### BUG-002 — 🟡 MEDIUM — اسپردِ تخمینیِ طلا ۰.۳۵$ به‌جای ۰.۳۳$ (مشخصاتِ حساب) 🆕
**فایل:** `web_tool/src/trade_manager.ts` خط ۲۰۲.

**شرح:** `const spreadEst = price >= 100 ? 0.35 : ...`. اما مشخصاتِ رسمیِ حسابِ دمو (در
README و پرامپت) اسپردِ واقعیِ طلا را **۰.۳۳$/oz** تعریف می‌کند. اینجا ۰.۳۵ استفاده شده.
این عدد در محاسبهٔ `neutralBand` (ناحیهٔ خنثیِ نویز/اسپرد) به‌کار می‌رود ⇒ ناحیهٔ خنثی کمی
بزرگ‌تر از واقعیت می‌شود. اثرش کوچک است اما ناسازگار با مشخصاتِ دقیقِ حساب.

**پیشنهادِ رفع:** ۰.۳۵ ⇒ ۰.۳۳ (هم‌راستا با `engine/market_spec.py`).

**وضعیت:** 🆕 ثبت‌شده.

---

### BUG-003 — 🟠 HIGH — فیلدِ `pnlUsd` واحدِ قیمت است نه دلار (CONTRACT_SIZE نادیده) 🆕
**فایل:** `web_tool/src/trade_manager.ts` خطوط ۱۸۳، ۵۵–۵۶ (تعریفِ `TradeStatus`).

**شرح:** فیلد `pnlUsd` (نامش «سود/زیان به دلار») این‌گونه محاسبه می‌شود:
`const pnlUsd = round2(rawMove)` که `rawMove` صرفاً **اختلافِ قیمت** (price − entry) است،
نه سودِ دلاری. طبقِ مشخصاتِ حساب `CONTRACT_SIZE=100` (هر ۱.۰۰$ حرکت = ۱۰۰$/لات). پس
سودِ دلاریِ واقعیِ ۱ لات = `rawMove × 100`. الان اگر قیمتِ طلا ۲۰ دلار حرکت کند،
`pnlUsd=20` نمایش داده می‌شود در حالی که سودِ ۱ لات = ۲۰۰۰$ است. کامنتِ خطِ ۵۵ هم مبهم
است («به ازای ۱ لات پایه … نسبت به فاصله»). این فیلد یا باید به `priceMove` تغییرِ نام
دهد یا در CONTRACT_SIZE ضرب شود تا با واقعیتِ حساب هم‌خوان باشد.

**نکته:** UI کنونی شاید این فیلد را نمایش ندهد (باید در `app.js` بررسی شود)، اما هر
مصرف‌کننده‌ای که فرض کند این «دلار» است، عددِ ۱۰۰ برابر غلط می‌گیرد.

**وضعیت:** 🆕 ثبت‌شده (بررسیِ مصرفِ فیلد در فرانت‌اند لازم است).

> **✅ پیگیریِ نشستِ جاری (تکمیلِ بررسیِ فرانت‌اند):** بررسی شد که `app.js` (خطوط ۷۱۹–۷۲۰)
> فقط **`pnlR`** (سود/زیان بر حسبِ R) را به کاربر نمایش می‌دهد و فیلدِ `pnlUsd` (که واحدش
> غلط است) در هیچ‌جای UI رندر نمی‌شود. بنابراین باگ فعلاً **نهفته** است و شدتش در سطحِ HIGH
> باقی می‌ماند (نه CRITICAL) — اما اگر روزی نمایشِ «سود دلاری» به UI اضافه شود، فوراً به
> عددِ ۱۰۰× غلط تبدیل می‌شود. رفع (ضرب در `CONTRACT_SIZE × lots` یا تغییرِ نام به `priceMove`)
> همچنان لازم است تا این تلهٔ آینده بسته شود. (`pnlR` صحیح است چون `rawMove/riskDist` نسبت
> است و واحدِ قیمت در صورت و مخرج حذف می‌شود.)

---

### BUG-004 — 🔴 CRITICAL — ناسازگاریِ تایم‌فریم: لایهٔ Monday (S140⁺⁺، «M5») روی کندلِ M15 اجرا می‌شود 🆕
**فایل‌ها:** `web_tool/src/router.ts` (لایهٔ Monday، خطوط ۴۵۶–۵۷۸) + `web_tool/src/index.tsx`
(خطوط ۵۷۴–۵۷۶، فراخوانیِ `decide` برای کارتِ `XAUUSD`) + `web_tool/src/monday_drift.ts`
(خطوط ۳۱–۳۳).

**شرح:** طبقِ README و کامنت‌های `monday_drift.ts`، لایهٔ **S140⁺⁺** در نشستِ S190–S195 از
M15 به **M5** ارتقا یافت و پارامترهایش مخصوصِ M5 کالیبره شدند:
`MONDAY_SL_PIP=100`, `MONDAY_TP_PIP=200`, `MONDAY_MAX_HOLD=288` (کامنتِ خطِ ۳۳: «۲۴ ساعت
روی M5 = ۲۸۸×۵ دقیقه»).

**اما** این لایه داخلِ تابعِ `decide()` قرار دارد و `decide()` در `index.tsx` **فقط برای
کارتِ `XAUUSD` (تایم‌فریم M15)** صدا زده می‌شود (خط ۵۷۶). کارتِ `XAUUSD-M5` تابعِ جداگانهٔ
`decideGoldM5` را صدا می‌زند که Monday را **ندارد**. نتیجه:

1. سیگنالِ Monday عملاً روی دادهٔ **M15** تولید می‌شود، نه M5 — برخلافِ استراتژیِ اثبات‌شده.
2. `MONDAY_MAX_HOLD=288` که در `manage.maxHoldBars` می‌رود، روی کارتِ M15 معادلِ
   288×15دقیقه = **۷۲ ساعت** است، نه ۲۴ ساعت که طراحی شده (چون `barsHeld` کاربر بر مبنای
   کندلِ همان کارت شمرده می‌شود).
3. TP/SL کالیبرهٔ M5 (100/200 pip) روی نوسانِ بزرگ‌ترِ M15 اعمال می‌شود ⇒ رفتارِ متفاوت از
   بک‌تست.

**چرا CRITICAL:** این یعنی لایه‌ای که در سند به‌عنوان «تأییدشده و افزایشیِ +$1,553 روی M5»
ثبت شده، در محصولِ زنده روی تایم‌فریمِ اشتباه اجرا می‌شود ⇒ رفتارِ واقعیِ سیگنال با
بک‌تست هم‌خوان نیست. (همین ایراد احتمالاً برای S139/S141 نیز صادق است چون آن‌ها هم داخلِ
`decide` روی کارتِ M15 هستند در حالی که برخی pip/max-hold مخصوصِ تایم‌فریمِ دیگرند —
نیازمندِ بررسیِ جداگانه.)

**پیشنهادِ رفع:** یا لایهٔ Monday به مسیرِ `decideGoldM5` منتقل شود (اجرای واقعی روی M5)،
یا اگر عمداً روی M15 می‌ماند، `MONDAY_MAX_HOLD` و TP/SL برای M15 بازتعریف شوند و سند
اصلاح شود. نیاز به تصمیمِ معماری دارد.

**وضعیت:** ⏭️ ثبت‌شده — نیازمندِ تصمیمِ معماری/بررسیِ S139/S141 مشابه.

---

### BUG-005 — 🟠 HIGH — سیگنالِ ساعت-محورِ S73 روی کندلِ M5 چند بار در ساعت تکرار می‌شود 🆕
**فایل:** `web_tool/src/eurusd_router.ts` (`decideEurusd`, خطوط ۳۱۸، ۴۵) + `index.tsx`
(کارتِ `EURUSD` با `tf='5m'`, خطوط ۵۰۳، ۶۲۸–۶۳۱).

**شرح:** کارتِ اصلیِ EURUSD در S187–S189 از M15 به **M5** ارتقا یافت (`tf='5m'`). اما شرطِ
ورودِ S73 هنوز ساعت-محور است: `if (nowUtcHour === ENTRY_HOUR_UTC && pb.met)` که
`ENTRY_HOUR_UTC=0`. روی دادهٔ M5، ساعتِ ۰ UTC شاملِ **۱۲ کندلِ M5** است (۰۰:۰۰، ۰۰:۰۵،
… ۰۰:۵۵). یعنی تا وقتی `pb.met` (pullbackِ ۴ کندلِ اخیر نزولی) برقرار بماند، سیگنالِ ENTRY
برای **هر کدام از این ۱۲ کندل** صادر می‌شود — نه یک‌بار در ابتدای سشن.

در بک‌تستِ S73 روی M5 (که +$8,911 داد) ورود معمولاً «open اولین کندلِ ساعتِ ۰» است، نه هر
کندلِ داخلِ ساعت. اگر منطقِ زنده هر کندلِ ساعتِ ۰ را سیگنال بگیرد، رفتارِ سیگنال با بک‌تست
هم‌خوان نیست و کاربر ممکن است چند سیگنالِ متوالی در یک ساعت ببیند.

**تبصره:** `signal_latch`/فرانت‌اند شاید سیگنال را قفل کند (باید بررسی شود)، اما منطقِ سرور
ذاتاً باید فقط **اولین کندلِ ساعت** را ماشه کند (مثلاً با چک‌کردنِ اینکه کندلِ قبلی ساعتِ
متفاوتی داشته: `prevHour !== ENTRY_HOUR_UTC`).

**وضعیت:** 🆕 ثبت‌شده — نیازمندِ افزودنِ گیتِ «اولین کندلِ ساعت».

---

### BUG-006 — 🟡 MEDIUM — S164 روی کندلِ M5 هم چند بار در ساعتِ ۱۳ تکرار می‌شود 🆕
**فایل:** `web_tool/src/eurusd_router.ts` (`decideEurusd`, خطوط ۲۵۴، ۳۸).

**شرح:** مشابهِ BUG-005: شرطِ ورودِ S164 `s164Day && nowUtcHour === S164_ENTRY_HOUR_UTC`
(۱۳ UTC) است. روی کارتِ EURUSD که اکنون M5 است، ساعتِ ۱۳ شاملِ ۱۲ کندلِ M5 است ⇒ سیگنالِ
SHORT برای هر کندلِ ساعتِ ۱۳ در آن روزِ خاص تکرار می‌شود. بک‌تستِ S164 روی M15 کالیبره شده
(`S164_MAX_HOLD_BARS=12` = «۳ ساعت» با کندلِ M15). روی M5 این ۱۲ کندل = ۱ ساعت، نه ۳ ساعت
⇒ هم تکرارِ سیگنال و هم ناسازگاریِ maxHold مثلِ BUG-004.

**وضعیت:** 🆕 ثبت‌شده — همان ریشهٔ BUG-004/005 (لایهٔ ساعت/M15-کالیبره روی کندلِ M5).

---

### BUG-007 — 🟠 HIGH — VWAP روی دادهٔ Yahoo GC=F با volumeِ ناقص/صفر بی‌اعتبار می‌شود 🆕
**فایل:** `web_tool/src/signal.ts` (`anchoredVWAP`, خطوط ۷۱–۸۳) + `index.tsx` (کندلِ زنده با
`volume: 0`).

**شرح:** `anchoredVWAP` وزن‌دهیِ قیمت را با `volume` انجام می‌دهد و در نبودِ حجم به `close`
سقوط می‌کند (`out[i] = cumV > 0 ? cumPV / cumV : c[i].close`). دو مشکل:
1. دادهٔ Yahoo `GC=F` اغلب volume را برای کندل‌های داخل‌روزیِ M15 ناقص یا صفر می‌دهد؛ در آن
   صورت VWAP عملاً برابرِ close می‌شود ⇒ فیلترِ `vwap_dist_atr` (وزنِ ۰.۳۵ در امتیازِ سیگنال)
   بی‌اثر/غلط می‌شود.
2. کندلِ زندهٔ در حالِ شکل‌گیری در `rebaseFuturesToSpot`/`mergeLiveQuote` با `volume: 0`
   ساخته می‌شود؛ حتی اگر بقیهٔ کندل‌ها volume داشته باشند، کندلِ جاری در جمعِ VWAP سهمِ صفر
   دارد (هرچند این به closedBars حذف می‌شود، در `/api/analysis` که closedBars ندارد باقی است).

**اثر:** `a.probability` که مستقیماً گیتِ ورودِ S67 (خط ۱۱۲۸ router: `p >= P_MIN`) است، از
یک VWAPِ احتمالاً بی‌اعتبار تغذیه می‌شود ⇒ احتمالِ سیگنالِ نادرست.

**پیشنهادِ رفع:** اگر مجموعِ volume روز ~۰ بود، به VWAPِ بدونِ وزن (میانگینِ typical-price)
یا EMA سقوط کن و در UI شفاف اعلام کن که VWAP در دسترس نیست.

**وضعیت:** 🆕 ثبت‌شده.

---

### BUG-008 — 🟡 MEDIUM — قفلِ سیگنال (latch) می‌تواند سیگنالِ لایهٔ ساعت-محورِ منقضی را ۳ رفرش زنده نگه دارد 🆕
**فایل:** `web_tool/public/static/signal_latch.js` (خطوط ۵۶–۷۳، `NEUTRAL_TOLERANCE=3`).

**شرح:** مکانیزمِ ضدِ flicker وقتی سرور از ENTRY به NEUTRAL می‌رود، تا `NEUTRAL_TOLERANCE=3`
نمونهٔ متوالی سیگنالِ ENTRY را در حالتِ `_fading` زنده نگه می‌دارد. برای لایه‌های
**رژیم-محور** این منطقی است (نوسانِ گذرا). اما برای لایه‌های **ساعت-محور** (S73/S139/S140/
S141/S164) که پنجرهٔ ورودشان دقیقاً با گذرِ ساعت **بسته** می‌شود، این یعنی سایت ممکن است تا
۳ رفرش پس از بسته‌شدنِ پنجره هنوز «BUY پایدار» نشان دهد — یعنی سیگنالِ منقضی. با BUG-005/006
(تکرارِ سیگنال روی M5) تعامل می‌کند.

**پیشنهادِ رفع:** برای تصمیم‌هایی که `timeGate.windowOpen===false` شده، latch باید فوراً
باطل شود (نه صبر تا ۳ نمونه).

**وضعیت:** 🆕 ثبت‌شده.

---

### BUG-009 — 🔴 CRITICAL — ناسازگاریِ RSI بین انجینِ لایو (TS) و بک‌تستِ پایتون: در نبودِ ضرر، RSI=100 به‌جای NaN 🆕
**فایل‌ها:** `web_tool/src/indicators.ts` (تابع `rsi`, خطوط ۷۴–۸۷) در برابر
`engine/indicators.py` (تابع `rsi`, خطوط ۱۷–۲۴).

**شرح:** فرمولِ RSI در دو سمتِ پروژه رفتارِ متفاوت در حالتِ لبه (edge case) دارد:

- **پایتون (مرجعِ بک‌تست):**
  ```python
  rs = avg_gain / avg_loss.replace(0, np.nan)   # وقتی avg_loss=0 ⇒ rs=NaN
  return 100 - (100 / (1 + rs))                 # ⇒ 100 - NaN = NaN
  ```
  یعنی وقتی در پنجرهٔ اخیر **هیچ کندلِ نزولی‌ای** نبوده (avg_loss=0)، RSI برابرِ **NaN**
  می‌شود (و در شرط‌های `rsi > X` نادیده/False می‌شود).

- **TypeScript (انجینِ لایو):** خط ۸۳–۸۴:
  ```ts
  const rs = al[i] === 0 ? NaN : ag[i] / al[i]
  out[i] = isNaN(rs) ? 100 : 100 - 100 / (1 + rs)
  ```
  یعنی همان حالت (avg_loss=0) به **RSI=100** ترجمه می‌شود، نه NaN.

**چرا CRITICAL:** این دقیقاً همان حالتی است که در **روندِ صعودیِ قوی/بدونِ پولبک** رخ می‌دهد
(چند کندلِ پیاپیِ صعودی). در آن لحظه:
- بک‌تستِ پایتون: RSI=NaN ⇒ شرطِ اشباعِ خرید `rsi >= 70` **برقرار نمی‌شود** (چون NaN>=70 غلط است).
- لایوِ TS: RSI=100 ⇒ شرطِ `rsi >= 70` **برقرار می‌شود** (مثلاً `confirmation_filter.ts:94`
  که بازهٔ مجازِ RSI را `35..70` می‌گیرد ⇒ در RSI=100 فیلتر **رد** می‌کند؛ اما در پایتون
  همان کندل NaN بوده و رفتارِ فیلتر متفاوت می‌شد).

یعنی سیگنال/فیلترِ زنده در نقاطِ حساسِ روندِ خالص، با نتیجهٔ بک‌تست هم‌خوان نیست — نمونه‌ای
دیگر از واگراییِ لایو-از-بک‌تست (هم‌خانوادهٔ BUG-004/005). حالتِ دوم (هم gain و هم loss صفر،
بازارِ کاملاً flat) نیز در پایتون NaN و در TS مقدارِ ۱۰۰ می‌دهد — که حتی از نظرِ نظری هم غلط
است (بازارِ بی‌حرکت باید RSI≈۵۰ یا نامشخص باشد، نه ۱۰۰).

**پیشنهادِ رفع:** خطِ ۸۴ به‌جای بازگرداندنِ ۱۰۰، همان رفتارِ پایتون را بازتولید کند: در
`al[i]===0` مقدار را NaN بگذارد (و مصرف‌کننده‌ها با `Number.isFinite` مدیریت کنند)، یا اگر
عمداً ۱۰۰ می‌خواهیم، بک‌تستِ پایتون هم باید همین‌طور شود تا هم‌راستا بمانند. تصمیمِ نهایی باید
مرجعِ واحد داشته باشد.

**وضعیت:** 🆕 ثبت‌شده.

---

### BUG-010 — 🟠 HIGH — کامنتِ گمراه‌کننده + ناسازگاریِ محاسبهٔ ATR در فیلترِ تأیید (SMA به‌جای EWM/Wilder) 🆕
**فایل:** `web_tool/src/confirmation_filter.ts` (تابع `atrRaw`, خطوط ۴۰–۵۹، مصرف در ۷۳–۷۴، ۸۷–۸۹).

**شرح:** تابعِ `atrRaw` داخلِ فیلترِ تأیید، ATR را با **میانگینِ متحرکِ سادهٔ (SMA)** روی
TrueRange حساب می‌کند (خطوط ۵۱–۵۷: پنجرهٔ لغزانِ ساده). کامنتِ خطِ ۵۰ صریحاً می‌گوید:
«میانگینِ متحرکِ ساده روی TR (**سازگار با ind.atr پروژه که SMA(TR) است**)». اما این ادعا
**غلط** است: تابعِ رسمیِ `ind.atr` در `indicators.ts` (خطوط ۱۰۰–۱۰۲) از
`ewmAlpha(trueRange(c), 1/period)` استفاده می‌کند — یعنی **EWM با آلفای Wilder (1/period)**،
نه SMA. همچنین معادلِ پایتونِ آن (`engine/indicators.py` خط ۳۵) نیز `tr.ewm(alpha=1/period)`
است.

**چرا مهم است:** شرطِ سومِ فیلتر (`ATR14 > ATR100`، رژیمِ نوسانِ فعال) که یکی از ۵ مؤلفهٔ
امتیازِ تأیید است، با یک ATRِ SMA-محور سنجیده می‌شود که مقدارش با ATRِ EWM-محورِ بقیهٔ پروژه
(و بک‌تست) **متفاوت** است. SMA به‌ویژه در ATR100 نسبت به EWM واکنشِ بسیار متفاوتی به
شوک‌های نوسان دارد ⇒ گیتِ score ممکن است در برخی کندل‌ها متفاوت از آن‌چه در بک‌تستِ فیلتر
کالیبره شده باز/بسته شود. این مستقیماً روی تصمیمِ ENTRY لایه‌های Monday/Turn-of-Month اثر
می‌گذارد (همان‌هایی که این فیلتر برای بالا بردنِ WRشان اضافه شد).

**پیشنهادِ رفع:** یا `atrRaw` را به EWM(1/period) تبدیل کن تا با `ind.atr` و پایتون یکی شود،
یا اگر عمداً SMA است، کامنت اصلاح شود و در سندِ کالیبراسیونِ فیلتر ثبت گردد که آستانهٔ score
با ATRِ SMA کالیبره شده (که با بک‌تستِ EWM هم‌خوان نیست). ترجیحِ فنی: EWM برای هم‌راستایی
با بک‌تست.

**وضعیت:** 🆕 ثبت‌شده.

---

### BUG-011 — 🟠 HIGH — لایهٔ «TP/SL ثابت» با heuristicِ روندِ معکوس زودهنگام بسته می‌شود (ترتیبِ شرط‌ها) 🆕
**فایل:** `web_tool/src/trade_manager.ts` (بلوکِ `overallAction`, خطوط ۴۲۲–۴۵۰؛ به‌ویژه ۴۲۹ در برابر ۴۳۱).

**شرح:** در جمع‌بندیِ `overallAction`، ترتیبِ `else if`ها این است:
```
... reachedTp / reachedSl / closeForMaxHold ...
else if (trendAgainst && inRealLoss)      → overallAction='close'   // خط ۴۲۹
else if (managePlan?.style==='fixed-tp-sl') → overallAction='hold'  // خط ۴۳۱
```
چون شرطِ `trendAgainst && inRealLoss` **قبل** از شرطِ `fixed-tp-sl` بررسی می‌شود، معامله‌ای که
از یک لایه با سبکِ **TP/SL ثابت** باز شده، به‌محضِ اینکه روندِ لحظه‌ایِ سایت معکوس شود و کمی در
ضرر برود (`inRealLoss`)، پیشنهادِ کلیِ **«ببند» (close)** می‌گیرد — دقیقاً برخلافِ فلسفهٔ
همان لایه که خطِ ۴۳۱ توضیح می‌دهد: «TP/SL ثابت دارد؛ به همان پلنِ اولیه پایبند بمان و جابه‌جا
نکن».

**چرا مهم است:** لایه‌های `fixed-tp-sl` (طبقِ اصلِ پروژه) عمداً باید **فقط** با برخوردِ
TP/SL خودشان یا maxHold بسته شوند، نه با سیگنالِ روندِ heuristic که به هیچ‌یک از پارامترهای
بک‌تستِ آن لایه مربوط نیست. این یعنی کاربر ممکن است معامله‌ای که در بک‌تست تا TP دویده را
زودهنگام ببندد ⇒ سودِ خالصِ واقعی < بک‌تست. این هم‌خانوادهٔ «اشتباهِ رایج: TP/SL یکسان/مدیریتِ
غیرِ لایه-محور» است.

**پیشنهادِ رفع:** شرطِ `managePlan?.style==='fixed-tp-sl'` باید **قبل** از
`trendAgainst && inRealLoss` بیاید (یا داخلِ آن شرط، حالتِ fixed مستثنا شود)، تا مدیریتِ
لایه‌ای بر heuristicِ روند اولویت داشته باشد — همان‌طور که در بخشِ `layerAwareAdvices`
(خطوط ۲۷۱–۲۷۷) برای باکس‌های جزئی رعایت شده اما در جمع‌بندیِ کلی نشده.

**وضعیت:** 🆕 ثبت‌شده.

---

### BUG-012 — 🟠 HIGH — وقتی VWAP/RSI/ADX نامعتبر (NaN) باشند، امتیازِ سیگنال جریمهٔ منفیِ ناخواسته می‌گیرد 🆕
**فایل:** `web_tool/src/signal.ts` (تابع `analyze`, خطوط ۱۳۹–۱۶۸).

**شرح:** چند مؤلفهٔ امتیازدهی با زنجیرهٔ `if/else if/... else` نوشته شده‌اند که شاخهٔ `else`
یک **جریمهٔ منفی** دارد. اما مقایسه‌های عددی با `NaN` همیشه `false` برمی‌گردند، پس اگر آن
اندیکاتور نامعتبر (NaN) باشد، اجرا به شاخهٔ `else` می‌افتد و جریمه اعمال می‌شود — به‌جای
«نادیده گرفتنِ عاملِ نامشخص». نمونه‌ها:

1. **VWAP** (خطوط ۱۳۹–۱۴۶): اگر `vwap`=NaN (دادهٔ GC=F با volume صفر — رجوع BUG-007) یا
   `atr`=NaN، آن‌گاه `vwapDistAtr`=NaN؛ هر سه شرطِ مثبت false می‌شوند و کنترل به
   `else → vwapContrib = -0.15` می‌رسد. یعنی «VWAPِ نامشخص» به **جریمهٔ ۰.۱۵−** ترجمه
   می‌شود، نه صفر.
2. **RSI** (خطوط ۱۵۶–۱۶۱): اگر `r`=NaN، شاخه‌های مثبت/منفیِ مشخص رد می‌شوند و مقدارِ
   `rsiContrib` روی ۰ می‌ماند (اینجا پیش‌فرض ۰ است، پس بی‌خطر) — **اما** ترکیب با BUG-009
   (که RSI به‌جای NaN مقدارِ ۱۰۰ می‌دهد) یعنی در روندِ خالص `r=100 > 75 ⇒ rsiContrib=-0.30`
   اعمال می‌شود؛ حال آن‌که در بک‌تستِ پایتون همان کندل RSI=NaN بود و جریمه‌ای نداشت.
3. **ADX** (خطوط ۱۶۴–۱۶۸): پیش‌فرضِ `adxContrib=0` است، پس NaN بی‌خطر است (فقط برای کامل بودنِ ممیزی).

**چرا مهم است:** `score` نهایی از سیگموئید عبور کرده و `probability` را می‌سازد که مستقیماً
گیتِ ورودِ چند لایه است (`p >= P_MIN`). یک VWAPِ نامعتبر که ۰.۱۵ واحد از score کم می‌کند،
می‌تواند در مرزِ آستانه سیگنالِ معتبر را حذف یا سیگنالِ معتبرِ دیگری را تضعیف کند — بدونِ اینکه
کاربر بداند علت، «دادهٔ ناقص» بوده نه ضعفِ واقعیِ ستاپ.

**پیشنهادِ رفع:** پیش از امتیازدهی، هر مؤلفه با `Number.isFinite` گارد شود؛ اگر نامعتبر بود،
`contrib=0` (خنثی) گذاشته شود و در breakdown با یادداشتِ «داده ناکافی» علامت بخورد — نه جریمهٔ
`else`. به‌ویژه شاخهٔ `else` در VWAP نباید حالتِ NaN را با حالتِ «خیلی زیرِ VWAP» یکی بگیرد.

**وضعیت:** 🆕 ثبت‌شده.

---

### BUG-013 — 🔴🔴 CRITICAL (بحرانی‌ترین تاکنون) — تعریفِ pip طلا در انجینِ لایو ۱۰ برابر کوچک‌تر از بک‌تست ⇒ TP/SL واقعیِ کاربر ۱۰× غلط 🆕
**فایل‌ها:** `web_tool/src/gold_m5_router.ts` (خط ۴۹: `const PIP = 0.01`، و خطوط ۴۷–۴۸،
۳۳۶–۳۳۷) + `web_tool/src/gold_m5_late_entry.ts` (خطوط ۴۴–۴۶) در برابر منبعِ حقیقتِ بک‌تست
`engine/scalp_engine.py` (خطوط ۴۹، ۵۹–۶۰، ۷۱: `pip=0.10`) و `engine/market_spec.py`
(`XAU_PIP = 0.10`).

**شرح:** دو تعریفِ **ناسازگار** از «pip طلا» در پروژه وجود دارد:

| مرجع | تعریفِ pip طلا |
|---|---|
| بک‌تستِ پایتون (`scalp_engine.py`, `market_spec.py`) | **`pip = 0.10$`** (۱ pip = ۱۰ point) |
| انجینِ لایوِ TS (`gold_m5_router.ts` خط ۴۹) | **`PIP = 0.01$`** (۱ pip = ۱ point) |

اعدادِ TP/SL همهٔ لایه‌های اسکالپِ طلا (S79/S214/…) در بک‌تستِ پایتون بر حسبِ **pip=0.10**
کالیبره شده‌اند. برای نمونهٔ اثبات‌شدهٔ S214 (`strategies/s214c…py` خط ۳۷: `SL, TP = 150, 300`):

- **بک‌تستِ پایتون:** `SL = 150 × 0.10 = $15.00` , `TP = 300 × 0.10 = $30.00`
  (net +$726, WR 50.9% روی همین اعداد به‌دست آمد).
- **انجینِ لایوِ TS:** `S214_HIDDEN_SL_PIP=150 × PIP(0.01) = $1.50` , `TP=300 × 0.01 = $3.00`.

یعنی **همان لایه در محصولِ زنده با TP/SL دقیقاً ۱۰ برابر کوچک‌تر اجرا می‌شود.** همین برای
لایهٔ پیش‌فرضِ اسکالپ نیز صادق است: `HIDDEN_TP_PIP=120, HIDDEN_SL_PIP=80` ⇒ در لایو
`$1.20/$0.80` به‌جای `$12/$8` بک‌تست.

**چرا این بحرانی‌ترین باگ است:**
1. کاربر مستقیماً TP/SL را از سایت می‌گیرد؛ یک SL=$1.50 روی طلایی که ATR(M5)‌اش اغلب چند
   دلار است، تقریباً بلافاصله با نویز/اسپرد می‌خورد ⇒ زنجیرهٔ استاپ‌های زودهنگام.
2. رفتارِ واقعیِ معامله هیچ ربطی به WR/net بک‌تستِ ثبت‌شده ندارد ⇒ کلِ ادعای «سودِ خالصِ
   لایه» برای کاربرِ زنده بی‌اعتبار می‌شود.
3. حتی کامنتِ خودِ پایتون (خطوط ۵۹–۶۰ `scalp_engine.py`) این تله را هشدار داده:
   «اسپردِ طلا = ۴۰ pip بر تعریفِ بروکر (۱pip=۰.۰۱$)... چون در موتور pip=0.10 است، معادلِ
   ۴.۰ pipِ موتور است». یعنی TS اشتباهاً **تعریفِ بروکر (۰.۰۱ = point)** را برداشته اما
   اعدادِ TP/SL بر مبنای **pipِ موتور (۰.۱۰)** کالیبره‌اند.

**پیشنهادِ رفع (نیازمندِ دقتِ بالا):** یا `PIP` در انجینِ لایوِ طلا به `0.10` اصلاح شود تا با
بک‌تست هم‌واحد گردد، **یا** همهٔ ثابت‌های `*_PIP` طلا در TS ۱۰ برابر شوند (مثلاً
`S214_HIDDEN_SL_PIP` از ۱۵۰ به ۱۵۰۰ با نگه‌داشتنِ PIP=0.01 — یعنی همان $15). قبل از رفع باید
**همهٔ** مصرف‌کننده‌های `PIP` در `gold_m5_router.ts` (و هر لایهٔ اسکالپِ طلای دیگر که از این
ماژول ارث می‌برد) ممیزی شوند تا واحد یکدست شود و دوباره ۱۰× در جهتِ عکس نشکند. تصمیمِ نهایی
باید تک-منبعِ‌حقیقت داشته باشد (ترجیحاً `market_spec` مشترک بین پایتون و TS).

**تبصرهٔ مهم برای بررسیِ بعدی:** باید بررسی شود لایه‌های EURUSD (که در `eurusd_router.ts`
`const PIP` جداگانه دارند و اعدادشان مثل `SL_PIP=12`, `S213_SL_PIP=150` است) آیا همین
ناسازگاری را با `EUR_PIP=0.0001` پایتون دارند یا نه — احتمالِ باگِ خواهر بالاست.

**وضعیت:** 🆕 ثبت‌شده — نیازمندِ رفعِ فوری و ممیزیِ کاملِ واحدِ pip در کلِ انجینِ لایو.

---

### BUG-014 — 🟠 HIGH — کمیسیونِ ۷$/لات در محاسبهٔ حجمِ لات، برخلافِ مشخصاتِ حساب (کمیسیون=۰) 🆕
**فایل:** `web_tool/src/router.ts` (`ASSET_SPECS`, خطوط ۲۴۸–۲۵۰؛ مصرف در `computeLots` خط ۲۸۱).

**شرح:** در `ASSET_SPECS` هر دو نمادِ قابلِ معامله `commissionPerLot: 7.0` دارند. این مقدار در
`computeLots` وارد محاسبهٔ ضررِ هر لات می‌شود:
```ts
const lossPerLotAtSl = slDist * spec.valuePerPricePerLot + spec.commissionPerLot  // +7
lots = riskDollars / lossPerLotAtSl
```
اما مشخصاتِ رسمیِ حسابِ دموِ کاربر (هم در پرامپت و هم در منبعِ حقیقتِ `engine/market_spec.py`:
`XAU_COMMISSION_PER_LOT = 0.0`, `EUR_COMMISSION_PER_LOT = 0.0`) صراحتاً می‌گوید **کمیسیونِ
جداگانه‌ای وجود ندارد؛ هزینه فقط از اسپرد است**.

**چرا مهم است:**
1. حجمِ لاتِ پیشنهادی به کاربر کمی **کوچک‌تر** از مقدارِ درست محاسبه می‌شود (چون مخرج بزرگ‌تر
   فرض شده) ⇒ ریسکِ واقعیِ هر معامله زیرِ `riskPct` هدف می‌افتد و در بلندمدت سودِ خالصِ دلاریِ
   لایه (که معیارِ اصلیِ پروژه است) کمتر از پتانسیلِ کالیبره‌شده خواهد بود.
2. ناسازگاری با «single source of truth» که خودِ سند (`market_spec.py`) تأکید می‌کند هزینه
   نباید کمیسیونِ جداگانه داشته باشد و مدلِ قدیمیِ ۵۷$ (اسپرد+کمیسیون) اشتباه بوده.

**پیشنهادِ رفع:** `commissionPerLot` را برای XAUUSD/EURUSD/AUDUSD به `0` تغییر بده تا با
`market_spec.py` هم‌راستا شود (هزینهٔ اسپرد جدا از این تابع و در سطحِ بک‌تست/PnL لحاظ می‌شود).
اگر می‌خواهیم هزینهٔ اجرایی در sizing لحاظ شود، باید معادلِ اسپردِ واقعی (۰.۳۳$/oz) باشد نه
عددِ ثابتِ ۷$.

**وضعیت:** 🆕 ثبت‌شده.

---

### ✅ یادداشتِ ممیزیِ منفی (نه باگ) — تعریفِ pip در EURUSD درست است
هنگامِ بررسیِ BUG-013 (تعریفِ pip طلا) نمادِ EURUSD نیز وارسی شد: در `eurusd_router.ts` خط ۴۹
`const PIP = 0.0001` **دقیقاً** با `EUR_PIP = 0.0001` پایتون هم‌خوان است، و اعدادِ
`S213_SL_PIP=150/TP=225` نیز با بک‌تستِ `s213_finalize.py` (`sl=150, tp=225`, EURUSD_M15)
مطابقت دارند و روی کارتِ درستِ **EURUSD-M15** (تابع `decideEurusdM15`) اجرا می‌شوند. پس
ناسازگاریِ ۱۰×ِ BUG-013 مختصِ طلا است و به EURUSD سرایت نکرده. (ثبت برای جلوگیری از بررسیِ
تکراری در آینده.)

---

### BUG-015 — 🟡 MEDIUM — متنِ `note`ِ S164 عددِ «TP 18pip / SL 18pip، ناحیهٔ پایدار ۱۸/۱۸» می‌گوید، در حالی که کد و بک‌تست ۲۰/۱۵ است 🆕
**فایل:** `web_tool/src/eurusd_router.ts` (ثابت‌ها خطوط ۴۰–۴۱؛ متنِ `manage.note` خطِ ۲۶۸).

**شرح:** ثابت‌های واقعیِ لایهٔ S164 در همین فایل چنین‌اند:
```ts
const S164_SL_PIP = 15.0   // خط ۴۰
const S164_TP_PIP = 20.0   // خط ۴۱
```
و این‌ها با منبعِ حقیقتِ بک‌تست کاملاً هم‌خوان‌اند
(`strategies/s164_eurusd_pre_month_end_fix_reversal.py` خطوط ۳۵–۳۶: `SL_PIP=15, TP_PIP=20`؛
سند `results/EURUSD_PreMonthEndFixReversal_NetProfit_221895.md` هم WR=60.71٪ را روی همین اعداد
گزارش می‌کند). پس اعدادِ عملیاتی درست‌اند.

**اما** متنی که به کاربر نشان داده می‌شود (فیلدِ `manage.note`، خطِ ۲۶۸) می‌گوید:
> «این لایهٔ رویداد-محور TP/SL ثابت دارد (TP **18**pip / SL **18**pip، ناحیهٔ پایدار **۱۸/۱۸**).»

یعنی سه عددِ نمایشی (۱۸/۱۸/۱۸) با اعدادِ واقعیِ اجراییِ لایه (TP=20، SL=15) و با اعدادِ
`rr` و `tpPlan`/`slPlan` همان تصمیم (که درست ۲۰ و ۱۵ می‌گویند، خطوط ۲۷۷/۲۸۵/۲۸۶) **در تضاد**
است. کاربر در یک باکسِ کارت «۲۰/۱۵» و در باکسِ دیگر «۱۸/۱۸» می‌بیند.

**چرا مهم است:** طبق «تعریفِ سایت»، بخشِ ورود باید TP/SL را **صریح و درست** به کاربر بگوید.
تناقضِ عددی در همان کارت، اعتماد و صحتِ سیگنال را خدشه‌دار می‌کند و کاربر ممکن است SL را روی
عددِ اشتباهِ ۱۸ بگذارد (SL بزرگ‌تر از ۱۵ ⇒ ریسکِ دلاریِ بیشتر از کالیبراسیون). «۱۸/۱۸» احتمالاً
بازمانده از یک نسخهٔ grid قدیمی‌تر است که بعداً به ۲۰/۱۵ نهایی شد اما این رشته به‌روزرسانی نشد.

**پیشنهادِ رفع:** متنِ خطِ ۲۶۸ به «TP ۲۰pip / SL ۱۵pip» اصلاح شود (هم‌راستا با ثابت‌ها و بک‌تست).
ترجیحاً از خودِ ثابت‌های `S164_TP_PIP`/`S164_SL_PIP` در رشته درون‌ریزی (interpolation) شود تا
دیگر هرگز از کد جدا نیفتد.

**وضعیت:** 🆕 ثبت‌شده.

---

### BUG-016 — 🔴 CRITICAL — گیتِ ورودِ `probability` (و RSI/ADX/VWAP/رابطهٔ EMA) از کندلِ زندهٔ ناتمام محاسبه می‌شود ⇒ repaintِ باقی‌مانده با وجودِ closed-bar gate 🆕
**فایل‌ها:** `web_tool/src/index.tsx` (خطوط ۵۴۸، ۵۷۶ و ۶۲۰) + `web_tool/src/signal.ts`
(`analyze`, `i = n-1` خطِ ۱۰۵ و ساختِ `probability`/`rsi`/`adx`/`vwap`) +
`web_tool/src/router.ts` (`regimeOf` خطوط ۳۲۰–۳۲۶ و گیتِ `p >= P_MIN` خطِ ۱۱۲۸).

**شرح:** سندِ `results/BUGFIX_LiveSignalRepaint_ClosedBarGate_293236.md` باگِ repaint را «رفع‌شده»
اعلام کرده، اما رفع **ناقص** است. آن‌چه اصلاح شد فقط آرایه‌های OHLCِ ماشه بود: در `index.tsx`
سیگنال‌ها از `sig = closedBars(useCandles)` گرفته می‌شوند (کندل‌های بسته). **اما** شیءِ تحلیلِ
`result` هم‌چنان این‌گونه ساخته می‌شود:
```ts
const useCandles = merged.candles          // شاملِ کندلِ زندهٔ در حالِ شکل‌گیری (mergeLiveQuote)
const result = analyze(useCandles)         // خطِ ۵۴۸ و ۶۲۰ — روی کندلِ زنده!
...
dec = decide(result, closes /* = sig بسته */, ...)   // خطِ ۵۷۶
```
و در `signal.ts` تابعِ `analyze` همهٔ مقادیرِ کلیدی را روی `i = n-1` (یعنی **همان کندلِ زندهٔ
ناتمام**) حساب می‌کند: `price`, `rsi14`, `adx`, `vwap`, `ema50/ema200`, و از همه مهم‌تر
`probability` (خروجیِ سیگموئیدِ score).

سپس در `router.ts`:
- `regimeOf` (خطوط ۳۲۰–۳۲۶) رژیم و `trendy` را از `a.trend/a.price/a.ema50/a.ema200/a.adx`
  می‌سازد — همه از کندلِ زنده.
- گیتِ **ورودِ اصلیِ لایهٔ S67** خطِ ۱۱۲۸: `if (p >= P_MIN)` که `p = a.probability` است.

**چرا CRITICAL:** `probability` مستقیماً تعیین می‌کند کارت به حالتِ **ENTRY** برود یا نه. چون از
کندلِ ناتمام تغذیه می‌شود، تا وقتی آن کندل بسته نشده مقدارش با هر تیکِ قیمت **تغییر می‌کند** ⇒
دقیقاً همان repaintی که سند ادعا کرده رفع شده. سناریو: در میانهٔ یک کندلِ M15، قیمت بالا می‌رود،
`probability` از زیرِ `P_MIN` به بالای آن می‌پرد و کارت «BUY» می‌دهد؛ کاربر وارد می‌شود؛ تا پایانِ
همان کندل قیمت برمی‌گردد و در واقع سیگنالی که بک‌تست (روی close کندلِ بسته) می‌دید هرگز صادر
نمی‌شد. این عیناً واگراییِ لایو-از-بک‌تست است — همان چیزی که closed-bar gate قرار بود حذف کند اما
فقط نیمی از مسیر (شرط‌های مبتنی بر آرایهٔ close) را پوشش داد و نیمهٔ `probability`/اندیکاتورهای
اسکالر را جا انداخت.

**تبصره:** این با BUG-007 (VWAPِ بی‌اعتبار) و BUG-012 (جریمهٔ NaN) هم‌افزایی می‌کند، چون هر سه
روی همان `result`ِ زندهٔ ناتمام سوارند.

**پیشنهادِ رفع:** یک `analyze` جداگانه روی `closedBars(useCandles)` اجرا شود و **همان** به
`decide`/`regimeOf` به‌عنوانِ منبعِ گیت داده شود؛ فقط `price`ِ نمایشی (برای «open کندلِ بعد»)
می‌تواند زنده بماند. یعنی `resultForGate = analyze(sig)` جدا از `resultForDisplayPrice`. این کارِ
معماریِ کوچکی است اما گیتِ ورود را با بک‌تست (که روی کندلِ بسته + shift(1) است) هم‌تراز می‌کند.

**وضعیت:** 🆕 ثبت‌شده — repaintِ باقی‌مانده؛ نیازمندِ اجرای `analyze` روی کندلِ بسته برای مسیرِ گیت.

---

### BUG-017 — 🟠 HIGH — کارتِ M30 در نبودِ OHLC کامل به استراتژیِ سوختهٔ S81 (WR ۲۸٪، حذف‌شده) سقوط می‌کند + کامنتِ منبعِ کارتِ M30 غلط است 🆕
**فایل‌ها:** `web_tool/src/gold_m30_router.ts` (`decideGoldM30TrendLine` خطوط ۲۰۳–۲۱۷، به‌ویژه
fallbackِ خطِ ۲۱۶) + `web_tool/src/index.tsx` (کامنتِ خطِ ۴۶۵).

**شرح — دو ایرادِ هم‌ریشه:**

**(الف) fallbackِ خطرناک به لایهٔ حذف‌شده.** تابعِ فعالِ کارتِ M30 این است:
```ts
export function decideGoldM30TrendLine(a, close, capital, riskPct, open?, high?, low?) {
  if (open && high && low && high.length === close.length && low.length === close.length) {
    ... // لایهٔ اثبات‌شدهٔ S215 (Trend-Line) + S219 (Channel)
  }
  return decideGoldM30(a, close, capital, riskPct)   // ← خطِ ۲۱۶: fallback
}
```
`decideGoldM30` همان لایهٔ **S81** است که طبقِ کامنتِ خطوط ۱۹۵–۱۹۶ همین فایل و README «در نشستِ
S163 حذف شد چون WR=۲۸٪ بود و رساندنش به WR≥۴۰٪ سود را نابود می‌کرد». اما این تابعِ حذف‌شده هنوز
منطقِ **ENTRY**ِ کامل دارد (خطوط ۱۱۸–۱۶۰: `if (rsi < RSI_ENTRY) → state:'ENTRY'`). پس اگر به هر
دلیل آرایه‌های OHLC ناقص/ناهم‌طول باشند (مثلاً نقصِ موقتِ داده، یا مسیرِ فراخوانی که فقط `close`
می‌دهد)، کارت به S81 سقوط می‌کند و می‌تواند سیگنالِ **ورودِ واقعی** از یک استراتژیِ **سوخته و
رسماً ردشده** بدهد — نقضِ مستقیمِ قانونِ اصلیِ پروژه (فقط لایهٔ اثبات‌شده با WR≥۴۰٪ حق سیگنال دارد).

**چرا HIGH:** یک fallback باید «امن» باشد (مثلِ NEUTRAL یا «در دستِ تحقیق»)، نه یک استراتژیِ
مردود که سیگنالِ خرید صادر می‌کند. کاربر نمی‌داند سیگنالی که می‌بیند از لایهٔ S215 آمده یا از
S81ِ حذف‌شده.

**(ب) کامنتِ گمراه‌کنندهٔ منبعِ کارت.** در `index.tsx` خطِ ۴۶۵:
> «XAUUSD (M30) — لایهٔ نوسانیِ نوِ **S81** (Swing Trend-Pullback، **+۱۴٬۳۲۷$**، منطقِ decideGoldM30).»

این کاملاً منسوخ است: کارتِ M30 اکنون لایهٔ **S215 (Trend-Line) + S219 (Channel)** را اجرا می‌کند
(خطِ ۵۶۷ همان فایل `decideGoldM30TrendLine` را صدا می‌زند)، و S81 با WR=۲۸٪ حذف شده. عددِ
+۱۴٬۳۲۷$ نیز به لایه‌ای مربوط است که دیگر فعال نیست ⇒ هر خواننده‌ای (یا خودِ ما در نشستِ بعد)
گمراه می‌شود.

**پیشنهادِ رفع:**
1. fallbackِ خطِ ۲۱۶ به یک تصمیمِ امن تغییر کند: یا `placeholderDecision` معادلِ M30 (NEUTRAL،
   «داده ناکافی») یا نسخه‌ای از `decideGoldM30` که فقط رژیم را **نمایش** دهد و هرگز `state:'ENTRY'`
   نساخت. ساده‌ترین رفع: در ابتدای `decideGoldM30`، شاخهٔ ENTRY را با یک پرچمِ `allowEntry=false`
   (پیش‌فرض) گارد کنیم و در fallback آن را false نگه داریم.
2. کامنتِ خطِ ۴۶۵ به «S215 (Trend-Line, Al Brooks فصلِ ۱۳) + S219 (Channel)» اصلاح شود و عددِ
   S81 حذف/به‌روز گردد.

**وضعیت:** 🆕 ثبت‌شده.

---

### BUG-018 — 🟡 MEDIUM — نقضِ «قانونِ طراحیِ سایت»: آمارِ داخلیِ تحقیق (سود خالص، WR، PF، شمارِ کندل، walk-forward) در متنِ نمایشیِ کاربر لو می‌رود 🆕
**فایل‌ها (متنِ `reason`/`note`ی نمایشی — نه کامنت):**
- `web_tool/src/overnight_drift.ts` خطوط ۵۵–۵۶ («بک‌تستِ ۱۳۵٬۰۰۰ کندل… سهمِ مستقلِ +۴۳٬۴۱۳$»).
- `web_tool/src/eurusd_router.ts` خطوط ۲۷۴–۲۷۵ («S164 روی ۲۰۰٬۰۰۰ کندل… +$3,473، WR=60.7٪،
  PF=1.71 و چهار پنجرهٔ walk-forward») و ۴۷۷ («S213… سهمِ مستقلِ +$418 با WR=59.6٪، PF=2.13»).
- `web_tool/src/brooks_high2.ts` خطِ ۹۸ («سهمِ مستقلِ +$۱٬۳۵۱، WR ۴۸.۸٪، PF ۱.۱۰»).
- `web_tool/src/router.ts` خطوط ۸۵۸ («سهمِ مستقل +۲۰٬۴۳۵$»)، ۹۵۹ («سهمِ مستقلِ OUT… +$۱٬۳۵۱»)،
  ۱۰۰۵ («سهمِ مستقلِ ناهمبسته… +$۸٬۱۳۰»)، ۱۰۴۵ («سهمِ مستقلِ ناهمبسته +$6,338 (WR ۵۱.۷٪)»)، ۱۰۵۹.
- `web_tool/src/turn_of_month_drift.ts` خطِ ۸۲ («سهمِ مستقلِ… +۴٬۱۶۲$»).
- `web_tool/src/vwap_confluence.ts` خطِ ۱۲۹ («تحلیلِ ۲۰۰٬۰۰۰ کندلِ M5…»).
- `web_tool/src/signs_of_strength.ts` خطِ ۱۴۴ («…walk-forward مثبت»).

**شرح:** «قانونِ مهمِ طراحیِ سایت» در پرامپت صریح است:
> «هیچ اطلاعات اضافه نباید به کاربر نشان داده شود. نتایجِ تحقیق تا به حال، سود خالصِ فعلی و…
> همگی اضافه‌اند.»

فیلدهای `reason`/`note`/`slPlan.note`/`tpPlan.note` در `RouterDecision` **مستقیماً** در کارتِ
سایت به کاربر نمایش داده می‌شوند (بخش‌های «ورود/نزدیک‌شدن»، طبقِ تعریفِ سایت). اما ده‌ها مورد از
این رشته‌ها آمارِ داخلیِ تحقیق را فاش می‌کنند: مبلغِ سودِ خالصِ مستقلِ لایه، درصدِ WR، PF، تعدادِ
کندلِ بک‌تست (۱۳۵٬۰۰۰ / ۲۰۰٬۰۰۰)، و تعدادِ پنجره‌های walk-forward. جالب آنکه خودِ پروژه در
`gold_m30_router.ts` (کامنتِ خطوط ۲۰–۲۱) و در `gold_channel.ts`/`gold_trend_line.ts`
(خطوطِ ۴۴–۴۵: `indepNet` صراحتاً «فقط برای مستندسازیِ داخلی») این اصل را می‌شناسد و رعایت
کرده — یعنی رفتار **ناسازگار** است: بعضی لایه‌ها آمار را پنهان می‌کنند، بعضی لو می‌دهند.

**چرا مهم است:** ۱) نقضِ مستقیمِ یک قانونِ صریحِ طراحی. ۲) این اعداد برای کاربر بی‌معنی/گمراه‌کننده‌اند
(کاربر net کلِ تاریخیِ بک‌تست را با انتظارِ سودِ همین معامله اشتباه می‌گیرد). ۳) با تغییرِ آیندهٔ
اعداد در تحقیق، این رشته‌های hard-code شده منسوخ می‌شوند و به منبعِ باگِ اطلاعاتِ غلط بدل می‌گردند
(همان‌طور که BUG-015/017 نشان دادند اعدادِ hard-code شده کهنه می‌شوند).

**پیشنهادِ رفع:** همهٔ ارجاع‌های آماری (net$/WR٪/PF/شمارِ کندل/walk-forward) از فیلدهای نمایشیِ
`reason`/`note` حذف شوند و با توضیحِ کیفیِ «چرا این ستاپ خوب است» جایگزین گردند (مثلِ «این لبه
مستقل از سایرِ سیگنال‌هاست و در آزمون‌های تاریخی پایدار بوده»). آمارِ کمی فقط در فایل‌های
`results/*.md` و کامنت‌های کد بماند، نه در خروجیِ کاربر. یک تستِ ساده هم اضافه شود که مطمئن شود
هیچ `reason` شاملِ الگوی `\+\$?[۰-۹0-9,٬]+` یا «WR» نیست.

**وضعیت:** 🆕 ثبت‌شده — نقضِ قانونِ طراحی؛ چند فایل.

---

### BUG-019 — 🟡 MEDIUM — نشتِ آمارِ داخلی در `noEntryReason`ِ `signal.ts` (موردِ جاافتاده از BUG-018) 🆕
**فایل:** `web_tool/src/signal.ts` (متنِ `noEntryReason`، خطوط ۲۷۰–۲۷۵).

**شرح:** BUG-018 نشتِ آمارِ تحقیق در فیلدهای نمایشیِ `reason`/`note` را در ده‌ها فایل فهرست کرد،
اما فایلِ `signal.ts` را جا انداخت. در همین فایل، رشتهٔ `noEntryReason` که برای حالتِ «رژیمِ
نزولی» ساخته می‌شود، صریحاً آمارِ داخلیِ تحقیق را به کاربر لو می‌دهد:
> «… S88 … نشان داد … روی **۱۵۰k کندل** ضرر می‌دهد (**net=−۲٬۰۸۳$، PF=۰.۹۳**). … رکوردِ رسمیِ
> سودِ خالص (**+۵۸٬۲۹۵$** = فقط لایه‌های Long) …»

سه دسته آمارِ ممنوع طبقِ «قانونِ مهمِ طراحیِ سایت» اینجا هست: (۱) شمارِ کندلِ بک‌تست (۱۵۰k)،
(۲) سود/زیانِ خالص و PF (net=−۲٬۰۸۳$، PF=۰.۹۳)، (۳) سودِ خالصِ کلِ پرتفوی (+۵۸٬۲۹۵$).

**مسیرِ نمایش:** `noEntryReason` در خروجیِ `analyze()` (خطِ ۳۳۶) قرار می‌گیرد. هرچند در مسیرِ
اصلیِ کارت، `router.ts` معمولاً `reason`ِ خودش را می‌سازد و این را بازنویسی می‌کند، اما در
مسیرِ `/api/analysis` (که خروجیِ خامِ `analyze` را می‌دهد) این رشته می‌تواند مستقیماً به سطحِ
نمایش برسد. صرفِ نظر از مسیر، وجودِ آمارِ داخلی در یک فیلدِ *نمایشی* (نه کامنت) نقضِ قانون است.

**تبصره:** کامنتِ خطِ ۱۷۶ («روی ۱۵۰k کندل PF=۱.۲۴، Sharpe=۲.۵۸») چون *کامنتِ کد* است نه متنِ
نمایشی، مشمولِ قانونِ طراحی نیست و باگ محسوب نمی‌شود (فقط برای شفافیت ذکر شد).

**چرا مهم است:** همان دلایلِ BUG-018 — نقضِ قانونِ صریحِ طراحی + گمراهیِ کاربر + کهنه‌شدنِ
اعدادِ hard-code شده با تغییرِ آیندهٔ تحقیق.

**پیشنهادِ رفع:** رشتهٔ خطوط ۲۷۰–۲۷۵ به توضیحِ کیفی بازنویسی شود (مثلاً «طلا بایاسِ صعودیِ
ساختاری دارد و short کردنش در آزمون‌های تاریخی لبهٔ پایدار نداشت؛ پس مغزِ طلا فقط Long است»)
بدونِ ذکرِ هیچ عدد net/PF/شمارِ کندل. این مورد باید به همان دستهٔ رفعِ BUG-018 افزوده شود.

**وضعیت:** 🆕 ثبت‌شده.

---

### BUG-020 — 🔴🔴 CRITICAL — تکرارِ باگِ ۱۰×ِ pip طلا در لایهٔ S211 (`triple_sma_pullback.ts`) — TP/SL واقعیِ کاربر ۱۰ برابر کوچک‌تر از بک‌تست 🆕
**فایل‌ها:** `web_tool/src/triple_sma_pullback.ts` (خطوط ۳۴، ۱۱۵–۱۱۹) در برابر منبعِ حقیقتِ
بک‌تست `strategies/s211_final_triple_sma_all_tf.py` (خطِ ۷۶: `'M15': dict(sl=150, tp=300, mh=32)`)
که از `engine/scalp_engine.py` (خطِ ۷۱: `XAUUSD … pip=0.10`) و `simulate_trades(sl_pip, tp_pip, …)`
استفاده می‌کند.

**شرح:** این هم‌خانوادهٔ BUG-013 است اما در فایلی **کاملاً متفاوت** که BUG-013 آن را ممیزی
نکرده بود. زنجیرهٔ حقیقت:

| مرجع | تبدیلِ `sl=150` طلا به قیمت |
|---|---|
| بک‌تستِ پایتونِ S211 (`scalp_engine.simulate_trades`, `pip=0.10`) | `SL = 150 pip × 0.10 = **$15.00**` , `TP = 300 × 0.10 = **$30.00**` |
| انجینِ لایوِ TS (`triple_sma_pullback.ts` خطوط ۱۱۸–۱۱۹) | `sl = pNow − 150 × **0.01** = **$1.50**` , `tp = pNow + 300 × 0.01 = **$3.00**` |

یعنی همان لایهٔ S211 که در رکورد با «net +$6,338 ، WR 51.7٪» ثبت شده (روی SL=$15/TP=$30 بک‌تست)،
در محصولِ زنده با **TP/SL دقیقاً ۱۰ برابر کوچک‌تر ($1.50/$3.00)** به کاربر پیشنهاد می‌شود.

**ریشهٔ خطا — سردرگمیِ «point» در برابر «pip»:** کامنتِ خطِ ۳۴ می‌گوید «`slPip: 150 point (واحدِ
point طلا = 0.01$)`» و خطِ ۱۱۵–۱۱۶ می‌گوید «point طلا = 0.01$ ⇒ … SL=150 point = 1.50$». این
تفسیر **غلط** است: در بک‌تستِ S211 عددِ ۱۵۰ به‌عنوانِ `sl_pip` (نه point) به موتوری داده شده که
`pip=0.10` دارد ⇒ SL واقعیِ بک‌تست $15 بوده، نه $1.50. نویسندهٔ TS اشتباهاً ۱۵۰ را «point»
(۰.۰۱$) تفسیر کرده و در ۰.۰۱ ضرب کرده.

**چرا CRITICAL (عیناً دلایلِ BUG-013):**
1. کاربر مستقیماً این TP/SL را از سایت می‌گیرد؛ SL=$1.50 روی طلایی که ATR(M15)‌اش چند دلار است،
   بلافاصله با نویز/اسپرد (اسپردِ خودِ طلا ۰.۳۳$) می‌خورد ⇒ زنجیرهٔ استاپِ زودهنگام.
2. R:R نمایشی «۱:۲» درست می‌ماند اما مقادیرِ مطلق بی‌ربط به بک‌تست‌اند ⇒ ادعای «net +$6,338 /
   WR 51.7٪» برای کاربرِ زنده کاملاً بی‌اعتبار می‌شود.
3. رشتهٔ `reason` (خطِ ۱۲۲) این اعدادِ غلط ($1.50/$3.00) را صریحاً به کاربر نشان می‌دهد.

**پیشنهادِ رفع:** ضریبِ خطوط ۱۱۸–۱۱۹ از `0.01` به `0.10` تغییر کند (تا با `XAU_PIP=0.10`ِ
بک‌تست هم‌واحد شود: SL=$15، TP=$30)، و کامنت‌های خطوط ۳۴/۱۱۵–۱۱۶ اصلاح شوند («۱۵۰ pip موتور،
هر pip=۰.۱۰$»). این رفع باید هم‌زمان با رفعِ BUG-013 و با یک منبعِ‌حقیقتِ واحدِ pip (مثلاً
مقدارِ مشترک از `market_spec`) انجام شود تا دوباره در جهتِ عکس ۱۰× نشکند.

**تبصرهٔ بررسیِ بعدی:** باید همهٔ لایه‌های دیگرِ طلا که مقادیرِ `*Pip * 0.01` دارند نیز ممیزی
شوند (مثلِ `gold_channel.ts`, `gold_trend_line.ts`, `gold_m30_router.ts`, `brooks_high2.ts`,
`squeeze_breakout.ts`) — احتمالِ باگ‌های خواهرِ بیشتر بالاست.

**وضعیت:** 🆕 ثبت‌شده — نیازمندِ رفعِ فوری و ممیزیِ کاملِ واحدِ pip در همهٔ لایه‌های طلای TS.

---

### BUG-021 — 🔴🔴 CRITICAL — باگِ ۱۰×ِ pip طلا در لایهٔ Brooks-High2 (`brooks_high2.ts`، S168) 🆕
**فایل‌ها:** `web_tool/src/brooks_high2.ts` (خطوط ۱۸–۲۴، ۵۴–۵۵، و متنِ نمایشیِ ۹۹) در برابر
منبعِ حقیقتِ بک‌تست `strategies/s168_brooks_high2_low2.py` (خطِ ۱۵۸: گریدِ برندهٔ XAUUSD
`(300, 450)`) که از `engine/scalp_engine.py` (`XAUUSD … pip=0.10`) و `se.simulate_trades(…,
sl_pip, tp_pip, …)` استفاده می‌کند (خطوط ۱۲۶–۱۲۷).

**شرح:** هم‌خانوادهٔ BUG-013/BUG-020 اما با «تلهٔ point» صریح‌تر. زنجیرهٔ حقیقت:

| مرجع | تبدیلِ `300/450` به قیمت |
|---|---|
| بک‌تستِ پایتونِ S168 (`scalp_engine`, `pip=0.10`) | `SL = 300 pip × 0.10 = **$30.00**` , `TP = 450 × 0.10 = **$45.00**` |
| انجینِ لایوِ TS (`brooks_high2.ts` خطوط ۲۴/۵۴–۵۵) | `POINT=0.01` ⇒ `slDist = 300 × 0.01 = **$3.00**` , `tpDist = 450 × 0.01 = **$4.50**` |

بک‌تستِ S168 عددِ ۳۰۰ را به‌عنوانِ `sl_pip` (pipِ موتور = ۰.۱۰$) داده ⇒ SL واقعیِ بک‌تست
**$30** بوده. اما TS آن را «point = ۰.۰۱$» تفسیر کرده ⇒ SL=**$3.00** به کاربر می‌دهد.
**یعنی TP/SL لایو دقیقاً ۱۰ برابر کوچک‌تر از بک‌تست است.**

**اثباتِ نمایشیِ باگ:** متنِ `reason` خطِ ۹۹ صریحاً به کاربر می‌گوید «SL=۳.۰۰$ زیرِ ورود،
TP=۴.۵۰$ بالای ورود (R:R=۱.۵)» — این همان عددِ غلطِ ۱۰× است که مستقیماً به کاربر نشان
داده می‌شود.

**چرا CRITICAL:** SL=$3.00 روی طلا (ATR M15 چند دلار، اسپرد ۰.۳۳$) تقریباً بلافاصله می‌خورد؛
رفتارِ واقعیِ معامله هیچ ربطی به WR/net بک‌تستِ ثبت‌شده ندارد.

**پیشنهادِ رفع:** `POINT` (خطِ ۲۴) از `0.01` به `0.10` تغییر کند (هم‌واحد با `XAU_PIP`ِ بک‌تست)،
و کامنت‌های خطوط ۱۸/۸ و متنِ `reason` خطِ ۹۹ به «SL=۳۰.۰۰$ / TP=۴۵.۰۰$» اصلاح شوند. یا نامِ
ثابت‌ها از `*_POINT` به `*_PIP` تغییر کند تا سردرگمی برطرف شود. تک-منبعِ‌حقیقتِ pip الزامی است.

**وضعیت:** 🆕 ثبت‌شده — نیازمندِ رفعِ فوری.

---

### BUG-022 — 🔴🔴 CRITICAL — باگِ ۱۰×ِ pip طلا در لایهٔ Signs-of-Strength (`signs_of_strength.ts`، S171) 🆕
**فایل‌ها:** `web_tool/src/signs_of_strength.ts` (خطوط ۲۰–۲۷، ۱۰۸–۱۰۹، متنِ نمایشیِ ۱۴۴–۱۴۵)
در برابر منبعِ حقیقتِ بک‌تست S171 (`s171_brooks_signs_of_strength_filter.py` /
`s171_sos_standalone_validate.py`) که مانندِ S168 از `scalp_engine` با `XAUUSD pip=0.10` و
`sl_pip/tp_pip` استفاده می‌کند.

**شرح:** دقیقاً همان تلهٔ BUG-021. `SOS_SL_POINT=300`, `SOS_TP_POINT=450` (خطوط ۲۱–۲۲) با
`POINT=0.01` (خطِ ۲۷) ضرب می‌شوند:

| مرجع | تبدیلِ `300/450` |
|---|---|
| بک‌تستِ پایتونِ S171 (`scalp_engine`, `pip=0.10`) | `SL = **$30.00**` , `TP = **$45.00**` |
| انجینِ لایوِ TS (خطوط ۱۰۸–۱۰۹) | `slDist = 300 × 0.01 = **$3.00**` , `tpDist = 450 × 0.01 = **$4.50**` |

⇒ TP/SL لایو ۱۰ برابر کوچک‌تر از بک‌تست. متنِ `reason` خطِ ۱۴۴–۱۴۵ نیز عددِ غلطِ «SL=۳.۰۰$ /
TP=۴.۵۰$» را به کاربر نشان می‌دهد (نقضِ صحتِ سیگنال + باگِ ۱۰×).

**چرا CRITICAL:** عیناً دلایلِ BUG-021 (SL بسیار کوچک‌تر از ATR/اسپرد ⇒ استاپِ زودهنگام،
واگراییِ کاملِ رفتارِ لایو از بک‌تست).

**پیشنهادِ رفع:** `POINT` (خطِ ۲۷) به `0.10` اصلاح شود و متن/کامنت‌های نمایشی به $30/$45 به‌روز
شوند. ترجیحاً نامِ `*_POINT`→`*_PIP`. با رفعِ BUG-013/020/021 هم‌زمان و با تک-منبعِ‌حقیقتِ pip.

**تبصرهٔ جمع‌بندیِ خانوادهٔ pip:** تا اینجا **دو الگوی متمایز** در لایه‌های طلای TS دیده شد —
گروهِ درست (`gold_channel.ts`, `gold_trend_line.ts`, `vwap_confluence.ts`: `PIP=0.10`) و گروهِ
باگ‌دارِ ۱۰× (`gold_m5_router.ts`/`gold_m5_late_entry.ts` [BUG-013]، `triple_sma_pullback.ts`
[BUG-020]، `brooks_high2.ts` [BUG-021]، `signs_of_strength.ts` [BUG-022]) که همگی از `0.01`
استفاده می‌کنند. باقی‌مانده برای ممیزی: `squeeze_breakout.ts` و `short_ma_confluence.ts`
(بررسیِ بعدی).

**وضعیت:** 🆕 ثبت‌شده — نیازمندِ رفعِ فوری.

---

### BUG-023 — 🟠 HIGH — تناقضِ داخلیِ ۱۰× در کارتِ S211: `sl` واقعی=۱.۵$ اما `trailDistPrice`=۱۵$ (فاصلهٔ trailing ۱۰ برابرِ خودِ SL) 🆕
**فایل:** `web_tool/src/router.ts` (بلوکِ تصمیمِ S211، خطوط ۱۰۳۵–۱۰۵۵؛ به‌ویژه ۱۰۴۸ در برابر
۱۰۳۶/۱۰۵۴) در تعامل با `web_tool/src/triple_sma_pullback.ts` (خطوط ۱۱۸–۱۱۹).

**شرح:** این باگ *نتیجهٔ مستقیمِ* ناسازگاریِ BUG-020 است اما یک تناقضِ **درون-کارتیِ** مجزا
می‌سازد که حتی اگر BUG-020 هنوز رفع نشده باشد، خودش یک خطای منطقیِ مستقل است. در همین بلوک دو
مسیرِ متفاوت برای «فاصلهٔ SL طلا» استفاده می‌شود:

| مقدار | منبع | محاسبه | نتیجه |
|---|---|---|---|
| `sl` واقعیِ معامله (خطِ ۱۰۵۴: `sl: tsma.sl`) | `triple_sma_pullback.ts` خطِ ۱۱۸ (`* 0.01`) | `entry − 150×0.01` | **SL = ۱.۵۰$** |
| `slDist` نمایشی (خطِ ۱۰۳۶) | `tsma.entry − tsma.sl` | `= ۱.۵۰$` | **۱.۵۰$** |
| `trailDistPrice` مدیریت (خطِ ۱۰۴۸) | `DEFAULT_TRIPLE_SMA.slPip * pip` با `pip=0.1` | `150×0.1` | **۱۵.۰۰$** |
| متنِ trailing (خطِ ۱۰۵۰) | `(slPip * pip).toFixed(1)` | `150×0.1` | **۱۵.۰$** |
| متنِ «SL اولیه» (خطِ ۱۰۴۹) | hard-code | — | **۱.۵$** |

یعنی در **یک کارتِ واحد**، به کاربر گفته می‌شود SL اولیه ۱.۵$ است، اما فاصلهٔ trailing‌ای که
باید پس از بریک‌ایون اعمال کند ۱۵$ (۱۰ برابرِ خودِ SL) است. این از نظرِ مدیریتِ معامله **بی‌معنی**
است: یک trailing-stop نباید ۱۰ برابر دورتر از حدِ ضررِ اولیه باشد؛ پس از بریک‌ایون، trailing
معمولاً نزدیک‌تر یا هم‌اندازهٔ ریسکِ اولیه است، نه ۱۰× دورتر (که عملاً یعنی هیچ حفاظتی ندارد و
معامله می‌تواند کلِ سودِ ۱R را پس بدهد).

**ریشه:** یک‌طرفِ کد (`tsma.sl`) از تبدیلِ غلطِ `×0.01` (BUG-020) تغذیه می‌شود و طرفِ دیگر
(`trailDistPrice`, `pip=0.1`) از تبدیلِ درست. چون این دو در یک بلوک کنارِ هم‌اند، ناسازگاری به
شکلِ یک تناقضِ آشکارِ ۱۰× در همان کارت ظاهر می‌شود.

**چرا HIGH:** حتی پس از رفعِ BUG-020، باید مطمئن شد هر دو مسیر از **یک** ثابت (با `pip=0.1`)
تغذیه شوند تا این تناقض بازنگردد. تا آن زمان کاربر یک SL بسیار کوچک و یک trailing بسیار بزرگ
می‌بیند که با هم ناسازگارند.

**پیشنهادِ رفع:** با رفعِ BUG-020 (تبدیلِ `triple_sma_pullback.ts` به `×0.10`)، `tsma.sl`
خودبه‌خود ۱۵$ می‌شود و با `trailDistPrice` هم‌تراز می‌گردد؛ سپس متنِ hard-codeی «۱.۵$» در خطِ
۱۰۴۹ باید به `${slDist.toFixed(2)}$` (یا مستقیماً `${(DEFAULT_TRIPLE_SMA.slPip*pip)}$`) تبدیل
شود تا دیگر هرگز از مقدارِ واقعی جدا نیفتد. توصیه: در کلِ بلوک فقط از یک منبع (`slPip*pip`)
برای هر سه (sl واقعی، slDist نمایشی، trailDistPrice) استفاده شود.

**وضعیت:** 🆕 ثبت‌شده — وابسته به رفعِ BUG-020؛ نیازمندِ یکسان‌سازیِ منبعِ SL در بلوکِ S211.

---

### BUG-024 — 🟠 HIGH — تناقضِ سیاستیِ منسوخ: `signal.ts` به کاربر می‌گوید «طلا هرگز SHORT نمی‌دهد» اما router/secondary یک لایهٔ SHORTِ پذیرفته‌شده (S97–S102) را فعال و نمایش می‌دهند 🆕
**فایل‌ها:** `web_tool/src/signal.ts` (متنِ `noEntryReason`، خطوط ۲۶۹–۲۷۵) در برابر
`web_tool/src/router.ts` (بلوکِ SHORT-MA، خطوط ۷۰۴–۷۳۰+) و `web_tool/src/secondary_layers.ts`
(خطوط ۱۱۹–۱۳۳) و رکوردهای رسمیِ `results/ShortMAConfluence_Trailing_NetProfit_76082.md` /
`results/ShortExitLetWinnersRun_NetProfit_95645.md` / `results/ShortExitReopt_Recent3y_NetProfit_88955.md`.

**شرح:** در `signal.ts`، وقتی رژیمِ نزولی تشخیص داده می‌شود، متنِ `noEntryReason` که به کاربر
نشان داده می‌شود صریحاً و با قاطعیت اعلام می‌کند:
> «چرا سایت SHORT نمی‌دهد؟ … طلا داراییِ با بایاسِ صعودیِ ساختاری است؛ short کردنش ذاتاً
> بازنده است (قانونِ L53). **پس مغزِ طلا فقط Long است.**»

**اما** این ادعا با وضعیتِ فعلیِ پروژه در تضاد است:
1. `router.ts` خطِ ۷۰۴: `if (spec.id === 'XAUUSD' && reg.activeStream !== 'bull')` یک لایهٔ کاملِ
   **SHORT** (S97–S102، MA-Confluence) دارد که `state:'ENTRY', direction:'SHORT'` تولید می‌کند و
   TP/SL/لات به کاربر می‌دهد (خطوط ۷۲۰+).
2. `secondary_layers.ts` خطوط ۱۲۲–۱۲۳ همان لایه را به‌صورتِ `SHORT ENTRY` در فهرستِ ثانویه
   نمایش می‌دهد.
3. این لایه **رسماً در رکوردِ سودِ خالص پذیرفته شده** است: `ShortMAConfluence_Trailing_
   NetProfit_76082.md` و ارتقاهایش (`ShortExitLetWinnersRun_NetProfit_95645.md` سهمِ SHORT
   +$۳۴٬۵۴۲؛ `ShortExitReopt_Recent3y_NetProfit_88955.md`) — هر دو نیمهٔ داده مثبت، WF معتبر.

پس متنِ `signal.ts` **منسوخ** است: زمانی که نوشته شد (نشستِ S83/S88) هنوز SHORTِ سودده کشف
نشده بود، اما بعداً S97–S102 یک لبهٔ SHORTِ معتبر یافت و به پرتفوی افزوده شد. متن به‌روزرسانی نشد.

**چرا HIGH:** ۱) کاربر می‌تواند در یک لحظه هم پیامِ «سایت هرگز طلا را short نمی‌کند» (از مسیرِ
`signal.ts`/`/api/analysis`) و هم یک سیگنالِ **SHORT ENTRY** (از مسیرِ router/کارتِ اصلی) ببیند —
تناقضِ مستقیم که اعتماد و صحتِ سیگنال را نابود می‌کند. ۲) اطلاعاتِ غلط: ادعای «short طلا ذاتاً
بازنده است» دیگر درست نیست؛ خودِ پروژه خلافش را اثبات و ثبت کرده. ۳) این همان دستهٔ باگِ
«hard-code شده و کهنه‌شده» است (مثلِ BUG-015/017/019) — علاوه بر آن، متن آمارِ داخلیِ
`net=−۲٬۰۸۳$, PF=۰.۹۳, ۱۵۰k کندل` را هم لو می‌دهد (هم‌پوشان با BUG-019).

**پیشنهادِ رفع:** متنِ `noEntryReason`ِ رژیمِ نزولی در `signal.ts` (خطوط ۲۶۹–۲۷۵) بازنویسی شود تا
با سیاستِ فعلی هم‌خوان گردد: در رژیمِ نزولی، سایت SHORT را از طریقِ لایهٔ S97–S102 (نه جریانِ خامِ
bearScore) ارزیابی می‌کند؛ پس متن نباید بگوید «هرگز short نمی‌دهیم». ترجیحاً کلِ شاخهٔ
`signal.ts:bear` با مسیرِ router یکپارچه شود تا یک منبعِ واحدِ حقیقت برای پیامِ SHORT وجود داشته
باشد (و آمارِ داخلیِ BUG-019 هم هم‌زمان پاک شود).

**وضعیت:** 🆕 ثبت‌شده — تناقضِ سیاستیِ منسوخ بین دو مسیرِ تصمیم.
---

### BUG-025 — 🟠 HIGH — نبودِ cooldown پس از بستنِ معامله ⇒ سایت بلافاصله همان معامله را دوباره پیشنهاد می‌دهد (حلقهٔ چرن) 🆕
**فایل‌ها:** `web_tool/public/static/app.js` (هندلرِ بستنِ معامله، خطوط ۸۷۹–۸۸۷ و بستنِ اسکالپ
۹۰۹–۹۱۶) در تعامل با `web_tool/public/static/signal_latch.js` (منطقِ `computeLatched`، خطوط
۳۰ و ۴۵–۵۲).

**شرح:** وقتی کاربر یک معاملهٔ باز را می‌بندد، هندلرِ `.btn-close` فقط `setTrade(asset, null)`
را صدا می‌زند (خطِ ۸۸۲) و **نه latch را پاک می‌کند نه هیچ cooldownـی می‌گذارد**. زنجیرهٔ باگ:

1. حین بازبودنِ معامله، `hasTrade=true` ⇒ در `signal_latch.js` خطِ ۳۰ قفل null است (MANAGE فرمان است).
2. کاربر معامله را می‌بندد ⇒ `setTrade(asset,null)` ⇒ `hasTrade=false`.
3. در رفرشِ بعدی، اگر سرور هنوز `state==='ENTRY'` با همان جهت بدهد (کاملاً محتمل، چون شرایطِ
   بازار در چند ثانیه عوض نشده)، `computeLatched` خطوط ۴۵–۵۲ یک **قفلِ تازه** با
   `createdAt: now` می‌سازد ⇒ کارت **فوراً دوباره «ورود به معاملهٔ LONG/SHORT»** را نمایش می‌دهد.

**نتیجه:** کاربری که تازه معامله‌ای را بسته (چه با ضرر، چه چون تشخیص داده روند برمی‌گردد)،
**بلافاصله دوباره به همان معامله دعوت می‌شود**. این یک **حلقهٔ ورود→خروج→ورود (چرن)** می‌سازد که
اسپرد ($۰.۳۳/اونس طلا) و مارجین را هدر می‌دهد و از نظرِ منطقِ معامله‌گری کاملاً غلط است. مدلِ
چهار-حالتهٔ سایت انتظار دارد پس از «مدیریت معامله»→«بستن»، کارت به «خنثی» برگردد و منتظرِ یک
**ستاپِ جدید** بماند، نه اینکه همان ستاپِ قبلی را بازتولید کند.

**تأییدِ دامنه:** بستنِ اسکالپ (خطوط ۹۱۰–۹۱۵) هم همین نقص را دارد — `setLatch` صدا زده نمی‌شود.
جالب اینکه در *ثبتِ* اسکالپ (خطِ ۹۰۱) `setLatch(asset, null)` هست، اما در *بستن* نیست ⇒ ناسازگاری.

**چرا HIGH:** مستقیماً روی تجربه و سرمایهٔ کاربر اثر می‌گذارد (چرنِ مکرر)، ناقضِ منطقِ چهار-حالتهٔ
سایت است، و از نظرِ کاربر یک رفتارِ «مصرّانه و بی‌منطق» دیده می‌شود (سایت اصرار به بازکردنِ همان
معامله دارد).

**پیشنهادِ رفع:** در هر دو هندلرِ بستن (`.btn-close` و `.btn-scalp-close`) پس از `setTrade(...,null)`،
یک مُهرِ زمانیِ cooldown ذخیره شود (مثلاً `localStorage['cooldown_'+asset]=Date.now()`) و در
`computeLatched` (یا در `applyLatch` سمتِ app.js) تا پایانِ پنجرهٔ cooldown (مثلاً چند کندلِ همان
تایم‌فریم، یا حداقل تا وقتی که یک حالتِ non-ENTRY دیده شود) قفلِ تازه ساخته نشود. حداقل‌رفع:
پس از بستن، قفلِ تازه فقط وقتی مجاز باشد که بین بستن و ENTRYِ جدید دستِ‌کم یک نمونهٔ NEUTRAL/
non-ENTRY فاصله افتاده باشد (لبهٔ نزولی، نه صرفِ برقراریِ شرط).

**وضعیت:** 🆕 ثبت‌شده — باگِ منطقِ چرخهٔ معامله؛ نیازمندِ cooldown/edge-trigger پس از بستن.
---

### BUG-026 — 🟠 HIGH — دورهٔ تنفس (grace) و واحدِ `barsHeld` ثابتِ «M15» برای همهٔ تایم‌فریم‌ها (نقضِ اشتباهِ رایجِ «TP/SL یکسان برای همه TF») 🆕
**فایل:** `web_tool/src/trade_manager.ts` (خطِ ۲۰۷ `withinGraceTime`، و خطِ ۳۵ تعریفِ `barsHeld`؛
تابعِ `evaluateTrade` که در `index.tsx` خطِ ۳۵۵ برای **همهٔ** کارت‌ها صدا زده می‌شود).

**شرح:** تابعِ `evaluateTrade` یک منطقِ واحد است که برای همهٔ تایم‌فریم‌ها (EURUSD/XAUUSD روی
M1, M5, M15, M30, H1, H4, D1) اجرا می‌شود، اما دو ثابتِ **زمان-وابسته** را روی «M15» هارد-کد کرده:

1. **دورهٔ تنفس (grace):** خطِ ۲۰۷:
   ```js
   const withinGraceTime = ageSec < 45 * 60   // < ۴۵ دقیقه (۳ کندلِ M15)
   ```
   این «۴۵ دقیقه = ۳ کندلِ M15» را فرض می‌کند. برای معاملهٔ روی **D1/H4** این دورهٔ تنفس
   بی‌معنا کوتاه است (۴۵ دقیقه ≪ یک کندل ⇒ عملاً هیچ graceـی نیست و توصیهٔ «ببند» خیلی زود
   فعال می‌شود). برای **M1** برعکس، ۴۵ دقیقه = **۴۵ کندل** grace ⇒ معاملهٔ اسکالپِ M1 تقریباً
   تا انتهای عمرش «تازه» فرض می‌شود و هیچ هشدارِ مدیریتی نمی‌گیرد. دقیقاً همان اشتباهِ رایجِ
   «TP/SL (و اینجا: پارامترهای مدیریت) یکسان برای همهٔ تایم‌فریم‌ها».

2. **واحدِ `barsHeld`:** خطِ ۳۵ صراحتاً می‌گوید «تعداد کندلِ **M15** که معامله باز بوده». اما
   همین `barsHeld` در `layerAwareAdvices` (خطِ ۱۴۴) و بلوکِ SHORT-MA (خطِ ۳۵۱) با
   `maxHoldBars` مقایسه می‌شود. اگر کارتِ M5 یا M30 یا H1 مقدارِ `barsHeld` را بر حسبِ کندلِ
   *خودش* بفرستد (که منطقی است)، مقایسه با `maxHoldBars`ـی که برای M15 تنظیم شده غلط می‌شود
   (مثلاً ۴۸ کندلِ M15 = ۱۲ ساعت، اما ۴۸ کندلِ M5 = ۴ ساعت ⇒ سقفِ نگه‌داریِ کاملاً متفاوت).

**چرا HIGH:** مدیریتِ معامله (بخشِ اصلیِ چهار-حالتهٔ سایت طبقِ تعریفِ پروژه) روی تایم‌فریم‌های
غیرِ M15 رفتارِ غلط می‌دهد: یا خیلی زود «ببند» می‌گوید (TFهای بزرگ) یا هرگز هشدار نمی‌دهد
(TFهای کوچک). این مستقیماً کیفیتِ مهم‌ترین بخشِ تعاملِ کاربر را خراب می‌کند.

**پیشنهادِ رفع:** دورهٔ تنفس باید **بر حسبِ تعدادِ کندلِ همان تایم‌فریم** تعریف شود، نه دقیقهٔ
ثابت: `graceBars × tfMinutes × 60`. تابع باید `tfMinutes` (یا مستقیماً `graceSeconds`
مخصوصِ کارت) را به‌عنوان ورودی بگیرد (از `ASSETS[i].tf`/`layer` در `index.tsx`). همچنین واحدِ
`barsHeld` باید صریحاً «کندلِ همان کارت» باشد و `maxHoldBars`ِ هر لایه هم به همان واحد. این با
«قانونِ اولِ مولتی‌تایم‌فریم» و اجتناب از «اشتباهِ رایجِ TP/SL یکسان» هم‌خوان می‌شود.

**وضعیت:** 🆕 ثبت‌شده — پارامترِ مدیریتِ زمان-وابسته باید per-TF شود.
---

### BUG-027 — 🟡 MEDIUM (نهفته/latent) — تبدیلِ pip سخت‌کدشدهٔ `×10` در متنِ trailingِ تابعِ مشترکِ `layerAwareAdvices` (فرضِ «همه طلا») 🆕
**فایل:** `web_tool/src/trade_manager.ts` (خطِ ۱۳۰ در `layerAwareAdvices`).

**شرح:** در بلوکِ trailingِ لایه-محور، متنِ نمایشیِ فاصلهٔ pip چنین ساخته می‌شود:
```js
: `${round2(trailDist)} واحدِ قیمت (~${Math.round(trailDist * 10)}pip)`
```
ضریبِ `× 10` تبدیلِ «واحدِ قیمت → pip» را انجام می‌دهد و **فرض می‌کند ۱ pip = ۰.۱ واحدِ قیمت**
— که فقط برای **طلا** درست است. تابعِ `layerAwareAdvices` اما **generic** است و برای هر
دارایی (از جمله EURUSD که ۱ pip = ۰.۰۰۰۱ واحد ⇒ باید `× 10000` باشد) صدا زده می‌شود. اگر یک
لایهٔ EURUSD با سبکِ trailing (`let-run-trail`/`structural-trail`/`regime-atr-trail`) و
`trailDistPrice` تعریف شود، متنِ pip **۱۰۰۰ برابر غلط** به کاربر نشان داده می‌شود.

**چرا فعلاً trigger نمی‌شود (اما باگ است):** همهٔ لایه‌های فعلیِ EURUSD در `eurusd_router.ts`
(خطوط ۲۶۷، ۳۳۵، ۴۶۸) از `style: 'fixed-tp-sl'` استفاده می‌کنند که در خطوط ۹۸–۱۰۶ زودتر
return می‌شود و هرگز به بلوکِ trailing (خطِ ۱۲۰+) نمی‌رسد. پس در وضعیتِ فعلی این مسیر برای
EURUSD اجرا نمی‌شود — **اما** به محضِ افزودنِ یک لایهٔ EURUSD با trailing (که کاملاً محتمل است،
چون هدفِ پروژه یافتنِ لایه‌های جدید است)، این باگ بی‌سروصدا فعال و pipِ غلط نمایش داده می‌شود.

**ارتباط با خانوادهٔ pip:** این هم‌ریشه با BUG-013/020/021/022 است — نبودِ **تک‌منبعِ‌حقیقتِ
`pipSize` برای هر دارایی**. یک ثابتِ سراسری مثلِ `PIP_SIZE[asset]` باید همه‌جا (سرور، لایه‌ها،
متن‌های نمایشی) استفاده شود تا این فرض‌های سخت‌کدشده حذف شوند.

**پیشنهادِ رفع:** `layerAwareAdvices` باید `pipSize` (یا `isGold`) دارایی را ورودی بگیرد و
تبدیل را `round(trailDist / pipSize)` کند، نه `× 10` ثابت. یا اصلاً واحدِ «pip» از متن حذف و
فقط «واحدِ قیمت» گفته شود تا هیچ فرضِ داراییِ سخت‌کدشده نماند.

**وضعیت:** 🆕 ثبت‌شده — باگِ نهفته؛ با رفعِ خانوادهٔ pip (تک‌منبع `pipSize`) هم‌زمان رفع شود.
---

### BUG-028 — 🟠 HIGH — endpointِ `/api/trade/advice` برای همهٔ کارت‌های طلا همیشه دادهٔ M15 را تحلیل می‌کند (مدیریتِ معاملهٔ M30/H1/H4/D1 روی تایم‌فریمِ غلط) 🆕
**فایل:** `web_tool/src/index.tsx` (endpointِ `/api/trade/advice`، خطوط ۳۳۵ و ۳۴۳) در تعامل با
`app.js` خطِ ۱۱۰۴ (`barsHeld` با `/900`).

**شرح:** بخشِ «مدیریتِ معامله» (یکی از چهار حالتِ اصلیِ سایت) از طریقِ `refreshAdvice` →
`POST /api/trade/advice` کار می‌کند. اما این endpoint **بدونِ توجه به تایم‌فریمِ کارت**، همیشه
دادهٔ **M15** را می‌گیرد و تحلیل می‌کند:
```ts
if (meta_asset.isGold) { const { candles } = await fetchGold('15m', '1mo'); … }   // خطِ ۳۳۵
else { const { candles } = await yahooCandles(meta_asset.symbol, '15m', '1mo'); } // خطِ ۳۴۳
```
هیچ شاخه‌ای بر اساسِ `meta_asset.layer` یا `meta_asset.tf` وجود ندارد. در نتیجه برای کارت‌های
**XAUUSD-M30 / -H1 / -H4 / -D1** (که `layer` آن‌ها `swing-m30`/`htf` است و از همین مسیرِ
غیر-scalp عبور می‌کنند)، کلِ تحلیلِ مدیریت — `atr`, `trend`, `macdHist`, `rsi14`, `adx`,
`ema50/200` — از **M15** می‌آید، نه از تایم‌فریمِ خودِ کارت.

**اثرِ عملی:** کاربری که روی کارتِ **D1** طلا معامله باز کرده، توصیه‌های trailing/بریک‌ایون/
معکوس‌شدنِ روند را بر پایهٔ **ATR و روندِ M15** می‌گیرد. ATRِ M15 طلا حدوداً ۱۰–۲۰ برابر
کوچک‌تر از ATRِ D1 است ⇒ `neutralBand` (خطِ ۲۰۴ در trade_manager)، فاصلهٔ trailing (۱.۵×ATR)،
و آستانه‌های سود/زیان همگی ۱۰–۲۰ برابر کوچک‌تر از واقعیتِ D1 محاسبه می‌شوند ⇒ توصیه‌ها کاملاً
بی‌ربط و گمراه‌کننده‌اند (مثلاً «به ۳R سود رسیدی، بریک‌ایون کن» وقتی واقعاً هنوز داخلِ نویزِ D1
است). این هم‌خانوادهٔ BUG-026 است (زمان-وابستگیِ M15) اما ریشهٔ متفاوت: **داده/اندیکاتورِ اشتباه**
نه فقط شمارشِ کندل.

**تأییدِ مکمل:** `app.js` خطِ ۱۱۰۴ نیز `barsHeld = ⌊(now−openedAt)/900⌋` را با ۹۰۰ ثانیه
(کندلِ M15) برای همهٔ کارت‌های غیر-scalp حساب می‌کند ⇒ برای M30/H1/H4/D1 هم `barsHeld` غلط است
(نقضِ maxHoldBars لایه).

**چرا HIGH:** «مدیریتِ معامله» طبقِ تعریفِ پروژه مهم‌ترین/اصلی‌ترین کارِ سایت است و اینجا برای
تمامِ کارت‌های بالاتر از M15 روی تایم‌فریمِ غلط اجرا می‌شود.

**پیشنهادِ رفع:** endpoint باید تایم‌فریمِ داده را از `meta_asset.tf`/`meta_asset.layer` نگاشت
کند (M30→`30m`, H1→`60m`, H4→… , D1→`1d`) و همان را `fetchGold`/`yahooCandles` بدهد؛ و
`barsHeld` در `app.js` باید بر ثانیهٔ کندلِ همان تایم‌فریم تقسیم شود (نه ثابتِ ۹۰۰). این رفع باید
با BUG-026 هماهنگ شود (تک‌منبعِ `tfSeconds` برای هر کارت).

**وضعیت:** 🆕 ثبت‌شده — نیازمندِ نگاشتِ تایم‌فریمِ per-card در endpointِ advice.
---

### BUG-029 — 🔴🔴 CRITICAL — endpointِ `/api/scalp/manage` برای کارت‌های اسکالپِ EURUSD هم دادهٔ **طلا** را می‌گیرد (مقایسهٔ refPriceِ ۱.۰۸ یورو با livePriceِ ۲۶۵۰ طلا) 🆕
**فایل‌ها:** `web_tool/src/index.tsx` (endpointِ `/api/scalp/manage`، خطوط ۳۷۷–۴۰۰؛ به‌ویژه خطِ
۳۹۰) و `web_tool/public/static/app.js` (`refreshScalpManage`، بدنهٔ درخواست بدونِ `asset`).

**شرح:** endpointِ مدیریتِ اسکالپ **هیچ‌گاه `body.asset` را نمی‌خواند** و همیشه دادهٔ **M5 طلا**
را می‌گیرد:
```ts
const { candles } = await fetchGold('5m', '5d')   // خطِ ۳۹۰ — همیشه طلا
…
const livePrice = spot?.price ?? close[close.length - 1]   // قیمتِ طلا (~2650)
const res = manageGoldM5Scalp({ action, refPrice, livePrice, close, … })
```
سمتِ فرانت‌اند هم (`refreshScalpManage`) در بدنهٔ درخواست فقط `{ action, refPrice, tpPip, slPip }`
می‌فرستد و **`asset` را ارسال نمی‌کند**. پس endpoint اصلاً نمی‌داند دارایی چیست و پیش‌فرض همیشه طلاست.

**چرا CRITICAL:** طبقِ `index.tsx` دارایی‌های `layer:'scalp'` شاملِ **`EURUSD` (M5)** و
**`EURUSD-M15`** هستند و در `app.js` (خطِ ۴۳۱ `isScalp`, خطِ ۴۳۹) دقیقاً بخشِ
`renderScalpManage` → `/api/scalp/manage` را می‌گیرند. در نتیجه وقتی کاربر روی کارتِ **EURUSD**
یک معاملهٔ اسکالپ ثبت می‌کند:
- `refPrice` = قیمتِ ورودِ یورو (~**۱.۰۸**)،
- اما `livePrice` = قیمتِ زندهٔ **طلا** (~**۲۶۵۰**) و `close[]` = کندل‌های **طلا**.

`manageGoldM5Scalp` این دو را مقایسه می‌کند ⇒ انحرافِ نجومی ⇒ حالتِ `take_profit`/`wrong` کاملاً
بی‌معنا و تصادفی. کاربرِ EURUSD یک «مدیریتِ معاملهٔ» کاملاً غلط (بر پایهٔ طلا) دریافت می‌کند —
مهم‌ترین بخشِ تعاملیِ سایت برای این کارت‌ها **به‌کل خراب** است.

**تأییدِ دامنه:** `manageGoldM5Scalp` هم از نامش و هم از `pip`/آستانه‌هایش مخصوصِ طلاست؛ حتی اگر
داده درست می‌شد، منطقِ pip/آستانه‌اش برای یورو غلط بود (هم‌خانوادهٔ BUG-013/027 — نبودِ per-asset).

**پیشنهادِ رفع:** ۱) `app.js` باید `asset` را در بدنهٔ `/api/scalp/manage` بفرستد. ۲) endpoint باید
با `ASSETS.find(id)` تشخیص دهد دارایی طلا است یا فارکس و دادهٔ همان دارایی و تایم‌فریمِ همان کارت
را بگیرد (EURUSD-M5→`5m` یورو، EURUSD-M15→`15m` یورو). ۳) یک `manageScalp` عام با `pipSize`
per-asset جایگزینِ `manageGoldM5Scalp`ِ طلا-محور شود (هم‌راستا با تک‌منبعِ pip در رفعِ خانوادهٔ
BUG-013/020/021/022/027).

**وضعیت:** 🆕 ثبت‌شده — نیازمندِ رفعِ فوری؛ مدیریتِ اسکالپِ EURUSD کاملاً روی دادهٔ طلا اجرا می‌شود.
---

### BUG-030 — 🟠 HIGH — کارتِ H4 طلا: `gap=3600` (۱h) به `closedBars` داده می‌شود ولی کندل‌ها ۴ساعته‌اند ⇒ سیگنال روی کندلِ H4 **ناتمام** محاسبه می‌شود (repaint) 🆕
**فایل:** `web_tool/src/index.tsx` (جدولِ `GOLD_TF` خطِ ۵۳۴، تجمیعِ خطِ ۵۴۰، و مصرفِ
`closedBars(useCandles, tfc.gap)` خطِ ۵۵۳ + `rebaseFuturesToSpot(…, tfc.gap)` خطِ ۵۴۶؛ تعریفِ
`closedBars` خطوط ۲۲۷–۲۳۵).

**شرح:** برای کارتِ **XAUUSD-H4**، جدولِ `GOLD_TF` این است:
```ts
'XAUUSD-H4': { interval: '1h', range: '1y', gap: 3600 },  // H4 از تجمیعِ H1 ساخته می‌شود
```
داده با `interval='1h'` گرفته و سپس به کندلِ **۴ساعته** تجمیع می‌شود (خطِ ۵۴۰:
`aggregateCandles(rawCandles, 4)`). اما `tfc.gap` هنوز **۳۶۰۰** (۱ ساعت) است و همین به
`closedBars` داده می‌شود. تابعِ `closedBars` سطلِ زمانیِ جاری را چنین می‌سازد:
```ts
const curBucketStart = Math.floor(nowSec / intervalSec) * intervalSec   // intervalSec=3600
if (last.time >= curBucketStart) return candles.slice(0, -1)
```
با `intervalSec=3600`، مرزِ سطل **ساعتی** حساب می‌شود، نه ۴ساعته. یک کندلِ H4 که در ۰۸:۰۰ UTC
باز شده (و تا ۱۲:۰۰ در حالِ شکل‌گیری است)، اگر الان مثلاً ۰۹:۳۰ باشد:
`curBucketStart = ⌊09:30/1h⌋×1h = 09:00`؛ شرطِ `last.time(08:00) ≥ 09:00` **نادرست** است ⇒
کندل **حذف نمی‌شود** ⇒ کندلِ H4ِ **ناتمام** به‌عنوان «بسته‌شده» وارد منطقِ سیگنال (`sig`) می‌شود.

**نتیجه:** دقیقاً همان **repainting**ـی که کامنتِ خطوط ۵۴۹–۵۵۲ ادعا می‌کند رفع شده — برای کارتِ
H4 هنوز رخ می‌دهد. سیگنالِ H4 روی کندلِ در حالِ ساخت محاسبه می‌شود و با بسته‌شدنِ کندل تغییر
می‌کند (سیگنالِ ناپایدار/گمراه‌کننده). `rebaseFuturesToSpot` هم با `gap=3600` غلط، پنجرهٔ
هم‌ترازیِ spot را اشتباه می‌گیرد.

**چرا HIGH:** سیگنالِ ورودِ کارتِ H4 غیرِقابلِ‌اعتماد و repaint-دار است؛ کاربر ممکن است روی
سیگنالی وارد شود که چند دقیقه بعد ناپدید/عوض می‌شود. (کارت‌های دیگر سالم‌اند: M15 gap=900،
M30 gap=1800، H1 gap=3600، D1 gap=86400 همگی با intervalِ داده هم‌خوان‌اند — فقط H4 چون
تجمیع می‌شود ناسازگار است.)

**پیشنهادِ رفع:** پس از تجمیع به H4، مقدارِ gap باید **۱۴۴۰۰** (۴×۳۶۰۰) باشد. یا در `GOLD_TF`
یک فیلدِ جداگانهٔ `aggGap` اضافه شود و پس از `aggregateCandles`، متغیرِ محلیِ gap به ۱۴۴۰۰ ست
شود و همان به `rebaseFuturesToSpot` و `closedBars` داده شود.

**وضعیت:** 🆕 ثبت‌شده — ناسازگاریِ gap پس از تجمیعِ H4 ⇒ repaint در تنها کارتِ H4.

---

### BUG-031 — ⚠️ تکراری با BUG-014 (علامت‌گذاری‌شده) — کمیسیونِ `$7/لات` در سایت (`ASSET_SPECS`) در تضاد با مشخصاتِ حسابِ کاربر و موتورِ بک‌تست (هر دو کمیسیون=۰)

> **🔁 یادداشتِ تصحیحی (طبق قانونِ همپوشانی و دستورِ «هرگز باگِ تکراری»):**
> پس از ثبتِ BUG-031 کشف شد که این باگ ~۹۵٪ با **BUG-014** (ثبت‌شده در نشستِ پیشین) همپوشانی
> دارد: **همان فایل، همان خطوط (۲۴۸–۲۵۰)، همان علت** (کمیسیونِ $۷/لات در برابرِ کمیسیون=۰).
> تنها تفاوتِ ۵٪: BUG-031 علاوه بر `market_spec.py` به `scalp_engine.py` خطِ ۶۲ هم استناد
> کرد و اثرِ بزرگ‌شدنِ خطا روی اسکالپ‌ها (SL کوچک) را برجسته کرد. این تفاوت یک باگِ مستقل
> نیست. **مرجعِ معتبر = BUG-014.** این مدخل صرفاً برای تاریخچهٔ دفترچه نگه داشته می‌شود و
> نباید در شمارشِ باگ‌های یکتا حساب شود.

**فایل‌ها:** `web_tool/src/router.ts` (`ASSET_SPECS`، خطوط ۲۴۸–۲۵۰) در برابر منبعِ حقیقتِ
`engine/market_spec.py` (خطوط ۴۴، ۵۵) و `engine/scalp_engine.py` (خطِ ۶۲) و **مشخصاتِ صریحِ
حسابِ دموِ کاربر**.

**شرح:** مشخصاتِ حسابی که کاربر داده صراحتاً می‌گوید: «**کمیسیونِ جداگانه = ندارد**» و کلِ هزینه
فقط اسپردِ `0.33$/oz` است. موتورِ بک‌تست هم دقیقاً همین را پیاده کرده:
- `market_spec.py` خطِ ۴۴: `XAU_COMMISSION_PER_LOT = 0.0`
- `market_spec.py` خطِ ۵۵: `EUR_COMMISSION_PER_LOT = 0.0`
- `scalp_engine.py` خطِ ۶۲: صراحتاً می‌گوید فرضِ قدیمیِ `comm=7` **منسوخ** شد و اکنون کمیسیون صفر
  و هزینه فقط اسپرد است.

**اما** سایتِ زنده هنوز کمیسیونِ **$۷/لات** فرض می‌کند:
```ts
XAUUSD: { … commissionPerLot: 7.0, … },   // خطِ ۲۴۸
EURUSD: { … commissionPerLot: 7.0, … },   // خطِ ۲۴۹
AUDUSD: { … commissionPerLot: 7.0, … },   // خطِ ۲۵۰
```
این مقدار در `computeLots` خطِ ۲۸۱ استفاده می‌شود:
```ts
const lossPerLotAtSl = slDist * spec.valuePerPricePerLot + spec.commissionPerLot
lots = riskDollars / lossPerLotAtSl
```

**اثرِ عملی:**
1. **حجمِ لاتِ غلط:** `lossPerLotAtSl` با افزودنِ $۷ بیش‌برآورد می‌شود ⇒ `lots` کوچک‌تر از واقعی
   محاسبه و به کاربر پیشنهاد می‌شود. برای SLهای کوچک (اسکالپ) که `slDist × valuePerPricePerLot`
   خودش کوچک است، سهمِ $۷ نسبتاً بزرگ می‌شود و خطای لات چشمگیر است.
2. **ناسازگاری با رکوردِ سودِ خالص:** همهٔ اعدادِ net/WRِ ثبت‌شده در `results/` با کمیسیون=۰ به‌دست
   آمده‌اند؛ سایتی که با کمیسیون=۷ حجم می‌دهد، رفتاری متفاوت از بک‌تست تولید می‌کند.
3. **ریسکِ دلاریِ نمایشی:** متنِ «اگر SL بخورد ~X$ ضرر می‌کنید» بر پایهٔ همین مدلِ غلط ساخته می‌شود.

**چرا HIGH:** مستقیماً حجمِ معامله و ریسکِ دلاریِ پیشنهادی به کاربر را تحریف می‌کند و با هم
مشخصاتِ حسابِ کاربر و هم منبعِ حقیقتِ بک‌تست در تضاد است (نوعِ باگِ «پارامترِ کهنه/ناهماهنگ با
منبعِ حقیقت»، هم‌خانوادهٔ BUG-013/020).

**پیشنهادِ رفع:** `commissionPerLot` برای XAUUSD/EURUSD/AUDUSD به **۰** تغییر کند (هم‌راستا با
`market_spec.py`). در عوض، اگر هزینهٔ اسپرد در نمایش لازم است، باید به‌صورتِ اسپردِ per-price
(۰.۳۳$/oz طلا، ~۱.۲pip یورو) مدل شود، نه کمیسیونِ ثابتِ per-lot. ایده‌آل: `ASSET_SPECS` سایت از
همان اعدادِ `market_spec.py` تغذیه شود تا تک‌منبعِ‌حقیقت حفظ گردد.

**وضعیت:** 🆕 ثبت‌شده — پارامترِ هزینهٔ ناهماهنگ با مشخصاتِ حساب و موتورِ بک‌تست.

---

### BUG-032 — 🟡 MEDIUM — اسپرد (۰.۳۳$/oz طلا) در قیمتِ ورود/TP/SLِ نمایشی مدل نمی‌شود ⇒ TP خوش‌بینانه‌تر از بک‌تست 🆕
**فایل‌ها:** `web_tool/src/router.ts` (همهٔ لایه‌ها: `entry = a.price`، خطوط ۳۸۵/۵۱۷/۶۲۹/۷۲۳/
۸۳۷/۹۳۴/۹۸۱/۱۱۲۹) و `triple_sma_pullback.ts` (خطِ ۱۱۷ `entry = pNow`) در برابر منبعِ حقیقتِ
`engine/market_spec.py` (`XAU_SPREAD_PRICE = 0.35`, `EUR_SPREAD_PRICE = 0.00012`).

**شرح:** موتورِ بک‌تست هزینهٔ اسپرد را در هر معامله لحاظ می‌کند (طلا ۰.۳۵$/oz ≈ اسپردِ واقعیِ
۰.۳۳$؛ یورو ~۱.۲pip). اما در سایتِ زنده، قیمتِ ورود همه‌جا برابرِ **قیمتِ میانی** (`a.price`/
`close`) گرفته می‌شود و `tp`/`sl` هم از همان نقطهٔ میانی ساخته می‌شوند — **بدونِ هیچ تعدیلِ اسپرد**.

در واقعیت کاربرِ LONG با قیمتِ **ask** (میانی + ½اسپرد) وارد می‌شود و در **bid** خارج می‌شود؛
یعنی هر معامله از ابتدا ~۰.۳۳$ (طلا) در ضرر شروع می‌شود. جستجوی کلِ `web_tool/src/` نشان می‌دهد
اسپرد **فقط** در `trade_manager.ts` خطِ ۲۰۲ (`spreadEst`) و آن‌هم صرفاً برای `neutralBand`ِ تشخیصِ
معکوس‌شدن استفاده شده — **در محاسبهٔ entry/TP/SLِ هیچ لایه‌ای دخالت ندارد**.

**اثرِ عملی:**
- TPِ نمایشی به کاربر خوش‌بینانه‌تر از چیزی است که بک‌تست با آن net را حساب کرده. برای اسکالپِ
  M5 طلا با TP=۳$ (پس از رفعِ BUG-020: ۳۰$)، اسپردِ ۰.۳۳$ حدودِ ~۱٪ (یا در حالتِ باگ‌دارِ ۳$،
  ~۱۱٪) از سودِ هدف را می‌بلعد که در نمایش لحاظ نشده.
- WR واقعیِ کاربر کمی پایین‌تر از بک‌تست می‌شود، چون TP باید ½اسپرد دورتر و SL ½اسپرد نزدیک‌تر
  از مقدارِ نمایشی لمس شود.

**چرا MEDIUM (نه HIGH):** اثرش سیستماتیک اما کوچک است (مخصوصاً روی TPهای بزرگ‌ترِ HTF)، و بر
خلافِ BUG-020/029 یک واگراییِ چند-برابری نمی‌سازد. با این حال یک ناسازگاریِ واقعی با منبعِ حقیقت
است و روی اسکالپ‌ها اثرِ محسوس دارد.

**پیشنهادِ رفع:** در ساخت entry/TP/SL، ½اسپردِ per-asset (از `market_spec`/`ASSET_SPECS`) اعمال
شود: برای LONG، `entry = mid + ½spread` و برای SHORT، `entry = mid − ½spread`؛ یا دستِ‌کم در متنِ
`reason` تصریح شود که «قیمت‌ها میانی‌اند و اسپردِ ~۰.۳۳$ باید در نظر گرفته شود». هماهنگ با تک‌منبعِ
اسپرد در رفعِ BUG-031.

**وضعیت:** 🆕 ثبت‌شده — اسپرد در قیمت‌های نمایشی لحاظ نشده؛ ناسازگاریِ سیستماتیک با بک‌تست.

---

### BUG-033 — 🟡 MEDIUM — آستانهٔ کهنگیِ قیمت (۱۸۰s) ثابت برای همهٔ تایم‌فریم‌ها + سیگنالِ ENTRY با قیمتِ کهنه گیت نمی‌شود 🆕
**فایل‌ها:** `web_tool/public/static/app.js` (`applySpot`، خطِ ۱۱۵۴)، `web_tool/src/external.ts`
(کشِ ۵ دقیقه‌ایِ spot، خطوط ۹۳–۹۵)، و مسیرِ تصمیمِ `web_tool/src/index.tsx` (خطوط ۲۶۲/۶۰۳ که
`ageSec` را فقط می‌فرستند اما گیت نمی‌کنند).

**شرح:** دو نقصِ مرتبط دربارهٔ «کهنگیِ قیمت»:

1. **آستانهٔ ثابتِ ۱۸۰ ثانیه برای همهٔ تایم‌فریم‌ها:** در `applySpot` تنها هشدارِ کهنگی این است:
   ```js
   if (ageSec != null && ageSec > 180) ageEl.textContent = `(${Math.round(ageSec/60)} دقیقه تأخیر)`
   ```
   این ۱۸۰ ثانیه برای **همهٔ کارت‌ها** یکسان است. برای اسکالپِ **M1** (کندلِ ۶۰ ثانیه)، قیمتِ
   ۱۸۰ ثانیه = ۳ کندل کهنه است ولی **هیچ هشداری** نمی‌دهد؛ برای **D1** برعکس، ۱۸۰ ثانیه اصلاً
   کهنه نیست ولی ممکن است بی‌جهت هشدار دهد. همان الگوی «آستانهٔ یکسان برای همه TF» (هم‌خانوادهٔ
   BUG-026/028).

2. **سیگنالِ ENTRY با قیمتِ کهنه گیت نمی‌شود:** `external.ts` خطوط ۹۳–۹۵ قیمتِ spot طلا را تا
   **۵ دقیقه** کش می‌کند و هنگام خطای منبع، قیمتِ قدیمی را با `source+' (cache)'` برمی‌گرداند.
   مسیرِ `/api/decision` (خطوط ۲۶۲/۶۰۳) این `ageSec` را فقط **گزارش** می‌کند اما **در صدورِ
   ENTRY هیچ نقشی نمی‌دهد** — یعنی حتی با قیمتِ ۵ دقیقه کهنه، دکمهٔ «ورود به معامله» با TP/SL
   فعال می‌شود. هشدارِ کهنگی هم در مسیرِ **جداگانهٔ** `/api/spots` (نه در کارتِ سیگنال) نمایش
   داده می‌شود، پس ممکن است کاربر آن را نبیند و روی قیمتِ کهنه وارد شود.

**چرا MEDIUM:** فقط هنگامِ خطای منبعِ قیمت (fallback به کش) یا تأخیرِ شبکه فعال می‌شود، اما در آن
حالت مستقیماً کاربرِ اسکالپ را روی قیمتِ کهنه به معامله می‌برد (TP/SL بی‌ربط به قیمتِ واقعیِ لحظه).

**پیشنهادِ رفع:** ۱) آستانهٔ کهنگی per-TF شود (مثلاً `max(2×tfSeconds, 30s)`). ۲) در مسیرِ
`/api/decision`، اگر `ageSec` از آستانهٔ همان کارت بیشتر بود، وضعیت به‌جای ENTRY به «خنثی/منتظر»
با دلیلِ «قیمتِ زنده کهنه است» تنزل یابد (edge-safe). ۳) هشدارِ کهنگی روی خودِ کارتِ سیگنال (نه
فقط اسپاتِ جدا) نشان داده شود. هماهنگ با تک‌منبعِ `tfSeconds` در رفعِ BUG-026/028.

**وضعیت:** 🆕 ثبت‌شده — گیتِ کهنگیِ per-TF لازم است؛ ENTRY نباید روی قیمتِ کش‌شدهٔ کهنه صادر شود.

---

### BUG-034 — 🟠 HIGH — آستانه‌های سخت‌کدشدهٔ تشخیصِ «SL روی نقطهٔ ورود» (`0.02`/`0.05` واحدِ قیمت) در `trade_manager.ts` فرضِ «همه طلا» دارند ⇒ برای EURUSD صدها pip خطا 🆕
**فایل:** `web_tool/src/trade_manager.ts` — خطِ ۹۵ (`slAtOrBeyondEntry`) و خطِ ۲۸۰ (`slAtEntry`).

**شرح:** دو آستانهٔ سخت‌کدشده برای تشخیصِ اینکه «آیا SL روی نقطهٔ ورود (بریک‌ایون) است» وجود دارد:
```ts
// خطِ ۹۵ (داخلِ layerAwareAdvices):
const slAtOrBeyondEntry = isLong ? t.sl >= t.entry - 0.02 : t.sl <= t.entry + 0.02
// خطِ ۲۸۰ (بلوکِ عامِ بریک‌ایون):
const slAtEntry = Math.abs(t.sl - t.entry) < 0.05
```
هر دو عدد (`0.02` و `0.05`) بر حسبِ **واحدِ قیمت** هستند و فقط برای **طلا** (قیمت ~۲۶۵۰، که
۰.۰۵$ ≈ نیم‌پیپ ناچیز است) منطقی‌اند. اما `trade_manager.ts` تابعِ **generic** است و برای
**همهٔ دارایی‌ها** از جمله EURUSD صدا زده می‌شود:

- برای **EURUSD** (۱ pip = ۰.۰۰۰۱): آستانهٔ `0.05` = **۵۰۰ pip** و `0.02` = **۲۰۰ pip**.
  یعنی هر SLِ EURUSD که تا ۲۰۰–۵۰۰ pip از نقطهٔ ورود فاصله دارد، به‌غلط «روی نقطهٔ ورود / بریک‌ایون»
  تشخیص داده می‌شود. برای فارکس این عملاً یعنی «همیشه بریک‌ایون تشخیص داده می‌شود».

**اثرِ عملی:** توصیهٔ «بی‌ریسک کن (بریک‌ایون)» برای معاملاتِ EURUSD به‌غلط سرکوب می‌شود
(چون سیستم فکر می‌کند SL همین‌الان روی ورود است)، پس کاربرِ یورو هرگز پیامِ درستِ انتقالِ SL
به بریک‌ایون را نمی‌گیرد — یعنی مدیریتِ ریسکِ ناقص روی یورو.

**مسیرِ فعال‌شدن:** خطِ ۹۱ نشان می‌دهد وقتی معامله `managePlan` **ندارد** (مثلاً معاملهٔ دستیِ
کاربر یا لایه‌ای که plan تنظیم نکرده)، `handled=false` برمی‌گردد و اجرا به بلوکِ عامِ خطِ ۲۸۱
(`slAtEntry`) می‌رسد ⇒ خطِ ۲۸۰ برای یورو **زنده** اجرا می‌شود. خطِ ۹۵ نیز داخلِ همان مسیرِ
لایه‌محور برای هر لایهٔ trailingِ یورو (در صورت افزوده‌شدن) فعال است.

**ارتباط با خانوادهٔ pip:** هم‌ریشه با BUG-013/020/021/022/027 (نبودِ تک‌منبعِ‌حقیقتِ `pipSize`
per-asset) اما **خطوط و مکانیزمِ متفاوت** (تشخیصِ بریک‌ایون، نه نمایشِ pip در BUG-027 خطِ ۱۳۰،
و نه محاسبهٔ TP/SL در BUG-013). طبق قانونِ همپوشانی این یک موردِ مجزا با ارزشِ افزودنی است.

**پیشنهادِ رفع:** آستانه‌ها باید نسبت به دارایی مقیاس شوند — مثلاً `pipSize × k` یا
نسبتی از `riskDist` (مثلاً `0.02 * riskDist`) به‌جای عددِ مطلقِ واحدِ قیمت. بهترین راه‌حل
همان تک‌منبعِ `pipSize[asset]` است که کلِ خانوادهٔ pip را هم‌زمان رفع می‌کند.

**وضعیت:** 🆕 ثبت‌شده — آستانهٔ بریک‌ایونِ سخت‌کدشده؛ برای EURUSD صدها pip خطا.

---

### BUG-035 — 🟠 HIGH (ریشه‌ایِ خانوادهٔ per-asset) — `OpenTrade` فیلدِ `asset` ندارد ⇒ `trade_manager.ts` نوعِ دارایی را با heuristicِ شکنندهٔ `price >= 100` حدس می‌زند 🆕
**فایل:** `web_tool/src/trade_manager.ts` — تعریفِ `interface OpenTrade` (بدونِ فیلدِ `asset`)،
و مصرفِ heuristic در خطِ ۲۰۲ (`spreadEst`) و خطِ ۳۱۶ (`isGoldPrice`).

**شرح:** ساختارِ `OpenTrade` (معاملهٔ بازِ کاربر) فقط `side/entry/tp/sl/openedAt/barsHeld/managePlan`
دارد و **هیچ فیلدی برای شناسهٔ دارایی** (`asset`/`symbol`) ندارد. در نتیجه کلِ منطقِ مدیریتِ
معامله نمی‌داند معامله روی XAUUSD است یا EURUSD، و مجبور شده نوعِ دارایی را از **سطحِ قیمت**
حدس بزند:
```ts
const spreadEst = price >= 100 ? 0.35 : price >= 5 ? 0.03 : 0.0002   // خطِ ۲۰۲
const isGoldPrice = price >= 100                                       // خطِ ۳۱۶
```

**چرا این ریشهٔ خانوادهٔ per-asset است:** چون `asset` در دسترس نیست:
1. `pipSize` per-asset قابلِ محاسبهٔ دقیق نیست ⇒ همان زمینه‌ای که BUG-027 (نمایشِ pip ×10)
   و BUG-034 (آستانه‌های بریک‌ایونِ ۰.۰۲/۰.۰۵) را ناچار به سخت‌کدِ «فرضِ طلا» کرد.
2. `spreadEst` با حدسِ قیمت تعیین می‌شود؛ اگر روزی دارایی‌ای با قیمتِ بینِ ۵ و ۱۰۰
   (مثلاً نقره ~۳۰$ یا یک شاخص) اضافه شود، هم اسپرد و هم منطقِ `isMaShort` غلط طبقه‌بندی می‌شود.
3. ناسازگاری با بقیهٔ کد: `secondary_layers.ts` (خطِ ۷۰) و `external.ts` (خطِ ۱۶۶) نوعِ دارایی
   را **صریح** با `assetId === 'XAUUSD'` تشخیص می‌دهند — روشِ درست — اما `trade_manager.ts`
   تنها ماژولی است که به heuristicِ قیمت تکیه می‌کند.

**چرا فعلاً فاجعه نمی‌شود (اما باگِ ریشه‌ای است):** با دو داراییِ فعلی (XAUUSD ~۲۶۵۰،
EURUSD ~۱.۰۸) مرزِ `100` تصادفاً درست جدا می‌کند. اما این یک **قراردادِ ضمنیِ شکننده** است،
نه یک طراحیِ درست؛ و همین نبودِ `asset` علتِ اصلیِ ناچار شدن به ضرایب/آستانه‌های سخت‌کدِ طلا
در BUG-027/034 است.

**پیشنهادِ رفع (ریشه‌ای):** فیلدِ `asset: string` (یا `pipSize`/`spec`) به `OpenTrade` افزوده شود
و از فرانت‌اند (که کارت می‌داند روی کدام دارایی است) پر شود. سپس همهٔ heuristicهای `price >= 100`
و ضرایبِ `× 10` و آستانه‌های `0.02/0.05` با مقادیرِ per-asset از یک **تک‌منبعِ `ASSET_SPECS/pipSize`**
جایگزین شوند. این یک رفع، هم BUG-027، هم BUG-034 و هم این باگ را هم‌زمان می‌بندد.

**وضعیت:** 🆕 ثبت‌شده — ریشهٔ ساختاریِ خانوادهٔ per-asset در `trade_manager.ts` (نبودِ فیلدِ asset).

---

### BUG-036 — 🟡 MEDIUM (نهفته/شکنندگیِ طراحی) — گیتِ `isGoldM15 = assetId === 'XAUUSD'` به شناسهٔ بدون‌پسوندِ کارتِ M15 وابسته است ⇒ یک تغییرِ نامِ بی‌ضرر همهٔ لایه‌های زمان-محور را خاموش می‌کند 🆕
**فایل:** `web_tool/src/secondary_layers.ts` (خطِ ۷۰: `const isGoldM15 = assetId === 'XAUUSD'`)
در تعاملِ با `web_tool/src/index.tsx` (خطِ ۵۹۵ که `attachSecondary` را برای **همهٔ** کارت‌های طلا صدا می‌زند).

**شرح:** `attachSecondary` (index.tsx خطِ ۵۹۵) خارج از بلوکِ if/elseِ کارت‌ها و در بدنهٔ مشترکِ
`decideAsset` است، پس برای **همهٔ** کارت‌های طلا (`XAUUSD-M5/M30/H1/H4/D1` و کارتِ M15 با
`id='XAUUSD'`) اجرا می‌شود و `assetId: a.id` را می‌فرستد. در `secondary_layers.ts` لایه‌های
زمان-محورِ M15-کالیبره (Overnight S139، Monday S140، Turn-of-Month S141، SHORT-MA، Squeeze)
همگی پشتِ `isGoldM15` گیت شده‌اند که تعریفش این است:
```ts
const isGoldM15 = assetId === 'XAUUSD'   // برابریِ دقیق، نه startsWith
```

**وضعیتِ فعلی (چرا الان درست کار می‌کند):** کارتِ M15 طلا شناسه‌اش **بدون پسوند** است
(`'XAUUSD'`)، در حالی که بقیهٔ کارت‌های طلا پسوند دارند (`'XAUUSD-M5'`…). پس برابریِ دقیقِ
`=== 'XAUUSD'` تصادفاً فقط برای کارتِ M15 صادق می‌شود و لایه‌های M15-محور روی کارت‌های دیگرِ طلا
ظاهر نمی‌شوند — یعنی **رفتار فعلاً درست است** (ناسازگاریِ تایم‌فریمِ خانوادهٔ BUG-004/026/028
اینجا رخ نمی‌دهد).

**چرا باگِ نهفته/شکننده است:**
1. **نامِ گمراه‌کننده:** متغیر `isGoldM15` نام دارد اما در واقع «آیا شناسه دقیقاً `XAUUSD` است»
   را می‌سنجد، نه «آیا این کارتِ M15ِ طلاست». هر توسعه‌دهنده‌ای ممکن است این را با «همهٔ طلا» یا
   «همهٔ M15» اشتباه بگیرد.
2. **رگرسیونِ خاموش:** اگر روزی برای یکدست‌سازی، شناسهٔ کارتِ M15 از `'XAUUSD'` به `'XAUUSD-M15'`
   تغییر کند (کاملاً محتمل و به‌ظاهر بی‌ضرر)، ناگهان `isGoldM15` برای **همهٔ** کارت‌ها `false`
   می‌شود و همهٔ لایه‌های زمان-محور/ثانویه **بی‌صدا ناپدید** می‌شوند — بدون هیچ خطایی.
3. برعکس، اگر کسی گیت را به `assetId.startsWith('XAUUSD')` «اصلاح» کند تا صریح‌تر شود، آنگاه
   لایه‌های M15 روی همهٔ کارت‌های طلا ظاهر می‌شوند و ناسازگاریِ تایم‌فریم فعال می‌شود.

**پیشنهادِ رفع:** شناسهٔ کارت‌ها یکدست شود (M15 هم `'XAUUSD-M15'`)، و گیت به‌صورتِ صریح
`assetId === 'XAUUSD-M15'` با نامِ درست نوشته شود؛ یا `attachSecondary` فقط برای کارتِ M15
صدا زده شود (نه در بدنهٔ مشترک). ایده‌آل: تایم‌فریمِ کارت به‌عنوان فیلدِ صریح در `ProbeCtx`
منتقل شود تا هیچ گیتی به قرارداهایِ ضمنیِ نام‌گذاری وابسته نباشد.

**وضعیت:** 🆕 ثبت‌شده — درست‌کارکردنِ فعلی به یک قراردادِ نام‌گذاریِ ضمنی وابسته است (شکننده).

---

### BUG-037 — 🟡 MEDIUM — کارتِ H4 طلا از تجمیعِ زندهٔ Yahoo GC=F (آتی، rebase-شده) ساخته می‌شود، ولی بک‌تستِ H4 روی `data/XAUUSD_H4.csv` (spot، volumeِ واقعی) کالیبره شده ⇒ ناسازگاریِ منبع/volume 🆕

**فایل‌ها:** `web_tool/src/index.tsx` (خطِ ۴۹۴ `symbol:'GC=F'`, خطِ ۵۳۴ `GOLD_TF['XAUUSD-H4']` با `interval:'1h'` + خطِ ۵۴۰ `aggregateCandles(rawCandles,4)`) در برابر منبعِ حقیقتِ بک‌تست `data/XAUUSD_H4.csv`.

**شرح:** بررسی شد که مرزبندیِ زمانیِ تجمیع **درست** است — timestampهای `data/XAUUSD_H4.csv` دقیقاً روی ۰۰:۰۰/۰۴:۰۰/۰۸:۰۰/۱۲:۰۰/۱۶:۰۰/۲۰:۰۰ UTC می‌افتند، یعنی همان مرزِ `floor(time/14400)*14400` که `aggregateCandles` استفاده می‌کند (پس نگرانیِ «مرزِ جلسهٔ متفاوت» منتفی است ✅). اما دو تفاوتِ واقعیِ **منبع/ماهیتِ داده** باقی می‌ماند:

1. **مقیاس و منبع:** بک‌تست از `XAUUSD_H4.csv` روی مقیاسِ **spot** (قیمتِ اخیر ~۴۰۲۷، هم‌مقیاس با `XAUUSD_M15.csv`) استفاده می‌کند. سایت اما دادهٔ زندهٔ Yahoo **`GC=F` (آتیِ COMEX)** را می‌گیرد که خودِ کد می‌گوید «چند تا چند‌ده دلار بالاتر از spot» است، H1 را به H4 تجمیع می‌کند و بعد با `rebaseFuturesToSpot` (یک آفستِ **تخمینیِ** میانگین، نه تبدیلِ دقیق) به spot برمی‌گرداند. این آفستِ تخمینی می‌تواند چند دلار خطا داشته باشد.

2. **volume:** `XAUUSD_H4.csv` volumeِ واقعیِ H4 دارد (مثلاً ۱۳٬۷۸۴)، اما دادهٔ تجمیع‌شدهٔ Yahoo volumeِ آتیِ متفاوت دارد. هر لایهٔ H4 که به volume تکیه کند (VWAP، تأییدِ حجمی) روی سایت نتیجهٔ متفاوت از بک‌تست می‌دهد. (هم‌ریشه با BUG-007 که بی‌اعتباریِ volumeِ Yahoo را برای VWAP ثبت کرده بود — اما اینجا مخصوصِ کارتِ H4 و منبعِ تجمیعی است.)

**اثرِ عملی:** برای HTF با SL/TP بزرگ، آفستِ چنددلاریِ rebase کم‌اثر است، اما در آزمونِ **سیگنال** (کراسِ EMA۲۰/۵۰، لمسِ خطِ روند، RSI/ADX) می‌تواند مرزی باشد و کندلِ H4ِ سایت را نسبت به کندلِ H4ِ بک‌تست کمی جابه‌جا کند ⇒ سیگنالِ ENTRY/APPROACHING در زمانِ کمی متفاوت یا با مقادیرِ کمی متفاوت.

**چرا MEDIUM:** اثرش تدریجی و کوچک است (نه یک واگراییِ چندبرابری مثلِ BUG-020/029)، اما یک ناسازگاریِ ساختاریِ منبعِ داده با بک‌تست است که صحتِ ادعاهای H4 را رقیق می‌کند.

**پیشنهادِ رفع:** اگر داده‌ای هم‌ماهیت با `XAUUSD_H4.csv` (spot، با volumeِ درست) به‌صورتِ زنده در دسترس شود، مستقیماً استفاده شود؛ در غیرِ این‌صورت، لایه‌های H4 که به volume تکیه دارند غیرفعال یا با اخطارِ صحت علامت‌گذاری شوند، و کیفیتِ rebase (آفستِ تخمینی) در آزمونِ سیگنالِ H4 لحاظ شود. هماهنگ با رفعِ BUG-030 (gap H4) و BUG-007 (volumeِ Yahoo).

**وضعیت:** 🆕 ثبت‌شده (بازسازی‌شده پس از ریستِ سندباکس) — ناسازگاریِ منبع/ماهیتِ دادهٔ H4 با بک‌تست (مرزبندیِ زمانی اما درست است).

---

### BUG-038 — 🟡 MEDIUM — تابعِ پایهٔ `rsi()` در `indicators.ts` هنگامِ `avg_loss==0` مقدارِ `100` می‌دهد، ولی `indicators.py` مقدارِ `NaN` ⇒ واگراییِ بنیادیِ اندیکاتورِ پایه با بک‌تست 🆕

**فایل‌ها:** `web_tool/src/indicators.ts` (تابعِ `rsi`، خطوطِ ۸۳–۸۴) در برابر منبعِ حقیقتِ `engine/indicators.py` (تابعِ `rsi`، خطوطِ ۲۱–۲۴).

**شرح:** در حالتی که در پنجرهٔ محاسبه **هیچ کندلِ نزولی‌ای نبوده** (`avg_loss == 0`، یعنی روندِ صعودیِ خالص)، دو پیاده‌سازی نتیجهٔ متفاوت می‌دهند:

```python
# پایتون (منبعِ حقیقتِ بک‌تست) — خطوطِ ۲۳–۲۴:
rs = avg_gain / avg_loss.replace(0, np.nan)   # avg_loss==0 → rs = NaN
return 100 - (100 / (1 + rs))                 # → NaN
```
```ts
// TypeScript (سایت) — خطوطِ ۸۳–۸۴:
const rs = al[i] === 0 ? NaN : ag[i] / al[i]  // al==0 → rs = NaN
out[i] = isNaN(rs) ? 100 : 100 - 100 / (1 + rs)  // → 100  ← اینجا واگرا می‌شود!
```

پایتون در این حالت **`NaN`** برمی‌گرداند اما TypeScript **`100`**. این یک تفاوتِ رفتاریِ بنیادی در یک تابعِ پایه است که همهٔ لایه‌های وابسته به RSI (S138 squeeze، confirmation_filter، gold_htf_router، و…) از آن تغذیه می‌کنند.

**تحلیلِ اثرِ فعلی (چرا هنوز MEDIUM نه CRITICAL):** بررسی شد که در دو مصرف‌کنندهٔ اصلی، این واگرایی **تصادفاً** خنثی می‌شود:
- `squeeze_breakout.ts` خطِ ۱۸۱: `notOverbought = !isFinite(rsi14) || rsi14 <= 75`. با `rsi=100` (TS) ⇒ `notOverbought=false` (بلاک). سمتِ پایتونِ s135 هم `np.nan <= 75 = False` ⇒ بلاک. **هر دو بلاک می‌کنند** (نتیجه یکسان، مسیرِ متفاوت).
- `confirmation_filter.ts` خطِ ۹۴: `Number.isFinite(rsi14) && rsi14>=35 && rsi14<=70`. با `rsi=100` (TS) ⇒ `100<=70=false` ⇒ `met=false`؛ سمتِ پایتون `NaN` ⇒ `isfinite=false` ⇒ `met=false`. **هر دو false** (یکسان).

**چرا با این حال باگ است و باید رفع شود:** خنثی‌شدنِ فعلی یک **تصادف** است، نه طراحی. هر مصرف‌کنندهٔ آینده یا موجودی که RSI را با آستانه‌ای **غیرِ overbought** بسنجد (مثلاً «RSI > 50 برای تأییدِ روندِ صعودی»، یا میانگین‌گیری/zscore روی RSI) بلافاصله واگرا می‌شود: پایتون NaN را از میانگین حذف/رد می‌کند اما TS عددِ ۱۰۰ را وارد محاسبه می‌کند ⇒ سوگیریِ رو به بالا. این دقیقاً در **صعودهای خالصِ قوی** (بهترین ستاپ‌های مومنتوم) رخ می‌دهد.

**پیشنهادِ رفع:** خطِ ۸۴ به `out[i] = isNaN(rs) ? NaN : 100 - 100/(1+rs)` تغییر کند تا دقیقاً منطبق با pandas (`NaN`) شود؛ و مصرف‌کننده‌ها همگی `isFinite`-guard داشته باشند (که squeeze و confirmation_filter الان دارند). این تطبیقِ بنیادی از واگرایی‌های آیندهٔ خاموش جلوگیری می‌کند.

**✅ شاهدِ عددیِ قطعی (cross-check روی هر دو موتور):** روی سریِ صعودِ خالصِ `[100,101,…,116]` (بدونِ هیچ کندلِ نزولی):
- `PY_RSI (pure uptrend) last = nan`
- `TS_RSI (pure uptrend) last = 100`

و برای اطمینان، روی ۳۰۰ کندلِ واقعیِ `XAUUSD_M15` (حالتِ عادی، `avg_loss>0`) هر دو موتور **دقیقاً یکسان** بودند: `ATR=9.237336` (PY=TS)، `RSI=68.351144` (PY=TS). یعنی واگرایی **فقط** در حالتِ لبه‌ایِ `avg_loss==0` رخ می‌دهد و بقیهٔ محاسبهٔ RSI/ATR منطبق است.

**وضعیت:** 🆕 ثبت‌شده — واگراییِ تابعِ پایهٔ RSI؛ اثرِ فعلی تصادفاً خنثی اما تلهٔ آیندهٔ فعال. (با شاهدِ عددیِ cross-check تأیید شد.)

---

### BUG-039 — 🟠 HIGH — `analyze()` در `signal.ts` مقدارِ RSI را **بدونِ `isFinite`-guard** مصرف می‌کند ⇒ باگِ RSI=100 (BUG-038) اینجا نه‌تنها خنثی نمی‌شود، بلکه در صعودِ خالص جریمهٔ ۰.۳۰- می‌زند 🆕

**فایل:** `web_tool/src/signal.ts` — تابعِ `analyze`، خطوطِ ۱۵۶–۱۶۱ (و به‌همان‌شکل بدونِ guard: `a=adxArr[i]` خطِ ۱۶۴، `mh=macdHist[i]` خطِ ۱۷۱).

**شرح:** برخلافِ `squeeze_breakout.ts` و `confirmation_filter.ts` که برای RSI یک `isFinite`-guard دارند (و به‌همین‌دلیل باگِ BUG-038 در آن‌ها تصادفاً خنثی می‌شد)، تابعِ `analyze` مقدارِ RSI را **مستقیم و بدونِ guard** به امتیازدهی می‌دهد:

```ts
const r = rsi14[i]
let rsiContrib = 0
if (r >= 45 && r <= 65) rsiContrib = 0.25
else if (r > 75) rsiContrib = -0.30      // ← اینجا فعال می‌شود
else if (r < 35) rsiContrib = -0.10
add('rsi_14', r, rsiContrib, 'مومنتوم RSI')
```

در حالتِ صعودِ خالص (`avg_loss==0`):
- **منطقِ سازگار با پایتون (RSI=NaN):** همهٔ مقایسه‌ها `false` ⇒ `rsiContrib = 0` (خنثی).
- **موتورِ فعلیِ سایت (RSI=100 طبقِ BUG-038):** `100 > 75 = true` ⇒ `rsiContrib = **-0.30**` (جریمهٔ سنگین).

**اثرِ عملی:** دقیقاً در یک **روندِ صعودیِ قویِ خالص** (که `regimeOk=true` و بهترین لحظهٔ LONG است)، امتیازِ کل ۰.۳۰ واحد کم می‌شود. با توجه به وزن‌های دیگر (regime=۰.۵۵، vwap=۰.۳۵، macd_trigger=۰.۳۰)، این جریمهٔ ۰.۳۰- می‌تواند `score` را از آستانهٔ سیگموئیدِ ورود بیندازد و **سیگنالِ LONG را در بهترین ستاپ سرکوب کند** — یعنی دقیقاً عکسِ چیزی که مطلوب است.

**ارتباط با BUG-038:** این همان «تلهٔ آینده»ای است که در BUG-038 هشدار داده شد، و اینجا **در کدِ فعلی زنده است** (نه فرضی). BUG-038 ریشه (تابعِ پایه) را ثبت کرد؛ این مدخل مصرف‌کنندهٔ **بدونِ guard** و اثرِ جهت‌دارِ آن را مستند می‌کند. رفعِ ریشه‌ایِ BUG-038 (بازگرداندنِ `NaN`) این را نیز حل می‌کند، اما تا آن زمان `analyze` باید `Number.isFinite(r)` را قبل از شاخهٔ `r > 75` بگذارد (مثلِ دو مصرف‌کنندهٔ دیگر).

**نکتهٔ صحت‌سنجی:** خودِ `analyze` طبقِ کامنتِ خطوطِ ۵–۱۰ یک «تقریبِ شفاف» از مدلِ ONNXِ S14 است، نه بازتولیدِ دقیقِ بک‌تست؛ اما این ناسازگاریِ **درون‌سیستمی** (استفاده از تابعِ RSIِ خودِ پروژه با رفتارِ ۱۰۰ به‌جای NaN) صرف‌نظر از آن، یک منطقِ نادرست است.

**وضعیت:** 🆕 ثبت‌شده — مصرف‌کنندهٔ بدونِ guardِ BUG-038؛ در صعودِ خالص جریمهٔ ۰.۳۰- و سرکوبِ سیگنالِ LONG.

---

### BUG-040 — 🟠 HIGH — `anchoredVWAP` هنگامِ `cumV==0` مقدارِ `close` می‌دهد ولی `features.py` مقدارِ `NaN` ⇒ برای EURUSD (که Yahoo volume=0 می‌دهد) واگراییِ ۰.۵۰ امتیازیِ VWAP در `analyze` 🆕

**فایل‌ها:** `web_tool/src/signal.ts` (`anchoredVWAP` خطِ ۸۰، و مصرف در `analyze` خطوطِ ۱۳۹–۱۴۶) در برابر منبعِ حقیقتِ `engine/features.py` (خطوطِ ۱۰۸–۱۱۸). زمینه: `web_tool/src/external.ts` خطِ ۱۱۹ (`volume: q.volume?.[i] ?? 0`).

**شرح:** در `features.py` خطِ ۱۱۳ چنین است: `cum_v = vol.groupby(date).cumsum().replace(0, np.nan)` ⇒ وقتی حجمِ تجمعیِ روز صفر باشد، `vwap = pv/NaN = **NaN**`. اما در `signal.ts` خطِ ۸۰:
```ts
out[i] = cumV > 0 ? cumPV / cumV : c[i].close   // ← وقتی cumV==0، close (نه NaN)
```

**چرا این برای EURUSD زنده فعال می‌شود:** خطِ ۱۱۹ `external.ts` می‌گوید `volume: q.volume?.[i] ?? 0`. برای نمادِ فارکسِ `EURUSD=X`، Yahoo معمولاً **volume نمی‌دهد** ⇒ همهٔ کندل‌ها `volume=0` ⇒ `cumV` همیشه صفر می‌ماند ⇒ کلِ سریِ VWAP روی مسیرِ `else` (`close`) می‌افتد. (دادهٔ تاریخیِ CSV این مشکل را ندارد: صفرِ حجم = ۰ از ۲۰۰٬۰۰۰ ردیف؛ پس فقط **زنده** رخ می‌دهد — دقیقاً جایی که کاربر تعامل دارد.)

**اثرِ کمّی در `analyze` (خطوطِ ۱۳۹–۱۴۶):**
```ts
const vwapDistAtr = atr ? (price - vwap) / atr : 0
if (vwapDistAtr >= -0.5 && vwapDistAtr <= 1.0) vwapContrib = 0.35
...
else vwapContrib = -0.15
```
- **سازگار با پایتون (vwap=NaN):** `vwapDistAtr=NaN` ⇒ همهٔ شاخه‌ها false ⇒ `vwapContrib = -0.15`.
- **موتورِ فعلیِ سایت (vwap=close):** در آخرین کندل `price≈close` ⇒ `vwapDistAtr≈0` ⇒ شاخهٔ اول ⇒ `vwapContrib = **+0.35**`.

اختلاف = **۰.۵۰ امتیاز** (از ۰.۱۵- به ۰.۳۵+)، آن‌هم یک سوگیریِ **مثبتِ کاذب**: سایت به‌غلط فکر می‌کند قیمت «دقیقاً روی VWAP در ناحیهٔ سالمِ پول‌بک» است، در حالی که واقعاً VWAP نامعتبر است. این می‌تواند سیگنالِ LONGِ کاذب روی کارت‌های EURUSD (و هر کارتی که `analyze` را با دادهٔ بی‌حجم اجرا کند) بسازد.

**ارتباط با پرتفوی/باگ‌های دیگر:** هم‌خانواده با BUG-007 (بی‌اعتباریِ volumeِ Yahoo برای VWAP) و BUG-037 (منبعِ داده)، اما اینجا **مکانیزمِ متفاوت**: نه بی‌اعتباریِ مقدارِ volume، بلکه **رفتارِ `close` به‌جای `NaN` در حالتِ `cumV==0`** که یک سوگیریِ مثبتِ جهت‌دار تولید می‌کند. طبق قانونِ همپوشانی، موردِ مجزا با ارزشِ افزودنی.

**پیشنهادِ رفع:** خطِ ۸۰ به `out[i] = cumV > 0 ? cumPV / cumV : NaN` تغییر کند (منطبق با pandas)، و `analyze` قبل از استفاده از `vwapDistAtr` یک `Number.isFinite`-guard بگذارد تا در نبودِ volume، سهمِ VWAP **خنثی (۰)** شود، نه ۰.۳۵+ یا ۰.۱۵-. ایده‌آل: برای داراییِ بدونِ volume (فارکس)، VWAP از سبدِ feature حذف و وزنش بازتوزیع شود.

**وضعیت:** 🆕 ثبت‌شده — واگراییِ VWAP در `cumV==0`؛ روی EURUSD زنده سوگیریِ مثبتِ ۰.۵۰ امتیازی.

---

### BUG-041 — 🟠 HIGH — در رژیمِ نزولی، کارت «احتمالِ مدل: X٪» بالا (تا ۷۸٪) و `confidence='high'` نشان می‌دهد ولی `direction=NONE` ⇒ تناقضِ «اطمینانِ بالا بدونِ هیچ سیگنال» برای کاربر 🆕

**فایل‌ها:** `web_tool/src/signal.ts` (خطوطِ ۲۸۳ `shownProbability`، ۲۸۶–۲۸۷ `confidence`، ۳۳۵ `probability: shownProbability`) + `web_tool/public/static/app.js` (خطِ ۵۷۷ `احتمالِ مدل: ${d.probability}%`).

**شرح:** وقتی رژیمِ نزولی تشخیص داده می‌شود (`bearRegimeOk`)، کد `direction='NONE'` می‌گذارد (SHORT عمداً غیرفعال است — S83/S88، درست). اما احتمالِ نمایشی از `bearProbability` گرفته می‌شود:

```ts
const shownProbability = activeBrain === 'bear' ? bearProbability : probability   // خطِ ۲۸۳
...
if (shownProbability >= 66) confidence = 'high'                                   // خطِ ۲۸۶
else if (shownProbability >= 60) confidence = 'medium'
...
return { ..., direction /* =NONE */, probability: Number(shownProbability.toFixed(1)), confidence, ... }  // خطِ ۳۳۵
```

و فرانت‌اند (app.js خطِ ۵۷۷) این را مستقیم نشان می‌دهد: `احتمالِ مدل: ${d.probability}%`.

**نتیجهٔ متناقض:** در یک رژیمِ نزولیِ قوی، `bearProbability` می‌تواند تا **۷۸٪** برسد و `confidence='high'` شود، در حالی که `direction=NONE` است. یعنی کاربر یک کارت با **«احتمالِ مدل ۷۸٪ / اطمینانِ بالا»** می‌بیند اما **هیچ سیگنالی** به او داده نمی‌شود و متنِ `noEntryReason` می‌گوید «SHORT نمی‌دهیم». پرسشِ طبیعیِ کاربر: «۷۸٪ احتمالِ چه چیزی؟ اگر انقدر مطمئنی چرا معامله نمی‌دهی؟»

**بدتر (رفتارِ معکوس):** هرچه رژیمِ نزولی **قوی‌تر** باشد (`bearScore` بالاتر)، عددِ احتمالِ نمایشی **بالاتر** می‌رود — یعنی دقیقاً وقتی سایت مصرّانه می‌گوید «short نکن»، بزرگ‌ترین عددِ احتمال را نشان می‌دهد. این ضدشهودی و گمراه‌کننده است.

**تضاد با رفعِ قبلی:** کامنتِ خطوطِ ۲۰۶–۲۰۹ صراحتاً ادعا می‌کند «تصمیم دقیقاً بر پایهٔ همان probabilityِ نمایش‌داده‌شده است تا درصد و سیگنال هرگز متناقض نباشند» — اما این ضمانت فقط برای مسیرِ **bull** برقرار است. در مسیرِ **bear** نقض می‌شود: probabilityِ نمایشی بالا، ولی تصمیم همیشه NONE.

**نقضِ قانونِ طراحیِ سایت:** طبق تعریفِ سایت، حالتِ «خنثی» باید صراحتاً بگوید چرا وارد نمی‌شود؛ نمایشِ «احتمالِ ۷۸٪ + اطمینانِ بالا» در حالتی که عملاً خنثی است، «اطلاعاتِ گمراه‌کننده» است.

**پیشنهادِ رفع:** در حالتِ `bear` (که `direction=NONE`)، یا `probability` نمایشی صفر/نامرتبط اعلام شود و به‌جای درصد، فقط دلیلِ عدمِ‌ورود نمایش داده شود؛ یا `confidence` به `low` قفل شود و لیبلِ درصد به «احتمالِ رژیمِ نزولی (بدونِ سیگنال)» تغییر کند تا با `direction=NONE` سازگار باشد. اصولی‌ترین کار: وقتی `direction==='NONE'`، فیلدِ `probability` اصلاً به‌عنوان «احتمالِ مدل/سیگنال» به UI نرود.

**وضعیت:** 🆕 ثبت‌شده — تناقضِ احتمالِ بالا/اطمینانِ بالا با سیگنالِ NONE در رژیمِ نزولی.

---

### BUG-042 — 🟡 MEDIUM — پارامترهای سطوحِ S/R در سایت (`left/right=5`, `tol=0.0012`, `maxLevels=60`) با بک‌تستِ پایتون (`left/right=6`, `tol=0.0008`, `max_levels=40`) ناسازگارند ⇒ سطوحِ حمایت/مقاومتِ متفاوت از تحقیق 🆕

**فایل‌ها:** `web_tool/src/signal.ts` (خطوطِ ۲۹۰–۲۹۱: `findPivots(c, 5, 5)` و `activeLevels(c, pivots, 0.0012, 60, 1500)`) در برابر منبعِ حقیقتِ `engine/structure.py` (`sr_levels` پیش‌فرض `tol=0.0008, max_levels=40`) و استراتژی‌های S/R پایتون (`s10/s11/s12_*.py` که همگی `left=6, right=6, tol=0.0008` می‌گذارند).

**شرح:** موتورِ ساختارِ TS (`structure.ts`) بازتولیدِ **درستِ** منطقِ `engine/structure.py` است (بررسی شد: ادغامِ cross-kind، طبقه‌بندیِ نهایی بر اساسِ موقعیت نه kind، و منطقِ انقضا همگی منطبق‌اند ✅ — اجتناب از باگِ کاذب). اما **پارامترهایی که سایت به این توابع می‌دهد** با بک‌تست فرق دارد:

| پارامتر | بک‌تستِ پایتون (S10/S11/S12) | سایت (`analyze`) | اثر |
|---|---|---|---|
| `left/right` (pivot) | **۶ / ۶** | **۵ / ۵** | سایت pivotهای بیشتر و حساس‌تر ⇒ سطوحِ ضعیف‌ترِ اضافی |
| `tol` (ادغام) | **۰.۰۰۰۸** | **۰.۰۰۱۲** (۱.۵×) | سایت سطوحِ نزدیک را تهاجمی‌تر ادغام ⇒ سطوحِ پهن‌تر/کمتر |
| `max_levels` | **۴۰** | **۶۰** | سایت سطوحِ بیشتری نگه می‌دارد |

**اثرِ عملی:** سطوحِ حمایت/مقاومتی که سایت محاسبه می‌کند، به کاربر نمایش می‌دهد، و در `breakoutScenarios` (احتمالِ شکستِ سطح) و `nearestSR` استفاده می‌کند، با سطوحی که در تحقیق/بک‌تست کالیبره و اعتبارسنجی شده‌اند **یکسان نیستند**. یعنی «مقاومتِ X با N برخورد» که سایت نشان می‌دهد ممکن است در بک‌تست اصلاً یک سطحِ مجزا نباشد (یا با touchesِ متفاوت). این صحتِ سناریوهای شکست و انتخابِ نزدیک‌ترین S/R را رقیق می‌کند.

**چرا MEDIUM:** این سطوح در `analyze` (موتورِ «تقریبِ شفافِ» S14) مصرف می‌شوند که خودش سیگنالِ اصلیِ Entry را از رژیم/MACD می‌گیرد، نه صرفاً از S/R؛ پس اثرِ مستقیمِ آن روی تصمیمِ Long/Short محدود است. اما نمایشِ سطوح و سناریوهای شکست مستقیماً به کاربر می‌رسد و باید با تحقیق هم‌تراز باشد.

**پیشنهادِ رفع:** پارامترهای سایت با بک‌تست هم‌تراز شوند: `findPivots(c, 6, 6)`, `activeLevels(c, pivots, 0.0008, 40, 1500)`. اگر انحراف عمدی بوده (مثلاً برای نمایشِ سطوحِ بیشتر در UI)، باید مستند و توجیه شود؛ در غیرِ این‌صورت، هم‌ترازی با `structure.py` لازم است.

**وضعیت:** 🆕 ثبت‌شده (بازسازی‌شده پس از ریستِ سندباکس) — ناسازگاریِ پارامترهای S/R سایت با بک‌تست (خودِ الگوریتم درست بازتولید شده).
